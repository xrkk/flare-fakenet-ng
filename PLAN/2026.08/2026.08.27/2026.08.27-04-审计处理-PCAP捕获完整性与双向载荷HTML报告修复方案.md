# 审计处理: PCAP 捕获完整性与双向载荷 HTML 报告修复方案

会话角色: 作者/执行方  
处理日期: 2026-08-27  
处理轮次: 第 1 轮  
审核性质: 首次第三方全面审核结果引入  
处理结论: 首次审核后的作者自评闭环；无需再次第三方审核  
实施授权: 未授予

## 1. 身份与门禁

| 字段 | 身份或结论 |
|---|---|
| 原审计记录 | `/home/adminn/projects/flare-fakenet-ng/PLAN/2026.08.27/2026.08.27-03-审计-PCAP捕获完整性与双向载荷HTML报告修复方案.md` |
| 原审计记录 Git blob / SHA-256 | `0d8b844ad32c3feb551c8399ea6ba28f693a37d7` / `bc243725d2366bf6c815464c88bc10e956dbfe57b45fe104a07573cd87090257` |
| 被审核方案 | `/home/adminn/projects/flare-fakenet-ng/PLAN/2026.08.27/2026.08.27-01-PCAP捕获完整性与双向载荷HTML报告修复方案.md` |
| 处理前方案身份 | v0.1；`未审核, 不得实施`；Git blob `1e8d85867d0ccd4542c7eb168e7b7c08af03ce08`；SHA-256 `84323005f3f35bd8be4ab714ba7b510cd80f5ac898c040368c1773a16cf90663` |
| 处理后方案身份 | v0.2；`已审核, 可以实施`；Git blob `0bd8aea9a56d97f165b05e5fc6a68b08f06b4d20`；SHA-256 `b31091d2c4bde1ea946759e41be5f8b6f9a0858e9c2d227c80b03631c500cb4d` |
| 审计结论可引入性 | 通过。原审计结论为“存在不同意见”，非审计阻断或持久化阻断；审计身份与 v0.1 精确匹配 |
| 规范性来源复核 | `SRC-REQ-001`～`004`、`SRC-USER-001/002` 及其 BASE/TECH/CONV 依赖的稳定 ID、引用集合和规范含义均未改变 |
| 历史处理链 | 本方案首次审计的首次作者引入；此前没有审计处理记录 |
| 实施记录与授权 | 没有与本方案链匹配的实施记录；当前会话没有本方案实施提示词及门禁授权，因此 `实施授权: 未授予` |

原审计记录创建后保持不可变；本轮没有修改它。

## 2. 本地基线、工作区与漂移

- 审计本地基线：分支 `process-redirect-v23`，HEAD `feb39c575e2e74d057bde577573dc92a918a47e7`。
- 引入时本地基线：同一分支、同一 HEAD，没有提交级漂移。
- staged：无。
- 边界内工作区：只新增/修改本方案链文档；没有生产代码、测试、构建脚本或配置改动。
- 排除范围：用户原有 `PLAN/FaknetNG.md` 未提交修改仍存在，本轮只读复核其稳定行，未修改、暂存或覆盖。

决策关键路径逐项重算 Git blob，与原审计记录完全一致：

| 路径 | Git blob | 漂移结论 |
|---|---|---|
| `fakenet/gui/launcher.py` | `e06f849010b1522d9b621ae431882388ee26511f` | 无漂移 |
| `fakenet/fakenet.py` | `7193c01fabe43a5f1c08b46f4b4dadde76ee0a00` | 无漂移 |
| `fakenet/diverters/windows.py` | `f54d9c06d85adaba2cc50f38b1b1d9b47a9198f4` | 无漂移 |
| `fakenet/diverters/diverterbase.py` | `fda82ad4953d3e49ae64c2865edaf6a4a3f4255d` | 无漂移 |
| `fakenet/diverters/pcapwriter.py` | `d4c6fbdcd0ed43a34e1d80cee0dfb44589e41743` | 无漂移 |
| `fakenet/listeners/RawListener.py` | `31018afdf7cf2445495c2bd186c8dee68511e06f` | 无漂移 |
| `fakenet/configs/html_report_template.html` | `f97507ddda21f7890a67c9aed64fb5b0772a8572` | 无漂移 |

漂移总判定：决策关键路径无变化，原审计的代码、接口、数据、行为、约束和验收依据均有效。

## 3. 逐条处理

### `AUD-001`: 同意

- 原意见：`SRC-BASE-001` 只在来源定义中出现，没有出向挂接，来源到实施/验收的形式追溯不完整。
- 判断：同意。这是当前方案实施契约的追溯缺口，不是上层需求变化，也不是新的实现机制。
- 依据：`SRC-BASE-001` 明确说明既有 I7 只证明双 writer 成对健康，不能证明捕获覆盖完整；其规范含义直接支撑 `ROOT-002`、`IMP-002` 和 `ACC-002`。
- 实际修改：方案 v0.2 的 `IMP-002` 来源字段、`ACC-002` 来源字段和 §6 第一行均加入 `SRC-BASE-001`。
- 最小闭合条件：满足。稳定来源 ID 已接入根因、实施和验收链，规范含义未改变。
- 受影响既有通过项：只影响“来源到 IMP/ACC 双向追溯”的形式结论；`ROOT-002`、修复方向、风险和验收语义不变。
- 复审请求：无。修改可机械核对，且没有改变任何产品或实现方向。

### `AUD-002`: 同意

- 原意见：`ACC-002` 要求证明加载仓库 pydivert 2.1.0，但实际命令缺少直接身份探针，现有证据只能间接推断。
- 判断：同意。这是验收证据合同缺口；增加只读探针直达声明，不改变测试内容或产品行为。
- 依据：引入前使用方案相同的临时 wheel 覆盖层实际运行拟加入命令，exit 0，stdout 为 `2.1.0 z:/pydivert21/pydivert/__init__.py`。探针同时断言版本精确等于 `2.1.0`，规范化加载路径以 `z:/pydivert21/` 开头。
- 实际修改：方案 v0.2 的 §2.4 与 `ACC-002` 命令在 pytest 前加入已验证身份探针；`ACC-002` 证据栏加入探针 stdout 的机械通过边界。
- 最小闭合条件：满足。版本与加载路径都由同一固定 Docker/Wine 环境直接证明；非零退出会阻止后续验收成功。
- 受影响既有通过项：只影响 `ACC-002` 的依赖身份可复核性；`EVD-SPIKE-002`、71 项聚焦测试及 fail-closed 语义不变。
- 复审请求：无。命令已由作者独立实测，修改不引入新的实现方向。

没有不接受、部分接受或未解决的 `AUD-xxx`；没有同一根因复开，不触发审核循环熔断。

## 4. 实际 diff 与影响映射

### 4.1 方案实际修改清单

1. 方案版本 v0.1 升至 v0.2，状态由 `未审核, 不得实施` 改为 `已审核, 可以实施`，头部记录首次审核闭环身份。
2. `IMP-002`、`ACC-002` 和 §6 第一行新增 `SRC-BASE-001` 引用；没有新增、删除或重编号 IMP/ACC。
3. §2.4 和 `ACC-002` 各加入同一条 pydivert 版本/加载路径身份探针；`ACC-002` 证据栏固定 stdout 边界。
4. §10.2 写入作者引入结论；§11 新增 v0.2 版本记录。

### 4.2 受影响的既有通过项

- 来源切片到 `IMP-002/ACC-002` 的双向追溯：由形式缺口变为完整。
- `ACC-002` 的依赖身份可机械复核性：由间接推断变为命令内直接断言。
- 方案身份和状态：因首次审计意见全部闭合并完成作者复查，更新为 v0.2、`已审核, 可以实施`。

### 4.3 未受影响继承项

- 原审计 §2 的全部来源、运行证据和 Spike 独立复核结论。
- 原审计 §3 的 `ROOT-001`～`003`、`DEC-001`～`013`、直接修复、同源场景、可交接实施性和 fallback 结论。
- 原审计 §4 的 ID 唯一性、其余追溯、未建工具合同与硬失败边界。
- 原审计 §6 对方案九项审核重点的通过结论。

### 4.4 全局不变量

- `SRC-USER-001` 的完整双向 TCP/UDP payload 合同和 `SRC-USER-002` 的 fail-closed 合同不变。
- 事实产出位置、封口 raw PCAP 数据流、会话流归属、logical packet 观察索引和 HTML 单一 Base64 字节源不变。
- 主/入站 queue 固定值、精确回读、gap 边界、fatal 无成功报告和 pktmon 三方验收不变。
- v34 的 Ubuntu 真终点、停止时限、网络恢复和既有 A/P/停止门禁不变。
- `IMP-001`～`005`、`ACC-001`～`008`、`FB-001`～`005`、`DEC-001`～`013` 均保持稳定。

影响分析结论：修改只涉及当前方案的追溯和验收证据表达，影响范围可完全界定；没有规范性来源含义、范围、架构/根因、权限归属、依赖图、迁移或切换策略变化。

## 5. 作者引入后的完整复查

| 复查项 | 结论与证据 |
|---|---|
| 现象、触发、预期、影响和复现 | 通过；三件固定实机证据及审计独立重算结论未变化 |
| 根因证据链和合理替代根因排除 | 通过；决策关键代码无漂移，原审计对 write/log/send seam、writer 语义和 NBI-only 报告路径的独立核对继续有效 |
| 问题、根因、IMP、回归和 ACC 双向追溯 | 通过；`SRC-BASE-001` 已接入 `IMP-002/ACC-002` 与 §6；全部稳定 ID 唯一且无进度状态 |
| 直接修复 | 通过；仍是 no-console、coverage fail-closed、封口 raw PCAP 唯一事实源和安全完整 HTML，没有外围补丁或无依据重试 |
| 同源正常、异常、边界和旧路径退出 | 通过；原审计确认的矩阵与 fallback/硬失败边界未改 |
| 可交接实施性和决策闭合 | 通过；新增身份探针已实际运行，现有命令可直接执行，未建工具合同不变 |
| ACC 契约 | 通过；`ACC-002` 现在直接证明 wheel 版本和加载路径，其余 ACC 的前置、输入、前后结果、证据和 PASS/FAIL 边界不变 |
| 回归、兼容性、数据、可观察性和现实阻碍 | 通过；Docker/Wine、全仓、正式包、真实 VM 与三方 pktmon 层级不变，Wine 不替代 VM |
| fallback | 通过；`FB-001`～`005` 仍全部必要且不替代根因修复 |

修改后的完整方案业务复查通过，没有未解决的中/高风险意见或证据争议。

## 6. 再次第三方审核逐项判定

| 触发条件 | 判定 | 证据 |
|---|---|---|
| 跨两个以上独立项目并改变跨项目契约，或修改多业务共享契约 | 未命中 | 仅修改本仓库单一方案文档的来源引用和验收探针；无外部或共享契约变化 |
| 不可逆/高风险迁移、权限、安全、隐私或外部关键契约 | 未命中 | 没有数据、权限、生产配置或外部系统修改；探针只读且在临时覆盖层运行 |
| 总体架构、核心数据流、并发/一致性、恢复或切换策略 | 未命中 | 方案的全部 DEC、ROOT、事实源、状态归属、恢复与切换门禁原样保留 |
| 核心主路径且回滚困难或影响面无法限定 | 未命中 | 没有代码实施；两个文档修改点可逐行机械定位并独立回退，影响只限追溯和 ACC 身份证据 |
| 修改后仍有多个有证据的可行方向 | 未命中 | 两个 AUD 各有唯一最小闭合方式，身份探针已实测通过，没有方向分叉 |
| 用户或生效项目约定明确要求再次审核 | 未命中 | 用户要求按触发条件判断；`AGENTS.md` 没有要求这种低风险审计引入必须再次三方审核 |

结论：无需再次第三方审核。本记录同时构成 `首次审核后的作者自评闭环`，不提出复审请求。

## 7. 最终状态与下一动作

- 最终方案：v0.2；Git blob `0bd8aea9a56d97f165b05e5fc6a68b08f06b4d20`；状态 `已审核, 可以实施`。
- 审计意见：`AUD-001/002` 全部同意并闭合。
- 实施授权：未授予。首次审核引入和方案状态不能代替用户粘贴实施提示词。
- 下一动作：等待用户粘贴实施提示词；本轮不实施代码、不创建实施记录、不提交。
