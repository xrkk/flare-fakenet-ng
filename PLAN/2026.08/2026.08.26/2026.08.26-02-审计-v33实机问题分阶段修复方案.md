# 审计记录: v33实机问题分阶段修复方案

| 项 | 值 |
|---|---|
| 审计轮次 | 第 1 轮（首次第三方审计，无前轮审计记录） |
| 审计模式 | 全面审核（机械扫描 + 全部评审项语义审查 + 反例检查） |
| 审计日期 | 2026-08-26（Asia/Shanghai） |
| 审计环境 | 本地审核（直接访问本地仓库、本地 dist 证据与运行态文件；未使用任何作者自评对话或交接上下文） |
| 方案路径 | `PLAN/2026.08.26/2026.08.26-01-v33实机问题分阶段修复方案.md` |
| 方案版本 | v0.6 |
| 方案 blob（工作区） | `3e8e0143c3e905184636c20eb88049453f23af05` |
| 方案 SHA-256 | `85ad4b217cb71e273cfd25c7e9f3c2f7a627bf2fcb74b14ff5f4ada9f5f381c3` |
| 方案状态（审计前） | `未审核, 不得实施`（符合审计门禁） |
| 代码基线 | `411942fd6600b0388070f6e1268e8e0c149f708b`，经核对该值即当前 HEAD |
| 云端身份 | 无 audit_commit_sha（方案 §8.4 判定 `不可云端审核`，经复核成立：关键实机证据在本地未提交的 `dist/` 下且含敏感运行信息；`dist/` 在 `.gitignore` 第 17/103-105 行） |
| 结论 | **存在不同意见, 下一审核模式: 定向复审**（AUD-001 ~ AUD-005；均不推翻根因链、修复合同或验收契约的方向性正确性） |

## 1. 问题与需求来源及根因证据摘要

- 规范性来源 `PLAN/FaknetNG.md`：`> 2026.08.18` 恶意软件场景第 2 项、`> 2026.08.21` 第 2 项、`> 2026.08.26` 第 1–7 项均已在当前工作区文件中逐条核对存在且规范含义与 SRC-REQ-001~004 采用含义一致。其中 08.26 第 7 项（停止 1.0s/5.0s/3 轮合同）为方案 v0.5 驱动持久化，HEAD→工作区差异仅此一处新增。
- SRC-OLD-001（`PLAN/2026.08.21/2026.08.21-01-实机反馈六项修复方案.md` v0.2）：blob `1f12f4ab7b269f379e9319746376e5ebf84ebbff`、SHA-256 `f0de1a6e2a1886605f8ed62b1b4b0e6b8f3fbeb80753305c7c26ce9d78067582` 与工作区完全一致；其 §2.6/I5 明确记载 v33 "删除 `_handle_policy_packet` 中 `matches_takeover_sink` 命中即 ALLOW 重注入的整个分支"，证实 CODE-SINK-003 所述"有意删除、非偶发遗漏"。
- SRC-OPS-001（`test/gui_vm/README.md`）：blob `bc3a47a1bd3b16d1b69bf0ca655b1898ca26154c`、SHA-256 `2fa6e048171539dd2284c72c4643fedde2ff73dc7243f2aa8d45afc432f7bf69` 与工作区完全一致。
- 代码证据 9 个文件 SHA-256 逐一复算，全部与方案 §1.3 引用一致（HTTPListener.py、gui/app.py、gui/launcher.py、fakenet.spec、Build-GuiVmPackage.ps1、build.yaml、egresspolicy.py、windows.py、test_sink_divert_contract.py）。
- DEP-PYI-001/002：从 PyInstaller 上游 tag `v6.22.0`（commit `70fc17210920bce17f4ab09bbf8104b0dbd45338`）取 `bootloader/src/pyi_main.c`（SHA-256 `4b69f7fb...` 核对一致）与 `bootloader/src/pyi_utils_win32.c`（SHA-256 `07980fae...` 核对一致）；`pyi_win32_mitigate_locked_temporary_directory`（pyi_utils_win32.c:1175-1181）确为 `max_attempts=15`、`delay=1000ms`。
- EV-D3 全部 17 个文件按导出目录 `evidence-sha256.tsv` 逐项重算，17/17 通过；diagnostic-03 包 SHA-256 `852dc776...` 复核一致；`/tmp/fakenet_stop_design_probe.py`（EV-HOST-STOP-DESIGN）SHA-256 `37fb1b04...` 复核一致。

**根因证据链独立复核结论（关键切片逐条亲验）：**

- P-STOP HTTP 段：`fakenet/listeners/HTTPListener.py:276` `class ThreadedHTTPServer(http.server.HTTPServer)` 确无 ThreadingMixIn；`:261-265` `stop()` 同步 `shutdown()/server_close()`；`:292` handler 设配置超时（EV-D3 会话 INI `Timeout: 10`）。D3 核心日志 `STOP_PROVIDER_END name=HTTPListener80 elapsed_ms=6421`、`STOP_PHASE_END phase=complete elapsed_ms=8141 healthy=True`、`FakeNet-NG exiting: rc=0` 逐字核实。单线程同步 handler 拖住 shutdown 的机制有 EV-HOST-STOP-REPRO 宿主对照佐证。
- P-STOP one-file 段：D3 bootloader 日志逐字含 `failed to remove file: ..._MEI59522\pydivert\windivert_dll\WinDivert64.sys`、force-unload "found 0 DLL(s) loaded from application's temporary directory"、`trying to remove temporary directory (attempt 1..15 / 15)`、`given up after 15 attempts!`；进程 TSV `python_child_exit_observed` 单调时钟 `501244886800`（exit_code=0）→ `onefile_parent_exit_observed` `517218388400`，差 15.974s；GUI 日志 `gui_stop_callback_received=492984000000`、`gui_stop_feedback_visible` 同刻（0.000s）、`gui_process_wait_end=517156000000 code=0`（=T0+24.172s）、`gui_finish_session_begin=517187000000`（+31ms）。GUI 延迟假设被排除。
- P-SINK 段：D3 核心日志第 90 行 `PROCESS_FLOW disposition=DIVERT_FAKE dport=443 dst=192.168.204.1 proto=TCP`（target 探针被本地接管）、第 95-96 行 `disposition=DROP_EXTERNAL dst=192.168.204.1 dport=443 proto=UDP reason=no_authorized_route`；`dns_system_takeover PASS A=192.168.204.1`、`sentinel_preflight_tcp/udp PASS`（同 nonce `diag-633c3a4e...`）；当前 `dist/Logs/fnpr-sentinel-20260826-161544/sentinel.log` 全文 0 个 target 角色事件。代码侧 `Verdict` 枚举无 `ALLOW_TAKEOVER_SINK`、`matches_takeover_sink` predicate 仍在（egresspolicy.py:720）、`_compute_egress_verdict`（windows.py:1860-1887）无 sink 放行分支、`ProcessRedirectRouteGuard`（windows.py:179,2043-2051）与 `TAKEOVER_ROUTE_OK` 快照在。路由/DNS/Ubuntu listener 三种替代根因均被排除。

**重要说明**：方案引用的 EV-D2 证据目录 `dist/Logs-v33-d2/` 与包 `dist/Windows-GUI配置工具-VM诊断-v33-diagnostic-02.zip` 在本审核环境中不存在且从未入库（见 AUD-002）；上列 D3 切片已在事实层面独立覆盖 P-STOP/P-SINK 的修复前基线（`takeover_target_tcp FAIL WinError 10054`、`takeover_target_udp FAIL timed out`、`gui_stop_monotonic_timeline PASS feedback=0.000 handle=24.172`），故根因坐实不受影响。

## 2. 审核范围（本地审核，沿用方案 §8.4 范围划分并独立复核）

- 核心维护范围：方案本体；HTTPListener.py、ssl_utils（按需）、fakenet.spec、Build-GuiVmPackage.ps1、build.yaml、egresspolicy.py、windows.py、gui/procview.py、对应宿主测试、test/gui_vm 正式验收/导出。上述路径全部实际读取或哈希核对。
- 直接依赖范围：fakenet.py 停止编排（经 D3 日志间接核对 STOP_PHASE 序列）、gui/app.py 与 launcher.py（已读）、PyInstaller v6.22.0 两 bootloader 源文件（已取并核对）、fnpr_sentinel.py 与三个 Start 脚本（存在性核对）、diagnostic-02/03 只读证据（03 全量核验；02 缺失，见 AUD-002）。
- 明确排除范围：未进入门禁。
- 决策关键路径实际核验：方案 v0.6 全文；SRC-REQ-001~004 切片；CODE-STOP-001/002/003；DEP-PYI-001/002；EV-D3-RESULT/PROCESS/RUNTIME/BOOT/CORE/GUI/NET(经 tsv)/SESSION/PACKAGE；EV-D2-SENTINEL（当前文件切片）；CODE-SINK-001/002/003；ACC-002/003/006~010。
- 上下文路径：SRC-OLD-001（含 §2.6/I5 深读）；SRC-OPS-001；EV-HOST-STOP-REPRO/DESIGN。
- 最小本地证据：本记录第 1、3 节所列哈希与文件行号均为审核期间在本机亲自复算/读取所得；未复制敏感证据到本记录，未修改任何证据文件、方案或实施代码。

## 3. 全面审核覆盖清单

| # | 审核对象 | 结论 | 证据 |
|---|---|---|---|
| 1 | 方案身份与状态门禁 | 通过 | blob/SHA-256 见头部；状态 `未审核, 不得实施`；基线 commit=HEAD |
| 2 | 规范性来源身份 | 有意见 | SRC-OLD-001/SRC-OPS-001 精确一致；SRC-REQ-001~004 引用 blob 不可复现（AUD-001）；切片含义全部核对未变 |
| 3 | 现象、触发、预期、影响、复现 | 通过 | D3 results/timeline/GUI log/process TSV 逐条对照方案 §0/§1.2.1 描述，数值完全一致 |
| 4 | P-STOP 根因链（HTTP 段） | 通过 | §1 节证据；EV-HOST-STOP-REPRO 机制对照；替代根因（GUI 等待/UI 调度/DNS 停止）被单调时钟数据排除 |
| 5 | P-STOP 根因链（one-file 段） | 通过 | bootloader 日志 + 进程 TSV + DEP-PYI 源码常量三方互证；方案明确未越界声称识别文件锁持有者（§1.4 边界声明成立） |
| 6 | P-SINK 根因链 | 通过 | §1 节证据；路由/DNS/Ubuntu listener 排除链完整；反向契约测试证实有意删除 |
| 7 | 双向追溯与 ID 稳定性 | 通过 | §7 追溯表四行闭合；活动 IMP-002/004/005/006 与 ACC-002/003/006~010 无重复、无进度状态；撤销 ID（IMP-001/003、ACC-001/004/005、FB-004）不复用且声明"撤销非完成" |
| 8 | 修复直达根因（无屏蔽/无外围补丁） | 通过 | 合同 A 在真实 stop seam 打断活动 transport；合同 B onedir 消除一次性驱动删除矛盾；IMP-004 恢复精确 verdict；被否决方向（多线程/降超时/daemon/改重试常数/强杀/固定 _MEI/强制卸载）逐项有反例依据 |
| 9 | 同源正常/异常/边界与旧路径退出 | 通过 | ACC-002 矩阵（plain/TLS、无连接、完整 GET/POST、半 header、半 body、对端关闭、竞态、连续两连、重复 stop）；旧错误路径退出明确（反向契约测试改正向、`[DEBUG-STOP03]` 移除范围限定 app.py 取证代码、manifest bundle mode 切换、bootloader debug 关闭） |
| 10 | 可交接实施性 | 通过 | 每个 IMP 有允许修改清单、纯实现细节放权、禁止项；§4 顺序图；方向性决定闭合（GATE-STOP-REQ/PYI/DESIGN 三门禁证据均核验存在） |
| 11 | ACC 契约完备性 | 有意见 | 各 ACC 含前置/输入/命令或操作/前后精确结果/证据/失败边界；ACC-010 命令参数与 Build-GuiVmPackage.ps1 param() 实际一致；ACC-002 命令在本审核宿主不可执行（AUD-004）；IMP-005 引用不存在的 `Run-All-Tests.cmd`（AUD-005） |
| 12 | 回归、兼容、可观察性、现实阻碍 | 通过（含意见） | IMP-006 回归门禁与不可覆盖交付；FB-005 覆盖构建环境缺失；宿主测试环境装配未写明（并入 AUD-004） |
| 13 | Fallback 评定 | 通过 | FB-001/002/003/005 各有依据/触发/影响/退出方式，均不替代根因修复、不引入新错误源；隐式回落（sink→本机 listener、onedir→one-file、优雅→强杀、"DNS 成功即通信成功"）显式禁止；FB-004 撤销不复用 |
| 14 | 两链独立性与汇总边界 | 通过 | §0/§4/IMP-004 前置声明一致；ACC-006~008 仅验收 P-SINK；IMP-005/006 为唯一汇总点 |
| 15 | 审核环境判定（§8.4） | 有意见 | `不可云端审核` 判定与不伪造 audit_commit_sha 的做法正确；但"本地运行态依赖 `dist/Logs-v33-d2`"声明与现状矛盾（AUD-002） |
| 16 | 反例检查 | 未发现推翻性反例 | 5.0s 时限可行性按 D3 分解复核（6421ms 消除后 complete≈1.7s+onedir 无清理窗口，余量充足）；ACC-003"无 one-file `_MEI`"与 onedir 自洽；EV-D2 缺失不影响基线可重建性 |

## 4. 审计意见

> 本轮为首轮全面审核，5 条意见的发现来源分类均为"首轮全面审核"（AUD-001/002/003 来自机械扫描之身份核对，AUD-004/005 来自全部评审项语义审查）。

### AUD-001 规范性来源 FaknetNG.md 的引用身份不可复现

- 首次提出轮次：第 1 轮。
- 来源层级：规范性来源（SRC-REQ-001~004 共享的持久化对象）。
- 反例：`git cat-file -t cd2ac68d4b96436feb00df3da70f7273ef81cc80` 对象不存在；`git log --all` 全历史无该 blob；当前工作区 blob 为 `26b40c86a2807e49f55b480e0258f306de5768e2`、SHA-256 `bd4f05ace59567456ae91d76e69c4a97b28722b0f03592053b42db3db4b61922`；HEAD blob 为 `b8b91772e1068fac1565e4fce69d50091ef458c5`。引用值与任何可恢复版本都不匹配。
- 证据：方案 §1.1 SRC-REQ-001 行；上列 git 命令输出；HEAD→工作区 diff 仅为 08.26 第 7 项新增。
- 风险和影响：后续轮次无法用该 blob 锚定规范来源；若 FaknetNG.md 再被编辑，无法机械检测规范含义漂移。不阻断本轮：切片（08.18 第 2 项、08.21 第 2 项、08.26 第 1–7 项）经内容核对完整且含义未变。
- 最小闭合条件：作者在审计处理记录中将 SRC-REQ-001~004 的持久化身份更正为可复现值（建议提交该文件后引用提交 blob），并声明规范含义无变化。
- 受影响的既有通过项及所需复审证据：覆盖清单 #2；下轮复核更正后的身份与切片含义未变。

### AUD-002 EV-D2 实机证据在审核环境缺失，§8.4 运行态依赖声明失效

- 首次提出轮次：第 1 轮。
- 来源层级：实机证据（EV-D2-RESULT/TIME/CORE/GUI/CONFIG/PACKAGE 七项）。
- 反例：`dist/Logs-v33-d2/diagnostic-export-20260826-161800/` 目录不存在；`dist/Windows-GUI配置工具-VM诊断-v33-diagnostic-02.zip` 不存在；`git log --all -- dist/Logs-v33-d2*` 为空（`dist/` 被 .gitignore 覆盖，从未入库，无法恢复）；diagnostic-01 导出同样不在，仅 diagnostic-03 在。
- 证据：本机 `ls dist/` 与 find 结果；方案 §1.2（引用上述路径并声称"evidence-sha256.tsv 已逐项校验通过"）、§8.4（"本地审核需读取 `dist/Logs-v33-d2`"）、§0（"v33-diagnostic-01/02/03 包和证据只读保留"）。
- 风险和影响：EV-D2 全部哈希引用无法核验；"只读保留"承诺与现状矛盾；后续轮次若需 D2 独立证据（如 GUI 24s 会话时间线 EV-D2-TIME）将无法复核。关键事实不受影响：P-STOP/P-SINK 修复前基线已被 EV-D3（17/17 哈希通过）+ 当前 sentinel.log + 代码证据独立覆盖（见本记录 §1），根因结论不断阻。
- 最小闭合条件：作者恢复 D2 导出目录与包（从 VM/原始介质重新导出并重算 evidence-sha256.tsv），或在审计处理记录中说明其下落，并把 ACC-007"修复前精确结果"的证据指向改为可核验的 D3 等价切片、同步修正 §8.4 运行态依赖清单与 §0 保留表述。
- 受影响的既有通过项及所需复审证据：覆盖清单 #2/#15/#16 中涉及 EV-D2 的锚点；下轮复核恢复件哈希或改挂后的 D3 锚点行。

### AUD-003 EV-D2-SENTINEL 引用活日志的固定哈希已漂移

- 首次提出轮次：第 1 轮。
- 来源层级：实机证据（EV-D2-SENTINEL）。
- 反例：引用 SHA-256 `1531d77e1d9a11ff2d4e9aafc389fbf6f507c61b36577e458d86e5aeb5627523`，当前文件为 `ccb1d4e9f03fe8c2f69300cbfb3cf241cb7cbf752fa7a632584d066a41500dce`；文件在方案引用后被 diagnostic-03 会话（nonce `diag-633c3a4e...`，18:06:30 两行）追加。
- 证据：当前 sentinel.log 全文（8 行事件：ready + 3 组 preflight probe_ok，0 个 target 事件）；D2 nonce `diag-aaf110453b944847bf1b89afd161993f` 的 preflight TCP/UDP 切片完整保留。
- 风险和影响：与 AUD-001 同类（对可变对象引用不可变身份）。切片与结论未受影响。
- 最小闭合条件：处理记录中说明追加成因，引用方式改为"文件+切片定位（nonce/时间戳）"或对快照副本另建哈希。
- 受影响的既有通过项及所需复审证据：覆盖清单 #6 的 sentinel 锚点；下轮按新引用方式复核切片。

### AUD-004 ACC-002 宿主测试命令缺少可执行环境前置

- 首次提出轮次：第 1 轮。
- 来源层级：验收契约（ACC-002）与现实阻碍处理。
- 反例：本审核宿主 `python3 -m pytest` 不可用（系统 Python 与 ~/.local Python 3.13 均无 pytest 模块）；既有 `test/test_listener_error_routing.py` 顶层 `import pytest`，在本环境收集即失败；方案自身在 EV-HOST-STOP-REPRO 记载"当前 Ubuntu 缺 OpenSSL 依赖无法运行项目 handler，未改动环境"。ACC-002 只给了命令，未写明在哪个环境（哪台机器/哪个 Python/依赖如何装配）执行；FB-005 仅覆盖构建环境缺失，未覆盖宿主测试环境。
- 证据：本机 pytest 探测输出；EV-HOST-STOP-REPRO（方案 §1.3）；SRC-OLD-001 状态行记载宿主全量回归曾在作者环境通过（468 passed），说明可运行环境存在于方案作者侧但未在方案中固定。
- 风险和影响：实施者或后续审计者可能无法复跑 ACC-002 的"修复前精确结果"，削弱该 ACC 的可机械执行性；不改变测试矩阵本身的完备性（前置矩阵、前后结果、失败边界均已写明）。
- 最小闭合条件：作者在处理记录中固定宿主测试环境装配（机器/Python 版本/依赖清单或既有虚拟环境路径），或指明 ACC-002 命令的执行宿主；新增 `test/test_http_listener_stop.py` 的文件名如与 ACC-002 命令不一致须按方案既定规则先行同步。
- 受影响的既有通过项及所需复审证据：覆盖清单 #11/#12；下轮复核环境前置已固定且矩阵未删减。

### AUD-005 IMP-005 允许修改范围引用不存在的 `Run-All-Tests.cmd`

- 首次提出轮次：第 1 轮。
- 来源层级：实施契约（IMP-005 允许修改清单）。
- 反例：`find`/`git ls-files` 全仓库无 `Run-All-Tests*`；该字符串仅出现在方案 §3 IMP-005 一行。当前 test/gui_vm 下的正式入口是 `Run-Tests.cmd`（VM 验收）与 `run_vm_diagnostics.py`（诊断）。
- 证据：仓库检索输出；ACC-009 操作仅写"双击正式总入口"，未固定文件名。
- 风险和影响：实施者无法确定 v34 正式一键入口是新建 `Run-All-Tests.cmd` 还是改造既有 `Run-Tests.cmd`，与"可交接实施性"的其余部分相比存在一个未闭合的命名决定。
- 最小闭合条件：处理记录或方案勘误中固定 v34 正式总入口的确切文件名与位置（新建或沿用），使 IMP-005 允许修改清单与 ACC-009 操作指向同一对象。
- 受影响的既有通过项及所需复审证据：覆盖清单 #10/#11；下轮复核入口名称已唯一。

## 5. 评定与后续

- fallback 评定：FB-001、FB-002、FB-003、FB-005 全部必要（各有规范来源、明确触发、影响与退出方式），无一替代根因修复或引入新错误来源；FB-004 撤销处理正确；隐式回落禁止清单与各 IMP"禁止自行裁决"一致。
- 结论：**存在不同意见, 下一审核模式: 定向复审**。5 条意见均属证据身份/可执行环境/命名精度类缺陷，方案的根因证据链、修复直达性、同源场景、双向追溯、决策闭合与验收契约方向经全面审核均成立。
- 定向复审要求：作者先建立审计处理记录（逐条闭合 AUD-001~005）；下轮只复审未解决/复开意见、实际 diff、受影响通过项（覆盖清单 #2/#6/#10/#11/#12/#15/#16 相应锚点）与全局不变量；其余项目凭本记录直接继承。
- 本记录创建后不可修改；其 blob 见文末。

## 6. 记录身份

- 本文件：`PLAN/2026.08.26/2026.08.26-02-审计-v33实机问题分阶段修复方案.md`
- 身份声明：本记录内容自本节起固定，创建后不可修改。因文件无法内嵌自身哈希，其 Git blob 与 SHA-256 由审计方在创建时刻对本文件全文计算并在审计输出中公布；作者的处理记录与下一轮审计记录应引用该公布值进行身份核对。
