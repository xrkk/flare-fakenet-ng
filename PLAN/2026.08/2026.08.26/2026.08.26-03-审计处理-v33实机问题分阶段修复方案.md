# 审计处理记录：v33 实机问题分阶段修复方案

| 项 | 值 |
|---|---|
| 处理轮次 | 第 1 轮审计处理；对应首次全面审核 |
| 处理日期 | 2026-08-26（Asia/Shanghai） |
| 原审计记录 | `PLAN/2026.08.26/2026.08.26-02-审计-v33实机问题分阶段修复方案.md` |
| 原审计 blob / SHA-256 | `f389773442fe4a9071d92fc47232e22a41496abe` / `f7a9b5789ca62da6c8fbafc9ac74da860330a6e566902d4d06f59859915a49ec` |
| 处理前方案 | `PLAN/2026.08.26/2026.08.26-01-v33实机问题分阶段修复方案.md` v0.6；blob `3e8e0143c3e905184636c20eb88049453f23af05`；SHA-256 `85ad4b217cb71e273cfd25c7e9f3c2f7a627bf2fcb74b14ff5f4ada9f5f381c3` |
| 处理后方案 | 同路径 v0.7；blob `2a1b7c84047a22280f228750f4af8513ac4ca718`；SHA-256 `7226a8a6625b73f24994037678c756beb2f70c6518e3f25edb98b726e14f96d9` |
| 审计环境 | 本地持久化审核；直接读取本地仓库和 `dist` 证据 |
| 交付方式 | 本地审计记录，不是 PR 交付；PR 合并门禁不适用 |
| repository / 目标主分支 | `https://github.com/mandiant/flare-fakenet-ng` / `master` |
| 当前分支 / HEAD | `process-redirect-v23` / `411942fd6600b0388070f6e1268e8e0c149f708b` |
| audit_commit_sha | 无；本地审核不伪造云端审核 commit |
| 处理后方案状态 | `未审核, 不得实施` |
| 下一审核模式 | 定向复审 |
| 审核循环熔断 | 不触发；所有 AUD 均为首次处理 |

原审计记录保持不可变。本记录只引入可由规范来源、当前代码和已核验证据唯一确定的修改，不实施代码，不恢复、复制或伪造缺失证据。

## 1. 身份门禁与漂移核对

1. 原审计记录精确匹配方案路径、v0.6、blob `3e8e0143...` 和 SHA-256 `85ad4b21...`，结论不是口头意见。
2. 原审计为本地审核，没有 PR 或 `audit_commit_sha`，因此“审计文件已合并目标主分支”门禁不适用。
3. 审计时和本轮处理时 HEAD 均为 `411942fd6600b0388070f6e1268e8e0c149f708b`；不存在 `audit_commit_sha..HEAD` 区间。决策关键实现路径没有新的 commit 漂移。
4. 工作区中的 `PLAN/FaknetNG.md` 当前 blob 为 `26b40c86a2807e49f55b480e0258f306de5768e2`、SHA-256 `bd4f05ace59567456ae91d76e69c4a97b28722b0f03592053b42db3db4b61922`。审计已逐条确认 SRC-REQ-001~004 引用切片存在且规范含义未变，本轮只更正身份。
5. `test/gui_vm/README.md`、九个代码证据、PyInstaller v6.22.0 依赖证据和 diagnostic-03 的 17 个导出文件均继承原审计复算结论；本轮没有修改这些对象的规范含义或实现行为。
6. 原审计后唯一有意改变的决策关键对象是方案本身，变化全部列在本记录 §3。上下文路径、排除范围和用户既存 `.gitignore` 修改不作为阻断项。

漂移结论：`audit_commit_sha..当前 HEAD` 不适用于本地审核；HEAD 未变化，决策关键实现路径无漂移。方案和证据身份变化均由本轮审计处理显式记录，不影响原审核确认的根因结论。

## 2. AUD 逐条处理

### AUD-001 规范性来源 FaknetNG.md 的引用身份不可复现

- **处理结论**：同意。
- **独立判断**：方案 v0.6 写入的 blob `cd2ac68d...` 和 SHA-256 `2ae11545...` 无法由当前文件复算。当前可复现身份与原审计一致；引用切片集合和规范含义没有变化。
- **修改位置**：方案 §1.1 SRC-REQ-001；SRC-REQ-002~004 继续共享该对象身份。
- **实际修改**：改为当前工作区 blob `26b40c86a2807e49f55b480e0258f306de5768e2`、SHA-256 `bd4f05ace59567456ae91d76e69c4a97b28722b0f03592053b42db3db4b61922`，保留原稳定 ID、定位和规范含义。
- **证据**：原审计 §1/AUD-001；本轮 `git hash-object PLAN/FaknetNG.md` 与 `sha256sum` 复算；HEAD→工作区内容核对。
- **最小闭合条件**：已满足。没有上层需求变化。
- **复审请求**：定向复核新 blob/hash 可复现，以及四个来源切片含义未变。

### AUD-002 EV-D2 缺失与运行态依赖声明失效

- **处理结论**：同意。
- **独立判断**：`dist/Logs-v33-d2` 和 diagnostic-02 ZIP 当前不存在、未入库，不能继续作为可核验证据。恢复它们不是根因或修复合同的必要条件；diagnostic-03 已独立覆盖停止和 sink 修复前事实。
- **修改位置**：方案头部目标交付；§1.2；IMP-001/002/004/005；ACC-007/009；§7；§8.4；v0.7 版本记录。
- **实际修改**：撤销 EV-D2 系列当前证据锚点，不虚构恢复件；EV-D3-RESULT/CORE 和 EV-SENTINEL-AUDIT 接管修复前证据；移除本地审核对 D2 的运行态依赖；目标交付不再声称 diagnostic-01/02 仍被只读保留。
- **证据**：原审计 §1/AUD-002；本轮 `find dist` 复核；EV-D3 17/17 哈希校验和原审计独立切片结论。
- **最小闭合条件**：已满足。ACC-007 的前置失败结果和 §7 双向追溯均已改挂 D3。
- **复审请求**：定向复核所有活动合同中不再依赖 EV-D2，且 D3 等价切片完整覆盖修复前事实。

### AUD-003 Sentinel 活日志固定哈希漂移

- **处理结论**：部分同意。
- **同意部分**：活日志会追加，不能把整文件 SHA-256 当作永久切片身份；引用必须改为稳定事件键或不可变快照。
- **不同意部分**：原审计称当前日志有“8 行事件”，本轮逐行实读为 7 行：1 个 ready 加 3 组 TCP/UDP preflight。其 SHA-256 仍为原审计公布的 `ccb1d4e9...`，故行数表述是原审计自身的计数错误，不得写入方案事实。
- **修改位置**：方案 §1.2 新增 EV-SENTINEL-AUDIT；IMP-004、ACC-007、§7 和 §8.4 引用更新。
- **实际修改**：用 nonce `diag-633c3a4eb243464b8d9d52c3ce2b6130`、时间 `2026-08-26T18:06:30+0800`、role `preflight`、transport `tcp/udp` 定位两条事件；以不可变原审计 blob 固定独立复核结论；当前整文件 hash 只作本轮快照，不作永久身份。
- **证据**：原审计 AUD-003；本轮 `nl -ba` 实读第 6–7 行、`sha256sum` 复算；EV-D3-RESULT 同 nonce Windows 侧结果。
- **最小闭合条件**：已满足。活日志后续追加不会改变已定位事件；同 nonce target 缺失由原审计不可变记录和本轮实读共同固定。
- **复审请求**：定向复核事件键唯一、原审计计数错误没有被继承、P-SINK 根因不依赖可变整文件哈希。

### AUD-004 ACC-002 缺少可执行环境前置

- **处理结论**：部分同意。
- **同意部分**：ACC-002 v0.6 的裸 `python3 -m pytest` 未固定机器、Python 和依赖，当前宿主无法机械执行；该现实阻碍必须闭合。
- **不同意部分**：当前 Ubuntu 系统 Python 没有 pytest/OpenSSL 只说明宿主未装配，不证明测试矩阵或产品 module 存在新缺陷，也不要求新增抽象或在 Ubuntu 运行 FakeNet 核心。
- **修改位置**：IMP-002 允许路径和合同 C；ACC-002；ACC-006；FB-005；§8.4 核心范围；SELF-09。
- **实际修改**：复用已有 Wine builder module，固定镜像 `flare-fakenet-ng/gui-vm-diagnostic-builder:py3119-pyi6220`、Windows Python 3.11.9 和 `pytest==8.3.5`；规定 Dockerfile 构建期 import 检查和镜像身份取证；规定 `Build-GuiVmDiagnosticPackage.sh --image-only` 的工作目录、唯一输出、退出边界及“不生成 ZIP”；ACC-002/006 写入完整 `docker run` 命令。FB-005 扩展到宿主测试环境缺失。
- **seam 判断**：测试继续从 HTTPListener 和 verdict 的既有 interface 验证，不新增 adapter。环境装配留在现有 builder 内，调用方只学习一个 `--image-only` 入口，避免依赖散落到多条手工安装命令。
- **证据**：原审计 AUD-004；`tools/docker/gui-vm-diagnostic/Dockerfile` 已固定 Python 3.11.9 和运行依赖；现有 shell builder 已固定镜像名、安装器 SHA 和 Docker build 流程；`docs/testing.md` 要求隔离开发/测试环境。
- **最小闭合条件**：已满足于方案合同层。工具尚待方案审核后实施，ACC 已固定工作目录、输入、输出、退出码和可观察证据。
- **复审请求**：定向复核版本、命令、镜像输出和禁止运行核心的边界可机械执行，且未引入第二测试 adapter。

### AUD-005 IMP-005 引用不存在的 Run-All-Tests.cmd

- **处理结论**：同意。
- **独立判断**：仓库没有 `Run-All-Tests.cmd`；现有正式 GUI VM 入口、README、runner 和构建 manifest 均指向 `test/gui_vm/Run-Tests.cmd`。新建第二入口没有需求，会增加事实源。
- **修改位置**：IMP-005 允许路径；ACC-003/009/010 操作；SELF-09；v0.7 版本记录。
- **实际修改**：唯一固定 v34 正式总入口为包内 `test\gui_vm\Run-Tests.cmd`；禁止新建 `Run-All-Tests.cmd` 或其他并列入口。
- **证据**：原审计 AUD-005；仓库 `find`/`git ls-files`；`test/gui_vm/README.md` 用法；`Build-GuiVmPackage.ps1` 的 acceptance entry。
- **最小闭合条件**：已满足。实施和三个 VM ACC 指向同一对象。
- **复审请求**：定向复核允许修改清单、操作和 manifest 入口一致。

## 3. 实际 diff 清单与影响分析

### 3.1 实际 diff 清单

| 方案位置 | 变化 | 对应 AUD | 契约影响 |
|---|---|---|---|
| 头部 | v0.6→v0.7；加入原审计身份；修正 diagnostic-01/02 保留声明 | 001/002 | 身份和证据范围；业务范围不变 |
| §1.1 | 更正 SRC-REQ-001~004 共享对象 blob/hash | 001 | 无规范含义变化 |
| §1.2 | 删除不可核验 D2 表；增强 D3 sink/stop 事实；新增 Sentinel 稳定切片 | 002/003 | 证据锚点变化；根因不变 |
| IMP-001/002/004/005 | D2 引用改 D3；增加固定测试环境合同；正式入口改 `Run-Tests.cmd` | 002/004/005 | 当前方案契约变化，可可靠界定 |
| ACC-002/006 | 固定 Wine/Python/pytest、`--image-only`、docker 命令和证据 | 004 | 验收环境契约变化 |
| ACC-003/007/009/010 | D3 修复前基线与唯一 VM 入口 | 002/003/005 | 证据和操作精度变化 |
| FB-005 | 覆盖构建或宿主测试环境缺失 | 004 | fallback 触发范围扩展，不替代根因修复 |
| §7 | 双向追溯由 D2 改挂 D3/Sentinel | 002/003 | 证据身份变化 |
| §8.1/8.2/8.4 | 新增 SELF-09；更新维护/依赖/关键/运行态范围；固定定向复审 | 全部 | 审核交接变化 |
| §9/§10 | 新增 v0.7 记录和定向审核重点 | 全部 | 无业务行为变化 |

### 3.2 受影响的既有通过项

- 覆盖清单 #2：来源与证据身份，按 AUD-001~003 定向复核。
- 覆盖清单 #6：P-SINK 结论本身继承，只定向复核 D3/Sentinel 新锚点。
- 覆盖清单 #10/#11：实施可交接性和 ACC 命令，按 AUD-004/005 定向复核。
- 覆盖清单 #12：现实环境阻碍，复核固定 builder 能否机械创建验收环境。
- 覆盖清单 #15：本地审核运行态依赖，复核已彻底移除 D2。
- 覆盖清单 #16：反例结论继承，只复核 D3 是否完整接替 D2 修复前基线。

### 3.3 未受影响继承项

- 覆盖清单 #1、#3~#5、#7~#9、#13、#14 的原审计通过结论直接继承。
- P-STOP 两段根因、P-SINK verdict 根因、GUI 延迟反证、WinDivert 文件锁持有者未知边界均未改变。
- IMP-002 合同 A/B、IMP-004 裁决方向、两个问题独立实施并只在 IMP-005/006 汇总的依赖图未改变。
- ACC 的业务输入矩阵、1.0s/5.0s/三轮时限、Ubuntu 同 nonce TCP/UDP、网络恢复和不得强杀条件未改变。

### 3.4 全局不变量

1. 单机、单实例、单用户；不引入集群、高可用或远程协调机制。
2. Windows FakeNet/WinDivert 只在隔离 VM 运行；Ubuntu 宿主测试不得启动核心。
3. P-STOP 与 P-SINK 是两个独立问题；一项通过不能替代另一项。
4. HTTP 保持单线程正常语义；停止时只关闭当前 transport；不得以 daemon、多线程、降 timeout 或强杀取巧。
5. 正式核心采用 onedir 且包根只有一个 `fakenet.exe`；不得 fallback one-file。
6. 精确 sink TCP/UDP 原地址、协议和端口真实到达 Ubuntu；不得 fallback Windows listener。
7. v34 正式 VM 总入口只有 `test\gui_vm\Run-Tests.cmd`。

影响分析结论：规范性来源含义、问题范围、架构/根因、权限归属、依赖图和迁移/切换策略均未改变；变化局限于可可靠界定的证据身份、测试环境和入口契约。下一审核模式为定向复审，不需要全面复审。

## 4. 引入后复查

| 复查项 | 结论 | 依据 |
|---|---|---|
| 现象、触发、预期、影响和复现 | 通过 | D3 同次 results/core/process/GUI/bootloader 与 SRC-REQ-001~004 |
| 根因证据 | 通过 | D3、代码、PyInstaller 源码和宿主对照；D2 已移除而不留断链 |
| 双向追溯与 ID | 通过 | IMP/ACC 稳定不变；§7 全部改挂可核验证据；无进度状态 |
| 直接修复 | 通过 | stop seam、onedir、精确 sink verdict 均直达根因 |
| 同源正常/异常/边界 | 通过 | HTTP/TLS/竞态矩阵与 sink 正负矩阵未删减 |
| 回归与可观察性 | 通过 | ACC-002/006 固定环境和版本证据；VM 三轮与 Ubuntu nonce 证据不变 |
| 可交接实施性 | 通过 | `--image-only` 和唯一 `Run-Tests.cmd` 消除两个未闭合决定 |
| 决策闭合 | 通过 | 无并列环境或入口；无“视情况而定” |
| 验收契约 | 通过 | 已有工具写实际命令；待实现的 `--image-only` 写明工作目录、输入、输出和失败边界 |
| fallback | 通过 | FB-005 扩展有现实证据，不替代修复；其他 fallback 不变 |

没有未解决中/高风险意见，也没有证据争议。由于 AUD-004/005 的引入改变了当前方案的执行环境和正式入口契约，不能直接把方案置为已审核；方案 v0.7 保持 `未审核, 不得实施`，进入定向复审。

## 5. 定向复审请求

下一轮只需复核：

1. SRC-REQ-001~004 新 blob/hash 与切片含义。
2. 活动合同已无 EV-D2 依赖，D3/Sentinel 新锚点足以维持根因和修复前基线。
3. Sentinel 事件键和 7 行实读事实，避免继承原审计“8 行”误计。
4. ACC-002/006 固定镜像、Python、pytest、`--image-only` 和命令的机械可执行性。
5. IMP-005 与 ACC-003/009/010 均唯一指向 `test\gui_vm\Run-Tests.cmd`。
6. 本记录 §3 的受影响项和全局不变量；其他全面审核通过项直接继承。

定向复审通过前不得实施代码。
