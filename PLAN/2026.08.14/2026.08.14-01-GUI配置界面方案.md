# FakeNet-NG GUI 配置界面(fakenet-GUI)方案

| 项 | 值 |
|---|---|
| 方案版本 | **v1.28**(多域名接管放开 + 域名通配符,见 §12.31) |
| 日期 | 2026-08-18 |
| 基线分支 | `process-redirect-v23`(提交 `a3b2794`);实施在现状之上单独成组提交 |
| 状态 | **v1.28 全部门禁关闭(2026-08-18 18:27):v29 包策略一键测试十项全 PASS——精确/通配子域名各租得 3 个真实公网 IP、裸域与未放行域零租约、接管多域名就绪、裸域导向 sink、两阶段 rc=0(§12.31.4)。v28 已先行通过 A1–A9 与新 smoke 基线。当前交付版本 v29**。既有落地记录见 §12;本轮结果见 §12.28–§12.31。 |
| 上游文档 | `PLAN/FaknetNG.md`(路线图,本方案对应其中"GUI 配置界面"任务) |

**版本记录**
- v0(2026-08-14):初稿——访谈结论 + 设计 + 自评(§8.1)。
- v0.1(2026-08-14):并入重评估结论(§8.2)——setup.py 打包缺口、GUI 读取编码策略、验收环境定为 VM、CI 构建步骤隔离、ASCII 断言与无损往返矛盾消解、F7 升级、锚定测试补强及若干低风险补强。
- v0.2(2026-08-14):裁决第三方审计(§8.3)——P1–P12 采纳(P4 论据经实证勘误,见审计文档附录)、F1 改判 fail-closed、dev 命令修正、写出编码规范、实例互斥探测、ShellExecuteW 返回值、CI 拷贝门禁、模板动态枚举、逐阶段 DoD 等 13 处修订。
- v1.0(2026-08-14):**实施完成**——9 个实施提交(schema/configmodel/validator/launcher/UI/打包/验证/README),全量测试 324 通过零回归;偏离与超方案改动清单见 §12。
- v1.1(2026-08-14):**验收闭环**——VM 一键验收包(§12.5)在 VMware 虚拟机内执行,A1–A9 九项全部 PASS(7 实测 + 2 等效),证据核验通过;§12.4 遗留事项关闭。
- v1.2(2026-08-14):**验收后用户需求迭代**(§12.6)——可执行文件改名 fakenet-config.exe→fakenet-GUI.exe(全链路同步);校验面板支持复制单条/全部与完整消息查看、消息列不再截断。部署包升 v3。
- v1.3(2026-08-15):**核心启动日志**(§12.7)——纠正首轮将需求误解为 GUI 自身日志的偏差;日志下沉到 FakeNet 核心入口,保证 GUI/双击/命令行/脚本启动均记录,冻结版日志目录与 `fakenet.exe` 同级。
- v1.4(2026-08-15):**VM 实测问题闭环**(§12.8)——GUI 自身每次启动亦写 exe 同级 `Logs` 单文件;新建配置补齐默认 TCP/UDP 代理监听器段,消除启动即出现的两个悬空引用错误。
- v1.5(2026-08-15):**GUI 第一阶段界面优化**(§12.9)——保持 980×700 与纯标准库约束,重排校验抽屉/底部操作栏,统一字段布局,修复监听器长字段裁切,补充自定义响应空状态与克制的 Windows 安全分析工具视觉规范。
- v1.6(2026-08-15):**GUI 密度修正**(§12.10)——根据 v6 实际界面反馈,二值下拉改为 checkbox 并保持三套 INI 字面量;枚举下拉按内容定宽而不横向铺满;全局/出站策略组内改为双列紧凑排布,尽量消除非必要滚动。
- v1.7(2026-08-15):**主机视觉验收闭环**(§12.11)——实际运行 v7 exe 并以 980×700 自动切换四页截图;确认干净状态无多余滚动条,修正全局长标签断词与监听器工具栏偏宽,交付目标升 v8。
- v1.8(2026-08-15):**全字段说明**(§12.12)——为所有 schema 配置项补齐基于默认配置注释、源码与既有审计约束的中文提示;鼠标悬停显示 tooltip,键盘焦点继续更新顶部提示;无校验问题时隐藏无意义的“展开详情”。
- v1.9(2026-08-15):**配置文件快捷操作**(§12.13)——底栏新增“导入配置”和“恢复默认配置”;导入直接打开并绑定原文件,恢复默认经一次确认后立即以 GUI 安全默认配置覆盖当前绑定文件。
- v1.10(2026-08-15):**出站策略激活闭环**(§12.14)——修复从 Disabled 切到 DomainAllowList 后,锁定的 `ExternalAllowedTCPPorts` 未显式写入模型、用户无法编辑却持续报错的问题;同步代码强制值并明确一键补齐按钮状态与提示。
- v1.11(2026-08-15):**出站拓扑零操作补齐**(§12.15)——移除“一键补齐必需监听器”按钮;用户启用 DomainAllowList 时自动创建/启用 relay 与 UDP/TCP DNS/53,载入已启用但缺拓扑的配置时也在内存中自动修复并明确标为未保存修改。
- v1.12(2026-08-15):**出站策略总开关语义修正**(§12.16)——不再向用户暴露内部枚举值 DomainAllowList;用“出站策略总开关”复选框映射 `Disabled/DomainAllowList`,并把首组从“域名放行”更名为覆盖全部子策略的“基础策略”。
- v1.13(2026-08-16):**原生任务式重构与运行闭环**(§12.17)——基于 v13 全界面主机截图审查和 grill-me 决策,重构五标签页、出站策略任务控件、系统监听器分区、独立校验窗口与完整实时日志;增加 GUI 命名互斥体、启动提速与原生 splash,修复 SHA 写回、伪 dirty 和字段注册表漂移。部署包目标 v14。
- v1.14(2026-08-16):**核心无分流监听器启动修复与 VM 验收判定加固**(§12.18)——复核 v14 隔离 VM 导出证据时发现 A6/A8 假阳性:核心实际以 `UnboundLocalError`/rc=1 退出。经用户明确授权,本轮以最小核心修复、成功启动日志标记、验收结果 fail-closed 和回归测试闭环,部署包目标 v15。
- v1.15(2026-08-17):**界面可读性缩放与隐式锁定提示**(§12.19)——应用户反馈:全部界面字体与默认窗口尺寸放大 50%;删除"🔒 代码强制"等显示徽标,改为控件禁用 + 悬停 tip 追加锁定原因的隐式提示,消除启停切换时的布局抖动;修正出站策略总开关关闭时两个条件锁定字段仍可编辑的缺陷。部署包目标 v16。
- v1.16(2026-08-17):**v16 隔离 VM 试用反馈修复**(§12.20)——Treeview 行高随字体缩放;未保存修改可点击查看差异详情;dirty 由"闩锁"改为与基线渲染比对计算,且总开关关闭时反向撤销开启时自动补齐的纯净拓扑;底部三个操作按钮统一宽度;标签换行改为按字体实测像素计算并自动加宽标签列,消除"Diverter/级"式的拉丁-汉字边界断行。部署包目标 v17。
- v1.17(2026-08-17):**v17 隔离 VM 试用反馈修复**(§12.21)——弹窗(未保存修改详情/校验详情)居中于主窗口;底部三个操作按钮样式与尺寸完全统一(全部 Action.TButton + width=14);补全 Diverter 级进程黑/白名单与主机黑名单字段的作用说明提示。部署包目标 v18。
- v1.18(2026-08-17):**v18 隔离 VM 试用反馈修复**(§12.22)——启动加载上次配置(失败弹窗后回退默认工作配置);标题改为 `FakeNet-NG 配置工具 - {绝对路径}` 并消除未绑定状态;进程/主机黑/白名单互斥在界面上禁用体现;答疑原版名单与新增出站功能的差异及上游测试覆盖。部署包目标 v19。
- v1.19(2026-08-17):**v19 隔离 VM 试用反馈修复**(§12.23)——首次启动以信息框载入并记住不可变默认配置文件(保存必须另存、任何路径不得覆盖默认);标题固定,配置路径以加粗高亮行显示并提供 Explorer 定位按钮;修复启用私网接管时整页销毁重建导致的闪烁重排。部署包目标 v20。
- v1.20(2026-08-17):**页签条置底**(§12.24)——`TNotebook.tabposition='s'`,五个页签条移到窗口最下方,页内容全部位于其上。部署包目标 v21。
- v1.21(2026-08-17):**v1.20 需求误解返工**(§12.24)——用户需求实为“把页签控件下方的内容行(配置路径/校验摘要/操作栏)移到页签控件上方,让 Notebook 成为窗口最底层元素”,而非移动页签头;恢复默认页签头并调整网格行序(提示0、路径1、校验2、分隔线3、操作栏4、Notebook5 占满剩余高度)。v21 包(含误解实现)未交付 VM,由 v22 取代。
- v1.22(2026-08-17):**监听器工作线程异常入日志与代理竞态修复**(§12.25,经用户明确授权的核心修复)——VM 实机控制台出现海量 `AttributeError: NoneType.getOriginalDestPort` traceback 且不进日志文件。根因:socketserver 默认 `handle_error` 直写 stderr 绕过日志框架;非策略模式启动顺序为“监听器服务 → diverter.start() → acceptDiverter 注入”,窗口期被分流的包命中 `server.diverter=None`。三处修复:共享 `LoggingThreadingMixIn`(9 个线程服务器类)、`get_top_listener` 对 diverter=None 优雅降级、依赖注入提前到 diverter 启动前。部署包目标 v23。
- v1.23(2026-08-17):**验收证据一站式收集**(§12.26)——应用户需求:VM 验收 runner 启动时对包根 GUI Logs 目录做快照,结束时把本次运行新增/更新的 GUI 日志自动并入证据目录 `Logs\<时间戳>\gui-logs\`,用户只需拷贝测试目录一处, 不再需要第二处拷贝与重命名;results.tsv 保持有且仅有 A1-A9 九行。部署包 目标 v24(v23 含核心修复尚未交付 VM,一并并入 v24)。
- v1.24(2026-08-17):**DNS 枚举指针解引用与主异常打印去重**(§12.27,经用户实机反馈授权的核心修复)——用户在 VM 以 GUI 启动 DomainAllowList 完整核心时崩溃:`winutil.get_dns_servers` 沿 `Next` 遍历未对 `POINTER(IP_ADDR_STRING)` 解引用,≥2 个 DNS 的主机必崩(单 DNS 机器不触发,故此前未暴露);另主入口异常路径同时 `logger.exception` 与 `traceback.print_exc()`,控制台重复打印两遍。修复:解引用链式遍历;去掉裸 print_exc。部署包目标 v25。
- v1.25(2026-08-18):**Auto 上游 DNS 探测选择**(§12.28,经用户实机反馈授权的核心修复)——v25 实机日志证实:api.deepseek.com 被正确放行识别,但 Auto 上游 DNS 选中双网卡 VM 中不可用的 192.168.243.1,真实解析被重置(DNS_SERVFAIL),放行链路断在第一步。修复:Auto 模式对候选逐个探测(最小 DNS 查询,应答即存活),选第一个应答者;全部无应答回退首个并告警。部署包目标 v26。
- v1.26(2026-08-18):**TSO 长度自洽修正**(§12.29.3)——根因实证后的最小修复:环回 TSO 段 ip.len 失配时在 pydivert 发送视图原位收缩长度字段,配 TSO_LENGTH_FIX 聚合上报;部署包目标 v27。
- v1.27(2026-08-18):**GUI 反馈四项与遗留收尾**(§12.30)——窗口最小尺寸实测防截断、保存按钮 dirty 联动、默认值调整(ProxyTCP 8080/接管与进程重定向 B 预填 192.168.204.1)、多域名/通配符评估补测;runner 孤儿进程任务级清理 + Export-Logs 一键导出。未打包,目标 v28。
- v1.28(2026-08-18):**多域名接管放开 + 域名通配符**(§12.31,用户授权)——删除接管模式仅 api.deepseek.com 的核心硬约束;域名配置新增前导 *. 通配(匹配任意层级子域名、不含裸域),DNS 解析/租约/别名/SNI 全链接入。未打包,并入 v28。

---

## 1. 背景与目标

原版 FakeNet-NG 通过 INI 配置文件驱动,所有配置(含本 fork 新增的全部 `External*` 出站策略功能)都靠手写 INI。本方案新增一个图形配置工具 **fakenet-GUI**:可视化编辑全部配置项(原版 + fork 新增)、实时校验(尊重代码中的硬约束)、并从界面直接启动 FakeNet-NG。

**非目标**:除“当前由本 GUI 启动的 FakeNet 实例日志”外,不做连接、PCAP 或其他运行时监控;不修改 fakenet 核心逻辑(纯增量);不替代 PowerShell 启动器的完整安全流程(见 §10 差异说明)。

## 2. 需求确认记录(grill-me 访谈结论)

| # | 决策点 | 结论 |
|---|---|---|
| 1 | GUI 职责 | 配置编辑器 + 启动器 + 当前 GUI 启动实例的实时日志查看;不监控连接/PCAP |
| 2 | 技术框架 | tkinter/ttk,零新增第三方依赖(适配离线隔离 VM) |
| 3 | 配置范围 | 全量暴露 + 分区组织([FakeNet]/[Diverter] 全部 `External*`/全部监听器类型/自定义响应) |
| 4 | 启动方式 | GUI 直接拉起 `fakenet.exe -c <ini>`;GUI 自带轻量 VM 检测(fakenet 自身无 VM 检测,此为唯一补充的安全网) |
| 5 | 校验哲学 | 智能锁定 + 实时校验:硬锁死字段只读标"代码强制"、条件锁动态切换、格式/范围实时校验、硬错误禁用启动按钮 |
| 6 | 监听器编辑 | 侧边栏段列表 + 动态字段面板(字段随 Listener 类型变化,可多实例/增删复制) |
| 7 | 打包 | 独立 `fakenet-GUI.exe`(自己的 PyInstaller spec、无控制台、不提权);启动时 UAC 提权拉起 fakenet.exe |
| — | 界面语言 | 中文 |

关键澄清(纠正访谈中的一次误判):**fork 的全部新功能都在 `.py` 代码中实现**,`fakenet -c 合法.ini` 即可完整激活,不存在功能被 PowerShell 启动器"独占"的问题;模板 INI 中的 `__RUNTIME_*__`/`__*__` 占位符仅用于打包分发,GUI 产出的是已填实际值的 INI。直接启动相对 PowerShell 启动器**损失运维安全层共 5 项(v0.2 依审计 P3 补全)**:① VM 检测——**GUI 已补**(且不确定态 fail-closed,见 §5.5);② 上游 DNS 探测;③ 停止后 DNS 恢复对比;④ **实例互斥锁**(PS 版有独占锁文件 `Start-DomainAllowList.ps1:199-210`;GUI 以启动前进程探测近似替代,见 §5.5);⑤ **文件日志留存**(PS 版传 `-l` 落盘;GUI 启动路径无)。

## 3. 现状调研摘要(方案依据,均已核实到源码行号)

### 3.1 配置系统
- INI,`ConfigParser` 全默认参数:分隔符 `=`/`:` 皆可(项目惯用冒号 `Key: Value`);键读出即小写;**段名大小写敏感**(`[FakeNet]`/`[Diverter]` 精确匹配,其余任意段名一律视为监听器)。
- 加载链:`fakenet/fakenet.py:72-113 parse_config()` 分桶(仅收录 `Enabled` 为真的监听器段)→ `expand_listeners()`(端口区间 `60000-60010` 展开为 `<段名_端口>` 多实例)→ 注入 `ipaddr/configdir/networkmode` → 各监听器收普通 dict。
- 路径语义:`Custom:` 相对 CWD/主配置目录(`qualify_file_path`);`WebRoot`/`FTPRoot`/CA 等走 `abs_config_path`(CWD → exe 目录或 fakenet 包父目录)。

### 3.2 布尔三套语义(GUI 写文件必须精确产出对应字面量)
1. `Enabled`(监听器段):`configparser.getboolean()`,仅 `1/yes/true/on/0/no/false/off`;**键缺失即崩溃**。
2. fuzzy 布尔(`is_set`/`is_clear`,fnconfig.py:66-70):真词 `yes/on/true/enable/enabled`,假词 `no/off/false/disable/disabled`,**不含 1/0**。
3. 精确字符串比较:`DivertTraffic` 只认小写比较 `'yes'`;`UseSSL` **精确匹配大写 `'Yes'`**(各监听器);`Hidden` 只认 `.lower()=='true'`。

规范化策略:GUI 统一产出——`Enabled`→`True/False`,fuzzy/`DivertTraffic`→`Yes/No`,`UseSSL`→`Yes/No`,`Hidden`→`True/False`。

### 3.3 硬约束清单(校验器与锁定的依据)
- **始终硬锁死**(`egresspolicy.py:430-475`):`ExternalAllowedTCPPorts` 恰为 `{443}`;`ExternalVerifyTLSSNI/ExternalBlockExternalIPv6/ExternalBlockQUIC` 必须 Yes;8 项资源限额元组必须等于 `(5, 65536, 256, 32, 128, 16, 300, 1048576)`;进程重定向协议必须 `TCP`(`processredirect.py:682-684`)。
- **条件锁**:takeover 启用时域名仅 `api.deepseek.com`、`ExternalNonAllowedAction=Divert`、两个 DNS 段 `ResponseA == ExternalTakeoverIPv4`(egresspolicy.py:495-504、windows.py:608-613)。
- **范围/格式**:TTL 1–300、探测端口≤64、探测超时 100–5000、DnsTimeout 1–30、RelayPort 1–65535;域名 IDNA;IPv4 按 RFC1918/全球单播分类;SHA256 为 64 hex 且须与实盘文件一致;进程镜像路径须绝对盘符、禁 UNC/ADS/通配。
- **跨段拓扑**(DomainAllowList 模式,windows.py:585-617):恰 1 个 `DomainEgressRelay` 监听器(TCP,Port=`ExternalRelayPort`)+ 恰 2 个 53 端口 `DNSListener`(一 UDP 一 TCP)+ 其他监听器不得占用中继端口。
- **互斥**:`ProcessWhiteList`/`ProcessBlackList`、`HostWhiteList`/`HostBlackList`(监听器级与 Diverter 级);`RedirectAllTraffic` 开启时 `DefaultTCP/UDPListener` 必须引用已存在段名。
- **平台**:全部 `External*` 策略 Windows-only;Linux 对 Reviewed IPv4 规则/进程重定向直接拒绝。

### 3.4 打包与构建
- `fakenet.spec`:onefile、`console=True`、`uac_admin=True`、WinDivert 驱动随包;tkinter 是在 Analysis 后用旧式 `TOC` 减法剔除(PyInstaller 6 已移除该语法)。
- CI(`.github/workflows/build.yaml`):windows-2022 + Python 3.10 + 最新 PyInstaller;release zip 内 `fakenet.exe` 与 `fakenet\configs\`(模板来源)、`fakenet\defaultFiles\` 同级。
- `setup.py` entry_points:`fakenet=fakenet.fakenet:main`。
- 项目现有 ctypes 先例(`winutil.py` adapter 模式、`IsUserAnAdmin`)与 PowerShell 子进程先例(`windows.py:624-629`,`-NoProfile -NonInteractive -ExecutionPolicy Bypass` + `CREATE_NO_WINDOW`);`ShellExecuteW runas` 为新引入。

### 3.5 其他已核实的坑
- 值中裸 `%` 会被 BasicInterpolation 解析崩溃,必须写 `%%`。
- fakenet 读 INI 不带 encoding → 按系统区域编码(中文 Windows 为 GBK);GUI 写 UTF-8 中文注释会产生乱码,含非 ASCII 的值有损坏风险。
- `Proxy` 段的 `Listeners:` 键是死代码(ProxyListener 实际嗅探全部监听器)。
- 系统注入键 `ipaddr/configdir/networkmode` 不应出现在 INI。

## 4. 总体架构

```
fakenet/gui/                 # 新 Python 包(GUI 运行时 = 纯标准库,不 import 项目代码)
├── __init__.py
├── main.py        # main() 入口: DPI 设置、Tk root、异常兜底            (~30 行)
├── schema.py      # 声明式字段目录(纯数据,无 tkinter)                  (~600–800 行)
├── configmodel.py # INI 保真读写模型                                     (~280 行)
├── validator.py   # 校验规则引擎 → (error|warning, 段/字段定位, 消息)    (~450 行)
├── widgets.py     # 字段控件工厂 + 动态表单                              (~300–400 行)
├── app.py         # 主窗口:菜单/标签页/侧边栏/校验面板/启动按钮          (~1200–1500 行)
└── launcher.py    # VM 检测 + 目标定位 + UAC 提权启动                    (~140 行)
fakenet-config.spec          # 新 PyInstaller spec
test/test_gui_schema.py / test_gui_configmodel.py / test_gui_validator.py / test_gui_launcher.py
修改: setup.py(gui_scripts 入口)、.github/workflows/build.yaml(构建第二 exe)、README.md
```

**运行时依赖分界**:fakenet-config.exe = 纯标准库(tkinter + ctypes + subprocess);测试 = 完整项目依赖(可 import `fakenet.fakenet` 做往返验证)。

## 5. 模块设计

### 5.1 schema.py — 声明式字段目录
每字段一条记录:`key / 中文标签 / 控件类型 / 默认值 / 约束(min/max/枚举/regex/IP类别) / 锁定规则ID / 所属分组 / 提示`。控件类型枚举:`bool_yesno`、`bool_truefalse`、`int`、`string`、`enum`、`stringlist`、`portlist`、`portspec`(区间感知)、`ipv4`、`path_file`/`path_dir`(带浏览按钮)、`hex64`、`text`(多行,ExecuteCmd)。

内容:
- `[Diverter]` 全字段分 6 组:基础(NetworkMode/DebugLevel 值域 16 标签)、抓包(DumpPackets 双 PCAP/前缀)、DNS与网关修复(FixGateway/FixDNS/ModifyLocalDNS/StopDNSService)、重定向与黑名单(RedirectAllTraffic 联动、BlackListPortsTCP/UDP、**BlackListIDsICMP[Windows-only idlist,v0.2 补 P5:代码实现于 diverterbase.py:577,1350-1354,但 default.ini:158 仅注释样例]**、名单互斥)、Linux 专用 3 项、出站策略四个子组(基础策略/私网接管/公网 IPv4 放行/进程重定向,含全部 `External*` 25+ 项)。
- 锁定规则 ID 化:`LOCK_TCP_PORTS_443`、`LOCK_BOOL_YES×3`、`LOCK_RESOURCE_TUPLE`、`LOCK_PR_PROTOCOL_TCP`;条件锁 `COND_TAKEOVER_*`(仅 DomainAllowList + takeover 时生效)。**`External*` 全部字段仅在 `ExternalAccessPolicy=DomainAllowList` 时参与校验,Disabled 时灰显不校验**(与源码一致)。
- 监听器:共有 9 字段(Enabled/Port/Protocol/Listener 可选=匿名段/Hidden/两对互斥名单/ExecuteCmd)+ 10 类特有字段目录(DNS/HTTP/FTP/IRC/TFTP/Raw/SMTP/POP/Proxy/DomainEgressRelay),含陷阱标注(UseSSL 精确 'Yes'、Proxy `Listeners:` 仅文档、Hidden 只认 'true')。**盲区键补充(v0.2,P5,均为"代码实现但模板缺省/注释"键)**:DNS 的 `ResponseA` 按"字面 IPv4 ∪ 特殊枚举 `GetFirstNonLoopback`/`GetHostByName`"双态定义(DNSListener.py:224,232);`cert_dir`(UseSSL 动态证书目录,默认 `configs/temp_certs`,HTTPListener.py:217)列为高级已知字段。
- **值保真规则(v0.2,P5/F5)**:`\r\n`、`\t` 等转义序列是配置值的一部分(FTP Banner、自定义响应样例均使用),GUI 编辑时按字面处理,不得解释、转换或规范化。
- 自定义响应:已知字段目录(HTTP 型:HttpURIs/HttpHosts 至少其一、响应体三选一、ContentType 限制;Raw 型:Tcp/Udp 前缀四选一)。**平铺展示全部已知字段,不做交互式变体切换**(评估轮砍掉的简化)。
- 排除系统注入键(ipaddr/configdir/networkmode),不渲染。

### 5.2 configmodel.py — 保真读写
- **读**:裸 `ConfigParser` 读**全部**段(含 `Enabled=No` 的——`parse_config()` 会丢弃它们,编辑器必须保留);按文件顺序存 OrderedDict;schema 外的未知键进"额外键"区,**不丢任何数据**。
  - **读取编码策略(v0.1 新增,U2)**:以 `utf-8-sig` 优先尝试,解码失败回退系统区域编码(与 fakenet 读取语义一致)——防止 UTF-8 BOM 文件首段名混入 BOM 字符、或 GBK/UTF-8 混存文件被读错后在保存时静默损坏。非 ASCII 值一律交校验器出警告(§5.3)。
- **写**(严守 §3.2/§3.5):冒号风格;段名 `[FakeNet]`/`[Diverter]` 精确大小写;布尔按 §3.2 规范化字面量;`%`→`%%`;每监听器段必写 `Enabled`;无重复段/键;文件头 **ASCII 英文**注释。
  - **ASCII 责任边界(v0.1 消解 R2)**:GUI **新写入/编辑产生的值**保证 ASCII;**源文件既有的非 ASCII 值原样保真保留**(不转码、不丢弃,校验器警告)。"输出 ASCII-only"断言仅适用于 ASCII 源文件;非 ASCII 源文件的断言是"保真保留"(见 §6)。
- **写出编码规范(v0.2,P2,四项)**:① **保存编码 = 读取时探测到的源编码**(GBK 源写 GBK、utf-8-sig 源写 utf-8-sig),落实"非 ASCII 值不转码";② **BOM 原样保留**(utf-8-sig 读入的文件写回仍带 BOM)——带 BOM 文件在中文系统会被 fakenet 以 GBK 读取、BOM 三字节混入首段名导致解析错乱,故校验器对 BOM 文件出警告;③ **新建文件缺省**:纯 ASCII、无 BOM;④ **行尾风格保真**:CRLF/LF 按源文件保留,新建文件用 CRLF(Windows 平台)。
- **保存前外部修改防护(v0.1 新增,R1)**:保存时比对文件 mtime 与载入时快照,不一致则提示"文件已被外部修改",由用户选择覆盖或重新载入,防并发编辑丢失。
- 端口区间原样保留 + "将展开为 N 个实例"预览。

### 5.3 validator.py — 规则引擎
输入配置模型,输出 `(error|warning, 段/字段定位, 消息)` 列表;**errors 非空 → 禁用启动按钮**;字段变更 debounce 重跑;状态栏"⚠N 错误 / M 警告"。

规则(errors,按 §3.3 全量实现):
1. 段名与 `[fakenet]`/`[diverter]` 大小写不敏感相等但非精确匹配 → 错误(否则 fakenet 当监听器段崩溃)。
2. 监听器段必备 `Enabled/Port/Protocol`;`Protocol∈{TCP,UDP}`;`Listener` 值为合法类名(大小写敏感);端口可解析 1–65535;端口展开实例彼此及与既有段名无冲突;**展开后启用段之间 (Protocol, Port) 去重(v0.2,P4)——同协议同端口双绑定时 fakenet 运行时 bind 失败即 `sys.exit(1)` 整体退出(实证 fakenet.py:266-274 为 fail-fast,非"记错继续"),必须在启动前拦截**。
3. 名单互斥(监听器级 + Diverter 级);`RedirectAllTraffic` 开 → `DefaultTCP/UDPListener` 引用存在段名;`DivertTraffic=Yes` → `NetworkMode` 必填。
4. DomainAllowList 模式:`DivertTraffic` 必须 'yes';拓扑硬约束(§3.3 跨段拓扑);13 项硬锁死字段复核(UI 锁定为第一道,校验器复核为第二道——防"外部改文件再载入")。
5. takeover:IPv4 RFC1918 可用/非本地/≠DNS;TTL/探测端口/超时范围;域名条件锁;DNS ResponseA 联动。
6. Reviewed IPv4 规则:三段式语法/全球单播/≤32 条 16 IP/存在即非空/无重复/不与本地冲突。
7. 进程重定向:开启时 5 项全必填;协议 TCP;路径绝对盘符且存在;SHA256 64hex;A 全球单播、B RFC1918、A≠B、不与 DNS/takeover 冲突。
8. 通用:域名 IDNA 合法;`ExternalDnsServer=Auto` 或可用单播 IPv4;`__*__` 占位符 → "请填入实际值";ExecuteCmd 占位符(`{pid}` 等)合法。

规则(warnings,不阻断):Webroot/Custom/FTPRoot/TFTPRoot/CA 等路径不存在;DumpPackets 开而未设前缀;非 ASCII 值("fakenet 按系统区域编码读取,可能乱码");Linux 专用项配在 Windows 场景等。

**拓扑自动补齐**:DomainAllowList 激活时按 `domain_allowlist_windows.ini` 自动创建缺失的 relay + 2×DNS 段,并在 takeover 启用时把 DNS 段 `ResponseA` 填为接管 IP(已有值不动);不要求用户额外点击。

### 5.4 app.py — 主窗口(中文 UI)
- **菜单**:文件(新建/打开/保存/另存为);**模板载入动态枚举配置目录生成,不硬编码清单(v0.2,P12)**——dev 枚举仓库 `fakenet/configs/`(7 个主配置模板),冻结态枚举 exe 旁 `configs\`(release zip 剔除 burp/debug 后实为 5 个,缺失项不渲染);**排除 `sample_custom_response.ini`**——其 [Example*] 段无 Enabled 键,误作主配置模板载入会被 fakenet 当监听器段直接崩溃(正确性要求,非仅数量问题)。
- **标签页**:
  - 「全局」:[FakeNet] + [Diverter] 基础四组;
  - 「出站策略」:四个子框;🔒只读字段灰显 +「代码强制」标注;条件锁动态切换;启用策略时自动补齐必需监听器;
  - 「监听器」:左侧段列表(新增[类型下拉 10 类]/删除/复制/重命名)+ 右侧动态面板(共有 + 类型特有 + 额外键)+ 端口展开预览;
  - 「自定义响应」:管理 `Custom:` 引用的响应 INI(段列表 + 已知字段表单 + 额外键)。
- **底部**:校验面板(红=错误/黄=警告,双击跳转对应字段;跳转管线若超预算,首版降级为消息含 段/字段 定位)+ [保存配置] [▶ 启动 FakeNet-NG]。
- 入口调用 `SetProcessDpiAwareness`(高 DPI 清晰;失败静默)。

### 5.5 launcher.py — 启动流程
1. 校验通过(0 错误)→ 未保存则提示保存(另存为默认 exe 旁 `configs\`,不可写退用户目录);**启动前 fakenet.exe 进程探测(v0.2,P3)**:`tasklist /FI "IMAGENAME eq fakenet.exe"` 发现已运行实例即拒绝启动——PS 启动器有独占锁文件(`Start-DomainAllowList.ps1:199-210`,FileShare::None),GUI 以进程探测近似替代,防双 WinDivert 句柄 + 双重 DNS 改写的未定义行为(ShellExecuteW 不返回进程句柄,GUI 不做运行期管理)。
2. **VM 检测**(沿用 `windows.py:624` PowerShell 先例,`Start-*.ps1:31` 同款正则 `virtual|vmware|virtualbox|kvm|qemu|hyper-v|xen|parallels` 匹配 `Win32_ComputerSystem` Manufacturer/Model),**后台线程执行,不得冻结 UI 线程(v0.2,P7:CIM 查询秒级耗时)**。**不确定态裁决(v0.2,采纳审计 F1):fail-closed**——VM→放行;物理机→拒绝并红色警告;**不确定(PS 失败/超时)→ 拒绝启动,提示改用 PowerShell 启动器或修复 WMI/PowerShell 后重试**。与全仓 fail-closed 姿态(Start-*.ps1 `$ErrorActionPreference='Stop'`、PolicyConfigError 体系)对齐;WMI 损坏场景下 PS 启动器同样失败(其 720-724 行同样依赖 CIM),不产生 GUI 可用性倒退。
3. 定位目标:冻结 → 同目录 `fakenet.exe`;缺失时弹出路径选择,允许手工指定(复用 path_file 控件);**持久化到 `%APPDATA%\fakenet-config\settings.json`,持久化路径无效时回落同目录探测并提示(v0.2,P11,修正 v0.1"优先于同目录探测"的过强表述)**;**dev → `[sys.executable, '-m', 'fakenet.fakenet']` 且 cwd=仓库根(v0.2,P1 修正:直接跑 `fakenet/fakenet.py` 脚本不可用——fakenet.py:33 `from fakenet import listeners` 在脚本模式(sys.path[0]=包目录)下 ModuleNotFoundError,且开发机若曾 `pip install .` 会静默 import site-packages 旧副本造成版本错配;`-m` 形式与 `Start-DomainAllowList.ps1:286` 项目先例一致)**。**配置路径含引号/控制字符/尾反斜杠时拒绝启动**(防 ShellExecuteW 参数注入;v0.2,P9 补尾反斜杠:CommandLineToArgvW 语义下"尾反斜杠+闭引号"会把引号转义为字面量)。
4. UAC 提权:`ctypes.windll.shell32.ShellExecuteW(0,'runas',exe,'-c "<绝对路径>"',dir,SW_SHOWNORMAL)`(按 `winutil.py` 风格声明 argtypes);**检查返回值(v0.2,P6):≤32 为失败,`SE_ERR_ACCESSDENIED(5)`=用户取消 UAC——失败/取消时不得提示"已启动"**;成功后提示"已启动;后续配置修改不影响运行中实例(fakenet 启动时一次性读取配置)",fakenet 控制台独立运行,用户在其中 Ctrl+C 停止。
5. **Linux:不提供启动**,提示手动命令 **`python -m fakenet.fakenet -c <ini>`**(v0.2,P1:脚本直跑形式不可用,理由同第 3 条;评估轮砍掉 best-effort 终端探测)。

### 5.6 打包与构建
- `fakenet-config.spec`:现代语法(不用 PyInstaller 6 已移除的 `TOC` 减法);**不排除 tkinter**;`console=False`、不设 `uac_admin`、`icon='resources/fakenet.ico'`、onefile、`name='fakenet-config'`;Analysis 仅指向 `fakenet/gui/main.py`(`pathex=['fakenet']`),不含 WinDivert/diverters。不动现有 `fakenet.spec`。
- CI:构建 fakenet.exe 后追加 `py -3.10 -m PyInstaller fakenet-config.spec`,产物拷入 `fakenet_release\fakenet-config.exe` 并进 zip(与 fakenet.exe 同级——启动器默认探测布局;F7 升级后用户亦可手工指定)。**步骤隔离(v0.1,B2)**:新 spec 构建失败**不得阻断 fakenet.zip 主产物发布**——独立 step,首版验证期失败仅告警/标记(如 `continue-on-error`),稳定后收紧为强制;**拷贝/入 zip step 同门禁(v0.2,P10):以 `if: steps.<build>.outcome == 'success'` 跳过,CI 不因产物缺失额外飘红**。
- `setup.py` **两处改动(v0.1 修正 U1/B1,已实证)**:① `package_data` 增加 `'gui/*.py'`——现 `packages=['fakenet']` 只含顶层包,子包全部依靠 package_data 通配装运(`diverters/*.py`、`listeners/*.py` 即此模式),不加则 pip 安装后模块缺失、`fakenet-config` 入口一运行即 ImportError;② 增加 gui_scripts `fakenet-config=fakenet.gui.main:main`(Windows 下 pip 安装生成 pythonw 无控制台入口;无 tkinter 环境运行时报清晰错误)。

## 6. 测试与验证(pytest,纯逻辑,CI 可跑)

| 文件 | 内容 |
|---|---|
| `test_gui_schema.py` | schema 完备性:8 个 `configs/*.ini` 的每个键被 schema 覆盖;锁死值与 `egresspolicy.py` 期望元组一致;**枚举 `fakenet/listeners/*.py` 断言监听器类全覆盖**(防清单漂移);**端到端锚定(v0.1 补强 F8)**:4 个功能 profile 填掉 `__*__` 占位符后经校验器为 0 错误 |
| `test_gui_configmodel.py` | 载入 8 个 INI → 保存 → **两层断言(v0.1 修正 U5)**:① fakenet 视角——产物经 fakenet 自己的 `parse_config()+expand_listeners()` 重读,启用段的段/键/端口展开一致且无异常;② 编辑器视角——全部段(含 `Enabled=No`)在产物中保真保留。另有:`%%` 转义往返;三套布尔字面量断言;未知键保留;段名大小写规则;读取编码策略(utf-8-sig/BOM 处理);ASCII 断言按 §5.2 责任边界拆分(ASCII 源 → 产物 ASCII;非 ASCII 源 → 保真保留);mtime 外部修改防护 |
| `test_gui_validator.py` | 逐规则单测(锁定/拓扑/takeover 联动/IPv4 分类/互斥/占位符/注入拒绝等);场景:载入含 `__*__` 的 `process_redirect_windows.ini` → 报"请填入实际值" |
| `test_gui_launcher.py` | VM 检测三态解析(模拟 Start-*.ps1 真实输出串);目标定位;路径注入拒绝 |

tkinter 界面仅做导入冒烟(`import fakenet.gui.app`,无 tkinter 环境自动 skip);CI 不开窗;**schema/configmodel/validator/launcher 测试一律不 import tkinter**(v0.1,B4)。

验收环境(v0.1 修正 U3/O2):**提权启动与 VM 三态的真实验收必须在 VM 内进行**——物理机上的"提权启动"会被方案自身的 VM 检测拒绝,不可也不应绕过;本机仅做无副作用部分(构建 exe、载入 default.ini 编辑保存、fakenet 离线重读往返)。本机当前无 PyInstaller(已实证),实施前需先备好工具链(Python 3.10 + tcl/tk + PyInstaller);跑 GUI 测试需完整项目依赖(dpkt/dnslib 等,仓库 wheelhouse 可离线安装)。

## 7. 风险与对策
- **PyInstaller 版本**:CI 装最新版;新 spec 全现代语法;不动旧 spec。
- **tkinter 可用性**:windows-2022 + Python 3.10 自带 tcl/tk;Linux 缺 tkinter 时入口清晰报错。
- **约束双源**(schema 与 egresspolicy.py 各持一份):测试锚定(schema 锁死值断言 == 源码期望元组);fakenet 启动时仍是终审,GUI 校验仅前置拦截。
- **编码**:输出 ASCII 化 + 非 ASCII 值警告(§5.3);读取侧 utf-8-sig/区域编码回退(§5.2)。
- **杀软/EDR 误报**(v0.1):无签名 PyInstaller exe + `runas` 提权是 EDR 重点盯防组合,分析 VM 内可能被拦——README 提示放行/白名单方式。
- **GUI 与 fakenet.exe 版本错配**(v0.1):旧 exe 会静默忽略新配置键(未知键不报错)——README 提示两者使用同一构建产物。
- **Build-*.ps1 全树收集连带**(v0.1,B3):已实证 Build 脚本以 `git archive` 全树收集——合入后未来构建的功能包会自动带上 `fakenet/gui/` 源码(manifest 构建时现算,不自相矛盾,但包内容集变化);实施前验证 `test_process_redirect_vm_contract.py` 对包内容无精确断言。
- **运维提示类**(v0.1):精简 VM 缺中文字体 → 中文 UI 显示方块;onefile 首启有 2–5 秒解压延迟——均写入 README。

## 8. 评估结论

### 8.1 首轮自评(2026-08-14,v0)
- **低估项 6 处已对策化**:GBK 编码陷阱、app.py 体量上调(650→~1000 行)、VM 检测不确定态定义、段名大小写校验、高 DPI、测试构造细节。
- **砍 2 项过度设计**:Linux best-effort 启动 → 手动命令提示;自定义响应变体动态切换 → 平铺表单。
- **闭环补 3 缺口**:监听器类清单漂移测试锚定;一键补齐联动填 ResponseA;启动后编辑不影响运行实例的 UI 明示。
- **消解 2 矛盾**:中文 UI vs ASCII 文件输出;GUI 纯标准库运行时 vs 完整依赖测试环境。

### 8.2 重评估轮(2026-08-14,v0 → v0.1)
无"高"风险项;6 个"中"风险项已全部并入本版:
1. **U1/B1** setup.py 打包缺口(实证)→ §5.6 修正(`gui/*.py` 入 package_data)。
2. **U2** GUI 读取编码策略缺失 → §5.2 读取策略(utf-8-sig 优先、区域编码回退)。
3. **U3/O2** 验收环境自相矛盾(物理机无法验收提权启动)→ §6 验收明确 VM 内进行。
4. **B2** CI 新构建步骤失败会阻断主产物发布 → §5.6 步骤隔离。
5. **R2** "输出 ASCII-only"断言与无损往返承诺矛盾 → §5.2 ASCII 责任边界 + §6 两层断言拆分。
6. **F7** 偏弱 → 升级为"报错 + 手工指定 fakenet.exe 路径(持久化)"。

另并入低风险补强:体量估计上调(U4)、往返测试两层断言(U5)、测试不 import tkinter(B4)、mtime 并发编辑防护(R1)、端到端锚定用例(F8 补强)、README 运维提示(杀软误报 R3/版本错配 R5/中文字体 R6/启动延迟 R4)、Build 脚本全树收集连带验证(B3)、本机工具链准备(O1)。

fallback 复核(F1–F10):9 项维持原判合理,**F7 升级**;F1(VM 检测不确定态策略)仍列为第三方审核重点裁决项。

### 8.3 第三方审计轮(2026-08-14,v0.1 → v0.2)
审计文档:`2026.08.14-02-审计-GUI配置界面方案.md`(含方案方回应附录)。裁决:**P1–P12 全部采纳**(P4 论据经独立实证被勘误——源码 fakenet.py:266-274 为 `sys.exit(1)` fail-fast 而非"记错继续",勘误记录于审计文档附录 §八;结论不受影响反而更强);**F1 采纳审计裁决改判 fail-closed**;审计 §五 DoD 建议采纳。13 处修订落文:§2 安全差异清单补 2 项(实例互斥/文件日志)、§5.1 盲区键 3 个 + 值保真规则、§5.2 写出编码四项、§5.3 启用段 (Protocol,Port) 去重、§5.4 模板动态枚举、§5.5 六项修订(进程探测/fail-closed/后台线程/`-m` 命令/返回值/尾反斜杠)、§5.6 拷贝步骤门禁、§9 三行(F1/F4/F7)、§11 逐阶段 DoD。无整体驳斥项;P12 计数口径澄清见审计附录。

## 9. 未经显式确认的 fallback / 默认采纳项(待审核裁决,见 §10)
以下各项在访谈中**未获用户显式确认**,由方案自行设定,需审核时逐项裁决:

| # | 项 | 方案取值 | 备选 |
|---|---|---|---|
| F1 | VM 检测不确定态(PS 失败/超时) | **(v0.2 采纳审计裁决)fail-closed:拒绝启动,提示改用 PowerShell 启动器或修复 WMI 后重试** | ~~警告 + 二次确认放行~~(v0/v0.1 取值,审计驳斥后弃用:与全仓 fail-closed 姿态相悖,见 §5.5) |
| F2 | 校验面板双击跳转字段 | 核心目标;若实现超预算降级为消息含"段/字段"定位 | 不设跳转 |
| F3 | 保存位置 | 默认 exe 旁 `configs\`,不可写退用户目录 | 强制用户目录 |
| F4 | Linux 启动 | 不提供,提示手动命令 **`python -m fakenet.fakenet -c <ini>`**(v0.2,P1 修正命令形式) | best-effort 终端探测启动 |
| F5 | 自定义响应编辑形态 | 平铺全部已知字段表单 | 交互式 HTTP/TCP/UDP 变体切换 |
| F6 | 高 DPI 调用失败 | 静默忽略(界面可能模糊) | 报错提示 |
| F7 | fakenet.exe 缺失(冻结态) | **(v0.2 细化 P11)报错 + 手工指定路径;持久化 `%APPDATA%\fakenet-config\settings.json`,持久化值无效时回落同目录探测并提示** | ~~仅报错指引~~(v0)/~~优先于同目录探测~~(v0.1,表述过强,审计 P11 修正) |
| F8 | GUI 不复用项目解析/校验代码,自建 schema(双源真相) | 接受,测试锚定 | GUI import 项目代码(拖入 pydivert 等,exe 变重) |
| F9 | 界面语言=中文 | 访谈问询未获答复,按判断采纳 | 英文/双语 |
| F10 | 校验哲学=智能锁定+实时校验 | 访谈谈询未获答复,按推荐采纳 | 全可编辑仅警告 / 完全不校验 |

## 10. 审核重点(第三方审核请优先核查)
1. **双源真相一致性(最高风险)**:schema.py 的约束/锁定清单是否与 `egresspolicy.py`/`processredirect.py`/`windows.py` 逐一对应?§6 的锚定测试是否足以防漂移?有无遗漏的硬约束(对照 §3.3)?
2. **INI 写出兼容性**:§3.2 三套布尔、`%%` 转义、GBK 编码、段名大小写、`Enabled` 必写——还有没有未列出的读写陷阱?(可对照 `fnconfig.py`、各 Listener 源码复核)
3. **闭环完整性**:载入→编辑→校验→保存→启动全链路有无缺口?占位符流程、拓扑一键补齐、启动后编辑提示是否完备?
4. **范围合理性**:§9 F4/F5 两处砍项是否正确?"全量暴露"粒度(含额外键保真)是否过度?
5. **安全性**:VM 检测绕过面;`ShellExecuteW` 参数注入防护(§5.5 第 3 条)是否充分;GUI 不提权而启动提权的权限边界。
6. **打包与 CI**:PyInstaller 6 语法兼容、tkinter 纳入、双 exe 同级布局约束、gui_scripts 入口。
7. **§9 全部 10 项 fallback/默认项逐项裁决**(尤其 F1 的不确定态策略与 F8 的双源取舍)。
8. **(v0.1)修订项自身复核**:§5.2 读取编码回退次序(utf-8-sig 优先、区域编码兜底)是否会在 GBK/UTF-8 混存场景引入新误读;§5.6 `package_data` 通配与 `include_package_data=True` 的相互作用是否真正覆盖 `fakenet/gui`;§5.6 CI 步骤隔离(continue-on-error)是否会放行残缺 GUI 产物进 zip;§6 两层断言划分是否完备。

## 11. 实施顺序与逐阶段 DoD(v0.2,采纳审计 §五)
schema → configmodel → validator(同步测试)→ widgets + app → launcher → spec/setup/CI → 本机无副作用验证 → **VM 内验收** → README。

**每阶段完成定义(DoD)= 对应测试全绿 + 单独成组提交**:

| 阶段 | DoD | 提交切分 |
|---|---|---|
| schema | `pytest test/test_gui_schema.py` 全过(§6 全部锚定断言,含盲区键) | commit 1 |
| configmodel | `pytest test/test_gui_configmodel.py` 全过(两层断言/读写编码规范/`%%` 转义/mtime) | commit 2 |
| validator | `pytest test/test_gui_validator.py` 全过(§5.3 全规则,含 (Protocol,Port) 去重) | commit 3 |
| widgets + app | `import fakenet.gui.app` 冒烟 + 本机手工载入 default.ini 编辑保存往返 | commit 4 |
| launcher | `pytest test/test_gui_launcher.py` 全过(三态 fail-closed/注入拒绝/返回值/进程探测) | commit 5 |
| spec/setup/CI | 本机构建出 fakenet-config.exe 并无提权启动冒烟;`pip install .` 后入口可用(P1/U1 验证) | commit 6 |
| 本机无副作用验证 | 命令集:`pytest test/test_gui_*.py`;`pyinstaller fakenet-config.spec` 冒烟;`pytest test/test_process_redirect_vm_contract.py`(B3 核查 gui 文件入包无碍) | commit 7 |
| VM 内验收 | VM 三态(VM 放行/物理机拒绝/不确定 fail-closed)、提权启动冒烟、UAC 取消路径、双实例拒绝 | commit 8(证据) |
| README | 「GUI 配置工具」章节,含与 PowerShell 启动器安全差异(5 项,见 §2)及杀软/版本配套/中文字体/启动延迟运维提示 | commit 9 |

**前提(审计 §五)**:P1/P2/P3 修订已在本版落定,launcher 与 configmodel 阶段不再按错误规格实现。

---

## 12. 实施与落地记录(2026-08-14,v1.0)

### 12.1 落地清单(对照 §4/§5/§6/§11)

| 方案条目 | 落地 | 提交 |
|---|---|---|
| schema.py 字段目录(6 组 Diverter/10 监听器/自定义响应/锁定规则) | ✓ | `378518b` |
| configmodel.py 保真读写(读全部段/三套布尔/%%/编码/BOM/行尾/mtime/Enabled 必写/端口展开) | ✓ | `8352a10` |
| validator.py 规则 1–8 + 自定义响应校验 + 一键补齐(联动 ResponseA) | ✓ | `bb638ac` |
| launcher.py(VM 三态 fail-closed/双实例门/注入拒绝含尾反斜杠/设置持久化+回落/ShellExecuteW 返回值/-m 命令) | ✓ | `769a26a`+`7c68646` |
| widgets.py + app.py + main.py(四标签/🔒锁定/条件锁/跳转/防抖/模板动态枚举/DPI) | ✓ | `4b84a75` |
| fakenet-config.spec + setup.py(package_data+gui_scripts) + build.yaml(隔离构建+Test-Path 门禁) | ✓ | `c9fdaed` |
| 测试:schema 锚定(含端到端 4 profile)/configmodel 两层断言/validator 逐规则/launcher 三态/冒烟 | ✓ 98 项 | 各阶段 |
| 本机验证:全量 324 通过零回归;B3 契约 15 通过;PyInstaller 6.22 构建冒烟通过 | ✓ | `2276e88` |
| README「GUI 配置工具」章节(含 5 项安全差异与运维提示) | ✓ | `2e6f217` |
| 终检补齐(DumpPackets 前缀警告 + 新增监听器类型下拉,§5.3/§5.4 承诺) | ✓ | `0f923fc` |
| **VM 内验收(VM 三态/提权启动/UAC 取消/双实例拒绝)** | ✓ 九项全过(A1–A9:7 实测 + 2 等效),证据 `dist\Logs\20260814-181250\results.tsv` | `53d0b6d`(包)+ VM 执行 |

最终本机测试:全量套件 **325 passed + 18 subtests, 0 failed, 零回归**(基线 226)。

### 12.2 与方案的偏离(已确认的顺序调整)
- **commit 4/5 顺序互换**:先 launcher 后 widgets+app——保证每个提交自包含(app 依赖 launcher),DoD 不变。

### 12.3 超出方案文本的改动(原因与风险评估)
1. **configmodel 读入时防御性剥离双 BOM**(测试构造双 BOM 文件时暴露的崩溃路径)。风险:无——仅剥离 \ufeff 残留字符,正常文件零影响。
2. **launcher.is_fakenet_running 改字节比较**(实测发现:中文 Windows 上 tasklist 输出 GBK,UTF-8 模式 reader 线程崩溃导致 stdout=None)。风险:无——镜像名为 ASCII,字节比较编码免疫。
3. **schema 增加 DNSResponse 废弃键**(burp.ini 含此历史拼写,代码不读取;为满足"8 个 INI 每键被 schema 覆盖"锚定)。风险:极低——标注"仅保真",不产生误导。
4. **validator 锁定字段缺省语义**(缺键按 egresspolicy 默认值判定,唯 ExternalAllowedTCPPorts 必须显式 443)——方案文本未细化,实施时对齐源码。风险:无——与 egresspolicy.py 逐行对齐,端到端锚定测试覆盖。
5. **validator 增加裸 % 值警告**(§5.2 写侧转义的配套提示)。风险:无。
6. **policy=Disabled 时对已填 takeover IP 做基础校验**(提前暴露非法值)。风险:极低——仅报错不做转换。
7. **test_gui_smoke.py 独立文件**(方案将导入冒烟记在 §6,未指定文件);**.gitignore 增加 spec 例外**(gitignore 的 `*.spec` 规则挡新 spec,CI 需要)。风险:无。

### 12.4 遗留事项(2026-08-14 v1.1 全部关闭)
- **VM 内验收:已执行,九项全部 PASS。** 环境:VMware 虚拟机(VMware, Inc. / VMware20,1),包 `Windows-GUI配置工具-VM验收-v1.zip`(提交 `53d0b6d`),一键 `Run-Tests.cmd`,全程免操作。证据已导出 `dist\Logs\20260814-181250\`(results.tsv 九行全 PASS;fakenet.log 三步链路"加载 GUI 写出配置 → 注册停止旗标 → 旗标触发 Stopping... 优雅退出";smoke_config.ini 为 GUI 写出格式)。覆盖:VM 判定放行(实测)、物理机拒绝分类(等效)、不确定态 fail-closed(实测,剥离 PATH)、UAC 取消/失败分支(等效)、配置写入+校验一致(实测)、提权启动真实 fakenet.exe+日志落盘(实测)、双实例门(实测)、停止旗标优雅退出(实测)、GUI exe 启动冒烟(实测)。**至此方案无遗留事项。**

### 12.6 验收后用户需求迭代(2026-08-14,v1.2)
1. **exe 改名 `fakenet-GUI.exe`**(用户需求):全链路同步——spec 文件改名 `fakenet-gui.spec`(name 同步)、setup.py gui_scripts 入口改 `fakenet-gui`、CI 构建/拷贝步骤、.gitignore 例外、打包脚本(manifest 键改 `fakenet_gui_exe_sha256`,包版本 v3)、VM 验收 runner、README、`%APPDATA%\fakenet-gui` 设置目录(旧持久化设置弃置无害)、各 docstring/用户提示。刻意不改:fakenet.fakenet 自身的 `fakenet_config` 属性([FakeNet] 段字典)、建议配置保存名 `fakenet_config.ini`、PLAN/ 历史文档。提交 `1b6c8a9`。
2. **校验面板复制与不截断**(用户需求):消息完整文本始终存于 Treeview item values(复制永不截断);消息列 minwidth=1600 + 横向滚动条(显示不再截断)+ 纵向滚动条;右键菜单/Ctrl+C/顶部「复制选中」「复制全部」按钮,支持多选;「查看完整消息…」弹窗按 76 列折行显示全文。离屏验证:剪贴板收到未截断全文。提交 `6643288`。

### 12.7 FakeNet 核心每次启动单文件详细日志(2026-08-15,v1.3)
1. **位置与命名**:冻结版默认写到 `fakenet.exe` 同目录的 `Logs\fakenet-<本地时间>-p<PID>.log`;源码模式写到仓库根 `Logs\`。路径不依赖启动时 cwd,不得使用 `%APPDATA%\fakenet-gui\logs`。
2. **全入口覆盖**:日志在 `fakenet.fakenet:main` 内建立,因此 GUI 提权启动、直接双击 exe、命令行、PowerShell/CMD 和 `python -m fakenet.fakenet` 使用同一机制。GUI 自身不另建启动日志。
3. **单文件约束与兼容**:默认文件名使用微秒时间戳 + PID 并独占创建;单次核心进程仅挂载一个文件 handler。现有 `-l/--log-file` 保留为显式覆盖,指定时只写该文件,不再额外创建默认文件。
4. **详细度与失败语义**:UTF-8,记录启动参数、解释器、冻结态、cwd、最终日志路径、正常生命周期和异常堆栈;自动日志目录/文件无法创建时拒绝继续启动,避免无日志运行。
5. **验证与交付**:新增 3 项核心入口行为测试(冻结版任意 cwd 默认单文件、`-l` 早退唯一覆盖、失败 traceback)全过;核心日志 + 停止生命周期 + GUI launcher/smoke 聚焦 **22 passed**。全量 **329 passed + 18 subtests,1 failed**;唯一失败仍为 CP936 环境激活的既有 `test_gbk_source_round_trip`,与本改动无关。PyInstaller 6.22 的 `fakenet.exe`/`fakenet-GUI.exe` 双构建均通过;部署包版本升 v4,由提交快照生成新 ZIP。

### 12.8 GUI 启动日志与新建配置闭环(2026-08-15,v1.4)
1. **GUI 日志**:每次 `fakenet-GUI` 进程启动即创建一个 UTF-8 详细日志;冻结版固定写到自身 exe 同级 `Logs\fakenet-GUI-<本地时间>-p<PID>.log`,源码模式写仓库根 `Logs\`。初始化失败、未进入 Tk 主循环、正常关闭与未捕获异常均须落盘;日志目录/文件无法创建时 fail-closed。
2. **进程边界**:单次 GUI 进程只创建一个 GUI 日志;GUI 拉起的 `fakenet.exe` 是独立进程,继续按 §12.7 创建自己的一个核心日志。部署包中二者同级,故共同写入同一个 `Logs\` 目录,但不混写同一文件。
3. **初始模型一致性**:`ConfigModel.new_config()` 在写入 `DefaultTCPListener=ProxyTCPListener` 与 `DefaultUDPListener=ProxyUDPListener` 时,必须同步创建两个启用的、协议匹配的 `ProxyListener` 段;validator 仍严格拒绝真正不存在的段名,不得为修复 UI 假阳性而放宽 fail-closed 规则。
4. **回归与交付**:以 VM 原始错误文本为回归断言,并增加冻结版 GUI 从任意 cwd、Tk 初始化前失败仍创建唯一同级日志的入口测试。部署包升 v5。

### 12.9 GUI 第一阶段布局与交互优化(2026-08-15,v1.5)

#### 12.9.1 目标与边界

面向隔离 Windows VM 中的恶意样本分析人员,单一任务是让配置状态、当前编辑对象、校验结果和启动动作在 980×700 窗口内始终清楚可达。保持 tkinter/ttk 纯标准库、现有四标签页、中文界面及全部配置/校验/启动语义。

本阶段仅修改 `fakenet/gui/app.py`、`widgets.py` 及对应 GUI 测试/文档;除非测试暴露既有缺陷,不修改 schema、configmodel、validator、launcher、核心、INI 写出和打包依赖。出站策略渐进展开、右侧字段说明栏、监听器多列表格、启动准备度状态轨留待后续版本,避免把视觉调整扩大为状态机重构。

#### 12.9.2 视觉与布局规格

- **方向**:Windows 安全分析工作台,保留原生控件与清晰键盘焦点;不使用渐变、阴影、圆角卡片或新增第三方主题库。视觉记忆点只放在“校验抽屉 + 持久操作栏”的启动闭环上。
- **色彩 token**:背景 `#F4F6F8`;表面 `#FFFFFF`;正文 `#1F2937`;主操作/焦点 `#1769AA`;通过 `#18864B`;警告 `#B7791F`;错误 `#C9362B`。红色仅表达真实错误,锁定状态使用中性灰。
- **字体**:优先 `Microsoft YaHei UI` 9–10pt;标题使用同字体加粗;技术值沿用输入控件字体,不强依赖 VM 可能缺失的额外字体。
- **间距**:8px 基线;窗口保持初始 980×700,不得靠扩大窗口掩盖裁切。Notebook 为主要伸缩区,操作栏固定在底部。

目标结构:

```text
状态摘要(校验结果 + 当前提示)
四标签页主编辑区(伸缩)
校验抽屉(通过时折叠;有问题时展开;可手动切换)
持久操作栏(配置状态/路径 | 保存配置 | 启动 FakeNet-NG)
```

#### 12.9.3 本阶段功能清单

1. **校验抽屉**:校验通过时仅保留一行摘要,不渲染空 Treeview;出现错误/警告时自动展开。用户可用明确按钮手动展开/收起;展开后保留完整消息、横向滚动、双击跳转、Ctrl+C/右键和复制选中/全部能力。
2. **持久操作栏**:保存与启动按钮固定在窗口最底部且始终可见;启动为唯一主操作。左侧显示“未保存配置/已保存路径”及 dirty 状态,不得只依赖窗口标题表达。
3. **字段布局**:`FieldWidget` 使用统一标签列宽、输入列自适应和稳定行距;长标签不得挤压输入框。普通字符串仍单行;`Listeners` 这类 schema 中的文本长值必须完整可编辑且不被监听器分栏裁切。
4. **监听器页**:保持 Listbox(本阶段不升级为 Treeview),但左右 `PanedWindow` 初始比例须为约 1:3;右侧滚动区充分占宽,底部校验通过时不得遮挡字段。增/复制/重命名/删除动作保持现有行为。
5. **自定义响应空状态**:未加载文件时不显示无意义的空编辑分栏,而显示用途说明与“打开响应文件”“新建响应文件”两个入口;加载后恢复现有段列表/字段编辑功能。
6. **文案与语义**:“代码强制”改为中性锁定视觉,红/橙/绿同时配套文字或符号,不能只靠颜色传达状态。

#### 12.9.4 验收标准

- 新建默认模型在 980×700、Windows 100%/125% DPI 下无关键控件横向裁切;监听器 `Listeners` 完整可编辑。
- 校验通过时抽屉高度不超过一行摘要;主编辑区明显高于 v5。制造一个错误后抽屉自动展开,修复后自动收起;用户手动状态行为有单测。
- 保存/启动按钮在所有标签页、抽屉展开/收起状态下始终可见。
- 自定义响应未加载时存在明确空状态;新建/打开后可进入原编辑界面。
- 错误消息完整复制、双击跳转、字段锁、dirty 状态、保存与启动门禁无回归。
- GUI 聚焦测试全过;全量测试除已知 CP936 `test_gbk_source_round_trip` 基线外无新增失败。
- PyInstaller GUI/核心构建通过;部署包升 v6,manifest 绑定 v1.5 与源提交;包内不预置 Logs。

#### 12.9.5 实施前自评门禁

状态:**已通过(2026-08-15),允许实施**。

| 门禁 | 自评结论 | 依据/约束 |
|---|---|---|
| 范围 | 通过 | 可限制在 app/widgets 展示层与测试/文档;不需修改核心模块 |
| 依赖 | 通过 | 仅使用既有 tkinter/ttk,不引入主题包、字体包或图片资源 |
| 语义隔离 | 通过 | 不改变 schema、INI、validator、launcher;UI 仍调用既有公开行为 |
| 980×700 可行性 | 通过 | 空校验 Treeview 当前固定 8 行;折叠后可回收约 170px 给主编辑区 |
| 交互保真 | 通过 | Treeview item 继续保存完整消息;`_issues/_registry` 保留复制与跳转;抽屉只控制展示 |
| 动态重绘风险 | 通过(有防守项) | 抽屉与空状态使用独立 UI 状态;重绘期间沿用 `_building` 防止误标 dirty,测试覆盖 |
| 可验证性 | 通过 | 无头 Tk 可断言映射/高度/按钮可见/空状态;VM 用 100%/125% DPI 同尺寸截图复核 |
| 设计独特性 | 通过 | 以配置→校验→启动的工作闭环组织空间;不套用 Web 卡片/渐变/大圆角模板 |

自评未发现阻断项。按风险收敛原则只实施 §12.9.3 六项;监听器 Treeview、右侧说明栏、策略渐进展开和启动准备度状态轨不夹带进入 v1.5。

#### 12.9.6 实施结果

- **布局**:根窗口改为四区网格(状态/伸缩 Notebook/校验抽屉/持久操作栏);校验通过时详情 Treeview 完全解除映射,只保留 27px 摘要,980×700 下 Notebook 实测获得 578px 高度。出现问题自动展开 6 行详情;手动收起在问题仍存在时保持,问题清零后复位。
- **字段与监听器**:LabelFrame 子列获得伸缩权重,字段标签统一 20 字符列宽并可换行;普通 Entry 去掉固定 48 字符请求宽度;Proxy `Listeners` 使用 2 行自动换行 Text 且逻辑值不插入视觉换行。监听器实测左/右约 284/660px,选中字段容器 620px、输入区 468px。
- **状态与空态**:底栏持续显示配置路径/未保存状态,保存与启动始终映射;自定义响应未加载时显示用途说明,打开/新建后切换到原双栏编辑器;锁定标记由错误红改为中性灰,错误/警告/通过仍以符号+文字+语义色共同表达。
- **自动化**:新增 4 项无头 UI 行为测试(抽屉自动/手动状态、持久操作栏与 dirty 文案、自定义响应空态切换、Listeners 多行完整值)。GUI 聚焦 **106 passed,1 deselected**(主动排除已知 CP936 基线);全量 **335 passed + 18 subtests,1 failed**,唯一失败仍为既有 `test_gbk_source_round_trip`,无新增回归。
- **交付门禁**:源提交完成后由 `Build-GuiVmPackage.ps1` 构建 v6,manifest 标记 plan v1.5;脚本仍验证双 exe/平铺资源/哈希且禁止把 Logs 写入包内。

### 12.10 GUI 布尔控件与组内紧凑布局(2026-08-15,v1.6)

#### 12.10.1 用户反馈与根因

v6 实际界面仍有三项问题:① Yes/No 下拉增加无意义点击;②枚举下拉随输入列横向铺满,弱化值域感知;③每组保持单列导致纵向密度不足,仍需滚动。根因是 v1.5 只为字段容器增加伸缩权重,未针对控件类型与宽屏组内排布区分布局策略。

#### 12.10.2 修订规格

1. **二值 checkbox**:`bool_yesno` 与 `bool_truefalse` 均使用 `ttk.Checkbutton`;界面勾选语义统一为“启用”。模型读写仍分别精确映射 `Yes/No` 与 `True/False`,不得把两套字面量混写。锁定状态继续禁用 checkbox,条件解锁后恢复可操作。
2. **下拉按内容定宽**:`enum`/`ipv4_or_enum` Combobox 取枚举最长项决定字符宽,并限制在 10–32 字符;使用左对齐 `sticky=w`,不随容器横向铺满。可编辑枚举仍允许输入自定义 IPv4/文本值,行为不变。
3. **组内双列**:全局与出站策略的每个 LabelFrame 使用两列 FieldWidget,按字段顺序从左到右、从上到下排布;Text、路径、SHA-256、`Listeners` 等需要长输入的控件独占整行。小分组可在页面网格中左右并排,长分组跨整行;出站策略的小分组允许在同一网格单元内纵向组合,以利用进程组右侧对应空间。监听器与自定义响应右侧因可用宽度较小,继续单列。
4. **密度与滚动**:字段行距收紧到 2–4px、标签列宽适当缩短;滚动容器仅在内容实际高于视口时显示垂直滚动条。允许内容确实超高时滚动,但不得常驻显示无效滚动条或以截断代替滚动。
5. **无语义扩张**:不修改 schema 字段、validator、configmodel、launcher 和核心;不增加折叠组/搜索/新状态机。部署包目标升 v7,manifest plan v1.6。

#### 12.10.3 验收标准

- Yes/No、True/False checkbox 的 `get/set` 精确往返各自字面量;用户点击触发原有 on_change。
- enum 下拉的 grid sticky 不含 `e`,宽度不超过 32 字符;长枚举仍可完整选择。
- 全局/出站策略组内两个普通字段位于同一网格行的左右列;长字段跨两列;监听器仍为单列。
- 980×700 下“全局”首屏至少容纳 FakeNet、基础、抓包、DNS与网关四组;内容未超高的页面不显示垂直滚动条。
- 现有锁定、dirty、校验、复制/跳转、保存和 INI 两层往返测试无回归;全量除已知 CP936 基线外无新增失败。

#### 12.10.4 实施结果

- **控件语义**:`bool_yesno` 与 `bool_truefalse` 均已改为“启用”复选框,分别精确输出 `Yes/No` 与 `True/False`;锁定与 on_change 行为沿用原链路。枚举下拉按最长候选值计算宽度并限制在 10–32 字符,固定左对齐且不横向拉伸。
- **紧凑布局**:全局与出站策略组内改为双列;路径、SHA-256、Text 与 `Listeners` 类长值跨整行。全局小组左右排布,出站策略将私网接管与公网 IPv4 放行纵向组合,监听器继续单列。字段标签与行距同步收紧。
- **滚动策略**:滚动容器按内容请求高度动态显示/隐藏纵向滚动条。980×700 实测全局内容/视口为 527/540px、出站策略为 500/540px,两页均不显示滚动条;内容确实超高时仍可滚动。
- **自动化**:新增 4 项无头测试,覆盖两套布尔字面量、枚举宽度/对齐、静态双列/监听器单列及 980×700 下动态滚动条。GUI 聚焦 **110 passed,1 deselected**(主动排除已知 CP936 基线);全量 **339 passed + 18 subtests,1 failed**,唯一失败仍为既有 `test_gbk_source_round_trip`,无新增回归。
- **交付门禁**:部署包目标升级为 v7,manifest 标记 plan v1.6;继续执行双 exe、平铺资源、哈希与无 Logs 检查。

### 12.11 GUI 主机视觉验收闭环(2026-08-15,v1.7)

#### 12.11.1 实测方法与结论

- **实际产物**:在主机解压并运行 v7 `fakenet-GUI.exe`,以 Win32 窗口句柄对 996×759 外框(应用客户区 980×700)取图;只启动配置工具,未启动 FakeNet 核心。
- **逐页控制**:另用同提交源码的临时 Tk 驱动在程序内部选择四个 Notebook 标签并抓取 980×700 客户区;清除源码目录缺少平铺证书引起的两条环境性路径警告后,校验抽屉状态与正式包一致。
- **滚动结论**:全局、出站策略、监听器在干净默认模型下均完整显示且没有非必要滚动条;自定义响应空状态完整。告警抽屉展开时压缩编辑区属于既定可恢复交互,不作为干净状态布局失败。
- **视觉缺口**:全局双列中的 `进程白名单(Diverter 级)` 等长标签因 12 字符标签列而在英文单词中间断行;监听器四个横排按钮的总请求宽度把左栏撑至约 294px,高于内容所需。

#### 12.11.2 修订规格与自评

1. `[FakeNet]` 与全局“重定向与黑名单”使用 20 字符标签列,“Linux”使用 16 字符标签列;不改字段文案、schema 或 INI 键,以横向富余换取稳定单词边界和更少行高。
2. 监听器操作栏继续单行、键盘可聚焦;按钮统一 5 字符宽,将“重命名”缩为语义等价的“改名”,其余增/复制/删除行为不变。
3. 回归测试锚定长标签列宽、监听器按钮文案/宽度和 980×700 无多余滚动条;重新自动截图对比全局与监听器页。
4. 范围只涉及 `app.py` 展示参数、无头测试、README 与本方案;不改 schema、validator、configmodel、launcher 或核心。复杂度低,无新增依赖,实施前自评通过。部署包目标升 v8,manifest plan v1.7。

#### 12.11.3 验收标准

- 全局截图中 `Diverter` 不在单词中间断开,长标签不再占三行;字段输入仍有足够宽度。
- 监听器左栏请求宽度下降,右侧完整显示 `Listeners`、CA 证书/私钥字段;四项操作行为和中文可读性不变。
- 干净默认模型在 980×700 下四页关键内容完整;全局、出站策略没有非必要纵向滚动条。
- GUI 聚焦测试与全量测试除既有 CP936 基线外无新增失败;v8 包通过 manifest、双 exe 哈希、平铺资源和无预置 Logs 检查。

#### 12.11.4 实施结果

- **标签密度**:`[FakeNet]` 与“重定向与黑名单”标签列已升至 20 字符,Linux 组为 16 字符。复截图中 `进程白名单(Diverter 级)`、`主机黑名单(Diverter 级)` 均保持单行,全局内容请求高度由 v7 的 527px 降至 420px,540px 视口内不显示滚动条。
- **监听器分栏**:四个操作按钮统一 5 字符宽,“重命名”缩为“改名”;命令绑定与键盘可聚焦行为不变。980×700 复截图中左栏由约 294px 降至 255px,右侧完整显示 `Listeners`、CA 证书与私钥字段。
- **四页证据**:`dist/GUI配置工具截图/v8-host/` 保存全局、出站策略、监听器、自定义响应四张 980×700 主机截图。全局滚动状态为空且内容/视口 420/540px;出站策略为空且为 500/540px;监听器页关键字段完整;自定义响应空状态完整。
- **自动化**:新增 1 项回归测试,锚定三类标签列宽和四个监听器按钮。GUI 聚焦 **111 passed,1 deselected**;全量 **340 passed + 18 subtests,1 failed**,唯一失败仍为既有 `test_gbk_source_round_trip`,无新增回归。
- **交付门禁**:部署包目标升级为 v8,manifest 标记 plan v1.7;源提交后执行正式打包与 ZIP 独立复核。

### 12.12 GUI 全字段悬停说明与校验详情消歧(2026-08-15,v1.8)

#### 12.12.1 现状与依据盘点

- schema 共 **117** 个字段实例:FakeNet/Diverter、监听器公共与类型字段、自定义响应字段。现有 60 个含 `hint`,57 个为空;空项集中在默认配置注释已说明的网关/DNS/黑名单、监听器过滤与服务参数、自定义 TCP/UDP 响应。
- 现有 `FieldWidget` 只在 Label/Input 获得焦点时调用 `_hint`,鼠标悬停没有说明浮层;顶部 hint 也不会随鼠标更新。
- 提示内容以 `fakenet/configs/default.ini` 注释为第一依据,再与监听器实现、`egresspolicy.py`、自定义响应文档和 validator 的实际范围/互斥约束对齐。运行时只使用 schema 副本,不导入核心。
- “展开详情”属于校验问题 Treeview 的展开/收起按钮。`_issues` 为空时按钮被禁用但仍显示,形成不可解释的死控件。

#### 12.12.2 修订规格与自评

1. **全字段说明**:补齐 57 个空 `hint`,使 117/117 字段均有中文描述;内容说明作用、有效条件、格式/范围、互斥关系或路径含义,不复制超长源码注释。schema 继续是 GUI 运行时唯一提示来源。
2. **鼠标 tooltip**:`FieldWidget` 为标签、输入及其子控件绑定共享 tooltip;进入后约 450ms 显示“字段名 + 说明”,离开、点击或控件销毁立即关闭。浮层限制宽度并自动避开屏幕右/下边缘;同一字段最多一个浮层。
3. **键盘与状态提示**:焦点进入行为保留;鼠标进入时同步更新顶部 hint,因此不用鼠标精确停留也可读。tooltip 不抢焦点,不改变 Tab 顺序或字段值。
4. **保真扩展键**:schema 未收录的监听器额外键显示统一说明“未知扩展项,原样保存且不校验”,不得伪造源码语义。
5. **详情按钮消歧**:无校验问题时隐藏“展开详情”和复制操作;出现错误/警告时重新显示并可展开/收起。抽屉自动展开、手动收起状态机不变。
6. **范围与门禁**:只改 schema/widgets/app 展示层、测试与文档;不改 configmodel、validator、launcher 或核心。新增纯标准库 Toplevel,无依赖。复杂度中等,主要风险是 tooltip 生命周期与动态重绘,以单例浮层、Destroy 取消和无头 Tk 测试防守。实施前自评通过;部署包目标 v9,manifest plan v1.8。

#### 12.12.3 验收标准

- 测试遍历 117 个 schema 字段,`hint.strip()` 全部非空;默认、监听器类型与自定义响应各有代表性内容断言。
- 鼠标进入字段后 tooltip 可见且包含字段标签与 hint;离开/点击/销毁后关闭;顶部 hint 同步更新。
- 新建干净配置不映射“展开详情”;制造一个错误后按钮出现且可展开,修复后再次隐藏。复制、双击跳转和手动收起无回归。
- 980×700 主机截图中 tooltip 不遮挡当前字段,不越过屏幕边界;四页布局和滚动状态无回归。
- GUI 聚焦与全量测试除既有 CP936 基线外无新增失败;v9 包通过 manifest、双 exe 哈希、平铺资源和无预置 Logs 检查。

#### 12.12.4 实施结果

- **字段说明**:已按默认配置注释、监听器实现、`egresspolicy.py`、自定义响应文档及 validator 约束补齐 schema,自动遍历确认 **117/117** 个已知字段均有非空中文 `hint`。未知监听器扩展键显示“原样写回、不转换、不校验”的保真说明。
- **悬停交互**:`FieldWidget` 标签、输入框、复选框、下拉框及其子控件共享延迟约 450ms 的 tooltip;浮层不抢焦点,离开/点击/销毁即关闭,按虚拟屏幕边界调整位置。鼠标进入同时同步顶部提示,原有键盘焦点提示不变。
- **详情消歧**:校验结果为空时不再映射“展开详情”和复制操作;出现错误/警告时重新显示并沿用自动展开、手动收起、双击跳转状态机。主机制造 1 个错误实测按钮按需出现。
- **主机证据**:`dist/GUI配置工具截图/v9-host/01-global-tooltip.png` 展示 980×700 全局页“网络模式”的说明浮层;`02-validation-expand-action.png` 展示错误状态下按需出现的“展开详情”。无问题状态截图确认该死控件已隐藏,页面无新增滚动条。
- **自动化**:新增 3 项测试并强化校验抽屉状态断言,覆盖 117 字段完整性、已知字段 tooltip/顶部提示、未知扩展键保真提示和详情按钮显隐。GUI 聚焦 **114 passed,1 deselected**;全量 **343 passed + 18 subtests,1 failed**,唯一失败仍为既有 `test_gbk_source_round_trip`,无新增回归。
- **交付门禁**:部署包目标升级为 v9,manifest 标记 plan v1.8;源提交后执行正式打包与 ZIP 独立复核。

### 12.13 底栏导入与直接恢复默认配置(2026-08-15,v1.9)

#### 12.13.1 需求语义与影响面

1. **导入配置**:底栏“导入配置”与现有“文件→打开…”语义一致,选择外部 INI 后直接打开并绑定原文件;后续“保存配置”写回该路径。沿用未保存修改确认、解析失败提示、源编码/BOM/行尾保真和外部修改冲突检测。
2. **恢复默认配置**:只作用于主配置。当前模型有绑定路径时,弹出一次包含完整路径的不可撤销确认;用户确认后,立即用 `ConfigModel.new_config()` 生成的 GUI 安全默认配置覆盖该文件,成功后界面切换到新模型并处于已保存状态。未绑定路径时提示先保存或导入,不打开另存为对话框。
3. **默认来源**:不得直接载入 `fakenet/configs/default.ini`;该文件包含 4 个需部署者填写的 `ExternalProcessRedirect*` 占位符,会使校验失败。`new_config()` 已包含协议匹配且实际存在的 TCP/UDP 代理监听器,是恢复动作的唯一默认源。
4. **文件保真与直接覆盖**:恢复模型继承当前文件的 `encoding`、BOM 与换行风格;确认发生在实际写盘之前。该动作的产品语义就是直接覆盖,因此确认后不再弹出第二层外部修改确认。写盘失败时保留当前界面模型并显示中文错误。
5. **界面层级**:底栏从左到右为“状态 | 导入配置 | 恢复默认配置 | 保存配置 | 启动 FakeNet-NG”;前两项使用普通次级按钮,启动仍是唯一主按钮。两项均有 tooltip,明确“导入后保存会写回原文件”和“恢复会立即覆盖绑定文件”。980×700 下不得挤压或截断主操作。
6. **范围与复杂度**:主要修改 `app.py`、GUI 冒烟测试、README、本方案与打包版本;configmodel 现有构造/保存能力足够,validator、launcher 与核心不改。复杂度低到中,风险集中在覆盖目标、取消分支和文件格式保真;以临时文件字节断言及主机实跑防守。部署包目标 v10,manifest plan v1.9。

#### 12.13.2 验收标准

- “导入配置”选择文件后 `model.path` 绑定其绝对路径,底栏显示该路径;取消文件选择或拒绝放弃未保存修改时模型不变。
- 未绑定文件点击“恢复默认配置”只提示、不写盘;拒绝确认时目标文件字节完全不变。
- 确认恢复后目标文件立即被安全默认配置覆盖,重新读取后默认 TCP/UDP 监听器存在且校验无错误;GUI 保持同一路径、dirty=False。
- GBK/UTF-8 BOM 与 CRLF/LF 至少各有代表性自动化断言,确认恢复沿用绑定文件格式;自定义响应模型不受影响。
- 980×700 主机截图中四个操作按钮完整、层级清晰;tooltip 与确认文案均为中文且说明直接写回/覆盖语义。
- GUI 聚焦与全量测试除既有 CP936 基线外无新增失败;v10 包通过 manifest、双 exe 哈希、平铺资源和无预置 Logs 检查。

#### 12.13.3 实施结果

- **最短交互链**:底栏已按“导入配置→恢复默认配置→保存配置→启动 FakeNet-NG”排列。导入按钮直接复用 `open_file()`,选择后绑定原文件;两项新增按钮均提供中文 tooltip,明确保存写回和确认后立即覆盖语义。
- **直接覆盖**:`restore_defaults()` 仅检查绑定路径并进行一次不可撤销确认;确认框包含绝对路径并绑定主窗口。随后以 `ConfigModel.new_config()` 覆盖目标,继承编码、BOM 与行尾,不再增加外部修改二次确认。成功后界面保持同一路径且为已保存状态;未绑定、取消和写盘失败均不替换当前模型。
- **主机证据**:`dist/GUI配置工具截图/v10-host/01-file-actions.png` 展示 980×700 下四个完整操作按钮与绑定路径;`02-restore-confirmation.png` 展示原生确认框的覆盖目标、不可撤销说明和是/否动作。干净配置无多余校验抽屉或滚动条。
- **自动化**:新增 **7** 项 pytest 用例并强化 1 项底栏断言,覆盖导入绑定、拒绝丢弃、未绑定提示、取消字节不变、写盘失败、确认后覆盖外部改动、UTF-8 BOM/LF 与 locale/CRLF 保真。GUI 聚焦 **121 passed,1 deselected**;全量 **350 passed + 18 subtests,1 failed**,唯一失败仍为既有 `test_gbk_source_round_trip`,无新增回归。
- **交付门禁**:部署包目标升级为 v10,manifest 标记 plan v1.9;源提交后执行正式打包与 ZIP 独立复核。

### 12.14 DomainAllowList 激活时同步代码强制值(2026-08-15,v1.10)

#### 12.14.1 精确复现与根因边界

1. **用户路径**:GUI 新建配置→将 `ExternalAccessPolicy` 从 `Disabled` 改为 `DomainAllowList`→点击“一键补齐必需监听器”。拓扑两项错误可由按钮消除,但 `ExternalAllowedTCPPorts` 仍报“必须显式为 443”,且字段处于代码强制锁定状态,用户无操作路径。
2. **根因**:`ConfigModel.new_config()` 有意不预写代码锁定字段;核心可为大部分锁定字段应用安全缺省,唯 `ExternalAllowedTCPPorts` 的核心缺省为空,策略启用时必须显式为 `443`。GUI `_refresh_locks()` 只禁用控件,没有在用户主动激活策略时把 schema 强制值同步到模型。
3. **类似项**:`ExternalProcessRedirectProtocol` 等锁定字段也可能呈现“模型缺键、界面锁定”的同类状态;接管条件锁也只改变显示。修复应统一同步当前已激活的强制值,不得只给 443 打特例。

#### 12.14.2 修订规格与验收标准

- 用户在 GUI 中激活 `DomainAllowList` 或修改其子策略时,把 `schema.LOCKED_FIELD_VALUES` 全部精确写入 `[Diverter]`;接管 IP 非空时同步 `ExternalAllowedDomains=api.deepseek.com` 与 `ExternalNonAllowedAction=Divert`。该同步属于用户编辑动作,不在纯加载时静默改写文件。
- 锁定控件显示与模型值保持一致;切回 `Disabled` 不删除已写入值,核心在禁用态忽略这些键,再次启用可稳定复用。
- “一键补齐必需监听器”在 `Disabled` 时禁用,切到 `DomainAllowList` 后启用;增加中文 tooltip,明确创建/启用 1 个 relay 与 UDP/TCP 各 1 个 DNS/53,以及接管时补空 `ResponseA`。按钮不负责切换策略。
- 红测必须驱动真实 FieldWidget 事件链:切换策略后先确认 443 错误已消失、拓扑错误仍存在;调用一键补齐后策略相关错误全部消失。另测全部硬锁值和两项条件锁均落入模型。
- 不修改 validator、configmodel、launcher 或核心;只改 app/schema 展示与交互层、GUI 测试和文档。部署包目标 v11,manifest plan v1.10。

#### 12.14.3 实施与主机验收结果

- **实现**:`app.py` 在用户明确编辑出站策略字段时统一同步 `LOCKED_FIELD_VALUES`,接管条件成立时同步两项条件强制值;纯加载不触发同步。拓扑按钮随策略状态启用/禁用,tooltip 明确其创建对象及“不启用策略”的边界。`schema.py` 同步补充激活提示。
- **真实事件回归**:新增 2 项无头 GUI 测试。实际驱动 `ExternalAccessPolicy` 控件从 Disabled 切到 DomainAllowList 后,`ExternalAllowedTCPPorts=443` 与全部硬锁值已落入模型;此时只保留 2 项监听器拓扑错误。执行 `autofix_topology()` 后策略错误归零;切回 Disabled 时按钮禁用且已写值保留。接管 IP 编辑亦会把域名与未放行动作校正为源码强制值。
- **主机视觉验收**:实际运行 980×700 Tk 窗口并截图。补齐前界面显示锁定值 443、2 个拓扑错误且按钮可用;补齐后 0 错误,只剩既有 CA 路径警告,启动按钮恢复可用。证据位于 `dist/GUI配置工具截图/v11-host/`。
- **自动化**:GUI 聚焦 **122 passed,1 failed**,全量 **352 passed + 18 subtests,1 failed**;两处唯一失败均为既有 CP936 环境下 `test_gbk_source_round_trip`,没有新增回归。差异格式检查通过。

### 12.15 DomainAllowList 拓扑零操作补齐(2026-08-15,v1.11)

#### 12.15.1 需求修订与影响面

用户明确要求“一键补齐必需监听器”不应由用户点击。v1.10 的两步交互“启用策略→手动补齐”修订为单步交互“启用策略即完成全部必需配置”。影响仅限 GUI 编排层、校验提示、无头 GUI 测试、README 与打包版本;核心策略、拓扑生成规则和 INI 序列化保持不变。

#### 12.15.2 交互与数据规则

- 移除出站页的手动补齐按钮及对应弹窗;不保留不可点击占位控件。
- 用户把 `ExternalAccessPolicy` 切换为 `DomainAllowList` 时,在同一个控件事件内先同步代码强制值,再调用既有拓扑生成规则创建/启用恰好 1 个 DomainEgressRelay 与 UDP/TCP 各 1 个 DNSListener/53,最后统一校验;用户不会看到中间错误态。
- DomainAllowList 激活期间修改接管 IP 等出站字段时再次幂等执行拓扑同步,从而自动补空 DNS `ResponseA`;已有非空 `ResponseA` 继续保留。
- 打开或载入已启用 DomainAllowList 但缺少必需拓扑的配置时,重建界面阶段自动在内存模型中补齐,并把文件状态标为“有未保存修改”;不在用户保存前直接覆盖原文件。
- 纯 `Disabled` 配置不创建任何策略监听器;切回 Disabled 不删除已生成段,避免策略开关造成数据丢失。
- 校验器仍保留拓扑防线,但错误文案不再引导点击已移除的按钮;若用户之后手工破坏监听器拓扑,错误仅说明实际约束。

#### 12.15.3 验收标准

- 真实 FieldWidget 事件测试确认:界面不存在补齐按钮;切换策略后无需额外调用即可得到 443、relay 与双 DNS/53,策略错误为 0。
- 载入缺拓扑的 DomainAllowList 文件后自动补齐、状态为未保存修改且磁盘原文件字节不变;保存后补齐内容才写盘。
- 幂等测试确认重复编辑出站字段不会增加重复监听器;切回 Disabled 不删除现有监听器。
- 主机 980×700 实际截图确认按钮已移除、页面更紧凑、启用后错误区无拓扑错误。部署包目标 v12,manifest plan v1.11。

#### 12.15.4 实施与主机验收结果

- **实现**:出站页不再创建手动按钮,原域名放行/私网接管/进程重定向布局整体上移一行。`_sync_active_egress_policy()` 在策略编辑和模型重建入口统一同步代码强制值并幂等调用拓扑生成器;有实际变化时更新监听器列表和中文状态提示。
- **载入安全**:打开已启用但缺拓扑的文件会完成内存修复并显示“有未保存修改”,精准测试确认原文件字节不变;只有用户执行保存才写回。Disabled 配置不创建策略段,切回 Disabled 不删除已有段。
- **界面与文案**:`schema.py` 提示改为“自动写入并补齐”,validator 移除已不存在的按钮引导,README 同步说明零操作行为。
- **自动化**:新增 1 项载入修复测试并改写激活测试;GUI 冒烟 **23 passed**,GUI 聚焦 **123 passed,1 deselected**,全量 **353 passed + 18 subtests,1 failed**。唯一全量失败仍为既有 CP936 环境下 `test_gbk_source_round_trip`,没有新增回归。
- **主机视觉验收**:实际运行 980×700 Tk 窗口并由真实下拉事件启用策略;断言按钮不存在、策略错误为 0、监听器共 5 段。截图 `dist/GUI配置工具截图/v12-host/01-domain-allowlist-auto-topology.png` 显示按钮已移除、内容上移且仅保留既有 CA 路径警告。

### 12.16 出站策略总开关复选框与通用命名(2026-08-15,v1.12)

#### 12.16.1 问题与设计判断

`DomainAllowList` 是核心 INI 的内部策略枚举名,但当前出站页同时管理域名放行、私网接管、公网 IPv4 放行和进程重定向。把该字面量直接显示在仅有两项的下拉框中,既把总策略误解为单一域名功能,也增加不必要的一次选择操作。

#### 12.16.2 展示与序列化规则

- 首组显示名从“域名放行”改为“基础策略”;字段标签改为“出站策略总开关”,编辑器使用可键盘聚焦/空格切换的原生 checkbox,文字为“启用”。
- 新增 GUI 专用两态展示类型,精确映射:未勾选→INI `ExternalAccessPolicy=Disabled`;勾选→INI `ExternalAccessPolicy=DomainAllowList`。不得改写成 `Yes/No` 或 `True/False`,不得修改核心字段名与核心枚举值。
- 加载 `Disabled` 显示未勾选;加载 `DomainAllowList` 显示已勾选。其他未知字面量按未勾选展示但保留原模型值,直至用户明确操作;现有校验/核心兼容行为不扩展。
- checkbox 的变更事件继续走 v1.11 同一入口:勾选立即同步强制值并自动补齐拓扑;取消勾选仅写 `Disabled`,不删除已生成监听器。
- 面向用户的字段 hint、自动补齐状态和 GUI validator 错误改用“出站策略启用时”等通用中文;技术文档可注明内部映射。

#### 12.16.3 验收标准

- schema 锚定新展示类型、标签、分组以及两个精确 INI 字面量;字段总数不变。
- 真实 FieldWidget 测试确认控件为 `ttk.Checkbutton`:默认未勾选且 `get()=Disabled`;点击后模型为 `DomainAllowList`、拓扑自动完整;再次点击模型为 `Disabled`。
- 保存/重载测试确认两个状态逐字往返,没有误写三套既有布尔字面量;原有 Yes/No 与 True/False checkbox 测试继续通过。
- 主机 980×700 截图确认不再显示 `DomainAllowList` 下拉框,首组标题与总开关语义准确。部署包目标 v13,manifest plan v1.12。

#### 12.16.4 实施与主机验收结果

- **schema 与控件**:新增 `T_BOOL_POLICY`、`EGRESS_POLICY_DISABLED`、`EGRESS_POLICY_ENABLED`;FieldWidget 使用原生 `ttk.Checkbutton` 且仅对该类型执行精确匹配。原 Yes/No、True/False 两套 checkbox 分支保持原行为。
- **命名**:出站首组统一改为“基础策略”,字段显示“出站策略总开关:[ ] 启用”;schema hint、自动补齐状态、GUI validator 错误不再向用户暴露 DomainAllowList 模式名。README 明确技术映射。
- **真实事件与往返**:测试从默认未勾选/Disabled 开始,调用 checkbox `invoke()` 后模型精确为 DomainAllowList 且自动拓扑完整;再次点击精确恢复 Disabled。两个状态分别保存并重新加载,字面量逐字一致。
- **自动化**:新增 1 项 schema 映射锚定并强化 GUI 事件测试;GUI 聚焦 **124 passed,1 deselected**,全量 **354 passed + 18 subtests,1 failed**。唯一失败仍为既有 CP936 环境下 `test_gbk_source_round_trip`,没有新增回归。
- **主机视觉验收**:实际运行 980×700 Tk 窗口并点击 checkbox;断言控件类型、内部值 DomainAllowList、策略错误 0、监听器 5 段。截图 `dist/GUI配置工具截图/v13-host/01-egress-policy-checkbox-enabled.png` 显示“基础策略”与已勾选总开关,不存在内部枚举下拉框。

### 12.17 原生任务式重构、实时日志、单实例与启动提速(2026-08-16,v1.13)

#### 12.17.1 实测依据与设计边界

本轮先在宿主机实际运行 v13 冻结产物,逐页、逐菜单、逐对话框检查四个标签页,并对出站策略总开关、域名放行、私网导向、公网 IPv4 规则和进程重定向逐步编辑截图。完整原始证据位于 `dist/GUI配置工具截图/v13-full-review/`,共 39 张 PNG。实测确认:

1. 当前双列布局虽比早期版本紧凑,但锁定实现字段与用户任务混排;校验抽屉展开后占据约三分之一窗口并遮挡后续策略;公网 IPv4 规则单行框过窄;监听器无法区分用户对象与策略自动管理对象。
2. `FieldWidget._hash_file()` 只更新界面变量,未触发模型写回,导致界面显示 SHA-256 而保存值仍为空。
3. FieldWidget 的 FocusOut 无值变化也触发 dirty;监听器面板重绘会从 `_registry` 删除 `[FakeNet]`/`[Diverter]` 静态字段,破坏校验双击跳转。
4. 自定义响应删除最后一段后留下空白编辑区,缺少可行动的空状态。
5. v13 onefile 冻结程序从启动到窗口可见中位约 3.87 秒;源码模式约 1.31 秒。进一步剖析显示冻结解包约 1.3–2.0 秒,首次 Tk 构建约 0.9–2.6 秒;纯 validator 计算仅约 0.0003 秒。关闭 UPX 后 onefile 中位约 2.26 秒但仍超过 2 秒门槛。

边界继续为纯标准库 `tkinter/ttk` 原生 Windows 小工具。保持单窗口、菜单、键盘焦点和 980×700 初始尺寸;不引入 WebView、前端框架、动画、第三方主题、运行时核心 import 或响应式 Web 模式。信息层级服务于“配置→校验→启动→看日志”的实际工作流,不以装饰性卡片扩大留白。

#### 12.17.2 主窗口与验证交互

1. Notebook 改为五页:`基础配置`(原“全局”)、`出站策略`、`监听器`、`自定义响应`、`实时日志`。非当前页延迟到首次选择时构建,减少首屏 Tk 控件数量;首次校验通过 `after_idle()` 延后,窗口先完成显示。
2. 主窗口底部只保留一行校验状态。字段错误在对应控件下方显示简短红色文本;点击状态行或启动时存在错误,打开可调整大小的原生 `Toplevel` 校验窗口,复用现有 Treeview、完整消息、复制与双击跳转。删除主窗“展开详情”及校验抽屉。
3. 所有已知配置字段继续保留鼠标悬停 tooltip 与键盘焦点提示;只读、安全约束和 FakeNet 运行中锁定状态不得使 tooltip 失效。说明来源仍为默认配置注释、核心源码和 validator/schema 约束副本。
4. 纠正无变化 FocusOut 造成的伪 dirty;监听器动态重绘仅清理本监听器字段注册项,不得删除 `[FakeNet]`/`[Diverter]` 静态字段。
5. 自定义响应无段时显示说明和新增入口,不显示不可解释的空白面板。

#### 12.17.3 出站策略任务模型

1. 保留一个总复选框,内部仍精确映射 `Disabled/DomainAllowList`;四块任务按“真实域名访问”“将其他域名导向私网分析主机”“公网 IPv4 直连”“按指定程序重定向”组织,不是互斥场景下拉框。
2. 代码强制/派生字段默认不作为禁用输入框占据主任务区;显示“已应用 N 项安全约束”摘要,允许展开只读实现详情。锁定值仍由 schema/validator 双源锚定并精确写入 INI。
3. 放行域名在未开启私网导向时使用原生列表编辑器,支持多个合法 hostname。开启“将其他域名导向私网分析主机”时,真实联网域名必须按核心合同固定为唯一 `api.deepseek.com`;若现值不同,确认后替换并锁定。关闭后恢复可编辑,但不猜测或恢复旧列表。
4. 私网导向说明固定为“放行域名保持真实访问;其余域名解析到指定私网 IPv4”。关闭总策略只把策略写为 `Disabled`;不删除、不回滚或统一停用 DNS/relay 监听器,避免破坏普通 FakeNet DNS。
5. 公网 IPv4 规则使用原生 `ttk.Treeview`,列为“协议/IP/端口”,提供添加、编辑、删除;序列化继续为 `协议/IPv4/端口` 逗号列表,保持核心合同、最多 32 条/16 个 IP及 `*` 端口语义。
6. 进程重定向保持当前单目标“绝对 PE 路径 + SHA-256 + 原公网 A + 私网 B”合同。路径选择或文本值变化后自动异步计算 SHA-256,计算期间禁用启动;SHA 只读且无“重新计算”按钮。导入值在文件存在时自动核验;不存在则保留配置、显示错误并禁用启动。修复界面 SHA 未写回模型的缺陷。
7. 自动补齐策略拓扑继续零操作、幂等。用户看到面向任务的结果摘要,不再看到“一键补齐”动作或中间错误态。

#### 12.17.4 监听器与运行生命周期

1. 监听器页分为“用户监听器”和“系统自动管理”。策略使用的 UDP/53 DNS、TCP/53 DNS 与 DomainEgressRelay 在系统区可见且只读,不能编辑、重命名或删除关键字段;提供“重新生成”以按既有拓扑规则恢复。普通/被复用的 DNS 监听器不得在关闭策略时被批量删除或停用。
2. GUI 启动 FakeNet 时先在 `fakenet.exe` 同级 `Logs` 中独占预留本次唯一日志路径,并显式传递 `--log-file`;核心因此只写这一文件。`实时日志`只跟随当前由本 GUI 启动的 FakeNet 实例,不混入 GUI 日志或直接启动实例日志。
3. 日志页启动前显示“尚未启动”;启动后显示完整路径、打开日志目录、暂停/继续自动滚动。使用 `after()` 增量读取;暂停只固定视口,仍持续追加。当前启动日志完整显示、不截断;过大时只提示性能风险,磁盘与界面文本保持一致。
4. 启动成功后 GUI 保持打开并切到实时日志页;配置编辑、导入、恢复默认、保存和再次启动全部锁定,但 tooltip 与日志操作可用。通过 `ShellExecuteExW + SEE_MASK_NOCLOSEPROCESS` 保留提升后进程句柄,等待 FakeNet 退出后自动解锁并显示退出状态;失败路径必须关闭句柄。

#### 12.17.5 GUI 单实例与启动性能

1. GUI 每次启动仍先创建自己的同级 `Logs` 日志。随后创建进程寿命期 Windows 命名 mutex;第二实例在自己的日志中记录重复拒绝,尝试恢复/激活标题匹配的既有主窗口后退出。激活成功不弹框;激活失败才显示中文警告。mutex 句柄在正常退出释放,崩溃时由 Windows 自动回收。
2. `fakenet-gui.spec` 关闭 UPX,降低 onefile 启动解压/扫描成本;非当前标签页延迟构建,首次校验延至 idle。构建后重新测量三次;若中位仍大于 2 秒或最大值大于 3 秒,启用 PyInstaller 原生 Splash。
3. 根据已完成的 `upx=False` 实测中位 2.26 秒,本轮直接配置 PyInstaller 原生 Splash,复用项目 `resources/fakenet.png`;主窗口完成初始化后立即关闭 splash。PyInstaller 的文本槽会自动滚动 `_tcl_data` 等解包文件名,实测不适合作为用户提示,因此正式实现只显示项目图像、不启用文本槽,不伪造进度、不暴露内部路径,也不引入新图片或依赖。

#### 12.17.6 明确 TODO(不在 v1.13 实施)

1. **放宽私网导向的真实联网域名限制**:当前核心和已审计网络合同只允许唯一 `api.deepseek.com`。未来若支持多个域名,必须修改 `egresspolicy.py` 等核心模块,重新升版安全方案、第三方审计、网络矩阵和隔离 VM 验收;GUI 不得先行放宽。
2. **进程重定向多目标支持**:一个配置同时绑定多个目标进程/目标映射。
3. **仅按进程名匹配(支持正则)**:需要定义大小写、路径冲突、正则拒绝服务和身份误匹配的失败关闭合同。
4. **仅按 PID 匹配**:需要定义 PID 复用、进程退出与重启、启动竞态和权限不足语义。

后三项同样涉及核心 `processredirect.py`/Windows 数据面与安全边界,不属于 GUI 单方面可实现的展示功能。

#### 12.17.7 验收标准

- 980×700 下五页关键任务无需依赖主窗校验抽屉;常用策略区不出现不必要的水平滚动,普通页面尽可能无垂直滚动。所有 checkbox、按钮、Treeview 和对话框可用键盘操作。
- 多域名列表、私网导向固定域名确认、公网 IPv4 规则表、目标 PE 自动 SHA、系统监听器只读/重新生成均有真实控件事件测试与 INI 往返断言。
- 校验弹窗完整消息、复制、双击跳转、字段内联错误及静态字段注册表回归均有测试;所有字段 tooltip 在普通、只读与运行锁定状态下可触发。
- GUI mutex 的首实例、重复实例激活成功/失败、句柄释放路径有隔离单测;FakeNet 启动命令精确携带一个 `--log-file`,路径位于 exe 同级 Logs,单次不创建第二个核心日志。
- 实时日志增量追加、不截断、暂停自动滚动、进程退出解锁与状态变化有无头测试;不读取其他日志文件。
- PyInstaller v14 GUI 构建含 `Splash`,`upx=False`;主机至少三次记录启动到窗口可见耗时,并逐页、关键出站编辑、校验弹窗和实时日志空/运行状态截图复核。
- GUI 聚焦测试全过;全量测试除既有 CP936 `test_gbk_source_round_trip` 基线外无新增失败。`run_gui_vm_acceptance.py` 在物理机保持 REFUSED/exit 2。正式 ZIP 平铺双 exe 与运行资源,不预置 Logs。

#### 12.17.8 实施前自评门禁

状态:**已通过(2026-08-16),允许实施**。

| 门禁 | 自评结论 | 依据/防守项 |
|---|---|---|
| 维护范围 | 通过 | 仅修改 GUI 包、GUI 测试、GUI spec/打包脚本、README 与本方案;核心只读 |
| 核心兼容 | 通过 | 所有新控件仍序列化为现有字段和值;日志复用核心已支持的 `--log-file` |
| 原生可行性 | 通过 | Notebook、Checkbutton、Treeview、Toplevel、Text、named mutex 和 ShellExecuteEx 均可由标准库实现 |
| 安全边界 | 通过 | mutex、VM/重复 FakeNet 门禁、路径校验和 fail-closed 不放宽;系统监听器不做破坏性回滚 |
| 性能 | 通过(需复测) | 已定位 onefile/首屏 Tk 两段成本;UPX、lazy tab、idle validation 与原生 splash 分层处理 |
| 可测试性 | 通过 | 平台 API 封装可 monkeypatch;日志 tail 与生命周期状态机可用临时文件/伪句柄验证 |
| 信息密度 | 通过 | 主窗移除校验 Treeview,锁定字段折叠;常用任务优先,高级实现仍可发现且可复制 |
| 数据保真 | 通过 | ConfigModel 与三套布尔/编码/BOM/行尾规则不改;表格/列表仅改变展示和编辑方式 |
| 可回退性 | 通过 | 本轮为独立 GUI 版本与 v14 ZIP;不覆盖用户配置或核心部署包 |

自评未发现阻断项。实施中若发现必须修改核心策略或放宽安全合同,立即停止该分支并另行升版,不得以 GUI 便利性为由夹带修改。

#### 12.17.9 实施结果

- **原生五页工作流**:主窗口已落地“基础配置/出站策略/监听器/自定义响应/实时日志”五页,只同步构建首屏并把首次校验延至 idle。主窗校验区收敛为一行状态,错误在字段下内联显示;完整结果改为可调整大小的独立 Treeview 窗口,保留复制、完整文本和双击跳转。自定义响应无段时恢复带新增入口的空状态。
- **任务式出站策略**:真实域名改为原生多项列表;私网分析主机、审核公网 IPv4 规则分别由复选框控制,公网规则以协议/IP/端口表编辑;安全实现字段折叠为 13 项只读详情。启用私网导向时确认并锁定唯一 `api.deepseek.com`;关闭后重新允许多域名。目标程序路径变化后在后台自动计算并写回 SHA-256,导入已有 SHA 时自动核验,停用进程策略不做无意义核验。
- **监听器与状态正确性**:用户监听器和当前策略占用的系统监听器分区显示,系统段只读且可按现有拓扑规则重新生成。修复了监听器重绘删除静态字段注册、无变化 FocusOut 产生伪 dirty、出站注册映射误锁基础字段、延迟页首次打开未补画内联错误、私网子键孤立以及 Tk 工作线程直接调用 UI 等问题;普通、只读和 FakeNet 运行锁定字段的 tooltip 均保留。
- **启动与实时日志闭环**:GUI 为本次核心运行在所选 `fakenet.exe` 同级 `Logs` 独占预留一个路径并精确传递一次 `--log-file`;实时日志页只增量读取该文件,完整追加且暂停只停止自动滚动。`ShellExecuteExW` 保留进程句柄;运行期间锁定配置/文件操作,退出后完成末尾解码、显示退出码并自动解锁。GUI 自身日志补充配置载入/保存、VM 门、提权和核心生命周期记录。
- **单实例与启动反馈**:入口在 Tk 之前创建 Windows 命名 mutex。主机冻结版实测第二次启动未创建第二主窗;窗口激活受自动化前台权限限制时进入唯一中文警告分支,第二实例日志记录 `duplicate GUI instance refused: activated=False`,关闭警告后返回 1。`fakenet-gui.spec` 已设 `upx=False` 并生成无文本槽原生 Splash;最终复拍中约 0.48 秒出现完整 FN 图像,没有 `_tcl_data` 路径,主窗初始化后关闭。
- **主机视觉与计时**:`dist/GUI配置工具截图/v14-host-review/` 保存 18 张真实窗口句柄截图,覆盖冻结 splash/主窗、五页、策略关闭/启用、多域名、私网导向、公网表、自动 SHA、安全详情、系统监听器、校验弹窗、日志空/运行态及运行锁定 tooltip。980×700 下基础页无滚动条;常用出站任务完整显示,仅主动展开 13 项安全详情时出现纵向滚动。最终冻结版三次主窗可见耗时为 **5.149/4.536/4.373 秒**,中位 **4.536 秒**,因此保留 splash。
- **自动化与门禁**:新增/改写回归覆盖列表/表格往返、私网确认、SHA 自动计算与导入不一致、系统监听器只读/再生、运行锁定、完整日志、进程退出、核心日志落点、ShellExecuteEx 句柄、mutex 与重复激活成功/失败。阶段性 GUI 聚焦为 **143 passed,1 failed**;全量为 **372 passed + 18 subtests,1 failed**。唯一失败仍是既有 CP936 `test_gbk_source_round_trip`,无新增失败;`git diff --check` 与 `py_compile` 通过。物理机验收前置 fail-closed,脚本实际 `python_exit=2`,未进入 FakeNet 启动步骤。
- **正式交付**:`dist/Windows-GUI配置工具-VM验收-v14.zip` 已从源码提交 `b370994ce46eb7f2d76af4ba792368b0340fa570` 隔离构建。独立复核为 **53,798,930 字节/257 条目/manifest 256 文件**,包级 SHA-256 `3b38761ea6f39c2a795996040b2be2d691c7a847322fb260a311a67beb8ffc50`;包内 `fakenet.exe`/`fakenet-GUI.exe` SHA-256 分别为 `16cf41273037639bae629a3d7412200e3f05c4e9a9266ffdcf5217098d2e372f`、`18798ea58f4bff36e79b474cd4e5906fc272de3102487851d8dc7eeba287f5d9`,均与 manifest 一致。平铺 `configs/default.ini`、`defaultFiles/`、`listeners/ssl_utils/` 存在,无预置 `Logs`、无 `.sha256` 边车。从正式 ZIP 单独解出 GUI 后主窗口可见、正常退出码 0,exe 同级恰好生成 1 个 GUI 启动日志且含启动记录;临时解压目录已清理。

### 12.18 核心无分流监听器启动修复与 VM 验收判定加固(2026-08-16,v1.14)

#### 12.18.1 v14 隔离 VM 证据复核与根因

用户从隔离 VMware VM 导出 `dist/Logs-GUI-v14/20260816-144731/`。`results.tsv` 将 A1-A9 全部记为 PASS,但同目录 `fakenet.log` 明确记录 `UnboundLocalError: cannot access local variable 'fn_addr'`、`FakeNet-NG terminated with an error` 与 `FakeNet-NG exiting: rc=1`。因此该证据集内部不一致,不得据此关闭 v14 隔离 VM 验收事项。

根因由宿主机最小公开生命周期调用确定性复现:

1. 核心 `Fakenet.start()` 只在 `DivertTraffic=Yes` 分支初始化监听器绑定地址 `fn_addr`,随后却对所有启用监听器无条件使用该变量。`DivertTraffic=No` 且存在任一启用监听器时,在绑定端口或启动 WinDivert 之前即异常退出。该缺陷来自既有核心路径,不是 v1.13 GUI 重构引入。
2. VM runner 的 A6 只要求 `fakenet.exe` 曾短暂出现,A8 只要求其随后消失;二者均未拒绝 traceback、非零退出或缺少 stop-flag 命中证据,故快速崩溃同时满足错误的 PASS 条件。
3. runner 文案声称“单 RawTCPListener”,但 `ConfigModel.new_config()` 自带两个启用的 ProxyListener 段,脚本没有删除,实际写出 3 个启用监听器。

#### 12.18.2 修订规格

1. **最小核心修复**:监听器默认绑定地址在可选 Diverter 初始化之前设为 `0.0.0.0`;启用流量分流时继续沿用原平台/接口选择逻辑。不得修改 DomainAllowList、WinDivert、DNS、进程重定向或公网 IPv4 安全合同。
2. **可判定启动标记**:核心在 `Fakenet.start()` 正常返回后、进入停止旗标/捕获失败等待循环前记录唯一 `FakeNet-NG started successfully`。异常路径不得出现该标记。
3. **真实最小配置**:VM runner 显式删除默认 ProxyTCP/UDP 段,保留 1 个 RawTCPListener,继续使用 `DivertTraffic=No`、`RedirectAllTraffic=No` 和 `DumpPackets=No`;这既避免网络分流副作用,又覆盖本次核心缺陷的真实触发组合。
4. **A6 fail-closed**:必须同时观察到真实 `fakenet.exe` 存活、非空日志和成功启动标记;日志出现 traceback、`terminated with an error`、`stop failed` 或非零退出即 FAIL。仅 ShellExecute 返回成功或进程短暂出现不得通过。
5. **A7 稳定前提**:只在 A6 已证明稳定启动后执行双实例进程门检查;A6 失败时 A7/A8 不得继续伪造通过结论。
6. **A8 fail-closed**:写入 stop flag 前实例必须仍存活;退出后日志必须同时包含 `Stop flag found at`、`Stopping...` 和 `FakeNet-NG exiting: rc=0`,且不得含失败标记。进程单纯消失不得通过。
7. **证据与交付**:runner 的判断逻辑提供可由导出日志重放的纯函数测试;正式包升 v15,不得覆盖 v14。v15 必须重新执行宿主机构建门禁和隔离 VM A1-A9;只有新 `results.tsv` 与核心日志一致时才可声明验收通过。

#### 12.18.3 验收标准

- 核心公开生命周期回归测试覆盖 `DivertTraffic=No + 启用监听器`,确认正常完成监听器配置且默认绑定地址为 `0.0.0.0`。
- 核心成功路径只记录一次 `FakeNet-NG started successfully`;启动异常路径不记录该标记并保持 rc 非零。
- VM runner 日志判定测试至少覆盖:成功启动、启动 traceback、未命中 stop flag、`Stopping...` 后 rc=1、完整 stop flag/rc=0 五类样本。
- runner 生成的 smoke INI 恰有 1 个启用 RawTCPListener,没有 ProxyTCP/UDP 段,GUI validator 为 0 错误。
- GUI 聚焦测试与全量测试除既有 CP936 `test_gbk_source_round_trip` 基线外无新增失败;`py_compile` 与 `git diff --check` 通过。
- v15 ZIP 继续满足双 exe、平铺运行资源、manifest 哈希、无预置 Logs、无 `.sha256` 边车;隔离 VM 重跑前不得把 v14 结果改写为通过。

#### 12.18.4 实施前自评门禁

状态:**已通过(2026-08-16),经用户明确授权允许修改最小核心生命周期路径**。

| 门禁 | 自评结论 | 依据/防守项 |
|---|---|---|
| 根因可复现 | 通过 | 导出日志与宿主机最小 `Fakenet.start()` 调用均在同一行稳定触发 `UnboundLocalError` |
| 核心修改范围 | 通过(例外授权) | 仅移动/补充默认绑定地址初始化并增加成功日志标记;不触碰策略、Diverter 或 Windows 数据面 |
| 安全边界 | 通过 | `DivertTraffic=No` 保持不启动 Diverter;验收判定由宽松存在性检查收紧为 fail-closed |
| 测试接缝 | 通过 | 核心走公开 `start()` 生命周期;runner 走日志证据判定和实际 smoke 配置输出 |
| VM 副作用 | 通过 | smoke 配置无流量分流、无 PCAP、仅本地 Raw TCP 监听器;停止仍使用核心 stop flag |
| 兼容与回退 | 通过 | 既有 `DivertTraffic=Yes` 地址选择不变;新包使用 v15,保留 v14 原件与证据 |

#### 12.18.5 实施结果

- **核心修复**:`Fakenet.start()` 在可选 Diverter 分支前初始化监听器绑定地址 `0.0.0.0`,`DivertTraffic=Yes` 的平台/接口选择路径保持不变。核心 `main()` 仅在 `start()` 正常返回后记录一次 `FakeNet-NG started successfully`;异常路径不记录该标记。
- **验收加固**:runner 的 smoke 配置已删除两个默认 Proxy 段,实际仅含 1 个 RawTCPListener。A6 同时要求进程存活、非空日志、成功标记且无失败证据;A7 只在 A6 后执行;A8 同时要求 stop flag 命中、`Stopping...`、进程退出和 `rc=0`。失败前提均写 FAIL 并执行受控清理。
- **TDD 与证据重放**:核心无分流监听器测试先稳定复现 `UnboundLocalError` 后转绿;成功日志和 runner 判定同样按红/绿切片完成。新增日志矩阵覆盖成功启动、v14 traceback/rc=1、缺 stop flag、停止清理后 rc=1、完整 stop flag/rc=0。新版判定函数直接重放 `dist/Logs-GUI-v14/20260816-144731/fakenet.log`,A6/A8 均返回 `False/核心日志记录异常终止`。
- **自动化结果**:核心+runner 聚焦 **14 passed**;核心启动/停止聚焦 **8 passed**;GUI 聚焦(排除既有 CP936 用例) **145 passed,1 deselected**。全量 **380 passed + 18 subtests,1 failed**,唯一失败仍为既有 `test_gbk_source_round_trip`,没有新增失败。`py_compile` 与 `git diff --check` 通过。
- **正式 v15 交付**:`dist/Windows-GUI配置工具-VM验收-v15.zip` 已从源提交 `866c39d2bbc5ff0cd6db3e9316e0bd65851fe9cf` 构建。独立复核为 **53,861,426 字节/258 条目/manifest 257 文件**,包级 SHA-256 `02d2854c5a15bb6802c4cc56c81c9d0f3c0de112f978ba4bec383488b2855481`;包内 `fakenet.exe`/`fakenet-GUI.exe` SHA-256 分别为 `8da5a295469876531e0871bf02e75a18abc42e5c8e525242d54b6fccf8d6f6fd`、`c4f5618a3be58b8fb008c53579c8b64c5c6027f755e129f5499ec68ac1bf6884`,均与 manifest 一致。平铺 `configs/default.ini`、`defaultFiles/`、`listeners/ssl_utils/` 存在,无预置 `Logs`、无 `.sha256` 边车。
- **正式包主机冒烟**:从 v15 ZIP 单独解出 GUI 后,主窗口标题“未命名 - FakeNet-NG 配置工具”和有效窗口句柄均出现;正常窗口关闭成功,exe 同级恰好生成 1 个 GUI 日志且包含主窗口初始化/退出记录,临时目录已清理。另从正式 ZIP 临时解包运行包内 runner,宿主机按前置门禁返回 `REFUSED/exit 2`(未提权),未进入 FakeNet 启动步骤。
- **剩余门禁**:隔离 VM A1-A9 仍须使用 v15 包重跑,并导回新 `results.tsv` 与 `fakenet.log`;只有 A6 成功标记与 A8 stop flag/`rc=0` 证据一致时才能声明验收完成。v14 导出证据结论保持“不通过”。
- **隔离 VM A1-A9 验收通过(2026-08-17)**:环境为 VMware Workstation 虚拟机 `win10h2-MalBox-20241110`(CIM Manufacturer/Model 实测 `VMware, Inc.` / `VMware20,1`;guestOS `windows9-64` 即 Windows 10 x64 家族,VM 命名指向 20H2,具体 build 号不在导出证据内,可由 `winver` 实测补充;基线快照 `快照 16-ApateDNS-双卡-小屏`,本次运行未新增快照)。宿主机复核包级 SHA-256 `02d2854c5a15bb6802c4cc56c81c9d0f3c0de112f978ba4bec383488b2855481` 后,由用户将正式 v15 ZIP 复制入 VM 并解压至 `C:\样本分析 P04\Windows-GUI配置工具-VM验收-v15`,双击包内 `test\gui_vm\Run-Tests.cmd`(一次 UAC 后免操作);9 项全 PASS 且无 REFUSED/SKIP,按 runner 构造推得退出码 0。证据原件导出至 `dist\Logs-GUI\Logs\20260817-084959\`(dist 已忽略,不入库):`results.tsv` 有且仅有 A1-A9 九行 PASS(7 实测 + 2 等效:A2/A4);`smoke_config.ini` 仅含 `[FakeNet]`/`[Diverter]`/`[RawTCPListener]` 三段,恰 1 个启用监听器(Port 1337),无 ProxyTCP/UDP 段,`DivertTraffic`/`DumpPackets`/`RedirectAllTraffic` 均为 No;`fakenet.log` 七个生命周期标记 `FakeNet-NG startup:` → `Loaded configuration file:` → `Will seek stop flag at` → `FakeNet-NG started successfully`(恰 1 次)→ `Stop flag found at` → `Stopping...` → `FakeNet-NG exiting: rc=0` 严格按序出现,监听器 `ipaddr: 0.0.0.0` 直接证实本节无分流默认绑定修复生效,机检 `Traceback`/`terminated with an error`/`stop failed`/`UnboundLocalError`/`rc=1` 全部 0 命中;核心 08:50:06 启动、08:50:08 命中 stop flag 并优雅退出。三份证据文件 SHA-256:`results.tsv` `d2b22734ac5576358449413b8c5acac5809186fd7d8bb0e23a6d272cf8988051`,`fakenet.log` `a925c9a48c362befa89c11b1f2a254b99813bdd75007793da14acab2acbd5eaa`,`smoke_config.ini` `22e36cf9f6bd64d082d7f72fa914fb1ab99ef8f75b68b3c57003e8f82bfb5940`。**至此 v15 隔离 VM A1-A9 验收完成,v14 证据结论维持“不通过”,v15 ZIP 不再重打包。**

### 12.19 界面可读性缩放与隐式锁定提示(2026-08-17,v1.15)

#### 12.19.1 需求(用户反馈)

1. 界面字体过小:全部界面字体在现有基础上放大 50%,主窗口与校验窗口默认尺寸同步放大 50%(用户明确"不合适后续再改")。
2. "🔒 代码强制"显示徽标在功能启用/禁用切换时插入/移除子控件,破坏行布局:改为**隐式提示**——去掉徽标,控件禁用,鼠标悬停 tip 中提示锁定原因("代码强制"等)。
3. 出站策略总开关关闭时,应禁用出站策略的全部其他控件;实际"真实联网域名"(ExternalAllowedDomains)与"非放行流量动作"(ExternalNonAllowedAction)两个条件锁定字段仍可编辑。

#### 12.19.2 修订规格

1. **缩放集中化**:`widgets.FONT_SCALE = 1.5` 与 `scaled()` 为唯一缩放来源;named fonts、全部显式 font 元组(9→14、8→12、10→15、11→17、Consolas 9→14)、tip 与内联错误 wraplength、树表列宽、标签换行像素估算按同一比例放大;主窗口 `980x700 → 1470x1050`(minsize `900x640 → 1350x960`),校验窗口 `900x420 → 1350x630`(minsize `680x300 → 1020x450`)。按钮/输入框宽度以字符为单位随字体自动放大,不做逐像素调参。
2. **隐式锁定**:`FieldWidget`/`CsvListFieldWidget`/`IPv4RulesFieldWidget` 的 `set_locked(locked, forced_value=None, reason='代码强制')` 统一为:锁定时仅禁用控件并把悬停 tip 追加一行 `🔒 <reason>`,解锁时恢复基础 tip;不再创建/销毁任何徽标子控件,行布局在启停切换时保持稳定。reason 语义:`field.lock` 与条件强制 → "代码强制";SHA-256 → "自动计算";运行期锁定 → "FakeNet 运行中";总开关关闭 → "出站策略未启用";子功能未启用 → 对应功能未启用提示。
3. **总开关语义修正**:`_refresh_locks()` 的条件锁定分支改为三级判定——总开关关闭时一律锁定(reason=出站策略未启用);总开关开启且私网接管开启才施加 `forced_value`(api.deepseek.com / Divert);总开关开启且接管关闭保持可编辑。"总开关关闭 ⇒ 除总开关自身外全部出站控件禁用"自此完整成立(该理解经核对 egresspolicy.py 语义确认正确:Disabled 下核心不读取任何 External* 键)。
4. 仅改 `fakenet/gui/` 与 `test/test_gui_*.py`;不改 INI 布尔语义、编码合同、fail-closed 门禁、双源真相锚定与核心代码。

#### 12.19.3 验收标准

- 默认主窗口尺寸与 named font 尺寸等于缩放常量推导值;锁定任何字段不再新增徽标子控件,grid 行列在锁定/解锁前后不变。
- 总开关关闭:域名列表、非放行动作、连接参数、私网接管、公网 IPv4、进程重定向全部禁用且悬停可见原因;开启后按各子功能开关状态恢复。
- 既有 takeover 条件锁定、域名列表/公网规则往返用例不回归;GUI 聚焦测试与全量测试除既有 CP936 基线外无新增失败;`py_compile`、`git diff --check` 通过。
- 本轮修改 `fakenet/gui/`,按 §12/§13 规则升部署包 v16 后方可进入下一轮隔离 VM 验收。

#### 12.19.4 实施结果(2026-08-17)

- **缩放**:`widgets.FONT_SCALE = 1.5` 与 `scaled()` 为唯一来源;named fonts 与全部显式 font 元组按 9→14、8→12、10→15、11→17、Consolas 9→14 放大,tip/内联错误 wraplength(420/440/500→630/660/750)、域名/IPv4 树表列宽与标签换行像素估算同比例;主窗口 `980x700→1470x1050`(minsize `→1350x960`),校验窗口 `900x420→1350x630`(minsize `→1020x450`);样式内边距(Tab/LabelFrame/Button)按比例同步。字符为单位的宽度随字体自动放大。
- **隐式锁定**:三类字段控件 `set_locked(locked, forced_value=None, reason='代码强制')` 统一为禁用 + 悬停 tip 追加 `🔒 <reason>`,不再创建/销毁徽标子控件。reason 语义按 §12.19.2 落地;复核时补齐两处遗漏调用点(监听器只读区"系统自动管理"、自定义响应面板运行期锁定,原以 badge 传参,已改 reason)。
- **总开关语义**:`_refresh_locks()` 条件锁定分支改为三级判定(policy off → 锁定"出站策略未启用";policy+takeover on → forced_value;policy on+takeover off → 可编辑),其余字段按组附加"未启用私网接管/公网 IPv4 直连/进程重定向"等准确原因。总开关关闭时除总开关自身外全部出站控件禁用,开启后按子功能恢复。
- **测试**:新增 3 个用例(隐式锁定不增删子控件且 grid 布局前后不变、总开关关闭全禁用+开启恢复含 tip 原因、字体/窗口缩放默认值);既有滚动条用例改用 `MAIN_WINDOW_SIZE` 常量。smoke **37 passed**;GUI 聚焦 **148 passed,1 deselected**;全量 **383 passed + 18 subtests,1 skipped**(skip 为 `test_gbk_source_round_trip` 在 Git Bash 非 GBK locale 下自跳过;PowerShell 下仍表现为既有 CP936 基线失败,无新增失败);`py_compile`、`git diff --check` 通过。
- **正式 v16 交付**:`dist\Windows-GUI配置工具-VM验收-v16.zip` 从功能源提交 `2883fd47b33c05a30b5ee7c2d37a86df1cfbf304` 构建(manifest `v16`/`v1.15`/source_commit 一致)。独立复核:**53,874,704 字节/259 条目**,包级 SHA-256 `b14c6fac853b99e40f5fac86179e0da7ee60b46e75e2495c748d4a1d64a4a2f4`(较 v15 多 1 条目,为 ddbd1f7 交接文档,属已提交仓库内容);包内 `fakenet.exe`/`fakenet-GUI.exe` SHA-256 分别为 `b7fb72ac0aa1316b87a68cc426c4abed7a9aed3f54199908a6d5d91b0062f170`、`84791519791b89decd384caf7bfa13ee1194e7350efad66101fae6481d62ed1e`,与 manifest 一致;平铺 `configs/default.ini`、`defaultFiles/`、`listeners/ssl_utils/` 存在,无预置 `Logs`、无 `.sha256` 边车。
- **正式包主机冒烟**:从 v16 ZIP 解出 GUI 后,主窗口"未命名 - FakeNet-NG 配置工具"初始化,WM_CLOSE 优雅关闭成功(日志含 `fakenet-GUI main window initialized` 与 `fakenet-GUI exiting`),每次启动恰好生成 1 个 GUI 日志;实例存续期间第二实例被命名互斥体正确拒绝(`duplicate GUI instance refused: activated=True`,rc=0)。方法学备注:PyInstaller onefile 为引导父进程+应用子进程双进程结构,taskkill/CloseMainWindow 若只命中父进程不会关闭真实窗口,主机验证需按窗口标题定位应用进程。包内 runner 在宿主机未提权环境返回 `REFUSED/exit 2`,未进入 FakeNet 启动步骤。
- **剩余门禁**:隔离 VM 须使用 v16 包重跑 A1-A9 并导回证据;v15 ZIP 与已归档的 v15 验收结论保持不变。
- **隔离 VM A1-A9 验收通过(2026-08-17)**:同一隔离 VMware 分析 VM `win10h2-MalBox-20241110`(CIM Manufacturer/Model 实测 `VMware, Inc.` / `VMware20,1`)。正式 v16 ZIP(包级 SHA-256 `b14c6fac853b99e40f5fac86179e0da7ee60b46e75e2495c748d4a1d64a4a2f4`)由用户复制入 VM 并解压至 `C:\样本分析 P04\Windows-GUI配置工具-VM验收-v16`,双击包内 `test\gui_vm\Run-Tests.cmd`(一次 UAC 后免操作);9 项全 PASS 且无 REFUSED/SKIP,按 runner 构造退出码 0。证据原件导出至 `dist\Logs-GUI\Logs\20260817-125409\`:`results.tsv` 有且仅有 A1-A9 九行 PASS(7 实测 + 2 等效:A2/A4);`smoke_config.ini` 仅含 `[FakeNet]`/`[Diverter]`/`[RawTCPListener]` 三段、恰 1 个启用监听器,SHA-256 `22e36cf9f6bd64d082d7f72fa914fb1ab99ef8f75b68b3c57003e8f82bfb5940` 与 v15 隔离 VM 运行字节一致(v1.15 未修改配置写出器,跨版本交叉印证);`fakenet.log` 七个生命周期标记严格按序出现、`FakeNet-NG started successfully` 恰 1 次、监听器 `ipaddr: 0.0.0.0`、禁止标记(`Traceback`/`terminated with an error`/`stop failed`/`UnboundLocalError`/`rc=1`)机检 0 命中,核心 12:54:13 启动、12:54:14 命中 stop flag 并以 rc=0 优雅退出。三份证据文件 SHA-256:`results.tsv` `ab9077964c96b8083da67a816803de982c9acc0a8f80576cdb7c00e371f4b6a2`,`fakenet.log` `673b8647a3d75ee21f90794da300829af6aca31c7e63c5a9525cf6d9238331d3`,`smoke_config.ini` `22e36cf9f6bd64d082d7f72fa914fb1ab99ef8f75b68b3c57003e8f82bfb5940`。**至此 v16 隔离 VM A1-A9 验收完成,v15/v16 包与既有验收结论均不再变更,v1.15 全部门禁关闭。**

### 12.20 v16 隔离 VM 试用反馈修复(2026-08-17,v1.16)

#### 12.20.1 需求(用户在 v16 验收 VM 内试用反馈)

1. 字体大小可接受,但树表(Treeview)行高未随字体缩放,条目竖向只显示一半。
2. 左下"● 有未保存修改"应能看到详情(改了什么)。
3. "有未保存修改"判定不对:仅多次开关出站策略总开关,状态一直为"有未保存修改",关闭后也是如此。
4. 右下三个操作按钮(导入配置/恢复默认配置/保存配置)大小不一致。
5. 基础配置"进程白名单(Diverter级)"等标签在拉丁词与汉字之间断行("…(Diverter / 级):"),界面多处存在;根因是 wraplength 以"字符数×系数"估算,未按字体实测像素。

#### 12.20.2 修订规格

1. **Treeview 行高**:`configure_styles` 用 `TkDefaultFont.metrics('linespace') + 8` 设置 `style.configure('Treeview', rowheight=…)`,全部树表(域名列表、IPv4 规则、监听器列表、校验面板、自定义响应列表)随字体缩放;经典 Listbox 行高本随自身字体缩放,不需要改。
2. **未保存修改详情**:左下文件状态标签可点击(带悬停说明),打开"未保存修改详情"窗口,以 `difflib.unified_diff` 展示当前 `model.render()` 与基线渲染的行级差异;无差异时明确提示。
3. **dirty 语义修正**:废弃闩锁式 `_mark_dirty`,改为计算式——`dirty = 当前 render ≠ 基线 render`;基线在新建、载入(取 `ensure_sections` 后、自动补齐前的文件态)、保存、恢复默认四处刷新。 `_mark_dirty`/`_clear_dirty` 保留为兼容别名(刷新/设基线)。
4. **总开关反向撤销(纯净自动补齐的镜像)**:`_sync_active_egress_policy()` 在补齐前后对模型做段/键快照,记录自动新增段(created + pristine 内容)与被改键的先前值;当 `ExternalAccessPolicy` 切回 `Disabled` 时,仅当自动新增段的当前内容仍与补齐时一致(用户未改动)才整段移除、被改键按先前值还原,用户改动过的段/键保留。新建/载入/恢复默认时清空追踪。由此"开→关"后模型回到基线,计算式 dirty 归零,精确对应用户预期;有用户改动的自动段不被误删。
5. **按钮统一**:底部 导入配置/恢复默认配置/保存配置 三个按钮统一 `width=14`。
6. **标签换行像素化**:标签 wraplength 改为 `TkDefaultFont` 的 `'0'` 实测宽度 × 列字符数(+6px 余量);`build_group_frame` 按组内最长标题实测宽度自动加宽标签列(上限 34 字符),使常规标签单行显示,彻底消除拉丁-汉字边界断行;超长标签仍按列宽在汉字/空格边界自然换行。
7. 仅改 `fakenet/gui/` 与 `test/test_gui_*.py`;不改 INI 语义、编码合同、fail-closed 门禁、双源真相与核心代码。注意:反向撤销只作用于 GUI 本次会话自动补齐且未被用户改动的拓扑,属于界面状态管理,不改变 `ensure_domain_allowlist_topology` 的补齐合同。

#### 12.20.3 验收标准

- 树表 rowheight ≥ 字体 linespace,条目完整显示;三个按钮宽度一致。
- 新建配置上开关总开关:开启 → dirty 且拓扑段出现;关闭 → 拓扑段移除、dirty 归零、标题无 `*`;用户手工改过的自动段在关闭时保留。
- 未保存详情窗口/差异行函数:有修改时输出包含对应 `+`/`-` 行,无修改时为空。
- 标签(含"进程白名单(Diverter级):")单行显示:wraplength ≥ 标题实测像素宽。
- 既有用例(含载入活动策略自动补齐标 dirty、未变更 focus-out 不标 dirty、takeover 物化等)不回归;两处直接调用 `_mark_dirty` 的旧用例改为真实内容变更;GUI 聚焦与全量除既有基线外无新增失败;`py_compile`、`git diff --check` 通过。
- 升部署包 v17 后进入下一轮隔离 VM 验收。

#### 12.20.4 实施结果(2026-08-17)

- **树表行高**:`configure_styles` 以 `TkDefaultFont.metrics('linespace')+8` 设置全部 Treeview `rowheight`,域名/IPv4/监听器/校验面板条目完整显示。
- **dirty 与详情**:计算式 dirty(当前 `render()` 对比基线)落地,基线在新建/载入(文件态,自动补齐前)/保存/恢复默认刷新;`_mark_dirty`/`_clear_dirty` 变为兼容别名。左下状态标签可点击,打开"未保存修改详情"窗口(`difflib.unified_diff`,等宽字体,只读);无修改时明确提示。旧闩锁语义下"改回去仍显示未保存"的问题消除。
- **总开关反向撤销**:`_sync_active_egress_policy()` 补齐前后对模型做段/键快照,记录自动新增段与被改键先前值;`ExternalAccessPolicy` 切回 `Disabled` 时,纯净自动段整段移除、被改键还原先前值,用户改动过的段/键保留并同步控件显示。新建配置上"开→关"后模型回到基线,dirty 归零、标题无 `*`。既有"物化后关闭保留拓扑"的用例按新语义更新(关闭后锁定键未设置、DomainEgressRelay 移除、用户编辑的 `ExternalDnsServer` 保留);该用例原先以 `set()+_changed()` 模拟编辑,实际因 `_last_reported` 去重并未触达模型,现改用 `set(notify=True, force=True)` 真实传播。
- **按钮与标签**:底部 导入配置/恢复默认配置/保存配置 统一 `width=14`;标签 wraplength 按 `TkDefaultFont` 的 `'0'` 实测像素 × 列字符数 +6px,`build_group_frame` 按组内最长标题自动加宽标签列(上限 34 单位),"进程白名单(Diverter级):"等标签单行显示,拉丁-汉字边界断行消除;超长标签仍按列宽自然换行。
- **测试**:smoke **40 passed**;GUI 聚焦 **151 passed,1 deselected**;全量 **386 passed + 18 subtests,1 skipped**(既有 GBK locale 自跳过基线,无新增失败);`py_compile`、`git diff --check` 通过。新增用例:总开关开→关反向撤销与 dirty 归零(含用户改动段保留)、树表行高 ≥ linespace 与三按钮统一宽度、长标签单行(wraplength ≥ 标题实测宽度)、未保存差异行(`+DNSListener`/`+DomainEgressRelay`)。
- **正式 v17 交付**:`dist\Windows-GUI配置工具-VM验收-v17.zip` 从功能源提交 `fe2dbbfd8981449e49259a067cb992500c65992d` 构建(manifest `v17`/`v1.16`/source_commit 一致)。独立复核:**53,920,571 字节/259 条目**,包级 SHA-256 `ca35fe048cc737e8b2da5ccc7e1551a3f726fa68c6ec39deb1e9ab15f18c402c`;包内 `fakenet.exe`/`fakenet-GUI.exe` SHA-256 分别为 `df370d00c97c01193c975e6199bfa3fcd58e5818c844aa70ea9f7a529783ba83`、`1a2cc0d9340500004c2954951289687574cd33ee4d57ffcc0c62b24b37048d14`,与 manifest 一致;平铺布局存在,无预置 `Logs`、无 `.sha256` 边车。
- **正式包主机冒烟**:从 v17 ZIP 解出 GUI 后,主窗口"未命名 - FakeNet-NG 配置工具"初始化,WM_CLOSE 优雅关闭(日志含 initialized/exiting),恰好 1 个 GUI 日志;包内 runner 宿主机未提权返回 `REFUSED/exit 2`。
- **剩余门禁**:隔离 VM 使用 v17 包重跑 A1-A9 并导回证据;v15/v16 包与既有验收结论不变。
- **隔离 VM A1-A9 验收通过(2026-08-17)**:同一隔离 VMware 分析 VM `win10h2-MalBox-20241110`(CIM `VMware, Inc.` / `VMware20,1`)。正式 v17 ZIP(包级 SHA-256 `ca35fe048cc737e8b2da5ccc7e1551a3f726fa68c6ec39deb1e9ab15f18c402c`)解压于 VM `C:\样本分析 P04\Windows-GUI配置工具-VM验收-v17`,双击包内 `test\gui_vm\Run-Tests.cmd`(一次 UAC 后免操作);9 项全 PASS 且无 REFUSED/SKIP,按 runner 构造退出码 0。证据原件导出至 `dist\Logs-GUI\Logs\20260817-132421\`:`results.tsv` 有且仅有 A1-A9 九行 PASS(7 实测 + 2 等效:A2/A4);`fakenet.log` 七个生命周期标记严格按序、成功标记恰 1 次、监听器 `ipaddr: 0.0.0.0`、禁止标记机检 0 命中,核心 13:24:24 启动、13:24:25 命中 stop flag 并以 rc=0 优雅退出;`smoke_config.ini` 仅三段、恰 1 个启用监听器,SHA-256 `22e36cf9f6bd64d082d7f72fa914fb1ab99ef8f75b68b3c57003e8f82bfb5940` 与 v15/v16 隔离 VM 运行字节一致(配置写出器跨三个版本稳定)。三份证据文件 SHA-256:`results.tsv` `89102022538e555c3f8cab8bbf4eac7ee77f59461c0ea265ca1c5f026a9fa2ce`,`fakenet.log` `4691fdada59278cea3ea27dafdd99845215f8dab40a77c2dcc4cf59d24817170`,`smoke_config.ini` `22e36cf9f6bd64d082d7f72fa914fb1ab99ef8f75b68b3c57003e8f82bfb5940`。**至此 v17 隔离 VM A1-A9 验收完成,v1.16 全部门禁关闭;v15/v16/v17 包与既有验收结论均不再变更。**

### 12.21 v17 隔离 VM 试用反馈修复(2026-08-17,v1.17)

#### 12.21.1 需求(用户 v17 试用反馈)

1. 标题总以"未命名"开头——答疑:窗口标题第一段是当前配置的文件名,"未命名"表示尚未绑定任何 INI(新建配置未保存);导入或保存后即显示文件名,有未保存修改时加 `*`。非缺陷,不改行为。
2. 点击"新配置尚未保存"状态打开的弹窗位置在屏幕右上角且遮挡主窗口——修复:弹窗居中于主窗口。
3. "保存配置"按钮明显大于"导入配置/恢复默认配置"——修复:三个按钮样式与尺寸完全统一。
4. 询问"进程黑/白名单""主机黑/白名单"用途——答疑见 §12.21.2;同时发现 Diverter 级三个过滤字段的提示未写明作用(白名单只写互斥、主机黑名单只写"逗号分隔 IPv4"),本轮补全。

#### 12.21.2 黑/白名单语义(双源真相对齐核心 diverterbase.py)

- **Diverter 级(全局,基础配置"重定向与黑名单"组;SingleHost 模式数据面首先应用)**:
  - `ProcessWhiteList`:仅列表内进程的出站流量被 FakeNet 接管,其他进程直接转发;与 `ProcessBlackList` 互斥,同时存在启动失败。
  - `ProcessBlackList`:列表内进程的流量直接放行转发,不进任何监听器;与白名单互斥同上。
  - `HostBlackList`(仅黑名单):发往列表内 IPv4 的流量直接放行转发;逗号分隔。Diverter 级无主机白名单(与上游一致)。
- **监听器级(每个监听器段)**:在同一 Diverter 全局筛选之后,决定某条流量是否交给该监听器:`ProcessWhiteList` 仅列表内进程进该监听器;`ProcessBlackList` 列表内进程绕过该监听器;`HostWhiteList/HostBlackList` 按目标 IPv4 同理。同一监听器内进程黑/白互斥、主机黑/白互斥(核心 `ListenerBlackWhiteList` 异常)。

#### 12.21.3 修订规格

1. 新增 `_center_over_parent(window, width, height)`:按主窗口当前几何居中;主窗口未映射(尺寸不可信)时退回屏幕居中。应用于"未保存修改详情"与"配置校验详情"两个弹窗。
2. 底部 导入配置/恢复默认配置/保存配置 三个按钮统一 `style='Action.TButton'` + `width=14`。
3. schema.py Diverter 级 `ProcessWhiteList`/`ProcessBlackList`/`HostBlackList` 提示补全作用说明(与 §12.21.2 措辞一致);不改键名、默认值或校验。
4. 仅改 `fakenet/gui/` 与 `test/test_gui_*.py`;升部署包 v18 后进入下一轮隔离 VM 验收。

#### 12.21.4 验收标准

- 两个弹窗的几何为主窗口居中位置(测试:主窗口 deiconify 后断言 `x = rootx+(w-width)//2`、`y` 同理)。
- 三个按钮 `style` 与 `width` 完全一致。
- 三个 Diverter 级字段 hint 含作用关键词(仅接管/直接放行/互斥)。
- 既有用例不回归;GUI 聚焦与全量除既有基线外无新增失败;`py_compile`、`git diff --check` 通过。

#### 12.21.5 实施结果(2026-08-17)

- **弹窗居中**:`_center_over_parent(window, w, h)` 按主窗口几何居中(未映射时屏幕居中),应用于"未保存修改详情"(900x480)与"配置校验详情"(1350x630);弹窗不再落在屏幕右上角。
- **按钮统一**:导入配置/恢复默认配置补齐 `style='Action.TButton'`,与保存配置(原有该样式)一致,三者 `width=14`,完全同尺寸。
- **提示补全**:Diverter 级 `ProcessWhiteList`/`ProcessBlackList`/`HostBlackList` hint 按 §12.21.2 语义补全作用说明;键名、默认值、校验不变。
- **测试**:smoke **42 passed**(连续多次稳定);GUI 聚焦 **153 passed,1 deselected**;全量 **388 passed + 18 subtests,1 skipped**(既有基线,无新增失败);`py_compile`、`git diff --check` 通过。新增用例:两弹窗几何=主窗口居中坐标、三按钮 `style`/`width` 全等、Diverter 级三字段 hint 含作用关键词。
- **正式 v18 交付**:`dist\Windows-GUI配置工具-VM验收-v18.zip` 从功能源提交 `6c26ee1c7dfe354679f56b099d702b4324b4210a` 构建(manifest `v18`/`v1.17`/source_commit 一致)。独立复核:**53,923,622 字节/259 条目**,包级 SHA-256 `0e0557f3fc77fcdab194132c140dc0ddf08815ec6fc92690b41e19d61df295d3`;包内 `fakenet.exe`/`fakenet-GUI.exe` SHA-256 分别为 `3a83686c27964983c234da8d3e48d88f556f64f0dc3ba2ef875428c2c8bd40c4`、`f980783bbfa9f5eef37fa3f37f0834a151c5cd7f3dc9c2489ae099d8f910e584`,与 manifest 一致;平铺布局存在,无预置 `Logs`、无 `.sha256` 边车。
- **正式包主机冒烟**:从 v18 ZIP 解出 GUI 后,主窗口初始化、WM_CLOSE 优雅关闭(日志含 initialized/exiting)、恰好 1 个 GUI 日志;包内 runner 宿主机未提权返回 `REFUSED/exit 2`。
- **剩余门禁**:隔离 VM 使用 v18 包重跑 A1-A9 并导回证据;v15/v16/v17 包与既有验收结论不变。
- **隔离 VM A1-A9 验收通过(2026-08-17)**:同一隔离 VMware 分析 VM(`win10h2-MalBox-20241110`,CIM `VMware, Inc.` / `VMware20,1`)。正式 v18 ZIP(包级 SHA-256 `0e0557f3fc77fcdab194132c140dc0ddf08815ec6fc92690b41e19d61df295d3`)在 VM 内以 `test\gui_vm\Run-Tests.cmd` 运行,证据导出至 `dist\Logs-GUI\Logs-v18\20260817-161213\`(用户命名的新导出目录):`results.tsv` 九行全 PASS(7 实测 + 2 等效);`fakenet.log` 七个生命周期标记严格按序、成功标记恰 1 次、`ipaddr: 0.0.0.0`、禁止标记 0 命中;`smoke_config.ini` 三段单监听器,SHA-256 与 v15/v16/v17 字节一致。三份证据 SHA-256:`results.tsv` `d57a2c2e9f1f3efd1e04fc1ee1063d9cedb6721041ad4c698a19b4dd7137c79b`,`fakenet.log` `c22f9f459d85a5f8ad1c96c75d3d73c3199c325228f5e947447d8b31e1697e93`,`smoke_config.ini` `22e36cf9f6bd64d082d7f72fa914fb1ab99ef8f75b68b3c57003e8f82bfb5940`。**v1.17 全部门禁关闭。**

### 12.22 v18 隔离 VM 试用反馈修复(2026-08-17,v1.18)

#### 12.22.1 需求(用户 v18 试用反馈)

1. 启动时加载"上次的配置";加载失败弹窗提示后加载默认配置。
2. 标题改为 `FakeNet-NG 配置工具 - {当前配置文件绝对路径}`,且任何情况下都避免"未绑定 INI"状态;保留未保存修改时路径后加 `*`。
3. "进程黑/白名单互斥""主机黑/白名单互斥"要在界面上体现(一侧有值时另一侧禁用并提示互斥原因)。
4. 答疑(不改代码):原版进程/主机黑白名单与本项目新增的域名放行、公网 IPv4 直连、域名导向私网、进程重定向的区别与测试覆盖,见 §12.22.2。

#### 12.22.2 原版黑白名单 vs 新增出站功能(双源真相对齐)

- **原版(上游 FakeNet-NG,WinDivert 数据面)**:`ProcessWhiteList/ProcessBlackList`(Diverter 级与监听器级)、`HostBlackList`(Diverter 级)、监听器级 `HostWhiteList/HostBlackList`。它们只回答"这条流量要不要被拦下交给监听器":黑名单=放行直发,白名单=只拦名单内。维度是进程名与原始 IP(L3/L4);不参与 DNS 解析、不做真实 DNS/数据中继、不改写目的地址。
- **新增功能(本项目,出站策略框架)**:域名放行(真实上游 DNS + TLS SNI 校验 + 数据中继,上游无任何等价机制);公网 IPv4 直连(与 Diverter `HostBlackList` 在"放行指定公网 IP"上功能重叠,但处于 `ExternalAccessPolicy` 合同内:受总开关、规则/地址数量上限、去重、TCP 锁 443 与配置哈希审计约束,而 HostBlackList 是无约束豁免);域名导向私网(DNS 层 ResponseA 改写,上游无此维度);进程重定向(目的地址透明改写 A→B,与"拦不拦"的进程白名单完全不同)。**结论:无重复实现;唯一重叠点(公网 IPv4 直连 vs HostBlackList)是受管与不受管之别。**
- **上游测试覆盖**:`test/test.py` 为上游真实环境集成套件,含 `testBlacklistProcess`/`testWhitelistProcess`(Diverter 级)、监听器级进程/主机黑白名单、黑名单端口/主机用例;但该文件不在本仓库 pytest 基线内(收集即 `ModuleNotFoundError: No module named 'irc'`,且需真实分流环境)。当前自动化基线对名单的实际覆盖仅为:GUI 校验器互斥报错(test_gui_validator)、egress 判定桩接口(test_windows_egress_verdict)、DNS 策略桩(test_dns_policy)。可选后续(不阻塞):在隔离 VM 按上游方式补跑 test.py 或补名单过滤的单元级测试。

#### 12.22.3 修订规格

1. **持久化位置**:frozen 运行时状态与工作配置放 exe 同级;开发运行放系统临时目录。状态文件 `fakenet-GUI.state.json` 仅记 `last_config` 绝对路径;默认工作配置 `fakenet-GUI-config.ini`(不存在时由默认模板创建)。均为尽力写入,失败不阻断。
2. **启动流程**:`main()` 构建主窗口后调用 `startup_load()`:有 last 且可解析 → 直接载入;last 缺失或解析失败 → 单一警告弹窗说明原因并载入默认工作配置(必要时创建)。`_load_path` 返回布尔以便启动分支判断(静默模式不弹两个窗)。
3. **绑定不变量**:新建配置、载入、另存为、恢复默认均绑定具体文件路径并更新 `last_config`;标题格式 `FakeNet-NG 配置工具 - {绝对路径}`(+ 脏标记 `*`)。理论残余:工作目录不可写且临时目录亦不可写时保留未绑定兜底并弹窗说明(文档化边界)。
4. **互斥体现**:`_refresh_mutex_locks()`——Diverter 级 `ProcessWhiteList↔ProcessBlackList`;当前监听器面板内 `ProcessWhiteList↔ProcessBlackList`、`HostWhiteList↔HostBlackList`。一侧有值时另一侧以"与××互斥"原因锁定(tip 显示),清空后解锁;FakeNet 运行期与系统监听器只读态不参与互斥刷新(避免覆盖运行锁)。挂接:静态字段变更、监听器字段变更、监听器面板渲染与整体重建之后。校验器既有互斥报错保持不变(UI 预防 + 校验兜底)。
5. 仅改 `fakenet/gui/` 与 `test/test_gui_*.py`;升部署包 v19 后进入下一轮隔离 VM 验收。

#### 12.22.4 验收标准

- 有 last 且有效:启动直接载入,标题显示该路径;last 文件被删/损坏:弹一次警告并载入默认工作配置,标题显示工作配置路径;新建/载入/另存/恢复默认后 `last_config` 更新。
- 标题任何可见状态下均为 `FakeNet-NG 配置工具 - <绝对路径>`(+`*`);不存在"未命名"标题。
- Diverter 级与监听器级黑/白名单互斥:有值一侧使另一侧禁用且 tip 含互斥说明;清空恢复;运行期锁定不被互斥解锁破坏。
- 既有用例除受"始终绑定"影响的文件状态断言(随语义更新)外不回归;GUI 聚焦与全量除既有基线外无新增失败;`py_compile`、`git diff --check` 通过。

#### 12.22.5 实施结果(2026-08-17)

- **启动持久化**:exe 同级(开发运行取临时目录)`fakenet-GUI.state.json` 记录 `last_config`;`fakenet-GUI-config.ini` 为默认工作配置(缺失时由默认模板创建)。`main()` 在主窗口初始化并落日志后调用 `startup_load()`:上次配置有效则直接载入;缺失或解析失败弹一次警告(注明"文件不存在/无法解析该文件")后载入默认工作配置。`startup_load` 捕获窗口中途关闭的 `TclError` 并安静退出(主机冒烟曾暴露"标题出现即可关窗"竞态导致启动期 traceback,已随启动顺序调整与该保护消除,复测日志无 Traceback)。
- **始终绑定**:新建/载入/另存/恢复默认均绑定具体文件并更新 `last_config`;标题 `FakeNet-NG 配置工具 - {绝对路径}`(+ 脏标记 `*`);"未命名"仅保留为目录不可写时的文档化残余(弹窗说明)。`_load_path` 返回布尔并支持静默解析失败(启动分支只弹一次窗)。
- **互斥体现**:`_mutex_pair()` 四分支(双设/仅白/仅黑/全空),仅解除自己施加的锁(按控件标记),不破坏运行期/系统只读锁;挂接静态字段变更、监听器字段变更、监听器面板渲染与整体重建。Diverter 级进程黑/白与监听器级进程、主机黑/白名单,一侧有值即禁用另一侧并在悬停 tip 说明,清空自动恢复;校验器既有互斥报错保留。
- **测试**:smoke **46 passed**;GUI 聚焦 **157 passed,1 deselected**;全量 **392 passed + 18 subtests,1 skipped**(既有基线,无新增失败);`py_compile`、`git diff --check` 通过。新增用例:上次配置载入、缺失/损坏回退(单次警告+工作配置创建)、标题始终为绝对路径(+`*`)、Diverter 级与监听器级双对互斥(含"设主机黑名单后进程黑名单仍因进程白名单锁定")。既有"恢复默认需绑定文件"用例改为显式模拟残余未绑定态;文件状态断言随始终绑定语义更新。
- **正式 v19 交付**:`dist\Windows-GUI配置工具-VM验收-v19.zip` 从功能源提交 `be6023df17ac363312025e0587eef510e97b74c4` 构建(manifest `v19`/`v1.18`/source_commit 一致;首个 v19 构建因启动竞态修复重建,旧件已删未交付)。独立复核:**53,931,355 字节/259 条目**,包级 SHA-256 `da35618ea90066e7896f22d3ada5e88634ab50a90c8f2465b2711b5028310621`;包内 `fakenet.exe`/`fakenet-GUI.exe` SHA-256 分别为 `a81b044118025edd5794fae5a5962190bd5249f1f7b805ffffa8f6df72ead4ec`、`2b7329479cd8820491a7e00abffa0d589c5d29aae3494e4ad0345fe99f66e126`,与 manifest 一致;平铺布局存在,无预置 `Logs`、无 `.sha256` 边车。
- **正式包主机冒烟**:从 v19 ZIP 解出 GUI:首次启动自动创建工作配置与状态文件,标题显示其绝对路径;就绪轮询(initialized+configuration loaded)后 WM_CLOSE 优雅关闭,日志含 initialized/configuration loaded/exiting 且无 Traceback,恰好 1 个 GUI 日志;再次启动经状态文件载入同一工作配置(标题验证)。包内 runner 宿主机未提权返回 `REFUSED/exit 2`。
- **剩余门禁**:隔离 VM 使用 v19 包重跑 A1-A9 并导回证据;v15–v18 包与既有验收结论不变。
- **隔离 VM A1-A9 验收通过(2026-08-17)**:同一隔离 VMware 分析 VM(`win10h2-MalBox-20241110`,CIM `VMware, Inc.` / `VMware20,1`)。正式 v19 ZIP(包级 SHA-256 `da35618ea90066e7896f22d3ada5e88634ab50a90c8f2465b2711b5028310621`)在 VM 内以 `test\gui_vm\Run-Tests.cmd` 运行,证据导出至 `dist\Logs-GUI\Logs\20260817-191301\`:`results.tsv` 九行全 PASS(7 实测 + 2 等效);`fakenet.log` 七个生命周期标记严格按序、成功标记恰 1 次、`ipaddr: 0.0.0.0`、禁止标记 0 命中;`smoke_config.ini` 三段单监听器,SHA-256 与 v15–v18 字节一致。三份证据 SHA-256:`results.tsv` `d968b1b70835633fa8dd6714c1ffeaa98a39defade19bc9104715a80009b4c31`,`fakenet.log` `6179f9306deea5e9080331fe494bbd4b4a4e31767b0bd007c09ea48e3f0e6fad`,`smoke_config.ini` `22e36cf9f6bd64d082d7f72fa914fb1ab99ef8f75b68b3c57003e8f82bfb5940`。**v1.18 全部门禁关闭。**

### 12.23 v19 隔离 VM 试用反馈修复(2026-08-17,v1.19)

#### 12.23.1 需求(用户 v19 试用反馈)

1. 首次启动:加载默认配置并将其设为"上次加载的配置",此时不应出现"无法加载上次的配置文件",而应提示"当前加载的是默认配置文件"。
2. 因存在"恢复默认",默认配置不可被更改:绑定默认配置且有修改时,保存必须另存到非默认位置;任何路径都不得覆盖默认配置文件。
3. 工具标题固定为"FakeNet-NG 配置工具";当前配置文件绝对路径移到"配置提示"(校验摘要行)上方独立一行,加粗+主色高亮,右侧提供"在 Explorer 中查看文件"按钮;未保存标记 `*` 保留(显示在路径行)。
4. 答疑+修复:启用"将其他域名导向私网分析主机"时界面闪烁重排——根因是 `_toggle_takeover` 调用 `_render_egress_tab()` 销毁重建整个出站策略页,而其他启用开关只做原位锁定刷新。

#### 12.23.2 修订规格

1. **默认配置文件**:exe 同级 `fakenet-GUI-default.ini`(缺失时由 GUI 安全默认模板创建),此文件视为不可变默认配置。首次启动(无 last 记录)创建并载入之,弹 `showinfo`"当前加载的是默认配置文件:<路径>",并把该路径写入 last;上次配置缺失/损坏仍弹一次 `showwarning` 后载入默认配置文件(不再使用旧的 `fakenet-GUI-config.ini` 工作文件概念;v19 状态指向的旧文件按普通用户文件载入)。
2. **不可变默认**:`save()` 绑定默认配置时——无修改则不写;有修改则提示"默认配置不可被修改"并进入另存为流程;另存为选到默认配置路径时拒绝并报错。`restore_defaults()` 绑定默认配置时仅在有修改时确认后重新载入(不写盘);绑定用户文件时维持既有"覆盖当前绑定文件"语义。
3. **标题与路径行**:标题恒为 `FakeNet-NG 配置工具`(不再随绑定变化);新增配置路径行(校验摘要行上方):左侧加粗主色显示当前绝对路径(+ 未保存 `*`),右侧"在 Explorer 中查看"按钮(explorer /select 选中文件;文件不存在时打开其目录)。左下文件状态简化为"● 有未保存修改"/"✓ 已保存"(点击查看差异的交互保留)。
4. **私网接管闪烁修复**:`_toggle_takeover` 不再整体重建出站策略页;改为原位刷新——模型更新后同步出站控件显示(`_sync_egress_widgets_from_model` 扩展为对缺失键清空显示)、`_refresh_locks()` 原位启停、监听器列表按需刷新。开关前后出站页各控件对象保持同一实例(不闪烁、不丢滚动位置)。
5. 仅改 `fakenet/gui/` 与 `test/test_gui_*.py`;升部署包 v20 后进入下一轮隔离 VM 验收。

#### 12.23.3 验收标准

- 首次启动:仅出现一次"当前加载的是默认配置文件"信息框,默认文件被创建、绑定并记为 last;无"无法加载"警告。缺失/损坏 last:一次警告后载入默认文件。
- 绑定默认且有修改:保存 → 提示后另存为;另存为选中默认路径 → 拒绝;默认文件字节不变。绑定用户文件:保存/另存/恢复默认语义不变。
- 标题恒定;路径行显示绝对路径(+`*`),Explorer 按钮可定位文件;状态行简化且点击差异详情仍可用。
- 私网接管开关前后,出站策略页控件实例不变(无重建);既有 takeover 物化/往返用例不回归。
- 既有用例随语义更新;GUI 聚焦与全量除既有基线外无新增失败;`py_compile`、`git diff --check` 通过。

#### 12.23.4 实施结果(2026-08-17)

- **首次启动/不可变默认**:默认配置文件为 exe 同级 `fakenet-GUI-default.ini`(缺失时创建);首次启动(无 last 记录)弹 `showinfo`"当前加载的是默认配置文件:<路径>"并绑定、记入 last;缺失/损坏弹一次 `showwarning` 后载入默认。关键修复:`new_config()`(构造期即执行)不再写状态文件——否则首次启动时 last 总指向尚不存在的默认文件而误报"无法加载"(主机验证 v20 首版实测复现了该缺陷,即用户 v19 所见现象;修复后日志含 `first launch` 且无 `failed to load`)。新增生产顺序回归用例(构造前绑定状态目录)。
- **保存/恢复语义**:`save()` 绑定默认且有修改 → 提示"默认配置不可被修改"进入另存为;另存为选中默认路径 → 拒绝;绑定默认无修改 → 保存为无操作。`restore_defaults()` 绑定默认 → 仅在有修改时确认后重载(不写盘);绑定用户文件语义不变。
- **标题/路径行/状态行**:标题恒为 `FakeNet-NG 配置工具`;配置路径行(校验摘要行上方)加粗主色显示绝对路径(+`*`),右侧"📂 在 Explorer 中查看文件"按钮(explorer /select 选中文件,文件不存在打开其目录);左下状态简化为"● 有未保存修改"/"✓ 已保存",点击查看差异保留。
- **私网接管闪烁修复**:`_toggle_takeover` 改为原位刷新(模型同步含缺失键清显 + `_refresh_locks` + 监听器列表按需刷新),不再 `_render_egress_tab()` 整页销毁重建;新增用例断言开关前后出站页控件实例不变,既有 takeover 物化/往返用例不回归。
- **测试**:smoke **49 passed**;GUI 聚焦 **160 passed,1 deselected**;全量 **395 passed + 18 subtests,1 skipped**(既有基线,无新增失败);`py_compile`、`git diff --check` 通过。
- **正式 v20 交付**:`dist\Windows-GUI配置工具-VM验收-v20.zip` 从功能源提交 `c2d8170469d9703ae6f7bb3aa58f9b99ec9a20de` 构建(功能提交 `677165e` + 首启修复 `c2d8170`;首版 v20 因该修复重建,旧件已删未交付)。独立复核:**53,938,526 字节/259 条目**,包级 SHA-256 `e033ee6e5cbae7437abd1d75168799d5381cec8298170dddf0b50cb814eb588d`;包内 `fakenet.exe`/`fakenet-GUI.exe` SHA-256 分别为 `abee4426432915d1de9ceca047c536f4d7e2af3cdb5c7d8b536884b84273c17e`、`77ff4f4e8a0fec9518248f4fc9f8d7b54ad5f5460325df0eebaf22786c23fd2a`,与 manifest 一致;平铺布局存在,无预置 `Logs`、无 `.sha256` 边车。
- **正式包主机冒烟**:从 v20 ZIP 解出 GUI:首次启动走 first-launch 分支(日志 `first launch`、无 `failed to load`、无 Traceback),默认配置与状态文件生成,标题恒定 `FakeNet-NG 配置工具`,优雅关闭(日志含 initialized/configuration loaded/exiting);再次启动无任何弹窗直接载入。包内 runner 宿主机未提权返回 `REFUSED/exit 2`。
- **剩余门禁**:隔离 VM 使用 v20 包重跑 A1-A9 并导回证据;v15–v19 包与既有验收结论不变。

### 12.24 内容行移至页签控件上方,Notebook 置底(2026-08-17,v1.20 误解返工为 v1.21)

#### 12.24.1 需求(用户)

把页签控件(Notebook)下方的内容行——配置路径行、校验摘要行、分隔线、底部操作栏——全部移到页签控件上方,让 Notebook 成为窗口最“底层”元素(占据剩余高度)。

**返工说明**:v1.20 将需求误解为“页签头移到 Notebook 底部”(`tabposition='s'`),经用户指正后于 v1.21 返工;误解实现所在的 v21 包未交付 VM,由 v22 取代。

#### 12.24.2 修订规格(v1.21)

页签头恢复默认位置(位于 Notebook 顶部);主窗口网格行序调整为:配置提示行 row0、配置路径行 row1、校验摘要行 row2、分隔线 row3、操作栏 row4、Notebook row5(`rowconfigure(5, weight=1)` 占满剩余高度,成为最底层元素)。各行的控件内容、交互与行为不变。

#### 12.24.3 验收标准(v1.21)

Notebook 的网格行号为主窗口内最大(最底层),其余内容行全部位于其上且保持 0-4 顺序;`tabposition` 不为 `'s'`(页签头在 Notebook 顶部);既有全部用例不回归;`py_compile`、`git diff --check` 通过;随部署包 v22 进入隔离 VM 验收。

#### 12.24.4 实施结果(2026-08-17,v1.21)

- 页签头恢复默认位置;网格行序调整为 提示行0、配置路径行1、校验摘要行2、分隔线3、操作栏4、Notebook5(`rowconfigure(5, weight=1)`,窗口最底层)。新增用例:Notebook 行号为主窗口最大、其余内容行全部在其上、`tabposition != 's'`。
- smoke **50 passed**;全量 **396 passed + 18 subtests,1 skipped**(既有基线);`py_compile`、`git diff --check` 通过。功能提交 `fix(gui): place notebook at window bottom with rows above`。
- 说明:v1.20 误解实现(`tabposition='s'`)所在的 v21 包未交付 VM;本轮 v22 包取代之。`test_restore_defaults_immediately_overwrites_bound_file_and_keeps_format` 在本轮出现过两次不可复现的偶发失败(定向与整包复跑 10+ 次均通过,且与本轮改动无关,持续观察)。
- **正式 v22 交付**:`dist\Windows-GUI配置工具-VM验收-v22.zip` 从功能源提交构建,包含 v1.19+v1.21 全部改动;manifest `v22`/`v1.21`/source_commit 一致;包级与 exe 哈希见独立复核记录。
- **剩余门禁**:隔离 VM 使用 v22 包重跑 A1-A9 并导回证据;v15–v19 包与既有验收结论不变;v20/v21 ZIP 保留在 dist 作为未采纳构建记录。
- **隔离 VM A1-A9 验收通过(2026-08-17)**:同一隔离 VMware 分析 VM(`win10h2-MalBox-20241110`,CIM `VMware, Inc.` / `VMware20,1`)。正式 v22 ZIP(包级 SHA-256 `c2468a816ccec7e9d7e248dcecf47486fac6a95441a878635d2ec06c3c11580`)以 `test\gui_vm\Run-Tests.cmd` 运行,证据导出至 `dist\Logs-GUI\Logs-guivm\20260817-201149\`:`results.tsv` 九行全 PASS(7 实测 + 2 等效);`fakenet.log` 七个生命周期标记严格按序、成功标记恰 1 次、禁止标记 0 命中;`smoke_config.ini` 三段单监听器,SHA-256 与 v15–v19 字节一致。三份证据 SHA-256:`results.tsv` `0b160dbc3feeb20e705b71f03ed83b1cb4ee8f19ca3cb2a15b8911d47550813b`,`fakenet.log` `23609f745cc8df31c2a663f2e3492a564fc8ab47da01c723958e542cd6823f0f`,`smoke_config.ini` `22e36cf9f6bd64d082d7f72fa914fb1ab99ef8f75b68b3c57003e8f82bfb5940`。**v1.21 全部门禁关闭。**
- **附带发现(待后续修复,不影响本轮验收)**:同批导出的包根 `Logs\`(`dist\Logs-GUI\Logs-root\`)显示 A9 的 GUI 冒烟实例在验收结束后仍存活(pid 4552)并被用户继续操作——PyInstaller onefile 双进程结构下 runner 的 `terminate()` 只结束引导父进程,真实 GUI 子进程成为孤儿(持单实例互斥体)。A9 判定本身有效(6 秒存活断言成立),但下一版应按镜像名做任务级清理。该目录同时是一次真实人工使用的完整记录:用户经 GUI 启动按钮拉起带 WinDivert 的完整核心(20:14:52 启动、成功标记出现、DNS 接管与进程重定向热路径工作、20:15:53 `Stopping...` 干净停止并产出 PCAP 双通道汇总与 HTML 报告),证明 v22 GUI→核心全链路在 VM 内可用。

### 12.25 监听器工作线程异常入日志与代理竞态修复(2026-08-17,v1.22)

#### 12.25.1 现象与根因(用户 VM 实机反馈)

GUI 启动 fakenet 后控制台出现海量 `Traceback ... AttributeError:
'NoneType' object has no attribute 'getOriginalDestPort'`(来自
`ProxyListener.get_top_listener`,UDP/TCP handle 调用),且**日志文件中完全
没有**这些 traceback。

1. **可观测性缺陷**:所有监听器的线程服务器类(ProxyListener×2/
   DNSListener×2/IRC/POP/SMTP/TFTP/Raw 共 9 个)继承
   `socketserver.ThreadingMixIn`,其默认 `handle_error` 把异常 traceback 用
   `print` 直写 stderr——绕过 logging 框架,控制台可见、日志文件不可见
   (HTTPListener 的自定义 `handle_error` 只记 `%s` 无堆栈,本轮不扩大范围)。
2. **时序竞态**:`fakenet.py` 非策略模式启动顺序为“监听器 `start()` 绑定并
   开始服务 → `diverter.start()` 开始把分流流量灌入 → 逐监听器
   `acceptDiverter` 注入引用”。窗口期内到达代理监听器的每个数据包都以
   `server.diverter=None` 调用 `getOriginalDestPort` 崩溃;用户 VM 双网卡
   环境 NBNS/mDNS/SSDP 噪声每秒多发,故“超级多”。

#### 12.25.2 修订规格(最小核心修复,经用户明确授权)

1. 新增 `fakenet/listeners/servermixins.py`:`LoggingThreadingMixIn`
   (`daemon_threads=True`)的 `handle_error` 改为 `logger.exception`(含
   客户端地址与完整堆栈;无 `self.logger` 时回退 `logging.getLogger
   ('fakenet')`);上述 9 个 `ThreadedTCPServer/UDPServer` 类基类换为该
   mixin。
2. `ProxyListener.get_top_listener`:`diverter is None` 时直接返回 `None`
   (无分流引用时无法得知原始目的端口;由 handle 既有 None 分支丢弃该包),
   不再抛 AttributeError。
3. `fakenet.py` `start()`:把 acceptListeners/acceptDiverter/
   acceptDiverterListenerCallbacks 注入循环整体移到 `# Start the diverter`
   之前,从源头消除“已分流流量命中未注入监听器”的窗口(策略模式路径不变:
   其依赖在 configure_dependencies 阶段更早注入)。
4. 不改:WinDivert 数据面、策略合同、INI 语义、fail-closed 门禁、GUI 行为。

#### 12.25.3 实施前自评

| 门禁 | 结论 | 依据/防守项 |
|---|---|---|
| 根因可判定 | 通过 | traceback 链与 fakenet.py 启动顺序逐一对应;两缺陷独立成立、独立修复 |
| 修改范围 | 最小 | 新增 1 个共享 mixin 模块 + 7 个监听器文件基类替换(共 9 个服务器类)+ get_top_listener 5 行守卫 + fakenet.py 块序搬移 |
| 行为影响 | 通过 | handle_error 仅改输出通道(stderr→logging,消息与堆栈保留);None-diverter 分支原本即崩溃路径,降级为丢弃;注入顺序提前消除的是崩溃源窗口 |
| 测试 | 通过 | 先红后绿:test_listener_error_routing.py 4 用例(None 容忍、mixin 入日志带堆栈、全模块 mixin 覆盖锚定、注入先于 diverter 启动的源序锚定);LISTENER_CLASSES 漂移锚定把 servermixins 列入与 ListenerBase 同类的排除名单(基础设施模块,不进监听器类型枚举) |
| 兼容 | 通过 | 全量 400 passed + 18 subtests(既有 1 skip 基线);GUI 双源真相不变 |

#### 12.25.4 验收标准

- 监听器工作线程任何异常以含堆栈的日志记录出现在日志文件(不再仅 stderr)。
- diverter 注入前到达代理监听器的包被丢弃,无 AttributeError。
- 既有核心/GUI/VM runner 用例全量无新增失败;v23 包过主机门禁后进隔离 VM
  验收,并在 VM 内用 GUI 启动完整核心复验控制台无 traceback。
- **实施与交付(2026-08-17)**:TDD 先红后绿;全量 **400 passed + 18 subtests,1 skipped**(既有基线);`py_compile`、`git diff --check` 通过;功能提交 `82d19ff`。正式 v23 包 `dist\Windows-GUI配置工具-VM验收-v23.zip`(53,946,018 字节/261 条目,包级 SHA-256 `30e31342ddfee5da8c2eb635624bf76a090a43d992354b291db2d598cd05b52c`;manifest `v23`/`v1.22`/`82d19ff`;包内 `fakenet.exe`/`fakenet-GUI.exe` SHA-256 分别为 `3ae9bea9a32ea368e338921c2cdfa87978b4d82ca93250cedb1141d25318f3c3`、`4c71d7fdf09c73a0abcc796bff59f09d653c134126edd88c9fe7dd7ce03152d7`)。主机冒烟:GUI 初始化/优雅关闭/无 Traceback,runner REFUSED/exit 2。**剩余门禁:隔离 VM 以 v23 重跑 A1-A9,并在 VM 内经 GUI 启动完整核心,确认控制台无 traceback、原异常以含堆栈日志记录出现在日志文件。**

### 12.26 验收证据一站式收集(2026-08-17,v1.23)

#### 12.26.1 需求(用户)

此前导出证据需要拷贝两个位置(测试目录 + 包根 Logs)并手动重命名;要求把包根
GUI 日志自动写入测试目录。

#### 12.26.2 修订规格

1. `run_gui_vm_acceptance.py`:`main()` 在创建证据目录后调用
   `snapshot_gui_logs()` 记录包根(脚本上两级)Logs 中全部 `.log` 的
   {文件名: mtime};`finish()` 调用 `collect_gui_logs(before)` 把**本次运行
   新增或 mtime 变化**的 GUI 日志拷入 `Logs\<时间戳>\gui-logs\` 并在控制台
   打印清单。拷贝为尽力而为(失败不判 FAIL);目录不存在时静默跳过(dev 树)。
2. `results.tsv` 保持有且仅有 A1-A9 九行(验收合同不变);A9 的孤儿 GUI 子
   进程可能仍在写日志,拷贝取 finish 时刻内容(可接受,后续孤儿修复见
   §12.24.4 附带发现)。
3. README 用法同步说明;纯 runner/文档改动,核心与 GUI 代码不变。

#### 12.26.3 验收标准与实施结果

- 纯函数测试:快照仅含已存在 `.log`;仅收集新增/更新文件(旧且未动的不拷);
  根目录缺失返回空。全量 **402 passed + 18 subtests,1 skipped**(既有基线);
  `py_compile`、`git diff --check` 通过。
- 交付:并入 v24 包(v23 因本轮改动未交付 VM,保留在 dist 作为构建记录)。
- **正式 v24 交付(2026-08-17)**:功能源提交 `afce13d44e189e3bea75b76e194fc1737fae4219`,包级 SHA-256 `bd827a20034263437f428ac05b51d2ef83814d292465261413ebd99dfb428f2d`,53,949,442 字节/261 条目;manifest `v24`/`v1.23`;包内 `fakenet.exe`/`fakenet-GUI.exe` SHA-256 分别为 `3f56c8c5b7a3d4cadc4544dfd723ce22b6151b1731a6e05bb00e6c866b05a6a1`、`7e94afc9d5191aa41de3c1518ff7076da8459aa1e2a0c27237f5d9ba02adc42a`;包内 runner 已含 `collect_gui_logs`。主机冒烟:GUI 初始化/优雅关闭/无 Traceback;runner REFUSED/exit 2(快照前退出,收集逻辑由单测覆盖)。**剩余门禁:隔离 VM 以 v24 重跑 A1-A9 + GUI 启动完整核心复验控制台无 traceback;证据仅拷贝 `test\gui_vm\Logs\<时间戳>\` 一处即可(含 gui-logs 子目录)。**
- **隔离 VM A1-A9 验收通过(2026-08-17)**:同一隔离 VMware 分析 VM(`win10h2-MalBox-20241110`,CIM `VMware, Inc.` / `VMware20,1`)。正式 v24 ZIP(包级 SHA-256 `bd827a20034263437f428ac05b51d2ef83814d292465261413ebd99dfb428f2d`)以 `test\gui_vm\Run-Tests.cmd` 运行,证据一站式导出至 `dist\Logs\20260817-205821\`(含 gui-logs 子目录,用户仅需拷贝一处):`results.tsv` 九行全 PASS(7 实测 + 2 等效);`fakenet.log` 七个生命周期标记严格按序、成功标记恰 1 次、禁止标记 0 命中;`smoke_config.ini` 三段单监听器,SHA-256 与 v15–v22 字节一致;`gui-logs\fakenet-GUI-20260817-205827-426973-p6368.log` 为 A9 GUI 实例日志(初始化/first-launch 分支正常,无 Traceback;无 exiting 记录属 §12.24.4 已记载的 A9 孤儿子进程现象)。四份证据文件 SHA-256:`results.tsv` `0da589491ec010b9dc24a6ee98433e9c52065a3c3ac86b308519bab07175c666`,`fakenet.log` `9d389f07c2d056c261b8bce4ae253bd9853bd7eb8979fe66a24a87950b1ec01c`,`smoke_config.ini` `22e36cf9f6bd64d082d7f72fa914fb1ab99ef8f75b68b3c57003e8f82bfb5940`(与 v15–v22 字节一致),gui-logs GUI 日志 `820fc3be86bdcfedbf2d4f6917c62b97b6810a7e5a3905e64daab66deca9a051`。**v1.23 全部门禁关闭。**
- **遗留待办(不阻塞验收)**:§12.25 的"VM 内经 GUI 启动完整核心复验控制台无 traceback"本轮证据中未见(`FakeNet launch requested` 未出现于 gui-logs);验收冒烟配置仅 RawTCPListener,不触发代理路径。下次 VM 会话用 v24 包做一次 GUI 启动完整核心并确认控制台无 traceback 即可关闭该项,无需重新打包。

### 12.27 DNS 枚举指针解引用与主异常打印去重(2026-08-17,v1.24)

#### 12.27.1 现象与根因(用户 VM 实机反馈,21:03 手动 GUI 启动)

用户经 GUI 以自存配置 `configs\fakenet_config1111.ini`(DomainAllowList)启动
完整核心,启动即崩:`AttributeError: 'LP_IP_ADDR_STRING' object has no
attribute 'IpAddress'`(fakenet.py main → windows.py `Diverter.__init__` →
`_select_external_dns_server` → winutil.py `get_dns_servers`)。

1. **指针未解引用**:`IP_ADDR_STRING.Next` 为 `POINTER(IP_ADDR_STRING)`;
   `get_dns_servers` 第一跳用内嵌结构体正常,沿 `Next` 取到指针后直接访问
   `.IpAddress` 崩溃。**≥2 个 DNS 服务器的主机必崩**(该 VM 双网卡);
   单 DNS 机器永远走不到第二跳,故此前策略模式 VM 运行未暴露。
2. **控制台重复打印**:主入口 except 同时 `logger.exception`(文件+控制台)
   与裸 `traceback.print_exc()`(仅控制台)——用户看到的"一大坨"实为两遍。

#### 12.27.2 修订规格(最小核心修复)

1. `winutil.get_dns_servers`:遍历时对带 `contents` 的节点解引用后再取
   `.IpAddress.String`;单节点/无 FixedInfo 行为不变。返回值仍为 bytes 字符串,
   调用方(_select_external_dns_server)既有 bytes 解码逻辑不变。
2. `fakenet.py` 主异常路径移除 `traceback.print_exc()`(logger.exception 已含
   完整堆栈,文件与控制台各一遍)。
3. 不改:DNS 选择策略、策略合同、数据面其余部分。

#### 12.27.3 实施前自评

| 门禁 | 结论 | 依据/防守项 |
|---|---|---|
| 根因可判定 | 通过 | traceback 逐帧对应 ctypes 定义;第二跳必崩由结构定义直接推出 |
| 修改范围 | 最小 | winutil 4 行守卫 + fakenet 1 行删除 |
| 行为影响 | 通过 | 修复前该路径必崩(≥2 DNS),无既有可依赖行为;打印去重不改变日志内容只去控制台重复 |
| 测试 | 通过 | 新增 3 用例:三节点链(内嵌+两级指针)、单节点、FixedInfo 缺失;全量 404 passed |
| 兼容 | 通过 | bytes 返回与调用方解码兼容 |

#### 12.27.4 验收标准与实施结果

- 单元:DNS 链 1/2/3 节点全部正确枚举;`get_network_params` 失败返回空。
- 全量 **404 passed + 18 subtests,1 skipped**(既有基线);`py_compile`、
  `git diff --check` 通过。
- **VM 复验(通过,2026-08-18)**:v25 包在 VM 内经 GUI 以 DomainAllowList 配置启动完整核心:
  不再崩溃、`_select_external_dns_server` 正常、控制台异常只打印一遍。
  (v24 的 A1-A9 验收结论不受影响——其冒烟配置不走该路径。)
  复验取证:v25 20260818-085339 与 v26 20260818-091036 两轮手动运行,DomainAllowList 配置下全日志 0 Traceback、解析器选择正常、异常只打印一遍。
- **实施与交付(2026-08-17)**:功能提交 `2c11653`(指针解引用+主异常去重)+ `297f8a7`(停止失败路径同样去重,首版 v25 重建,旧件已删未交付)。全量 **404 passed + 18 subtests,1 skipped**;正式 v25 包 `dist\Windows-GUI配置工具-VM验收-v25.zip`(53,950,126 字节/261 条目,包级 SHA-256 `d47987e4bacbe7e840c96f7f357705348fe7b963aa66d24e113d09198b1e9026`;manifest `v25`/`v1.24`/`297f8a7`;`fakenet.exe`/`fakenet-GUI.exe` SHA-256 分别为 `07ccfc3e5e911cdc77e14b635feaf7cd56b36483773cc368c5234ef25b925909`、`0107c37939399be02bb3746d93a7f6520b0c498e4d438022acfcc1020ce4da2e`)。主机冒烟:GUI 就绪/优雅关闭,runner REFUSED/exit 2。
- **隔离 VM A1-A9 验收通过(2026-08-17)**:正式 v25 包(包级 SHA-256 `d47987e4…9026`)以 `test\gui_vm\Run-Tests.cmd` 运行,证据一站式导出至 `dist\Logs\20260817-211431\`:`results.tsv` 九行全 PASS;`fakenet.log` 七个生命周期标记严格按序、禁止标记 0 命中;`smoke_config.ini` 与历轮字节一致;gui-logs 为 A9 GUI 实例日志(初始化/first-launch 正常,无 Traceback)。证据 SHA-256:`results.tsv` `bde71499ce71ba535fb2c1814d11d4820b6ab89b3c6e48536572fdc53caf9b9d`,`fakenet.log` `944f4b75dec5968bf43f06a3c9ee15671314cd336d203524f81c5f37bc75e928`,`smoke_config.ini` `22e36cf9…b5940`,gui-logs `7edfd3bda00216b5977a4a4212eaf661ab2f9cf0059197abade3c51e67e736ef`。**v1.24 A1-A9 门禁关闭;§12.27 的 DomainAllowList 手动复验仍在进行(见下)。**

### 12.28 Auto 上游 DNS 探测选择(2026-08-18,v1.25)

#### 12.28.1 现象与根因(v25 实机取证)

用户在 VM 以 DomainAllowList 配置(GUI 启动)测试 api.deepseek.com 未放行。
`Logs-root\fakenet-20260818-085339-…-gui-p5532.log` 证据链:核心正常启动
(v1.24 修复实机验证:无 Traceback、`DOMAIN_ALLOWLIST_READY dns=192.168.243.1
relay_port=38927`);两次 deepseek 解析请求均被策略正确识别并放行
(`ALLOW_INTERNAL_UPSTREAM`),但上游 `192.168.243.1` 将查询重置
(`ConnectionResetError` → `DNS_SERVFAIL`),客户端收到 SERVFAIL,后续连接无从
发生。

根因:`_select_external_dns_server` 的 Auto 模式取系统 DNS 列表**第一个**
语法合法的非本机候选;该 VM 双网卡,列表首个 192.168.243.1 是不可用解析器
(另一网卡 204 网段的 NAT DNS 可用)。**结论:非放行策略缺陷,而是上游解析器
选择在多网卡环境下的脆弱性。**

#### 12.28.2 修订规格(最小核心修复)

1. `windows.py` 新增模块级 `probe_dns_resolver(resolver, timeout=1.0, port=53)`:
   发送最小 DNS 查询(`probe.fakenet.invalid` SOA),任何 ≥12 字节应答即视为
   存活(REFUSED/NXDOMAIN 均算);OSError(重置/超时)视为不存活。port 参数
   仅为可测性。
2. `_select_external_dns_server` Auto 模式:收集全部合法候选后**按序探测前 3
   个**,选第一个应答者(探测耗时 ≤3×1s,发生在 Diverter 构造期,WinDivert 尚
   未启动,探测流量不受分流影响);跳过首个时记录告警;全部无应答回退首个候选
   并告警(保留旧行为)。显式配置 `ExternalDnsServer` 不探测(行为不变)。
3. 不改:放行判定、DNS/TLS 中继、数据面其余部分。

#### 12.28.3 实施前自评

| 门禁 | 结论 | 依据/防守项 |
|---|---|---|
| 根因可判定 | 通过 | 实机日志逐行对应:放行已发生、上游被重置、SERVFAIL |
| 修改范围 | 最小 | 1 个模块级探测函数 + 选择循环改造;探测在构造期、分流未启动 |
| 行为影响 | 通过 | 显式配置路径不变;Auto 首选应答时结果与旧版一致;仅“首选死、次选活”场景结果改善 |
| 测试 | 通过 | 6 用例:真实 UDP 应答器/死端口探测、跳过死首选、全死回退、首选应答不变、显式配置不探测;全量 410 passed |
| 兼容 | 通过 | 探测失败不影响显式配置;启动延迟上限 3s 可接受并已记录 |

#### 12.28.4 验收标准与实施结果

- 单元:6 用例全过;全量 **410 passed + 18 subtests,1 skipped**(既有基线);
  `py_compile`、`git diff --check` 通过。
- **VM 复验(2026-08-18,通过)**:v26 包在双网卡 VM 手动运行(GUI 拉起核心,Auto 上游
  DNS + DomainAllowList,用户配置 fakenet_config111.ini):核心日志第 3 行即
  `Auto upstream DNS: first candidate 192.168.243.1 did not answer; selected 192.168.204.2 after probe`,
  随后 `DOMAIN_ALLOWLIST_READY dns=192.168.204.2 relay_port=38927`(行 3/39);09:30:47、
  09:34:46、09:34:58 三轮完整放行链:`Received A request 'api.deepseek.com'` →
  `ALLOW_INTERNAL_UPSTREAM kind=dns resolver=192.168.204.2` → `DNS_LEASE_ADD
  ip=101.71.73.135/116.140.43.136`(真实公网 IP)→ `REDIRECT_TLS_RELAY` →
  `TLS_SNI_ALLOW sni=api.deepseek.com`(行 2722/3169/3390);PCAP 双通道 8496/8496 healthy,
  核心 rc=0、GUI 干净退出。证据:`dist\Logs\Logs-root\fakenet-20260818-091036-792046-gui-p6356.log`
  (745,283 字节,SHA-256 `4fe012edc1f1cb86d05f0afceab3d9b00d1acc7ac1c518eb82457b5f1e43c344`)与
  `fakenet-GUI-20260818-090813-749460-p6356.log`(3,096 字节,SHA-256
  `6b221d2ee1f866c782ef12ef405cd8f3a3c0ad2c638c40a0af040d113f9b5e8c`,首启默认加载/保存/VM 门/
  会话 rc=0 全周期干净)。v1.25 视为实机验证通过;v26 的 A1–A9 正式验收(results.tsv)仍待执行。
  同轮新发现 §12.29 重注入缺陷(不影响本项结论)。用户侧替代方案(v25 即可用)保留:
  在 GUI“上游 DNS”显式填 192.168.204.2(或可用公共解析器)。
- **实施与交付(2026-08-18)**:功能提交 `318a2bb`;正式 v26 包 `dist\Windows-GUI配置工具-VM验收-v26.zip`(53,956,655 字节/262 条目,包级 SHA-256 `73d494630c7a98c8f451cc76a96a8e1e91fe42b524d287de7709c1e5a40d916c`;manifest `v26`/`v1.25`/`318a2bb`;`fakenet.exe`/`fakenet-GUI.exe` SHA-256 分别为 `34166344f29677a03d39d7ff3bfcdf26c030a869d8e026e579f3006b835926ae`、`8d106a0bd75bee5672ba669fa9de54234e5bfe0c3ce86eb851dfe44a6913de2b`)。

### 12.29 DIVERT_FAKE 改写报文重注入 WinError 87(2026-08-18 取证,修复待授权)

#### 12.29.1 现象与影响(v26 实机手动运行)

- 会话:2026-08-18 09:10:49–09:35:53,v26 包、用户配置(DomainAllowList、Auto 上游 DNS、
  ExternalNonAllowedAction=Divert)。全日志 0 Traceback(v1.24/v1.25 修复保持)。
- 283 组三连错误:`ERROR: packet reinjection failed: [WinError 87] 参数错误` +
  `Failed to send outbound external TCP packet` + `TCP 192.168.204.130:<sport>->192.168.204.130:38926`,
  分布 09:14:37–09:35:26。
- 精确对照:282/283 紧跟 `DIVERT_FAKE original_ip=192.168.204.130 original_port=443`(即改写到
  GUI 默认唯一汇聚监听器 ProxyTCPListener:38926)之后;本机 53(129 次,未改写)与本机 80
  (37 次,同样改写 38926)均 0 失败;本机 443 改写共 1855 次,失败 282 次(约 15%)。
- 失败按流确定:61 个流(sport 落在 49832–50000 连续段),每流在 5–11 秒窗口内重复失败
  1–11 次后不再出现,直至客户端放弃;日志未记录 TCP 标志位,单流 SYN 重传与短周期重连
  仅凭日志无法区分(间隔 0–6 秒,非严格指数退避)。发起进程为 svchost/backgroundTaskHost
  等系统服务,目标均为假 DNS 应答的本机 443。
- 影响:这部分**非放行域名**的系统服务连接无法到达伪汇聚监听器,表现为连接不可达。失败方向为
  丢弃(fail-closed),**无外泄**;放行链路(deepseek)不受影响——仅 1 次中继 BrokenPipeError →
  `TLS_SNI_DENY`(09:32:20),随后两轮重试成功。

#### 12.29.2 根因实证(2026-08-18 第二轮取证,干净 VM + 双 PCAP 逐包比对)

干净 VM 重跑 v26 再次复现(会话 11:42:06–,40 次错误/13 流,同样集中在本机 443→38926),
并首次取得双 PCAP。逐包比对(脚本配对每个改写包与其改写前快照)结论:

**根因链条(每一环均有直接证据):**
1. **捕获层**:客户端对本机假 IP 的大块写入走环回路径的 TCP 分段卸载(TSO/LSO),
   WinDivert 捕获到的"段"是 1500 字节缓冲区,而其 IPv4 头 total-length 字段仍为整个
   大发送的总长(实测样本 2781/7161/9902;PCAP 记录 #1989/#1991/#2281:caplen=origlen=1500、
   ip.len 字段=2781 等,写入器 snaplen=262144 排除截断可能)。
2. **改写层**:FakeNet 经 dpkt 视图改写 dport(dpkt 重新序列化会把 ip.len 修正为 1500,
   故 PCAP mangled 快照自洽);但**发送走 pydivert 包对象**——pydivert 头部是捕获缓冲区的
   **原位视图**(`pydivert/packet/header.py:26` 起,`raw_property` 直接 `struct.pack_into`),
   `wdpkt.dst_port = …` 只补丁 TCP 字段,**IP 头 total-length 保持失配值**。
3. **驱动层**:`WinDivertSend` 收到"声称 2781 字节、实际 1500 字节"的报文,驱动解析
   IPv4 总长 > 提供长度 → 报文非法 → ERROR_INVALID_PARAMETER,即 [WinError 87]。

**相关性封口(616 个配对改写包):** 34 个错误(流,秒)中 33 个同秒存在 TSO 形态包,
唯一例外是该秒无配对包的 50ms 配对窗口伪影;反向**零误报——0 个携带 TSO 包的流未报错**。
时间轴证明:握手小包(52B SYN/40B ACK/小数据段)自洽、正常送达(每个失败流都收到了 SYN-ACK,
小数据段也被服务端 ACK),失败精确始于**首个 TSO 大数据段**,其重传因积压合并使失配值越滚越大
(2781→9902),直至应用放弃。这同时修正了第一轮"SYN 从未送达、连接从未建立"的错误推断。

**对既有观察的完整解释**:端口 80 流量仅小请求无大写入故零失败;deepseek 放行走以太网出口
(MSS 1460,真实分段自洽)不受影响;失败 sport 集中于 49832–50000 只是临时端口计数器当时位置
(成功流 sport 同区间共存已证);第一轮候选 A(pydivert 丢 Loopback/Impostor 标志)与候选 C/D
被排除——失败由报文长度失配单独、充分地解释。

#### 12.29.3 修复实施(2026-08-18 用户授权"开始执行",v1.26)

**影响面分析(授权前评估,已随授权采纳)**
- 触达面:`WindowsPacketCtx` 为 legacy 全分流模式与 DomainAllowList 策略模式**共用**,
  名义触达"Windows 数据面全部 diverted 流量";但修正条件(IPv4 total-length 字段 >
  实际捕获缓冲区长度)与"当前被 WinDivertSend 以 87 拒绝丢弃"的报文集合重合(616 个
  配对样本零反例),故对一切当前可正常收发的报文为**字节级 no-op**;唯一行为变化是
  TSO 失配段从"丢弃"变为"送达汇聚监听器"(恢复分流本意,并顺带修复上游原版在
  全分流模式下的同一缺陷)。
- 只收缩不放大;不改五元组、不触及放行判定/策略语义/监听器/§12.28 探测选择;校验和
  沿用 pydivert `send(recalculate_checksum=True)` 既有重算。pcap 初始快照今后记录
  修正后长度(取证呈现变化,已记录)。IPv6 不在范围(未观测到同类失败)。

**实现**
1. `WindowsPacketCtx.__init__`:读 IPv4 头 total-length,当 declared > len(buffer) 时
   `struct.pack_into(">H", view, 2, len)` 原位补丁 pydivert 发送视图,并记录
   `self.tso_length_fix = (declared, buffer_len)`;随后 fnpacket/dpkt 视图从补丁后
   字节构建,两个视图自此一致。正常报文(declared ≤ buffer)零改动、零开销一次比较。
2. `Diverter._send_packet`:检测 `tso_length_fix`,惰性计数器聚合,`TSO_LENGTH_FIX`
   egress 事件(declared/buffer/total 字段)首次立即上报、此后 30 秒节流,不逐包刷屏。

**测试与门禁**
- 单元(新增 `test/test_windows_tso_length.py`):失配缓冲区补丁生效且仅改 2:4 字节、
  正常报文与 padding(declared < buffer)字节不变、`_send_packet` 聚合上报与 30 秒节流。
- 全量回归对 410 passed 基线;`py_compile`、`git diff --check`。
- VM 复验(待):v27 包在 VM 大写入场景(系统遥测 POST)观察 87 错误清零、
  `TSO_LENGTH_FIX` 事件出现、TSO 大数据段可达 38926 汇聚监听器;A1–A9 照常执行。

**实施与交付(2026-08-18)**
- 提交:`2e2755b`(方案授权记录)、`9284889`(修复 + 测试)、`bf6ec08`(打包脚本 v27)。
- 测试:新增 4 用例全过;全量 **414 passed + 18 subtests,1 skipped**(基线 410,零回归);
  `py_compile`、`git diff --check` 通过。
- 正式 v27 包 `dist\Windows-GUI配置工具-VM验收-v27.zip`(53,962,978 字节/263 条目,包级
  SHA-256 `7fe7939426b423335adb7a4c10cb6b4b1d74b2f5b2302058c4771b0d3844f810`;manifest
  `v27`/`v1.26`/`bf6ec08`;`fakenet.exe`/`fakenet-GUI.exe` SHA-256 分别为
  `d86cb878d8ede4adbc7f439e9ded7edd151b58654001e9e02a50da299763e28d`、
  `e7cf2a71076d62f40f367396e82918e5872e78a9da356bb7d058fe3f22f5cdb9`)。
- 主机冒烟:v27 GUI 就绪 → WM_CLOSE → 干净退出 rc=0,日志无 Traceback、无残留进程;
  单实例互斥体行为顺带验证(孤儿实例在场时新实例拒绝启动)。
- **VM 复验(2026-08-18 12:25 会话,通过)**:v27 包在 VM 以同用户配置运行约 3 分钟(自然系统
  遥测大写入场景),三信号全部达成:
  ① 87 错误 **0 命中**(修复前同场景两轮分别为 283/40 次);
  ② `TSO_LENGTH_FIX` 事件出现,节流上报 4 条,累计 **107 次**(declared=1555/2781/2170/1789,
  与实证根因的 TSO 形态值吻合);
  ③ PCAP 端到端:259/259 改写到 38926 的流全部有回程流量,246 个服务端 ACK 完全覆盖
  客户端数据、1460 字节大段正常流动,其余 13 个为会话结束在途/收尾(非发送失败,与 0 错误
  一致);全 pcap(10,941 包)零长度失配——修复在构造期补丁,初始快照记录修正后长度,与方案
  预记录的取证呈现变化一致。
  附:deepseek 放行链无回归(TLS_SNI_ALLOW×2、0 DENY);核心 rc=0、PCAP 双通道 healthy、
  GUI 全周期干净。证据 SHA-256:`packets_20260818_122515.pcap`(3,214,603 字节)
  `2e878e8ef484125f4de88c6c1306adcb793641aba0af08290ebdd6df56379b30`;转换版
  `3198dcfa49a01fff1922877dcb58c8de95ef3c6915fa143ed5703661acd205bd`(3,367,777 字节);
  核心日志 `f15534aed99fd225bc03fa42555a42f6685636a0b9b8dfc3d9f6c26f8fb1e932`(663,300 字节);
  GUI 日志 `4ca9aaebc69e1129f485baca1f99bb1fe479d97b427a6b0c90af34802157b4ac`(3,096 字节)。
  **v1.26 修复门禁关闭**。
- **隔离 VM A1–A9 验收通过(2026-08-18 12:45)**:正式 v27 包以 `test\gui_vm\Run-Tests.cmd` 运行,
  证据一站式导出至 `dist\Logs\20260818-124518\`:`results.tsv` 恰九行全 PASS(7 实测 + 2 等效);
  `fakenet.log` 生命周期标记严格按序(startup → started successfully → stop flag found →
  Stopping → exiting rc=0),禁止标记(Traceback/异常退出)零命中;`smoke_config.ini` SHA-256
  `22e36cf9…b5940` 与 v15–v26 历轮字节一致;gui-logs 为 A9 GUI 实例日志,无 Traceback。
  证据 SHA-256:`results.tsv` `5a673d69b226ec3bcd5011aca7f65ba995dd3fd8126a06ac3f7f670d391b87b2`,
  `fakenet.log` `a2b7d2129d47bdc9e9fa05142e323bc36c1f43ff14f12cab3413d398be51bf2b`,
  `smoke_config.ini` `22e36cf9f6bd64d082d7f72fa914fb1ab99ef8f75b68b3c57003e8f82bfb5940`,
  gui-logs `6dccd137e995cd97883ada0bc73d5283f8eb8380a3946de74b6e8ee636818fc2`。
  **v1.26 全部门禁关闭**;v26 未单独执行正式验收,由包含同一 DNS 修复的 v27 覆盖。

#### 12.29.4 证据补强(2026-08-18 已执行,结论见 §12.29.2)

- 干净 VM 重跑 v26(手动运行,GUI 拉起,用户配置 fakenet_config1111.ini):探测修复保持
  (日志第 3 行跳过 243.1 选中 204.2),再次复现 40 次 87 错误/13 流。
- 用户按 §12.29.4 原方案导出包根双 PCAP + 两份日志,逐包比对完成根因裁决(§12.29.2)。
  证据 SHA-256:`packets_20260818_114207.pcap`(1,363,486 字节)
  `c2c2e211395913e10096030fd120bc4427a9444c85e6bef0097e3c044440c388`;
  `packets_20260818_114207-converted.pcap`(1,412,220 字节)
  `c2bf300f7035a15b187ec2abc04e8dc89d1dfa6af822702969aea9d60f666a98`;
  `Logs\fakenet-20260818-114204-667677-gui-p5036.log`(232,064 字节)
  `348851b20d27eed9ec9d3b5657cf9d770d856380f69da3b9a127cfa89a0b84ee`;
  `Logs\fakenet-GUI-20260818-114143-546165-p5036.log`(2,905 字节)
  `77926b1e36ff7ccd56a42c0843eb5b4c2cf321173d14cf3c837270204f83af02`(GUI 生命周期干净)。
- 本轮仍为手动运行,v26 的 A1–A9 正式验收(results.tsv)继续待执行。

### 12.30 GUI 反馈四项与遗留事项收尾(2026-08-18,v1.27)

#### 12.30.1 窗口缩放截断修复
- 现象:横向拖小窗口时组件被截断——静态下限 `MAIN_WINDOW_MIN_SIZE`(1350,960)低于布局实际需求。
- 修复:`_build_layout` 后 `update_idletasks()` 实测 `winfo_reqwidth/reqheight` 设为 minsize,
  任意字体/DPI 下下限精确等于布局需求;默认尺寸 1470×1050 不变。测试断言 minsize ≥ reqsize。

#### 12.30.2 保存按钮 dirty 联动
- `_refresh_dirty`:绑定文件且无未保存修改 → 禁用;dirty 或未绑定文件(新建配置必须可保存)→ 可用。
- 与既有"默认配置不可变"(未 dirty 时 save() 对默认文件 no-op)语义一致,UI 现在可见地表达。
- 测试:新建可用 → 绑定+无修改禁用 → 修改启用 → 保存后禁用。

#### 12.30.3 默认值调整(用户指定,实验室约定)
- 新配置 ProxyTCPListener 端口 38926 → **8080**(仅 TCP;UDP 维持 38926)。
- 私网接管复选框启用时 ExternalTakeoverIPv4 预填 '' → **192.168.204.1**(TTL 60 等既有预填不变);
  接管仍为"键存在=启用"语义,默认配置不含该键,不改变总开关默认行为。
- ExternalProcessRedirectTargetIPv4 默认 '' → **192.168.204.1**:核心与校验器在
  ExternalProcessRedirectEnabled≠Yes 时均提前返回,预填完全惰性。
- **基线影响**:new_config 渲染新增 `ExternalProcessRedirectTargetIPv4: 192.168.204.1` 一行,
  smoke_config.ini 哈希基线自 v28 起更换(旧基线 `22e36cf9…b5940` 对应 v15–v27)。

#### 12.30.4 多域名/多 IPv4/通配符评估结论(用户问询)
- **多域名(ExternalAllowedDomains)**:解析/匹配/租约/中继按域名独立,机制支持 N 个;新增单测
  (双域名各自解析、父/子域名不误匹配、双域名各自建立 relay 映射)。此前单测仅单域名、实机仅
  deepseek——策略级已补测,数据面路径与单域名同构。
  **限制(决策点,未改)**:开启私网接管时核心硬性仅允许 api.deepseek.com
  (egresspolicy.py `reviewed takeover mode permits only api.deepseek.com`),多域名+接管组合
  直接拒绝启动;单测已锚定该现状。是否放开属核心策略变更,待用户另行授权。
- **公网 IPv4 直连(ExternalAllowedIPv4Rules)**:多条规则充分测试——单测覆盖 4 规则/2 IP、
  通配端口+精确端口、跨协议、绑定与状态精确性、冲突/重复/上限 fail-closed;v18 包
  (Windows公网指定IPv4放行)曾整轮实机验收。结论:充分。
- **通配符**:域名**不支持且为设计性禁止**(normalize_hostname 拒绝 `*`,仅精确 FQDN + IDNA
  归一;父/子域名均不匹配,单测锚定)。IPv4 规则中**端口支持 \***(单 IP 通配与精确端口互斥),
  IP 本身必须精确全球单播。

#### 12.30.5 遗留事项收尾
- **A9 孤儿进程**:runner 新增 `kill_leftover_processes()`——A9 冒烟判定后与 `finish()` 首位
  按镜像名 `taskkill /F /T /IM fakenet-GUI.exe`、`fakenet.exe`(仅 VM 验收上下文可达;
  未找到=正常静默)。单测覆盖调用参数/flags 与静默分支。
- **手动日志一键导出**:新增 `test/gui_vm/Export-Logs.cmd`(免提权)+ `Export-Logs.ps1`——
  包根 `Logs\*.log`、`packets_*.pcap`、`report_*.html` 集中拷贝到
  `test\gui_vm\Logs\manual-export-<时间戳>\`;README 补文档。宿主功能测试以临时包布局实测
  拷贝正确且排除无关文件。

#### 12.30.6 测试与状态
- 全量 **422 passed + 18 subtests,1 skipped**(基线 414,+8:保存联动 2、多域名/通配符/接管锚定 3、
  runner 清理 2、导出功能 1);`py_compile`、`git diff --check` 通过。
- **未打包(用户指令"先别打包")**:以上并入 v28,待用户放行后构建。

### 12.31 多域名接管放开与域名通配符(2026-08-18,v1.28,用户授权)

#### 12.31.1 规格与语义
1. **接管多域名放开**:删除 egresspolicy `takeover_enabled` 下 `allowed_domains != {api.deepseek.com}` 即拒绝的硬约束——接管模式下配置的全部域名照常放行,其余域名仍解析到私网分析主机。接管强制 `ExternalNonAllowedAction=Divert`、TTL/ResponseA/探测等既有约束全部保留。GUI 同步移除:接管复选框的替换确认弹窗、域名控件的条件锁定(COND_TAKEOVER_DOMAINS)、_sync_active_egress_policy 的域名强制、校验器的同名规则;`DOMAIN_TAKEOVER_READY` 事件改为输出实际域名/通配清单。
2. **域名通配符**:配置项支持 `*.example.com` 形式(仅允许前导 `*.` 单层通配;`*`、`a.*.com`、`*a.example.com` 一律拒绝)。语义:`*.example.com` 匹配其**任意层级子域名**(a.example.com、a.b.example.com),**不含裸域本身**(需并列 example.com);查询名匹配是精确 FQDN + 后缀判定,不存在部分标签匹配。接入点:resolve_dns_rule(命中返回实际主机名)、replace_leases/register_alias 的守卫(通配覆盖的主机名可建立租约/别名,租约键为实际主机名),TLS SNI 校验保持实际 SNI == 租约域名不变。`normalize_hostname` 对查询名保持严格禁 `*`。

#### 12.31.2 影响面自评
- 触达:egresspolicy(解析/匹配/租约守卫 + 删约束)、windows.py 就绪日志、GUI schema/app/validator 同步、测试。数据面其余(中继/NAT/IPv4 规则/进程重定向/探测选择)不动。
- 安全方向:通配符仅用于**放行匹配**,不会把非放行流量导向公网(非匹配域名仍走接管/分流/丢弃路径);通配范围越大放行面越大,由用户显式配置并负其责。SNI 精确校验保持,防租约外主机名搭车。接管多域名化为既有机制的自然泛化,无新增旁路。
- 兼容:单域名/无通配配置行为不变;旧配置(含接管+单 deepseek)继续合法。

#### 12.31.3 测试与门禁
- 核心单测:通配语义(子域名多级命中/裸域不命中/后缀陷阱不命中)、通配主机名租约+别名+中继、语法拒绝矩阵、接管+多域名+通配组合构造与解析。
- GUI:接管开关不再改写域名列表、域名控件接管下可编辑;校验器接受多域名/通配、拒绝非法通配。
- 全量回归对 422 基线;`py_compile`、`git diff --check`。
- **实施(2026-08-18)**:核心 `egresspolicy.py`(`normalize_allowed_domain`/`_parse_allowed_domains`/`_match_allowed_domain`,解析拆分精确/通配两个不可变集合,resolve_dns_rule/register_alias/replace_leases 三守卫接入,删除接管单域名约束);`windows.py` `DOMAIN_TAKEOVER_READY` 输出实际域名/通配清单(截断 240 字符)+ 数量;GUI 同步(schema 删 COND_TAKEOVER_DOMAINS 与提示更新、app 删替换弹窗/强制改写/条件锁定、validator 删同名规则且域名校验接受前导 `*.` 通配)。语义要点:`*.example.com` 匹配任意层级子域名、不含裸域;查询名侧 `normalize_hostname` 仍严格禁 `*`;SNI 校验保持实际 SNI == 租约域名。单测:通配命中/裸域不命中/后缀陷阱、通配主机名租约+别名+中继、语法拒绝矩阵、接管+多域名+通配组合;GUI:开关不改写域名列表且接管下可编辑、校验器接受/拒绝分支。提交 `f6dcd3e`(核心+策略测试)、`8594b8e`(GUI+GUI 测试)、`860df1b`(方案授权记录)。
- 全量 **424 passed + 18 subtests,1 skipped**(基线 422);`py_compile`、`git diff --check` 通过。未打包,并入 v28。
- **VM 复验(待,并入 v28 一轮)**:接管+多域名(含通配)配置实机验证——通配子域名解析放行、裸域按接管导向、SNI 校验保持;A1–A9 照常(smoke 基线按 §12.30.3 更换)。

#### 12.31.4 策略功能一键测试脚本与 v28 交付(2026-08-18)

**一键测试脚本(用户要求包内自带)**:`test/gui_vm/Run-Policy-Tests.cmd`(自提权,一次 UAC,此后免操作;物理机/非确定 VM 状态拒绝——会启用真实流量劫持)→ `run_policy_feature_tests.py`:
- 阶段 1(域名放行,`api.deepseek.com,*.deepseek.com`):GUI 写出器+校验器生成配置并启动真实 fakenet.exe;nslookup 触发 4 查询;断言 精确域名/通配子域名(www.deepseek.com)获得 **全球单播** DNS 租约、裸域 deepseek.com 与未放行 example.com **无**真实租约、四查询均到达 DNS 监听器、停止旗标 rc=0。
- 阶段 2(私网接管,同域名清单,sink 192.168.204.1):断言 `DOMAIN_TAKEOVER_READY` 清单包含 两项、裸域应答为 sink IP(nslookup 解析,排除 DNS 服务器地址)、通配子域名照常放行、rc=0。
- 证据(两份配置/核心日志/policy-results.tsv)一站式落 `test\gui_vm\Logs\policy-<时间戳>\`;结束按镜像名做任务级清理(§12.30.5 同机制)。`DOMAIN_TAKEOVER_READY` 域名清单改为无空格逗号连接,保证日志 kwargs 可解析。
- 宿主单测(5 用例):配置构造器与校验器一致(含接管 ResponseA 拓扑与锁字段显式化)、租约解析过滤私网应答、就绪清单解析、nslookup 输出抽取。
- 全量 **429 passed + 18 subtests,1 skipped**(基线 424);提交 `f7f12a1`。

**v28 交付**:`dist\Windows-GUI配置工具-VM验收-v28.zip`(53,980,119 字节/268 条目,包级 SHA-256 `9e8397cb8a0a3978d68ef8a43cbeaaa05273d2a59dd1473e24cc798973274c20`;manifest `v28`/`v1.28`/`ef30a4c`;`fakenet.exe`/`fakenet-GUI.exe` SHA-256 分别为 `e12986646bde519a363ca0a1fab70e1705492d8dcf9446c97e86fad95ed38658`、`8a51d25f819cee85cf9dd38a8e31e7e11e338910f8bc6bd315d09a7fd2253dd5`)。包内已验证含 Run-Tests.cmd、**Run-Policy-Tests.cmd + runner**、Export-Logs.cmd/.ps1 五件套。宿主冒烟:GUI 就绪→WM_CLOSE→干净退出 rc=0,无残留。

**v28 实机验收(2026-08-18 13:53–13:57,通过/部分通过)**:证据 `dist\Logs-v28\`。
- A1–A9 九项全 PASS;`fakenet.log` 生命周期严格按序 rc=0、禁止标记零命中;gui-logs 干净。
- **smoke 新基线兑现**:`smoke_config.ini` 934 字节,SHA-256 `9e0560542773760bdfd52adcb096a308980b1837650a69a2de5a8922300cda35`,新增行正是
  `ExternalProcessRedirectTargetIPv4: 192.168.204.1`(§12.30.3 预测);results.tsv `5bca35cd…468`、fakenet.log `30bcc923…745`。
- 策略一键测试 6/10:P1/P4/P6/P7/P8/P10 过(**接管多域名就绪、裸域导向 sink、两阶段 rc=0 均实机验证——解除的接管限制在实机工作**);P2/P3/P5/P9 失败。
- **P2/P3/P5/P9 根因(运行器缺陷,核心无罪)**:phase 日志显示全部 nslookup 查询被连接特定 DNS 后缀搜索追发为 `api.deepseek.com.localdomain` 等形式(VMware NAT 适配器后缀),无一个裸 FQDN 到达,放行匹配从未触发;租约断言与查询覆盖检查因此落空。单测语义、DOMAIN_TAKEOVER_READY 清单、P8 sink 应答共同排除 v1.28 核心回归。
- 修复(`a8a6a4c` 前置提交):nslookup 以 `名称.`(尾点 FQDN)查询杜绝后缀追发;查询覆盖检查改前缀匹配容忍后缀形态;新增宿主单测。全量 **430 passed + 18 subtests,1 skipped**。
- **v29 交付**:`dist\Windows-GUI配置工具-VM验收-v29.zip`(53,980,572 字节/268 条目,包级 SHA-256 `cbc9a77f39ef37e1abebc9bff3114270b2a3193bdb395262679a2667070d60f7`;manifest `v29`/`v1.28`/`a8a6a4c`;exe 哈希 `40717fa6…646`/`4dc10284…c59`);包内已验证修复后 runner 在列。宿主冒烟通过。

**v29 复跑(2026-08-18 18:27,十项全 PASS,v1.28 关闭)**:证据 `dist\Logs\policy-20260818-182742\`。四查询均以裸 FQDN 到达(无 .localdomain 后缀,
尾点修复生效);api.deepseek.com 与 www.deepseek.com(经 *.deepseek.com 覆盖)各租得同一组
3 个真实公网 IP(101.71.73.135/116.140.43.136/116.153.78.163);deepseek.com 与 example.com
零租约;接管清单完整、裸域应答 sink 192.168.204.1;两阶段 rc=0、零重注入错误、零 Traceback。
证据 SHA-256:`policy-results.tsv` `aaeeaebff70f4b16315c69b77bb00a4e0e810f8b535a4e4bcc584dff26f3e576`;`phase1.log` `bfb74e00bc4d4a6fc62ab78f2a0a8603b63cf21cfa40f8d81b38d5f44f3ef43a`(73,023 字节);
`phase2.log` `de88c202a0d7fbaa0569847fc4dfcd80e52acbccfe3023a73e917bda10604b22`(67,018 字节);
`policy1.ini` `d9c603cfb54b1f956e86492febf0bf2f1534b609b7dfa17de06b363b3a70e35f`;`policy2.ini` `9c5b9e585bff4cd69de0cd3a3b5e672b5b3877b6580bcba0b42b81e213150f25`。
**多域名接管放开与域名通配符完成实机端到端验证,v1.28/v1.27/v1.26 全部关闭;交付版本 v29。**

### 12.5 VM 一键验收脚本(2026-08-14 追加,应用户需求)
`test/gui_vm/Run-Tests.cmd`(自提权,仅开头一次 UAC 同意,此后免操作)→ `run_gui_vm_acceptance.py`,覆盖 §6 验收清单 9 项(A1–A9),逐项标注证据级别(实测/等效),产出 `Logs\<时间戳>\results.tsv` + fakenet 运行日志,退出码 0/1/2(PASS/FAIL/REFUSED)。物理机/未提权/检测不确定均 REFUSED(本机物理机已实测 REFUSED+退出码 2)。为可确定性验证 UAC 取消分支,launcher 抽取了纯函数 `interpret_shell_result`(重构无行为变化,单测覆盖)。
配套改动:`test/gui_vm/{Run-Tests.cmd, run_gui_vm_acceptance.py, README.md}`、`fakenet/gui/launcher.py`(interpret_shell_result 抽取)、`test/test_gui_launcher.py`(返回值解释单测)。A6–A8 依赖 fakenet.exe 镜像名,缺失时明确 SKIP。

**VM 部署包(2026-08-14 追加;v2 修正直接双击布局)**:`Build-GuiVmPackage.ps1` 按仓库 Build-* 惯例产出 `dist\Windows-GUI配置工具-VM验收-v2.zip`。v1 在 VM 验收后暴露布局缺口:exe 直接双击(不带 -c)按冻结版语义寻找 **exe 旁** `configs\default.ini`,而 v1 为纯源码树布局(default.ini 在 `fakenet\configs\`),导致 SystemExit——连带 frozen GUI 模板菜单为空。v2 修正:平铺官方 release 布局(`configs\`、`defaultFiles\`、`listeners\ssl_utils\`)到包根 exe 旁(与 build.yaml 的 release 组装一致),并对三件套做构建期断言。原始产物为 v1——git archive 全源码 + 从暂存树内构建的 `fakenet.exe`/`fakenet-config.exe`(包内二进制与包内源码对应,manifest 以 SHA-256 绑定)+ 验收负载;确定性 ZIP(固定 2000-01-01 时间戳、拒绝覆盖版本包、无 Logs、无 .sha256 边车)。已实测构建并校验(227 条目、exe 哈希与 manifest 一致)。附带发现:老 `fakenet.spec` 在 PyInstaller 6.22 下可正常构建(审计曾担忧的 TOC 断点实测不存在);PS 5.1 下 PyInstaller stderr 与 EAP=Stop 的冲突已在脚本内处理;PS1 中文字面量需 UTF-8 BOM(既有惯例)。

~~本方案未经审核通过,不得实施。~~(v0.2 时已审核通过;v1.0 已实施,见 §12。)

## 12.32 v1.29 直连兜底专项测试 + 进程网络视图 + 术语更名(2026-08-18 立项,待授权)

### 12.32.0 背景与勘误

用户以五段式恶意软件行为链(真实联网探测→默认DNS C2→未知公网IPv4直连→裸8.8.8.8
DNS→DoH)审问当前覆盖能力。裁定:第1/2/4/5项由现有"放行+接管+未审核IPv4一律Divert"
架构覆盖;第3项同样覆盖,但其依据是 `ExternalNonAllowedAction=Divert` 的 IP 层兜底
(windows.py `_handle_policy_packet` 末段→`handle_pkt`→`maybe_redir_port/redir_ip`),
**不是**进程重定向。勘误:此前口头表述"进程维度可把样本进程所有TCP改写"有误,
processredirect.py:318 出站包 `target_ipv4 != rule.original_ipv4` 即 NOT_APPLICABLE,
进程重定向=进程身份+目的IP(A)双重匹配,GUI 要求填写"原公网IPv4(A)"与此相符。
IPv6 明确排除在本期范围外(现行为:出站外部 IPv6 直接 DROP,阻断而非接管)。

行为链逐项机制(代码依据):
1. 放行域名真实联网:真实上游解析+租约+SNI 校验 relay(v27/v29 已实测)。
2. 默认 DNS C2 接管:接管地址应答(v29 P1-P10 已实测)。
3. 未知公网 IPv4 直连 cc2:非审核IP、非接管地址、无租约→Divert 兜底改道默认监听器。
4. 裸 8.8.8.8 DNS:仅配置的真实上游被登记控制流(egresspolicy.py:859);其余 53 端口
   流量被 Divert 后经 `maybe_redir_port` 改写至本机 DNS 监听器(策略模式强制
   UDP/53+TCP/53 双监听,windows.py:662-669),假应答返回接管地址,后续直连仍入彀。
5. DoH:非53端口看不到域名,但目的公网IPv4未审核→同第3项IP层兜底;即使DoH因放行
   通配(如 *.google.com 覆盖 dns.google)意外成功,后续直连真实IPv4仍被第3项兜底截住。

### 12.32.1 专项一键测试(整合进现有 Run-Policy-Tests)

在 `test/gui_vm/run_policy_feature_tests.py` 追加两个相位,复用现有 verdict/日志框架:
- P11 未知公网IPv4直连兜底:测试进程向一个**未列入审核清单**的公网 IPv4(如
  93.184.216.34:80)发起 TCP 连接;PASS 判据=fakenet.log 出现
  `DIVERT_FAKE original_ip=93.184.216.34` 且样本侧连接被假监听器应答(TCP 握手完成、
  收到响应字节),同时该 IP 无 `ALLOW_REVIEWED_IP`。
- P12 裸解析器 DNS 拦截:`nslookup <未放行域名> 8.8.8.8`(带尾点FQDN,防后缀搜索);
  PASS 判据=应答地址等于接管地址 192.168.204.1(而非真实IP),且 fakenet.log 记录该
  查询经本机监听器应答(DNS 日志行)。反向对照:`nslookup <放行域名> 8.8.8.8` 若
  8.8.8.8≠配置上游,同样应答接管地址(放行域名的真实解析只经配置上游通道)。
宿主侧新增单测覆盖判定函数;VM 实测通过才算关闭(沿用 v29 流程)。

### 12.32.2 GUI 新增"进程网络访问"窗口

目标:按 进程名/PID/进程树 过滤查看各进程的网络访问日志。设计:
- 数据面:数据面已具备按需 owner 解析(winutil GetExtendedTcpTable 封装+进程身份
  API,进程重定向热路径已用)。新增:仅对**新建 TCP 流(首个SYN)** 做一次 owner
  查询并按 (源端口) 缓存至流结束,发出 `PROCESS_FLOW` 事件字段:
  pid/process_name/final_path/proto/目标IPv4/目标端口/处置(DIVERT_FAKE|
  ALLOW_REVIEWED_IP|RELAY|TAKEOVER_SINK)/关联域名(有租约时)。UDP 仅对 53 端口外
  首包做同样归因。热点风险:SYN 才查+缓存,复用进程重定向的延迟仪表监控,超阈值
  (复用 OWNER_QUERY_LATENCY)自动降级为"未知进程"不阻塞。
- 事件落盘:fakenet.log 同源(log_egress_event 通道),GUI 不新增采集通道。
- GUI:主窗口新增"进程网络视图"按钮→独立 Toplevel:树形列表(进程树按 parent PID
  折叠)+过滤栏(进程名子串/PID/处置类型)+暂停/继续+跟随滚动;数据源=跟踪解析
  fakenet.log 增量行,只读,不影响主配置流程。
- 影响面:核心新增热路径一次SYN期owner查询(有降级);GUI 新窗口+解析器;验收脚本
  追加 A10(VM 实测:启动样本进程产生直连,窗口按进程名过滤可见对应 PROCESS_FLOW)。

### 12.32.3 术语更名:去除"DomainAllowList 策略模式"字样

现名已名不副实(现策略=放行+接管+进程重定向+IP层兜底的完整出站管控)。更名方案:
- 概念名/UI/文档:**出站管控(EGRESS_CONTROL)**。
- 配置值兼容:`ExternalAccessPolicy` 读取时同时接受 `DomainAllowList`(旧,含v15-v29
  交付包生成的配置)与 `EgressControl`(新);GUI 写入新值;校验错误文案同步。
- 代码标识符:`domain_allowlist_mode`→`egress_control_mode` 等,全量更名(64处/
  18文件,纯机械);日志事件名 `DOMAIN_ALLOWLIST_READY`→`EGRESS_CONTROL_READY`,
  同步更新本仓库内全部解析方(验收/策略测试脚本、单测),历史导出日志不做迁移。
- 不改:ini 键名 `ExternalAccessPolicy`、其余 `External*` 键名、pcap/报告格式。

### 12.32.4 影响面与复杂度自评

- 12.32.1(专项测试):低。仅测试脚本+单测,无核心改动;VM 一轮。
- 12.32.2(进程网络视图):**高**。核心热路径新增 owner 归因(有SYN才查+缓存+降级,
  预计稳态开销≈进程重定向已验证水平),GUI 新窗口,验收新增 A10。分两步交付:
  先数据面+日志(VM 验证开销),后 GUI 窗口。
- 12.32.3(更名):中。机械但面广;配置值双向兼容保证旧包配置可读;日志事件更名使
  新旧包日志字段不一致,验收解析方同步更新,需在 README 标注分界版本。
- 顺序建议:12.32.3 先行(后续新代码不再产生旧术语)→ 12.32.1 → 12.32.2。
- 版本:方案 v1.29(本节),实现落 v1.30(更名)/v1.31(测试)/v1.32(进程视图),
  交付包 v30 起。

**状态:已获授权并实施完毕(2026-08-18)。宿主门禁全关;VM 门禁待实机执行 Run-Tests(A1–A10)与 Run-Policy-Tests(P1–P13)。交付版本 v30。**
### 12.32.5 实施记录(2026-08-18,v1.30/v1.31/v1.32)

顺序按 12.32.4 建议:更名 → 专项测试 → 进程视图。

**v1.30 术语更名(提交 4c6ec9f)**:
- 机械更名 64+ 处/18 文件:概念名 EgressControl;标识符 egress_control_mode/
  build_egress_control_filter/ensure_egress_control_topology;日志事件
  DOMAIN_ALLOWLIST_READY→EGRESS_CONTROL_READY(仓库内全部解析方同步:
  gui_vm/egress_control_vm/process_redirect_vm/public_ipv4_allowlist_vm 脚本与单测);
- 兼容:ExternalAccessPolicy 读取双收(diverterbase 校验 + schema.
  egress_policy_enabled 别名助手;widgets/app×4 处/validator._policy_active 全部走
  助手),GUI 一律写 EgressControl;新增别名单测;
- 更名工件:configs/domain_allowlist_windows.ini→egress_control_windows.ini、
  Start-DomainAllowList.{ps1,cmd}→Start-EgressControl.{ps1,cmd}、
  test/domain_allowlist_vm→test/egress_control_vm(Run-EgressControlTests.ps1);
  模板 pcap 前缀同步 egress_control_packets(GUI 生成配置默认 packets 不受影响);
- 遗留审记:历史 PLAN 章节中的旧词为当时记录,不做追溯改写;README/default.ini
  保留一处"legacy alias DomainAllowList"说明性提及。

**v1.31 专项测试相位(提交 a48fb25)**:run_policy_feature_tests 新增阶段 3:
- P11 未知公网IPv4直连兜底:probe_direct_ipv4(93.184.216.34:80) 断言连接成功
  +DIVERT_FAKE original_ip 命中 + 无 ALLOW_REVIEWED_IP(同行双标记判定);
- P12 裸解析器DNS拦截:nslookup example.com 8.8.8.8(尾点FQDN)断言应答仅
  192.168.204.1 且查询到达本机监听器;P13 rc=0 干净退出;
- 判定函数(divert_fake_logged/reviewed_allow_logged/answers_only_sink/
  probe_direct_ipv4)宿主单测 9 项(含行级隔离防误判用例)。

**v1.32 进程归因+进程网络视图(提交 9ae4003)**:
- 数据面:finalize_egress_verdict 拆为 wrapper+_compute_egress_verdict,策略模式
  下每条出站流首包发出 PROCESS_FLOW(proto/src/sport/dst/dport/disposition/
  pid/process/domain);owner 每流一次(TCP FIN/RST 驱逐、120s 空闲清理、4096
  上限、任何内部异常即挂起审计,绝不影响包路径);
- 修复更名回归:egress_control_mode 判定此前仅认 domainallowlist,新值
  EgressControl 会以 legacy 模式运行(windows.py:472);
- GUI:procview.ProcessFlowWindow(进程→流两级树、进程名子串/PID/处置过滤、
  暂停/清空/自动滚动、独立日志文件模式),实时日志工具栏"进程网络视图"按钮,
  增量数据复用 _append_log_text 通道;
- 验收:A10(验收进程自身直连未审核 IPv4,断言 PROCESS_FLOW 含本进程 pid/
  映像名且 DIVERT_FAKE);
- 宿主门禁:全量回归 446 passed + 18 subtests,1 skipped(新增 12 测试:
  flow_audit 7 + procview 5)。

**构建 v30(提交 8df3cd5 后构建)**:Build-GuiVmPackage.ps1 v30/plan v1.32;
`dist\Windows-GUI配置工具-VM验收-v30.zip` SHA-256
`a941e41c7cdfd10993d3708f21354fbad6422f9c8d4a0c59d6249671c2b32b48`
(54,006,853 字节/271 条目,manifest v30/v1.32/8df3cd5);包内验证:procview.py、
P11/P12 runner、A10 验收、egress_control_windows.ini 均在;宿主 GUI 冒烟
(ready→WM_CLOSE→rc=0 零 Traceback)PASS。

**门禁状态**:宿主门禁全关;VM 门禁待用户实机执行
Run-Tests.cmd(A1–A10)与 Run-Policy-Tests.cmd(P1–P13)后裁定。


