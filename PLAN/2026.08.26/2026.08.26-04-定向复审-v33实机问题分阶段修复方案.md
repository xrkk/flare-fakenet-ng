# 定向复审记录：v33 实机问题分阶段修复方案

| 项 | 值 |
|---|---|
| 复审轮次 | 第 2 轮；针对第 1 轮审计处理结果的定向复审 |
| 复审日期 | 2026-08-26（Asia/Shanghai） |
| 复审环境 | 本地审核；直接读取仓库、方案、审计处理记录和本地 `dist` 证据 |
| 复审模式 | 定向复审：AUD-001～AUD-005、对应方案差异、受影响既有通过项和全局不变量 |
| 被复审方案 | `PLAN/2026.08.26/2026.08.26-01-v33实机问题分阶段修复方案.md` v0.7 |
| 方案 blob / SHA-256 | `2a1b7c84047a22280f228750f4af8513ac4ca718` / `7226a8a6625b73f24994037678c756beb2f70c6518e3f25edb98b726e14f96d9` |
| 方案状态 | `未审核, 不得实施` |
| 原审计记录 | `PLAN/2026.08.26/2026.08.26-02-审计-v33实机问题分阶段修复方案.md` |
| 原审计 blob / SHA-256 | `f389773442fe4a9071d92fc47232e22a41496abe` / `f7a9b5789ca62da6c8fbafc9ac74da860330a6e566902d4d06f59859915a49ec` |
| 审计处理记录 | `PLAN/2026.08.26/2026.08.26-03-审计处理-v33实机问题分阶段修复方案.md` |
| 处理记录 blob / SHA-256 | `d6cdddb53e1b6f3bc3728dd1407d42f23a6370c1` / `029f88e7e0898c04f0e56d29eb4dac362c4bcea562778d864adef50739f7a50f` |
| repository / 目标主分支 | `https://github.com/mandiant/flare-fakenet-ng` / `master` |
| 当前分支 / HEAD | `process-redirect-v23` / `411942fd6600b0388070f6e1268e8e0c149f708b` |
| audit_commit_sha | 无；本轮沿用本地审核，不伪造云端审核 commit |
| 复审结论 | **审计通过；AUD-001～AUD-005 全部闭合，无新增中高风险意见** |

本记录只审核并固化结论，不修改被审核方案、原审计记录、审计处理记录或实施代码。方案状态只有在本复审结果被正式引入后才能改为 `已审核, 可以实施`；本记录创建本身不授权实施。

## 1. 身份门禁、范围和漂移

### 1.1 身份门禁

1. v0.7 方案、原审计记录和第 1 轮审计处理记录的路径、版本、Git blob 值及 SHA-256 均与处理记录公布身份精确一致。
2. 原审计针对 v0.6，处理记录将其意见引入 v0.7；本轮只审核 v0.7 blob `2a1b7c8...`，没有把当前对话当作方案事实来源。
3. 原审计、处理和本轮复审均为本地审核；不存在 PR 交付或 `audit_commit_sha`，合并门禁不适用。
4. 原审计、处理和本轮复审时 HEAD 均为 `411942fd6600b0388070f6e1268e8e0c149f708b`，不存在提交级决策关键路径漂移。工作区其他用户修改不属于本轮门禁。

### 1.2 实际复审范围

- **核心范围**：v0.7 方案中 §1.1～1.2、IMP-002/005、ACC-002/003/006/007/009/010、FB-005、§7、§8.4、§9～10；原审计 AUD-001～AUD-005；第 1 轮处理记录全文。
- **直接证据范围**：`PLAN/FaknetNG.md` 的 SRC-REQ-001～004 切片；diagnostic-03 的 17 个导出文件及 `evidence-sha256.tsv`；Ubuntu Sentinel 活日志的指定 nonce 切片；Wine builder Dockerfile/脚本；`Build-GuiVmPackage.ps1`、`test/gui_vm/Run-Tests.cmd` 和 `test/gui_vm/README.md`。
- **受影响既有通过项**：原审计覆盖清单 #2、#6、#10、#11、#12、#15、#16。
- **明确排除**：原审计已通过且处理记录声明未受影响的 #1、#3～#5、#7～#9、#13、#14；未实施的产品代码；无关工作区修改、旧包和其他日志。

本轮没有重新扩大为全面审核，也没有把未实施的 `pytest` 安装或 `--image-only` 行为误当成当前代码已经具备的能力；审核对象是这些行为在方案中的实施与验收合同是否足以机械落地。

## 2. AUD 逐条独立复审

### AUD-001 规范性来源身份

- **复审结论**：闭合。
- **反驳尝试**：重新计算 `PLAN/FaknetNG.md` 身份，并检查 2026.08.18 恶意软件场景第 2 项、2026.08.21 第 2 项、2026.08.26 第 1～7 项是否与方案采用含义冲突或缺失。
- **证据**：当前 Git/worktree blob 为 `26b40c86a2807e49f55b480e0258f306de5768e2`，SHA-256 为 `bd4f05ace59567456ae91d76e69c4a97b28722b0f03592053b42db3db4b61922`，与 v0.7 一致；四个来源 ID 的定位条目均存在，停止、Ubuntu 真实终点、无本地 fallback、一键提示、证据导出和 1.0s/5.0s/三轮含义未改变。
- **受影响通过项**：覆盖清单 #2 恢复通过。
- **残余风险**：无。以后若该来源文件变化，必须重新计算身份并复核引用切片，不能沿用本轮结论。

### AUD-002 缺失 D2 证据与 D3 接替

- **复审结论**：闭合。
- **反驳尝试**：搜索活动方案合同是否仍依赖 `EV-D2`、`Logs-v33-d2` 或 diagnostic-02；重新校验 D3 是否同时覆盖 P-STOP、P-SINK 和修复前精确结果。
- **证据**：本地没有 D2 目录或 diagnostic-02 包；v0.7 对 D2 的两处剩余文字仅用于声明撤销和版本历史，不再充当活动证据。D3 `evidence-sha256.tsv` 的 17 个条目逐项 `sha256sum -c` 全部通过。D3 明确包含：TCP/UDP preflight PASS、系统 DNS 返回 `192.168.204.1`、target TCP/UDP FAIL、核心 `DIVERT_FAKE`/`DROP_EXTERNAL`、HTTPListener80 6421ms、完整停止 8141ms、GUI 反馈 0.000s、顶层句柄 24.172s、子进程至 one-file 父进程 15.974s和 bootloader 15 次重试。
- **受影响通过项**：覆盖清单 #6、#15、#16 恢复通过；P-SINK 根因和停止修复前基线没有断链。
- **残余风险**：D2 仍不可恢复，但它已退出活动证据集合，不再构成方案实施或验收依赖。

### AUD-003 Sentinel 活日志切片

- **复审结论**：闭合。
- **反驳尝试**：逐行读取原始日志，检查事件数、定位键唯一性、同 nonce 是否已有 target 事件，以及方案是否仍把可变整文件哈希当作永久身份。
- **证据**：当前日志精确为 7 行，SHA-256 为 `ccb1d4e9f03fe8c2f69300cbfb3cf241cb7cbf752fa7a632584d066a41500dce`。nonce `diag-633c3a4eb243464b8d9d52c3ce2b6130` 只在第 6～7 行出现，时间均为 `2026-08-26T18:06:30+0800`，分别为 role=`preflight` 的 TCP/UDP；没有同 nonce target。D3 Windows 结果使用相同 nonce 并记录 target TCP/UDP 失败。v0.7 以 nonce、时间、role、transport 定位事件，并通过不可变原审计记录固定当时的“无 target”结论；整文件 hash 只声明为复核快照。
- **原审计反例处置**：原审计“8 行”是计数错误；实际是 1 个 ready 加 3 组 TCP/UDP，共 7 行。v0.7 没有继承该错误。
- **受影响通过项**：覆盖清单 #6 的 Sentinel 锚点恢复通过。
- **残余风险**：活日志仍可追加，但不会改写已固定的历史事件；后续验收必须为新会话使用新 nonce 和独立证据 hash。

### AUD-004 ACC-002/006 固定测试环境

- **复审结论**：闭合于方案合同层。
- **反驳尝试**：检查现有 builder 是否真有可复用的 Windows Python/Wine seam、未来工具的工作目录/输入/输出/退出边界是否明确、ACC 命令是否会错误启动 FakeNet 核心或要求散落的手工装配。
- **证据**：当前 Dockerfile已固定 Windows Python 3.11.9、Python 安装器 SHA-256、pydivert 2.0.9 和 PyInstaller 6.22.0；当前 shell builder 已固定镜像名、安装器缓存/校验、Docker build 和 UID/GID。v0.7 只要求在该 seam 增加 `pytest==8.3.5` 与 import 检查，并把现有脚本扩展为仓库根目录执行的 `--image-only`：唯一产出为指定镜像，退出 0，不生成或覆盖 ZIP。ACC-002/006 给出同一镜像、Windows Python、工作目录、挂载、测试文件、输出证据和失败边界；两条命令均不得启动 FakeNet 主程序或 WinDivert。
- **机械执行边界**：Dockerfile 当前尚未安装 pytest，脚本当前尚未实现 `--image-only`，这是 IMP-002 明确列出的待实施内容，不是复审失败。实施后必须先由 ACC-002 的构建/import/版本证据证明环境成立；构建失败按 FB-005 判 FAIL，不能绕过或手工拼装结果。
- **受影响通过项**：覆盖清单 #10、#11、#12 恢复通过。
- **残余风险**：无未闭合方向性决定。镜像 ID、Python 和 pytest 版本必须进入验收证据，以避免只凭可变 tag 宣称同一环境。

### AUD-005 正式 VM 唯一入口

- **复审结论**：闭合。
- **反驳尝试**：全仓库搜索 `Run-All-Tests.cmd` 和正式验收入口，核对方案允许范围、三个 VM ACC、README 与打包 manifest 是否指向同一文件。
- **证据**：仓库不存在 `Run-All-Tests.cmd`；现有文件为 `test/gui_vm/Run-Tests.cmd`。`Build-GuiVmPackage.ps1` 的 acceptance entry、存在性门禁和 manifest 均指向该文件，README 也以它为正式一键入口。v0.7 IMP-005、ACC-003、ACC-009、ACC-010 全部唯一指向包内 `test\gui_vm\Run-Tests.cmd`，并禁止创建第二入口。
- **受影响通过项**：覆盖清单 #10、#11 恢复通过。
- **残余风险**：无。

## 3. 受影响通过项和全局不变量

| 对象 | 复审结论 | 依据 |
|---|---|---|
| 覆盖清单 #2 来源与证据身份 | 通过 | SRC-REQ blob/hash 可复现；D2 已退出；D3/Sentinel 新锚点可核验 |
| 覆盖清单 #6 P-SINK 根因 | 通过 | D3 Windows 结果、核心裁决、Ubuntu preflight/无 target 与代码证据互相一致 |
| 覆盖清单 #10 可交接实施性 | 通过 | 固定 builder seam 和唯一 VM 入口；无并列方向 |
| 覆盖清单 #11 ACC 契约 | 通过 | ACC-002/006 有未来工具的工作目录、输入、命令、输出、证据和失败边界；ACC-003/009/010 入口唯一 |
| 覆盖清单 #12 现实阻碍 | 通过 | FB-005 将构建或宿主测试环境缺失判为 FAIL，不能绕过验收 |
| 覆盖清单 #15 本地审核依赖 | 通过 | 只依赖 D3、Sentinel 定位切片和必要本地证据，不再依赖 D2 |
| 覆盖清单 #16 反例 | 通过 | 没有发现 D3 无法接替 D2、镜像命令需要第二 adapter 或入口仍有歧义的反例 |

以下全局不变量经核对未被 v0.7 修订改变：

1. 单机、单实例、单用户，不引入集群、高可用或远程协调。
2. Windows FakeNet/WinDivert 只在隔离 VM 运行；Ubuntu 宿主测试不启动核心。
3. P-STOP 与 P-SINK 独立实施和验收，只在 IMP-005/006 汇总。
4. HTTP 保持单线程正常语义；停止只中断当前 transport，不以多线程、daemon、降 timeout 或强杀达标。
5. 正式核心固定 onedir、包根唯一 `fakenet.exe`，不回落 one-file。
6. 精确 sink TCP/UDP 保持地址、协议和端口真实到达 Ubuntu，不回落 Windows listener。
7. v34 正式 VM 总入口只有 `test\gui_vm\Run-Tests.cmd`。

## 4. 定向复审结论

- AUD-001～AUD-005 的最小闭合条件全部满足。
- 第 1 轮处理改动只影响证据身份、测试环境合同和正式入口精度；没有改变规范性来源含义、问题范围、根因、修复方向、权限归属、依赖图或迁移/切换策略。
- 没有新发现需要复开的中风险或高风险意见，没有审核循环，也不需要全面复审。
- 原审计中未受影响的通过项按其不可变记录继续继承。
- 本轮没有执行未实施的 ACC，也没有据此声称产品问题已经修复；结论仅表示方案 v0.7 已达到可实施的审核质量。

**最终结论：审计通过。** 下一步应引入本复审结果，仅把方案 v0.7 的状态和必要审核身份更新为 `已审核, 可以实施`；在完成该引入前，方案文件头仍为 `未审核, 不得实施`，不得开始实施。

## 5. 记录身份

- 本文件：`PLAN/2026.08.26/2026.08.26-04-定向复审-v33实机问题分阶段修复方案.md`
- 本记录创建后应保持不可变。文件无法在正文中嵌入自身最终哈希；Git blob 和 SHA-256 由创建完成后的复审输出公布，后续审计结果引入记录必须引用该身份。
