# 审计: PCAP 捕获完整性与双向载荷 HTML 报告修复方案

会话角色: 第三方审核方 (独立于作者/执行方会话)

| 字段 | 值 |
|---|---|
| 审核性质 | 首次审核 (同目录无任何已由作者引入的有效第三方审计祖先; 不要求作者自评记录) |
| 审核模式 | 全面审核 (机械扫描 + 全部评审项语义审查 + 反例检查) |
| 方案路径 | `PLAN/2026.08.27/2026.08.27-01-PCAP捕获完整性与双向载荷HTML报告修复方案.md` |
| 方案版本 / 状态 | v0.1 / `未审核, 不得实施` (符合门禁要求) |
| 方案工作区 SHA-256 | `84323005f3f35bd8be4ab714ba7b510cd80f5ac898c040368c1773a16cf90663` |
| 问题或需求来源 | `SRC-REQ-001`～`SRC-REQ-004` (PLAN/FaknetNG.md 第 144/145/159/160-165 行, 行号与内容已逐行核对); `SRC-USER-001/002` (方案 §1.2 固定的用户决定) |
| 根因证据基线 | `EVD-LOG-001`、`EVD-PCAP-001`、`EVD-HTML-001` 三件本地证据哈希全部复核一致; `EVD-SPIKE-002` 的镜像身份与命令已独立复跑; §2.3 三个方向的重组字节与 SHA-256 已由本审计用固定 Docker/Wine 镜像独立重算, 完全一致 |
| 历史审计与处理记录 | 不适用 (首次审核; 本方案无前轮审计) |
| 本地基线 HEAD | `feb39c575e2e74d057bde577573dc92a918a47e7` (分支 `process-redirect-v23`, 与方案声明一致) |
| 边界内 git 状态 | 无 staged; unstaged `M PLAN/FaknetNG.md` (用户未提交修改, 方案 §1.1 已声明只读); 未跟踪 `PLAN/2026.08.27/`; 本审计记录自身在基线固定后新建 |
| 轮次 | 第 1 轮 |
| 结论 | **存在不同意见** (AUD-001、AUD-002, 均 P3 轻微; 无阻断性缺陷) |

## 1. 审核范围

### 1.1 核心维护范围 (决策关键路径, 含内容指纹)

| 路径 | Git blob (基线 HEAD) |
|---|---|
| `fakenet/gui/launcher.py` | `e06f849010b1522d9b621ae431882388ee26511f` |
| `fakenet/fakenet.py` | `7193c01fabe43a5f1c08b46f4b4dadde76ee0a00` |
| `fakenet/diverters/windows.py` | `f54d9c06d85adaba2cc50f38b1b1d9b47a9198f4` |
| `fakenet/diverters/diverterbase.py` | `fda82ad4953d3e49ae64c2865edaf6a4a3f4255d` |
| `fakenet/diverters/pcapwriter.py` | `d4c6fbdcd0ed43a34e1d80cee0dfb44589e41743` |
| `fakenet/listeners/RawListener.py` | `31018afdf7cf2445495c2bd186c8dee68511e06f` |
| `fakenet/configs/html_report_template.html` | `f97507ddda21f7890a67c9aed64fb5b0772a8572` |

### 1.2 直接依赖范围

- 测试: `test/test_gui_launcher.py` (`265091e5…`)、`test/test_gui_startup_logging.py`、`test/test_fakenet_startup_logging.py`、`test/test_diverter_pcap_lifecycle.py` (`33f3abcd…`)、`test/test_windows_inbound_capture.py` (`9c775e5e…`)、`test/test_windows_process_redirect.py`、`test/test_windows_egress_verdict.py`。
- 工具与配置: `test/gui_vm/` (Export-Logs.ps1/cmd、Run-Tests.cmd、README.md)、`test/egress_control_vm/Run-EgressControlTests.ps1` (pktmon seam)、`Build-GuiVmPackage.ps1`、`Build-GuiVmDiagnosticPackage.sh`、`Start-FNPR-Sentinel.sh`、`setup.py` (`b16140f6…`)、`CHANGELOG.txt`、`AGENTS.md`。
- 规范与依赖: `docs/architecture.md`、`docs/srs.md`、`docs/developing.md`、`wheelhouse/pydivert-2.1.0-py2.py3-none-any.whl` (已解包核对 consts/windivert)。
- 证据与来源 (只读): `dist/样本实测/` 三件证据、`PLAN/FaknetNG.md` (工作区版)、`PLAN/2026.08.21/2026.08.21-01-…修复方案.md`、`PLAN/2026.08.26/2026.08.26-01-…方案.md` 与其实施记录。

### 1.3 明确排除范围

同实例 Ubuntu 侧网络/防火墙/Sentinel 协议修改面、无同源证据的错误 (`WinError 592` 等)、`dist/` 其他历史产物、其他日期 PLAN 目录的无关内容、一切生产代码修改 (本审计只读)。审计临时脚本只存在于 `/tmp`, 未进入仓库。

## 2. 来源与证据独立复核 (覆盖清单 A)

| 审核对象 | 结论 | 证据 |
|---|---|---|
| 全部来源切片身份 (SRC-REQ/BASE/TECH/CONV) | 通过 | 8 个被引文件的 `git hash-object` 与 SHA-256 逐一重算, 与方案 §1.1 完全一致 (如 FaknetNG.md blob `69cbdaa6…`/SHA `bec61c44…`; v33 方案 v0.7 blob `a10a33d4…`; 实施记录含 §27 blob `011d0265…`) |
| 规范性来源行号与含义 | 通过 | FaknetNG.md 第 144 行 BUG4、145 行 BUG5、159 行边界 6、160-165 行边界 7 逐行比对; architecture.md 1-17 行 ProxyListener full-duplex seam; srs.md 284-298 行 paired PCAP (同时含 initial 及 mangled 后 final 视图、snaplen 262144); developing.md 139-166 行测试与 3.5→3.6 版本同步合同 |
| `EVD-LOG-001` | 通过 | 日志第 1 行 GUI argv 只有 `-c/--log-file/-f/-p`, 无 `-n`; 第 120 行 `gap_ms=266546 queue_time_ms=2000`; 全日志无 `WINDIVERT_QUEUE_PARAM` 成功标记; `cycle_max_ms=266547.0 pcap_max_ms=0.0 send_max_ms=16.0`; MainThread 11:29:36 写入 "Stop flag found"; 末尾 `healthy=True`、`rc=0` |
| `EVD-PCAP-001` | 通过 | 本审计用固定镜像只读解析: 155 包、跨度 310.997375 s; 出站 236 字节 payload 的 PCAP 时间戳 11:25:17.625848; 恢复处理的首个包 11:29:44.172763; 停顿窗口内仅入站方向 (192.168.204.1→.187) 由入站线程继续写入, 支持单线程阻塞而非全局文件/PCAP 锁阻塞 |
| §2.3 Spike 结果 | 通过 | 独立重算三方向: 597/341/531 字节, SHA-256 `d5708b8d…dd506`/`70bf126e…4fbb`/`3741f27a…7153`, 与方案表逐字符一致 |
| `EVD-HTML-001` | 通过 | 报告仅含 DNS/mDNS NBI; 全文无 `1111.exe`、无 192.168.204.1:3585; 仅有的两处 `192.168.204.187` 是本地 listener 会话的 Destination IP |
| `EVD-SPIKE-002` | 通过 | 镜像 ID 重读为 `sha256:1b33b518…00a5e`; 镜像内 pydivert 为 2.0.9 且 `hasattr(check_filter)=False`; 按方案 §2.4 原命令 (wheel 覆盖层) 复跑四文件聚焦组得 `71 passed in 1.27s` |
| `SRC-TECH-001` | 通过 | wheel 解包: `consts.py` `QUEUE_LEN …< 8192`、`QUEUE_TIME 128 < default 512 < 2048`、`QUEUE_SIZE …< 33554432`; `windivert.py` 存在 `set_param/get_param`。DEC-002 的 8192/2048/33554432 是文档上界值, 合法; 代码后备 2000 与文档默认 512 不一致, 支持 ROOT-002.3 |
| 版本与工具现状 | 通过 | `setup.py` version='3.5'; banner 3.5; CHANGELOG 顶部 3.5; `dist/` 无任何 `v35-r*` 目录 (目标目录从 `v35-r1` 起, 与目标交付规则一致); 7 个被引测试文件、`Run-Tests.cmd`、`Export-Logs.ps1`、`Start-FNPR-Sentinel.sh`、`Build-GuiVmPackage.ps1` 均存在; pktmon seam 存在于 `test/egress_control_vm/Run-EgressControlTests.ps1` |
| v34 基线数字 (SRC-BASE-003) | 通过 | 实施记录 §26/§27 及正文: 525 PASS + 2 个已解释环境 SKIP (非 GBK locale、Wine PowerShell 空壳)、A 13/13、P 20/20、三轮 44/44 |

## 3. 根因与修复设计审查 (覆盖清单 B)

| 审核对象 | 结论 | 证据 |
|---|---|---|
| `ROOT-001` 调用链 | 通过 | `launcher.py:391-427` 两条命令构造均无 `-n`; `fakenet.py:663-687` `console=not options.no_con_out` 未传时安装同步 `StreamHandler`; `windows.py:1345→1379-1383` 同一 WinDivert 接收线程先 `_timed_write_pcap`、再同步 `log_egress_event('ALLOW_TAKEOVER_SINK')`、后 `_send_packet`; `log_egress_event` (`windows.py:1976-1998`) 是同步 `logger.info`。排除项成立: pcap_max=0/send_max=16 排除写包与发送, MainThread 11:29:36 仍写文件日志排除文件 handler 全局阻塞, 停顿窗口内入站线程继续写双 PCAP 排除 writer 锁全局阻塞。方案明确不把 QuickEdit 等未证实触发写成根因, 边界诚实 |
| `ROOT-002` | 通过 | `pcapwriter.py:203-206` healthy 仅等于无 write/close/discard 错误且 raw/converted 计数相等; `windows.py:1249-1263` gap 只记日志不转 fatal; `windows.py:954-965` 队列参数 best-effort、后备 2000; `_open_inbound_capture` (`windows.py:1031-1057`) 与 `_inbound_capture_loop` 不设置、不回读队列参数也无 gap 判定 |
| `ROOT-003` | 通过 | `diverterbase.py:2100-2102` `logNbi` 在 `sessions.get(orig_sport)` 为 None 时直接 return (ALLOW_TAKEOVER_SINK 流到 Ubuntu 不产生本地 session/NBI); `generate_html_report` (`diverterbase.py:2206-2228`) 仅渲染 `self.nbis` 且在 `stop()` 中先于 `_close_capture()` 执行 (`diverterbase.py:896→909`), fatal 分支仍调用 `_generate_reports()` (`:920`); `RawListener.py:345` `hexdump_lines[:16]`; 模板第 336 行 "showing first 256 bytes"; 模板第 5 行 font-awesome CDN; `_flow_audit` `windows.py:1795-1796` 120 s TTL/4096 条清空, 条目只存时间戳 (`:1849`) |
| DEC-001～013 方向闭合 | 通过 | 每条 DEC 有依据且与 IMP/ACC 挂接; DEC-002 值为 wheel 文档上界且强制回读相等、失败即启动失败; DEC-003 `gap >= readback queue_time` 判 fatal 与 SRC-USER-002 "不接受不完整报告" 一致 (保守 false-positive 由产品决定固定); DEC-005/006 保持唯一事实源且不复用 TTL 缓存; DEC-007 报告后移与五秒停止合同的冲突已由 §9 回流门禁覆盖; DEC-013 的 logical_packet_id/observation_role 与 SRS initial/final 合同一致且不改 PCAP 行为 |
| 修复直达根因 | 通过 | IMP-001 直达 argv 缺口; IMP-002 把覆盖破坏纳入既有 `_record_capture_failure(PcapWriteError)` fail-closed 而非新增外围补丁; IMP-003/004 更换事实源到封口 raw PCAP, 不以解析文本日志或加长截断代替 |
| 同源场景与旧路径退出 | 通过 | IMP-005.1 夹具矩阵覆盖双向/乱序/相同重传/冲突 overlap/内部 gap/端口复用/UDP 重复/IPv4/IPv6/未知 owner/恶意字符串/8 MiB/多观察视图/不同 logical ID 的相同 UDP; fatal 会话不生成正常 HTML、`DumpPackets=No` 标注边界、RawListener 旧展示降级为 preview、CDN 移除, 旧错误路径退出方式明确 |
| 可交接实施性 | 通过 | 每个 IMP 含允许修改/现状/目标行为/前置/顺序/可裁决/不允许; 尚未创建的工具 (ACC-003/004/005 verifier、IMP-005.4 样本一键入口、IMP-005.6 Linux 正式构建入口) 均固定工作目录、输入、输出 schema 与路径、退出码 (0/1/2) 和判定边界, 未虚构命令名; 与 `AGENTS.md` 对正式 PowerShell 打包合同 Docker 兼容入口的约束一致 |
| Fallback 评定 | 通过 (全部必要) | FB-001 仅 `--image-only` 重建镜像且禁止 ZIP; FB-002 是显式配置能力边界而非降级; FB-003 无损显示规则; FB-004 未知 owner 显式 `unknown` 且不丢流; FB-005 是失败诊断入口而非成功报告替代。均不替代根因修复; 队列绑定失败/gap/重组冲突/报告失败/pktmon 缺失或三方不一致均列为硬失败 |
| 授权与仓库约定 | 通过 | §8 提交边界明确不暂存用户 `PLAN/FaknetNG.md` 修改与 `dist/` 证据; 不用 Wine 结果替代 VM 验收; 不自动把样本复制到宿主/容器; v35 目标目录规则与当前 `dist/` 现状 (无 v35-r*) 一致 |

## 4. 机械扫描 (覆盖清单 C)

- 方案对象/类型/同日编号: 通过 (本文件为同日 01, 非旧方案改版)。
- ID 唯一性: 通过 (`IMP-001`～`005`、`ACC-001`～`008`、`FB-001`～`005`、`DEC-001`～`013`、`ROOT-001`～`003`、全部 SRC/EVD 无重复定义)。
- §6 双向追溯: 通过 — 每个 IMP 至少被一个 ACC 直接验证 (IMP-001→ACC-001; IMP-002→ACC-002/007; IMP-003→ACC-003/005; IMP-004→ACC-004; IMP-005→ACC-006), 每个 ACC 均引用 IMP 与来源 ID。
- 占位符/虚构命令: 通过 (现存工具写实际命令且 ACC-002 命令已由本审计复跑; 未建工具只写合同)。
- 未声明 fallback: 通过 (§7 全量列出并区分硬失败)。
- 后序依赖: 不适用 (一般修复方案; 实施顺序在 §8 固定)。

## 5. 审计意见

### AUD-001 (P3): `SRC-BASE-001` 声明为规范性来源切片但无任何出向引用

- 首次提出轮次: 第 1 轮 (发现来源: 首次全面审核·机械扫描)。
- 来源层级: 方案 §1.1 与 §6。
- 反例: 对方案全文机械统计, `SRC-BASE-001` 恰好出现一次 (其 §1.1 定义行); §6 追溯表 8 行均不含它; `IMP-005` 来源写作 `SRC-BASE-002`～`SRC-BASE-003`, 显式排除了 001。其余来源均有出向挂接 (`SRC-TECH-001`→`DEC-002` 依据; `SRC-CONV-001`→`IMP-003` 来源)。
- 风险和影响: 低。其内容 (I7 只证明双 writer 健康, 不证明无捕获空窗、不定义完整载荷报告) 实质上支撑 `ROOT-002` 与 DEC-004/ACC-002/ACC-008 的动机, 但形式上成为"声明即弃"的切片, 削弱来源→实施→验收的双向追溯完备性。
- 最小闭合条件: 作者在修订时把 `SRC-BASE-001` 加入 §6 第 1 行 (PCAP 方向与覆盖不完整) 的来源列, 或加入 `IMP-002`/`IMP-005` 的来源字段, 或将其降级为调查证据并说明。
- 受影响的既有通过项及所需复审证据: 不影响 §3 中 ROOT-002 审查结论; 复审只需核对引用补齐且规范含义未变。

### AUD-002 (P3): `ACC-002` 把 "加载的 pydivert 身份为仓库 2.1.0" 列为修复后精确结果, 但固定命令与证据清单中无身份探针步骤

- 首次提出轮次: 第 1 轮 (发现来源: 首次全面审核·ACC 契约可复核性审查)。
- 来源层级: 方案 §5 `ACC-002` 与 §2.4。
- 反例: §2.4 的 Spike 明确执行过身份探针 (输出版本 2.1.0、加载路径 `Z:\pydivert21\pydivert\__init__.py`), 但该探针未进入 `ACC-002` 的实际命令; `ACC-002` 证据栏只有 "pytest 输出、结构化 marker 样例、失败/成功状态机断言"。当前身份只能由 "未挂覆盖层时聚焦组必失败 (`check_filter` 缺失, 本审计已验证镜像 2.0.9 无该方法)" 间接推断, 无命令内直接证据。
- 风险和影响: 低。`PYTHONPATH=Z:\pydivert21` 覆盖是确定性的, exit 0 隐含覆盖层生效; 但若未来镜像预装版本变化 (如预装 2.1.0), 该间接推断失效而 ACC 文本不会报警, 身份断言沦为不可机械复核的声明。
- 最小闭合条件: 在 `ACC-002` 实际命令前追加一行身份探针 (如 `-c "import pydivert; print(pydivert.__version__, pydivert.__file__)"`) 或在新增测试中固定断言加载版本/路径, 并把探针输出列入证据栏。
- 受影响的既有通过项及所需复审证据: 不影响 §2 中 EVD-SPIKE-002 复核结论; 复审只需核对命令/证据补齐, 无需重跑语义审查。

## 6. 审核重点回应 (方案 §"审核重点" 1-9)

1. ROOT-001 排他性充分: 文件 handler (MainThread 11:29:36 仍写入)、PCAP writer (pcap_max_ms=0 且停顿期入站线程持续写双 PCAP)、`_send_packet` (send_max_ms=16) 均被证据排除; 未知外部触发未被写成事实。**通过**。
2. DEC-002/003 与绑定 WinDivert 1.3.0/pydivert 2.1.0 合同一致: 三值为 wheel 注释文档的上界, set/get API 存在, 强制回读相等、失败即启动失败; `>=` 边界在 ACC-002 夹具中三向覆盖。**通过** (真实 VM 回读仍为待核实运行事实, 方案已如实标注并绑定 DEC-002 终止路径)。
3. `DumpPackets=No` (FB-002) 与捕获 fatal (FB-005/硬失败) 分界清晰, 无暗藏降级。**通过**。
4. 会话级流归属独立于 `_flow_audit` TTL/容量策略 (代码常量已核对), 且禁止生产解析文本日志。**通过**。
5. TCP/UDP 重组规则与夹具矩阵足以机械证明; §2.3 已明确 "序号无缺口 ≠ 捕获覆盖完整", 覆盖完整性由 DEC-003/004 + ACC-008 独立观察面承担。**通过**。
6. DEC-013 不改变 paired PCAP 合同, logical packet 先去重再处理网络重传, UDP 不跨 logical packet 合并。**通过**。
7. HTML 单一 Base64 源、派生显示、禁 innerHTML/CDN、临时文件自检后原子发布闭合。**通过**。
8. ACC-008 pktmon NIC/FakeNet/HTML 三方流级字节+哈希比较构成独立证据, 并显式排除本地 divert/drop 流的错误等值比较; v34 停止/恢复/真终点合同在 ACC-006/007 全部保留。**通过**。
9. 未建工具均固定输入/输出/schema/退出码/失败边界, 实际命令回流实施记录与 README、合同变化回流方案的边界清晰。**通过** (其中 ACC-002 的身份探针缺口见 AUD-002)。

## 7. 结论

首次全面审核完成。方案的现象、根因、来源、证据、设计决定、实施条目、验收契约、fallback 和授权边界全部经独立复核成立, 双向追溯、fail-closed 语义与仓库约定一致; 发现两条 P3 级轻微意见 (AUD-001 来源切片追溯缺口、AUD-002 验收身份证据缺口), 无阻断性缺陷。

**结论: 存在不同意见。**

本轮到此结束; 是否修订、是否引入意见后再次第三方审核及采用何种模式, 由作者/执行方在原方案会话决定。本记录创建后不可修改。
