# 自评: PCAP 捕获完整性与双向载荷 HTML 报告修复方案 v0.3

会话角色: 作者/执行方  
自评日期: 2026-08-27  
阶段判定: 已有首次审核祖先的后续改版  
审核路线: AI 自评闭环，无需再次第三方审核  
实施授权: 进行中  
下一入口: 恢复已授权的方案实施

## 1. 对象和审核链身份

| 对象 | 身份 |
|---|---|
| 方案 | `/home/adminn/projects/flare-fakenet-ng/PLAN/2026.08.27/2026.08.27-01-PCAP捕获完整性与双向载荷HTML报告修复方案.md`；v0.3；`88406db0d83b44f6c0258cadfd08d82efae2a685`；SHA-256 `ea318b68ad486d6a5f79e5906f51c351f83b5ca583512e2cf8db2821bbc3bf7c`；状态 `已审核, 可以实施` |
| 改版前方案 | v0.2；Git blob `0bd8aea9a56d97f165b05e5fc6a68b08f06b4d20`；SHA-256 `b31091d2c4bde1ea946759e41be5f8b6f9a0858e9c2d227c80b03631c500cb4d` |
| 首次作者自评 | `2026.08.27-02-自评-PCAP捕获完整性与双向载荷HTML报告修复方案.md`；Git blob `3c4130b654b5abf4b3a53bff616aef2dad2c0719` |
| 首次第三方全面审计 | `2026.08.27-03-审计-PCAP捕获完整性与双向载荷HTML报告修复方案.md`；Git blob `0d8b844ad32c3feb551c8399ea6ba28f693a37d7`；SHA-256 `bc243725d2366bf6c815464c88bc10e956dbfe57b45fe104a07573cd87090257` |
| 首次审计引入 | `2026.08.27-04-审计处理-PCAP捕获完整性与双向载荷HTML报告修复方案.md`；Git blob `656ea8b16abc58543bac6de6e0792770f2f35fb2`；SHA-256 `c5da7d048acabda864eaf3deaa5010d0115174062c9c38cf46619a72a5173be0` |
| 实施记录 | `2026.08.27-01-PCAP捕获完整性与双向载荷HTML报告修复方案-实施记录.md`；自评时 Git blob `77e17d37cb0a62dbf18bfb8d34d5b24d1b713572` |
| 本地审核基线 | 仓库 `/home/adminn/projects/flare-fakenet-ng`；分支 `process-redirect-v23`；HEAD `56c10c1ed71843e6d9e88213431d830d6cc511a8` |

首次审计记录与引入记录精确证明 v0.1 第三方全面审核已由作者引入，v0.2 取得实施资格。本轮是执行中由真实 VM 证据触发的 v0.3 后续改版，不是首次审核未完成的新方案。

## 2. 规范性决定与新证据

- `SRC-USER-001/002` 的完整双向载荷与捕获不完整时 fail-closed 要求未改变。
- `SRC-USER-003` 固定：保留 WinDivert 1.3；两种 handle 只设置并回读 queue length=`8192` 和 time=`2048 ms`；不调用已证实不支持的 queue size；其余完整性门禁不变。语义指纹 `9030e39692fc0dd5e6915cb0aabecab6a69661c72e7e297b53a5bf0c40c652b3`。
- `EVD-VM-QUEUE-001` 直接证据：`Logs/v35-r2/second-run/20260827-173113/results.tsv` SHA-256 `a8aaaea9efa48b9005ed55bd51009558e7c9210f4957fef7f723c2c3207acb6b`；`a10_fakenet.log` `687cd2015a0fde254331d1f1dc9fac831ab073569088c935b3f93e8c1cfdfe88`；`a11_fakenet.log` `fb1d7338735fc08356eeb66faf2ce3086b27ad4a3452f94e64655125d0db4d9a`。两次独立核心启动均通过 length/time，然后在 queue size 以 WinError 87 失败并正确返回非零。
- 绑定 wheel `wheelhouse/pydivert-2.1.0-py2.py3-none-any.whl` SHA-256 `382db488e3c37c03ec9ec94e061a0b24334d78dbaeebb7d4e4d32ce4355d9da1`。Python 枚举暴露 queue size 不能替代对绑定 WinDivert 1.3 真实 handle 的运行验证。

## 3. 五类范围与内容指纹

### 3.1 核心维护范围

- `fakenet/diverters/diverterbase.py` blob `3d0d65a172dadad40a8ba2ed9bdedff9710645db`：队列参数权威列表和 capture fail-closed 生命周期。
- `fakenet/diverters/windows.py` blob `b01a6bcd992bf736c5f85fec1599aa41c167101a`：两种 WinDivert handle 的参数设置/回读与记录。

### 3.2 直接依赖范围

- `test/test_windows_inbound_capture.py` blob `f2bf10a225fae8c98c1c7fa01a312a58dbff646b`。
- `test/test_diverter_pcap_lifecycle.py` blob `c7ab735bf36ef249df72b4dbfc920535de93bff7`。
- `test/gui_vm/run_gui_vm_acceptance.py` blob `6d29e125bf633bc2539336d3850a6438df572707`。
- `test/test_gui_vm_acceptance.py` blob `80cb1498b8f5fc8890da7ba90aa621a24ae8f99b`。
- `tools/build_gui_vm_package_wine.py` blob `552d422be5bedece1189e16ffb8934833af6ad5f`。
- `Build-GuiVmPackage.sh` blob `f31e1d6714b2f21cf0fdbbcda091321097a8b5a6`。
- `test/gui_vm/README.md` blob `010eee1531cd5e95b26229eccc5c42fb2d6750d6`。

### 3.3 明确排除范围

- Ubuntu Sentinel 协议、网络/防火墙、DNS/route 接管、process redirect 裁决、HTML payload 数据模型与 pktmon 独立观察面。
- WinDivert 驱动或 pydivert 升级、数据迁移、权限/安全机制变更。
- 用户现有未提交 `PLAN/FaknetNG.md`、`Logs/` 运行证据和 `dist/` 已有构建产物。

### 3.4 决策关键路径

- 方案 v0.3 的 `SRC-TECH-001`、`SRC-USER-003`、`EVD-VM-QUEUE-001`、`DEC-002`、`IMP-002`、`ACC-002/007`、§6、§10～11。
- `fakenet/diverters/diverterbase.py`、`fakenet/diverters/windows.py`、`test/test_windows_inbound_capture.py`、`test/gui_vm/run_gui_vm_acceptance.py`、`test/test_gui_vm_acceptance.py`、`tools/build_gui_vm_package_wine.py`。
- `Logs/v35-r2/second-run/20260827-173113/results.tsv`、`a10_fakenet.log`、`a11_fakenet.log`的上述 SHA-256。

### 3.5 上下文路径

- 首次审计、审计处理和实施记录。
- `AGENTS.md`、`test/gui_vm/README.md`、`Build-GuiVmPackage.sh`，只用于确认已建立构建/验收入口和实施授权边界。

## 4. 业务自评

| 评估项 | 风险 | 结论、依据和证据 |
|---|---|---|
| 现象、触发、预期、影响和复现 | 低 | 通过。v35-r2 的 A10/A11 均在相同 queue-size set 位置返回 WinError 87，length/time 先行成功；A1～A9 及 Sentinel TCP/UDP PASS 排除一般环境和网络前置。 |
| 根因证据链 | 低 | 通过。原根因 `ROOT-001`～`003` 未变；本次阻碍追到两种真实 handle 的同一参数设置调用、WinDivert 1.3 返回码和 fail-closed 状态变化，已排除 length/time、Ubuntu 终点和 GUI 前置。 |
| 双向追溯 | 低 | 通过。`SRC-USER-003` 已接入 `DEC-002`、`IMP-002/005`、`ACC-002/007` 及 §6；稳定 `IMP-001`～`005`、`ACC-001`～`008` 未重编号或复用。 |
| 直接修复 | 低 | 通过。修复契约直接删除不被驱动接受的参数调用和对应断言，不增加重试、默认值、降级分支或驱动升级。 |
| 同源正常、异常和边界 | 低 | 通过。两种 handle 都要验证 length/time；支持参数 set/readback 失败仍 fatal；`gap < / == / > queue_time` 、失败无成功报告与安全清理均保留。 |
| 回归验证 | 低 | 通过。`ACC-002` 必须断言不调用 queue size；`ACC-007` 必须在真实 VM 看到两种 handle 的 `8192/2048`、无 queue-size 调用和无 coverage fatal；其余全量/三方验收不变。 |
| 可交接实施性 | 低 | 通过。允许修改位置、目标值、禁止行为、测试与 VM 可观察结果均已机械化；工具和构建入口使用已存在的实际名称。 |
| 决策闭合 | 低 | 通过。用户已决定保留 WinDivert 1.3 并删除不支持参数；无“视情况”、并列方向或待决策 fallback。 |
| 验收契约 | 低 | 通过。已存在的 Docker/Wine、`Run-Tests.cmd`、`Run-SamplePayloadAcceptance.cmd`、verifier 和正式构建入口都已用实际名称固定；PASS/FAIL 边界无歧义。 |
| Fallback | 低 | 通过。`FB-001`～`005` 未改；不支持的 queue size 被从需求中删除，不是新 fallback；length/time 失败仍是硬失败。 |

机械预检结论：对象和来源唯一；稳定 ID 无重复定义或复用；双向追溯完整；无占位符和未声明 fallback；新证据哈希已重算一致。所有已发现项都能由用户决定和已证实代码/运行事实唯一修正，无中/高风险遗留。

## 5. 再次第三方审核触发判定

| 触发条件 | 判定 | 具体证据 |
|---|---|---|
| 跨两个以上独立项目或多业务共享契约 | 未命中 | 只改变本仓库 WinDivert 参数列表与直接测试/验收断言，没有跨项目或共享接口。 |
| 不可逆/高风险迁移、权限、安全、隐私或外部关键契约 | 未命中 | 无数据、schema、权限、隐私或外部协议变更；明确不升级驱动。 |
| 总体架构、核心数据流、并发/一致性、恢复或切换策略 | 未命中 | 两种 handle、capture fail-closed、PCAP 事实源、HTML 模型、pktmon 和恢复顺序全部保留。 |
| 核心主路径且回滚困难或影响面无法限定 | 未命中 | 运行位置在捕获启动主路径，但变更是删除单个已稳定失败的参数调用，影响可限定到参数权威列表、直接测试和 VM marker，可逐 hunk 回滚。 |
| 多个均有证据的可行方向 | 未命中 | 用户已在“升级驱动”与“保留 1.3”之间选定后者；方向唯一。 |
| 用户或生效项目约定要求再次审核 | 未命中 | 用户明确同意本次指标；`AGENTS.md` 允许已审核方案在范围内根据构建/验收证据纠错、提交并重建。 |

结论：首次第三方审核祖先有效；v0.3 修改已由真实 VM 证据和用户决定唯一确定，不命中再次审核条件。本记录构成“首次审核后的作者自评闭环”。方案 v0.3 状态为 `已审核, 可以实施`，原实施授权保持 `进行中`，恢复已授权实施。
