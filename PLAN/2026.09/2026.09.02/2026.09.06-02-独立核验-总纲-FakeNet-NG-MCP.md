# 独立核验记录 — 总纲 FakeNet-NG MCP（r58 总体验收，第 4 轮）

> 创建日：2026-09-05（Asia/Shanghai）
>
> 角色：独立核验方（第 4 轮，隔离上下文）。与实施方无共享上下文；全部结论仅依据磁盘真实文件与 git 历史。
>
> 模式：只读核查。除本记录文件外未写入、未修改任何代码、文档或证据；未重置 VM、未启停产品服务、未重建 ZIP、未提交代码。
>
> 核查 HEAD：`6f5f1707d2261c81c9060d0e4a02cb9869475a02`（分支 `process-redirect-v23`）
>
> 被核验对象：`2026.09.06-01-总体验收-FakeNet-NG-MCP.md`（blob `7a6c203208e649c6b8092f536102d0e83a55887b`，r58 总体验收记录）
>
> 结论：**核查不通过**（CHK-030—033 共 4 项；其中 CHK-030/031 使指派的身份绑定核查项不成立，且验收记录 §1 的对应声明与证据相悖）。
>
> CHK 编号续接前三轮（CHK-001…029，见 2026.09.04-01、2026.09.04-02、2026.09.05-05）；旧记录仅作沿革参照，本轮不沿用其结论。
>
> 本记录封存后不补写；下一轮须另建记录。不是第三方方案审核，不产生、关闭或重计 AUD，不改变审核额度，不整改任何被核验对象。

## 1. 对象、身份和保护边界

| 对象 | 绝对路径（仓库根相对） | 本轮实测身份 |
| --- | --- | --- |
| 需求文档 | `PLAN/2026.09/2026.09.02/2026.09.02-01-需求提炼-FakeNet-NG-MCP.md` | `git hash-object` = `420e0288a027cc990c0d0b353aa3702ea64b67b9`（与指派一致） |
| 总纲 v3 | `PLAN/2026.09/2026.09.02/2026.09.02-02-总纲-FakeNet-NG-MCP.md` | `git hash-object` = `b7da9968da72a435b0fa53b680c137b37d754daa`（与指派一致；版本行 v3） |
| 被核验验收记录 | `PLAN/2026.09/2026.09.02/2026.09.06-01-总体验收-FakeNet-NG-MCP.md` | blob `7a6c2032…`，随 `6f5f170` 提交 |
| 最终候选证据树 E | `Logs/fakenetng-mcp/mcp-c94e7abdb-57b2ee8a7dde/` | 28 个含 result.json 的目录（27 条 acc_index + ACC-017-SUMMARY 辅助记录）+ `release/`（151 文件）+ 流水线日志 |
| 发布清单 | `dist/mcp-c94e7abdb-57b2ee8a7dde-release/release-manifest.json` | 与 E/`release/release-acc-index.json` 内容逐字段相同（程序化比对 = True） |
| 候选包 | `dist/mcp-r58/fakenetng-mcp-candidate.zip` | 实测 SHA-256 = `57b2ee8a7ddea36076883241f9653999168a267d98c72ccf03d6509482d31450` |
| P05 实施记录沿革节 | `PLAN/2026.09/2026.09.02/2026.09.03-30-…P05….md` §"CHK 修复批次与 r52→r58 最终候选沿革" | 已读，与 git 逐项核对 |

D = 上述 PLAN 目录。核查期间 HEAD 未移动；`git status` 现场未改动。r52 的 `attic-serial-acc004` 位于旧候选目录 `Logs/fakenetng-mcp/mcp-cc9cdea21-034f71f9916d/`，按指派边界不计入本轮。

## 2. 逐条核查结果

### 2.1 对象身份（要点 1）— PASS

| 项 | 证据 | 结果 |
| --- | --- | --- |
| 需求/总纲 blob | `git hash-object`（见 §1 表） | 与指派值逐字符一致 |
| 源 commit | `git cat-file -t 94e7abdb141417f225a0e64e126232b8eaa76f4b` = `commit`；`git merge-base --is-ancestor … HEAD` 通过 | 存在且为 HEAD 祖先 |
| 候选 id | `dist/mcp-r58/candidate-id.txt` = `mcp-c94e7abdb-57b2ee8a7dde` | 一致 |
| 包 SHA256 | `sha256sum dist/mcp-r58/fakenetng-mcp-candidate.zip` = `57b2ee8a…31450`；`candidate-verification.json` 的 `zip_sha256` 同值，`verified_files:163`、`manifest_match:true`、`size_hash_match:true`、`verdict:PASS`；`mcp-candidate-manifest.json` 实数 163 个 files 条目 | 一致 |
| 发布清单 | `release-manifest.json`：`candidate_id=mcp-c94e7abdb-57b2ee8a7dde`、`source_commit=94e7abdb1414…`、`package_sha256=57b2ee8a…31450`、`os_version=10.0.19045.0` | 与候选包逐字段一致；`os_version` 符合要点 5 要求 |

### 2.2 ACC 覆盖（要点 2）— FAIL（CHK-030、CHK-031）

程序化遍历 E 下全部 28 份 `result.json`：

| 子项 | 结果 |
| --- | --- |
| `status` | 28/28 全 `pass`，无 fail/blocked/tool-error 残留 |
| `candidate_id` | 28/28 全 `mcp-c94e7abdb-57b2ee8a7dde` |
| `p_id` | 与总纲 §12.2 映射逐条相符（ACC-001→P03、002/003/016→P01、005/009-PRE/010/011→P02、004-S*/006/007/008/009→P03、012/013/017(+SUMMARY)→P05、014/015/018/019/FAULT-POINTS→P04、P01-ENTRY→P01） |
| `requirements_blob` | 28/28 全 `b91ded55e75b96e60f66c8d58125d8a220d62a56` — **不等于** 当前需求 blob `420e0288…`（CHK-030） |
| `master_plan_blob` | 28/28 全 `e5c734ea6a5225f1191b72e1c5864ba722dbb3ea` — **不等于** 当前总纲 v3 blob `b7da9968…`（CHK-030） |
| `environment_identity` | 两种取值：`win10h2-MalBox-20241110 | config=listen=192.168.204.149:28788`（24 份）与 `win10h2-MalBox-20241110 | unknown | config=listen=…`（4 份）——**无一份含 `DESKTOP-3FI41GR`**（CHK-031） |

CHK-030 根因定位：`Logs/fakenetng-mcp/r58-seq.py` 第 9—10 行硬编码 `--requirements-blob b91ded55…`/`--master-plan-blob e5c734ea…`；git 考古证实该对 blob 恰为 commit `0b35056`（2026-09-03 23:56 +0800，PLAN 文档首次提交）时的文档对——彼时总纲尚为 v2、需求文档未回流 DEC-007/008。`75117df`（v3 回流）与 `d3334a1` 修订后常量从未更新（r54 `mcp-c038bf76b`、r55 `mcp-c628cdee7`、r56 `mcp-c177fee91` 候选目录抽样同样记录 b91ded55/e5c734ea）。缓解事实：`git diff 0b35056 d3334a1 -- 总纲` 显示 v2→v3 仅为 DEC-007/008 两条冻结行、承接数 38→40 与 v3 变更日志，**未触及 §9.2/§12.2 的 ACC/RACC 行**，验收合同语义不受影响；且当前 v3 总纲第 21 行自身仍自引过时的需求 blob `b91ded55…`（陈旧引用的文档级源头）。

CHK-031 根因定位：`test/mcp/acceptance/run_p01_acc.py:557` 起以 `channel.computer_name()` 做 fail-closed 身份门（非 `DESKTOP-3FI41GR` 即 blocked），4 份 p01 记录（ACC-016/P01-ENTRY/ACC-002/ACC-003）的 `actions` 均留有 `vm-identity: DESKTOP-3FI41GR @ win10h2-MalBox-20241110` 实证；但收尾 `write_result` 复用从未被探测值回填的局部变量 `guest_identity='unknown'`（第 553 行初始化后无赋值路径），且 p02/p03/p04/p05 各 runner 从不探测计算机名。故身份保证依赖 4 条 action 日志与门禁代码，而非 `environment_identity` 字段本身。

### 2.3 ACC-004 拆分六场景（要点 2 专项）— 场景语义 PASS；时间线证据缺口见 CHK-032

六目录 `acc_id` = `ACC-004-S1…S6`，全部 pass，`p_id=P03`。语义与总纲 §9.2 第 312 行六场景逐一对应（runner `run_p03_acc.py:296-432` 源码核对）：

| 场景 | 目录/checks（全 true） | 探活时间线证据 |
| --- | --- | --- |
| S1 正常接管 | `start_ok`/`link_alive_during_takeover`/`link_alive_after_stop` | `acc004-s1-takeover-probe.json` 24 样本 ×0.5s ≈12s，全 ok |
| S2 初始化失败 | `invalid_config_rejected`（坏配置 load 被拒） | 无时间线文件（见 CHK-032） |
| S3 未处理异常 | `start_ok`/`unhandled_exception_revokes_health`/`link_alive_during_exception` | `acc004-s3-exception-probe.json` 8 样本 ≈4s，全 ok |
| S4 无效防护参数 | `invalid_protection_fails_closed`/`service_survives_invalid_filter`；`acc004-s4-invalid-filter-start.json` 存 error 响应 | 无时间线文件（见 CHK-032） |
| S5 核心线程卡死 | `link_alive_during_core_thread_hang`/`hang_stop_bounded`/`link_alive_after_hang`；`acc004-s5-hang-stop.json` state=stopped | `acc004-s5-hang-probe.json` 16 样本 ≈8s，全 ok |
| S6 非受控退出 | `endpoint_restored_after_uncontrolled_exit`/`link_alive_after_uncontrolled_exit`；`acc004-s6-uncontrolled-restart.json` 为恢复后快照 | 无持久化时间线（wait_state 轮询 + 4s 后探活仅记布尔；见 CHK-032） |

拆分决策（fd29254"split ACC-004 into six independent scenario invocations"）与验收记录 §2#1 描述相符；`normalize_service`/`restart_service` 兜底在 `run_p03_acc.py:77/94` 可定位。

### 2.4 发布矩阵（要点 3）— PASS

- `E/release/normal-builtin-001..050.json` 与 `normal-custom-001..050.json` 共 100 份：逐份校验 `probe.all_ok=true`、`stop_state=final_state=stopped`、`lock_held_during_run=true`、`lock_released_after_stop=true`、`audit_diff={}`——零异常。
- `fault-{policy_pause,listener_stop,diverter_stop,child_hang,cleanup_error}-001..010.json` 共 50 份：零 `failure` 键、`probe.all_ok=true`、`audit_diff=[]`（空）。终态分布：child_hang/cleanup_error/diverter_stop/listener_stop 各 10×`stopped`；policy_pause 10×`failed`+`failure_reason="stop grace exceeded"`——与 `run_p05_release.py:274-276` 的既定收敛判据（`stopped` 或 `failed`+该 reason）逐字一致，属 fail-closed 设计终态而非产品失败。
- 摘要数值：`E/ACC-012/normal-summary.json` 与 `E/release/normal-summary.json` 均 `{builtin:50, custom:50}`；`E/ACC-013/fault-summary.json` 与 `E/release/fault-summary.json` 均五类各 10。
- `E/ACC-017/final-checks.json`：`identity/load_ok/start_ok/healthy/link_during_run/stopped/audit_clean` 全 true，`tested_windows_version=10.0.19045.0`。
- 发布清单：`missing=[]`、`failures={}`、`wrong_candidate=[]`、`record_integrity_failures={}` 全空；`acc_index` 恰 27 条全 `pass` 且 candidate_id 一致；27 条与 E 目录一一对应（E 多出的 `ACC-017-SUMMARY` 为辅助汇总记录，result.json 亦 pass）。

### 2.5 记录保真：修复链（要点 4）— PASS

§2 所列 13 个 commit 全部存在于 `process-redirect-v23` 历史（fd29254/97f8346/038bf76/628cdee/177fee91/cdb377b/94e7abdb/3881326/04de520/0fa7cdb/50ca2ca/ee036d5/58f945d，短哈径 177fee9 展开为 `177fee9157f1fa858eeaee5cefc44424e819b991`）。指派要求的 4 个 diff 抽查全部与描述相符：

| commit | 描述 | diff 实证 |
| --- | --- | --- |
| `038bf76` snapshot 锁序 | snapshot() 在元数据锁外采样健康详情 | `fakenet/mcp/coordination.py`：`health_detail` 调用移出 `self._lock`，附 ABBA 死锁成因注释（r53 S3 py-spy）与回归测试 `test_coordination.py` |
| `cdb377b` metric 列 | 路由身份弃自动调优 metric | `fakenet/mcp/baseline.py`：路由行 5 列取前 4 列（目的/掩码/网关/接口），附 metric 漂移回归测试 |
| `177fee91` mtime 过滤 | 按运行起始 mtime 只登记本运行产物 | `supervisor.py` 新增 `_run_started_at` + start 时打点；`artifacts.py` `register_fakenet_outputs(keep=…)` 过滤历史输出（r54 二次方拖慢注释） |
| `97f8346` 进程内转储 | rundll32 comsvcs → 进程内 MiniDumpWriteDump | `fakenet/mcp/incident.py`：删除 rundll32 子进程路径，改 ctypes `dbghelp` 进程内调用，含 r52 S3 三线程 Suspended 复现注释与单元测试 |

§3 RACC→ACC 映射与证据树一致（19 行逐一比对；唯"19/19 RACC"计数口径见 CHK-033）。

### 2.6 一致性陷阱排查（要点 5）— PASS（除已单列的 CHK-030/031）

- E 内无任何 `status≠pass` 的 result.json 残留（含递归子目录）；E 内无 attic 目录；r52 `attic-serial-acc004` 在旧候选目录 `mcp-cc9cdea21-…` 下，按指派排除。
- 跨目录污染：`grep -rl mcp-c94e7abdb-57b2ee8a7dde Logs/fakenetng-mcp/ --include=result.json` 除 E 自身外零命中——同一 ACC 标签无多目录/多版本矛盾。
- 部署包陷阱：`r58-seq.py` 注释"deploys r53 first"为陈旧注释；`E/ACC-016/vm-download.json` 实际下载 `mcp-c94e7abdb-57b2ee8a7dde.zip` 且 SHA-256 `57b2ee8a…31450` 与 r58 候选逐字符一致，安装于 `C:\Progra~1\FakeNet-NG-MCP`（ff48f64 后正式布局，非旧 `candidate\`）——无跨候选拼装。
- 发布清单 `os_version=10.0.19045.0` ✓。
- 证据完整性抽查：6 个 ACC 目录 38 个证据文件按 result.json 登记的 sha256/size 逐一复算，全部吻合。

### 2.7 已知限制核对（要点 6）— PASS

- §5"ACC-001 GUI 互斥真机证据以单元测试代替"与证据相符：`E/ACC-001/acc001-checks.json` 中 `gui_refused_while_service_runs/gui_runs_when_service_down/service_refused_while_gui_runs` 三项为 null；`test/mcp/test_singleinstance.py:34` 存 `test_shared_operator_mutex_conflict_reports_gui` 单元替代；真机第二实例拒绝腿有实据（`acc001-second-instance.json`：`fakenetng-mcp.exe debug` 退出码 1）。
- §5"r50-r58 分批落地"、Linux TODO、SSE 瞬断（基础设施注记）与 P05 沿革节及流水线日志一致，未见矛盾。

## 3. CHK 问题列表（本轮 CHK-030—033）

- **CHK-030（阻断本轮通过）** 证据树 28 份 result.json 的 `requirements_blob`/`master_plan_blob` 全部钉在 `0b35056` 时代的文档对 `b91ded55…`/`e5c734ea…`（总纲 v2、DEC-007/008 回流前的需求基线），并非当前 `420e0288…`/`b7da9968…`；根因为 `Logs/fakenetng-mcp/r58-seq.py:9-10` 硬编码陈旧常量（r54-r56 各候选同样未更新），且验收记录 §1 第 19—20 行"总纲 v3 blob b7da9968…/需求 blob 420e0288…｜27 份 result.json 的 master_plan_blob（requirements_blob）一致"的声明与证据相悖。缓解：v2→v3 差异不触及 §9.2/§12.2 合同行，验收语义未变；当前 v3 总纲第 21 行亦残留对需求 blob `b91ded55…` 的过时自引。
- **CHK-031（阻断本轮通过）** 无任何一份 result.json 的 `environment_identity` 含 `DESKTOP-3FI41GR`；p01 runner 收尾写结果时复用未回填的 `guest_identity='unknown'`（`run_p01_acc.py:553` 初始化后无赋值），p02-p05 runner 不探测计算机名。客户机身份仅由 4 份 p01 记录的 `actions`（`vm-identity: DESKTOP-3FI41GR @ win10h2-MalBox-20241110`）与 fail-closed 门禁代码间接保证。
- **CHK-032（证据缺口，不单独否定 pass）** ACC-004 六场景中仅 S1/S3/S5 持久化了持续探活时间线数组（24/8/16 样本）；S2（初始化失败）、S4（无效防护）、S6（非受控退出）无时间线证据文件——S2/S4 探活隐含于成功 MCP 调用、S6 依赖 wait_state 轮询与 4s 后单点布尔，均未按总纲 §9.2 ACC-004 证据列"持续探活时间线"落盘。
- **CHK-033（计数口径失准）** 验收记录 §3"19/19 RACC 全部通过"高估需求级 RACC 数：需求文档仅定义 RACC-001—018（grep 全文无 RACC-019），总纲 §12.2 亦只映射 18 条；ACC-019 源自记录 024/REQ-006 回流（总纲 v2 变更日志），并非 RACC-019。正确表述应为 18/18 RACC + ACC-019。

## 4. 结论与封存

对象身份、发布矩阵（100+50 轮、summary、final-checks、acc_index）、修复链 commit/diff 保真、一致性陷阱排查与已知限制核对全部通过；产品验收数据本身未见伪造成拼装迹象。但本轮指派的 ACC 覆盖核查项中两项身份绑定子判据不成立（CHK-030 证据-文档 blob 绑定错代且验收记录声明与证据相悖；CHK-031 environment_identity 未携带已验证的客户机身份），另察明 CHK-032 时间线证据缺口与 CHK-033 计数口径失准。总体验收记录 §1 的身份声明按其字面为不实陈述，故本轮只能裁定：

**核查不通过。**

本记录封存后不补写；下一轮须另建记录。不是第三方方案审核，不产生、关闭或重计 AUD，不改变审核额度，不整改任何被核验对象。
