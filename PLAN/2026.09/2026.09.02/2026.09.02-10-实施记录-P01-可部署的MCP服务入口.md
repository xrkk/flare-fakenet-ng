# 实施记录 — P01 可部署的 MCP 服务入口

> 会话角色：`作者/执行方`
>
> workflow_id：`fakenetng-mcp-20260902-6ada6479`（长程实施授权：用户入口消息显式授予，进行中）
>
> 实施子方案：`PLAN/2026.09/2026.09.02/2026.09.02-06-实施子方案-P01-可部署的MCP服务入口.md`，`v2`，Git blob `2f3744acdb19a460c6adc9c03b9b6253fc16f880`，状态 `已审核, 可以实施`
>
> 审核闭环：`08-审核-P01`（blob `75abc557…`，AUD-P01-001—008）+ `09-审核处理-P01`（blob `bd049eb7…`，全部闭合，作者自评闭环）
>
> 总纲：`02-总纲` v2 blob `e5c734ea…`；需求：`01-需求提炼` 正式需求 v1 blob `b91ded55…`；`AGENTS.md` SHA-256 `c84a7533…`
>
> 审核基线 HEAD：`4ac9abf28d4ec3527f93539345dcd7343ca48ea0`；实施基线分支：`process-redirect-v23`
>
> 任务边界：仅新增 `fakenet/mcp/**`、`test/mcp/**`、`fakenet-mcp.spec`、`tools/build_fakenetng_mcp_wine.py`、`Build-FakeNetNgMcpPackage.sh`、`wheelhouse/` MCP 轮子增量与 `SOURCES.md` 增量、本记录与方案/审核文档；`Logs/fakenetng-mcp/**` 与 `dist/` 为产物目录。启动快照 48 条用户既有改动全部受保护，不暂存、不修改、不清理。
>
> 状态：`未开始 → 进行中`（2026-09-02 起）
>
> 本记录按轮次追加，不覆盖历史。

## IMP/ACC 总览

| 项 | 状态 |
| --- | --- |
| IMP-P01-01 目标客户端身份冻结与探针通道 | 完成（身份冻结；探针通道判定真实阻碍，见“阻碍与解除”） |
| IMP-P01-02 Python MCP 实现探针与 wheelhouse 固定 | 完成（25 轮子 + 冒烟通过） |
| IMP-P01-03 服务骨架（配置/目录/单实例/SCM） | 完成（SCM 运行时 v3 修订为 pywin32） |
| IMP-P01-04 MCP 服务端（绑定/探针工具/Header 中间件） | 完成 |
| IMP-P01-05 防火墙规则幂等管理 | 完成 |
| IMP-P01-06 Windows 打包（spec/构建器/候选身份） | 完成（最终候选 `mcp-c32803520-672e070e8b81`） |
| IMP-P01-07 单元与 Windows-Python 测试 | 完成（宿主 32 通过；Wine 门禁 32 通过 + 2 posix 计划跳过） |
| IMP-P01-08 ACC 验收 runner | 完成（`test/mcp/acceptance/`） |
| IMP-P01-09 真实 VM 部署与 ACC 执行 | 完成（ACC-016/003/P01-ENTRY 通过；ACC-002 被阻断） |
| ACC-002 / ACC-003 / ACC-016 / P01-ENTRY | blocked / **pass** / **pass** / **pass** |

### IMP-P01-06/08/09 执行与验证（2026-09-02）

- 构建历程（10 次构建，全部可归因修复并逐次提交）：#1 Wine pip 缺 win32 标记依赖 `pywin32`→补轮子；#2 门禁跳过集按模块名比较的笔误；#3/#4 冻结 exe 冒烟（PYI-216=文件句柄 stdout 触发，改 PIPE+读线程）；#5/#6 `wintypes.VOID` 不存在（真冻结 exe 首次跑通）；#7 冒烟断言改双层 JSON 解析→**候选 `mcp-cd4b2607c-d36b1ca476d9` 构建成功**；真机 ACC 暴露 SCM 无控制台流下 uvicorn 格式化器崩溃→#8 修复；真机暴露 sc create 1072 重装竞争→#9 修复；真机暴露 ctypes SCM 桩整体失效→#10 切换 pywin32（DEC-003 补充）→**最终候选 `mcp-c32803520-672e070e8b81`**（源 commit `328035202e86c99b190e41356221140f961db0ee`，ZIP SHA-256 `672e070e8b81b79e303b7d2e21e1646c4c28146cf90d2c4f65f78c89c8d0ff81`，32 测试 + Wine 冻结冒烟 PASS，manifest 含目标客户端身份与安全偏离说明）。
- IMP-P01-08：`test/mcp/acceptance/run_p01_acc.py` + `helpers.py`（Win10VM MCP PowerShell 通道含 `Response:/Status Code:` 解包；仅绑 `192.168.204.1` 的临时传输服务只暴露候选 ZIP；非允许源探测绑定宿主另一本地地址 `192.168.255.1`，无需 sudo）。历次修复均以真机证据驱动。
- IMP-P01-09：VM 按门禁重置（快照唯一性 count=1、单次 `revertToSnapshot`、VMX `checkpoint.vmState=Snapshot99.vmsn`、`vmware -x`、完成门禁 Win10VM MCP 首轮即返回 `DESKTOP-3FI41GR`）；候选 4 部署（下载哈希一致、122 文件、install+防火墙+`sc create` LocalSystem/auto、`sc start`→RUNNING(STOPPABLE)）。
- ACC 结果（证据：`Logs/fakenetng-mcp/mcp-c32803520-672e070e8b81/<acc-id>/`，`result.json` 字段齐全，退出码合同遵守）：
  - **ACC-016 pass**：AUTO_START/LocalSystem/RUNNING/属主 `NT AUTHORITY\SYSTEM` 会话 0/`net use` 空（无映射盘依赖）/五目录+service.json 布局/仅 `192.168.204.149:28788` LISTENING 无通配/工具面仅 `ping`。
  - **ACC-003 pass**：作用域监听、规则唯一且幂等、防火墙启用复验（临时保护 28787→允许源 `192.168.204.1` 可达、非允许源 `192.168.255.1` 被阻断→恢复关闭并删除临时规则）、包内 README-SECURITY.md 载明明文/无 TLS/token/Origin 偏离与 DNS rebinding 剩余风险。
  - **P01-ENTRY pass**：同一候选三元组原生安装+服务启动+endpoint 可达（非 ACC-017 通过声明）。
  - **ACC-002 blocked**：raw 协议部分全部通过（带控制者头 `valid_uuid`、无头 `missing` 只读、未知版本 400+`-32022`+supported、`server/discover` 200）；目标客户端（ZCode 3.10.2）互操作步骤被阻断，见下节。

## 阻碍与解除（ACC-002 目标客户端步骤）

受影响：P01 / IMP-P01-01、IMP-P01-09 / ACC-002（客户端互操作子步骤）。

阻碍消除步骤（按执行顺序）：

1. 【执行方：用户】任选其一为本机 ZCode CLI 提供模型访问能力：在 `~/.zcode/cli/config.json` 写入模型提供方配置（或以 `zcode login` 完成登录并确认 CLI 可用），并明示允许本工作流用其驱动一次无头探针（涉及模型调用量约 1-2 轮，请确认费用可接受）。所需输入/权限：ZCode 账户/模型凭据与费用许可。完成判据：`node /opt/ZCode/resources/glm/zcode.cjs --prompt "回复 OK" --no-color` 不再报 "Model config is missing"。
2. 【执行方：用户（替代 1）】把探针服务器临时加入自身 ZCode 配置（`mcp.servers.fakenetng-probe = {type:http, url:http://192.168.204.149:28788/mcp, headers:{"X-FakeNet-Controller-ID":"<用户自选 UUID>"}}`）并重启一个 ZCode 会话，完成自动发现并手动调用一次 `ping` 工具（或授权任意新会话执行）。完成判据：该会话可见 `mcp__fakenetng-probe__ping` 工具且调用返回；服务端日志记录到来自 ZCode 客户端的完整请求头。
3. 【执行方：主会话】上述任一判据成立后，重跑 `--acc ACC-002`（runner 将把目标客户端互操作纳入判定并落证据）；若客户端表现为 legacy 纪元（initialize 握手被本服务 400/-32022 拒绝），按总纲 FB-001 保存原始请求/响应证据并停止 P01，公开重新裁决最小兼容层（不得静默切旧 transport）。

解除后恢复检查点：`执行状态.json` 的 “P01/IMPLEMENT(IMP-09 ACC-002 blocked)” 处；已通过的 ACC-016/003/P01-ENTRY 证据仍属同一候选 `mcp-c32803520-672e070e8b81`，无需重采（该候选服务仍在 VM 上运行）。

期间现场保持：VM 保持已部署运行状态（防火墙恢复关闭、临时规则已删、传输服务已随 runner 会话停止）；宿主工作区所有用户既有改动未被触碰。

## 轮次 1（2026-09-02 起）

### 已完成的实施前核对

- 规范性来源无漂移（需求 SHA-256 `83a7f663…`、总纲 SHA-256 `1ec3d4d0…` 与审核记录一致）。
- 子方案 v2 由 08+09 记录支持，状态精确 `已审核, 可以实施`，无待复审项。
- 需求/OUT/Pxx/IMP/ACC 双向映射与保真计数（6/6/0/0/0，审核处理记录 §4）有效。
- 无后序依赖；任务边界与受保护现场如头部所述。

### IMP-P01-01｜目标客户端身份冻结与探针通道（2026-09-02）

- 身份冻结（DEC-001）：ZCode 桌面 3.10.2（运行日志 version=3.10.2）；捆绑 CLI `/opt/ZCode/resources/glm/zcode.cjs` 0.16.5；`~/.zcode/cli/config.json` 含 `mcp.servers.Win10VM={type:http,url:http://192.168.204.149:28787/mcp}` 且本会话可调用；`headers` 静态头能力证据钉在 zcode-guide 0.1.0 版本化路径第 31-33/55-57/96-98 行。
- 探针通道核实（DEC-006）：无头 CLI 存在（`--prompt` 单发、`/mcp` 斜杠命令、`doctor`），但 `--prompt`/`/mcp` 均要求 `~/.zcode/cli/config.json` 存在模型提供方配置，该文件当前仅有 `mcp.servers`；`doctor` 不做 MCP 连接；桌面实例重启会中断当前工作流会话。自主代配模型提供方或自主驱动模型调用涉及凭据/付费外发，未获授权。**判定：ACC-002 的目标客户端互操作步骤构成真实阻碍**（解除步骤见本记录“阻碍与解除”节）；其余 ACC 不受影响。全程未发生任何模型付费调用。

### IMP-P01-02｜Python 实现探针与 wheelhouse（2026-09-02）

- 轮子集：`pip download --platform win_amd64 --python-version 3.11 --only-binary=:all: mcp==2.1.1` 落 24 轮子；构建期发现 `mcp` 的 `sys_platform=='win32'` 标记依赖 `pywin32>=311` 未包含（Linux 侧下载不解析 win32 marker），补 `pywin32-312-cp311-cp311-win_amd64.whl`；共 25 新轮子全部钉入 `wheelhouse/SOURCES.md`（SHA-256）。
- SDK 冒烟（本机 venv，`mcp==2.1.1`）：裸 HTTP 证实 `tools/call` 200（正确 `_meta` 信封需含 clientInfo/clientCapabilities）、未知版本 400+`-32022`+`data.supported=["2026-07-28"]`、`server/discover` 200、通知 202、GET 406；**SDK 默认双纪元**（legacy `initialize` 返回 200/2025-06-18）且不强制版本头与头-体一致 → 决定由自研 ASGI 守卫中间件落实子方案 §3“仅现代纪元”冻结（非 SDK 行为修改，属实现机制）。
- 结论：所选官方实现“实际支持 2026-07-28”成立（探针通过），协议缺口由守卫闭合。

### IMP-P01-03/04/05/07｜服务骨架/服务端/防火墙/测试（2026-09-02）

- 实际落点：`fakenet/mcp/{__init__,paths,config,singleinstance,firewall,transportguard,server,winservice,cli,__main__}.py`、`test/mcp/test_{config,paths,singleinstance,firewall,transportguard,sdk_probe}.py`。
- 宿主验证：`PYTHONPATH=. venv python -m pytest test/mcp -q` → **30 passed**（含端到端协议探针：正向/缺版本头/未知版本/GET 405/legacy initialize 拒绝/头-体不一致/无控制者头只读/`server/discover`/工具面仅 `ping`）。
- 中间修正：删除 `test/mcp/__init__.py`（该包在 pytest 将 `test/` 插入 sys.path 后遮蔽 SDK `mcp` 包）；单实例 posix 测试需持有锁引用；UUID 校验测试改用真非法十六进制。
- 服务行为：debug 模式绑定 `127.0.0.1:39733` 实测 8/8 协议探测符合合同（输出留存 `/tmp/t1..t8.json`，证据已并入测试）。

