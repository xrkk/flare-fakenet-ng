# 总纲首次第三方审核处理记录 — FakeNet-NG MCP

> 会话角色：`作者/执行方`
>
> 处理性质：首次第三方审核结果引入；第 1 轮；首次审核后的作者自评闭环
>
> 原审计记录：`/home/adminn/projects/flare-fakenet-ng/PLAN/2026.09/2026.09.02/2026.09.02-04-审核-总纲-FakeNet-NG-MCP.md`
>
> 原审计记录 Git blob：`f4dc1a13832e1be259ff6f045f91416e3d8c14ad`
>
> 原审计记录 SHA-256：`e39d85d5cd5d5d72eacfb261d04763915e1c2da5314a515671df1a4ab8e907b7`
>
> 处理前总纲：`/home/adminn/projects/flare-fakenet-ng/PLAN/2026.09/2026.09.02/2026.09.02-02-总纲-FakeNet-NG-MCP.md`，`v1`，Git blob `66d0198fdd4b5c1d38b6611a032e27514f0c21c3`，状态 `未审核, 不得实施`
>
> 处理后总纲：`/home/adminn/projects/flare-fakenet-ng/PLAN/2026.09/2026.09.02/2026.09.02-02-总纲-FakeNet-NG-MCP.md`，`v2`，Git blob `e5c734ea6a5225f1191b72e1c5864ba722dbb3ea`，SHA-256 `1ec3d4d044834ba0a586a90c8b9b4fabd2f9720d187f1eb383f47b42869696dc`，状态 `已审核, 可以实施`
>
> 规范性来源：`/home/adminn/projects/flare-fakenet-ng/PLAN/2026.09/2026.09.02/2026.09.02-01-需求提炼-FakeNet-NG-MCP.md`，`正式需求 v1`，Git blob `b91ded55e75b96e60f66c8d58125d8a220d62a56`
>
> 来源切片：`累计有效结论` 1—38；REQ-001—REQ-016、CON-001—CON-008、NON-001—NON-010、RACC-001—RACC-018；记录 040；`方案组织形式第二轮独立复评（v1 当前）` 的 `方案组织结果`
>
> 本地审核基线 HEAD：`4ac9abf28d4ec3527f93539345dcd7343ca48ea0`
>
> 最终判定：全部 AUD 已闭合，修改后的总纲通过作者完整复评；未命中再次第三方审核触发条件

本记录创建后不可修改。原审计记录保持不可变；本记录不覆盖原审核意见，只记录作者/执行方的独立处理、实际修改和闭合依据。

## 1. 身份、来源与漂移门禁

### 1.1 对象匹配

- 原审计明确绑定总纲绝对路径、`v1`、Git blob `66d0198f...`，与本次处理前对象完全一致。
- 原审计明确绑定需求文档 Git blob `b91ded55...`、38 条当前有效结论、52 个稳定来源 ID、记录 040 和组织结果切片；实际读取后路径、ID 集合和规范含义一致。
- 原审计结论为 `存在不同意见`，不是口头结论、受阻审计或持久化失败结果，允许引入。
- 处理前总纲状态为 `未审核, 不得实施`；此前不存在第三方审核处理记录。本次属于首次审核引入，不是复审处理。

### 1.2 决策关键路径漂移检查

只检查原审计列出的决策关键路径；上下文路径和排除范围不作为阻断条件。

| 决策关键路径 | 审计基线 SHA-256 | 当前 SHA-256 | 结论 |
| --- | --- | --- | --- |
| `AGENTS.md` | `c84a7533dc7f96bc5a759410bb9b2f6160a8bf26c22ade35090b6e6383a4a426` | 同左 | 无漂移 |
| `fakenet/fakenet.py` | `c1197059d662f7f8210aa9a3983621bc0129daae38c530c854d2a7672d5ea974` | 同左 | 无漂移 |
| `fakenet/diverters/windows.py` | `bca29b6f05ca8ceb5f0bde3ecfaebab07e037da18203c501beba8e0d8d0f1131` | 同左 | 无漂移 |
| `Build-GuiVmPackage.sh` | `68dc5b4102392d8dce846fc5f48fcc6c40f729ae0b1162e207e7d33744168491` | 同左 | 无漂移 |
| `Build-GuiVmPackage.ps1` | `9cc76b2a9c3374af8133dcb8f87394500b2d3cbc431077a651cb9cc15031ec62` | 同左 | 无漂移 |
| `tools/build_gui_vm_package_wine.py` | `fe176e83b313df0e4f68d264486d6fb456c997cbfa0750c4b5bdad2ebcdfeeef` | 同左 | 无漂移 |
| `test/test_fakenet_stop_lifecycle.py` | `7e4ca7e067532084fc5db9103d71ac1246b265931329aebc047d14b562b9938f` | 同左 | 无漂移 |
| `test/gui_vm/run_formal_stop_acceptance.py` | `af300758a679bceb013366637ab530d46fedeacb106c2103427c6c19fb1f7175` | 同左 | 无漂移 |
| `setup.py` | `4ba1bac09dba8a4030fc5f8a9d4c32e5b0d7db7dc784c750d29c46934f389bb7` | 同左 | 无漂移 |

漂移结论：九个决策关键路径内容指纹全部精确匹配；审计所依据的接口、数据、行为、约束和验收事实未失效，无需最小 Spike。

## 2. AUD 逐项处理

### AUD-001｜配置变更审计记录子句无任何落点

- 接受度：`完全同意`。
- 独立依据：当前有效结论 30/记录 032要求运行期间非活动配置变更记录控制者、时间、前后哈希和结果；当前有效结论 31/记录 033要求停止态变更审计身份、命令和前后版本/哈希。总纲 `v1` 只冻结并发、版本和原子替换，没有冻结这些普通日志字段，原 ACC-009 也不能据此判失败。
- 实际修改：
  - §6.2 增加创建、导入、编辑、重命名、删除及失败/冲突/未落盘请求的普通日志级审计合同；字段包括控制者身份、时间、`command_id`、目标配置、操作、前后版本/SHA-256和结果。
  - 明确审计日志不构成命令重放、事务恢复或第二状态源，继续服从 CON-006 的最小持久化边界。
  - 扩充 OUT-009、P02 范围/产出/验收和 ACC-009 动作、证据、失败边界；OUT-009 反向来源补入记录 032/033。
- 证据位置：修改后总纲 §6.2、§7 OUT-009、§8 P02、§9 ACC-009、§12.3 OUT-009。
- 最小闭合条件核对：每类配置变更及失败结果均有完整审计字段和可观察失败判据；没有引入事务、重放或数据库。`已闭合`。

### AUD-002｜正常服务停止/升级触发路径无落点、无 ACC 覆盖

- 接受度：`完全同意`。
- 独立依据：当前有效结论 23/记录 024及 REQ-006明确要求可处理的正常服务停止或升级先拒绝新变更、保持查询、完成 FakeNet-NG停止和环境恢复审计，并只在真实`stopped`后退出。总纲`v1`的ACC-007只覆盖强制结束MCP后的异常恢复，不能验收正常分支。
- 实际修改：
  - 新增§5.5，冻结正常SCM停止/升级的触发、拒绝新变更、不排队、endpoint关闭前保持查询、复用§5.4完整收敛、审计失败保持`failed`以及外部强杀转异常路径的顺序和结束条件。
  - 扩充OUT-007，使其同时承接异常退出与可处理的正常服务停止/升级，但不混淆两条路径。
  - P03只交付正常SCM控制信号进入统一停止协调器的入口；P04完成查询保持、完整恢复审计和退出/升级门槛，维持P01→P05既定顺序并避免P03反向依赖P04。
  - 接续新增ACC-019并归P04，分别执行正常SCM停止、升级前受控停止和恢复审计失败注入；ACC-007继续只负责异常崩溃路径。
  - REQ-006主映射保持OUT-007/ACC-007/P03，补充落点增加ACC-019/P04；OUT-007反向来源补入记录024；P05同一候选证据汇总扩为ACC-001—019。
- 证据位置：修改后总纲 §5.5、§7 OUT-007、§8 P03/P04/P05、§9 ACC-019、§12.1 REQ-006、§12.3 OUT-007。
- 最小闭合条件核对：受控退出的参与者、触发、拒绝/查询行为、收敛顺序、成功/失败结束状态、升级等待和可观察证据全部冻结；异常kill-on-close不能替代正常停止。`已闭合`。

### AUD-003｜构建入口第三参数事实错误

- 接受度：`完全同意`。
- 独立依据：`Build-GuiVmPackage.sh`把非空第三参数作为显式输出目录传给builder；`tools/build_gui_vm_package_wine.py`仅在该参数为空时从第二参数output-root分配下一个`v35-rN`目录。原`unused`描述与代码不符。
- 实际修改：§10把现有稳定调用固定为`./Build-GuiVmPackage.sh HEAD dist`，并明确第三参数是可选显式输出目录，提供时直接使用，省略时才自动编号；继续明确此GUI v35入口不能冒充MCP包构建入口。
- 证据位置：修改后总纲§10；决策关键路径`Build-GuiVmPackage.sh`和`tools/build_gui_vm_package_wine.py`的未漂移内容指纹。
- 最小闭合条件核对：命令形状与代码事实一致，不再存在误导性占位参数。`已闭合`。

## 3. 实际 diff 与影响分析

### 3.1 实际 diff 清单

1. 总纲身份由`v1`升为`v2`；OUT、既有ACC、Pxx和Fallback未重编号、删除或复用，新ACC接续为ACC-019。
2. §5新增正常服务停止/升级受控退出分支。
3. §6、OUT-009、P02和ACC-009新增配置变更普通日志审计合同。
4. OUT-007、P03/P04/P05、ACC及追溯矩阵增加正常服务停止/升级的分工与ACC-019；ACC-017的同一候选聚合前置同步纳入ACC-019。
5. §10修正正式GUI v35包装入口第三参数语义。
6. §13保真说明更新为18 OUT、19 ACC，并说明ACC-019不改变RACC编号。
7. §14记录v2契约变化；§15新增对应审核重点。
8. 完整复评通过后，文档状态由`未审核, 不得实施`改为`已审核, 可以实施`。

### 3.2 受影响的既有审核项

- 原审核项4“需求保真”：由35完整/3弱化修正为38完整/0弱化。
- 原审核项9“架构与生命周期”：§5.5补齐正常服务停止/升级触发路径。
- 原审核项10“接口与数据合同”：配置审计字段和最小持久化边界闭合。
- 原审核项13“ACC合同”：新增ACC-019，ACC-009补足配置审计可观察性。
- 原审核项14“稳定操作与代码事实”：第三参数说明与代码一致。

### 3.3 未受影响的继承项

原审核项1—3、5—8、11—12、15—16的身份、来源、稳定ID、双向追溯、版本/交接、小模块边界、OUT覆盖、Pxx固定组织、Fallback和无过度设计结论继续成立。原审核项4、9、10、13、14中已经通过的其他子项也保持继承；本次没有扩大其检查范围。

### 3.4 全局不变量

- 仍是单个Windows SCM主管服务、单受管FakeNet-NG、单控制者、多只读连接；没有超时、租约或接管。
- 控制链路必须在唯一主WinDivert filter打开前排除并校验，失败关闭。
- Job Object异常兜底、SCM Recovery和正常服务受控退出保持两条可区分路径；重启后不自动启动FakeNet-NG、不重放旧命令。
- 只有完整环境恢复审计通过才能报告`stopped`并释放控制权；失败保持`failed`且禁启动。
- 内置配置永久只读；自定义配置只在专用根内；普通日志不升级为事务、事件重放、数据库或第二状态源。
- Windows首版、Linux TODO、host-only明文无认证的既定信任边界不变。
- P01→P05数量、顺序、职责与入口不变；正式100/10实机门禁和同一候选身份不变。
- 产品不新增任意命令、任意路径、正文下载、独立看门狗或GUI共控能力。

## 4. 引入后完整复查

### 4.1 机械结果

- 规范来源：16 REQ、8 CON、10 NON、18 RACC，共52个稳定来源ID；无缺失、重复、复用或歧义。
- 总纲合同：18 OUT、19 ACC、5个固定Pxx、14个Fallback；编号连续，新ID只接续ACC-019。
- 双向追溯：每个REQ/CON/NON/RACC均有唯一主OUT/ACC/Pxx；18个OUT均有反向来源。记录024、032、033新增补充落点与反向来源一致。
- 文档身份：路径不变，契约变化升为v2；正文无checkbox、百分比或实施执行状态。
- Pxx依赖：P01→P02→P03→P04→P05，无后序依赖。P03交付受控停止入口，P04完成其完整恢复验收，ACC-019由P04负责，分工无重复或悬空。
- ACC闭合：每个ACC均有负责Pxx、前置、动作、精确预期、证据、通过/失败/被阻断边界；P05汇总同一候选ACC-001—019。
- Fallback：FB-001—014均有依据、触发、影响与退出条件；本次修改没有新增未声明fallback。

### 4.2 需求保真确认

- 应承接数：`38`
- 完整承接数：`38`
- 未落点数：`0`
- 语义弱化数：`0`
- 不可验收数：`0`
- 异常ID：`无`
- 结论：`通过`

参与者、输入输出、触发/顺序、状态/生命周期、不变量、异常/边界/结束、方向适用前提/重新裁决条件和可观察验收，均可从v2总纲及稳定来源引用完整恢复。结论23由§5.5、OUT-007和ACC-019恢复；结论30/31由§6.2、OUT-009和ACC-009恢复。

### 4.3 可交接性、范围与验收隔离

- 不依赖制定对话即可确定小模块范围、六个逻辑职责、生命周期数据流、MCP/配置/状态合同、关键决定、P01—P05顺序与ACC。
- 数据仍仅限ProgramData下custom/state/logs/baselines/artifacts；无数据库实例、迁移、全局锁或其他模块数据操作。
- 核心维护范围仍限`fakenet/mcp/`、直接共享入口、打包/服务入口、相关测试、总纲和规范来源；直接依赖仅为总纲§2.1列出的FakeNet、Windows diverter和交付接口。
- GUI、Linux实现、无关listener/报告、其他模块代码/测试/数据及既有且未恶化的问题继续排除；只有本方案引入/恶化或无法获得等价ACC证据时才影响对应条目。
- 风险、现实阻碍及FB-001—014均保持闭合；本次恢复的普通审计和受控退出没有产生新权限、持久化或恢复旁路。

## 5. 再次第三方审核逐项判定

本轮意见全部处理并持久化，构成一轮已引入的首次第三方审核。由于总纲契约发生修改，按规则默认允许作者自评闭环，逐项检查再次审核触发条件如下。

| 触发条件 | 具体修改与实际影响 | 回滚或隔离能力 | 证据位置 | 判定 |
| --- | --- | --- | --- | --- |
| 跨越至少两个独立项目并改变跨项目契约，或改变多业务共享契约 | 修改只发生在同一FakeNet-NG仓库的MCP小模块总纲；未改变外部项目或多业务共享契约 | 影响限定于P02配置审计及P03/P04服务停止交接 | 总纲§2、§6、§8 | 未命中 |
| 不可逆或高风险迁移、权限、安全、隐私或外部关键契约 | 未增加迁移、数据库或权限；配置审计写入既定普通日志，host-only明文/无认证信任边界不变；构建说明只是代码事实修正 | 无数据迁移；方案文本可按版本回溯，运行实现尚未开始 | 总纲§2、§3、§6、§10；CON-003/006 | 未命中 |
| 改变总体架构、核心数据流、并发一致性、恢复或切换机制 | 未新增恢复机制；§5.5只把记录024已要求的正常触发接入既有§5.4停止/恢复审计。异常Job/SCM路径、六逻辑职责、串行并发合同均不变 | 入口和完整收敛分别隔离于P03/P04；ACC-019可独立验收 | 记录024、REQ-006；总纲§3、§5.4—5.5、ACC-007/019 | 未命中 |
| 核心路径难以回滚或影响面无法可靠限定 | 修改为未实施的方案契约；影响项和继承项已经逐项列明，范围限定OUT-007/009、P02/P03/P04及ACC-009/019 | 由稳定ID、v1/v2身份和独立处理记录完整回溯 | 本记录§3；总纲§14 | 未命中 |
| 修改后仍有多个均有证据的可行方向 | 三项意见均由记录024/032/033及现有构建代码唯一决定；没有遗留产品取舍或相互竞争方向 | 不需要Spike或用户重新裁决 | 本记录§2；审计AUD-001—003 | 未命中 |
| 用户或生效项目约定明确要求再次审核 | 用户要求执行本章判定，没有强制复审；`AGENTS.md`也未要求本次引入后再次第三方审核 | 不适用 | 当前指令；`AGENTS.md` | 未命中 |

再次审核判定：`无需再次第三方审核`。不选择定向或全面复审模式，也不生成定向复审请求范围。

## 6. 最终结论与紧邻入口状态

- AUD-001：完全同意，已修改并闭合。
- AUD-002：完全同意，已修改并闭合。
- AUD-003：完全同意，已修改并闭合。
- 未解决或复开AUD：无。
- 审核循环熔断：未触发；这是第1轮处理，不存在同一根因两次复开。
- 修改后总纲：`/home/adminn/projects/flare-fakenet-ng/PLAN/2026.09/2026.09.02/2026.09.02-02-总纲-FakeNet-NG-MCP.md`，`v2`，Git blob `e5c734ea6a5225f1191b72e1c5864ba722dbb3ea`，状态`已审核, 可以实施`。
- 结论：`总纲已修改并由作者自评闭环, 无需再次第三方审核, 当前状态: 已审核, 子方案流程入口状态: 已满足`。
- 长程授权：`未授予`。本记录只恢复子方案流程入口资格，不编写子方案、不实施业务代码、不运行最终验收。
- 本记录路径：`/home/adminn/projects/flare-fakenet-ng/PLAN/2026.09/2026.09.02/2026.09.02-05-审核处理-FakeNet-NG-MCP.md`；其Git blob和SHA-256在文件写入后计算并随本轮输出交付，避免自引用改变文件身份。
