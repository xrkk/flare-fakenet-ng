# 自评记录 — 实施子方案 P01（可部署的 MCP 服务入口）

> 记录路径：`/home/adminn/projects/flare-fakenet-ng/PLAN/2026.09/2026.09.02/2026.09.02-07-自评-P01-可部署的MCP服务入口.md`
>
> 会话角色：`作者/执行方`
>
> 自评日期：`2026-09-02`
>
> 自评结论：`通过，必须首次第三方审核`
>
> workflow_id：`fakenetng-mcp-20260902-6ada6479`
>
> 本记录为不可变自评快照；后续自评必须新建记录。

## 1. 对象与来源身份

- 子方案：`PLAN/2026.09/2026.09.02/2026.09.02-06-实施子方案-P01-可部署的MCP服务入口.md`
- 自评后版本：`v1`；Git blob `5426d20469d2b0ae8d45dac32733fa5f4b7b1c18`；SHA-256 `313d2bef5da44bec7618d795ff90ad6d0b7831a3b5ef1f209fc1dca1735d3ec4`
- 子方案状态：`未审核, 不得实施`
- 总纲：`2026.09.02-02-总纲-FakeNet-NG-MCP.md`，`v2`，blob `e5c734ea6a5225f1191b72e1c5864ba722dbb3ea`，状态 `已审核, 可以实施`；审核闭环 = `04-审核`（blob `f4dc1a13…`）+ `05-审核处理`（blob `e0463092…`）。
- 需求：`2026.09.02-01-需求提炼-FakeNet-NG-MCP.md`，`正式需求 v1`，blob `b91ded55e75b96e60f66c8d58125d8a220d62a56`。
- 项目约定：`AGENTS.md`（SHA-256 `c84a7533…a426`，与总纲审核基线精确一致，无漂移）。
- 历史第三方审核/处理记录（本子方案）：无。历史链判定：`首次审核未完成`。
- 本地基线 HEAD：`4ac9abf28d4ec3527f93539345dcd7343ca48ea0`（分支 `process-redirect-v23`）。

## 2. 核心维护范围 / 直接依赖范围 / 排除范围

- 核心维护：本子方案、新建 `fakenet/mcp/**`、新建 `test/mcp/**`（含 acceptance runner）、新建 `fakenet-mcp.spec`、`tools/build_fakenetng_mcp_wine.py`、`Build-FakeNetNgMcpPackage.sh`、`wheelhouse/` 新增 MCP SDK 轮子及 `SOURCES.md` 增量、`Logs/fakenetng-mcp/**` 与 `dist/` 的 P01 产物。
- 直接依赖（只读或按合同扩展）：`AGENTS.md`；`Build-GuiVmPackage.sh`（SHA-256 `68dc5b41…8491`）与 `tools/build_gui_vm_package_wine.py`（`fe176e83…eeef`）作为构建模式参照，二者与总纲审核基线一致且本方案不修改；`tools/docker/gui-vm-diagnostic/Dockerfile`（`ea6ead15…d7ab`，当前工作区身份）与镜像 `sha256:1b33b518…05e`；`wheelhouse/` 机制（`SOURCES.md` 当前 `a865f050…2287`）；`setup.py`（`4ba1bac0…8bb7`，P01 计划不改：冻结交付不经 pip 安装路径，理由见 §4）；`fakenet.spec`（`808d06b3…661`，仅作 onedir 模式参照）；`test/gui_vm/run_formal_stop_acceptance.py`（`af300758…7175`，runner 经验参照）。
- 明确排除：`fakenet/fakenet.py`、`fakenet/diverters/**`（P03 边界）、`fakenet/gui/**`、既有 GUI 测试与 v35 包装合同正文、用户工作区全部既有未提交改动（启动快照 48 条，见长程执行目录）、其他 PLAN 目录与 `research/`。

## 3. 决策关键路径与上下文路径

- 决策关键路径：§2 所列 8 个直接依赖路径 + 本子方案自身。上表 SHA-256 即内容指纹。
- 上下文路径（仅辅助理解）：`dist/` 既有 v34/v35 产物、`test/gui_vm/` 其余 runner、需求文档 Grill-Me 记录全文。

## 4. 自评发现与修正

| ID | 发现 | 修正 |
| --- | --- | --- |
| SE-P01-1 | ACC-003 启用防火墙的复验会同时阻断 Win10VM MCP 验收编排通道（28787 仅宿主可达），验收自伤；且“非允许源”原先无可靠构造方式（Windows 对本机接口 IP 的回环处理不可靠） | v1 已修正：启用前临时放行 28787（仅 `192.168.204.1`）、复验后恢复并删除；负样本固定为宿主 host-only 接口临时次级 IP（如 `192.168.204.250`），结束移除 |
| SE-P01-2 | `setup.py` 是否需要为 `fakenet.mcp` 扩展 packages/entry_points 存疑 | 冻结决定：不改 `setup.py`。P01 交付物是 PyInstaller 冻结服务包（spec 入口 `fakenet/mcp/__main__.py`），不经 pip 安装路径；现有 `packages=['fakenet']` 从未枚举子包，为既有仓库形态。若后续 Pxx 需要 pip 入口再升版处理 |
| SE-P01-3 | IMP-P01-02 的自研回落是否构成未声明 fallback | 已在子方案 IMP-P01-02 允许裁决细节与 DEC-002 中显式声明为“明确隔离的兼容适配层”（协议仍为 2026-07-28，非旧 transport），触发条件=SDK 探针失败且有实测证据，非静默 |

其余逐项检查（对象身份、来源切片、总纲/Pxx 边界、稳定 ID 与双向追溯、无子方案冲突、无后序依赖、功能闭环、可交接实施性、方向性决定闭合、验收契约、兼容性、数据一致性、资源、测试、fallback）未发现其余问题：IMP 引用的命令均为已存在工具（git archive、pip --no-index、xvfb-run wine pytest、sc.exe、netsh、vmrun、docker）；未虚构尚不存在的产品命令；runner 由 IMP-P01-08 真实创建。

## 5. 需求保真确认（独立重做）

- 应承接数：`6`（结论 8、9、10、26 全量；结论 7、38 的 P01 子句）。
- 完整承接数：`6`。
- 未落点数：`0`。
- 语义弱化数：`0`。
- 不可验收数：`0`。
- 异常 ID：无。
- 结论：`通过`。

核对路径：结论 8→OUT-002→F-01/F-02+IMP-P01-02/04/06+ACC-002；结论 9/10→OUT-003→IMP-P01-03/05+ACC-003+§3 冻结表；结论 26→OUT-016→IMP-P01-03+ACC-016；结论 7 的不硬编码子句→§3 全参数可配置（F-08 仅为默认值）；结论 38→Docker/Wine 仅在实施环境与证据位置出现。参与者/输入输出/触发顺序/状态生命周期/不变量/异常边界/适用前提/重新裁决/可观察验收均可由 需求→总纲→子方案 IMP/ACC 完整恢复。

## 6. 审核路线判定

- 历史状态：`首次审核未完成`。
- 审核路线：`必须首次第三方审核`（首次审核祖先独立门禁，不做复杂度判断）。
- 审核模式：全面审核（机械扫描 + 全部评审项语义审查 + 反例检查）。
- 子方案保持：`未审核, 不得实施`。

## 7. 实施授权

- workflow `fakenetng-mcp-20260902-6ada6479` 的长程实施授权已由用户入口消息显式授予，覆盖本子方案首次审核引入后的实施；当前阶段（自评完成、待审核）尚未进入实施。

## 8. 本地工作区与安全边界

- 基线 HEAD `4ac9abf`；工作区 48 条既有改动全部为排除范围（启动快照已持久化于长程执行目录）。
- 本自评只新建本记录与子方案文件，未修改业务代码、未运行构建、未触碰 VM。
- 运行态只读证据：Docker 镜像身份（F-04）、ZCode 客户端身份与配置（F-03）、PyPI/规范抓取（已记入外发调用账本，免费）。
