# 独立核验记录 — 总纲 FakeNet-NG MCP（r58c 总体验收，第 6 轮）

> 创建日：2026-09-06（Asia/Shanghai，05:41 CST）
>
> 角色：独立核验方（第 6 轮，隔离上下文）。与实施方无共享上下文；全部结论仅依据磁盘真实文件与 git 历史。权威 blob 一律由本轮亲自运行 `git hash-object` 取得，未采信任何记录中的转写值。
>
> 模式：只读核查。除本记录文件外未写入、未修改任何代码、文档或证据；未重置 VM、未启停产品服务、未重建 ZIP、未提交代码。
>
> 核查 HEAD：`9683bfce6c1eba7ee7cb70a5eeca1cf7a1c183f2`（分支 `process-redirect-v23`）
>
> 被核验对象：`PLAN/2026.09/2026.09.02/2026.09.06-01-总体验收-FakeNet-NG-MCP.md`（§7 声明 CHK-030—035 闭合）及其引用的 r58c 最终证据树 `Logs/fakenetng-mcp/mcp-c94e7abdb-57b2ee8a7dde/`
>
> 结论：**核查不通过**（CHK-030/031/032/033/034 五条确认闭合——blob 修复、实机名回填、六场景探活时间线、RACC 计数表述全部实证成立，r58c 重跑证据真实且全部 pass；但 CHK-035 未完全闭合：验收记录 §4 仍写 "release manifest 27 条 acc_index"，实测 28 条，§7 表格 "本记录 §1/§4 同步为 28" 的声明与 §4 实际文字不符。另新开低危 CHK-036：验收记录 §7 未披露 r58c 重跑中途 ACC-008 轮 1 失败与 73732ef 运行器修复插入，该过程仅在 git 提交说明与流水线日志中可溯源）。
>
> CHK 编号续接前五轮（CHK-001…035，见 2026.09.04-01、2026.09.04-02、2026.09.05-05、2026.09.06-02、2026.09.06-03）；本轮新开 CHK-036。
>
> 本记录封存后不补写；下一轮须另建记录。不是第三方方案审核，不产生、关闭或重计 AUD，不改变审核额度，不整改任何被核验对象。

## 1. 对象、身份和保护边界

| 对象 | 绝对路径（仓库根相对） | 本轮实测身份 |
| --- | --- | --- |
| 需求文档 | `PLAN/2026.09/2026.09.02/2026.09.02-01-需求提炼-FakeNet-NG-MCP.md` | 亲自 `git hash-object` = `420e0288a027cc990c0d0b353aa3702ea64b67b9`（40 字符，含索引 17—18 位 "c0"；与第 4/5 轮实测一致） |
| 总纲 v3 | `PLAN/2026.09/2026.09.02/2026.09.02-02-总纲-FakeNet-NG-MCP.md` | 亲自 `git hash-object` = `b7da9968da72a435b0fa53b680c137b37d754daa`（40 字符；§12.2 恰 RACC-001—018 共 18 行，程序化计数 = 18） |
| 被核验验收记录 | `PLAN/2026.09/2026.09.02/2026.09.06-01-总体验收-FakeNet-NG-MCP.md` | 随 `9683bfc` 提交（§7 声明 CHK-030—035 闭合） |
| 第 4/5 轮核验记录 | 同目录 `2026.09.06-02`、`2026.09.06-03` 号文件 | 已读；CHK-030—035 原始定义与判据以此为准 |
| 最终候选证据树 E | `Logs/fakenetng-mcp/mcp-c94e7abdb-57b2ee8a7dde/` | 28 个含 result.json 的目录（26 个 `ACC-*` + `FAULT-POINTS` + `P01-ENTRY`）+ `release/`（153 文件）+ `pipeline.log`/`pipeline-b.log`/`pipeline-c.log`/`seq-python.log`/`diag.log` |
| 发布清单 | `dist/mcp-c94e7abdb-57b2ee8a7dde-release/release-manifest.json` | 与 E/`release/release-acc-index.json` 程序化整文档比对 = True |
| 候选包 | `dist/mcp-r58/fakenetng-mcp-candidate.zip` | 实测 `sha256sum` = `57b2ee8a7ddea36076883241f9653999168a267d98c72ccf03d6509482d31450`；`candidate-id.txt` 内容 = `mcp-c94e7abdb-57b2ee8a7dde`；`candidate-verification.json` verdict=PASS（verified_files=163, manifest_match=true） |
| r58c 重跑脚本 | `Logs/fakenetng-mcp/r58c-seq.py`、`r58c-pipeline.sh` | 两文件第 9—10 行 blob 常量与本轮实测值逐字符一致（见 §2.1） |

D = 上述 PLAN 目录。核查期间 HEAD 未移动；未触碰任何被核验对象。现场记录（非 CHK）：工作区存在与 MCP 验收无关的未提交变更（约 48 条：30 个 2026.08 旧 PLAN 文件的未提交删除、`fakenet/gui/*` 等，与第 5 轮现场记录同性质）——均未提交、不进入候选（候选 ZIP SHA 实测不变佐证），按 AGENTS.md 属应保留的无关工作，不计入本轮问题；`git diff --stat 94e7abdb..HEAD -- fakenet/` 为空（见 §2.6）。

## 2. 逐条核查结果

### 2.1 CHK-030/034 闭合核验（blob 残缺转写）— CLOSED

权威值由本轮亲自 subprocess 独立取证：`git hash-object` 需求 = `420e0288a027cc990c0d0b353aa3702ea64b67b9`、总纲 = `b7da9968da72a435b0fa53b680c137b37d754daa`（两值均 40 字符）。

| 子项 | 实测命令/路径 | 结果 |
| --- | --- | --- |
| r58c-seq.py 常量 | `Logs/fakenetng-mcp/r58c-seq.py:9-10` | `--requirements-blob 420e0288a027cc990c0d0b353aa3702ea64b67b9`、`--master-plan-blob b7da9968da72a435b0fa53b680c137b37d754daa` — 与实测值逐字符一致（含第 17—18 位 "c0"，第 5 轮 CHK-034 指认的缺失位已补全） |
| r58c-pipeline.sh 常量 | `Logs/fakenetng-mcp/r58c-pipeline.sh:9-10` | 同上，逐字符一致 |
| 28 份 result.json 两 blob 字段 | 程序化遍历 E 下 `*/result.json`（28 份） | `requirements_blob` 28/28 = `420e0288…b67b9`（40 字符）、`master_plan_blob` 28/28 = `b7da9968…4daa`（40 字符）；不等者列表 = 空 |
| 验收记录声明 | `2026.09.06-01` §1 第 19—20 行、§7 第 90 行 | "28 份 result.json 的（requirements/master_plan）_blob 一致"、blob 实测值 `420e0288…`（40 字符）——与磁盘证据一致 |

判定：CHK-030 与其根因条目 CHK-034 全部实证闭合。

### 2.2 CHK-031 闭合核验（environment_identity 含实机名）— CLOSED

程序化遍历 28 份 result.json 的 `environment_identity` 字段：28/28 均含 `DESKTOP-3FI41GR`（缺失列表 = 空）。样例（`ACC-004-S3/result.json`）：`win10h2-MalBox-20241110@DESKTOP-3FI41GR | config=listen=192.168.204.149:28788`。

### 2.3 CHK-032 闭合核验（ACC-004 六场景探活时间线）— CLOSED

| 场景 | 证据文件 | 实测 |
| --- | --- | --- |
| S1 正常接管 | `ACC-004-S1/acc004-s1-takeover-probe.json` | JSON 数组 len=24，全元素含 `t`/`ok` 键，样例 `{"t": 1788642292.5, "ok": true}` |
| S2 初始化失败 | `ACC-004-S2/acc004-s2-init-probe.json` | 数组 len=8，同格式 |
| S3 未处理异常 | `ACC-004-S3/acc004-s3-exception-probe.json` | 数组 len=8，同格式 |
| S4 无效防护 | `ACC-004-S4/acc004-s4-survival-probe.json` | 数组 len=8，同格式 |
| S5 核心线程卡死 | `ACC-004-S5/acc004-s5-hang-probe.json` | 数组 len=16，同格式 |
| S6 非受控退出 | `ACC-004-S6/acc004-s6-uncontrolled-exit-probe.json` | 数组 len=8，同格式 |

六份 result.json 的 `evidence` 均以 `{name, path, sha256, size}` 登记上述文件；S3 两件证据 sha256 实测（`323580a4…b27c`、`3923b0ee…3429`）与登记值吻合。

### 2.4 CHK-033 闭合核验（RACC 计数表述）— CLOSED

验收记录 §3（第 39—63 行）为 RACC-001—018 共 18 行映射表，收束句为 "**18/18 RACC 全部通过， 另有 ACC-019（源自记录 024/REQ-006 回流, 非需求 RACC 编号）一并通过——共 19 项 ACC 合同**"；全文 grep `19/19` 零命中（无残留）。总纲 §12.2 程序化计数恰 18 行 RACC（RACC-001…RACC-018），佐证口径正确。

### 2.5 CHK-035 闭合核验（计数口径 27→28）— **OPEN（残留）**

| 子项 | 证据 | 实测 |
| --- | --- | --- |
| §1 计数 | `2026.09.06-01` 第 19—20 行 | "28 份 result.json 的 `master_plan_blob`/`requirements_blob` 一致" — 已同步为 28 ✓ |
| §4 计数 | `2026.09.06-01` 第 67 行 | "release manifest **27 条** acc_index 全 pass, missing/failures/wrong_candidate/integrity 全空" — **仍为 27** ✗ |
| acc_index 实测 | `dist/mcp-c94e7abdb-57b2ee8a7dde-release/release-manifest.json` | `acc_index` dict len = **28**（26 个 `ACC-*` + `FAULT-POINTS` + `P01-ENTRY`），全 pass、candidate_id 一致；键集合与 E 的 28 个 result.json 目录程序化比对 = True |
| §7 闭合声明 | `2026.09.06-01` 第 94 行 | "035 计数口径 27→28｜本记录 **§1/§4 同步为 28**（含 ACC-017-SUMMARY 聚合标签）" — §4 部分与实际文字不符，声明失实 |

判定：第 5 轮 CHK-035 点名的两处（§1 第 19—20 行、§4）只修正了 §1；§4 的 "27 条 acc_index" 残留，且 §7 表格对其做出了与磁盘不符的闭合声明。低报不产生错误 pass（acc_index 28 条本身全 pass），但按本轮判据（§1/§4 计数均为 28）未满足，判 OPEN。

### 2.6 全量重查（防回归）

| 要点 | 实测 | 结果 |
| --- | --- | --- |
| 1 对象身份 | `candidate-id.txt`=`mcp-c94e7abdb-57b2ee8a7dde`；`sha256sum dist/mcp-r58/fakenetng-mcp-candidate.zip`=`57b2ee8a…31450`（与验收记录 §1、release manifest、candidate-verification、28 份 result.json 的 `package_sha256` 五方一致）；`git merge-base --is-ancestor 94e7abdb141417f225a0e64e126232b8eaa76f4b HEAD` 通过；manifest `candidate_id`/`source_commit`/`package_sha256` 三字段与记录一致；`os_version`=`10.0.19045.0` | PASS |
| 2 28 份全 pass | 程序化遍历：status 非 pass 列表 = 空；candidate_id 不一致列表 = 空；ACC-004-S3 抽查：`acc004-s3-checks.json` 覆盖 `start_ok`/`unhandled_exception_revokes_health`/`link_alive_during_exception` 三项全 true | PASS |
| 3 发布矩阵 | `release/` 100 normal（50 builtin+50 custom）+ 50 fault（policy_pause/listener_stop/diverter_stop/child_hang/cleanup_error 各 10）共 150 份轮记录；以运行器权威判据（`run_p05_release.py` 写入的 `failure` 键存在即失败轮）程序化校验，带 `failure` 键的记录 = 空（150/150 clean）；`normal-summary`={builtin:50,custom:50}、`fault-summary`=5 类×10；ACC-017 `final-checks.json`：identity/load_ok/start_ok/healthy/link_during_run/stopped/audit_clean 全 true、`tested_windows_version`=`10.0.19045.0`（vm now=2026-09-06T05:27:17，r58c 代际）；manifest `missing`/`failures`/`wrong_candidate`/`record_integrity_failures` 全空、acc_index 28 条全 pass；`pipeline-c.log` 尾部 `R58C COMPLETE`（05:27:45） | PASS |
| 4 修复链 | `git log` 含 `c87ef0e`/`73732ef`/`9683bfc`；`73732ef` diff 仅 `test/mcp/acceptance/run_p03_acc.py`（+25/-10）：`load_and_start` 在 `state=='failed'` 且无 `error` 时 sleep 10s 后有界重试一次，持续失败仍失败——纯测试运行器，未触碰 `fakenet/` 产品代码；`c87ef0e` 仅 5 个 `test/mcp/acceptance/*` 运行器；`9683bfc` 仅文档/记录；`git diff 94e7abdb..HEAD --stat -- fakenet/` 输出为空（0 行）——候选二进制未变，包 SHA 仍成立 | PASS |
| 5 一致性排查 | E 全树 grep `"status": "fail/blocked/skip"` 零命中（无非 pass 残留）；`Logs/fakenetng-mcp/` 全部旧候选目录（r52—r57 等）grep 引用 r58 candidate id 的 result.json = 零（无多版本同标签矛盾）；`dist/` 其余 3 个 `*-release` 目录均为不同 candidate id，验收记录 §6 已声明作废旧候选仅作修复链历史 | PASS |
| 6 ACC-008 | `ACC-008/result.json`：status=pass，started=`2026-09-05T21:14:58Z`、ended=`21:16:34Z`（本地 05:14:58—05:16:34），属 r58c 代际（E 全部 28 份 result.json 的 started_at 均落于 `2026-09-05T21:03:10Z`—`21:27:45Z` 窗口，出窗列表 = 空）；轮 1 失败—修复—续跑链完整可溯：`pipeline-c.log` 第 36—38 行 `05:09:41 ACC-008 start`→`05:12:00 exit=1 fail`→`sequence halted at ACC-008`，`73732ef` 提交于 `05:14:47`，第 40—41 行 `05:14:58 start`→`05:16:34 exit=0 pass`——result.json 的 started_at 与续跑起点逐秒吻合 | PASS（披露缺口另见 CHK-036） |

### 2.7 r58c 重跑正当性

- 执行清单核验：`r58c-seq.py` ORDER 24 项（含 `FAULT-POINTS`、`P01-ENTRY`）+ `r58c-pipeline.sh` 追加 normal/fault/final/summary 四模式（normal/fault 模式写 ACC-012/ACC-013 的 result.json，final 写 ACC-017，summary 写 ACC-017-SUMMARY）= 24+4 = 28 份 result.json，与证据树一一对应；§7 "重跑 27 项 ACC + normal + fault + final + summary" 的 27 = 24 ORDER + ACC-012 + ACC-013 + ACC-017（summary 产物单独列出），口径成立。
- 候选不变：包 SHA 实测仍为 `57b2ee8a…31450`，`fakenet/` 自 `94e7abdb` 起零改动，r58c 仅重跑取证、未重编译产品。
- 三份流水线日志对应三代重跑：`pipeline.log`（r58 首跑，04:03:59 PIPELINE COMPLETE）、`pipeline-b.log`（r58b，04:53:24 PIPELINE COMPLETE）、`pipeline-c.log`（r58c，05:27:45 R58C COMPLETE），与第 4/5 轮时间线及本记录 §2.6 要点 6 吻合。

## 3. CHK 问题列表（本轮新开 CHK-036；CHK-035 判 OPEN，030/031/032/033/034 判 CLOSED）

- **CHK-035 — OPEN（低危，记录口径残留）**：验收记录 `2026.09.06-01` §4 第 67 行仍写 "release manifest 27 条 acc_index 全 pass"，而本轮实测 `release-manifest.json` 的 `acc_index` 为 28 条（含 `ACC-017-SUMMARY`，全 pass、candidate 一致，键集合与 28 个 result.json 目录一一对应）；§7 第 94 行 "本记录 §1/§4 同步为 28" 的闭合声明与 §4 实际文字不符。第 5 轮点名的两处计数只修正了 §1（已为 28），§4 残留。低报不产生错误 pass，不阻断证据效力，但属记录与证据不一致的口径残留，CHK-035 按其判据（§1/§4 均为 28）未完全闭合。
- **CHK-036（低危，记录完整性披露缺口）**：验收记录 §7 第 96 行将 r58c 表述为 "重跑 27 项 ACC + normal + fault + final + summary 全部 pass"，未披露该重跑系两段：第一段于 `ACC-008` 轮 1 失败中止（`pipeline-c.log`：`05:12:00 exit=1 fail`，适配器重配置竞态致 diverter fail-closed "No active ethernet interfaces detected"），插入运行器修复 `73732ef`（`05:14:47`，仅 `test/mcp/acceptance/run_p03_acc.py` 有界重试）后自 ACC-008 续跑至 `05:27:45` 完成。该过程在 git 提交说明（`73732ef` message、`9683bfc` message 均提及 adapter-race retry）与 `pipeline-c.log` 中完整透明、可机械归因，候选身份未变，不产生错误 pass；但作为总体验收记录，§7 未明示中途一次真实失败与运行器代码变更插入，"断点续跑"一词仅用于 normal/fault 轮级记录。属文档级披露缺口，建议下一版验收记录补记一句。

本轮零阻断性新发现；上述两条均为低危记录级问题。证据本体（28 份 result.json、150 轮记录、manifest、包 SHA、修复链 diff）经全量程序化复查未发现任何伪造、错配或回归。

## 4. 封存声明

本记录为第 6 轮独立核验的完整产出，封存后不补写；如需复议另建第 7 轮记录。本轮除本文件外未写入、未修改任何文件；未执行任何改变候选、证据或 git 历史的操作（全部命令为只读：`git hash-object`/`git log`/`git show`/`git diff`/`git merge-base`/`git status`、`sha256sum`、`python3 -c` 解析、`grep`/`ls`/`sed -n`/`date`）。CHK-035 的整改（§4 第 67 行 27→28 一处数字）与 CHK-036 的补记留给实施方在下一记录版本处理，处理时须保持本记录与第 4/5 轮记录原样。
