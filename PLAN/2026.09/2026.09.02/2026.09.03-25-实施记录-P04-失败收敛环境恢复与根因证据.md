# 实施记录 — P04 失败收敛、环境恢复与根因证据

> 会话角色：`作者/执行方`
>
> workflow_id：`fakenetng-mcp-20260902-6ada6479`（长程实施授权进行中）
>
> 实施子方案：`PLAN/2026.09/2026.09.02/2026.09.03-21-实施子方案-P04-失败收敛环境恢复与根因证据.md`，`v1`（blob `9887a503…`），状态 `已审核, 可以实施`
>
> 审核闭环：`2026.09.03-23-审核-P04`（10 AUD 含 1 高危）+ `2026.09.03-24-审核处理-P04`（全部闭合，作者自评闭环）
>
> 总纲 v2 blob `e5c734ea…`；需求正式 v1 blob `b91ded55…`；`AGENTS.md` `c84a7533…`
>
> 审核基线 HEAD：`b6f1ce3`；分支：`process-redirect-v23`
>
> 任务边界：`fakenet/mcp/{incident,artifacts,faultinject}.py` 新建；`{config,supervisor,cli,tools,coordination,winservice,baseline,server}.py` 增量；`test/mcp/test_p04_units.py`、`test/mcp/acceptance/run_p04_acc.py`；候选 r24—r34；`Logs/fakenetng-mcp/**`。用户既有改动未触碰。
>
> 状态：`未开始 → 进行中 → 完成`（2026-09-03）

## IMP/ACC 总览（最终）

| 项 | 状态 |
| --- | --- |
| IMP-P04-01 停止上限与配置 | 完成（`stop_grace_seconds` 默认 60, 范围 5..600, install 参数） |
| IMP-P04-02 incident 采集器 | 完成（14 项基础层+逐项清单/失败原因/SHA+comsvcs MiniDump 条件升级+60s/180s 预算+512MiB 配额） |
| IMP-P04-03 内部失效自动处置 | 完成（终止性判据=异常+probe 失败或连续 2 周期；protective stop 经 Coordinator；`record_terminal_failure` 释放所有权并保留 last_run_outcome=failed） |
| IMP-P04-04 产物登记与元数据 | 完成（run 目录登记+元数据-only 面） |
| IMP-P04-05 全节恢复审计+故障注入器 | 完成（五节规范化比较；五类注入器 env 门控进程内钩子， 不暴露 MCP 工具） |
| IMP-P04-06 正常服务停止/升级受控退出 | 完成（draining 拒新变更+internal 例外；SvcStop checkpoint/waithint 刷新；endpoint 最后关闭；SCM 真实 STOPPED） |
| IMP-P04-07 测试+构建 | 完成（宿主 88/88；全仓 Wine 门禁；r24—r34 共 11 次可归因构建） |
| IMP-P04-08 ACC runner+真机执行 | 完成 |
| ACC-014 / ACC-015 / ACC-018 / ACC-019 | **pass / pass / pass / pass**（候选 `mcp-cdcfee761-d59bd6af1f05`，源 commit `dcfee761…`，ZIP `d59bd6af…`；同候选 ACC-016 回归 pass） |
| FAULT-POINTS（ACC-012/013 机制份额） | **pass**（五类各一次触发并收敛： policy_pause/listener_stop/diverter_stop/child_hang/cleanup_error） |

## 轮次 1 结果（2026-09-03）

- 产品修复（全部有真机证据， 提交 `2d9581d…b6f1ce3` 与后续 P04 提交）： readiness 中继线程未 start（SCM 60s 放弃重启循环）、双 AppContext（受控退出 drained 了未被服务的协调器）、drain 门拦截了自身的受控停止（internal 例外）、SvcStop 控制处理器 NameError×3（wait-hint 常量拼写/常量丢失/缺 threading 导入——事件日志逐一定位）、保护性停止绕过协调器导致 run_id 永不释放、incident 清单 Windows 路径在 POSIX 下不分隔、FAULT-POINTS 调度把退出码当布尔。
- 注入手段从 VM 级 `taskkill python.exe`（误杀 Win10VM MCP 观察通道）改为进程内钩子（env 门控+服务重启装载）；因此误杀事件按 AGENTS 门禁完成一次快照重置（2026-09-03, 唯一快照/单次 revert/`vmware -x`/`DESKTOP-3FI41GR` 完成门）。
- 里程碑：ACC-014 真机 incident 包完整（14 基础项+manifest 逐项哈希+条件转储）；ACC-019 正常 SCM 停止全链（新变更拒绝/查询单调保持/恢复审计通过后真实 STOPPED/升级器在 stopped 后替换文件/干净重启）；FAULT-POINTS 五类收敛（policy_pause 经 60s 宽限后 failed 收敛、其余 stopped）。
- ACC-018：8 项运行期真机缺陷闭环沿革表已落证据（uvicorn SCM 崩溃/sc 1072/ctypes 桩/diverter 键/pydivert 冻结/filter 语言/包数据缺失/restart 竞态）， 本轮无新阻断缺陷。
- 证据目录：`Logs/fakenetng-mcp/mcp-cdcfee761-d59bd6af1f05/{ACC-014,ACC-015,ACC-018,ACC-019,FAULT-POINTS,ACC-016}`。

## 结论

**P04 子方案状态：已完整落地（CHILD_DONE）。**
