# 实施子方案 P01 — 可部署的 MCP 服务入口

> 子方案路径：`/home/adminn/projects/flare-fakenet-ng/PLAN/2026.09/2026.09.02/2026.09.02-06-实施子方案-P01-可部署的MCP服务入口.md`
>
> 子方案版本：`v8`
>
> 状态：`PLAN-CHANGE-007受影响切片待必要审核`（当前PLAN-CHANGE-006资格见2026.09.10-05必要方案定向复审；不表示ACC/CHK通过）
>
> 会话角色：`作者/执行方`
>
> 创建日期：`2026-09-02`
>
> workflow_id：`fakenetng-mcp-20260902-6ada6479`

## 1. 对象身份与来源

### 1.1 规范性来源

- 总纲：`PLAN/2026.09/2026.09.02/2026.09.02-02-总纲-FakeNet-NG-MCP.md`，`v2`，Git blob `e5c734ea6a5225f1191b72e1c5864ba722dbb3ea`，状态 `已审核, 可以实施`；其首次第三方审核与引入记录为同目录 `04-审核`（blob `f4dc1a13…`）与 `05-审核处理`（blob `e0463092…`）。
- 需求：`PLAN/2026.09/2026.09.02/2026.09.02-01-需求提炼-FakeNet-NG-MCP.md`，`正式需求 v1`，Git blob `b91ded55e75b96e60f66c8d58125d8a220d62a56`。
- 生效项目约定：仓库根 `AGENTS.md`（输出目录、Docker/Wine builder、VM 重置与 host-only 授权）。
- 执行顺序：总纲 §1.1/§8 固定 `P01 → P02 → P03 → P04 → P05`；P01 是最早未完成 Pxx。前序子方案：无。后序子方案（P02—P05）不存在，本方案不依赖它们。

### 1.2 P01 切片（来自总纲 §7/§8/§9/§12）

- 主责 OUT：OUT-002（协议与客户端可用入口）、OUT-003（受限 host-only 暴露面）、OUT-016（LocalSystem 与机器级布局）。
- 部分承接：OUT-017 的候选安装/启动入口证据（最终验收归 P05）。
- 主责 ACC：ACC-002、ACC-003、ACC-016；并产出 ACC-017 所需安装/启动前置证据（不宣称 ACC-017 通过）。
- 排除：FakeNet-NG 生命周期、业务工具 schema 冻结（P02）、WinDivert 链路、恢复审计、incident、正式压力计数（P03—P05）。

## 2. 已固定事实（编写本方案时的只读证据）

| # | 事实 | 证据 |
| --- | --- | --- |
| F-01 | 官方 MCP 规范 `2026-07-28` Streamable HTTP：单 MCP endpoint、仅 POST；每请求必需 `Accept: application/json, text/event-stream`、`MCP-Protocol-Version`、`Mcp-Method`、（tools/call 等）`Mcp-Name` 头；版本同时出现在头与 `_meta.io.modelcontextprotocol/protocolVersion`，不一致拒绝 `-32020 HeaderMismatch`；未知版本 `400`+`-32022 UnsupportedProtocolVersionError`（含 `data.supported`）；未知方法 `404`+`-32601`；通知回 `202`；响应为单 JSON 或请求内 SSE；GET/DELETE 回 `405`；无 session、无 GET SSE、无 Last-Event-ID；服务端必须实现 `server/discover`，无 initialize 握手；规范要求 Origin 校验（本产品按用户裁决显式偏离，见 §5.3） | `https://modelcontextprotocol.io/specification/2026-07-28/basic/transports/streamable-http` 与 `.../basic/versioning`（2026-09-02 抓取，记录于外发调用账本） |
| F-02 | 官方 Python SDK `mcp` `2.1.1`（v2 重写）声明支持 `2026-07-28` 及更早全部修订，含 Streamable HTTP 服务端；`requires_python>=3.10`，与镜像内 Windows Python 3.11.9 兼容 | `https://pypi.org/pypi/mcp/json`（2026-09-02 只读抓取） |
| F-03 | 目标 AI 客户端（本部署实际使用方）为 ZCode 桌面/CLI MCP 客户端 `3.10.2`；其 MCP 服务器配置支持 `type: http` + `url` + 静态自定义 `headers` 字段并在全部 HTTP 请求自动发送；本机 `mcp.servers` 已有 `Win10VM = type:http, http://192.168.204.149:28787/mcp` 且本会话可调用其工具，证明该客户端经 host-only 直连 VM HTTP MCP 可用 | 本机 ZCode 3.10.2 运行日志；`~/.zcode/cli/config.json`（脱敏读取）；版本化路径 `/home/adminn/.zcode/cli/plugins/cache/zcode-plugins-official/zcode-guide/0.1.0/skills/diagnosing-mcp/SKILL.md` 第 31-33、55-57、96-98 行（0.1.0 缓存副本为实测对象；会话插件清单另列 0.4.1，复核时以该版本化路径为准） |
| F-04 | 构建镜像存在且身份匹配 AGENTS.md 历史值：`flare-fakenet-ng/gui-vm-diagnostic-builder:py3119-pyi6220` = `sha256:1b33b518a7c71ecd13264f0d1c41faf254e301b8702e2da8ca11376cff700a5e`（2026-09-02 `docker image inspect` 只读核验） | 本工作流实施前只读检查 |
| F-05 | 正式 GUI v35 包装合同：`Build-GuiVmPackage.sh [source-commit] [output-root] [output-directory]` → Docker 内 `tools/build_gui_vm_package_wine.py`（归档不可变源 commit → Windows-Python 回归门禁 → PyInstaller `fakenet.spec`/`fakenet-gui.spec` → 确定性 ZIP + manifest + 复验）。该入口产出 GUI 包，不能宣称生成 MCP 包 | `Build-GuiVmPackage.sh`、`tools/build_gui_vm_package_wine.py`（当前工作区） |
| F-06 | 仓库已有离线 `wheelhouse/` 机制（Windows cp311 轮子 + `SOURCES.md`）；v35 构建对轮子的既有用法是“解压到 staging 目录并设 `PYTHONPATH` 供回归门禁使用”（pydivert 2.1.0 前例），不是 pip 安装——“Wine 内 `pip --no-index` 离线安装 wheelhouse 轮子”是本方案 IMP-P01-06 的新增待实测验证点 | `wheelhouse/` 目录、`tools/build_gui_vm_package_wine.py:120-140` |
| F-07 | `fakenet/mcp/` 不存在；`setup.py` 无 MCP 依赖；`fakenet.spec` `hiddenimports=[]`。MCP 服务代码与打包全部新建 | 本工作流只读检查 |
| F-08 | 验收 VM（即 Win10VM MCP 所在 Win10 验收机）host-only 地址 `192.168.204.149`；宿主 vmnet 地址 `192.168.204.1`（AGENTS.md 授权临时传输服务绑定地址）；二者构成默认部署参数，但实现不得硬编码（REQ-015） | 需求记录 009、AGENTS.md、F-03 配置 |
| F-09 | 总纲 §10 冻结的验收 runner 合同：工作目录=仓库根；输出 `Logs/fakenetng-mcp/<candidate-id>/<acc-id>/`；`result.json` 最小字段与退出码 `0/1/2/3+`；`<candidate-id>` 由 P01 按“不可变源 commit + 候选包 SHA-256 + manifest”生成且候选期内不变 | 总纲 §9.1 |

## 3. P01 冻结的纯实现细节（总纲 §4 授权 P01 冻结项）

| 冻结项 | 值 | 依据 |
| --- | --- | --- |
| endpoint 路径 | `/mcp` | 总纲 §4（单 endpoint 不变） |
| SCM 服务名 / 显示名 | `fakenetng-mcp` / `FakeNet-NG MCP (fakenetng-mcp)` | 总纲 §4 |
| 默认监听端口 | `28788`（配置可改；与 Win10VM MCP 的 28787 区分） | 总纲 §4（端口为交付细节） |
| 探针工具方法名 | `ping`（无副作用；返回服务身份、协议版本、接收到的 `X-FakeNet-Controller-ID` 存在性与格式判定） | 总纲 §4（工具名为实现细节；P01 只提供无副作用探针） |
| 机器级服务配置文件 | `%ProgramData%\FakeNet-NG-MCP\configs\service.json`（字段：`listen_ip`、`listen_port`、`allowed_host_ips[]`、`programdata_root` 覆盖项用于测试） | 依据为 REQ-014“至少划分”措辞（总纲 §2.1 为“仅”五目录冻结）：`service.json` 属服务部署配置，置于 `configs\` 之下、不进入产品数据最小集合枚举、不改变总纲五目录冻结；该放置决定见 DEC-005 |
| 协议支持范围 | 默认仅启用现代基线 `2026-07-28`；`allow_legacy_protocol=false`。部署显式置 true 后兼容 `2024-11-05`、`2025-03-26`、`2025-06-18`、`2025-11-25` 的 HTTP 请求；版本逐项声明、逐项互操作验证，默认/开启两模式都保持现代必需 Header 校验，旧版按其版本处理且不得冒充现代合规。单 endpoint、无独立 GET SSE/session、控制者 Header 和 host-only 访问边界不变。版本/客户端探针失败公开重新裁决，不静默降级。 | PLAN-CHANGE-003／记录008、FB-001 |
| Windows 服务运行时 | `pywin32` ServiceFramework 承载 SCM 服务入口（Initialize/PrepareToHostSingle/Dispatcher），安装/卸载经 `sc.exe`；不引入 ctypes 手写分发器（v3 修订：真机证据显示 ctypes 桩的状态上报全部失效且进程不退出，而 pywin32 本就是 mcp==2.1.1 的 win32 标记依赖，见 DEC-003 补充） | 真机验收证据（ACC-016 历轮）+ 最小新增依赖为零（DEC-003 修订） |
| Python MCP 实现 | 官方 `mcp==2.1.1`，以离线 wheel 进入 `wheelhouse/` 并在构建期安装后由 PyInstaller 冻结；hiddenimports 按 PyInstaller 探针输出冻结 | F-02、F-06（DEC-002） |
| 候选包形态 | PyInstaller onedir `fakenetng-mcp.exe` + `_internal\`，加 `install-fakenetng-mcp.ps1`/`uninstall-fakenetng-mcp.ps1` 与 manifest 打成确定性 ZIP，写入 `dist/` | 总纲 §2.1 交付集成面、F-05 模式 |

## 4. 实施项 IMP

每项含：范围/现状/目标行为/前置/依赖/顺序/实际落点/允许裁决的纯实现细节。

### IMP-P01-01｜目标客户端身份冻结与探针通道

- 范围：把实际目标 AI 客户端身份（ZCode 3.10.2 桌面/CLI MCP 客户端，`headers` 静态头能力）写入实施记录并冻结为 ACC-002 的验收输入身份；建立“隔离配置的 ZCode 客户端探针通道”：在不重启当前工作会话的前提下，用独立配置目录（独立 `HOME`/配置文件）启动一次性 ZCode 进程，使其按 `type:http + url + headers` 连接被测服务并完成发现与一次 `ping` 调用。
- 现状：F-03 已证实配置能力与既有 host-only 连接；隔离进程探针通道尚未验证。
- 目标行为：探针通道命令可重复执行；返回原始 HTTP 证据（服务日志 + 客户端侧输出）；探针失败时能区分“客户端不支持现代纪元/不支持 Header”（→FB-001/FB-013 失败证据）与“探针通道自身不可用”（→ACC-002 阻断）。
- 前置：无（P01 首项）。
- 依赖：无。顺序：第 1 项。
- 实际落点：`test/mcp/probe/zcode_client_probe.py`（或同等命名 runner）+ 实施记录；客户端身份记录写入实施记录与候选 manifest。
- 允许裁决细节：探针通道的具体启动方式（CLI 子进程、参数与环境变量）由实现探针时的实测结果决定，并记录 DEC；若一切隔离通道均不可行，则 ACC-002 的客户端互操作步骤列为真实阻碍并输出解除步骤（不得用其他客户端替代通过）。

### IMP-P01-02｜Python MCP 实现探针与 wheelhouse 固定

- 范围：把 `mcp==2.1.1` 及其传递依赖以精确版本下载为 Windows cp311 兼容轮子加入 `wheelhouse/`（更新 `wheelhouse/SOURCES.md` 记录来源与哈希）；在 Linux 宿主用同版本验证 SDK 服务端能以 `2026-07-28` 语义应答（server/discover 或直接 tools 调用、必需头校验、`-32022` 行为）。
- 现状：F-02（PyPI 声明）；仓库无该依赖。
- 目标行为：`wheelhouse/` 内新增轮子集合可离线安装；最小 SDK 冒烟脚本在本机通过，证明所选实现“实际支持 2026-07-28”而不只是包名声称。
- 前置：无。依赖：无。顺序：第 2 项（与 IMP-P01-01 可并行）。
- 实际落点：`wheelhouse/*`（新增轮子）、`wheelhouse/SOURCES.md`、`test/mcp/test_sdk_probe.py`（冒烟）。
- 允许裁决细节：依赖版本解析集合以 `pip freeze` 实测冻结；若 SDK 在 Wine/PyInstaller 冻结后不可用（后续 IMP 发现），按“明确隔离的兼容适配层”回落为自研传输模块 `fakenet/mcp/transport.py`（协议仍为 2026-07-28，不是旧 transport），须记录 DEC 与实测证据。

### IMP-P01-03｜`fakenet/mcp/` 服务骨架（配置、目录、单实例、SCM 服务）

- 范围：新建 `fakenet/mcp/` 包：`config.py`（读取 `service.json`，全部绝对路径，默认值遵循 §3 表）、`paths.py`（ProgramData 五目录惰性创建：`configs\custom\`、`state\`、`logs\`、`baselines\`、`artifacts\`）、`singleinstance.py`（命名互斥体，二次实例立即退出）、`winservice.py`（ctypes SCM 入口：`run` 作为服务运行、调试模式前台运行）、`cli.py`（`install`/`uninstall`：`sc.exe create/delete`，`start`/`stop`，参数 `--listen-ip/--port/--allowed-host` 写入 `service.json`）。
- 现状：F-07（全部新建）。
- 目标行为：`fakenetng-mcp.exe install --listen-ip <vm-ip> --port 28788 --allowed-host <host-ip>` 幂等完成：写配置、创建服务（`start= auto`、默认 LocalSystem）；防火墙规则的建立由 IMP-P01-05 的 `firewall.py` 承担并在该 IMP 集成进 install 命令（本 IMP 阶段 install 不含防火墙动作）；`run` 在 SCM 与前台调试两种模式下行为一致；无登录桌面依赖、不读用户 profile、不依赖 cwd、不依赖映射盘。
- 前置：IMP-P01-01/02 结论（客户端身份与 SDK 选型已冻结）。
- 依赖：无产品代码依赖。顺序：第 3 项。
- 实际落点：`fakenet/mcp/{__init__,config,paths,singleinstance,winservice,cli}.py`。
- 允许裁决细节：ctypes 服务桩的具体结构、退出码、日志格式；互斥体命名。

### IMP-P01-04｜MCP 服务端（host-only 绑定 + 探针工具 + Header 中间件）

- 范围：`fakenet/mcp/server.py`：用 `mcp==2.1.1` 暴露单 endpoint `/mcp`，仅绑定 `service.json` 的 `listen_ip:listen_port`；注册唯一无副作用工具 `ping`；HTTP 中间件读取 `X-FakeNet-Controller-ID`（UUIDv4 格式校验），值随 `ping` 结果返回（存在/缺失/格式三类）；服务端完全不读取、不校验 `Origin`；无 TLS、无 token。
- 现状：新建。
- 目标行为：服务启动后（P01 阶段）对任何可达连接提供 `ping` 与 SDK 内建协议方法；未配置或无法绑定 host-only 地址时启动失败并给出结构化原因；日志（普通文本）写入 `logs\`。
- 前置：IMP-P01-02/03。
- 依赖：`mcp` SDK。顺序：第 4 项。
- 实际落点：`fakenet/mcp/server.py`（及必要的小模块拆分）。
- 允许裁决细节：SDK 服务组装 API 的具体用法（FastMCP 等价层或低层 ASGI）、SSE/JSON 响应形式选择、线程模型。

### IMP-P01-05｜防火墙规则幂等管理

- 范围：`fakenet/mcp/firewall.py`：以 `netsh advfirewall firewall` 维护单条入站规则（名称 `FakeNet-NG MCP`，`dir=in action=allow protocol=TCP localport=<port> remoteip=<allowed_host_ips 逗号列表>`）；实现完成后集成进 IMP-P01-03 的 CLI：`install` 时先删后建（幂等），`uninstall` 时删除；服务启动时只校验规则存在且参数一致，缺失/漂移时记录并修复（仍幂等）。
- 现状：新建。
- 目标行为：重复安装不产生重复规则；防火墙开关状态不影响规则存在性；仅允许配置源 IP。
- 前置：IMP-P01-03。顺序：第 5 项。
- 实际落点：`fakenet/mcp/firewall.py`。
- 允许裁决细节：netsh 参数拼写细节、规则组名。

### IMP-P01-06｜Windows 打包：spec、构建器与候选身份

- 范围：新增 `fakenet-mcp.spec`（onedir，入口 `fakenet/mcp/__main__.py` → `fakenetng-mcp.exe`，hiddenimports 按 PyInstaller 实测冻结）；新增 `tools/build_fakenetng_mcp_wine.py`：归档不可变源 commit → 从 `wheelhouse/` 用 `pip --no-index` 离线安装 `mcp==2.1.1` 及依赖（该 Wine 内离线安装方式为新增待实测验证点，区别于 F-06 的解压+PYTHONPATH 先例；失败时按 DEC-002 回落或修复并记录）→ 运行与 MCP 相关的 Windows-Python pytest 门禁 → PyInstaller 冻结 → 组装候选目录（exe + `_internal` + `configs/` 内置只读示例 + 安装/卸载脚本 + `mcp-candidate-manifest.json`：源 commit、包 SHA-256、SDK/Python/PyInstaller 版本、目标客户端身份记录、冻结端口与 endpoint）→ 确定性 ZIP + 复验文件写入 `dist/`（输出目录显式指定，不与 v35 冲突）；新增 Linux 包装 `Build-FakeNetNgMcpPackage.sh [source-commit] [output-root] [output-directory]`（复用同一 Docker 镜像）。生成并登记 `<candidate-id>`：`mcp-c<commit[:8]>-<zip-sha256[:12]>`，候选期内不变。
- 现状：F-05/F-06/F-07。
- 目标行为：从不可变源 commit 一步产出可复验候选 ZIP；不改不覆盖 `Build-GuiVmPackage.*` 的 v35 合同；Windows-Python 测试命令形状遵守 AGENTS.md（`xvfb-run -a wine 'C:\Python311\python.exe' -m pytest …`，仓库挂载 `/workspace`）。
- 前置：IMP-P01-02…05 完成。
- 依赖：Docker 镜像（F-04；若缺失按 FB-012 以 `--image-only` 重建并记录新镜像身份）。
- 顺序：第 6 项。
- 实际落点：`fakenet-mcp.spec`、`tools/build_fakenetng_mcp_wine.py`、`Build-FakeNetNgMcpPackage.sh`、`dist/` 产物。
- 允许裁决细节：manifest 字段扩展、确定性 ZIP 时间戳沿用 v35 模式、pytest 门禁的文件选择。

### IMP-P01-07｜单元与 Windows-Python 测试

- 范围：`test/mcp/` 下新增：配置加载与绝对路径、Header 中间件三类判定、`ping` 无副作用、防火墙命令构造（不实际执行 netsh，用命令字符串断言）、单实例互斥体语义（本机可测部分）、服务 CLI 参数解析；全部测试在 Linux 与 Docker Windows-Python 双侧可运行（Windows 专属断言按既有仓库模式跳过或走 VM 验收）。
- 现状：新建。
- 目标行为：`python3 -m pytest test/mcp -q` 在宿主通过；`xvfb-run -a wine 'C:\Python311\python.exe' -m pytest test/mcp -q`（仓库挂 `/workspace`）在镜像内通过；并纳入 IMP-P01-06 的构建门禁。
- 前置：IMP-P01-03…05。顺序：与 IMP-P01-06 交叉（spec 构建门禁引用其结果）。
- 实际落点：`test/mcp/*`。
- 允许裁决细节：测试文件拆分、mock 边界。

### IMP-P01-08｜ACC 验收 runner（§9.1 合同实现）

- 范围：`test/mcp/acceptance/run_p01_acc.py`：实现总纲 §9.1 冻结合同——输入：`--source-commit`、`--package`（路径+SHA-256 复核）、`--manifest`、`--requirements-blob`、`--master-plan-blob`、`--vm-identity`（目标 VM 身份）、`--config-identity`（本次部署 `service.json` 参数身份）、`--acc`（`ACC-002|ACC-003|ACC-016|P01-ENTRY`）；全部输入写入 `result.json` 对应字段（`requirements_blob`、`master_plan_blob`、`environment_identity`、配置身份并入 `candidate_id` 记录与 `actions`）；输出：`Logs/fakenetng-mcp/<candidate-id>/<acc-id>/`（`result.json` 最小字段齐全 + 原始 stdout/stderr + 逐项证据 + SHA-256 清单）；退出码 `0/1/2/3+`。VM 侧动作经 Win10VM MCP（PowerShell）执行；包与证据经仅绑定 `192.168.204.1` 的临时 host-only 传输服务进出，按 SHA-256 核验，会话后停止服务。
- 现状：不存在（总纲不虚构命令，本项创建真实工具）。
- 目标行为：每个 ACC 一条可重复命令；被阻断输出 `2` 并写 `blocker`。
- 前置：IMP-P01-06 产出候选包。顺序：第 8 项。
- 实际落点：`test/mcp/acceptance/run_p01_acc.py`、`test/mcp/acceptance/` 辅助模块。
- 允许裁决细节：runner 内部结构、传输服务实现（Python http.server 即可）。

### IMP-P01-09｜真实 Win10 VM 部署与 P01 ACC 执行

- 范围：按 AGENTS.md 门槛重置验收 VM（唯一目标快照、一次 `revertToSnapshot`、`vmware -x`、以 Win10VM MCP `$env:COMPUTERNAME=DESKTOP-3FI41GR` 为完成门）；传输候选包到 VM；执行安装（LocalSystem、无交互）。执行顺序固定：先按 ACC-016 程序安装并采集其核心断言（`sc qc` 账户/自启/布局），再运行 ACC-002（IMP-P01-01 探针通道 + 服务端证据）、ACC-003，最后汇总 ACC-016 完整结论并采集 `P01-ENTRY` 前置证据；若 ACC-016 最终结论为失败，则同轮已采的 ACC-002/003 证据标记为“待重采”，不得引用为通过样本。全部证据回传 `Logs/fakenetng-mcp/<candidate-id>/`。
- 现状：VM 当前处于未知状态，执行时按门禁重置。
- 目标行为：ACC-002/003/016 的 `result.json` 全部 `status=pass`（或按真实结果记录失败/阻断并回流）。
- 前置：IMP-P01-01…08。
- 顺序：第 9 项（最后）。
- 实际落点：`Logs/fakenetng-mcp/<candidate-id>/**`、实施记录。
- 允许裁决细节：VM 内安装脚本的具体形式（PowerShell）、证据文件组织。

## 5. 验收项 ACC（总纲 ACC 的可执行形式）

ACC 编号沿用总纲（全局唯一）。所有 ACC 由 IMP-P01-08 的 runner 执行，证据落 `Logs/fakenetng-mcp/<candidate-id>/<acc-id>/`，退出码遵守 §9.1。

### ACC-002｜协议与客户端互操作（P01）

- 前置：候选包已按 ACC-016 先行安装并在 `192.168.204.149:28788/mcp` 监听；ZCode 3.10.2 探针通道（IMP-P01-01）就绪。
- 输入：候选包 + manifest（含目标客户端身份记录）+ 探针 UUID。
- 动作与精确预期：
  1. 隔离配置的 ZCode 客户端进程（配置 `url=http://192.168.204.149:28788/mcp`、`headers={"X-FakeNet-Controller-ID":"<uuid>"}`）完成服务发现（`server/discover` 或版本协商内联请求）并取得工具清单；全程逐消息 POST。
  2. 客户端调用 `ping` 一次；响应含服务身份、协议版本 `2026-07-28`、controller header 判定 `valid_uuid`。
  3. 服务端日志/抓取证据同时记录：请求头（`MCP-Protocol-Version`/`Mcp-Method`/`Mcp-Name`/`Accept`/`X-FakeNet-Controller-ID`）、响应形式（JSON 或请求内 SSE）。
  4. 最小服务包以 Windows 服务身份运行（`sc query fakenetng-mcp=RUNNING`）。
  5. 对照组：无 Header 裸 HTTP 调用 `ping` 仍成功且判定为 `missing`（只读探活不要求身份）。
- 证据：`result.json`、客户端进程原始输出、服务日志、工具清单 JSON、HTTP 头记录。
- 通过：目标客户端（ZCode 3.10.2）、Python 实现（mcp==2.1.1 冻结包）与 Windows 服务包三者真实互操作，Header 静态携带被证实。
- 失败：客户端静默落入旧 transport（GET SSE/session）或必需 Header 缺失/不发送（→FB-001/FB-013，保存原始证据并停止 P01）。
- 阻断：目标客户端不可取得或探针通道自身不可用（记录 blocker，不计通过/失败样本）。

### ACC-003｜受限 host-only 暴露面（P01）

- 前置：ACC-016 安装完成；host-only 参数有效。
- 动作与精确预期：
  1. VM 内 `netstat -ano`（或 `Get-NetTCPConnection`）证据：仅 `192.168.204.149:28788` LISTEN，无 `0.0.0.0`/`::`。
  2. `netsh advfirewall firewall show rule name="FakeNet-NG MCP"` 导出：`RemoteIP=192.168.204.1`、`LocalPort=28788`、方向入站、动作允许。
  3. 启用 Windows 防火墙复验（先临时放行 Win10VM MCP 通道：增加仅允许 `192.168.204.1` 访问 TCP 28787 的临时入站规则，防止验收编排通道自伤；复验后恢复防火墙原状态并删除临时规则）：从宿主 `192.168.204.1` 连接 `28788` 成功；从非允许源连接不可达（连接超时/拒绝证据）。非允许源的构造方式：在宿主 host-only 接口上临时添加次级 IP（如 `192.168.204.250`）发起连接，结束后移除；该 IP 不在规则 `RemoteIP` 内即构成合法负样本。
  4. 重复 `install` 幂等：规则不重复、参数不漂移。
  5. 交付物风险说明：包内 README/manifest 记录“明文 HTTP、无 TLS/token/Origin 校验的显式偏离及 DNS rebinding 剩余风险”（对应 REQ-002/CON-003/NON-004）；代码审查记录服务端无 Origin 读取逻辑。
- 证据：监听导出、规则导出、两端连接原始结果、防火墙开关状态时间线、README/manifest 摘录。
- 通过：只绑定 host-only IP、只允许配置源、幂等、安全偏离已公开。
- 失败：出现 `0.0.0.0` 监听、宽放行、规则漂移或风险说明缺失。
- 阻断：host-only 设施故障（VMnet 不可用、IP 冲突）。

### ACC-016｜LocalSystem 与机器级布局（P01）

- 前置：安装包可用；VM 可达。
- 动作与精确预期：
  1. 无交互安装后 `sc qc fakenetng-mcp`：`BINARY_PATH_NAME` 指向 `%ProgramFiles%\FakeNet-NG-MCP\`，`START_TYPE=AUTO_START`，账户为 LocalSystem（`OBJE` 未指定即 LocalSystem）。
  2. 服务启动后进程属主为 `SYSTEM`、会话 0（`Get-Process -IncludeUserName`/`tasklist /v` 证据）；全程无桌面交互。
  3. 布局核对：程序与内置配置在 `%ProgramFiles%\FakeNet-NG-MCP\`；`%ProgramData%\FakeNet-NG-MCP\` 下存在 `configs\custom\`、`state\`、`logs\`、`baselines\`、`artifacts\`；`configs\service.json` 生效。
  4. cwd/profile/映射盘独立性：服务进程 cwd 为 `C:\Windows\System32`（SCM 默认）仍正常应答 `ping`；不引用任何用户 profile 路径（配置与代码审查证据）；映射盘条件独立性：`net use` 输出为空（无任何映射盘）的环境证据 + 代码审查确认全部资源/配置路径为绝对路径、无盘符相对或映射盘引用（REQ-014“不得依赖…映射盘”）。
  5. 负向：经 MCP 无法触达任意命令/任意路径能力——P01 阶段工具面仅 `ping` 与协议内建方法（工具清单证据），无文件类工具。
- 证据：`sc qc`/`sc query` 输出、进程属主/会话、目录树清单（含 ACL 摘要）、工具清单、cwd 证据。
- 通过：无交互依赖、布局/账户正确、无越权工具。
- 失败：依赖用户环境、布局错位或暴露越权工具。
- 阻断：目标 VM 权限设施损坏（无法安装/启动服务）。

### P01-ENTRY｜候选入口前置证据（P01 自有证据标签，非总纲 ACC 编号）

- 命名声明：`P01-ENTRY` 是本子方案定义的前置证据标签，不属于总纲 §9.1 冻结的 `<acc-id>`（ACC-001—ACC-019）词表；其证据目录与 `result.json` 均携带“非 ACC-017 通过声明”标记，供 P05 汇总时与正式 ACC-017 区分。
- 三边界：通过=同一候选三元组（源 commit+包 SHA-256+manifest）在指定真实 Win10 VM 原生安装、作为服务启动且 MCP 端口可达的完整证据链闭合；失败=安装/启动/可达任一环节失败（保存原始输出）；阻断=VM 或传输设施不可用（`blocker` 记录，不计样本）。
- 证据：安装日志、服务状态、端口探测、候选身份三元组。

## 6. 需求保真门禁（P01 范围）

- 应承接数：`6`（累计有效结论 8、9、10、26 全量；7、38 的 P01 子句——可配置可迁移不硬编码、实施环境纪律）。
- 完整承接数：`6`。
- 未落点数：`0`。
- 语义弱化数：`0`。
- 不可验收数：`0`。

核对方式：结论 8（REQ-002）→ OUT-002 → 本方案 F-01/F-02、IMP-P01-02/04/06、ACC-002；结论 9/10（CON-003、NON-004）→ OUT-003 → IMP-P01-03/05、ACC-003 与 §3 冻结表（无 Origin/TLS/token）；结论 26（REQ-014）→ OUT-016 → IMP-P01-03、ACC-016；结论 7 的入口子句（REQ-015 不硬编码）→ §3 表全部参数可配置 + F-08 只作默认值；结论 38（记录 040 纪律）→ Docker/Wine 仅出现于 IMP-P01-06/08 实施环境与 F-04/F-05 证据，不进入产品能力或 ACC 通过语义。参与者、输入输出、触发/顺序、状态、不变量、异常/边界、适用前提、重新裁决条件与可观察验收均可经 需求→总纲 OUT/Pxx→本方案 IMP/ACC 完整恢复。

## 7. 版本变更记录

- **v0 / 2026-09-02：** 首次编写。建立 IMP-P01-01—09、ACC-002/003/016 可执行形式与 ACC-017-PRE、§3 冻结实现细节、事实表 F-01—F-09、需求保真门禁。无既有版本，无删除或复用 ID。
- **v1 / 2026-09-02：** 作者自评修正。ACC-003 步骤 3 增加：启用防火墙前临时放行 Win10VM MCP 通道（28788 之外的保护规则，复验后恢复与删除），避免验收编排通道自伤；非允许源负样本固定为宿主 host-only 接口临时次级 IP（如 `192.168.204.250`）并在结束后移除。IMP/ACC 编号不变。
- **v2 / 2026-09-02：** 引入首次第三方审核（AUD-P01-001—008，全部同意）。ACC-016 补回映射盘条件独立性证据位（AUD-P01-001）；§3 service.json 行改依 REQ-014“至少划分”并声明不扩张总纲“仅”五目录冻结、放置决定记 DEC-005（AUD-P01-002）；`ACC-017-PRE` 更名 `P01-ENTRY` 并声明为 P01 自有标签+三边界（AUD-P01-003）；F-06 改述为解压+PYTHONPATH 先例、Wine 内 pip 离线安装明示为待实测新验证点（AUD-P01-004）；IMP-P01-09 执行顺序固定为 ACC-016 核心断言先行、失败时 ACC-002/003 证据标记待重采（AUD-P01-005）；IMP-P01-08 输入补齐需求 blob/总纲 blob/VM 身份/配置身份四项（AUD-P01-006）；IMP-P01-03 install 移除防火墙动作、归 IMP-P01-05 集成（AUD-P01-007）；F-03/DEC-001 证据钉住版本化插件路径（AUD-P01-008）。IMP 编号与总纲 ACC 编号不变。
- **v3 / 2026-09-02：** 实施期修订（作者自评闭环，未命中再次审核触发条件）：§3“Windows 服务运行时”由纯 ctypes 手写 SCM 分发器改为 pywin32 ServiceFramework。真机证据：ctypes 桩的 SetServiceStatus 全部无效（CHECKPOINT 恒 0）、进程卡 START_PENDING 不退出；pywin32 为 mcp==2.1.1 的 `sys_platform=='win32'` 标记必带依赖，实际新增依赖为零。IMP/ACC 编号、其余冻结值与全部验收边界不变。

## 8. 审核重点

- §3 冻结值与总纲 §4 裁决权限是否一致（endpoint/服务名/端口/工具名由 P01 冻结；单 endpoint、权限与状态契约未被改变）。
- ACC-002 的“目标客户端不可替代”边界与 FB-001/FB-013 失败路径是否可观察。
- 协议支持范围“仅 2026-07-28 现代纪元”是否与 REQ-002 及需求记录 008 的兼容探针语义一致（不得静默旧 transport；客户端不兼容时停止并公开重裁）。
- IMP-P01-06 是否未触碰 v35 GUI 正式包装合同；`dist/` 与 `Logs/` 归属是否符合 AGENTS.md。
- IMP-P01-09 的 VM 重置动作是否完整内嵌 AGENTS.md 门槛（唯一快照、单次 revert、`vmware -x`、`DESKTOP-3FI41GR` 完成门、guest 证据先导出）。
- 无后序依赖：P01 不依赖 P02—P05；`ping` 之外不预实现业务工具。
- wheelhouse 新增与 ctypes 服务桩不构成无依据机制或 fallback。

## 9. 2026-09-09 回流版本来源与资格

当前规范输入：同目录需求正式v3（含当日回流节）、总纲v5§16—17；§1旧blob和前序CHILD_DONE仅描述历史时点，不作为新候选通过证明。规范文件身份、逐项保真与自评见当日回流自评记录。原有非冲突条款继续有效，历史审核/验收不改。

### v4 P01补充冻结

IMP-P01-02/04/06/09和ACC-002同时覆盖默认拒绝legacy、显式开启后四版本initialize/通知/工具调用、未知版本拒绝、现代Header不被兼容模式绕过、所有版本控制者Header一致。目标客户端身份沿用当前冻结对象并复测其实际版本；若不可用只阻断ACC-002，不用脚本客户端替代。完整host-only边界及无Origin等已有明示偏离继续标注。兼容声明必须绑定真实目标客户端及版本矩阵证据。

## PLAN-CHANGE-005 当前子方案承接

IMP-P01-02/03/04负责固定128控制/ACL、CLI stop、卸载及升级等待包装；P01只验证服务入口与依赖注入接口，完整业务收敛由P03/P04后续实现，禁止倒置Pxx前置。

当前共同规范为正式需求v3及总纲v5§17；本子方案逐条承接其完整参与者、输入输出、权限、触发顺序、硬期限、状态/恢复责任、失败重试、结束条件、原生证据及失效/重裁条件，不以本段摘要降低规范。必要复审通过前受影响实施仍暂停。


## PLAN-CHANGE-006回流（2026-09-10，待必要审核）

规范输入为当前需求v3和总纲v7§18；本节完整引用§18的采用理由、适用边界、硬时限、失效/重裁条件，不削弱记录034/035。旧版本身份只作历史追溯，不授予本次实施资格。

P01 IMP-P01-03/06负责总纲§18.2的随包助手及SHA、固定产品映像登记、外部冲突拒绝、原值/类型/存在性保存、失败回滚与升级卸载精确恢复；保留其他GlobalFlag位，禁止共享全局策略和LOCAL_DUMP。交付P03的是已验证的能力与身份入口，不是未经实机验证的配置字符串。先完成§17停服再替换登记/文件；无活动助手与无遗留登记均需实证。

P01→P02→P03→P04→P05顺序、既有REQ/OUT/ACC/IMP稳定ID不变。主会话保持作者/执行方，全部CHK开放；必要审核通过前本变更受影响产品实施暂停。

PC006-R01—R04修订承接：本Pxx依总纲v7§18完整执行自己的唯一责任。P01交付专用受管映像/不监测主管CLI和助手；P03保留真实目标句柄到有界P04完成并发布/失效一次性当前stop intent；P04只保存通知原始整数、不查询发起者，执行全局单飞1、拒绝≤1秒/零持久输出及临时512MiB、真实句柄确认和正常退出分类；P05验证这些跨层顺序、外部零码/旧intent/迟到/过早关句柄/同名突发负载与最终清理。完整路径过滤候选未通过，不作为fallback。状态仍为待必要复审，未获得新实施资格。


## PLAN-CHANGE-007：v8 当前负责契约（待必要审核）

当前来源为需求v4及总纲v8§19，优先于历史§18无条件硬结束解释；保留非冲突全部既有合同。

IMP-P01-03/06：固定未登记SPE的诊断内部入口、资产SHA和原子Job创建能力；不能暴露任意命令。安装/升级/卸载在现预停链上核对实际无助手和恢复责任已解除，未结束/错身份/平台阻断禁止替换，旧包和登记原值保护保持。交P03的是已验证的包身份与能力。

采用理由是隔离Win32不可保证终止的在途I/O；仅有原生可归因证据的平台分支不再承诺同限全部归零，仍failed并保留恢复责任。普通产品故障60/180秒、活动512MiB、真实清理与同候选验收不减弱。输入、输出、触发顺序、状态与不变量按本负责合同及总纲§19逐条验收；无第二常驻服务/后台队列/旧任务重放。失效条件为入口/查询/普通故障结束/单飞预算不可证明，需回流重新裁决，不能扩大平台豁免。

007握手细化：完整承接总纲§19.1同一逻辑单飞责任及固定从属工作；不竞争自己已持锁，不授予第二份截止/额度；P01保证内部入口、P03先固定SPE句柄/Job再ACK、P04从属核验与整个责任清理、P05验证持槽握手/阻塞/迟到/Job归属失败的原生反例。

PC007-R01/R02修订承接：总纲§19.2仅固定16KiB以内协议握手/内存授权可并行，真正采集执行者始终≤1且移交唯一剩余额度；stop-intent/state/incident元数据不在主管锁内等待I/O。每个对象实际结束才释放本对象句柄，助手结束不替代目标结束；全对象结束及全审计通过后才解除整体责任。P05按§19.2.3验证同责任并发/峰值和助手结束但目标未结束反例。待同实例复审，不由作者关项。
