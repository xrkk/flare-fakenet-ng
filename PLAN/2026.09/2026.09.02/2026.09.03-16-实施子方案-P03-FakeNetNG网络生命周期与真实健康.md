# 实施子方案 P03 — FakeNet-NG 网络生命周期与真实健康

> 子方案路径：`/home/adminn/projects/flare-fakenet-ng/PLAN/2026.09/2026.09.02/2026.09.03-16-实施子方案-P03-FakeNetNG网络生命周期与真实健康.md`
>
> 子方案版本：`v7`
>
> 状态：`PLAN-CHANGE-007受影响切片待必要审核`（当前PLAN-CHANGE-006资格见2026.09.10-05必要方案定向复审；不表示ACC/CHK通过）
>
> 会话角色：`作者/执行方`
>
> 创建日期：`2026-09-03`
>
> workflow_id：`fakenetng-mcp-20260902-6ada6479`

## 1. 对象身份与来源

- 总纲 v2 blob `e5c734ea6a5225f1191b72e1c5864ba722dbb3ea`（§3 不变量、§5 生命周期、§6.3 快照合同、§7 OUT-001/004/006/007/008/009、§8 P03 切片、§9 ACC-001/004/006/007/008/009、§11 FB-002/003/004/008/009）。
- 需求正式 v1 blob `b91ded55e75b96e60f66c8d58125d8a220d62a56`（记录 001/003—007/011/014/018/019/021—025/032/033/038）。
- 前序：P01、P02 均 CHILD_DONE（候选 `mcp-cf9d06376-bb9ec6bf0511` 五项验收 pass）；P02 交付的稳定工具/身份/并发/配置/冲突合同是本方案的直接输入。
- 生效项目约定：`AGENTS.md`（SHA-256 `c84a7533…a426`）。
- 代码事实（本方案勘察，经 AUD-P03-001 修正）：`fakenet/fakenet.py::Fakenet`（`parse_config:151`、`start:225`、`stop:439`，`stop()` 带阶段汇总且重复调用收敛；`wait_for_capture_failure:543` 可复用作健康探针）；`fakenet/diverters/windows.py` 主 filter 基础构造于 `:511-513`（`outbound and (ip or ipv6)` / `outbound and ip`），**且在 EgressControl+`ExternalProcessRedirectEnabled` 时经 `build_egress_control_filter` 于 `:586` 一带重建并覆盖 `self.filter`**（内置 `configs/process_redirect_windows.ini` 即此组合）；句柄打开 `:958`；`WinDivert.check_filter` 于 `:1028`（仅 process-redirect 路径条件执行）。排除注入必须覆盖基础构造与重建两条路径，并在句柄打开前对**最终** filter 做包含排除子句的断言（缺失即拒绝启动=FB-002）；现有停止测试 `test/test_fakenet_stop_lifecycle.py`。

## 2. P03 切片（总纲 §7/§8/§9/§12）

- 主责 OUT：OUT-001（唯一无头主管→ACC-001）、OUT-004（FakeNet 故障域外控制链路→ACC-004）、OUT-006（真实健康→ACC-006）、OUT-007（Job Object 与 SCM 崩溃收敛→ACC-007）、OUT-008（最小当前恢复快照→ACC-008）、OUT-009 最终验收（真实活动锁/FakeNet 读取兼容/重解析点→ACC-009）。
- 本方案还交付：CLI预停128/升级受控退出入口（§5.5 拒绝新变更+进入统一停止序列；完整查询保持与审计归 P04/ACC-019）。
- 排除：完整 incident 采集与条件式转储、完整恢复审计故障矩阵、正式 100/10 计数（P04/P05）。
- 实现策略（PLAN-CHANGE-001）：FakeNet独立受管子进程调用现有Fakenet接口；主管创建并独占Job句柄，自身不加入Job。以STARTUPINFOEX的JOB_LIST随CreateProcess原子关联Job，禁止先创建后Assign；失败清理句柄并拒启，见总纲§16.1。

## 3. P03 冻结的纯实现细节（总纲 §4 授权 P03/P02 冻结项）

| 冻结项 | 值 | 依据 |
| --- | --- | --- |
| 健康轮询周期 | 2.0 秒（探测线程间隔；周期为调优细节， 总纲只要求持续探测且不产生虚假健康） | 总纲 §4 行“健康轮询周期 P03 冻结” |
| 健康三条件 | 受管PID+创建时间存活；子进程报告关键listener/WinDivert初始化；有界主动探测得到当次有效响应。IPC失联/过期/日志异常均撤销健康，≤2周期反映；不得仅凭进程存在 | REQ-005/记录004 |
| 主 filter 排除注入 | diverter 配置新增键 `[Diverter] ControlLinkExcludeIp/ControlLinkExcludePort`（由 MCP 启动时注入到已解析配置， 不改用户 ini 语义）；windows.py 主 filter 构造处拼入 `and not (ip.DstAddr == <ip> and tcp.SrcPort == <port>)`（legacy 与 EgressControl 两分支同样注入）；打开前 `check_filter` 校验含排除子句， 构造或校验失败→拒绝启动（FB-002） | 记录 005/006、总纲 §8 P03“唯一主 filter” |
| 状态文件 | `%ProgramData%\FakeNet-NG-MCP\state\state.json`；字段冻结为总纲 OUT-008 七字段（`run_id/controller_id/state_version/command_id/config_sha256/baseline_path/needs_recovery`）；写入=同目录临时+`fsync`+`os.replace` | REQ-007/记录 038 |
| 基线捕获 | 启动前采集到 `baselines\<run_id>.json`：路由表、DNS 服务器、进程清单（定义为：加载 WinDivert 模块的进程——`tasklist /m WinDivert*.sys`——加上 `fakenetng-mcp`/`fakenet` 进程）、监听端口与相关服务状态（与 P04 恢复审计共用同一 schema；P03 捕获+核对=清单差异比对） | 记录 015 |
| 恢复审计（P03 范围） | 服务启动发现 `needs_recovery=true`：进入 `recovering`，按基线核对（进程/路由/DNS/端口/锁）， 全部一致→写真实 `stopped`；不一致或快照损坏且有残留→`failed` 禁启动（FB-004） | 记录 025/026/038 |
| 真实活动配置锁 | 顺序冻结：先取 `CreateFileW(FILE_SHARE_READ)` 句柄→再读内容/算 SHA→再 parse/校验→注入排除→启动（锁先于读， 消除 TOCTOU 读法）；句柄持有至停止完成；重解析点判定：目标或任一父目录含 `FILE_ATTRIBUTE_REPARSE_POINT` → 拒绝（`path_escape_blocked`） | 记录 014/013 |
| 单实例 | 服务命名互斥体与实际GUI/受管运行互斥；子进程只有主管创建且每次运行唯一，双向GUI冲突均真实验证 | CON-001/OUT-001 |
| Job Object | 主管创建未命名kill-on-close Job，句柄不可继承；仅子进程和后代加入，主管不加入。确认归属先于接管执行；超限终止Job、等待树清空 | PLAN-CHANGE-001/记录025 |
| 受控退出入口 | 固定预停128→draining拒新变更（not_allowed_in_state）→等在途→同一停止/恢复责任接口；完整编排归P04。未成功冻结前不接受SvcStop；成功冻结且SCM宣告接受STOP后才发布当前attempt成功，再由CLI发送标准STOP | 当前需求v3/总纲§17 |
| 内部失效自动处置入口 | 健康监测取得终止性证据（未处理异常/核心失效）→ 撤销健康、置 `failed` 并触发同一停止序列；P03 交付机制入口， incident 证据包与条件转储归 P04 | 记录 034（P03 子句） |
| 停止宽限上限 | 沿用P04默认60、范围5..600；超限有界取证→终止本次受管Job→补偿恢复/全审计，未完成不得stopped | PLAN-CHANGE-001/P04 |
| 受管FakeNet入口 | 独立内部子进程入口运行Fakenet；主管通过仅限本次运行的私有通信传启动/健康/停止/取证，带run/序号校验和有限超时；无额外网络监听/产品工具 | PLAN-CHANGE-001 |

## 4. 实施项 IMP

### IMP-P03-01｜WinDivert 主 filter 控制链路排除（v2 扩充： 管理面额外端口 + 环回排除）

- v2 增补（2026-09-04, 用户裁决回流, 总纲 v3 冻结表）：
  - **管理面额外排除端口**(记录 041/DEC-007): `service.json` 的 `extra_control_ports`(1—65535 数组, fail-closed 校验)与安装参数透传；验收标准集 `{28787, 28790, 29095, 29094}`; 产品默认仍为最小集(宿主 IP + 产品 MCP 监听端口)。
  - **环回排除**(记录 042/DEC-008): 主接管过滤器排除 IPv4 `127.0.0.0/8`(区间形式 `ip.DstAddr < 127.0.0.0 or ip.DstAddr > 127.255.255.255`)与 IPv6 `::1`; 折入 `fakenet/mcp/controlfilter.py` 的 `apply_loopback_exclusion`(全已知形态+fail-closed), 在 `_apply_control_link_exclusion` 钩子内于控制链路折叠后应用并经同一 `check_filter` 校验; 入站 record-only 句柄保留环回记录。
- 范围：`fakenet/diverters/windows.py` 在**全部**主 filter 产生点注入排除子句——基础构造（`:511-513` 两分支）与 EgressControl+ExternalProcessRedirectEnabled 的 `build_egress_control_filter` 重建路径（进入 `:586` 前传入或重建后统一再拼入）；并在 diverter 打开句柄前对最终 `self.filter` 断言包含排除子句， 断言或 `check_filter` 失败→拒绝启动（FB-002， 失败关闭而非静默丢失）。宿主地址为 IPv6 时同样以精确地址子句排除， 无法构造即拒绝启动。`fakenet/mcp/supervisor.py` 启动前注入键值并独立预校验。
- 现状：排除机制不存在；黑名单机制（`BlackListPortsTCP/HostBlackList`）不满足要求（记录 005 已核查）。
- 目标行为：无排除参数时 filter 与现状逐字节一致（回归）；有参数时语义为“出站且非（目的=宿主 IP 且源端口=监听端口）”。
- 前置：P02。落点：`fakenet/diverters/windows.py`（最小 hunk）+ `fakenet/mcp/supervisor.py`。
- 允许裁决细节：filter 子句书写形式。

### IMP-P03-02｜受管生命周期与真实健康（supervisor）

- 范围：`fakenet/mcp/supervisor.py`：受管子进程内 `parse_config→注入排除→Fakenet.start()`；启动前：配置校验（P02 校验器）、保护参数校验、基线捕获（IMP-03-04）、活动配置 OS 锁（IMP-03-05）、`state.json` 写入（IMP-03-03）；运行期健康监测线程（§3 三条件、2s 周期、日志异常扫描本次运行日志）；停止序列：`Fakenet.stop()`（宽限）→基线恢复核对→锁释放→`state.json` 清恢复标记；替换 P02 `testdouble`（工具合同不变）。
- 目标行为：`starting→healthy/degraded→stopped/failed` 真实迁移；每步经 Coordinator 推进版本。
- 前置：IMP-P03-01。落点：`fakenet/mcp/{supervisor,health}.py`、`tools.py` 接线。
- 允许裁决细节：探针实现细节、日志扫描模式。

### IMP-P03-03｜state.json 快照

- 范围：`fakenet/mcp/snapshot.py`：七字段、临时+fsync+replace、读回校验；启动前写失败→拒绝启动（FB-003）；损坏/缺失+残留检测（残留=基线核对不一致）→`failed`（FB-004）。
- 前置：无。落点：`fakenet/mcp/snapshot.py`。

### IMP-P03-04｜环境基线捕获

- 范围：`fakenet/mcp/baseline.py`：`route print`、`Get-DnsClientServerAddress`、WinDivert 进程清单、监听端口与相关服务状态；写 `baselines\<run_id>.json`；恢复核对函数（比对当前 vs 基线， 输出差异清单）。
- 前置：无。落点：`fakenet/mcp/baseline.py`。

### IMP-P03-05｜真实活动配置锁与重解析点判定

- 范围：`fakenet/mcp/configlock.py`：`CreateFileW(FILE_SHARE_READ)` 句柄持有；路径重解析点属性检查并入 ConfigStore 逃逸矩阵（Windows 分支）。
- 前置：P02 ConfigStore。落点：`fakenet/mcp/configlock.py`、`configstore.py` 钩子。

### IMP-P03-06｜Job Object、SCM 恢复与受控退出入口

- 范围：`fakenet/mcp/jobobject.py`（主管持有、只分配受管树的 kill-on-close）；`winservice.py` 预停128→受控退出事件（draining拒新变更+在途排空+统一停止序列）；仅成功冻结后允许标准SvcStop；服务启动路径：单实例→Job→`state.json` 恢复判定（`recovering`）→恢复审计→`stopped/failed`。
- 前置：IMP-P03-02/03。落点：`fakenet/mcp/{jobobject,winservice,cli,server}.py`。

### IMP-P03-07｜测试与构建

- 范围：`test/mcp/test_supervisor*.py`（Linux 可测部分：快照、基线 schema、filter 构造字符串含重建路径、状态机、**健康探测时序测试**——以可配置短周期驱动探针， 断言探测失败在 ≤2 个周期内反映为状态回退且不出现虚假 healthy， 对应总纲 §4 的附加义务）、既有 `test_fakenet_stop_lifecycle` 不回归、Windows-Python 门禁、候选 r6 构建（含 windows.py 改动的最小 hunk 回归：全仓 Wine 测试门禁沿用 P01 构建器）。
- 前置：IMP-P03-01…06。落点：`test/mcp/*`、`dist/mcp-r6`。

### IMP-P03-08｜ACC runner 与真机执行

- 范围：`test/mcp/acceptance/run_p03_acc.py`：ACC-001（单实例/第二实例/共控拒绝）、ACC-004（六类 FakeNet 场景+持续直连+无效保护参数拒启）、ACC-006（三条件破坏/恢复矩阵+日志异常撤销， 含时序断言：破坏后 ≤2 个探测周期内状态回退）、ACC-007（强杀 MCP→Job 收敛→SCM 重启→recovering→不续跑）、ACC-008（快照五场景+禁框架扫描——禁项依据 CON-006/NON-008 定义为“数据库引擎/事务日志/事件重放/schema 迁移框架”， 扫描 `_internal` 模块清单与 wheelhouse SOURCES.md， 断言不含 `sqlite3`/`_sqlite3`/`dbm` 等 DB 引擎模块名）、ACC-009 完整（含运行期外部写锁、重解析点、停止解锁）与**真实 `restart` 工具调用复验**（绑定原 `run_id`、重试幂等， 对应记录 023 真实运行份额）。执行按 AGENTS VM 门禁（沿用已部署链路， 每轮先 uninstall+清 ProgramData 再部署）。
- 故障注入手段冻结（v3替代旧模拟）：初始化失败在子进程实际初始化点触发；未处理异常和捕获线程异常在相应受管代码线程实际抛出并保留栈；卡死暂停受管进程/实际停止阶段；受控退出走子进程停止，非受控退出只杀FakeNet子进程。MCP自身强杀只归ACC-007。全部测试门控，不新增产品工具，不用追加日志代替实际故障。
- 前置：IMP-P03-07。落点：runner + `Logs/fakenetng-mcp/<cid>/`。

## 5. 验收项 ACC（可执行形式）

三边界均由 runner 断言；证据/退出码合同同总纲 §9.1。

- **ACC-001**：服务初始stopped，无自动FakeNet/GUI；第二服务/第二受管实例拒绝；实机实际GUI运行时MCP启动FakeNet被拒，MCP运行时GUI启动FakeNet被拒。空结果或模拟第二CLI不能替代双向GUI证据。
- **ACC-004**：正常接管（启动→持续 2s 间隔直连探活全绿）、初始化失败（注入坏配置→启动拒绝）、未处理异常（注入 listener 崩溃→健康撤销但链路仍通）、核心线程异常（diverter 句柄关闭模拟→degraded/failed 且链路通）、卡死（stop 挂起宽限）、受控/非受控FakeNet子进程退出前后链路持续可达；无效保护参数→未启动（服务日志证据）。
- **ACC-006**：healthy 仅在三条件同时满足出现；逐项破坏（实际终止受管子进程/初始化证据缺失/探测失败）→状态回退；日志注入未处理异常文本→健康及时撤销且原因可复核。
- **ACC-007**：运行中 `taskkill /F` MCP→Job 收敛受管树（无 fakenet 残留进程）、SCM 重启（restart/30000）、新进程 `recovering`→审计→`stopped`、不自动启动 FakeNet、旧 command_id 不续接。
- **ACC-008**：原子替换（同卷）、写失败（目录只读模拟→拒启）、损坏/缺失+残留→`failed`、交付物扫描无数据库/事件重放/迁移框架（wheelhouse 与 _internal 清单比对白名单）。
- **ACC-009**（P03 最终）：在 r6 候选上完整重跑 P02 的 ACC-009-PRE 全部子项（不引用 r5 证据）+ 真实 Windows 文件锁（运行期外部写/换名/删除被拒）、停止解锁、重解析点逃逸（建 junction/symlink 指向外部→拒绝）、FakeNet 读取兼容（锁不阻止服务进程读配置）。

## 6. 需求保真门禁（P03 范围）

- 应承接数：`15`（记录 001/003/004/005/006/007/011/014/018/019/021/022/023/025/026 全量）+ 记录 013/024/038 的 P03 子句（重解析点真机、受控退出入口、快照恢复语义）= `18`。
- 完整承接数：`18`；未落点数：`0`；语义弱化数：`0`；不可验收数：`0`。
- 核对：记录 001→OUT-001→IMP-03-02/06+ACC-001；003→OUT-004→IMP-03-01+ACC-004；004→OUT-006→IMP-03-02 health+ACC-006；005/006→排除注入+预校验+ACC-004 负样本；007（Linux TODO 不弱化 Windows 承诺）；011→初始 stopped+仅 AI 启动（IMP-03-06 启动路径）；014→IMP-03-05+ACC-009；018→Coordinator（P02 已交付， P03 真实运行复验）；019/020/021→P02 交付（Header 服务端语义在真实运行复验）；022→无计时器断言延续；023→restart 绑定 run_id+幂等（真实运行复验）；025→Job/基线/恢复标记→IMP-03-03/04/06+ACC-007/008；026→SCM 恢复路径+不重启循环→IMP-03-06+ACC-007；013 子句→重解析点；024 子句→受控退出入口；038 子句→快照语义。

## 7. 版本变更记录

- **v2 / 2026-09-04：** 独立核验回流(用户裁决): IMP-P03-01 扩充管理面额外排除端口(DEC-007)与环回排除(DEC-008)两条款; 对应总纲 v3 冻结表与需求记录 041/042。其余 IMP/ACC/冻结表未变。
- **v0 / 2026-09-03：** 首次编写。IMP-P03-01—08、ACC-001/004/006/007/008/009 可执行形式、§3 冻结表（进程内 Fakenet+自 Job 策略）、保真门禁 18 项。

## 8. 审核重点

- v3独立受管子进程是否在执行接管前加入主管持有的Job；主管自身不在Job内，故障不切断HTTP；全部受管后代归属与清理可证明。
- 主 filter 排除注入是否只经唯一构造点、无第二套捕获机制；无参数时字节级不变。
- 快照七字段与 FB-003/004 边界；恢复审计与 P04 的分工不重叠（P03=机制+单场景， P04=故障矩阵）。
- 受控退出入口是否恰好覆盖 §5.5 的 P03 份额（入口+拒绝新变更）， 未提前实现 P04 的查询保持/升级门槛验收。
- ACC-004 六场景在真机的可注入性与判定；ACC-008 禁框架扫描的白名单依据。
- 停止宽限默认值的权限归属（P04 冻结）未被本方案越权固定。
- **v1 / 2026-09-03：** 引入首次第三方审核（AUD-P03-001—010， 全部同意）。高危修正：排除注入覆盖 `build_egress_control_filter` 重建路径并对最终 filter 断言失败关闭（001）；健康时序测试义务落点（002）；故障注入手段冻结（003）；真实 `restart` 复验场景补入（004， 未落点数清零）；ACC-009 前置在 r6 全量重跑（005）；锁先于读顺序冻结（006）；IMP 拼写统一（007）；基线进程清单定义（008）；Job 未命名+句柄不继承（009）；check_filter 条件执行/IPv6 失败关闭/禁框架白名单依据/探针复用（010）。IMP/ACC 编号不变。

## 9. 2026-09-09 回流版本来源与资格

当前规范输入：同目录需求正式v3（含当日回流节）、总纲v5§16—17；§1旧blob和前序CHILD_DONE仅描述历史时点，不作为新候选通过证明。规范文件身份、逐项保真与自评见当日回流自评记录。原有非冲突条款继续有效，历史审核/验收不改。

### v3 P03实施与验收补充

- IMP-P03-02/03/06：内部入口拟落`fakenet/mcp/managed.py`，Job保持在主管；通信限制为本次run的固定领域消息，无任意命令/路径操作。IPC失联不能阻塞HTTP。活动配置锁由主管持有，先锁后读SHA，子进程只读；启动前快照写真实command_id、接受版本、controller_id及具体baseline文件。恢复先使live Coordinator进入recovering并提供HTTP诊断，再有界恢复；失败保留责任/禁启动，不把临时恢复视图结果丢失。
- IMP-P03-04/08：与P04使用同一完整基线。强杀树后仅恢复本次可归因变更；没有基线不能猜测修复。每次失败保留快照/incident；审计及锁释放证据完成才清标记/控制权。
- IMP-P03-08/ACC-004严格执行总纲v5§16—17四端口/非法值/双栈环回矩阵；记录配置SHA、过滤器、端点请求响应、PCAP路径/SHA及标记包定位。额外端口默认不启用，不得扩大到任意远端流量。
- P03→P04新增必要运行身份数据为直接运行观测/基线，不增加state.json七字段之外的通用持久状态；无命令恢复/事件重放。

### v3 必要审核引入细化

IMP-P03-02/06按总纲§16.1原子创建（Job→STARTUPINFOEX属性→CreateProcess，失败释放临时资源），不得保留先Create后Assign兼容路径。生命周期日志保存API阶段、PID/创建时间与Job观测；ACC-007包含每个创建关键窗口的主管强杀和原生无孤儿/无接管证据。

健康IPC按总纲§16.1固定run_id/递增seq、1秒主动请求/2秒周期、一次超时degraded/连续两次终止性failed；EOF、身份错、倒序重放、子进程退出立即failed，终止性后调用P04有界证据与Job清理/恢复审计。ACC-004/006必须逐项注入真实IPC故障，保存请求/响应序号、时刻、子进程身份和状态时间线；单次超时后只在新的三条件全部有效时恢复healthy。栈不可得不是无限等待理由，交P04条件转储和缺项失败清单。

## PLAN-CHANGE-005 当前子方案承接

IMP-P03-02/06交付draining、在途完成等待、真实受管身份、恢复责任与成功冻结接口；服务停服入口移为预停128，MCP领域stop原义不变。

当前共同规范为正式需求v3及总纲v5§17；本子方案逐条承接其完整参与者、输入输出、权限、触发顺序、硬期限、状态/恢复责任、失败重试、结束条件、原生证据及失效/重裁条件，不以本段摘要降低规范。必要复审通过前受影响实施仍暂停。


## PLAN-CHANGE-006回流（2026-09-10，待必要审核）

规范输入为当前需求v3和总纲v7§18；本节完整引用§18的采用理由、适用边界、硬时限、失效/重裁条件，不削弱记录034/035。旧版本身份只作历史追溯，不授予本次实施资格。

P03 IMP-P03-02/04负责总纲§18.3/6：原子Job创建后、首个start/接管前交付实际句柄确认的run/受管身份/命令行及有限采集截止；能力自检失败拒启。首ready前真实线程栈保持，取证关联文件不是状态源。退出立即failed但保持HTTP，经P04证据与恢复后才决定stopped；交接测试包含最早ready后S6与错身份/迟到通知。

P01→P02→P03→P04→P05顺序、既有REQ/OUT/ACC/IMP稳定ID不变。主会话保持作者/执行方，全部CHK开放；必要审核通过前本变更受影响产品实施暂停。

PC006-R01—R04修订承接：本Pxx依总纲v7§18完整执行自己的唯一责任。P01交付专用受管映像/不监测主管CLI和助手；P03保留真实目标句柄到有界P04完成并发布/失效一次性当前stop intent；P04只保存通知原始整数、不查询发起者，执行全局单飞1、拒绝≤1秒/零持久输出及临时512MiB、真实句柄确认和正常退出分类；P05验证这些跨层顺序、外部零码/旧intent/迟到/过早关句柄/同名突发负载与最终清理。完整路径过滤候选未通过，不作为fallback。状态仍为待必要复审，未获得新实施资格。


## PLAN-CHANGE-007：v7 当前负责契约（待必要审核）

当前来源为需求v4及总纲v8§19，优先于历史§18无条件硬结束解释；保留非冲突全部既有合同。

IMP-P03-02/04：在接管前固定run/实际目标PID/创建时间/镜像/主管实例和保留句柄，交P04一次采集责任；HTTP/状态锁不等待阻塞诊断。先failed再采集，任务未结束保持owner/needs_recovery并拒新接管；P04确认结束后才释放保留句柄及执行恢复审计，不凭Job空/None伪造归零。

采用理由是隔离Win32不可保证终止的在途I/O；仅有原生可归因证据的平台分支不再承诺同限全部归零，仍failed并保留恢复责任。普通产品故障60/180秒、活动512MiB、真实清理与同候选验收不减弱。输入、输出、触发顺序、状态与不变量按本负责合同及总纲§19逐条验收；无第二常驻服务/后台队列/旧任务重放。失效条件为入口/查询/普通故障结束/单飞预算不可证明，需回流重新裁决，不能扩大平台豁免。

007握手细化：完整承接总纲§19.1同一逻辑单飞责任及固定从属工作；不竞争自己已持锁，不授予第二份截止/额度；P01保证内部入口、P03先固定SPE句柄/Job再ACK、P04从属核验与整个责任清理、P05验证持槽握手/阻塞/迟到/Job归属失败的原生反例。

PC007-R01/R02修订承接：总纲§19.2仅固定16KiB以内协议握手/内存授权可并行，真正采集执行者始终≤1且移交唯一剩余额度；stop-intent/state/incident元数据不在主管锁内等待I/O。每个对象实际结束才释放本对象句柄，助手结束不替代目标结束；全对象结束及全审计通过后才解除整体责任。P05按§19.2.3验证同责任并发/峰值和助手结束但目标未结束反例。待同实例复审，不由作者关项。
