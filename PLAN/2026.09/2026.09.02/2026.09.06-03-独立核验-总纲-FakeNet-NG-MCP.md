# 独立核验记录 — 总纲 FakeNet-NG MCP（r58b 总体验收，第 5 轮）

> 创建日：2026-09-05（Asia/Shanghai）
>
> 角色：独立核验方（第 5 轮，隔离上下文）。与实施方无共享上下文；全部结论仅依据磁盘真实文件与 git 历史。
>
> 模式：只读核查。除本记录文件外未写入、未修改任何代码、文档或证据；未重置 VM、未启停产品服务、未重建 ZIP、未提交代码。
>
> 核查 HEAD：`cfbfbbf6843e549489f987b52e30801da59bc72f`（分支 `process-redirect-v23`）
>
> 被核验对象：`2026.09.06-01-总体验收-FakeNet-NG-MCP.md`（含 §7 的 CHK-030—033 闭合声明）及其引用的 r58b 最终证据树 `Logs/fakenetng-mcp/mcp-c94e7abdb-57b2ee8a7dde/`
>
> 结论：**核查不通过**（CHK-030 的 requirements_blob 侧未真正闭合——修复以残缺转写常量替换旧常量，28 份 result.json 的 `requirements_blob` 为非法 38 字符串，仍不等于需求文档真实 blob，验收记录 §1/§7 对应声明失实；另察明记录计数口径未随 r58b 更新。CHK-031/032/033 三条确认闭合）。
>
> CHK 编号续接前四轮（CHK-001…033，见 2026.09.04-01、2026.09.04-02、2026.09.05-05、2026.09.06-02）；本轮新开 CHK-034、CHK-035。
>
> 本记录封存后不补写；下一轮须另建记录。不是第三方方案审核，不产生、关闭或重计 AUD，不改变审核额度，不整改任何被核验对象。

## 1. 对象、身份和保护边界

| 对象 | 绝对路径（仓库根相对） | 本轮实测身份 |
| --- | --- | --- |
| 需求文档 | `PLAN/2026.09/2026.09.02/2026.09.02-01-需求提炼-FakeNet-NG-MCP.md` | `git hash-object` = `420e0288a027cc990c0d0b353aa3702ea64b67b9`（40 字符；与第 4 轮记录 §1 实测一致；最后一次变更 commit `d3334a1`，2026-09-05 11:00:56 +0800，早于 r58b 重跑） |
| 总纲 v3 | `PLAN/2026.09/2026.09.02/2026.09.02-02-总纲-FakeNet-NG-MCP.md` | `git hash-object` = `b7da9968da72a435b0fa53b680c137b37d754daa`（40 字符；与指派一致；版本行 v3；§12.2 恰 RACC-001—018 共 18 行） |
| 被核验验收记录 | `PLAN/2026.09/2026.09.02/2026.09.06-01-总体验收-FakeNet-NG-MCP.md` | 随 `cfbfbbf` 提交（§7 声明 CHK-030—033 已闭合） |
| 第 4 轮核验记录 | `PLAN/2026.09/2026.09.02/2026.09.06-02-独立核验-总纲-FakeNet-NG-MCP.md` | 已读，CHK-030—033 原始定义以此为准 |
| 最终候选证据树 E | `Logs/fakenetng-mcp/mcp-c94e7abdb-57b2ee8a7dde/` | 28 个含 result.json 的目录 + `release/`（153 文件）+ `pipeline.log`/`pipeline-b.log`/`seq-python.log`/`diag.log` |
| 发布清单 | `dist/mcp-c94e7abdb-57b2ee8a7dde-release/release-manifest.json` | 与 E/`release/release-acc-index.json` 程序化整文档比对 = True |
| 候选包 | `dist/mcp-r58/fakenetng-mcp-candidate.zip` | 实测 `sha256sum` = `57b2ee8a7ddea36076883241f9653999168a267d98c72ccf03d6509482d31450`；`candidate-id.txt` = `mcp-c94e7abdb-57b2ee8a7dde` |
| r58b 重跑脚本 | `Logs/fakenetng-mcp/r58b-seq.py`、`Logs/fakenetng-mcp/r58b-pipeline.sh` | 两文件第 9—10 行携带 blob 常量（master 常量正确，requirements 常量残缺，见 §3 CHK-034） |

D = 上述 PLAN 目录。核查期间 HEAD 未移动；未触碰任何被核验对象。现场记录（非 CHK）：工作区存在与 MCP 验收无关的未提交修改（`fakenet/gui/*`、`fakenet/diverters/*`、若干 GUI 测试与 30 个 2026.08 旧 PLAN 文件的未提交删除、untracked `fakenet/gui/logview.py` 等）——均未提交、不进入候选（候选 ZIP SHA 实测不变佐证），按 AGENTS.md 属应保留的无关工作，不计入本轮问题；`git diff --stat 94e7abdb..HEAD -- fakenet/` 为空（见 §2.7）。

## 2. 逐条核查结果

### 2.1 对象身份（全量重查要点 1）— PASS

| 项 | 证据 | 结果 |
| --- | --- | --- |
| 需求/总纲 blob | `git hash-object`（§1 表） | 总纲 `b7da9968…` 与指派逐字符一致；需求 `420e0288…`（完整 40 字符值见 §1）与第 4 轮实测一致 |
| 源 commit | `git merge-base --is-ancestor 94e7abdb141417f225a0e64e126232b8eaa76f4b HEAD` 通过 | 存在且为 HEAD 祖先 |
| 候选 id / 包 SHA | `dist/mcp-r58/candidate-id.txt`；`sha256sum dist/mcp-r58/fakenetng-mcp-candidate.zip` | `mcp-c94e7abdb-57b2ee8a7dde`；`57b2ee8a…31450`（与指派、manifest、28 份 result.json 的 `package_sha256` 四方一致） |
| candidate-verification | `dist/mcp-r58/candidate-verification.json` | `zip_sha256` 同值、`verified_files:163`、`manifest_match:true`、`size_hash_match:true`、`verdict:PASS` |
| 发布清单三字段 + os | `release-manifest.json` 行 3—6 | `candidate_id=mcp-c94e7abdb-57b2ee8a7dde`、`source_commit=94e7abdb1414…`、`package_sha256=57b2ee8a…31450`、`os_version=10.0.19045.0` |

### 2.2 CHK-030 闭合核验 — **OPEN（requirements_blob 侧）**

程序化遍历 E 下全部 28 份 result.json（`python3` 逐一解析）：

| 子项 | 实测 | 判定 |
| --- | --- | --- |
| `master_plan_blob` | 28/28 全 `b7da9968da72a435b0fa53b680c137b37d754daa` = 总纲 v3 实测真实 blob | 一致，总纲侧闭合 |
| `requirements_blob` | 28/28 全 `420e0288a027cc990d0b353aa3702ea64b67b9`——**38 字符，非合法 SHA-1**；需求文档真实 blob 为 `420e0288a027cc990c0d0b353aa3702ea64b67b9`（40 字符） | **不一致，requirements 侧未闭合（CHK-034）** |

字符级程序复核（`git hash-object` 子进程独立取证）：两串首个分歧在索引 17，真实值第 17—18 位 `"c0"` 在记录值中缺失；`len(记录值)=38`，`len(真实值)=40`。溯源：

- 根因：`Logs/fakenetng-mcp/r58b-seq.py:9` 与 `Logs/fakenetng-mcp/r58b-pipeline.sh:9` 均硬编码 `--requirements-blob 420e0288a027cc990d0b353aa3702ea64b67b9`（38 字符残缺转写）；c87ef0e 将 runner 的 blob 从硬编码改为参数传入后，调用侧常量本身写错。
- 该 38 字符串无任何合法来源：需求文档全部历史版本 blob（`0b35056`→`b91ded55…`、`75117df`→`80b15fe5…`、`d3334a1`→`420e0288…`40 字符）无一匹配；两文档内部自引的 blob（需求文档第 27/364/378/460/514 行、总纲第 21 行）也无此值。
- 验收记录声明失实：§1 第 20 行“需求 blob `420e0288…`｜27 份 result.json 的 `requirements_blob` 一致”（缩写声明，实质即与当前需求 blob 绑定）与 §7 第 90 行“28 份 result.json 两 blob 全部为当前值”——对 `requirements_blob` 均不成立（“当前值”只能是磁盘文档的真实 git blob，唯一权威即 `git hash-object`，第 4 轮记录 §1 亦如此实测）。
- 缓解事实（与第 4 轮同）：v2→v3 文档差异不触及总纲 §9.2/§12.2 验收合同行；本轮 28 份 result.json 的 requirements_blob 互相一致（同一残缺值），未产生证据树内部互相矛盾的 blob。

### 2.3 CHK-031 闭合核验 — CLOSED

- 28/28 份 result.json 的 `environment_identity` 均含 `DESKTOP-3FI41GR`，两种格式恰与 runner 家族对应：
  - 24 份（p02—p05 runner）：`win10h2-MalBox-20241110@DESKTOP-3FI41GR | config=listen=192.168.204.149:28788`（ACC-001、ACC-004-S1…S6、ACC-005…ACC-011、ACC-012、ACC-013、ACC-014、ACC-015、ACC-017、ACC-017-SUMMARY、ACC-018、ACC-019、FAULT-POINTS）；
  - 4 份（p01 runner）：`win10h2-MalBox-20241110 | DESKTOP-3FI41GR | config=listen=192.168.204.149:28788`（ACC-002、ACC-003、ACC-016、P01-ENTRY），且各自 `actions` 仍保留 `vm-identity: DESKTOP-3FI41GR @ win10h2-MalBox-20241110` 原始实证。
- 代码证据：`c87ef0e` diff——`run_p01_acc.py` 增加 `guest_identity = identity` 回填；`run_p02_acc.py`/`run_p03_acc.py`/`run_p04_acc.py`/`run_p05_release.py` 的 `environment_identity` 改为嵌入探测到的计算机名。实机名已成为 result.json 一级字段，不再依赖 action 日志推断。

### 2.4 CHK-032 闭合核验 — CLOSED

`E/ACC-004-S1…S6/` 六目录各有文件名含 probe 的 json 时间线证据，内容均为 `[{t: <unix 时间戳>, ok: <bool>}]` 数组、全部样本 `ok=true`：

| 场景 | 证据文件（相对 E/ACC-004-S*） | 样本数 | 跨度 | 全 ok |
| --- | --- | --- | --- | --- |
| S1 正常接管 | `acc004-s1-takeover-probe.json` | 24 | 11.7s | true |
| S2 初始化失败 | `acc004-s2-init-probe.json` | 8 | 3.5s | true |
| S3 未处理异常 | `acc004-s3-exception-probe.json` | 8 | 3.6s | true |
| S4 无效防护参数 | `acc004-s4-survival-probe.json`（另有 `acc004-s4-invalid-filter-start.json` 存 error 响应） | 8 | 3.6s | true |
| S5 核心线程卡死 | `acc004-s5-hang-probe.json`（另有 `acc004-s5-hang-stop.json` state=stopped） | 16 | 7.6s | true |
| S6 非受控退出 | `acc004-s6-uncontrolled-exit-probe.json`（另有 `acc004-s6-uncontrolled-restart.json` 恢复后快照） | 8 | 3.5s | true |

六文件 mtime 均为 2026-09-06 04:35:58—04:38:26（c87ef0e 提交 04:23:29 之后的新鲜产物），与各 result.json `started_at`（UTC 20:35:41—20:37:43，即北京 04:35—04:37）吻合。S2/S4/S6 的补测对应 c87ef0e 中 `run_p03_acc.py` 的三处 `continuous_probe`+`add_evidence` 新增。

### 2.5 CHK-033 闭合核验 — CLOSED

- 验收记录 §3 尾行现为：“**18/18 RACC 全部通过， 另有 ACC-019（源自记录 024/REQ-006 回流， 非需求 RACC 编号）一并通过——共 19 项 ACC 合同**”；全文 `grep -n "19/19"` 零命中，“19/19 RACC”表述已消除。
- 交叉验证：需求文档 grep `RACC-0xx` 去重恰为 RACC-001—018（18 个）；总纲 §12.2 映射表恰 18 行 RACC-001—018；§3 的 19 行 ACC 映射与证据树 27 条 ACC（含 ACC-004 拆分与 ACC-019）一致。

### 2.6 ACC 覆盖与发布矩阵（全量重查要点 2、3）— PASS（blob 除外，见 §2.2）

- 28/28 份 result.json `status=pass`、`candidate_id=mcp-c94e7abdb-57b2ee8a7dde`、`package_sha256=57b2ee8a…31450`；递归全树无 `status≠pass` 的 result.json 残留。
- ACC-004-S3 抽查（要点 2 指定）：`E/ACC-004-S3/acc004-s3-checks.json` = `{"start_ok": true, "unhandled_exception_revokes_health": true, "link_alive_during_exception": true}`——“未处理异常撤销健康 + 链路存活”覆盖在位；result.json `acc_id=ACC-004-S3`、`p_id=P03`。
- 六场景 `acc_id` 逐份 = 目录名（ACC-004-S1…S6），`p_id` 全 P03，checks 语义与总纲 §9.2 第 312 行六场景逐一对应。
- 发布矩阵程序化全量核验（150 份轮记录 + 2 份 summary + final-checks + acc-index）：
  - `release/normal-builtin-001…050` + `normal-custom-001…050` 共 100 份：零 `failure` 键、`probe.all_ok=true`、`stop_state=final_state=stopped`、`lock_held_during_run=true`、`lock_released_after_stop=true`、`audit_diff` 空——零异常。
  - `fault-{policy_pause,listener_stop,diverter_stop,child_hang,cleanup_error}-001…010` 共 50 份：零 `failure` 键、`probe.all_ok=true`、`audit_diff` 空；终态分布 listener_stop/diverter_stop/child_hang/cleanup_error 各 10×`stopped`、policy_pause 10×`failed`+`failure_reason="stop grace exceeded"`——与既定 fail-closed 收敛判据逐字一致。
  - `release/normal-summary.json` = `{"builtin":50,"custom":50}`，与 `ACC-012/normal-summary.json` 相同；`release/fault-summary.json` 五类各 10，与 `ACC-013/fault-summary.json` 相同。
  - `ACC-017/final-checks.json`：`identity/load_ok/start_ok/healthy/link_during_run/stopped/audit_clean` 全 true，`tested_windows_version=10.0.19045.0`；`vm: boot=2026-08-31T08:54:50` 与全部轮级记录 `vm` 字段一致（r58b 全程同一 VM boot 会话）。
  - `release-manifest.json`：`missing=[]`、`failures={}`、`wrong_candidate=[]`、`record_integrity_failures={}` 全空；`acc_index` 实测 **28 条**全 `pass` 且 candidate_id 一致，键集合与 E 的 28 个 result.json 目录**一一对应（程序化比对 = True）**。指派口径“27 条”与第 4 轮实测 27 条系 r58 首跑时状态；r58b 的 summary 阶段（`release/release-acc-index.json` mtime 04:53:23）把 ACC-017-SUMMARY 也登记入 acc_index——该多出条目本身 pass 且 candidate 一致，不产生错误 pass，但验收记录 §4 仍写“27 条 acc_index”，计数口径未更新（CHK-035）。

### 2.7 r58b 重跑正当性与修复链（全量重查要点 4、5）— PASS

- 修复链 8 commit 全在 `process-redirect-v23` 历史：`fd29254`/`97f8346`/`038bf76`/`628cdee`/`177fee9157f1fa858eeaee5cefc44424e819b991`/`cdb377b`/`94e7abdb`/`c87ef0e`（`git log --oneline` 逐一定位）。
- `c87ef0e` diff 抽查（要点 4）：仅触碰 `test/mcp/acceptance/` 下 5 个 runner 文件——`run_p01_acc.py`(+1 行 guest_identity 回填)、`run_p02/p03/p04_acc.py`+`run_p05_release.py`（environment_identity 嵌入探测名）、`run_p03_acc.py`（S2/S4/S6 三处 continuous_probe+add_evidence）。确为“blob 参数化 + 身份回填 + 时间线”，**未触碰 `fakenet/` 产品代码**。
- 候选二进制未变：`dist/mcp-r58/fakenetng-mcp-candidate.zip` 实测 SHA-256 与指派、`candidate-verification.json`、`release-manifest.json` 四方一致；`git diff --stat 94e7abdb141417f225a0e64e126232b8eaa76f4b..HEAD -- fakenet/` 输出为空——94e7abdb 之后 4 个 commit（`5147a68` runner 诊断、`6f5f170`/`cfbfbbf` 仅 PLAN 文档、`c87ef0e` 仅 acceptance runner）无一修改产品代码。无“产品代码被改而候选未重建”情形。
- r58b 执行时间线（`pipeline-b.log` + 文件 mtime + result.json `started_at` 三方吻合）：
  - 04:23:29 第一次尝试 STAGE 1：ACC-016 exit=3 tool-error，序列即止（10 分钟窗口后重试，最终产物全 pass，无残留 tool-error result.json）；
  - 04:33:36—04:52:50 最终段：ACC-016→FAULT-POINTS 共 27 项 ACC 顺序全 pass——**28 份 result.json 的 `started_at` 全部落在最终段（UTC 20:33:37—20:53:23），一致代际，无新旧混合**；
  - 04:52:50—04:53:24 release 四模式：normal/fault 各约 1 秒（断点续跑，保留首段轮级记录）、final 30 秒、summary 2 秒。
  - 验收记录 §7“normal/fault 轮记录为断点续跑（同候选同 VM 同配置， 轮级记录不含 blob 字段， ACC-012/013/017 的 result.json 以正确参数重写）”与实测吻合：轮级记录字段集（`audit_diff/config/ended_at/final_state/lock_*/probe/round/started_at/stop_state/vm`）确无 blob/identity 字段；轮级记录生成于 01:46—04:03（normal 100 轮 01:46—02:59、fault 50 轮 03:57—04:03，均 c87ef0e 之前，但 c87ef0e 不改轮级逻辑）；ACC-012/013/017/017-SUMMARY 四份 result.json `started_at` 为 04:52:50—04:53:23（重写）；全程 `vm: boot=2026-08-31T08:54:50` 同一 VM boot 会话。
- 无关工作现场（工作区未提交 GUI 修改）未进入任何 commit/候选，见 §1。

### 2.8 一致性陷阱排查（全量重查要点 6）— PASS

- E 内无 `status≠pass` result.json 残留（递归）；无 attic 目录（r52 `attic-serial-acc004` 仍在旧候选目录 `mcp-cc9cdea21-…` 下，按指派边界排除）。
- 旧 blob 零残留：`grep -rl "b91ded55\|e5c734ea" E/` 零命中——r58 首跑的 v2 时代 blob 记录已被 r58b 完全覆盖，无同标签多版本矛盾。
- 跨目录污染：`grep -rl mcp-c94e7abdb-57b2ee8a7dde Logs/fakenetng-mcp/ --include=result.json` 除 E 自身外零命中。
- 证据完整性全量复核：28 份 result.json 登记的 **106 个证据条目**逐一经 `sha256`+`size` 复算，全部吻合（超出第 4 轮 6 目录/38 文件抽查范围）。
- 无矛盾多版本同标签证据：六场景 probe/checks、release summary 副本（ACC-012/013 与 release/ 逐字段相同）、`release-acc-index.json` 与 dist 发布清单整文档相同。

## 3. CHK 问题列表（本轮 CHK-034—035；CHK-030 判 OPEN，031/032/033 判 CLOSED）

- **CHK-030 — OPEN（阻断本轮通过）**：修复未真正闭合 requirements 侧。28 份 result.json 的 `requirements_blob` 全部为 `420e0288a027cc990d0b353aa3702ea64b67b9`（38 字符，非法 SHA-1），仍不等于需求文档真实 blob `420e0288a027cc990c0d0b353aa3702ea64b67b9`（40 字符；字符级复核：索引 17 起真实值 `"c0"` 缺失）。master_plan_blob 侧（`b7da9968…`）已正确闭合。
- **CHK-034（阻断本轮通过；CHK-030 的根因条目）**：`Logs/fakenetng-mcp/r58b-seq.py:9` 与 `r58b-pipeline.sh:9` 将 `--requirements-blob` 常量写为残缺转写值（38 字符），经 c87ef0e 参数化后的 runner 原样落入 28 份 result.json；该值不等于需求文档任何历史版本 blob，亦无任何文档自引来源。验收记录 §1 第 20 行与 §7 第 90 行“（两）blob 全部为当前值/一致”的声明与证据相悖。整改方向：以 `git hash-object` 重新取值重跑（或最低限度重写 result.json 该字段）并同步修正验收记录声明。
- **CHK-035（低危，记录口径）**：验收记录计数口径未随 r58b 更新——§1 第 19—20 行仍写“27 份 result.json”（实测 28 份，含 ACC-017-SUMMARY）；§4 仍写“release manifest 27 条 acc_index 全 pass”（实测 28 条，r58b summary 阶段已将 ACC-017-SUMMARY 登记入 acc_index，与 E 的 28 目录一一对应且全 pass）。低报不产生错误 pass，但属记录与证据不一致的口径残留。

## 4. 结论与封存

对象身份（blob/候选/SHA/manifest/os）、ACC 覆盖（28 份全 pass、S3 抽查、六场景 acc_id）、发布矩阵（100+50 轮零异常、summary 数值、final-checks、acc_index 全 pass 且四空字段为空）、修复链与 c87ef0e diff 保真、r58b 重跑正当性（候选二进制未变、94e7abdb 后零产品代码改动、断点续跑声明与实测吻合）、一致性排查（无残留/无污染/106 证据哈希全吻合）全部通过。CHK-031/032/033 三条经逐项实证确认闭合。但 CHK-030 的核心诉求——result.json 与被核验需求文档的真实 git blob 绑定——仍未成立：修复把 v2 时代旧常量换成了一个残缺转写的 38 字符串，验收记录 §1/§7 的对应声明按其字面仍为不实陈述。故本轮只能裁定：

**核查不通过。**

本记录封存后不补写；下一轮须另建记录。不是第三方方案审核，不产生、关闭或重计 AUD，不改变审核额度，不整改任何被核验对象。
