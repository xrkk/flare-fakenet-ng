# 交接提示词（复制到新会话）

/goal 完整修复独立核验发现的所有CHK问题并完成FakeNet-NG MCP验收

## 运行参数
- 运行平台: GLM
- 审核子智能体: sub-plan-reviewer
- 实施执行方: 主会话
- 状态目录: `PLAN/2026.09/2026.09.02/2026.09.02-02-总纲-FakeNet-NG-MCP-长程执行/`

## 精确状态（2026-09-05 22:00 检查点）

### 当前候选
**r52 = `mcp-cc9cdea21-034f71f9916d`**（源 `c9cdea21b6ada26f0231fec50ad8ed2873f726ec`，ZIP SHA `034f71f9916d12c8a9cf9090fc4baaeaeefd2d4521a9a5147ef5f060f59db337`），已部署在 VM（Snapshot 185，DEC-009 用户裁决）。

### 已通过的 ACC（9/19，在 r52 上稳定通过）
ACC-001, ACC-002, ACC-003, ACC-005, ACC-009-PRE, ACC-010, ACC-011, ACC-016, P01-ENTRY

### 阻塞点
**ACC-004** 因多场景串联（6 场景 × 服务重启）反复超时。需拆分为独立子测试。

### 产品修复（已提交，代码稳固）
- CHK-015: `perform_startup_recovery` 缺基线时 fail-closed（`supervisor.py`）
- CHK-016: `supervisor.restart` 停止失败时拒绝启动（`supervisor.py`）
- CHK-017: 宽限超限停止保持恢复标记（`supervisor.py`）
- CHK-018: `_run` 哨兵字符串 + TCP-only listen_ports 归一化（`baseline.py`）
- CHK-022: 协调器三阶段锁——execute 期间释放锁允许查询/立即拒绝（`coordination.py`）
- CHK-029: `_register_run_artifacts` 接线到 supervisor.stop（`supervisor.py` + `artifacts.py`）

### Runner 修复（已提交）
- CHK-014: 无效 filter 用 `not-an-ip`
- CHK-019: 探活失败导致轮次失败
- CHK-020: manifest 校验读 `$m.entries`
- CHK-021: drain 后发送变异
- CHK-024: 安装路径 `C:\Progra~1\FakeNet-NG-MCP`（DOS 8.3 短路径避免空格）
- CHK-027: 防火墙动态轮次必选

### 剩余工作
1. **拆分 ACC-004** 为独立子测试（每个场景一个 ACC 调用或一次只跑一个场景）
2. 跑剩余 ACC: 004(拆分后), 006, 007, 008, 009, 014, 015, 018, 019, FAULT-POINTS
3. 跑发布矩阵: normal(100轮) → fault(50轮) → final → summary
4. 重写总体验收记录（按总纲 §9 语义）
5. 发起新一轮独立核验

### 关键环境信息
- VM: `win10h2-MalBox-20241110`，快照 `Snapshot 185-Velociraptor-MCP依赖与情景基线`（DEC-009 用户裁决）
- VM 身份: `DESKTOP-3FI41GR`，Win10VM MCP 在 `192.168.204.149:28787`
- MCP 服务: `192.168.204.149:28788`
- 规范 blob: 需求 `420e0288…`，总纲 v3 `b7da9968…`
- 验收标准排除集: `{28787, 28790}`（DEC-007）
- runner venv: `.venv-mcp-runners/`（`pip install mcp anyio`）
- VM 直连通道: `/tmp/vmps.py`（使用 helpers.Win10VmChannel）

### 重要注意事项
- 服务可能在 failed 状态——每次 ACC 运行前先检查服务状态，必要时重启
- 服务重启需等 `StopPending` → `Stopped` 后再 `sc.exe start`
- ACC-011 混合生命周期测试后需重启服务（已在 pipeline 脚本中处理）
- `restart_service()` 已修正为等待完全停止
- Git 分支: `process-redirect-v23`，最后提交 `62ce880`

### 独立核验文件位置
- 第1轮: `PLAN/2026.09/2026.09.02/2026.09.04-02-独立核验-总纲-FakeNet-NG-MCP.md`（CHK-001—012）
- 第2轮: 派生的 CHK-R2-001—010（已修复）
- 第3轮: `PLAN/2026.09/2026.09.02/2026.09.05-05-独立核验-总纲-FakeNet-NG-MCP.md`（CHK-013—029）
