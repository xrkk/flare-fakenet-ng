# 总纲三方审核记录 — FakeNet-NG MCP

> 审核性质：首次第三方审核（同目录无任何历史 `审核`/`复审` 记录，强制全面审核；按首次审核门禁未读取作者自评记录）
>
> 审核对象：`/home/adminn/projects/flare-fakenet-ng/PLAN/2026.09/2026.09.02/2026.09.02-02-总纲-FakeNet-NG-MCP.md`
>
> 总纲版本：`v1`；状态：`未审核, 不得实施`（状态门禁通过）
>
> 总纲 Git blob：`66d0198fdd4b5c1d38b6611a032e27514f0c21c3`；SHA-256：`198afdb6664183035266a2d9cbb33a517a1fdbd18a58aee8b6576e87d2ebd62b`
>
> 来源切片：
> - 需求提炼文档 `2026.09.02-01-需求提炼-FakeNet-NG-MCP.md`，Git blob `b91ded55e75b96e60f66c8d58125d8a220d62a56`（与总纲 §1.1 声明一致），SHA-256 `83a7f6635786fd694ec95b9cba46723d60f269ac579397ecdc5172d11b5c4f46`
> - 切片内容核对一致：`累计有效结论` 1—38、`正式需求 v1` REQ-001—REQ-016、CON-001—CON-008、NON-001—NON-010、RACC-001—RACC-018（52 个稳定 ID）、记录 040 归属修正
> - 方案组织形式最终输出：`方案组织形式第二轮独立复评（v1 当前）`，输入 blob `8429d40f6121854feec6571283ba28c2901740a3`（与总纲 §1.1 声明一致）；最终组织形式总纲 + P01→P05、无独立 Spike 均与该节一致
>
> 历史审计/处理记录：无（本目录仅存在 01-需求提炼、02-总纲、03-自评三份文档）
>
> 本地审核基线 HEAD：`4ac9abf28d4ec3527f93539345dcd7343ca48ea0`（2026-09-01 18:06:46 +0800）
>
> 边界内工作区状态（未提交改动，均不阻碍本次审核）：`PLAN/FaknetNG.md`、`fakenet/configs/default.ini`、`fakenet/diverters/egresspolicy.py`、`fakenet/diverters/windows.py`、`fakenet/gui/{app,procview,schema,validator}.py`、`test/test_{dns_selection,egresspolicy,gui_procview,gui_smoke,gui_validator}.py` 已修改；`PLAN/2026.08/`、`PLAN/2026.09/`、`fakenet/gui/logview.py`、`research/`、`test/test_gui_logview.py` 未跟踪
>
> 审核模式：全面审核（机械扫描 + 全部评审项语义审查 + 反例检查）
>
> 决策关键路径（SHA-256）：
> - `AGENTS.md`：`c84a7533dc7f96bc5a759410bb9b2f6160a8bf26c22ade35090b6e6383a4a426`
> - `fakenet/fakenet.py`：`c1197059d662f7f8210aa9a3983621bc0129daae38c530c854d2a7672d5ea974`
> - `fakenet/diverters/windows.py`：`bca29b6f05ca8ceb5f0bde3ecfaebab07e037da18203c501beba8e0d8d0f1131`
> - `Build-GuiVmPackage.sh`：`68dc5b4102392d8dce846fc5f48fcc6c40f729ae0b1162e207e7d33744168491`
> - `Build-GuiVmPackage.ps1`：`9cc76b2a9c3374af8133dcb8f87394500b2d3cbc431077a651cb9cc15031ec62`
> - `tools/build_gui_vm_package_wine.py`：`fe176e83b313df0e4c68d264486d6fb456c997cbfa0750c4b5bdad2ebcdfeeef`
> - `test/test_fakenet_stop_lifecycle.py`：`7e4ca7e067532084fc5db9103d71ac1246b265931329aebc047d14b562b9938f`
> - `test/gui_vm/run_formal_stop_acceptance.py`：`af300758a679bceb013366637ab530d46fedeacb106c2103427c6c19fb1f7175`
> - `setup.py`：`4ba1bac09dba8a4030fc5f8a9d4c32e5b0d7db7dc784c750d29c46934f389bb7`
>
> 上下文路径（仅辅助理解，不作为意见证据）：`dist/`（含 v34-r9、v35-r10—r12 产物目录）、`PLAN/2026.09/2026.09.01/`、git 历史、`fakenet/gui/` 现状
>
> 排除范围：作者自评文档（首次审核门禁不读取）、`fakenet/gui/` 功能改造细节、listeners/、`research/`、历史 PLAN 方案、真实 VM/Docker 实际运行验证、需求重新访谈或重新拆分
>
> 轮次：第 1 轮
>
> 结论：`存在不同意见`

## 1. 审核执行摘要

来源身份、总纲状态、组织形式、代码事实与追溯结构经独立重建后总体成立：52 个稳定来源 ID 全部有唯一主 OUT/ACC/Pxx 落点，正向与反向追溯矩阵机械一致，P01—P05 与 `方案组织形式第二轮独立复评（v1 当前）` 的顺序、职责、入口、验收归属逐项一致，验收基础设施（runner 退出码、`result.json`、`Logs/fakenetng-mcp/<candidate-id>/`）符合 `AGENTS.md` 输出目录约定且未扩张产品范围。

但独立需求保真核对发现 3 条结论存在总纲无落点、亦无任何 ACC 可观察的语义子句（配置变更审计记录、正常服务停止/升级先收敛 FakeNet 的触发路径），总纲 §13 声称的 `语义弱化数：0` 不成立；另有一处 §10 代码事实描述错误（构建入口第三参数并非 unused）。语义弱化数非零，按门禁不得审计通过。

## 2. 逐项覆盖清单

| # | 评审项 | 结果 | 说明 |
| --- | --- | --- | --- |
| 1 | 身份与状态门禁（路径/版本/blob/状态） | 通过 | 总纲 blob `66d0198f...`；需求文档 blob `b91ded55...` 与 §1.1 声明一致；状态 `未审核, 不得实施` |
| 2 | 来源切片与组织形式一致性 | 通过 | 38 结论/16 REQ/8 CON/10 NON/18 RACC 与文档实际一致；第二轮复评输入 blob `8429d40f...` 一致；P01→P05 固定顺序与 `方案组织结果` 一致；无独立 Spike、探针归 P01 首要实施与验收一致 |
| 3 | 来源边界（只用已确认需求，不重新拆分/绑定阶段编号） | 通过 | 未重新访谈；评审基于正式需求 v1 及其来源记录 |
| 4 | 需求保真独立核对（38 条逐项恢复检查） | 不通过 | 完整 35 / 语义弱化 3（结论 23、30、31，见 §3 与 AUD-001、AUD-002）；未落点 0；不可验收 0 |
| 5 | 稳定 ID 与双向追溯（§12） | 通过 | 52 项正向映射与 18 项 OUT 反向来源逐条核对一致；无遗漏、复用、歧义 ID；每来源 ID 唯一主 OUT/ACC/Pxx |
| 6 | 版本记录与可交接性 | 通过 | v0→v1 变更记录明确（v1 为审核前自评修正，未改 OUT/ACC/Pxx 编号）；§8 交接条目齐备 |
| 7 | 关键决定与裁决权限（§4） | 通过 | 冻结项均标注来源与分类；Docker/Wine 正确归类为实施环境约束非产品决定；优雅停止上限交 P04、健康轮询交 P03 冻结且禁止无限等待/虚假健康，边界正确 |
| 8 | 小模块边界 / 最小持久化 / 验收归因边界（§2） | 通过 | 新代码仅 `fakenet/mcp/`；直接共享接口与实际代码相符（`Fakenet.start()/stop()` 于 `fakenet/fakenet.py:225,439`；主 filter 构造与 `WinDivert.check_filter` 打开前校验于 `fakenet/diverters/windows.py:511,586,958,1026-1035`）；无数据库/事务/重放/迁移；§9.1 证据只覆盖负责 Pxx 及直接依赖 |
| 9 | 总体架构、不变量、生命周期与数据流（§3/§5） | 部分不通过 | 六逻辑职责、强制不变量、启动/恢复/显式启动/运行期/停止流程与需求一致；但 MCP 可处理的正常服务停止/升级触发路径缺失（AUD-002） |
| 10 | 接口与最小数据合同（§6） | 部分不通过 | 能力合同、配置路径/原子替换/内置只读/活动锁、`state.json` 字段与 REQ-007 七字段一致；配置变更审计记录子句无落点（AUD-001） |
| 11 | OUT 覆盖（§7，18 项） | 通过 | OUT-001—OUT-018 与来源语义对应；OUT-014 明确"完整承接 REQ-012"避免证据清单弱化；OUT-012/013 机制归 P04、发布计数归 P05 与组织评估修订一致 |
| 12 | Pxx 划分/顺序/入口/独立性/冲突（§8） | 通过 | 与组织评估两轮修订逐项一致（RACC-009 真实锁归 P03、RACC-017 最终归 P05、唯一 filter 归 P03、不可变候选身份归 P05）；无后序依赖、职责重叠或悬空 |
| 13 | ACC 合同（§9，18 项动作/证据/边界） | 部分不通过 | 18 项均有负责 Pxx、前置、动作、证据与通过/失败/阻断边界；ACC-002 实际目标客户端门禁、ACC-014 缺项即失败且不阻塞清理的 v1 强化正确；但 ACC-007 仅覆盖强制结束（异常退出）路径，正常服务停止路径无 ACC 可观察（AUD-002 关联） |
| 14 | 现有稳定操作与证据位置代码事实（§10） | 部分不通过 | `git hash-object`、`docker image inspect`、`--image-only`、VM 重置流程、`xvfb-run ... pytest` 命令形状与 `AGENTS.md` 一致；`Build-GuiVmPackage.sh` 存在且实际构建 v35（脚本 echo "Building formal v35"，`dist/` 存在 v35-r10—r12），但第三参数"unused"描述与代码不符（AUD-003） |
| 15 | Fallback 完整性（§11，FB-001—FB-014） | 通过 | 14 项均有依据/触发/行为/退出条件；FB-014 覆盖 CON-003 信任边界变化的重新裁决；FB-013 禁止退回源 IP/模型记忆与记录 021 一致 |
| 16 | 无依据过度设计 | 通过 | 验收 runner 合同与 `result.json` 为有依据的验收基础设施且明确标注非产品行为；无第二套捕获/持久化/看门狗机制；未超出最近一次通过的 GrillMe 交接（阶段 8 PASSED、结束评估） |

## 3. 需求保真独立核对结果

- 应承接数：`38`（独立清点 `累计有效结论`，与文档声明一致）
- 完整承接数：`35`
- 未落点数：`0`
- 语义弱化数：`3` — 结论 23（记录 024）、结论 30（记录 032）、结论 31（记录 033）
- 不可验收数：`0`

弱化明细（其余 35 条的参与者、输入输出、触发/顺序、状态/生命周期、不变量、异常/边界/结束、适用前提/重新裁决与可观察验收均可在总纲及稳定引用中完整恢复，包括逐项核对过的结论 1—22、24—29 中 REQ-003 链路保护参数语义、REQ-005 六状态、REQ-007 七字段快照、REQ-009/010/021 Header 与幂等、REQ-011 停止审计与 100/10 计数、REQ-012 证据清单与有界采集、REQ-013/014/015/016 及 CON-001—008、NON-001—010）：

1. **结论 23（记录 024）：** "收到可处理的正常停止、重启、升级或服务控制信号时，必须拒绝新的状态变更、保持查询能力，并先按记录 015 的完整停止、必要强制收敛、补偿恢复和环境审计流程结束 FakeNet-NG。只有环境审计全部通过并进入真实 stopped 后，MCP 才能退出"。正式 REQ-006 含该分支（"正常服务停止/升级必须先停止 FakeNet-NG并恢复审计环境"），但总纲 OUT-007 只述异常退出（Job 收敛 + SCM 重启），§5.1—§5.4 无服务控制信号触发的收敛分支，ACC-007 动作仅为"强制结束 MCP"。该触发路径在总纲文本与全部 ACC 中均不可观察。
2. **结论 30（记录 032）：** 控制者维护非活动配置"变更必须串行，携带版本或预期 SHA-256 与 command_id，并记录控制者、时间、前后哈希及结果"。串行/版本/命令身份已落点（§6.1、§6.2、OUT-011），但"记录控制者、时间、前后哈希及结果"的审计子句在总纲、正式 REQ-008/REQ-010 文本及全部 ACC 中均无落点。
3. **结论 31（记录 033）：** 停止态变更"并审计身份、命令及前后版本/哈希"。同上，原子替换/不可覆盖/冲突拒绝已落点（§6.2、ACC-009），审计记录子句无落点。

因总纲 §1.2 明文规定"REQ、CON、NON、RACC及其来源记录是产品规范"，上述来源记录子句属于规范输入；其丢失使总纲 §13 声称的 `语义弱化数：0` 不成立。

## 4. 审计意见

### AUD-001｜配置变更审计记录子句无任何落点

- 首次轮次：第 1 轮
- 来源层级：需求层（累计有效结论 30/31 = 记录 032/033，映射 REQ-008/REQ-009/REQ-010；正式化文本亦遗漏该子句）
- 反例：P02 按 §6.2/OUT-009 完整实现并全部通过 ACC-005/009/010/011，产品期间不记录任何配置变更的控制者、时间、前后哈希与结果——无任何 ACC 判失败，记录 032/033 的审计子句被静默丢失，冲突取证与变更归因落空。
- 证据：需求文档记录 032"并记录控制者、时间、前后哈希及结果"、记录 033"并审计身份、命令及前后版本/哈希"；总纲 §6.1/§6.2/OUT-009/ACC-009/P02 范围与产出均无对应要求；正式需求 v1 REQ-008/REQ-010 文本亦未含该子句。
- 影响：配置变更可归因性不可验收；总纲 §13 保真门禁计数错误（语义弱化数应为非零）。
- 最小闭合条件：在 §6.2（或 OUT-009/ACC-009）补上"配置变更记录控制者、时间、前后哈希及结果"的普通日志级审计落点（与 CON-006 普通日志边界一致），或在来源层取得明确删除/降级该要求的用户裁决并同步修正 §13 计数与依据。
- 发现来源分类：全面审核之需求保真逐项恢复检查

### AUD-002｜MCP 可处理的正常服务停止/升级触发路径无落点、无 ACC 覆盖

- 首次轮次：第 1 轮
- 来源层级：需求层（累计有效结论 23 = 记录 024；REQ-006 正式文本含该分支）
- 反例：FakeNet-NG 运行中对服务执行正常停止或升级部署——若 P03 按 OUT-007/§5 实现，MCP 直接退出，kill-on-close Job 由内核终止受管树，无优雅停止、无恢复审计通过门槛（区别于崩溃路径的 SCM 重启 + `recovering` 审计）——REQ-006"正常服务停止/升级必须先停止 FakeNet-NG并恢复审计环境"被违反，且无 ACC 可判失败。
- 证据：总纲 OUT-007 仅述"异常退出由 Job收敛、SCM重启"；§5.1—§5.4 无服务控制信号触发的完整收敛分支；ACC-007 动作与证据仅覆盖"FakeNet运行时强制结束MCP"；REQ-006 文本明确含正常服务停止/升级分支。
- 影响：可处理的正常服务停止被降级为崩溃式收敛，可能留下未经审计的环境差异即退出；该行为差异不可由现有验收观察，§13 声称对 REQ-006"完整承接"不成立。
- 最小闭合条件：在 OUT-007（或 §5）补上"可处理的正常服务停止/升级必须拒绝新状态变更、保持查询、先按 §5.4 完整收敛且恢复审计全部通过后方可退出"的语义，并在 ACC-007（或 P03/P04 对应 ACC）加入该触发路径的可观察验收；或由作者以来源层裁决证明该场景可由 kill-on-close + SCM 重启路径等价满足 REQ-006。
- 发现来源分类：全面审核之需求保真逐项恢复检查（触发/顺序与可观察验收）

### AUD-003｜§10 构建入口第三参数描述为 unused，与代码事实不符

- 首次轮次：第 1 轮
- 来源层级：方案层代码事实（总纲 §10"现有稳定操作与证据位置"）
- 反例：P01 按 §10 以占位值调用 `./Build-GuiVmPackage.sh <commit> <output-root> <占位目录>`——`tools/build_gui_vm_package_wine.py:315-320` 将非空第三参数解析为 `destination_dir`，正式包被写入该占位目录而非 output-root 下自动编号目录。
- 证据：`Build-GuiVmPackage.sh`（参数形态 `[source-commit] [output-root] [output-directory]`，非空第三参数透传 `--output-directory`）；`tools/build_gui_vm_package_wine.py:315-320`（`destination_dir = Path(output_directory).resolve() if output_directory else next_output_directory(output_root)`）。
- 影响：低。可能误导 P01 误用第三参数造成产物落点错误；作为冻结命令合同的一节含事实错误。
- 最小闭合条件：把 §10 该行修正为与脚本一致的语义（第三参数为可选显式输出目录，省略时在 output-root 下自动编号），或删除 `<unused-output-directory>` 占位描述。
- 发现来源分类：全面审核之机械扫描与代码事实核对

## 5. 非意见观察项

1. `AGENTS.md` 称正式包装合同"currently produces the v34 onedir package"，而当前代码 `Build-GuiVmPackage.sh` echo "Building formal v35"、`Build-GuiVmPackage.ps1` 默认 `v35`、`dist/` 已有 v35-r10—r12 产物。总纲 §10 的 v35 表述与代码一致，不构成总纲缺陷；该分歧属仓库文档过时，仅作观察记录。
2. 记录 027 的"部署 ACL + 应用层内置配置身份校验"双层机制在总纲中压缩为 §6.2 内置只读、ACC-016 文件 ACL/布局证据与 ACC-009 负向调用；可观察要求（AI 无法修改内置配置）已保留，机制细节可由子方案承接，不计为弱化。
3. 工作区存在大量未提交改动（含 `fakenet/diverters/windows.py`）。总纲 §9.1 以"不可变源 commit + 候选包 SHA-256 + manifest"绑定候选身份，符合组织评估对脏工作树的保护要求；未发现总纲授权覆盖或清理无关改动。
4. 决策关键路径全部可访问，无缺失；未发现需纳入而未纳入的直接依赖。

## 6. 结论

`存在不同意见`。AUD-001、AUD-002 使独立保真核对结果为 完整 35 / 语义弱化 3 / 未落点 0 / 不可验收 0，语义弱化数非零，按门禁不得审计通过；AUD-003 为低严重度事实修正。其余 13 项评审维度通过。本记录交总纲作者/执行方引入处理；审核方不决定引入后的复审必要性或审核模式。

## 7. 记录身份

- 本记录路径：`/home/adminn/projects/flare-fakenet-ng/PLAN/2026.09/2026.09.02/2026.09.02-04-审核-总纲-FakeNet-NG-MCP.md`
- 本记录创建后不可修改；Git blob 与 SHA-256 见创建后计算结果（由输出方在交付消息中给出）。
