# 实施子方案 P05 — 真实 Win10 发布门禁与交付证据

> 子方案路径：`/home/adminn/projects/flare-fakenet-ng/PLAN/2026.09/2026.09.02/2026.09.03-26-实施子方案-P05-真实Win10发布门禁与交付证据.md`
>
> 子方案版本：`v3`
>
> 状态：`回流修订待必要审核，受影响实施暂停`（旧审核只对应旧版本；本次见PLAN-CHANGE-001—004）
>
> 会话角色：`作者/执行方`
>
> 创建日期：`2026-09-03`
>
> workflow_id：`fakenetng-mcp-20260902-6ada6479`

## 1. 对象身份与来源

- 总纲 v2 blob `e5c734ea6a5225f1191b72e1c5864ba722dbb3ea`（§7 OUT-012/013/017、§8 P05、§9 ACC-012/013/017 与 ACC-001—019 同一候选汇总、§11 FB-007、§12）。
- 需求正式 v1 blob `b91ded55e75b96e60f66c8d58125d8a220d62a56`（记录 015、016、017、007、040）。
- 前序：P01—P04 均 CHILD_DONE；P04 交付的故障注入器、恢复审计、incident、受控退出与最终候选构建链是本方案直接输入。
- 生效项目约定：`AGENTS.md`（`c84a7533…a426`；VM 重置门禁与 host-only 传输授权、`dist/`/`Logs/` 归属）。

## 2. P05 切片（总纲 §7/§8/§9/§12）

- 主责 OUT：OUT-012（100 次正常恢复门禁→ACC-012）、OUT-013（每类 10 次故障收敛门禁→ACC-013）、OUT-017（原生 Windows 最终候选→ACC-017）。
- 范围：不新增功能；固定最终候选身份并执行全部发布矩阵；**在最终候选上重跑 ACC-001—019 全部验收 runner**（P01—P04 历史候选证据只作实施沿革, 不作为本候选发布证据）。
- 失败回路：缺陷修复回到产生行为的 P01—P04；P05 只管理新候选、证据导出、快照恢复和计数重跑， 不在发布门禁中临时开发功能。

## 3. P05 冻结的纯实现细节（总纲 §4 授权/沿用）

| 冻结项 | 值 | 依据 |
| --- | --- | --- |
| 最终候选 | 本次全部整改完成后固定新源提交/ZIP SHA/manifest；旧r34/r58及95695d2候选仅为历史，不继承通过 | PLAN-CHANGE-001—004/OUT-017 |
| VM 连续性与门禁起点 | 发布门禁从一次指定干净快照重置开始（AGENTS 门禁）；每轮记录 VM 启动时间/uptime 作为连续性证据；非计划中断（宿主重启、VM 挂起、快照恢复、服务崩溃性重启）使当前计数序列作废并从头重跑（崩溃性重启若可归因于产品故障则先按产品失败处置） | 记录 016"连续"语义、AUD-P05-003 |
| 100 次计数配置集 | 内置 `default.ini` 与一个自定义配置 `release-custom.ini`（以 default.ini 为基础做有语义差异的最小变更：关闭 HTTPListener80 的 webroot 依赖——`DumpHTTPWebRoot` 置空/移除 defaultFiles 引用, 并将 DumpPacketsFilePrefix 改为 `release-custom`, 校验通过）各 50 轮， 合计 ≥100；计数允许跨命令分段续跑（每轮独立落盘、汇总合并校验唯一轮号）， 但任一轮失败后从头重跑时不得保留旧轮样本——"连续"指无失败中断的计数序列, 非单进程会话 | RACC-012 代表性内置与自定义 |
| 每轮审计 | 每轮记录：**运行期连续探活时间线**（沿用 P03 `continuous_probe`, 覆盖启动到停止全程, 非单点探活）、五节恢复审计（规范化比较）全一致、配置锁释放、最终真实 `stopped`、VM 连续性标识；任一异常即该轮失败 | RACC-012 逐轮门槛、AUD-P05-006 |
| 故障轮通过判据 | 仅最终真实stopped、受管树清空、五节全审计和锁恢复通过、全程无断联；故障结果保留last_run_outcome=failed及完整incident。failed/grace-exceeded不能算通过 | PLAN-CHANGE-001/RACC-013 |
| 计数失败处置 | 任一轮失败：停止计数、导出证据到 `Logs/`、定位修复、按 FB-007 恢复指定干净快照、部署新候选；**候选一经更换, 该候选名下全部发布证据（100 轮+五类×10 轮+ACC-001—019 重跑）整体作废并在新候选上全部重跑**, 不保留任何旧候选计数或 ACC 结果 | 记录 016/017、FB-007、OUT-012/013/017 同一候选 |
| 快照重置 | 每次重置严格按 AGENTS 门禁（唯一快照、单次 revert、`vmware -x`、`DESKTOP-3FI41GR` 完成门）；重置前导出 guest-only 证据 | AGENTS.md |
| 发布产物 | `dist/`：最终候选 ZIP + `mcp-candidate-manifest.json` + `candidate-verification.json` + `release-manifest.json`（绑定源 commit/包 SHA/100+50 轮结果/全部 ACC 索引/**实际完成验收的 Windows 版本声明**——取自目标 VM `[Environment]::OSVersion`, 只声明实测版本） | OUT-017、记录 007、AGENTS.md dist 归属 |
| 运行证据 | ACC 级证据保持总纲 §9.1 目录 `Logs/fakenetng-mcp/<candidate-id>/<acc-id>/result.json`（ACC-012/013/017 同样各落其 `result.json`）；`release/` 子目录存放逐轮 `round-NNN.json`、故障类分组、时间线与汇总, 两者并存且互相引用轮号 | AGENTS.md Logs 归属、总纲 §9.1、AUD-P05-009 |
| ACC-017 端到端 | 在指定真实 Win10 VM 原生安装同一候选， 完成安装→服务启动→配置加载→受管启动→健康→停止→恢复审计→SCM 停止全链；汇总核对 ACC-001—019 索引均指向该候选（P01—P04 历史候选证据列为沿革， 不冒充本候选） | OUT-017 |

## 4. 实施项 IMP

### IMP-P05-01｜发布 runner（100/10 计数引擎）

- 范围：`test/mcp/acceptance/run_p05_release.py`：`--mode normal|fault|final|summary`；normal=100 轮正常启停；fault=五类×10 轮；final=ACC-017 端到端；summary=ACC-001—019 同一候选索引核对。每轮 §3 门槛断言、§9.1 result.json/退出码合同、失败即停+证据。
- 落点：runner + `Logs/.../release/`。

### IMP-P05-02｜100 次正常启停矩阵

- 范围：按 §3 配置集执行；每轮 full audit diff + 锁释放 + stopped 断言；MCP 链路每轮探活。
- 预期：零未解释残留/零断联/零虚假 stopped。

### IMP-P05-03｜每类 10 次故障矩阵

- 范围：env 门控注入（沿用 P04 机制）；每轮注入→收敛→审计；失败按 §3 重置重跑。

### IMP-P05-04｜ACC-017 端到端与最终汇总

- 范围：§3 ACC-017 全链 + release-manifest + ACC 索引汇总；`dist/` 产物固化。

### IMP-P05-05｜发布结果记录与提交

- 范围：实施记录（27 号文档）、状态文件、提交；P05 CHILD_DONE。

## 5. 验收项 ACC（可执行形式）

- **ACC-012**：通过=同候选 ≥100 轮正常启停全部逐轮门槛通过（内置 50+自定义 50）， 证据逐轮落 `Logs/.../release/`；失败=任一轮残留/断联/虚假 stopped（该轮起停止计数并进入 §3 失败处置）；阻断=快照/宿主设施故障（该轮不计数）。
- **ACC-013**：通过=五类各 10 轮全部收敛且逐轮审计通过；任一产品失败→修复→新候选→受影响完整计数从头重跑；无法稳定注入=阻断（不能减少次数）。
- **ACC-017**：通过=同一候选在指定真实 Win10 VM 原生安装并通过端到端全链（SCM/WinDivert/Job/锁/恢复/端到端 MCP）, ACC-001—019 全部在该候选重跑通过, release-manifest 含实测 Windows 版本声明；失败=构建侧结果替代实机或任何跨候选拼证据；阻断=指定 VM 不可恢复。

## 6. 需求保真门禁（P05 范围）

- 应承接数：`5`（记录 015、016、017 全量；记录 007 的"只声明实测版本"子句；记录 040 的实机不可替代子句）。
- 完整承接数：`5`；未落点数：`0`；语义弱化数：`0`；不可验收数：`0`。
- 核对：015→IMP-02/03+ACC-012/013；016（计数与重跑）→§3 计数失败处置+ACC-012/013；017（导出/修复/快照/重测）→§3+FB-007 路径；007 子句→ACC-017 只声明实际验收的 Windows 版本；040 子句→ACC-017 实机不可被构建侧替代。

## 7. 版本变更记录

- **v0 / 2026-09-03：** 首次编写。IMP-P05-01—05、ACC-012/013/017 可执行形式、§3 冻结表（最终候选、配置集、计数失败处置、产物/证据归属）、保真门禁 5 项。

## 8. 审核重点

- 最终候选固定为 P04 候选 r34 的合法性（无新修复时不再重建；有修复时新候选身份与计数归零规则）。
- 100 轮"零断联/零虚假 stopped"的逐轮可观察断言是否充分；轮与轮之间的环境漂移如何隔离（不重置快照连续 100 轮 vs 失败后重置）。
- 每类 10 次故障注入与 RACC-013 类列表一致；P04 的 FAULT-POINTS 单次证明不可替代 10 次计数。
- ACC-017 索引汇总对 P01—P03 历史候选证据的处理（沿革标注 vs 冒充本候选）。
- VM 长时间占用与快照重置的授权边界；`dist/`/`Logs/` 归属。
- 不新增功能、不在门禁中临时开发的边界。
- **v1 / 2026-09-03：** 引入首次第三方审核（AUD-P05-001—009, 全部同意）。核心修正： 删除"沿革标注"通过支路, ACC-001—019 全部在最终候选重跑（001）；候选更换=全部发布证据作废重跑（002）；VM 连续性证据与非计划中断作废规则+干净快照门禁起点（003）；阻断枚举补基础设施类（004）；故障轮通过判据固定（005）；连续探活时间线（006）；自定义配置语义差异化（007）；release-manifest 增实测 Windows 版本声明（008）；ACC 级 result.json 与 release/ 并存关系（009）。IMP/ACC 编号不变。

## 9. 2026-09-09 回流版本来源与资格

当前规范输入：同目录需求正式v3（含当日回流节）、总纲v5§16—17；§1旧blob和前序CHILD_DONE仅描述历史时点，不作为新候选通过证明。规范文件身份、逐项保真与自评见当日回流自评记录。原有非冲突条款继续有效，历史审核/验收不改。

### v2 P05证据门补充

起点为AGENTS指定Snapshot183；PLAN-CHANGE-002使DEC-009旧185选择终止适用，旧记录保留。发布证据逐轮固定candidate/source_commit/package_sha256、配置身份、VM启动及服务进程身份与连续性、从start请求前至stop审计后的完整探活时间线。采集失败不算无差异；任何失败停止计数并保留原文件，新候选所有计数/ACC重跑。汇总逐项验证身份精确相等、原文件SHA、子场景和轮数门槛，拒绝混源/缺证/覆盖失败。

ACC-017除原19项ACC外，机械要求ACC-004含CONTROL-DEFAULT、四端口CONTROL-EXTRA、CONTROL-INVALID、LOOPBACK-V4/V6的原生证据，ACC-002包含实际目标客户端与显式兼容矩阵。不扩张Windows版本声明或用Wine替代原生。

### v2 必要审核引入细化

最终ACC-017必须检查ACC-007原子创建窗口、ACC-004/006 IPC故障及ACC-014条件转储；ACC-019采用总纲§17完整两阶段原生矩阵。所有证据绑定同候选源/包及本次服务实例；无效SCM迁移、缓存健康、孤儿、陈旧结果和失败升级替换均失败。

## PLAN-CHANGE-005 当前子方案承接

ACC-017同候选汇总P04/ACC-019的两阶段全矩阵，升级/卸载runner不得直接sc stop绕过预停，不得合成成功或继承旧候选结果。

当前共同规范为正式需求v3及总纲v5§17；本子方案逐条承接其完整参与者、输入输出、权限、触发顺序、硬期限、状态/恢复责任、失败重试、结束条件、原生证据及失效/重裁条件，不以本段摘要降低规范。必要复审通过前受影响实施仍暂停。
