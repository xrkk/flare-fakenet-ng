# 独立核验记录 — FakeNet-NG MCP 总纲

> 会话角色：`独立核验方`
>
> 核验性质：实施后独立全面核验；只产生 `CHK-xxx` 和核查结论，不产生或关闭 `AUD-xxx`，不整改任何规范性对象或实现对象。
>
> 核验日期：`2026-09-04`（Asia/Shanghai）
>
> workflow_id：`fakenetng-mcp-20260902-6ada6479`
>
> 结论：**核查不通过**

## 1. 角色、对象与不变边界

本主会话进入时未绑定作者/执行方或第三方审核方角色，已绑定并公开输出为 `会话角色：独立核验方`。本次未创建子智能体，全部阅读、重跑、反例定位和结论由当前 Codex 主会话亲自完成。

| 固定对象 | 路径/身份 | 核对结果 |
| --- | --- | --- |
| 需求文档 | `/home/adminn/projects/flare-fakenet-ng/PLAN/2026.09/2026.09.02/2026.09.02-01-需求提炼-FakeNet-NG-MCP.md`；`正式需求 v1`；Git blob `b91ded55e75b96e60f66c8d58125d8a220d62a56` | 存在、唯一、已跟踪 |
| 当前总纲 | `/home/adminn/projects/flare-fakenet-ng/PLAN/2026.09/2026.09.02/2026.09.02-02-总纲-FakeNet-NG-MCP.md`；`v2`；Git blob `e5c734ea6a5225f1191b72e1c5864ba722dbb3ea`；状态 `已审核, 可以实施` | 存在、唯一、已跟踪 |
| 分支 / 核验 HEAD | `process-redirect-v23` / `0b3505669f152809a8149fb86178d77d1cbeb1f6` | 固定 |
| 最终候选源 | `03cc4d3201a69872a782673c786c5a05b3e763af` | 从该提交到核验 HEAD 的产品、测试、构建路径 diff 为空；候选源可固定 |
| 候选包 | `dist/mcp-r42/mcp-r1/fakenetng-mcp-candidate.zip` | SHA-256 `2694bba008a9e875bd2eb65ea2627239415d8988d8aa99c828f33df7489eafaa`；`unzip -tq` 通过 |
| 候选 ID | `mcp-c03cc4d32-2694bba008a9` | 与 ZIP、candidate manifest 和 release manifest 主身一致 |
| 总体验收记录 | `2026.09.03-31-总体验收-FakeNet-NG-MCP.md`；blob `ec4f025a5926be82aa1728eff10dfef39d8a827a` | 已独立复核；其“通过”结论被本记录反例否定 |

规范上限严格限于当前需求文档与当前总纲。历史 GrillMe 对话只按需求文档中持久化的记录 001—040 作来源追溯，未把旧轮次、被否决方向或核验者建议扩张为需求。

总纲边界为 OUT-001—018、ACC-001—019 和 P01→P05 固定顺序。排除范围包括 Linux 实现 TODO、MCP 自身/Windows/网卡/VM/宿主基础设施故障、未复现的潜在旧缺陷，以及与本方案无机械归因关系的既有脏工作树。启动时 48 条工作区状态是受保护现场；本次未清理、还原或改写它们。

## 2. Pxx 对象与实施身份

| Pxx | 当前子方案 blob | 主实施提交 | 自评/审核/处理/实施记录与证据定位 |
| --- | --- | --- | --- |
| P01 | `96207d392c3db346cd12f148394bf530505aa2f0` | `328035202e86c99b190e41356221140f961db0ee` | `07-自评`→`08-审核`→`09-审核处理`→`10-实施记录`；ACC-002/003/016/P01-ENTRY |
| P02 | `042d33835b171c38b1e035084955abd7c38d3048` | `f9d0637608264dcc0a48017b72529b6e373b5edc` | `12-自评`→`13-审核`→`14-审核处理`→`15-实施记录`；ACC-005/009-PRE/010/011 |
| P03 | `5d39031a296208752da88a37a13addb1503c7513` | `9376869a835fee347a72fd5f09e2b2392af121a6` | `17-自评`→`18-审核`→`19-审核处理`→`20-实施记录`；ACC-001/004/006/007/008/009 |
| P04 | `9887a503c7421e2346d82fe228146c6a52cee74d` | `dcfee76124e1f44a13eb0355ac7470dd4e889951` | `22-自评`→`23-审核`→`24-审核处理`→`25-实施记录`；FAULT-POINTS/ACC-014/015/018/019 |
| P05 | `4c0703281ba7b0b0d74e94da847802fb48f17234` | `03cc4d3201a69872a782673c786c5a05b3e763af` | `27-自评`→`28-审核`→`29-审核处理`→`30-实施记录`；ACC-012/013/017/summary/release |

上表中的主实施提交是切片交接身份；所有最终产品落点又统一受最终候选源 `03cc4d32…` 和包 SHA 约束。
总纲自评/审核/审核处理链为 `03-自评`→`04-审核`→`05-审核处理`，总体验收为 `31-总体验收`；已全部定位并与当前 Pxx/候选身份反向核对。

## 3. 独立重跑与证据完整性

1. 从 `03cc4d32…` 以 `git archive` 生成 `/tmp/fakenetng-mcp-chk-20260904` 只读候选源快照，避免将当前工作树脏改动带入回归。
2. 宿主候选快照受影响测试：`test/mcp` + `test/test_fakenet_stop_lifecycle.py` + `test/test_http_listener_stop.py` + `test/test_dns_selection.py`，解除 sandbox 端口绑定限制后结果 `88 passed in 10.00s`。首次 sandbox 内的 21 个 socket bind error 只记环境观察，不形成 CHK。
3. Docker/Wine Windows-Python 回归：已重读现有镜像 `flare-fakenet-ng/gui-vm-diagnostic-builder:py3119-pyi6220`，当前 image ID 为 `sha256:1b33b518a7c71ecd13264f0d1c41faf254e301b8702e2da8ca11376cff700a5e`；候选快照中同一受影响集结果 `106 passed, 4 skipped, 2 warnings in 27.19s`。Wine 结果仅作 Windows-Python 回归，未代替真实 Windows 运行验收。
4. ZIP 的独立 SHA-256 与解压完整性通过；候选提交到当前 HEAD 的产品/测试/构建 diff 为空。
5. 现有候选目录中找到 23 份 `result.json`、68 个被引用证据文件；68/68 存在且 SHA-256 与记录一致。release manifest 索引 22 项、全部声称 `pass`。
6. `release/` 中实际存在 100 份正常轮记录（builtin 50 + custom 50）和 50 份故障轮记录（五类各10）；它们共用 `boot=2026-08-31T08:54:50`。这证明数量与候选身份，不能弥补每轮未执行的合同检查。
7. 收尾时对 Win10VM MCP `192.168.204.149:28787` 的只读 `$env:COMPUTERNAME` 点验无法建立 TCP 连接。本次未为已证实的静态/runner 反例冒险重置 VM；该情况只是收尾环境观察，不作为 CHK 或“被阻断”结论的依据。

## 4. 需求保真计数

计数单位严格分开：“应承接/完整/未落点/语义弱化”以 38 条累计有效结论为母集；“不可验收”以 18 个 RACC 为母集。各异常维度可重叠，不把其强行相加为 38。

| 指标 | 数量 | 异常 ID / 说明 |
| --- | ---: | --- |
| 应承接数 | 38 | 需求文档累计有效结论 1—38；另有 16 REQ + 8 CON + 10 NON + 18 RACC 的 52 个稳定条目映射 |
| 完整数 | 9 | 结论 9、10、11、19、20、21、29、35、37 在当前实现与可用证据中未发现反例 |
| 未落点数 | 3 | 结论 1、2、18：GUI 与 MCP 互斥没有共享的 OS 级防线（CHK-002） |
| 语义弱化数 | 5 | 结论 3、5、6、8、38：精确的“产品 MCP 监听端口”排除被扩大到观察通道 28787（CHK-003） |
| 不可验收数 | 14 | RACC-001、004、006、007、008、009、011、012、013、014、015、016、017、018（CHK-002、04—11） |
| 失效或复核条件缺失数 | 1 | `extra_control_ports`/28787 方向只以不存在的 `DEC-007` 名称出现，没有稳定决策、失效条件或重新裁决条件（CHK-003） |

最后四项中“未落点数”、“语义弱化数”、“不可验收数”和“失效或复核条件缺失数”均非零，依本次核验边界不得核查通过。

## 5. 需求→总纲→Pxx→实现→提交→证据映射

### 5.1 REQ 主向映射

| 需求 | 总纲 / Pxx | 实际落点（最终候选 `03cc4d32…`） | 验收证据 | 核验 |
| --- | --- | --- | --- | --- |
| REQ-001 | OUT-001 / ACC-001 / P03 | `fakenet/mcp/singleinstance.py`、`supervisor.py`、`fakenet/gui/launcher.py` | `ACC-001/result.json` | **未落点**；CHK-002 |
| REQ-002 | OUT-002/003 / ACC-002/003 / P01 | `transportguard.py`、`server.py`、`firewall.py`、`winservice.py` | ACC-002/003 | 未发现反例 |
| REQ-003 | OUT-004 / ACC-004 / P03 | `controlfilter.py`、`windows.py::_apply_control_link_exclusion`、`tools.py` | ACC-004 | **弱化且不可验收**；CHK-003/005 |
| REQ-004 | OUT-005 / ACC-005 / P02 | `tools.py`、`server.py`、`configstore.py` | ACC-005 + ACC-016 tools/list | 未发现反例 |
| REQ-005 | OUT-006 / ACC-006 / P03 | `health.py`、`supervisor.py` | ACC-006 | **不可验收**；CHK-005 |
| REQ-006 | OUT-007 / ACC-007/019 / P03/P04 | `jobobject.py`、`winservice.py`、`supervisor.py` | ACC-007/019 | **不可验收**；CHK-005/009 |
| REQ-007 | OUT-008 / ACC-008 / P03 | `snapshot.py`、`supervisor.py` | ACC-008 | **不可验收**；CHK-005 |
| REQ-008 | OUT-009 / ACC-009 / P02/P03 | `configstore.py`、`configlock.py`、`tools.py` | ACC-009-PRE/009 | **不可验收**；CHK-004 |
| REQ-009 | OUT-010 / ACC-010 / P02 | `coordination.py`、Header middleware、`tools.py` | ACC-010 | 未发现反例 |
| REQ-010 | OUT-011 / ACC-011 / P02 | `coordination.py`、`tools.py` | ACC-011 | **不可验收**；CHK-004 |
| REQ-011 | OUT-012/013 / ACC-012/013 / P04/P05 | `baseline.py`、`supervisor.py`、`run_p05_release.py` | 100 + 50 轮记录 | **不可验收**；CHK-010 |
| REQ-012 | OUT-014 / ACC-014 / P04 | `incident.py`、`supervisor.py` | ACC-014 | **不可验收**；CHK-006 |
| REQ-013 | OUT-015 / ACC-015 / P04 | `artifacts.py`、`server.py` | ACC-015 | **不可验收**；CHK-007 |
| REQ-014 | OUT-016 / ACC-016 / P01 | `paths.py`、`winservice.py`、安装/目录布局 | ACC-016 | 行为证据存在，但包 SHA 字段为 `null`；CHK-011 |
| REQ-015 | OUT-017 / ACC-017 / P05 | build spec/runner、ZIP、manifests | ACC-017 + release manifest | **弱化且不可验收**；CHK-003/010/011 |
| REQ-016 | OUT-018 / ACC-018 / P04 | 故障修复提交链与 `run_p04_acc.py` | ACC-018 | **不可验收**；CHK-008 |

### 5.2 CON/NON 反向检查

| 稳定条目 | 主 OUT/ACC/Pxx | 反向结果 |
| --- | --- | --- |
| CON-001、NON-001、NON-009 | OUT-001 / ACC-001 / P03 | GUI 共控未落点（CHK-002）；初始 stopped/不自动启动有证据 |
| CON-002、NON-002 | OUT-017 / ACC-017 / P05 | Linux TODO 边界保留；Windows 最终聚合仍受 CHK-010/011 影响 |
| CON-003、NON-004 | OUT-003 / ACC-003 / P01 | 适用前提、取舍和重裁条件均保留，未发现反例 |
| CON-004 | OUT-004 / ACC-004 / P03 | 故障域边界文字保留，但详列场景未执行（CHK-005） |
| CON-005、NON-007 | OUT-010/011 / ACC-010/011 / P02 | 控制者无租约证据可用；生命周期并发矩阵不完整（CHK-004） |
| CON-006、NON-008 | OUT-008/014 / ACC-008/014 / P03/P04 | 实现静态不见 DB/重放框架；快照故障与 incident 验收不完整（CHK-005/006） |
| CON-007 | OUT-014 / ACC-014 / P04 | 只检查文件名，未证明包内事实可定位根因（CHK-006） |
| CON-008、NON-005 | OUT-015 / ACC-015 / P04 | 产品工具面不含下载；带外双端 SHA/服务停止未验（CHK-007） |
| NON-003 | OUT-005/009 / ACC-005/009 / P02/P03 | 封闭工具面有证据；完整配置操作矩阵未验（CHK-004） |
| NON-006 | OUT-007 / ACC-007 / P03 | 实现用 SCM 且未引入看门狗；Job 完整且仅限受管树的验收不足（CHK-005） |
| NON-010 | OUT-015/017 / ACC-015/017 / P04/P05 | 正式验收将 Win10VM MCP 28787 纳入产品 filter 额外排除，超出观察无产品依赖边界（CHK-003） |

### 5.3 RACC 证据反向映射

| RACC → ACC / Pxx | 证据核验 | 结果 |
| --- | --- | --- |
| 001 → 001 / P03 | runner 未启动 GUI，实现 mutex 不共享 | **不可验收**（CHK-002） |
| 002 → 002 / P01 | 目标客户端、协议与 Windows 服务证据可定位 | 可支撑 |
| 003 → 003 / P01 | 限定监听、防火墙和安全偏离证据可定位 | 可支撑 |
| 004 → 004 / P03 | 六类故障/退出和无效 filter 未完整执行 | **不可验收**（CHK-005） |
| 005 → 005 / P02 | 只读前后快照、封闭工具集和越界负样可定位 | 可支撑 |
| 006 → 006 / P03 | 未分别破坏进程/初始化/主动探测，仅注入日志文本 | **不可验收**（CHK-005） |
| 007 → 007 / P03 | 仅按进程名计数，未证 Job 成员、不误杀及完整恢复审计 | **不可验收**（CHK-005） |
| 008 → 008 / P03 | 原子替换/写失败/损坏/缺失/残留不一致未注入 | **不可验收**（CHK-005） |
| 009 → 009 / P03 | 未执行 import、内置删除、全部运行期 MCP 写/改名/删除 | **不可验收**（CHK-004） |
| 010 → 010 / P02 | 不同 ID 拒绝、相同 ID 新连接和无超时实现/证据可定位 | 可支撑 |
| 011 → 011 / P02 | 并发只覆盖同版本 create_config，未覆盖生命周期与混合配置变更 | **不可验收**（CHK-004） |
| 012 → 012 / P05 | 100 轮存在，但每轮未检查配置锁恢复 | **不可验收**（CHK-010） |
| 013 → 013 / P05 | 50 轮存在，但每轮同样未检查配置锁恢复 | **不可验收**（CHK-010） |
| 014 → 014 / P04 | 只检查一次日志异常和文件名，无 dump/开发者定位矩阵 | **不可验收**（CHK-006） |
| 015 → 015 / P04 | 无实际临时 host-only 传输、双端 SHA 和服务停止证据 | **不可验收**（CHK-007） |
| 016 → 016 / P01 | 行为字段可定位，但 `result.json.package_sha256=null` | **不可验收**（CHK-011） |
| 017 → 017 / P05 | final 基线在 FakeNet 启动后捕获；release manifest 缺 Windows 版本且聚合器只信状态 | **不可验收**（CHK-010/011） |
| 018 → 018 / P04 | runner 无条件返回通过，且未索引 P05 新缺陷的快照/新包/重测链 | **不可验收**（CHK-008） |

## 6. CHK 记录

### CHK-001｜受保护现场被带入候选提交

- 对应：总纲实施边界、P01/P03 实施记录的“用户既有改动不暂存/不提交”保护声明。
- 证据：启动快照 `启动时工作区快照-4ac9abf.txt` 已将 `fakenet/diverters/windows.py` 和 `test/test_dns_selection.py` 标为用户既有修改；提交 `306ad04` 将 `windows.py` 中“Auto DNS 全无响应时从 fallback 改为拒绝”的非 P03 排除 hunk 一并提交，`a88a3eb` 又提交了对应的用户测试改动。
- 反例：如果仅提交 P03 可机械归因 hunk，DNS 语义和用户测试不应从脏工作树进入候选源。
- 影响：候选源不再只是已审核 Pxx 的可机械归因实施，受保护现场承诺失真。
- 最小闭合条件：作者在新候选中将非本方案 hunk 与 MCP 实施分离，或为其提供可追溯授权/边界归属，然后重新固定源/包/证据。

### CHK-002｜REQ-001/NON-001 的 GUI—MCP 互斥未实现，ACC-001 未测

- 对应：结论 1、2、18；REQ-001、CON-001、NON-001、OUT-001、ACC-001；P03。
- 证据：`fakenet/mcp/singleinstance.py:12` 使用 `Global\FakeNet-NG-MCP-SingleInstance`，`fakenet/gui/launcher.py:36` 使用不同的 `Local\FLARE_FakeNet_NG_GUI_Config_Tool`，两路无共享互斥名或交叉检查。`run_p03_acc.py:112-129` 没有启动 GUI，而是用第二次 MCP `start` 调用代替“GUI 共控”。
- 反例：MCP 服务持有自己的 Global mutex 时，GUI 只争用自己的 Local mutex，不会因 MCP 已运行而被该防线明确拒绝。
- 影响：唯一无头主管和人工 GUI 互斥的核心产品边界未闭合；RACC-001 的 pass 无效。
- 最小闭合条件：在不改需求语义的前提下实现双向互斥，并在真实 Windows 上并发启动 MCP/GUI，保存双方明确拒绝证据后重跑 ACC-001。

### CHK-003｜控制链路排除扩张到 Win10VM MCP 28787（PLAN-CHANGE 候选）

- 对应：结论 3、5、6、8、38；REQ-003/015、NON-010、OUT-004/017、ACC-004/017；P03/P05。
- 证据：需求只允许“宿主 IP + 产品 MCP 监听端口”。实现新增 `ServiceConfig.extra_control_ports`、CLI `--extra-exclude-port`，`tools.py:91-99` 将产品端口与所有额外端口拼为 WinDivert 排除参数。正式验收管线 `Logs/fakenetng-mcp/r42-seq.py:24` 为 ACC-016 部署传入 `--extra-exclude-port 28787`；实施记录只称其为 `DEC-007` 性质，但规范链中无可定位的 DEC-007。
- 反例：观察通道 28787 不是产品 MCP 监听端口；为了让它在 FakeNet 接管时继续可用而扩大产品 filter 例外，使观察设施影响了产品网络语义。
- 影响：无稳定决策便扩大了安全/捕获例外与正式验收边界，语义弱化；当前核查不通过。
- 最小闭合条件：如果 28787 仅为验收编排便利，删除产品额外端口语义并用不改变产品 filter 的方式验收；如果作者认为必须保护观察端口，必须回流 GrillMe 需求基线/总纲/P03，登记稳定 `PLAN-CHANGE-xxx`，明确安全范围、失效和重裁条件，重做保真/自评/必要审核后才能对新 HEAD 核验。

### CHK-004｜ACC-009/011 未执行完整配置与并发矩阵

- 对应：结论 12—14、22、30、31；REQ-008/010、RACC-009/011、OUT-009/011；P02/P03。
- 证据：`run_p02_acc.py:307-346` 未调用 `import_config`，且内置编辑将 `version_conflict`/`config_not_found` 也当作“内置只读”通过；`run_p03_acc.py:334-439` 只做活动文件的外部写/删除，未穷举 MCP 编辑/改名/删除。`run_p02_acc.py:221-243` 并发只覆盖六个同版本 `create_config`，未执行总纲要求的并发/重复/过期生命周期和混合配置请求。
- 反例：`import_config` 可回归、内置删除可逃逸、或 `start`/`stop` 与配置变更竞态产生副作用，当前 runner 仍可签发 pass。
- 影响：RACC-009 和 RACC-011 的全范围负样/并发不可验收。
- 最小闭合条件：在真实 Windows 上按 ACC-009/011 逐操作、逐冲突类补齐可观察矩阵，证明无静默覆盖、排队、副作用或缺失审计后重跑。

### CHK-005｜ACC-004/006/007/008 用少量代替场景或硬编码布尔值签发 pass

- 对应：结论 3—6、23—25、36；REQ-003/005/006/007、RACC-004/006/007/008；P03。
- 证据：`run_p03_acc.py:135-199` 没有执行核心线程异常、卡死、受控/非受控退出和无效 filter，且 `invalid_protection_fails_closed_unit=True`；`:202-237` 未分别破坏三个健康条件；`:240-277` 以名字进程数 0/1 代替 Job 成员/不误杀/恢复审计；`:280-331` 明示将原子写、写失败、损坏和残留不一致留给“unit matrix”，但 ACC result 未引用对应快照或原始结果。
- 反例：上述任一未执行场景失败时，当前 runner 仍可返回 0。
- 影响：控制链路、真实健康、Job/SCM 崩溃恢复和快照失效四条稳定验收均不可验收。
- 最小闭合条件：按总纲 ACC 原文在指定 Win10 VM 逐场景故障注入，每项存原始输入/时间线/环境差异/结束状态，不使用无证据布尔替代真机动作。

### CHK-006｜ACC-014 只检查单类故障的文件名，违反“不得只检文件存在”边界

- 对应：结论 32、33；REQ-012、CON-007、RACC-014、OUT-014、ACC-014；P04。
- 证据：`run_p04_acc.py:80-121` 只在日志追加一条 traceback，随后检查 `manifest.json` 及七个文件名是否存在。它没有解析 manifest 中每项结果/失败原因/大小/SHA，没有覆盖每类终止性故障、挂起/原生/无栈/原因不明、dump 和“开发者仅依包内事实定位”。
- 反例：空文件、哈希错误、dump 未生成或包内无法定位根因时，只要文件名凑齐就可 pass。
- 影响：RACC-014 的核心证据充分性未被验收。
- 最小闭合条件：逐故障类执行、校验清单全字段和文件哈希，对规定升级类验证 dump，并留下仅用包内事实得出的定位结论。

### CHK-007｜ACC-015 未执行获准带外传输合同

- 对应：结论 27、28；REQ-013、CON-008、NON-005/010、RACC-015、OUT-015、ACC-015；P04。
- 证据：`run_p04_acc.py:124-157` 正确检查产品元数据形状和未知下载工具，但所谓带外导出只是用 Win10VM MCP PowerShell 读一份 guest 内 manifest，`export_hashable` 仅检查输出是否含字符串 `sha256`。
- 反例：文件从未离开 guest、两端哈希不一致或临时服务未停止，当前 runner 仍可 pass。
- 影响：产品无下载能力部分有证据，但 RACC-015 的带外导出、双端 SHA 和临时服务收尾不可验收。
- 最小闭合条件：只绑定 `192.168.204.1` 执行一次真实 guest→host 传输，保存两端 SHA-256、网络绑定和服务停止证据，并引用进 result。

### CHK-008｜ACC-018 无条件通过且未覆盖最终候选缺陷链

- 对应：结论 17；REQ-016、RACC-018、OUT-018、ACC-018；P04。
- 证据：`run_p04_acc.py:160-185` 仅输出 8 条静态文本行并无条件 `return EXIT_PASS`，不检查复现证据、修复 diff/提交、新包 SHA、快照恢复或受影响 ACC 重测。P05 后续又修复 `d8d1ca8`、`eeab273`、`069fdd0`、`2b04465`、`03cc4d3`，但 ACC-018 静态表未索引这些最终缺陷链。
- 反例：表中任意缺陷从未修复或重测，该函数仍返回 0。
- 影响：RACC-018 不可验收；总体验收不能以该 pass 证明既有异常已闭环。
- 最小闭合条件：将每个实际复现缺陷机械绑定复现包、修复提交、新包 SHA、快照和相关 ACC 新证据，聚合器对缺失/身份不一致必须失败。

### CHK-009｜ACC-019 未测并发变更拒绝和恢复审计失败

- 对应：结论 17、23；REQ-006/011、RACC-007 的正常 SCM 补充、OUT-007，总纲 ACC-019；P04。
- 证据：`run_p04_acc.py:188-269` 在 SCM stop 时只轮询 `ping`；`no_post_stop_side_effect=True` 是硬编码，没有并发发送任何变更请求。“升级”只在服务停止后复制 exe，未验证升级器等待完整收敛。runner 也没有注入恢复审计失败，没有验证必须保持 `failed` 和诊断链路。
- 反例：停止期间仍接收并排队变更，或恢复审计失败仍报告正常停止，当前 runner 不会发现。
- 影响：正常 SCM 停止/升级与崩溃 kill-on-close 的区分没有被完整验收。
- 最小闭合条件：两种正常触发分别并发发送变更与只读请求，校验完整停止阶段、Job/锁/控制权/环境 diff；单独注入审计失败并验证 `failed`。

### CHK-010｜P05 100/10/final 未执行审核后冻结的完整门禁

- 对应：结论 15、16、38；REQ-011/015、RACC-012/013/017、OUT-012/013/017、ACC-012/013/017；P05。
- 证据：P05 v1 §3 明确要求每轮检查配置锁释放；`run_p05_release.py:65-84` 的 capture 只有路由、DNS、WinDivert 进程、监听端口和服务，`:151-253` 正常/故障轮记录也不检配置锁。`:338-366` 的 final 先启动 FakeNet，再捕获 `before`，它对比的是运行期快照而非启动前基线。
- 反例：每轮都泄漏活动配置锁时，100/50 轮仍可全 pass；final 也可把启动造成的已有漂移当成“before”而报告 `audit_clean`。
- 影响：实际轮数达标不等于 RACC-012/013 逐轮完整审计通过；ACC-017 终态恢复证据不可信。
- 最小闭合条件：以启动前基线重做 final，将配置锁纳入每轮强制断言；候选变更后按 P05 合同整体作废旧候选的 100+50+ACC 证据并在干净快照重跑。

### CHK-011｜候选身份/发布聚合记录不完整

- 对应：REQ-014/015、RACC-016/017、总纲 §9.1；P01/P05。
- 证据：`ACC-016/result.json` 声称 pass 但 `package_sha256` 为 `null`；P05 v1 §3/§5 要求 release manifest 包含实测 Windows 版本，实际 `release-manifest.json` 只有 schema/candidate/source/package/acc_index/missing/wrong_candidate/failures，没有 Windows 版本。版本 `10.0.19045.0` 仅存于 `ACC-017/final-checks.json`。`run_p05_release.py:375-418` 聚合器只读 status/candidate_id，不拒绝 package SHA 缺失、证据字段不全或 Windows 版本缺失。另有 `ACC-017-SUMMARY/result.json` 不在 22 项索引中，标签边界未声明。
- 反例：一份属于其他包或无法定位到任何包的 ACC-016 result，仍会因 candidate_id 字符串相同而被聚合为全绿。
- 影响：“当前需求→总纲→Pxx→落点→提交→同一包证据”链在 RACC-016 中断裂，发布声明未满足 P05 审核后合同。
- 最小闭合条件：新候选上重采含非空包 SHA 的 ACC-016；release manifest 显式写入实测 Windows 版本；聚合时校验每份 §9.1 必填字段、证据哈希、候选三元组与许可标签集。

### CHK-012｜总体验收记录错配 RACC 语义并以状态聚合代替逐条验收

- 对应：总体验收记录 §2—§6；全部 RACC 的结论真实性。
- 证据：记录第 26 行将 RACC-006 描述为 state.json 七字段（实为真实健康），将 RACC-008 描述为恢复审计失败禁启（实为 state.json 故障），将 RACC-009 描述为停止态环境审计（实为配置管理），将 RACC-012 描述为故障类覆盖（实为100次正常启停），将 RACC-014/015 的 incident/带外导出语义混淆，将 RACC-018 描述为 100/10 计数（实为复现缺陷修复重测）。随后第 30、47 行仅因 ACC status 全 pass 就宣告 52/52 与全部 OUT/ACC 通过。
- 反例：本次 CHK-002—011 中的任一 runner 缺口都不会改变 release manifest 中的 status 字符串，却会使实际 RACC 合同未成立。
- 影响：总体验收记录的 19/19、52/52 和“通过”声明不是可信的需求保真/验收结论。
- 最小闭合条件：在 CHK-002—011 已于新候选闭合后，作者从需求 v1 逐条重建 RACC→ACC→证据映射，不以 status 聚合代替实际动作和反例断言。

## 7. 总结论

**核查不通过。**

原因不是真机证据文件缺失或候选包损坏：ZIP 完整、候选身份主体一致、100+50 轮文件存在、68/68 引用证据哈希一致，主机/Wine 回归也通过。不通过的决定性原因是：

1. REQ-001/NON-001 的 GUI—MCP 互斥有直接实现反例；
2. 控制链路 filter 未经稳定决策扩大到非产品观察端口；
3. 14 个 RACC 的当前 pass 证据没有执行审核后冻结的关键场景或身份断言；
4. 发布聚合和总体验收把“status 字符串为 pass”误当成合同动作已完成。

本记录未修改需求、总纲、任何 Pxx、审核/实施/总体验收记录、代码、测试、候选包或既有证据；未创建/关闭任何 AUD，未整改 CHK，未转换会话角色。下一次独立核验必须以作者闭合 CHK 后的新 HEAD/新候选发起；若保留 CHK-003 的观察端口方向，必须先完成稳定 `PLAN-CHANGE-xxx` 回流。
