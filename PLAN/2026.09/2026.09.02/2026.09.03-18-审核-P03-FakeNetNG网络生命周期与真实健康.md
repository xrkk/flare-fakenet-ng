# 审核记录 — 实施子方案 P03 FakeNet-NG 网络生命周期与真实健康

> 审核性质: 首次第三方审核
>
> 审核模式: 全面审核(先机械扫描, 后逐项语义审查与反例检查)
>
> 轮次: 第 1 轮
>
> 会话角色: `第三方审核方`(独立隔离上下文, 不采信作者结论, 一切判断基于亲读对象与证据)
>
> 创建日期: `2026-09-03`(date 实测 2026-09-03 01:13 CST)
>
> workflow_id: `fakenetng-mcp-20260902-6ada6479`

## 1. 对象身份

- 被审对象: `PLAN/2026.09/2026.09.02/2026.09.03-16-实施子方案-P03-FakeNetNG网络生命周期与真实健康.md`
- 声明版本 `v0`; 声明状态 `未审核, 不得实施`。
- 身份门禁亲算: `git hash-object` = `d3ec927494bcde86b705a6d75090851e5255674d` = 声明 blob, 匹配。SHA-256 = `b871ea79d162e380d917b40ab8daeea42974380923d6943de55301d76d8df5fc`。
- 规范性来源亲算: 总纲 v2 blob `e5c734ea6a5225f1191b72e1c5864ba722dbb3ea` 匹配; 需求正式 v1 blob `b91ded55e75b96e60f66c8d58125d8a220d62a56` 匹配; `AGENTS.md` SHA-256 `c84a7533dc7f96bc5a759410bb9b2f6160a8bf26c22ade35090b6e6383a4a426` 匹配。
- 本地基线: HEAD = `4de07050686d348628a19c4c3d7e12d2edc75283`(commit 消息 `docs(mcp): P02 complete - all ACCs pass on candidate mcp-cf9d06376-bb9ec6bf0511; enter P03`), 亲算匹配任务包声明 `4de0705`。
- Pxx: P03。历史链: 本对象无历史审计/处理记录(首次, 强制全面审核)。首次审核门禁: 未读取 `2026.09.03-17-自评-P03-…md`(作者自评, 禁读); 未读取任何作者会话或未持久化判断。
- 前序事实亲验: P02 实施记录 `2026.09.03-15` 状态 `CHILD_DONE`, 候选 `mcp-cf9d06376-bb9ec6bf0511`(r5, 源 f9d06376, ZIP bb9ec6bf), ACC-005/010/011/009-PRE pass + ACC-016 回归 pass; P02 交付合同文件 `fakenet/mcp/{errors,coordination,configstore,tools,testdouble,winservice,singleinstance,server,cli}.py` 与 `test/mcp/` 全部实存亲验。

## 2. 审核范围与执行

- 范围: 任务包 §4 全部流程 — 机械扫描(a–h) + 语义审查 1–9 + 指定反例检查 + 独立需求保真门禁。
- 决策关键路径亲验(≥5 项, 全部亲读):
  1. `fakenet/fakenet.py`: `parse_config:151`、`start:225`、`stop:439`, `running_listener_providers` 初始化 `:143`; `stop()` 带 `STOP_PHASE_BEGIN/END`(policy_suspend/listeners/diverter/complete)阶段汇总, `_stop_started/_stop_complete/_stop_result/_stop_error` 重复调用收敛(:440-451, :538-541), 返回 healthy 布尔(:533); `wait_for_capture_failure:543` 存在(总纲 §2.1"捕获致命状态观察"接口)。子方案 §1 代码事实声明除下述 AUD-P03-001 外全部属实。
  2. `fakenet/diverters/windows.py`: SHA-256 `bca29b6f05ca8ceb5f0bde3ecfaebab07e037da18203c501beba8e0d8d0f1131` 与总纲审核基线一致; `:511-512` 初始构造(`outbound and (ip or ipv6)` / `outbound and ip`); **`:586` `self.filter = build_egress_control_filter(...)` 在 `ExternalProcessRedirectEnabled` 时重建并覆盖初始构造**(引擎构造于 `:583`); `build_egress_control_filter` 定义 `:144`(base `:146`, 复合 filter 含 inbound 分支); 句柄打开 `:958`; `check_filter` `:1028` 位于 `_validate_process_redirect_windivert_runtime`(`:1025`), 调用点 `:955-956` **条件执行**(仅 `process_redirect_engine` 非空)。
  3. `fakenet/mcp/`(P02 合同): `coordination.py`(严格串行、身份门→重放门→版本门冻结顺序、无计时器/记录 022、LRU 进程内命令缓存/记录 038); `testdouble.py`(`LifecycleDouble`, 接口 `start/stop/restart/health_detail`, 模块注释明示 P03 替换且不改工具合同); `configstore.py`(symlink/hardlink/`..`/保留名拒绝, 模块注释明示 "reparse-point enforcement is a P03 real-Windows hook"); `winservice.py`(pywin32 ServiceFramework, `SvcStop` 置 `stop_event` 并报 STOP_PENDING, 注释明示受控关闭契约落在 P03/P04); `singleinstance.py`(`Global\FakeNet-NG-MCP-SingleInstance` 命名互斥体, 第二实例 `SystemExit(3)`); `tools.py`(start/stop/restart 经 `ctx.runner`, restart 绑定原 run_id)。
  4. `test/test_fakenet_stop_lifecycle.py` 实存; `tools/build_fakenetng_mcp_wine.py` 实存; `test/mcp/acceptance/{run_p01_acc.py,run_p02_acc.py,helpers.py}` 实存(P03 的 `run_p03_acc.py` 将新增, 模式有先例)。
  5. `fakenet/configs/`: `process_redirect_windows.ini` 同时启用 `ExternalAccessPolicy: EgressControl`(:21)与 `ExternalProcessRedirectEnabled: Yes`(:43); `default.ini` 为 Disabled(:122); 另有 3 个 EgressControl 内置配置。→ AUD-P03-001 的现实触达路径。
  6. 决策记录 `2026.09.02-02-总纲-FakeNet-NG-MCP-长程执行/2026.09.02-01-自动决策记录-总纲-FakeNet-NG-MCP.md` 已读(DEC-001..006 及 DEC-003 真机修订), 无与 P03 冲突裁决。

## 3. 机械扫描结果

- a) 身份/状态/blob: 全部通过(§1)。
- b) 来源切片引用真实: 总纲 §2.1/§3/§4/§5.5/§7/§8/§9/§11/§12 章节与引用内容逐项相符; 需求记录 001/003—007/011/013/014/018—026/032/033/038 实存且语义相符; AGENTS.md VM 门禁/Logs-dist 政策真实。
- c) 稳定 ID: IMP-P03-01—08 唯一且顺序前置合理; ACC 复用总纲全局 ID(001/004/006/007/008/009)正确; P02 标签(ACC-009-PRE)边界清晰、P03 未复用非总纲标签。缺陷: ID 拼写混用见 AUD-P03-007。
- d) 占位符/未决方向: 无 TBD/待定项; 停止宽限默认 30 显式声明为非正式冻结(P04 权限), 无越权冻结。
- e) 后序依赖: 不依赖 P04/P05 结果; incident 完整采集/条件转储/故障矩阵/ACC-019 完整验收/正式 100/10 计数均未提前实现; 基线 schema"与 P04 共用"为前向交接约定(总纲 §8 P03→P04 交付"环境基线身份"), 非后序依赖。
- f) ACC 三边界: 声明"三边界均由 runner 断言; 证据/退出码合同同总纲 §9.1", 六项 ACC 均有场景与预期; 部分场景注入机制与判定细节不足(AUD-P03-003)。
- g) fallback 显式: FB-002(IMP-P03-01 预校验失败拒启)、FB-003/004(IMP-P03-03)、FB-005(冻结表"停止宽限上限"行)、FB-008(IMP-P03-06)显式; FB-009 由 P02 合同延续。
- h) 代码事实亲验: 见 §2; 子方案 §1 的 `:511-513` "唯一构造点"声明不成立(AUD-P03-001), 其余行号与语义属实。

## 4. 语义审查覆盖清单(任务包 §4 第二步 1–9 逐项)

1. **核心架构判断(进程内 Fakenet + 服务进程自入 kill-on-close Job)**: 独立推演结论=语义等价, 通过。理由: (i) 进程内运行时"受管树"= 服务进程内的 `Fakenet` 实例线程 + 其派生的全部 listener 外部子进程; (ii) Job 归属由内核在进程创建时自动继承(与句柄继承正交), 服务进程自入 Job 后全部子进程默认入 Job; 句柄 `bInheritHandle=False` 保证"唯一 Job 句柄"前提(记录 025); (iii) MCP 异常退出(含 taskkill /F)→ 内核关闭其进程句柄表 → Job 最后句柄关闭 → 内核终止 Job 内剩余进程, 观察等价于"完整且仅限受管树终止"(OUT-007/ACC-007); (iv) SCM 重启新进程创建新 Job, 旧 Job 已随旧进程终止, 无竞争窗口; (v) §5.5 正常退出路径先完成统一停止序列再退出, Job 关闭时受管子进程已清空, 不构成"直接降级为崩溃捷径"; (vi) 无第二主管进程, 满足总纲 §2.1"只允许一个长驻主管服务"与 NON-006(无独立看门狗); (vii) 与总纲 §2.1"直接共享接口 Fakenet.start()/stop()"的落地方式一致——总纲 §2.1 正是把 `Fakenet.start()/stop()` 列为直接共享接口, §3.1.4"通过现有 FakeNet 入口管理一个实例"涵盖进程内驱动; §5.2.5"启动唯一 FakeNet-NG 进程树"在进程内方案语义为"启动受管实例", 由 §2.1 授权, 不弱化任何不变量。残余风险: Job 命名性未冻结(AUD-P03-009)。
2. **主 filter 排除(OUT-004)**: 方向正确(唯一构造点注入意图、两分支注入、无参数字节级不变回归、supervisor 独立预校验 + 失败关闭 FB-002、无第二套捕获机制), **但"唯一主 filter 构造于 :511-513"的代码勘察不成立**: `:586` 重建路径(内置配置 `process_redirect_windows.ini` 即触发组合)会丢弃仅在 `:511-513` 注入的排除子句且不触发失败关闭 → 静默失去链路保护。反例成立, 见 AUD-P03-001(高)。IPv6 反例: 排除子句 `ip.DstAddr == <ip>` 在 ipv6 包上恒为假 → IPv6 捕获语义不受影响(正确); 宿主地址若配置为 IPv6 字面量, check_filter 将拒绝 → 失败关闭成立, 但子方案未写明该边界(AUD-P03-010b)。
3. **快照(OUT-008/REQ-007/记录 038)**: 七字段与总纲 §4 逐项一致(run_id/controller_id/state_version/command_id/config_sha256/baseline_path/needs_recovery); 同目录临时+fsync+os.replace 原子替换; 写失败拒启(FB-003); 损坏/缺失+残留→failed 不猜测 stopped(FB-004); 恢复只审计不重放旧命令(IMP-P03-06 recovering 路径 + P02 Coordinator 进程内命令缓存)。通过。
4. **真实健康(OUT-006/REQ-005/记录 004)**: 三条件(受管对象存在+diverter 句柄+关键 listener 绑定 / 内部主动探针 / 本次运行日志未处理异常扫描)完整映射记录 004 的"进程存活+关键初始化证据+主动探测+日志监控"; 不以进程存活代健康; 日志异常立即撤销。2.0s 周期冻结权归属 P03 正确。缺陷: 总纲 §4 该行附加义务"并用时序测试证明不会造成虚假健康"未落点到 IMP-P03-07 测试清单或 ACC-006 断言(AUD-P03-002)。注: 2s 健康轮询与记录 022"控制者无计时器"正交(健康探测≠控制权租约), 子方案未混淆。
5. **Job/SCM(OUT-007/记录 025/026)**: 崩溃→Job 收敛→SCM 重启→recovering→按基线审计→真实 stopped / failed; 不自动启动 FakeNet(NON-009); 不续接旧 command_id; 不形成重启循环(审计失败保持在线 failed)。与总纲 §5.1/§5.4、FB-008 一致。通过。
6. **受控退出入口与 §5.5 P03 份额**: 入口(winservice SvcStop 现有 stop_event 钩子实存)+拒绝新变更(state_conflict/operation_busy)+与 stop 工具同一停止序列(含基线恢复核对)+审计通过才报 STOPPED = 总纲 §8 P03 范围的"受控退出入口"; 查询保持与退出门槛的完整验收、ACC-019 明确归 P04(总纲 §8 P04 范围明文"查询保持和退出门窗"), 子方案 §2 的份额划分与总纲一致, 未提前实现 P04 验收。通过。
7. **ACC-009 最终归属 P03**: 真实 Windows 文件锁(运行期外部写/换名/删除被拒)、停止解锁、重解析点逃逸(junction/symlink→path_escape_blocked)、FakeNet 读取兼容(锁不阻止服务进程读)齐备; 与 P02 前置(ACC-009-PRE 明确排除真实锁/重解析点)衔接无结构性缺口, configstore 的 P03 钩子实存。缺陷: "P02 全部前置"在 r6 候选上的继承语义(重跑 vs 引用 r5 证据)未明确, 交界场景(真实 supervisor 运行期 MCP 侧活动配置拒绝等)复验落点含糊(AUD-P03-005)。
8. **分工边界(P03 vs P04/P05)**: 停止宽限默认 30 显式声明非正式冻结、数值归 P04; 转储工具与时限、incident 完整证据包、恢复审计故障矩阵、正式 100/10 计数全部排除且未提前; 记录 034 的 P03 份额只取"撤销健康+置 failed+触发停止序列"入口。无越权。通过。
9. **需求保真门禁(独立执行)**: 见 §5。

指定反例检查: (a) EgressControl IPv6 分支排除语义 — 见清单 2(IPv6 包不受影响; IPv6 宿主字面量失败关闭成立但未写明); (b) Job 句柄 SCM 重启竞争 — 无竞争(新进程新 Job), 但命名性未冻结(AUD-P03-009); (c) state.json 半写 — 临时+fsync+os.replace 覆盖(同卷原子替换前提已列 ACC-008 场景), 目录元数据刷新未明确(总纲合同同级, 不构成弱化); (d) 运行期 load_config 锁竞态 — 启动序列锁与读取/parse 相对顺序未冻结, 存在校验内容与实际运行内容不一致的 TOCTOU 读法(AUD-P03-006); (e) ACC-004 场景真机可注入性 — 注入机制未冻结(AUD-P03-003)。

## 5. 独立需求保真门禁(五项计数)

- 子方案 §6 声称: 应承接 18(15 全量: 001/003/004/005/006/007/011/014/018/019/021/022/023/025/026 + 3 子句: 013 重解析点、024 受控退出入口、038 快照语义), 完整 18/未落点 0/弱化 0/不可验收 0。
- 独立核对: 范围划定与总纲 §8 P03 范围一致, P03 六个主责 OUT(001/004/006/007/008/009)的直接来源记录均有主 ACC 落点, 无主责遗漏(012/015/032/033 由 P02 前置证据与 IMP-P03-02/05 实质覆盖)。逐项恢复链(需求→OUT/Pxx→IMP/ACC, 含参与者/输入输出/触发顺序/状态生命周期/不变量/异常边界/前提与重新裁决/可观察验收)后:
  - 13 项落点明确: 001(IMP-P03-02/06+ACC-001)、003(IMP-P03-01+ACC-004)、004(IMP-P03-02 health+ACC-006)、005(IMP-P03-01 预校验+ACC-004 负样本)、006(ACC-004 六场景)、007(Linux TODO 不弱化 Windows)、011(IMP-P03-06 启动路径)、014(IMP-P03-05+ACC-009)、025(IMP-P03-03/04/06+ACC-007/008)、026(IMP-P03-06+ACC-007)、013 子句(IMP-P03-05+ACC-009)、024 子句(IMP-P03-06)、038 子句(IMP-P03-03+ACC-008)。
  - 018/019/021/022 四项经全部 ACC 场景隐式复验(所有场景经 Coordinator/控制者 Header/单实例真实运行), 且 P02 代码已冻结对应语义, 判完整承接。
  - **023(restart 绑定 run_id+幂等"真实运行复验")无 ACC 场景落点**: IMP-P03-08 场景清单无 restart 工具真实调用; ACC-007 仅覆盖崩溃侧"旧 command_id 不续接"。该项为子方案自行纳入承接范围的份额, 声称复验但无落点 → 计入未落点。
- **五项计数: 应承接数 18; 完整承接数 17; 未落点数 1(记录 023 的 P03 复验份额, 对应 AUD-P03-004); 语义弱化数 0; 不可验收数 0。**
- 异常 ID: AUD-P03-001(代码事实错误直接威胁 OUT-004 承接)、AUD-P03-004(未落点)。

## 6. 审核意见

- **AUD-P03-001**(第 1 轮, 高, 来源层级=代码事实/方案 §1+IMP-P03-01): 反例: 子方案声称"唯一主 filter 构造于 :511-513"并将 IMP-P03-01 落点仅写":511-513 两分支"; 实际 `fakenet/diverters/windows.py:586` 在 `ExternalProcessRedirectEnabled` 时以 `build_egress_control_filter`(:144, base :146, 复合 filter 含 inbound 分支)重建 `self.filter` 并覆盖初始构造, 内置配置 `process_redirect_windows.ini`(:21 EgressControl + :43 Yes)即触发组合。证据: 上述行号亲读 + `grep -n 'self.filter = '` 仅 :511/:586 两处赋值。风险: 按字面实施时该模式下排除子句被覆盖丢弃, 重建后 filter 仍合法可开、不触发 FB-002 失败关闭 → 控制链路保护静默失效, 违反 REQ-003/记录 005/006/OUT-004/FB-002, ACC-004 在该类配置下必失败或(若验收只用 legacy 配置)形成未被验收覆盖的产品缺口。影响: OUT-004、ACC-004、IMP-P03-01、无参数回归承诺。最小闭合条件: 修正 §1 代码事实; IMP-P03-01 落点覆盖全部最终 filter 赋值路径(:511-512 与 :586/`build_egress_control_filter`, 含复合 filter 中否定子句的正确位置——须作用于 outbound 分支而非尾部全局拼接); supervisor 预校验/复核对象改为 diverter 最终 `self.filter`(注入完成后复核实际 filter 含排除子句, 否则拒启)。
- **AUD-P03-002**(第 1 轮, 中, 来源层级=总纲 §4 裁决权限行): 反例: 总纲对健康轮询周期的裁决为"P03 冻结并用时序测试证明不会造成虚假健康"; 子方案 §3 冻结 2.0s, 但 IMP-P03-07 测试范围("快照、基线 schema、filter 构造字符串、状态机")与 ACC-006 断言均无时序项。证据: 总纲 §4 行 93; 子方案 §3 表行 34、IMP-P03-07、ACC-006。风险: 轮询周期可能造成虚假健康窗口(破坏与撤销之间的观察盲区)无测试证明, 总纲附加义务未履行。影响: OUT-006、ACC-006。最小闭合: 在 IMP-P03-07 测试清单或 ACC-006 断言中加入时序证明(如健康撤销时延上界=周期+处理时延、破坏注入后窗口内状态转换可观察)。
- **AUD-P03-003**(第 1 轮, 中, 来源层级=ACC 可实施性): 反例: ACC-004/ACC-006 场景注入机制未冻结——"注入 listener 崩溃""diverter 句柄关闭模拟""日志注入未处理异常文本""初始化证据缺失"只有目标语义, 无注入手段与判定细节; P04 的可重复故障注入器不溯及 P03 验收。证据: 子方案 §5 ACC-004/006 场景描述; 总纲 §9.2 ACC-008 行对"无法注入=验收设施阻断"的同类处理先例。风险: 实施时发现关键场景不可稳定注入 → 验收阻断或静默弱化场景; ACC-004 是 P03 核心安全验收。影响: ACC-004、ACC-006、IMP-P03-08。最小闭合: 逐场景写明注入手段(测试配置/外部进程操作/日志通道/supervisor 测试钩子)与可观察判定, 并声明其不属于产品能力。
- **AUD-P03-004**(第 1 轮, 中, 来源层级=需求保真/方案 §6): 反例: §6 声称"023→restart 绑定 run_id+幂等(真实运行复验)", 但 IMP-P03-08 场景清单无 restart 工具真实调用场景(ACC-007 仅覆盖崩溃侧旧 command_id 不续接)。证据: 子方案 §6 行 110、§5 ACC 清单、`tools.py:228-249`(restart 工具与 run_id 绑定语义由 P02 在替身上验收)。风险: 真实 supervisor 上 restart 语义未经验收即交接 P04; 门禁未落点数非零。影响: 记录 023 承接、ACC-004 场景完整性。最小闭合: 在 ACC runner 场景(如 ACC-004 受控退出后或独立步)加入真实 restart 绑定原 run_id、幂等与冲突的断言, 或在 §6 中明确该份额的实际复验落点。
- **AUD-P03-005**(第 1 轮, 中, 来源层级=ACC 证据链): 反例: ACC-009 写"P02 全部前置 + 真实锁/重解析点/读取兼容", 未明确"P02 全部前置"是在 r6 候选重跑还是引用 r5 候选(mcp-cf9d06376)与 testdouble 环境的既有证据; 配置工具在真实 supervisor 下的交界行为(运行期 MCP 侧活动配置拒绝 `config_in_use`、停止后解锁、运行期非活动配置写授权分界/记录 032)复验落点含糊。证据: 子方案 §5 ACC-009; P02 实施记录(ACC-009-PRE 基于 r5+替身, 明确"本标签不构成 ACC-009 通过"); 总纲 §9.2 ACC-009 行动作含完整管理与审计矩阵。风险: r6 候选上 ACC-009 证据不完整(仅锁部分), 或交界缺陷在换入真实生命周期后才显现而无场景覆盖。影响: ACC-009、OUT-009。最小闭合: 明确 ACC-009 在 r6 候选上重跑 ACC-009-PRE 矩阵, 或至少覆盖与活动锁/真实运行交界的全部场景(含记录 012/032/033 的运行态份额)。
- **AUD-P03-006**(第 1 轮, 低, 来源层级=实施顺序): 反例: IMP-P03-02 文本"线程内 parse_config→注入排除→Fakenet.start();启动前:……活动配置 OS 锁(IMP-03-05)"存在锁后于 parse 的读法; 若配置读取/SHA-256/parse 先于锁取得, 存在校验内容与实际运行内容不一致的 TOCTOU 窗口(外部写者可在窗口内替换文件, 归因断裂, 违反记录 014 意图)。证据: 子方案 IMP-P03-02 范围句; 总纲 §5.2.2/§5.2.5(锁与启动同节但读取校验在前)。风险: 运行实例与登记 SHA-256/审计不一致。影响: IMP-P03-02/05、ACC-009。最小闭合: 冻结启动顺序为"取得活动配置 OS 锁→读取+SHA-256→parse→注入→start"(或等效原子性论证)。
- **AUD-P03-007**(第 1 轮, 低, 来源层级=稳定 ID 规范): 反例: IMP ID 拼写混用——IMP-P03-02 内引用"IMP-03-04""IMP-03-05"、IMP-P03-06 前置"IMP-03-02/03"、IMP-P03-07 前置"IMP-01…06", 与标题形式 IMP-P03-0x 不一致; 另 IMP-P03-02 存在拼写错误"`Fakenert.stop()`"。证据: 子方案 §4 各项原文。风险: 跨轮追溯与机械扫描歧义。影响: 文档质量。最小闭合: 统一为 IMP-P03-0x 并改正拼写。
- **AUD-P03-008**(第 1 轮, 低, 来源层级=基线/审计对应): 反例: IMP-P03-04 基线捕获列为"WinDivert 进程清单、监听端口与相关服务状态"等 5 类, 而 §3 恢复审计行按"进程/路由/DNS/端口/锁"核对、总纲 §5.2.3 列"启动前进程、WinDivert、路由、DNS、相关服务、监听端口和配置锁"——"进程"基线范围未明确定义, 可能存在无基线可比的核对项(如受管 listener 子进程残留特征)。证据: 子方案 §3 两行、IMP-P03-04; 总纲 §5.2.3。风险: 恢复审计对进程项的判定不可复现。影响: IMP-P03-04、ACC-007/008。最小闭合: 明确进程基线范围(=WinDivert 相关进程+受管实例派生进程特征或等价定义)。
- **AUD-P03-009**(第 1 轮, 低, 来源层级=记录 025 前提): 反例: Job Object 命名性未冻结。命名 Job 可被第三方 `OpenJobObject` 持有句柄, 延迟或阻止 kill-on-close 触发, 动摇记录 025"MCP 关闭其唯一 Job 句柄"前提; 未命名 Job 仅能经句柄继承/复制传播, 与已冻结的 `bInheritHandle=False` 组合即闭合。证据: 子方案 §3 Job Object 行; 记录 025 前提句"Job 句柄不被意外继承或复制"。风险: 极端情况下崩溃收敛不触发。影响: IMP-P03-06、ACC-007。最小闭合: 冻结为未命名 Job + `bInheritHandle=False`, 并在实施记录验证服务进程为唯一句柄持有者。
- **AUD-P03-010**(第 1 轮, 低, 来源层级=事实精确性合并备注): (a) §1/IMP-P03-01 描述"打开前校验 WinDivert.check_filter:1028"未提该路径为条件执行(`windows.py:955-956`, 仅 process_redirect_engine 非空时调用); supervisor 独立预校验弥补了失败关闭, 但方案事实描述应修正, 且 AUD-P03-001 闭合后预校验对象须与最终 filter 一致。(b) 排除子句模板仅 IPv4 语义(`ip.DstAddr`), 应写明宿主地址为 IPv6 时必须失败关闭(依赖 check_filter 语法拒绝是隐式保证)。(c) ACC-008"禁框架扫描(wheelhouse 与 _internal 清单比对白名单)"的白名单基准文件来源未指定。(d) 现有 `Fakenet.wait_for_capture_failure`(`fakenet.py:543`)/`_record_capture_failure` 捕获失效观察接口未纳入健康探针冻结, 实施时可复用以满足"核心线程异常"探测。证据: 各对应文件行号。风险: 低(描述精度与实施便利)。影响: IMP-P03-01/02/08。最小闭合: 逐项补充说明。

## 7. 结论

**存在不同意见**。

- 高严重度 AUD-P03-001(主 filter 构造点勘察错误 → EgressControl+ProcessRedirect 模式下控制链路排除静默失效路径)与需求保真门禁未落点数 1(AUD-P03-004)使本轮不能判定审计通过。
- 其余架构判断(进程内 Fakenet+自入 Job 等价性、§5.5 份额划分、P03/P04 分工、快照/健康/SCM 语义)经独立推演与代码亲验均成立, 方案骨架与总纲合同一致, 未发现越权、后序依赖或占位符。
- 本结论只表示本轮发现; 审计记录交回方案作者或执行方引入, 处理路线由其决定。本记录创建后不可变。

## 8. 记录身份

- 本记录路径: `PLAN/2026.09/2026.09.02/2026.09.03-18-审核-P03-FakeNetNG网络生命周期与真实健康.md`
- 编号依据: 2026-09-03 当日该目录已有 Markdown 最大流水号 17(2026.09.03-17), 本记录取 18; 不继承被审对象或前轮编号。
- 唯一写入: 本文件; 未修改任何方案、代码、配置、测试或作者记录。
