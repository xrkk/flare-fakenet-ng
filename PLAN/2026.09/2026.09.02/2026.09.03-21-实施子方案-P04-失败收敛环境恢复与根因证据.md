# 实施子方案 P04 — 失败收敛、环境恢复与根因证据

> 子方案路径：`/home/adminn/projects/flare-fakenet-ng/PLAN/2026.09/2026.09.02/2026.09.03-21-实施子方案-P04-失败收敛环境恢复与根因证据.md`
>
> 子方案版本：`v5`
>
> 状态：`已审核, 可以实施`（当前PLAN-CHANGE-006资格见2026.09.10-05必要方案定向复审；不表示ACC/CHK通过）
>
> 会话角色：`作者/执行方`
>
> 创建日期：`2026-09-03`
>
> workflow_id：`fakenetng-mcp-20260902-6ada6479`

## 1. 对象身份与来源

- 总纲 v2 blob `e5c734ea6a5225f1191b72e1c5864ba722dbb3ea`（§5.4/§5.5、§6.3、§7 OUT-012/013/014/015/018、§8 P04、§9 ACC-014/015/018/019 与 ACC-012/013 机制、§11 FB-005/006/007、§12）。
- 需求正式 v1 blob `b91ded55e75b96e60f66c8d58125d8a220d62a56`（记录 015—017、024、028—030、034、035、记录 036 有效部分）。
- 前序：P01/P02/P03 均 CHILD_DONE；P03 交付的可故障注入真实生命周期、基线身份与恢复标记是本方案直接输入（候选 `mcp-c9376869a-6ad29cc16091` 六项 ACC pass）。
- 生效项目约定：`AGENTS.md`（`c84a7533…a426`；host-only 证据传输授权）。

## 2. P04 切片（总纲 §7/§8/§9/§12）

- 主责 OUT：OUT-014（根因级 incident→ACC-014）、OUT-015（产物元数据与带外导出→ACC-015）、OUT-018（复现缺陷修复重测闭环→ACC-018）、OUT-012/013 的机制（有界停止/强制收敛/补偿恢复审计/可重复故障注入器， 发布计数归 P05）。
- 完整承接：§5.5 正常服务停止/升级的完整收敛与退出门槛→ACC-019（P03 已交付入口）。
- 排除：正式 100/10 发布计数、最终包身份汇总（P05）；新产品功能。

## 3. P04 冻结的纯实现细节（总纲 §4 授权 P04 冻结项）

| 冻结项 | 值 | 依据 |
| --- | --- | --- |
| 优雅停止硬上限 | `stop_grace_seconds` 默认 60（服务配置可改）， 真机证据下冻结； 超限→有界保存现场→仅终止受管范围→完整恢复审计 | 总纲 §4“P04 冻结数值” |
| 停止宽限期间服务配置 | service.json 可选 `stop_grace_seconds`（默认 60, 范围 5..600；下限防误配为 0 的无限等待、上限防超过 SCM 兜底窗口的失控值, AUD-P04-010） | 同上 |
| incident 位置与清单 | `%ProgramData%\FakeNet-NG-MCP\artifacts\<run_id>\incident\`；`manifest.json` 逐项 `{item, result, failure_reason, size, sha256}` | 记录 035 |
| incident 基础层 | 运行/命令/状态/健康时间线（EventLog 导出）、程序/依赖/OS/Python 版本与关键文件哈希、活动配置快照+哈希、完整 stdout/stderr 与本次运行日志（运行窗口）、异常/traceback（触发信号原文）、全部受管线程栈（`sys._current_frames` 快照）、进程树/命令行/**进程启动时间**/退出码/资源/**句柄摘要**（`Get-Process | Select HandleCount` 及受管进程 `handles` 计数）、WinDivert/filter 信息（含最终 filter 与排除子句）、基线五节与故障时差异、**防火墙规则基线差异**（`netsh advfirewall firewall show rule name="FakeNet-NG MCP"` 与启动前导出对比）、Windows Event Log 摘要（System/Application 最近 200 条）、产物路径/大小/哈希 | 记录 035 全列表 |
| 转储升级条件 | 挂起（线程栈含 diverter/主循环且无进展）、原生异常（非 Python 异常签名）、线程栈不可得、基础层不能解释失效 → 采集 Windows 用户态转储（`procdump -mm` 等价工具或 Windows Error Reporting LocalDumps 注册表方式；选型在实施时以实机可用性冻结并记录 DEC） | 记录 035 |
| 转储采集时限 | 单项 60s/总计 180s 硬上限；超限记 `timed_out` 并继续清理 | CON-007 |
| 磁盘保护 | incident 目录配额 512 MiB；超限停止采集新大项（转储）并记录 | 记录 035 |
| 产物登记 | `%ProgramData%\FakeNet-NG-MCP\artifacts\<run_id>\` 下运行产物（FakeNet PCAP/日志/报告）登记 `{path,type,size,complete,sha256}`；`list_artifacts` 只输出这些字段 | OUT-015 |
| 内部失效自动处置 | P03 健康监测终止性证据→先有界采集 incident→同一停止序列；采集失败记录缺项继续清理（验收判失败但不阻塞） | 记录 034/CON-007 |
| 故障注入器 | `fakenet/mcp/faultinject.py` 五类， 手段互异可归因（AUD-P04-006）：1)策略暂停=stop 序列在"listener 停止阶段"前阻塞（挂起 stop 阶段推进, 模拟策略层挂起）；2)listener 停止=对运行中 listener 的服务套接字执行 close/shutdown（触发 listener 层停止）；3)diverter 停止=直接关闭主 WinDivert 句柄；4)受管子进程不退出=FakeNet 停止后残留一个测试子进程挂起（在独立FakeNet受管树内生成真实不退出子进程，验收记录其PID/Job身份）；5)清理异常=stop 中途抛出指定异常。仅测试启用（服务端环境变量 `FAKENETNG_MCP_FAULT_INJECTION=1`+显式注入文件），产品默认关闭且不暴露 MCP 工具 | ACC-012/013 机制+RACC-013 列表 |
| 恢复审计器 | 五节全范围+配置锁；仅消除排序/格式/PID重排等不丢语义差异，保留UDP/TCP全部监听、路由metric和相关服务状态。瞬态环境噪声必须有当次可验证归因；缺节/采集失败/未知差异失败关闭。无阈值/协议豁免 | PLAN-CHANGE-002/OUT-012 |
| 正常服务停止/升级门槛（ACC-019） | 本地CLI预停128：SCM保持RUNNING，draining拒新变更→等在途→完整停止/全审计→真实stopped且冻结→向SCM宣告接受STOP→发布当前attempt成功→CLI核对身份/状态后标准STOP；预停失败failed/在线且禁止替换 | 总纲§17 PLAN-CHANGE-005 |
| 带外导出 | 复用 P01 传输服务模式（仅绑 `192.168.204.1`）导出 incident/产物到 `Logs/`， SHA-256 双端核对 | OUT-015/AGENTS.md |

## 4. 实施项 IMP

### IMP-P04-01｜优雅停止上限与服务配置

- 范围：`config.py` 增 `stop_grace_seconds`（默认 60）；`supervisor.py` 使用之（替换 P03 的 30s 常量）；`cli.py install --stop-grace` 参数。
- 落点：`fakenet/mcp/{config,supervisor,cli}.py`。前置：P03。

### IMP-P04-02｜incident 采集器（基础层+条件转储+清单）

- 范围：`fakenet/mcp/incident.py`：`collect(run_id, ...)` 按 §3 清单逐项采集（Windows 命令经 subprocess：`tasklist /v /fi`、`wmic`/`Get-CimInstance`、`wevtutil qe System /c:200`、`route print`、`netstat -ano`、`Get-DnsClientServerAddress`）；线程栈 `sys._current_frames()`；哈希；`manifest.json` 原子写；转储升级条件判定与有界执行；磁盘配额。
- 落点：`fakenet/mcp/incident.py`。前置：P03。

### IMP-P04-03｜内部失效自动处置接线

- 范围与触发链映射（AUD-P04-004）：(a) 未处理异常——健康循环扫描到异常签名→当前 P03 只降级 degraded, 本 IMP 修改为：终止性判据（异常签名且 probe 失败, 或连续 2 个周期异常仍在）→立即 `failed`+incident+停止序列；瞬时仅降级 degraded 语义保留；(b) 初始化失败——start_error 回滚路径在返回 failed 前采集 incident（有界）；(c) 挂起——健康探测持续失败+线程栈无进展（IMP-02 升级条件）→同 (a)。`last_run_outcome=failed`+incident 引用保留于 `get_status`。
- 落点：`fakenet/mcp/{supervisor,tools}.py`。

### IMP-P04-04｜产物登记与元数据

- 范围：`fakenet/mcp/artifacts.py`：run 目录登记 FakeNet 产物（PCAP/日志/报告）→`list_artifacts` 元数据输出；产品无正文路由（P02 已冻结， 复验）。
- 落点：`fakenet/mcp/{artifacts,tools}.py`。

### IMP-P04-05｜完整恢复审计器与故障注入器

- 范围：`baseline.py` 全节比对模式（P03启动恢复路径同样使用全节审计）；恢复审计矩阵新增"配置锁释放"验证项（活动配置文件可再次以写共享打开且 store 活动名为空, AUD-P04-002）；`faultinject.py` 五类注入（§3）；服务配置不暴露注入入口。
- 落点：`fakenet/mcp/{baseline,faultinject}.py`。

### IMP-P04-06｜正常服务停止/升级受控退出（ACC-019 门槛）

- 范围：按总纲§17固定预停128→唯一收敛worker（SCM RUNNING）→成功冻结后标准STOP；cli/server/winservice负责入口与最后关闭HTTP，supervisor/coordination负责排空、停止和全审计。升级/卸载必须调用CLI stop并核对当前实例成功及STOPPED后才替换。
- 落点：`fakenet/mcp/{coordination,supervisor,winservice}.py`。

### IMP-P04-07｜测试+构建

- 范围：incident 清单字段/时限/配额/升级条件单元测试（Linux 可测部分）；faultinject 单元；恢复审计全节比对单元；ACC-019 序列（替身）；全仓 Wine 门禁；候选 r24。
- 落点：`test/mcp/*`、`dist/mcp-r24`。

### IMP-P04-08｜ACC runner 与真机执行

- 范围：`run_p04_acc.py`：ACC-014（每类终止性故障 incident 完整性+开发者仅据包定位）、ACC-015（元数据+正文负向+带外导出 SHA 核对）、ACC-018（复现缺陷闭环——P03 轮次已修复的 7 项真机缺陷作为已验证闭环证据+如本轮复现新缺陷则完整闭环）、ACC-019（CLI预停128+升级前两阶段停服+审计失败同实例在线failed）；并证明 ACC-012/013 五类故障点稳定可触发（每类≥1 次, 不跑 10 次计数——归 P05）。
- 落点：runner + `Logs/fakenetng-mcp/<candidate-id>/`。

## 5. 验收项 ACC（可执行形式）

- **ACC-014**：对未处理异常/初始化失败/挂起(注入)三类终止性故障各生成 incident；断言：清单字段齐全（含失败原因/大小/SHA）、基础层每一必需项存在或显式记缺项原因、转储升级条件触发时转储文件存在、开发者仅据包定位（验收者回答“触发组件+失败链路”并写入 result.json 的 observed）；采集时限不阻塞清理（停止仍完成）。
- **ACC-015**：`list_artifacts` 只返回元数据字段；HTTP 负向：无下载路由（404/无工具）；带外导出 incident+产物到 `Logs/`， 双端 SHA-256 一致， 传输服务停止证据。
- **ACC-018**：口径统一（AUD-P04-007）：P01—P03 共产生 8 项**运行期真机缺陷**修复（uvicorn 无控制台流崩溃、sc 1072 重装竞争、ctypes SCM 桩失效、diverter `_dict` 键、pydivert 2.0.9 冻结缺 check_filter、filter 语言无一元否定、包缺 defaultFiles/ssl_utils/WinDivert 驱动、restart TIME_WAIT 竞态），每项具备 真机复现证据→修复提交→同轮或后继重测通过；另有 10 项构建/验收工具修复（不属 REQ-016 缺陷闭环, 列为工具沿革附表）。本 ACC 验证=8 项缺陷闭环沿革表完整且重测证据在 Logs/；本轮真机执行如复现新运行期缺陷则按同闭环处理后重测, 无新缺陷即通过。
- **ACC-019**：FakeNet运行时分别经CLI stop与升级包装发送预停128；验证draining、在途等待、查询保持和完整收敛/全审计。成功先冻结且向SCM报告接受STOP，再发布同实例attempt成功；CLI核对后标准STOP，真实STOPPED后才允许替换。原生覆盖直接STOP拒绝、预停审计失败同PID在线/不替换、超时、重复尝试、陈旧结果、成功发布竞态及无Recovery循环，预算与结果身份遵循总纲§17。
- **故障点可触发证明（ACC-012/013 机制份额）**：五类注入（策略暂停/listener 停止/diverter 停止/子进程不退出/清理异常）各执行 1 次：每类触发成功、强制收敛完成、全节恢复审计回到基线；正式 10 次计数不在本轮。

## 6. 需求保真门禁（P04 范围）

- 应承接数：`11`（记录 015、016、017、024、028、029、030、034、035 全量；记录 004 的自动处置子句；记录 036 有效保留子句）。
- 完整承接数：`11`；未落点数：`0`；语义弱化数：`0`；不可验收数：`0`。
- 核对：015→IMP-01/05/06+ACC-019/故障点证明；016（机制份额）→IMP-05+每类触发；017→失败→failed 禁启动+证据导出→IMP-03/08；024→IMP-06+ACC-019；028/029/030→IMP-04+ACC-015；034→IMP-03；035→IMP-02+ACC-014；004 子句→IMP-03；036 子句→无被否决机制（CON-006 边界延续）。

## 7. 版本变更记录

- **v0 / 2026-09-03：** 首次编写。IMP-P04-01—08、ACC-014/015/018/019 可执行形式与故障点触发证明、§3 冻结表（停止上限 60s 等）、保真门禁 12 项。

## 8. 审核重点

- incident 清单与记录 035 逐项一致（含版本/哈希/线程栈/WinDivert/基线差异/Event Log）；缺项显式失败但不阻塞清理。
- 转储升级四条件与选型权限（实机可用性冻结+DEC）；采集时限/磁盘配额数值依据。
- 故障注入器仅测试启用、不暴露 MCP 工具；五类与 RACC-013 列表一致。
- ACC-019 与 §5.5 逐条对应：拒绝新变更/保持查询/完整收敛/真实 stopped 才退出/失败不宣称干净停止；与 ACC-007 异常路径不混淆。
- 停止上限 60s 的真机证据义务（冻结数值需实机量测支撑）。
- ACC-018 以 P03 修复沿革为已验证闭环的合法性（缺陷均真机复现→修复→重测）。
- 不预跑 P05 的 100/10 计数。
- **v1 / 2026-09-03：** 引入首次第三方审核（AUD-P04-001—010, 全部同意）。恢复审计比较细化定义+配置锁释放项（001/002）；incident 基础层补进程启动时间/句柄摘要/防火墙基线差异（003）；终止性故障触发链映射（004）；SCM waithint/checkpoint 刷新+cli/server 落点+升级表述统一（005/010）；五类注入手段互异定义（006）；ACC-018 口径统一为 8 项运行期缺陷闭环（007）；保真计数修正 12→11（008）；退出拒绝码统一 `not_allowed_in_state`（009）。IMP/ACC 编号不变。

## 9. 2026-09-09 回流版本来源与资格

当前规范输入：同目录需求正式v3（含当日回流节）、总纲v5§16—17；§1旧blob和前序CHILD_DONE仅描述历史时点，不作为新候选通过证明。规范文件身份、逐项保真与自评见当日回流自评记录。原有非冲突条款继续有效，历史审核/验收不改。

### v2 P04跨进程收敛冻结

IMP-P04-02/03/05/06沿总纲v5§16—17：取证对象包含主管和受管树，Python栈从受管进程获取；转储通过可终止的有界辅助进程执行，单项60秒/总180秒以真实截止及清理验证，不能用调用前检查代替。采集命令退出非零/空必需项/超时逐项失败，缺项使ACC-014失败但继续清理。防火墙证据必须有基线与当前差异。

正常停止和升级同一受控链：拒新变更→等在途→HTTP查询保持→优雅停止→必要取证/Job终止→补偿恢复/全审计→锁/控制权释放→stopped→关闭endpoint→SCM完成。审计失败继续在线failed；预停全程SCM保持RUNNING且不报STOP_PENDING/checkpoint；仅成功后第二阶段实际退出进度才更新checkpoint，失败不得发正常退出信号。内部终止性故障先failed，收敛成功可以stopped但last_run_outcome保持failed。

ACC-014由独立验收者仅据包回答实际触发组件和失败链路；ACC-015导出实际incident和运行产物并核验SHA；ACC-019运行实际停止/文件升级和失败保持在线时序，禁止以布尔自报代替观测。

### v2 必要审核引入细化

IMP-P04-03/06与ACC-014/019遵守总纲§16.1 IPC终止性和§17两阶段停服。原STOP_PENDING返回RUNNING草案撤销。真实注入验证条件转储/失败取证、有限预停结果、重复请求与当前实例结果，禁止采集错误算完整证据。

## PLAN-CHANGE-005 当前子方案承接

IMP-P04-06唯一负责完整两阶段编排，ACC-019逐项执行总纲§17六条约束；复用IMP-P04-03/04/05的有界证据、终止、补偿全审计。

当前共同规范为正式需求v3及总纲v5§17；本子方案逐条承接其完整参与者、输入输出、权限、触发顺序、硬期限、状态/恢复责任、失败重试、结束条件、原生证据及失效/重裁条件，不以本段摘要降低规范。必要复审通过前受影响实施仍暂停。


## PLAN-CHANGE-006回流（2026-09-10，待必要审核）

规范输入为当前需求v3和总纲v7§18；本节完整引用§18的采用理由、适用边界、硬时限、失效/重裁条件，不削弱记录034/035。旧版本身份只作历史追溯，不授予本次实施资格。

P04 IMP-P04-02/05负责总纲§18.4—6全部通知身份验证、目标专属条件转储、60/180秒及512MiB硬边界、助手实际结束、原子完整发布和incident合并。正常授权自行零退出以外的退出不能免除根因判断；核对原生句柄身份，拒绝错run/错创建时间/同名非受管对象。对照外部/自行退出与正常退出、超时/配额/迟到/部分文件证明从包内定位；API原型不代替冻结产品ACC-014。基础取证、原生异常与挂起原有合同完整保留。

P01→P02→P03→P04→P05顺序、既有REQ/OUT/ACC/IMP稳定ID不变。主会话保持作者/执行方，全部CHK开放；必要审核通过前本变更受影响产品实施暂停。

PC006-R01—R04修订承接：本Pxx依总纲v7§18完整执行自己的唯一责任。P01交付专用受管映像/不监测主管CLI和助手；P03保留真实目标句柄到有界P04完成并发布/失效一次性当前stop intent；P04只保存通知原始整数、不查询发起者，执行全局单飞1、拒绝≤1秒/零持久输出及临时512MiB、真实句柄确认和正常退出分类；P05验证这些跨层顺序、外部零码/旧intent/迟到/过早关句柄/同名突发负载与最终清理。完整路径过滤候选未通过，不作为fallback。状态仍为待必要复审，未获得新实施资格。
