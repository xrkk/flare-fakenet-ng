# P01 实施子方案三方审核记录 — 可部署的 MCP 服务入口

> 审核性质：首次第三方审核（同目录无本对象的历史 `审核`/`复审` 记录，强制全面审核）
>
> 审核模式：全面审核（先机械扫描，后逐项语义审查与反例检查）
>
> 会话角色：第三方审核方（独立、隔离上下文；未读取作者自评 `2026.09.02-07-自评-P01-可部署的MCP服务入口.md`）
>
> 轮次：第 1 轮
>
> 创建日期：2026-09-02
>
> 审核记录路径：`/home/adminn/projects/flare-fakenet-ng/PLAN/2026.09/2026.09.02/2026.09.02-08-审核-P01-可部署的MCP服务入口.md`

## 1. 对象身份

- 被审对象：`PLAN/2026.09/2026.09.02/2026.09.02-06-实施子方案-P01-可部署的MCP服务入口.md`
- 声明版本：`v1`；实测 Git blob：`5426d20469d2b0ae8d45dac32733fa5f4b7b1c18`（`git hash-object` 亲算，与声明一致）；文件 SHA-256：`313d2bef5da44bec7618d795ff90ad6d0b7831a3b5ef1f209fc1dca1735d3ec4`
- 状态行：`未审核, 不得实施`（与声明一致，身份门禁通过）
- Pxx：P01（执行顺序 P01→P02→P03→P04→P05 中最早未完成者；后序子方案不存在）
- 历史链：无（本对象首次被审计；无历史审计/处理记录）
- workflow_id：`fakenetng-mcp-20260902-6ada6479`

## 2. 规范性来源核验（全部亲算）

| 来源 | 声明 | 实测 | 结果 |
| --- | --- | --- | --- |
| 总纲 `2026.09.02-02-总纲-FakeNet-NG-MCP.md` | v2，blob `e5c734ea…`，状态 `已审核, 可以实施` | blob `e5c734ea6a5225f1191b72e1c5864ba722dbb3ea` | 一致 |
| 总纲审核闭环 `…-04-审核-…` | blob `f4dc1a13…` | `f4dc1a13832e1be259ff6f045f91416e3d8c14ad` | 一致（首次第三方审核，对象为总纲 v1） |
| 总纲审核处理 `…-05-审核处理-…` | blob `e0463092…` | `e0463092a3421801e71b8d35644b7a00d4a58906` | 一致（首次审核结果引入→总纲 v2） |
| 需求 `…-01-需求提炼-…` | 正式需求 v1，blob `b91ded55…` | `b91ded55e75b96e60f66c8d58125d8a220d62a56` | 一致 |
| `AGENTS.md` | SHA-256 `c84a7533…` | `c84a7533dc7f96bc5a759410bb9b2f6160a8bf26c22ade35090b6e6383a4a426` | 一致 |
| 决策记录 `…-长程执行/2026.09.02-01-自动决策记录-总纲-FakeNet-NG-MCP.md` | DEC-001—004 | 亲读全文，条目存在 | 一致 |

- 本地基线 HEAD：`4ac9abf28d4ec3527f93539345dcd7343ca48ea0`，分支 `process-redirect-v23`（`git rev-parse HEAD` 亲算一致）。
- 工作区边界：启动快照 `…-长程执行/启动时工作区快照-4ac9abf.txt` 存在；48 条用户既有改动属排除范围，未作为意见证据。

## 3. 审核范围与路径指纹

- 核心维护范围（子方案声称，审核确认其为"全部新建"）：`fakenet/mcp/**`、`test/mcp/**`、`fakenet-mcp.spec`、`tools/build_fakenetng_mcp_wine.py`、`Build-FakeNetNgMcpPackage.sh`、`wheelhouse/` 新增轮子与 `SOURCES.md` 增量、`Logs/fakenetng-mcp/**` 与 `dist/` 的 P01 产物。
- 直接依赖范围内容指纹（SHA-256，7/7 全量复核，非抽验）：
  - `Build-GuiVmPackage.sh` = `68dc5b41…` ✓；`tools/build_gui_vm_package_wine.py` = `fe176e83…` ✓；`tools/docker/gui-vm-diagnostic/Dockerfile` = `ea6ead15…` ✓；`setup.py` = `4ba1bac0…` ✓；`fakenet.spec` = `808d06b3…` ✓；`wheelhouse/SOURCES.md` = `a865f050…` ✓；`test/gui_vm/run_formal_stop_acceptance.py` = `af300758…` ✓（均与任务包声明一致）。
- 明确排除（未作为门禁）：`fakenet/fakenet.py`、`fakenet/diverters/**`、`fakenet/gui/**`、既有 GUI 测试与 v35 包装合同正文、用户既有未提交改动、其他 PLAN 目录、`research/`。

## 4. 机械扫描结果（第一步 a—i 全部完成）

- a) 身份与状态：路径/版本/blob/状态行与声明一致（见 §1、§2）。**通过**。
- b) 来源切片：子方案引用的总纲 §1.1/§4/§7/§8/§9/§10/§12、需求 REQ-002/014/015、CON-003、NON-004、RACC-002/003/016、记录 007/008/009/010/027/040、FB-001/FB-012/FB-013、AGENTS.md 四类条款均真实存在且语义一致（亲读原文）。例外一处引用失实见 AUD-P01-002（§3 表对总纲 §2.1 的转述）。**通过（含 1 处引用修正）**。
- c) 稳定 ID 与双向追溯：IMP-P01-01—09 唯一、无复用；ACC 沿用总纲全局 ID（ACC-002/003/016）+ 新增标签 ACC-017-PRE（见 AUD-P01-003）；§6 映射（结论 7/8/9/10/26/38 ↔ OUT-002/003/016/017入口 ↔ IMP ↔ ACC）正向反向均可恢复（IMP-P01-01/07/08/09 反向分别锚定 ACC-002/总纲§9.1/多 ACC，无悬空 IMP）。**通过**。
- d) 占位符扫描：无 checkbox、无完成百分比、无实施进度、无未决占位符、无并列未裁决方向（grep `[ ]|[x]|TODO|TBD|待定|占位|百分比|进度` 零命中；IMP-P01-01 探针方式为"允许裁决细节+DEC+阻断出口"，非未决占位）。**通过**。
- e) 后序依赖：全部 IMP 前置仅引用 IMP-P01-xx 与既有仓库设施；无 P02—P05 产出依赖。**通过**。
- f) ACC 完整性：ACC-002/003/016 均具备前置/输入/动作与精确预期/证据/通过-失败-阻断三边界；ACC-017-PRE 为前置证据登记（无三边界结构，随 AUD-P01-003 要求补齐 runner 侧语义）。**通过（含 1 处补强要求）**。
- g) fallback 扫描：IMP-P01-02 自研传输回落（触发条件=SDK 在 Wine/PyInstaller 冻结后不可用；出口=DEC+实测证据；协议仍 2026-07-28 非旧 transport，与需求记录 008"明确隔离兼容层"一致）；FB-001/FB-012/FB-013 引用与总纲 §11 一致；IMP-P01-01 探针不可行→真实阻断而非客户端替代。未发现静默 fallback。**通过**。
- h) 代码事实抽验：`fakenet/mcp/` 不存在（亲验）；`setup.py` 无 MCP 依赖（亲读全文，requirements 列表无 mcp）；`Build-GuiVmPackage.sh` 用法行 `[source-commit] [output-root] [output-directory]` 与子方案 F-05 及总纲 §10 一致（亲读脚本头部）；`wheelhouse/` 存在且含 `SOURCES.md`（亲验）；镜像检查命令与 AGENTS.md 一致且 `docker image inspect` 实测 ID = `sha256:1b33b518a7c71ecd13264f0d1c41faf254e301b8702e2da8ca11376cff700a5e`（F-04 亲验一致）；`fakenet.spec` `hiddenimports=[]`（F-07 亲验一致）。**通过（F-06 表述偏差见 AUD-P01-004）**。
- i) 在线事实 F-01/F-02：**已独立复核，非"作者声明"**。亲取 `https://modelcontextprotocol.io/specification/2026-07-28/basic/transports/streamable-http` 与 `.../basic/versioning`、`https://pypi.org/pypi/mcp/json`：单 MCP endpoint 仅 POST ✓；每请求必需 `Accept: application/json, text/event-stream`、`MCP-Protocol-Version`、`Mcp-Method`、（tools/call 等）`Mcp-Name` ✓；版本头与 `_meta` 不一致 `400`+`-32020 HeaderMismatch` ✓；未知版本 `400`+`-32022 UnsupportedProtocolVersionError` 含 `data.supported`（versioning 页原文示例逐字一致）✓；未知方法 `404`+`-32601` ✓；通知 `202` ✓；GET/DELETE `405` ✓；无 session、无 GET SSE、无 Last-Event-ID ✓；"Servers MUST implement server/discover"、无协商握手 ✓；"Servers MUST validate the Origin header"（本产品按用户裁决显式偏离，子方案 F-01 已如实标注）✓；PyPI `mcp` 2.1.1、`requires_python>=3.10`、v2 重写、声明支持 2026-07-28 及更早修订、含 Streamable HTTP 服务端 ✓。外发调用账本 `…-长程执行/外发调用账本.jsonl` 含对应抓取条目（egress-free-001/002）✓。

## 5. 语义审查与反例检查覆盖清单（第二步 1—8 逐项）

1. **总纲/需求/P01 合规**：范围恰为 OUT-002/003/016 + OUT-017 候选入口（§1.2 与总纲 §7/§8 逐条对照一致）；不预实现 P02 业务工具（工具面仅 `ping` 与协议内建方法，ACC-016 步骤 5 有负向证据位）；不触碰 FakeNet 生命周期/WinDivert/恢复审计/incident/正式计数（P03—P05 边界）。反例检查：未发现任何 P01 IMP 引入状态机、state.json、Job Object、配置管理工具或恢复审计。**通过，除 AUD-P01-001（ACC-016 丢"映射盘"条件）与 AUD-P01-002（service.json 引用与位置）**。
2. **子方案间无重复/冲突/悬空**：当前仅 P01。`singleinstance.py` 属 P01"Windows 服务"切片、不宣称 ACC-001，不构成对 P03 OUT-001 的验收侵占。**通过**。
3. **可独立实施与验收**：总纲 §8 P01 入口三项均满足（总纲 v2 已审核；构建设施 F-04 亲验存在；目标客户端 F-03 经本地文档行亲验+本会话 `mcp__Win10VM__*` 工具面佐证 host-only HTTP MCP 直连可用；VM 按 AGENTS.md 门禁重置内嵌于 IMP-P01-09）。不依赖后序成功；ACC 失败/阻断路径均可独立回流。**通过**。
4. **改动位置/数据/接口/顺序可直接实施**：落点文件均真实或由该 IMP 创建；vmrun/vmware/sc.exe/netsh/docker 命令均为既有真实形态；`Build-FakeNetNgMcpPackage.sh` 三参数形态仿照已亲验的 v35 wrapper。缺陷两处：IMP-P01-03 的 install 目标行为含"建防火墙规则"但其前置不含 IMP-P01-05（AUD-P01-007）；IMP-P01-08 runner 输入枚举少于总纲 §9.1 冻结输入（AUD-P01-006）。**通过，含 2 处顺序/接口修正**。
5. **方向性决定闭合**：§3 冻结表逐项对照总纲 §4——endpoint `/mcp`、SCM 服务名/显示名、默认端口 28788（配置可改）、探针工具名 `ping` 均属总纲 §4"分别由 P01/P02 冻结"的 P01 权限；协议基线、控制者 Header、必备状态、当前快照、数据目录五目录、LocalSystem、网络安全明文合同均为总纲冻结且未被改变；候选包形态/ctypes 服务运行时/mcp==2.1.1 与 DEC-001—004 完全一致（亲读决策记录）。`<candidate-id>` 公式 `mcp-c<commit[:8]>-<zip-sha256[:12]>` 与总纲 §9.1"commit+包 SHA-256+manifest 绑定"语义一致（manifest 经 runner 输入与 result.json 绑定，确定性 ZIP 下同包同 manifest）。**通过，service.json 一项见 AUD-P01-002**。
6. **ACC 只覆盖当前 Pxx 与直接依赖**：三个 ACC 均不含仓库级验证通过条件；ACC-017-PRE 明确"非 ACC-017 通过声明"，不冒充 ACC-017（命名合同问题见 AUD-P01-003）。**通过**。
7. **工作量/共享影响/兼容性/资源/数据一致性**：不改不覆盖 `Build-GuiVmPackage.*` v35 合同（新文件 only）；wheelhouse 只增不减、SOURCES.md 增量记录；`dist/`=分发物、`Logs/fakenetng-mcp/`=运行证据，符合 AGENTS.md 与总纲 §10；ProgramData 五目录与 REQ-014/总纲 §2.1 一致（service.json 见 AUD-P01-002）；F-06 前例表述见 AUD-P01-004；防火墙复验已含保护 Win10VM MCP 编排通道（28787 临时规则+恢复+删除）与负样本构造（临时次级 IP），不自伤、不越授权边界。**通过，含 2 处事实/引用修正**。
8. **独立需求保真门禁**（见 §6）。

反例检查补充结论：客户端静默落入旧 transport 时，ACC-002 失败边界（"静默落入旧 transport（GET SSE/session）或必需 Header 缺失"→FB-001/FB-013）+ IMP-P01-01 探针的"客户端不兼容 vs 通道不可用"区分 + 仅现代纪元服务端对 GET 回 405 的日志证据，判定点可观察；防火墙复验自伤问题已由 v1 修正闭合；单 endpoint/仅 POST/必需头在 ACC-002 步骤 1/3 有逐项证据位；无 Header 裸调用 `ping` 判 `missing` 与记录 021 只读边界一致。

## 6. 独立需求保真门禁（五项计数）

以需求文档当前有效结论为上游，对归属 P01 的 6 项内容逐项恢复"需求→总纲 OUT/Pxx→子方案 IMP/ACC"链：

- 结论 7（记录 007，P01 子句：可配置可迁移不硬编码）：§3 全参数可配置 + F-08 仅默认部署参数且明令不得硬编码 + install CLI 参数化 → REQ-015→OUT-017 入口→IMP-P01-03/06。**完整**。
- 结论 8（记录 008 全量）：F-01/F-02（亲复核）+ IMP-P01-02（Python 实现探针）/04（服务端）/06（打包）+ ACC-002（客户端发现+工具调用+头证据+服务启动；失败边界禁静默旧 transport）→ REQ-002→OUT-002→ACC-002。**完整**。
- 结论 9（记录 009 全量）：IMP-P01-03/05（host-only 绑定失败即拒绝启动、防火墙幂等单规则）+ ACC-003 步骤 1—4 + §3 无 TLS/token → REQ-002/CON-003→OUT-003→ACC-003。**完整**。
- 结论 10（记录 010 全量）：IMP-P01-04"完全不读取、不校验 Origin" + ACC-003 步骤 5 代码审查与 README/manifest 风险公开（含 DNS rebinding）→ CON-003/NON-004→OUT-003→ACC-003。**完整**。
- 结论 26（记录 027 全量）：IMP-P01-03（LocalSystem 默认、五目录、绝对路径、不读 profile）+ ACC-016 步骤 1—5 → REQ-014→OUT-016→ACC-016。**除外一项**：REQ-014/RACC-016/总纲 ACC-016 均要求"映射盘"条件独立性，子方案 ACC-016 步骤 4 仅覆盖 cwd 与 profile，映射盘无验收证据位（AUD-P01-001）→ **语义弱化 1**。
- 结论 38（记录 040，P01 子句：实施环境纪律）：Docker/Wine 仅出现于 IMP-P01-06/08 实施环境与 F-04/F-05 证据，不进入产品能力或 ACC 通过语义。**完整**。

- **应承接数：6；完整数：5；未落点数：0；语义弱化数：1；不可验收数：0。**
- 异常 ID：AUD-P01-001。
- 后三项非零（语义弱化数=1），按门禁规则不得给出审计通过。

## 7. 审核意见（第 1 轮，共 8 条）

### AUD-P01-001｜ACC-016 可执行形式丢失"映射盘"条件独立性检查（中等严重度）

- 来源层级：总纲层（ACC-016 合同）+ 需求层（REQ-014、RACC-016）。
- 反例：若实现存在映射盘（盘符映射）依赖（例如经映射盘相对路径读取资源），P01 按 IMP 全部完成后 ACC-016 仍会通过——REQ-014 的映射盘子句在 P01 内不可观察。
- 证据：总纲 §9.2 ACC-016 行"改变cwd/用户profile/映射盘条件"；需求 REQ-014（`2026.09.02-01-需求提炼…md` 第 118 行）"不得依赖登录用户 profile、当前目录、桌面、映射盘或交互式会话"；RACC-016（第 163 行）；子方案 §5 ACC-016 步骤 4（第 190 行）仅含 cwd 与 profile 两项。
- 风险：子方案把总纲 ACC-016 弱化为合同的真子集；子方案 §6 自报"完整承接数 6/语义弱化数 0"与事实不符（应为 5/1）。
- 影响：ACC-016、IMP-P01-03/07 目标行为、子方案 §6 计数、需求保真门禁结论。
- 最小闭合条件：在 ACC-016（步骤 4 或等效步骤）补回映射盘条件变化的可观察验证位（如：无任何映射盘环境下安装/启动并应答 `ping` 的证据 + 代码审查确认无盘符/映射盘相对路径引用），并同步修正 §6 计数。

### AUD-P01-002｜§3 "机器级服务配置文件"行转述总纲失实，且该文件位于总纲冻结的 ProgramData 枚举之外（低严重度）

- 来源层级：子方案层（§3 证据引用）+ 总纲层（数据目录冻结）。
- 反例：实施者按子方案引用会认为总纲 §2.1 明文允许 `configs\` 下新增任意文件；后续 Pxx 可援引该扩张解释在 ProgramData 根新增条目，稀释总纲 §2.1"产品数据最小集合：仅…"与 §4"数据目录（总纲冻结）"不变量。
- 证据：总纲 §2.1 原文"产品数据最小集合：**仅** `%ProgramData%\FakeNet-NG-MCP\configs\custom\`、`state\`、`logs\`、`baselines\`、`artifacts\`"；总纲 §4"数据目录…裁决权限=总纲冻结"；"至少分为"一语实为需求 REQ-014（第 118 行）措辞；子方案 §3 第 53 行把"至少划分为…"归属总纲 §2.1。
- 风险：引用不实构成子方案级扩展总纲冻结枚举的先例（`configs\service.json` 是五目录之外的 ProgramData 直下新文件）。
- 影响：§3 冻结表、IMP-P01-03 config/paths、ACC-016 步骤 3 布局核对。
- 最小闭合条件：把该行依据改写为"REQ-014'至少分为'允许 + service.json 系服务部署配置、不扩大产品数据最小集合、不改变五目录冻结"，或为该新增位置补一条 DEC 记录；无需移动文件位置（物理布局满足 REQ-014）。

### AUD-P01-003｜ACC-017-PRE 不在总纲 §9.1 冻结的 acc-id 词表内（低严重度）

- 来源层级：总纲层（§9.1 输出 schema 合同）。
- 反例：P05 汇总阶段若按总纲 §9.1 机械枚举 `<acc-id>`（总纲已定义的 ACC-001—019），`ACC-017-PRE` 目录会被判为非法 acc-id 或与 ACC-017 混淆。
- 证据：总纲 §9.1"`<acc-id>` 是本总纲已定义的实际ACC编号"；子方案 §5 ACC-017-PRE 与 IMP-P01-08 `--acc` 枚举（第 133 行）含 `ACC-017-PRE`。
- 风险：证据目录命名与冻结合同冲突，存在 ACC-017 通过状态被误读的残余风险（尽管正文已声明"非 ACC-017 通过声明"）。
- 影响：IMP-P01-08 runner、`Logs/fakenetng-mcp/<candidate-id>/` 结构、P05 证据汇总。
- 最小闭合条件：在子方案与 runner 中显式声明 `ACC-017-PRE` 是 P01 自有前置证据标签（非总纲 ACC 编号），result.json 保持该标签并携带"非 ACC-017 通过"标记，且为该标签定义通过/失败/阻断三边界；或改用非 ACC 命名空间的证据目录名。

### AUD-P01-004｜F-06 对 wheelhouse 前例的表述过强（低严重度，事实修正）

- 来源层级：代码事实层。
- 反例：实施者据 F-06 认为"Wine 内离线 pip 安装 wheelhouse 轮子"已有仓库先例；实际 v35 构建对 pydivert 轮子的用法是解压到 staging 目录并设 `PYTHONPATH` 供回归门禁使用，不是 pip 安装。
- 证据：`tools/build_gui_vm_package_wine.py` 第 120—140 行（`wheel = stage / 'wheelhouse' / PYDIVERT_WHEEL` → `archive.extractall(pydivert_root)` → `env['PYTHONPATH'] = pydivert_prefix`）；全文件 `wheelhouse` 仅第 120 行一处命中。
- 风险：IMP-P01-06"从 wheelhouse/ 离线安装 mcp==2.1.1 及依赖"被误认为已证实机制；它实为新验证点。
- 影响：F-06 事实表、IMP-P01-06 的前置认知。
- 最小闭合条件：F-06 改述为"离线轮子解压+PYTHONPATH 使用先例"，并在 IMP-P01-06 明示"Wine 内 pip --no-index 离线安装"为待实测验证步骤（现有构建门禁设计已隐含覆盖，需明示）。

### AUD-P01-005｜IMP-P01-09 的 ACC 执行顺序与 ACC-002 前置表述自相矛盾（低严重度）

- 来源层级：子方案层。
- 反例：若 ACC-016 最终失败（如服务账户非 LocalSystem），此前按"依次 ACC-002、ACC-003、ACC-016"采集的 ACC-002/003 证据产生于不合格安装之上，需重采；且"已按 ACC-016 先行安装"与"ACC-016 最后运行"字面冲突。
- 证据：子方案 §4 IMP-P01-09（第 142 行"依次运行 ACC-002…、ACC-003、ACC-016"）对照 §5 ACC-002 前置（第 156 行"候选包已按 ACC-016 先行安装"）。
- 风险：证据有效性顺序弱化；实施者对执行序产生歧义。
- 影响：IMP-P01-09 顺序、ACC-002/003 证据效力。
- 最小闭合条件：将 ACC-016（或至少其 `sc qc`/账户/布局核心断言）调整为先于 ACC-002/003 执行；或明确"安装按 ACC-016 程序先行完成、ACC-016 验证结论后置，若后置验证失败则同轮 ACC-002/003 证据标记为待重采"。

### AUD-P01-006｜IMP-P01-08 runner 输入枚举不全，无法直接满足 §9.1 冻结输入与 result.json 字段来源（低严重度）

- 来源层级：总纲层（§9.1 合同）。
- 反例：实施者按字面实现 `--source-commit/--package/--manifest/--acc` 四参数 CLI，则 `requirements_blob`、`master_plan_blob`、`environment_identity` 无输入来源，result.json 字段要么缺失要么被伪造。
- 证据：总纲 §9.1 输入清单（含需求 blob、审核通过后的总纲 blob、目标 VM 身份、配置身份）与 result.json 最小字段；子方案 IMP-P01-08（第 133 行）仅枚举四项输入。
- 风险：验收证据合同字段完整性受损。
- 影响：IMP-P01-08、全部 P01 ACC 的 result.json。
- 最小闭合条件：在 IMP-P01-08 输入清单补齐需求 blob、总纲 blob、目标 VM 身份、配置身份（P01 为 service.json 部署参数身份）四项，或声明其由 manifest/固定路径派生并显式写入 result.json。

### AUD-P01-007｜IMP-P01-03 的 install 目标行为引用尚未存在的防火墙模块，顺序不可直接实施（低严重度）

- 来源层级：子方案层。
- 反例：严格按顺序实施时，第 3 项 IMP-P01-03 的目标行为"fakenetng-mcp.exe install …幂等完成：写配置、创建服务…、建防火墙规则"要求调用 `firewall.py`，而该模块属第 5 项 IMP-P01-05（其前置恰为 IMP-P01-03）。
- 证据：子方案 §4 IMP-P01-03 目标行为（第 86 行"建防火墙规则"）与前置"IMP-P01-01/02 结论"（第 87 行）；IMP-P01-05 范围/前置（第 104、107 行）。
- 风险：顺序依赖倒置导致 IMP-P01-03 在其序列点不可完整验收。
- 影响：IMP-P01-03、IMP-P01-05 的顺序与验收。
- 最小闭合条件：把"建防火墙规则"从 IMP-P01-03 的 install 目标行为移入 IMP-P01-05（IMP-P01-03 阶段 install 仅写配置+创建服务），或令 IMP-P01-03 显式依赖 IMP-P01-05 并调整顺序声明。

### AUD-P01-008｜F-03/DEC-001 的本地文档证据未钉住插件版本路径，行号跨版本不稳定（低严重度，事实修正）

- 来源层级：子方案层（证据稳定性）。
- 反例：zcode-guide 插件缓存存在多版本（本地实测 cited 行号与 0.1.0 缓存副本一致，但当前会话插件清单已列 0.4.1）；插件升级后第 31—33、56、97 行内容漂移，F-03 证据链弱化。
- 证据：`/home/adminn/.zcode/cli/plugins/cache/zcode-plugins-official/zcode-guide/0.1.0/skills/diagnosing-mcp/SKILL.md` 第 31—33 行（`http`/`sse`: requires `url`; optional `headers`…）、第 55—57 行（url+headers JSON 示例）、第 96—98 行（`~/.zcode/cli/config.json` 的 `mcp.servers.<name>`…restart the session）与子方案 F-03 引用吻合；子方案/DEC-001 未钉住版本化路径。
- 风险：跨轮复核时行号证据不可稳定定位。
- 影响：F-03、DEC-001、IMP-P01-01 的客户端能力依据。
- 最小闭合条件：在 F-03/DEC-001 补记证据文件的版本化绝对路径（与行号一起），或改引稳定小节标题。

## 8. 结论

**存在不同意见**。

- 身份门禁、机械扫描、在线事实复核（F-01/F-02 亲证）、双向追溯、后序独立性、fallback 显式性、冻结权限与 DEC 一致性均通过。
- 但独立需求保真门禁五项计数为 6/5/0/1/0（语义弱化数非零，异常 AUD-P01-001：ACC-016 丢失"映射盘"条件），按门禁规则不得给出审计通过；另有 AUD-P01-002—008 共 7 条低严重度修正意见。
- 本结论只表示本轮发现；审计记录交回方案作者或执行方引入，审核方不决定引入后的处理路线。

## 9. 记录不可变性声明

本记录为首次第三方审核的不可变审计记录，创建后不再修改。意见 ID `AUD-P01-001—008` 跨轮稳定，供后续复审引用。
