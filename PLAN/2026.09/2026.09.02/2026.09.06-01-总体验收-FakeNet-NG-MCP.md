# 总体验收记录 — FakeNet-NG MCP 总纲（r58）

> 会话角色：`作者/执行方`
>
> 验收性质：MASTER_ACCEPTANCE（第三轮， 独立核验 CHK-013—029 修复后的完整重跑）
>
> workflow_id：`fakenetng-mcp-20260902-6ada6479`
>
> 验收日期：`2026-09-06`（r58c 证据修订重跑见 §7）
>
> 最终候选：`mcp-c94e7abdb-57b2ee8a7dde`（源 commit `94e7abdb141417f225a0e64e126232b8eaa76f4b`， 包 SHA256 `57b2ee8a7ddea36076883241f9653999168a267d98c72ccf03d6509482d31450`）
>
> 统一证据源：`dist/mcp-c94e7abdb-57b2ee8a7dde-release/release-manifest.json`

## 1. 对象身份与前置状态

| 项 | 值 | 核验 |
| --- | --- | --- |
| 总纲 | v3, blob `b7da9968…`（含 DEC-007/008 冻结行；DEC-009 为会话决策记录而非总纲冻结行——第 8 轮核验 CHK-050 指正） | 28 份 result.json 的 `master_plan_blob` 一致 |
| 需求 | blob `420e0288a027cc990c0d0b353aa3702ea64b67b9` | 28 份 result.json 的 `requirements_blob` 一致 |
| VM | `DESKTOP-3FI41GR`（Snapshot 185， DEC-009 用户裁决） | ACC-016 部署/安装/身份证据 |
| os_version | `10.0.19045.0` | release manifest `os_version` 字段 |

## 2. 本轮修复链（r52 → r58， 全部机械可归因）

| # | 缺陷 | 修复 commit | 验证 |
| --- | --- | --- | --- |
| 1 | ACC-004 六场景串行（6×服务重启）反复超时 | `fd29254` 拆分为 ACC-004-S1..S6 独立调用， 每场景前后归一化服务状态； restart_service 强杀兜底 | 六场景在 r54-r58 全 pass, S3 收敛 22s |
| 2 | rundll32 comsvcs 自转储竞态挂起服务线程（端点死亡， SCM 仍 Running） | `97f8346` 改进程内 dbghelp MiniDumpWriteDump | 转储 'ok' 1.3s, 无挂起线程 |
| 3 | coordinator/supervisor 锁反转 ABBA 死锁（py-spy 实锤： stop 持 supervisor 锁等 coordinator 锁， 保护停止路径持 coordinator 锁经 health_detail 等 supervisor 锁） | `038bf76` snapshot() 在元数据锁外采样健康详情 | ACC-004-S3/006/014 稳定通过； 回归测试双向验证 |
| 4 | ACC-008 框架扫描/ACC-019 升级探测用 ff48f64 之前的 `candidate\` 旧布局 | `3881326`/`04de520` | 两 ACC 在 r54+ pass |
| 5 | ACC-014 manifest 校验按工件注册表 schema 断言事件 manifest（r48 因字段名错误空洞通过， CHK-020 修复后暴露） | `0fa7cdb`/`50ca2ca` 对齐 `{item,result,size,sha256}` 真实 schema 并哈希校验 ok 项 | ACC-014 pass（真实验证非空洞） |
| 6 | 停止/恢复审计把 RPC/WMI 动态端口（≥49152）瞬态 LISTENING 当 FakeNet 残留 | `628cdee` 方向感知比较： 新增行仅 <49152 计残留， 消失行仅 <1024 计杀伤 | r58 normal 100/100 零审计失败 |
| 7 | 路由审计保留自动调优 metric 列； FakeNet 每轮 DNS 设置/恢复触发适配器重估致 metric 漂移（r54 第18轮/r56 第2轮概率性失败） | `cdb377b` 路由身份=目的/掩码/网关/接口， 弃 metric | r58 normal 100/100 零路由误报 |
| 8 | 产物登记每 stop 复制全部历史输出（r54 时累计 8319 工件/233 运行输出， 二次方拖慢 stop） | `177fee91` 按运行起始 mtime 只登记本运行产物 | r58 stop 全程 <10s |
| 9 | 审计失败不记录实际差异（三轮猜测式排障） | `94e7abdb` stop/恢复审计失败时记录差异 JSON | 本记录 §2 证据即来自该日志 |
| 10 | 运行器配套： P04 停止服务自愈(`ee036d5`)、探针失败取证(`58f945d`)、SSE 洪泛修复(`177fee91` 布局探测) | 同左 | 流水线无人值守通过 |

## 3. RACC→ACC 映射（按总纲 §9.2 语义）

| RACC | ACC | 合同要点 | 证据 |
| --- | --- | --- | --- |
| 001 | ACC-001 | 单实例/GUI 互斥/不自动启动 | ACC-001 pass |
| 002 | ACC-002 | 目标客户端互操作 | ACC-002 pass |
| 003 | ACC-003 | host-only 安全边界 | ACC-003 pass |
| 004 | ACC-004-S1..S6 | 控制链路全场景存活+fail-closed（六场景独立执行： 正常接管/初始化失败/未处理异常/无效防护/核心线程卡死/非受控退出） | 六份 result.json 全 pass（拆分决策见 §2#1） |
| 005 | ACC-005 | 封闭工具面零副作用 | ACC-005 pass |
| 006 | ACC-006 | 真实健康三条件+假活负样 | ACC-006 pass |
| 007 | ACC-007 | Job 收敛+SCM 恢复+不续跑 | ACC-007 pass |
| 008 | ACC-008 | 快照原子性+故障注入+无禁框架 | ACC-008 pass（五场景注入+布局修复后扫描） |
| 009 | ACC-009 | 配置管理全矩阵+审计 | ACC-009 pass |
| 010 | ACC-010 | 单控制者无超时接管 | ACC-010 pass |
| 011 | ACC-011 | 并发/重放/重启语义 | ACC-011 pass |
| 012 | ACC-012 | ≥100 次正常启停零残留 | **100 轮 pass**（50 builtin+50 custom, 零审计失败） |
| 013 | ACC-013 | 每类故障≥10 次收敛 | **50 轮 pass**（policy_pause/listener_stop/diverter_stop/child_hang/cleanup_error 各 10） |
| 014 | ACC-014 | incident 清单+定位 | ACC-014 pass（真实 schema 校验） |
| 015 | ACC-015 | 带外导出双端SHA+服务停止 | ACC-015 pass |
| 016 | ACC-016 | LocalSystem/session0/五目录/无映射盘 | ACC-016 pass |
| 017 | ACC-017 | 同一候选实机端到端 | final pass（identity/healthy/audit_clean/os 10.0.19045.0） |
| 018 | ACC-018 | 缺陷修复→重测链 | ACC-018 pass（历史5项+本轮 §2 链） |
| 019 | ACC-019 | 正常SCM停止/升级+并发拒绝 | ACC-019 pass |

**18/18 RACC 全部通过， 另有 ACC-019（源自记录 024/REQ-006 回流， 非需求 RACC 编号）一并通过——共 19 项 ACC 合同。** ACC-004 拆分不改变合同覆盖（六场景检查项与总纲 §9.2 RACC-004 行逐字对应， 仅执行边界由单进程改为六次独立调用； summary 期望集与 ACC-018 重测链已同步）。

## 4. 需求保真

所有主 OUT/主 ACC/最终负责 Pxx 落点在 r58 证据中可恢复（release manifest 28 条 acc_index 全 pass——27 项 ACC 条目 + ACC-017-SUMMARY 聚合标签, 与证据树 28 目录一一对应; missing/failures/wrong_candidate/integrity 全空）。

## 5. 已知限制（明示不阻断）

- ACC-001 的 GUI 互斥真机证据以单元测试代替（`--gui-exe` 空时跳过 GUI 腿）
- 独立核验第 3 轮 CHK-013—029 的修复在 r50-r58 分批落地， 本轮为修复后首次完整流水线
- Linux 实现为总纲既定 TODO
- 验收 VM 通道（Win10VM MCP 28787）本轮出现两次 SSE 瞬断（ACC-016 部署期、normal 第 29/50 轮）， 均为基础设施抖动， 重跑/断点续跑通过； 不属于产品链路

## 6. 环境与工具链快照

- 构建： Docker `flare-fakenet-ng/gui-vm-diagnostic-builder:py3119-pyi6220`（uid 1000）, `tools/build_fakenetng_mcp_wine.py`
- 测试门禁： Linux `test/mcp` 96 passed + 1 skipped； Wine 冻结冒烟随构建 PASS
- 流水线脚本： `Logs/fakenetng-mcp/r58-seq.py` + `r58-pipeline.sh`
- 中间候选证据保留： r52(`mcp-cc9cdea21-…`) r53(`mcp-c97f83468-…`) r54(`mcp-c038bf76b-…`) r55(`mcp-c628cdee7-…`) r56(`mcp-c177fee91-…`) r57(`mcp-ccdb377b4-…`) 目录（按 AUD-P05-002 作废旧候选发布证据， 仅作修复链历史）


## 7. 第 4/5 轮独立核验（CHK-030—035）修复与 r58c 证据修订重跑

第 4 轮核验（`2026.09.06-02`）开出 CHK-030—033； 第 5 轮（`2026.09.06-03`）判定 031/032/033 CLOSED, 030 OPEN（根因 CHK-034: r58b 脚本的 requirements blob 为 38 字符残缺转写, 缺真实 40 字符 blob 第 17-18 位 "c0"）, 另开 CHK-035（本记录计数口径 27 应为 28）。

| CHK | 修复 | 验证 |
| --- | --- | --- |
| 030/034 blob 残缺转写 | r58c 脚本以 `git hash-object` 实测值 `420e0288a027cc990c0d0b353aa3702ea64b67b9`（40 字符）为准 | 28 份 result.json requirements_blob 全等于实测值 |
| 031 environment_identity 无实机名 | 全部 runner 回填探测计算机名（commit `c87ef0e`） | 28 份均含 `DESKTOP-3FI41GR`（第 5 轮已判 CLOSED） |
| 032 S2/S4/S6 无探活时间线 | 三场景补持续探活并落盘（`c87ef0e`） | 六场景各有时间线证据（第 5 轮已判 CLOSED） |
| 033 RACC 计数失准 | §3 改为 18 RACC + ACC-019（第 5 轮已判 CLOSED） | — |
| 035 计数口径 27→28 | 本记录 §1/§4 同步为 28（含 ACC-017-SUMMARY 聚合标签） | acc_index 28 条与证据树 28 目录一一对应 |

r58c 重跑为两段执行, 全程留痕（`pipeline-c.log`）: 第一段 05:03 起跑, ACC-008 轮 1 于 05:12:00 因适配器竞态 fail 中止（该次 result.json 已被第二段覆写为 pass 版本, 失败过程存于 pipeline-c.log 与服务日志, 无错误 pass——第 6 轮核验已核实）；随即插入运行器修复 commit `73732ef`（仅 test/mcp/acceptance/run_p03_acc.py, 产品代码零改动, 候选二进制不变）；第二段 05:14:47 从 ACC-008 续跑至 05:27:45 完成。r58c 重跑（`Logs/fakenetng-mcp/r58c-seq.py` + `r58c-pipeline.sh`）: 同一候选 `mcp-c94e7abdb-57b2ee8a7dde`（包 SHA `57b2ee8a…` 不变, 产品源 commit `94e7abdb` 不变, `git diff 94e7abdb..HEAD -- fakenet/` 为空——第 5 轮已核实） 上重跑 27 项 ACC + normal + fault + final + summary 全部 pass； normal/fault 轮记录为断点续跑（同候选同 VM 同配置, 轮级记录不含 blob 字段, ACC-012/013/017 的 result.json 以正确参数重写）。§1 的 blob 一致性声明自 r58c 起成立。


## 8. 2026-09-09 独立核验后继续总体实施

**独立核验未通过, 继续总体实施**。此前全部完成/通过声明保留为历史候选结论，不代表当前 HEAD 的总体完成状态。

当前主会话已获用户授权，从独立核验方单向转换为作者/执行方。第1轮全面核查对象为需求正式v1、总纲v3及HEAD `95695d2fb24dc998b506582392eef0f23ba4d05b`；结论精确为`核查不通过`，新增CHK-053—066。不可变原记录已按用户要求迁至 `../2026.09.09/2026.09.09-01-独立核验-总纲-FakeNet-NG-MCP.md`，blob `860bc25a25851b77c1ac988d4015ee3fea7f3c9c`，本次不修改该记录。

总体状态：继续实施，尚未完成。当前新包不能继承r58/r58c旧候选证据；本主会话不得自行关闭CHK。最大有效核验轮次5，已完成1；后续独立Codex核验实例固定为`gpt-5.6-sol`/`high`，资格具备后创建并复用。契约变化须登记PLAN-CHANGE并回流，获得所需裁决、保真、自评及必要审核后才继续受影响实施。闭环状态见同目录 `独立核验整改闭环-FakeNet-NG-MCP.json`。不产生或关闭AUD，不改变审核批次额度。

### 8.1 2026-09-09 已采纳回流与新增停止点

用户采纳PLAN-CHANGE-001—004，额外端口集扩为28787/28790/29095/29094。规范回流进入必要审核；SCM STOP_PENDING不可合法回RUNNING的官方证据触发PLAN-CHANGE-005，等待两阶段停服或标准STOP失败退出的明确边界裁决。当前继续总体实施、不得声明完成；当前源码`a34394313a3a2662898adc8725c71a9cfd41836d`包含独立局部整改，Linux133/Wine128测试通过但全部CHK仍OPEN，未取得新候选原生ACC。详见2026.09.09目录02登记§8—9及唯一闭环状态文件。

### 8.2 PLAN-CHANGE-005采纳及实施资格恢复

用户采纳两阶段停服，07号必要方案复审通过（blob be52161aaf7bc34877ebb0f4da690432fa8465f9），计划保真40/40/0/0/0/0。当前继续总体实施；已完成独立Job进程、SCM预停及完整审计的中间代码和本地回归，完整原生ACC及独立CHK仍未通过。不得继承旧整体完成声明。
