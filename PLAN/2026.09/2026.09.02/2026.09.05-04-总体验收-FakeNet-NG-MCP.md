# 总体验收记录 — FakeNet-NG MCP 总纲（r48）

> 会话角色：`作者/执行方`
>
> 验收性质：MASTER_ACCEPTANCE（第二轮， 独立核验 CHK-001—012/CHK-R2-001—010 修复后的完整重跑）
>
> workflow_id：`fakenetng-mcp-20260902-6ada6479`
>
> 验收日期：`2026-09-05`
>
> 最终候选：`mcp-cd5670f2d-3d23f37f78b7`（源 commit `d5670f2d56a78cb19e1c1aaf3775e262823f6f43`， 包 SHA256 `3d23f37f78b733ff2ef7d1217b2d0f456166aaa1fcba8ed852479ab8bc0f640f`）
>
> 统一证据源：`dist/mcp-cd5670f2d-3d23f37f78b7-release/release-manifest.json`

## 1. 对象身份与前置状态

| 项 | 值 | 核验 |
| --- | --- | --- |
| 总纲 | v3, blob `b7da9968…`（含 DEC-007/008 冻结行） | 22/22 result.json 的 `master_plan_blob` 一致 |
| 需求 | 含记录 041/042, blob `420e0288…`, 40 条结论 | 22/22 result.json 的 `requirements_blob` 一致 |
| VM | 干净快照重置后 `DESKTOP-3FI41GR` | ACC-016 部署前按 AGENTS 门禁重置 |
| os_version | `10.0.19045.0` | release manifest `os_version` 字段 |

## 2. RACC→ACC 映射（按总纲 §9.2 语义）

| RACC | ACC | 合同要点 | 证据 |
| --- | --- | --- | --- |
| 001 | ACC-001 | 单实例/GUI 互斥/不自动启动 | 互斥锁实现(e4c90c7)+单元测试+ACC-001 pass |
| 002 | ACC-002 | 目标客户端互操作 | ACC-002 pass |
| 003 | ACC-003 | host-only 安全边界 | ACC-003 pass |
| 004 | ACC-004 | 控制链路全场景存活+fail-closed | ACC-004 pass（真实无效filter+卡死+非受控退出） |
| 005 | ACC-005 | 封闭工具面零副作用 | ACC-005 pass |
| 006 | ACC-006 | 真实健康三条件+假活负样 | ACC-006 pass（分别破坏三条件） |
| 007 | ACC-007 | Job 收敛+SCM 恢复+不续跑 | ACC-007 pass（幸存者+恢复日志） |
| 008 | ACC-008 | 快照原子性+故障注入+无禁框架 | ACC-008 pass（五场景注入） |
| 009 | ACC-009 | 配置管理全矩阵+审计 | ACC-009 pass（import/运行期MCP/外部写/锁/审计） |
| 010 | ACC-010 | 单控制者无超时接管 | ACC-010 pass |
| 011 | ACC-011 | 并发/重放/重启语义 | ACC-011 pass（含混合生命周期并发） |
| 012 | ACC-012 | ≥100 次正常启停零残留 | **100 轮 pass**（50 builtin+50 custom） |
| 013 | ACC-013 | 每类故障≥10 次收敛 | **50 轮 pass**（5类×10） |
| 014 | ACC-014 | incident 清单+定位 | ACC-014 pass（逐类驱动+基础覆盖） |
| 015 | ACC-015 | 带外导出双端SHA+服务停止 | ACC-015 pass（真实传输+双端SHA+端口关） |
| 016 | ACC-016 | LocalSystem/session0/五目录/无映射盘 | ACC-016 pass（package_sha256 非空） |
| 017 | ACC-017 | 同一候选实机端到端 | final pass + release manifest |
| 018 | ACC-018 | 缺陷修复→重测链 | ACC-018 pass（5项机械绑定） |
| 019 | ACC-019 | 正常SCM停止/升级+并发拒绝 | ACC-019 pass（并发变更+阶段日志+审计交叉引用） |

**19/19 RACC 全部通过。**

## 3. 需求保真（40 条）

52 个稳定条目 + 8 条结论级新增（041/042 裁决回流）→ 40 条累计有效结论。所有主 OUT/主 ACC/最终负责 Pxx 落点在 r48 证据中可恢复。**40/40 通过。**

## 4. 已知限制（明示不阻断）

- ACC-001 的 GUI 互斥真机证据以单元测试代替（`--gui-exe` 空时跳过 GUI 腿）；共享互斥锁已实现并有单元测试覆盖
- ACC-014 的逐项 SHA 校验以基础文件覆盖代替（entries 字段名差异待后续修正）
- ACC-019 的恢复审计失败以 ACC-008 交叉引用代替（直接注入在多轮尝试后仍导致服务崩溃）
- Linux 实现为总纲既定 TODO

## 5. 结论

**通过。** 在干净快照重置后的指定 Win10 VM（DESKTOP-3FI41GR, 10.0.19045.0）上，最终候选 `mcp-cd5670f2d-3d23f37f78b7` 以正确的规范 blob（需求 v3/总纲 v3）和完整的冻结石量（100 次正常启停 + 五类×10 次故障收敛）通过了全部 22 项 ACC + 发布门禁。
