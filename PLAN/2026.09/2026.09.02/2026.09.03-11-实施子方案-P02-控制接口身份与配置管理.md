# 实施子方案 P02 — 控制接口、身份与配置管理

> 子方案路径：`/home/adminn/projects/flare-fakenet-ng/PLAN/2026.09/2026.09.02/2026.09.03-11-实施子方案-P02-控制接口身份与配置管理.md`
>
> 子方案版本：`v1`
>
> 状态：`已审核, 可以实施`（首次第三方审核 `2026.09.03-13-审核-P02-…` 已引入并全部闭合，处理记录 `2026.09.03-14-审核处理-P02-…`；未命中再次审核触发条件，作者自评闭环）
>
> 会话角色：`作者/执行方`
>
> 创建日期：`2026-09-03`
>
> workflow_id：`fakenetng-mcp-20260902-6ada6479`

## 1. 对象身份与来源

- 总纲：`2026.09.02-02-总纲-FakeNet-NG-MCP.md`，`v2`，blob `e5c734ea6a5225f1191b72e1c5864ba722dbb3ea`，`已审核, 可以实施`（其审核闭环为同目录 04/05 记录）。
- 需求：`2026.09.02-01-需求提炼-FakeNet-NG-MCP.md`，`正式需求 v1`，blob `b91ded55e75b96e60f66c8d58125d8a220d62a56`。
- 前序：P01 已 CHILD_DONE（实施记录 `2026.09.02-10`，候选 `mcp-c32803520-672e070e8b81` 四项验收全 pass），P02 入口满足。
- 生效项目约定：仓库根 `AGENTS.md`（SHA-256 `c84a7533…a426`）。
- 顺序：总纲 §1.1/§8；P02 依赖 P01 交接（transport、Header 能力、服务身份、数据目录、机器可检验 schema 稳定），不依赖 P03—P05。

## 2. P02 切片（总纲 §7/§8/§9/§12）

- 主责 OUT：OUT-005（封闭工具与完整只读诊断→ACC-005）、OUT-010（稳定单控制者→ACC-010）、OUT-011（串行、版本与幂等→ACC-011）。
- 部分承接：OUT-009 的 API/文件操作/审计语义与自动化前置证据；真实 Windows 活动文件锁、FakeNet 读取兼容与 Windows 重解析点逃逸的真机阻断验证全部归 P03 最终验收（本方案交付重解析点判定的代码钩子与 Linux 可构造的逃逸矩阵；ACC-009-PRE 明确声明该排除，不冒充完整 ACC-009）。
- 排除：WinDivert、Job Object、SCM 恢复、真实活动文件锁、环境恢复审计、incident、真实 FakeNet-NG 生命周期（P03）；正式 100/10 计数（P05）。
- 测试替身：`fakenet/mcp/testdouble.py` 提供可注入的生命周期替身（`stopped→starting→healthy→degraded/failed` 状态机 + 健康探测 + 受控停止），供生命周期工具在本阶段运行；P03 换成真实主管而不改工具合同。

## 3. P02 冻结的纯实现细节（总纲 §4 授权 P02 冻结项）

| 冻结项 | 值 | 依据 |
| --- | --- | --- |
| 工具命名 | snake_case：`ping`、`get_status`、`get_events`、`list_configs`、`validate_config`、`create_config`、`import_config`、`read_config`、`edit_config`、`rename_config`、`delete_config`、`load_config`（启动用）、`start`、`stop`、`restart`、`list_artifacts` | 总纲 §4（工具方法名为实现细节；能力合同不变） |
| 结构化错误码 | `controller_identity_missing`、`controller_conflict`、`state_conflict`、`operation_busy`、`duplicate_command`（非错误，幂等回放）、`config_not_found`、`name_conflict`、`version_conflict`、`path_escape_blocked`、`config_in_use`、`validation_failed`、`invalid_request`、`builtin_readonly`、`internal_error` | 总纲 §6.1“结构化失败类别”；JSON 字段拼写由 P02 冻结并进 schema |
| 变更请求公共字段 | `command_id`（UUID）、`expected_state_version`（int）；配置写操作另需 `expected_sha256` 或 `expected_version`（实现取 `expected_sha256`） | 总纲 §6.1 |
| 响应公共字段 | `state`、`state_version`、`run_id`（或 null）、`changed`（bool）、`error`（结构化或 null）、`command_id` 回显 | 总纲 §6.1 |
| 配置版本语义 | 版本=内容 SHA-256（无独立版本号文件）；冲突=请求 `expected_sha256` ≠ 当前 SHA-256 | 记录 033“所见版本或 expected_sha256” |
| 审计日志 | `%ProgramData%\FakeNet-NG-MCP\logs\config-audit.jsonl`，逐行 JSON：`timestamp`、`controller`、`command_id`、`target`、`operation`、`before_sha256`、`after_sha256`、`result`；失败/冲突/未落盘尝试同样记录（result 为对应错误码或 `ok`/`no_change`） | 总纲 §6.2（CON-006 普通日志边界；不驱动恢复/重放） |
| 近期事件 | 内存环形缓冲（容量 500 条）+ `get_events` 只读导出；进程重启即清空（不是事件重放源） | 记录 003“近期事件查询”；CON-006 |
| command 幂等范围 | 当前进程内 `command_id` 结果缓存（LRU 256）；重启后不保留（“MCP 崩溃后不续接命令”） | 记录 023/REQ-010 |
| 状态版本 | 单调递增 int，起点 1；每次被接受的变更 +1；替身状态迁移同样推进 | 记录 023 |
| 校验实现 | `Fakenet().parse_config(path)`（`fakenet/fakenet.py:151`，ConfigParser 语义）作为语法/基础语义校验；文件不存在时 parse_config 会 `sys.exit(1)`，校验器须先自检存在性 | 仓库事实（本方案勘察） |
| 运行期/停止态写授权分界 | 运行期（有活动 run/控制者）：全部配置写操作仅当前控制者，且活动配置任何写/改名/删除→`config_in_use`；停止态（无活动 run）：任何携带格式有效控制者头的连接可写，逐配置 `expected_sha256` 乐观并发 | 记录 032/033（REQ-008/REQ-010） |
| 校验顺序 | 控制者身份校验先于 `command_id` 回放：不同控制者携带他人 `command_id` → `controller_conflict`（防跨身份重放劫持）；同控制者重放→回放原结果 | 记录 018/023 语义推论 |
| 审计先行提交 | 写操作顺序：临时文件→审计追加（含前后 SHA 与预期结果）→`os.replace`；审计追加失败则中止提交（删临时文件）并返回 `audit_write_failed`——失败/冲突/未落盘尝试同样必须审计 | 记录 032/033“审计完整性” + CON-006 |

## 4. 实施项 IMP

### IMP-P02-01｜结构化错误与状态/命令协调器

- 范围：`fakenet/mcp/errors.py`（错误码枚举与 `McpError`）；`fakenet/mcp/coordination.py`：`Coordinator`——当前状态/`state_version`/`run_id`/活动 `controller_id`/活动操作/`command_id` 结果缓存；`submit(command_id, expected_version, controller, kind, fn)` 串行执行（单锁）、版本比较、幂等回放、冲突立即结构化拒绝（不排队）；替身注入接口。
- 现状：P01 仅有 ping 与传输守卫。
- 目标行为：同一时刻至多一个变更执行；身份/版本/状态/活动操作四类冲突可判；`changed=false` 对无状态变化操作。
- 前置：P01。顺序：第 1 项。
- 实际落点：`fakenet/mcp/{errors,coordination}.py`。
- 允许裁决细节：锁实现、缓存容量、事件缓冲内部结构。

### IMP-P02-02｜配置存储（自定义根管理 + 逃逸阻断 + 原子替换 + 审计）

- 范围：`fakenet/mcp/configstore.py`：根目录解析（`ProgramData\FakeNet-NG-MCP\configs\custom\`）、名称规则（`[A-Za-z0-9._-]+\.ini`，禁 `\`、`/`、`..`、绝对路径、驱动器前缀）、规范化后仍须以根为前缀；拒绝符号链接（`os.path.islink`）、硬链接（`st_nlink > 1` 检查）与 Windows 保留设备名（CON/PRN/AUX/NUL/COM1—9/LPT1—9，含任意扩展名）；同卷 `os.replace` 原子替换；重解析点判定钩子交付、真机验证归 P03；内置配置目录只枚举只读；写操作经临时文件→校验→`os.replace`；创建/导入默认拒绝覆盖（`name_conflict`，显式 `overwrite=false` 参数不存在）；全部操作（含失败/冲突/未落盘）写审计 JSONL。
- 目标行为：§3 冻结全部可观察；`expected_sha256` 冲突→`version_conflict`；活动配置名登记后 `config_in_use`。
- 前置：IMP-P02-01。顺序：第 2 项。
- 实际落点：`fakenet/mcp/configstore.py`。
- 允许裁决细节：临时文件命名、内置目录枚举的缓存。

### IMP-P02-03｜生命周期替身与生命周期/状态/事件工具

- 范围：`fakenet/mcp/testdouble.py`（状态机+可配置失败/降级注入+健康探测字段）；工具 `get_status`（状态、版本、run_id、失败原因、配置身份、控制者、健康明细）、`get_events`、`start`/`stop`/`restart`（经 Coordinator 串行；`restart` 绑定原 `run_id` 语义在替身上实现——stop+start 同 command 域）；启动前置：`load_config` 指定配置（校验+登记活动配置身份）。
- 目标行为：`start` 要求有效控制者+配置+版本匹配；替身迁移推进版本；无控制者变更调用→`controller_identity_missing`；非控制者→`controller_conflict`。
- 前置：IMP-P02-01/02。顺序：第 3 项。
- 实际落点：`fakenet/mcp/testdouble.py` + `fakenet/mcp/tools.py`（工具注册与 schema 描述）。
- 允许裁决细节：替身迁移耗时、事件字段集。

### IMP-P02-04｜配置工具与产物枚举、只读诊断面

- 范围：`list_configs`（内置+自定义，含 SHA-256/大小）、`validate_config`、`create_config`（正文写入）、`import_config`（同 create，语义别名）、`read_config`（返回原始 ini 正文——非产物正文下载边界：仅配置域内且 ≤1 MiB）、`edit_config`（全文替换，`expected_sha256`）、`rename_config`、`delete_config`、`list_artifacts`（`artifacts\` 树的受管路径+类型+大小+完成状态+SHA-256；无正文）。
- 目标行为：全部只读工具对任何可达连接可用且零副作用；变更工具按控制者/版本/幂等合同；内置目录任何写操作→`builtin_readonly`。
- 前置：IMP-P02-02/03。顺序：第 4 项。
- 实际落点：`fakenet/mcp/tools.py`。
- 允许裁决细节：正文大小上限数值、列表分页。

### IMP-P02-05｜测试（宿主 + Windows-Python 双侧）

- 范围：`test/mcp/test_coordination.py`（串行/版本/幂等/冲突矩阵/崩溃后缓存清空）、`test_configstore.py`（全部操作、逃逸矩阵：`..`、绝对、盘符、符号链接、硬链接（Linux 可建则测）、保留设备名、覆盖拒绝、SHA 冲突、审计逐条字段、活动配置 edit/rename/delete→`config_in_use`、运行期非控制者写→`controller_conflict`）、`test_tools.py`（工具 schema、只读零副作用快照、负向矩阵：无身份/错身份/旧版本/重复 command_id/未知工具、内置只读）、`test_audit.py`（每类操作及失败/冲突的审计字段完整性）。
- 目标行为：`python3 -m pytest test/mcp -q`（宿主 venv）与 Wine Windows-Python 门禁全过（计划跳过集显式声明）。
- 前置：IMP-P02-01…04。顺序：第 5 项（与 06 交叉）。
- 实际落点：`test/mcp/*`。

### IMP-P02-06｜候选包重建

- 范围：用 IMP-P01-06 构建链（不变更 v35 合同）从 P02 源快照构建候选 `mcp-rN`（新 candidate-id；manifest 增记 P02 工具清单）。
- 前置：IMP-P02-05。顺序：第 6 项。
- 实际落点：`dist/mcp-rN/`。

### IMP-P02-07｜P02 ACC runner 与执行

- 范围：`test/mcp/acceptance/run_p02_acc.py`（总纲 §9.1 合同同 P01：输入/输出/result.json/退出码）；`--acc ACC-005|ACC-010|ACC-011|ACC-009-PRE`。执行方式：候选部署到验收 VM（复用 P01 部署器，重装服务），全部断言经 host-only HTTP（目标客户端=SDK Client+raw HTTP；ACC-010 的真实客户端 Header 长离线无接管语义以 SDK/原始双连接+替身时间轴证据呈现——客户端身份能力已由 P01 ACC-002 冻结，此处为服务端语义验收）。
- ACC-005：所有只读工具前后状态快照零差异 + 负向矩阵（任意命令、任意路径、未授权变更、正文下载不可达——`read_config` 越权与 `list_artifacts` 无正文路由）。
- ACC-010：双连接（A 启动成为控制者；B 只读；A 断开重连同 ID 可控、换 ID 被拒；无 Header 只读；等待窗口内无接管）。
- ACC-011：并发 start/stop/edit 竞态、重复 `command_id` 回放、过期版本、真实服务重启（经 Win10VM MCP `sc stop`/`sc start` 使服务进程真实退出重启）后旧 `command_id` 不被续接/回放。
- ACC-009-PRE：P02 自有前置证据标签（非总纲 ACC 编号，三边界同 P01-ENTRY 模式）：内置负向、自定义全管理、穿越/链接/保留名阻断、活动配置 `config_in_use` 拒绝、运行期/停止态写授权分界、逐类操作+失败/冲突/未落盘尝试的审计字段完整性；明确声明“真实活动锁、FakeNet 读取兼容与重解析点真机验证归 P03，本标签不构成 ACC-009 通过”。
- 前置：IMP-P02-06。顺序：第 7 项。
- 实际落点：`test/mcp/acceptance/run_p02_acc.py`、`Logs/fakenetng-mcp/<candidate-id>/`。

## 5. 验收项 ACC（总纲 ACC 的可执行形式）

由 IMP-P02-07 runner 执行；证据/退出码合同同 §9.1。三边界逐项写入 runner 断言：

- **ACC-005**：通过=全部只读工具零副作用（快照 diff 空）且负向能力全部结构化拒绝；失败=任何越权/缺失诊断/副作用；阻断=替身或服务不可运行（工具错误）。
- **ACC-010**：通过=仅启动者（同稳定 ID）可控、其他/无效 ID 只读、断开重连后同 ID 仍控、等待窗口无自动接管；失败=按连接/源猜测或自动释放/接管；阻断=双连接设施不可用。
- **ACC-011**：通过=竞态串行或结构化冲突、无排队/覆盖/重复执行、同 `command_id` 回放原结果、“重启”后旧命令不续接；失败=任何竞态副作用；阻断=负载设施错误。
- **ACC-009-PRE**（P02 标签）：通过=内置负向+自定义全管理+穿越/链接阻断+审计字段完整（含失败/冲突尝试）；失败=任一逃逸、静默覆盖或审计缺字段；阻断=文件系统不支持所需语义。重解析点与真实活动锁明确排除（P03）。

## 6. 需求保真门禁（P02 范围）

- 应承接数：`13`（按记录号）：记录 012、013、023、031 全量；记录 003 的封闭工具/只读诊断/负向边界子句；记录 014 的“活动配置锁定接口”子句（真实锁归 P03）；记录 018/020/021 的控制者/幂等服务端语义子句；记录 022 的“断联不参与判定”服务端语义子句；记录 028—030 的“产物仅元数据”边界子句；记录 032 的运行期配置权与审计子句；记录 033 的停止态乐观并发与审计子句；记录 038 经 REQ-010 的“崩溃后不续接命令”子句。
- 完整承接数：`13`；未落点数：`0`；语义弱化数：`0`；不可验收数：`0`。
- 核对方式：记录 012/013→OUT-009(P02 部分)→IMP-P02-02/04+ACC-009-PRE；记录 023→OUT-011→IMP-P02-01+ACC-011；记录 031→OUT-009(P02 部分)→IMP-P02-02（停止态乐观并发）+ACC-009-PRE；记录 003 子句→OUT-005→IMP-P02-03/04+ACC-005 负向矩阵；记录 014 子句→活动配置登记与 `config_in_use` 拒绝（IMP-P02-02/05）；记录 018/020/021 子句→OUT-010 服务端语义→IMP-P02-01/03+ACC-010；记录 022 子句→无任何计时器（IMP-P02-01 断言）+ACC-010 等待窗；记录 028—030 子句→`list_artifacts` 仅元数据（IMP-P02-04）+ACC-005 负向；记录 032 子句→运行期写授权分界（§3 表新增行）+审计+ACC-009-PRE；记录 038 子句→命令缓存进程内 LRU、重启即失（§3 表）+ACC-011 真实重启步。

## 7. 版本变更记录

- **v0 / 2026-09-03：** 首次编写。IMP-P02-01—07、ACC-005/010/011 可执行形式与 ACC-009-PRE 标签、§3 冻结表、需求保真门禁。无删除或复用 ID。
- **v1 / 2026-09-03：** 引入首次第三方审核（AUD-P02-001—012，全部同意）。§6 保真门禁重计为 13 项并修正记录号映射（001/002）；重解析点边界统一为“代码钩子在本方案、真机验证归 P03”（003）；§3 新增运行期/停止态写授权分界、控制者校验先于命令回放、审计先行提交三行冻结（005/010/012）；configstore 补硬链接 `st_nlink` 与 Windows 保留设备名（006/011）；测试与 ACC-009-PRE 补 `config_in_use`、写授权分界覆盖（004/005）；ACC-011 改真实服务重启验证（007）；修头部自述路径、悬空 §9.1 引用与 parse_config 行号（008/009）。IMP/ACC 编号不变。

## 8. 审核重点

- 工具面是否恰好覆盖总纲 §6.1 能力合同而无任意命令/路径/正文下载扩张（`read_config` 返回 ini 正文是否越界——依据：记录 012 授予自定义 ini 的完整读写域，产物正文禁令针对 logs/PCAP/报告，非配置域）。
- 幂等/版本/冲突语义与记录 023 逐条一致；无超时/租约/队列残留（对照 NON-007）。
- 审计日志字段与记录 032/033 及总纲 §6.2 完全一致，且不构成重放源。
- 逃逸矩阵完备性（`..`/绝对/盘符/符号/硬链接；重解析点为何属 P03 且不构成 P02 弱化——P02 自动化在 Linux/无特权环境不可构造）。
- 替身边界：不预实现 P03 的 WinDivert/Job/快照/真实锁。
- runner 对 §9.1 输入/退出码合同的延续性与 ACC-009-PRE 标签三边界。
