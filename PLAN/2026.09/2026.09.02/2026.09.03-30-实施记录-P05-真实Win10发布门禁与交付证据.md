# 实施记录 — P05 真实 Win10 发布门禁与交付证据

> 会话角色：`作者/执行方`
>
> workflow_id：`fakenetng-mcp-20260902-6ada6479`（长程实施授权进行中）
>
> 实施子方案：`PLAN/2026.09/2026.09.02/2026.09.03-26-实施子方案-P05-真实Win10发布门禁与交付证据.md`，`v1`（blob `4c070328…`），状态 `已审核, 可以实施`
>
> 审核闭环：`2026.09.03-28-审核-P05`（9 AUD 含 1 高危）+ `2026.09.03-29-审核处理-P05`（全部闭合，作者自评闭环）
>
> 审核基线 HEAD：`39a2dcf`；分支：`process-redirect-v23`
>
> 状态：`进行中`（发布门禁执行中， 2026-09-03）

## IMP/ACC 总览（当前）

| 项 | 状态 |
| --- | --- |
| IMP-P05-01 发布 runner | 完成（`run_p05_release.py`： normal/fault/final/summary 四模式、逐轮断点续跑、五节规范化审计、连续探活、VM 连续性、FakeNet 端口归因收窄） |
| IMP-P05-02 100 次正常矩阵 | **进行中**（在最终候选上重跑； 见下方门禁沿革） |
| IMP-P05-03 五类×10 故障矩阵 | 未开始（待 100 轮通过后） |
| IMP-P05-04 ACC-017 final/summary | 未开始 |
| IMP-P05-05 记录与提交 | 本记录即部分产物 |

## 发布门禁沿革（缺陷—修复—候选更替， 全部按 AUD-P05-002 作废旧候选证据）

| 候选 | 门禁结果 | 根因与修复 |
| --- | --- | --- |
| r35 `mcp-c9bafd2d6…`（源 `9bafd2d6`） | ACC-001—019+FAULT-POINTS 全 pass； normal 矩阵第 29 轮失败 | **产品缺陷1**： 进程内模型下 FakeNet.stop() 后 listener 套接字泄漏（port 80 由 MCP 进程持有； 下轮启动 10048）。修复： supervisor 停止/回滚后强制清扫 listener server/sock 套接字（提交含 `force-close listener sockets`） |
| r36 `mcp-c3e219062…`（源 `3e219062`） | ACC-001—019+FAULT-POINTS 全 pass； normal 矩阵第 8 轮失败 | **产品缺陷2**： supervisor.stop 内部基线核对用了原始五节文本 diff（netstat PID/瞬态行噪声→"environment differs"误报）。修复： 改用 P04 规范化 `audit_compare`（`full_audit_diff`） |
| r37 `mcp-cb32303e1…`（源 `b32303e1`）当前 | ACC-016/002/003/P01-ENTRY/005/011/009-PRE pass； ACC-010 两次 `owner_reconnect_still_controls=false` | **新暴露（未修复）**： 前轮停止仍留 `healthy=False`（PayloadReportError 路径）且 SMTPListener port 25 下轮启动 10048 —— 套接字清扫未覆盖 SMTPListener 的持有结构； 同时停止返回 `failed` 使 ACC-010 的 owner 停止断言失败。**这是当前待修复的根因** |

## 当前精确检查点（恢复入口）

1. 最终候选（暂定）＝ r37 `mcp-cb32303e1-91355a36cacb`（commit `b32303e17d3e557bd3c3ae8ac3c8117ec63106db`， ZIP SHA `91355a36cacb2b1688a4fb5196829b751035e87d2c87861c80db0f6a94a49332`）， 已部署在干净快照后的 VM 上。
2. 已在该候选通过： ACC-016、ACC-002、ACC-003、P01-ENTRY、ACC-005、ACC-011、ACC-009-PRE。
3. 待修复缺陷（P03/P04 生命周期职责）： FakeNet 各类 listener（已知 SMTPListener port 25， 可能还有其他）停止后套接_sock.et 未释放； `PayloadReportError('cannot generate payload report before paired PCAP closes')` 使停止阶段 `healthy=False`。修复方向： 扩大清扫覆盖（按 listener 类型逐一核对其套接字持有属性）或让 FakeNet 停止路径在进程内模型下可靠释放。修复后**必须新建候选**（r38+）， 且 r37 全部发布证据按 AUD-P05-002 作废， 在新候选上重跑全部 ACC + 100 轮 + 五类×10 + final/summary。
4. VM 状态： 已从干净快照重置并部署 r37； 服务当前 `failed`（无活动 run）。
5. 发布 runner 已就绪： `test/mcp/acceptance/run_p05_release.py`（normal 矩阵约 40 分钟、fault 矩阵约 90 分钟， 均后台分段可续跑）。

## runner/工具侧已完成的修复（非产品缺陷）

- 发布 runner： FAULTS 导入、repo sys.path、capture 成功标记、完整 diff 载荷、FakeNet 端口归因。
- P02 runner： 生命周期超时 300s、idle 卫生、healthy 门、唯一配置名、after-run 版本竞态重试。

## 下一步（按序）

1. 修复 listener 套接字泄漏（覆盖 SMTP 等）→ 重建 r38 → 干净快照重置 → 部署 → 重跑全部 ACC-001—019+FAULT-POINTS → 100 轮 → 五类×10 → final → summary → release manifest。
2. 之后进入总纲与需求逐条总体验收（MASTER_ACCEPTANCE）与最终汇报。
