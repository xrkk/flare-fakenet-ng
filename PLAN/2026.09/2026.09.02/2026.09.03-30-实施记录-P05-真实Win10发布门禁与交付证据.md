# 实施记录 — P05 真实 Win10 发布门禁与交付证据

> 会话角色：`作者/执行方`
>
> workflow_id：`fakenetng-mcp-20260902-6ada6479`（长程实施授权进行中）
>
> 实施子方案：`PLAN/2026.09/2026.09.02/2026.09.03-26-实施子方案-P05-真实Win10发布门禁与交付证据.md`，`v1`（blob `4c070328…`），状态 `已审核, 可以实施`
>
> 审核闭环：`2026.09.03-28-审核-P05`（9 AUD 含 1 高危）+ `2026.09.03-29-审核处理-P05`（全部闭合，作者自评闭环）
>
> 审核基线 HEAD：`39a2dcf`；分支：`process-redirect-v23`
>
> 状态：`进行中`（发布门禁执行中， 2026-09-03）

## IMP/ACC 总览（当前）

| 项 | 状态 |
| --- | --- |
| IMP-P05-01 发布 runner | 完成（`run_p05_release.py`： normal/fault/final/summary 四模式、逐轮断点续跑、五节规范化审计、连续探活、VM 连续性、FakeNet 端口归因收窄） |
| IMP-P05-02 100 次正常矩阵 | **进行中**（在最终候选上重跑； 见下方门禁沿革） |
| IMP-P05-03 五类×10 故障矩阵 | 未开始（待 100 轮通过后） |
| IMP-P05-04 ACC-017 final/summary | 未开始 |
| IMP-P05-05 记录与提交 | 本记录即部分产物 |

## 发布门禁沿革（缺陷—修复—候选更替， 全部按 AUD-P05-002 作废旧候选证据）

| 候选 | 门禁结果 | 根因与修复 |
| --- | --- | --- |
| r35 `mcp-c9bafd2d6…`（源 `9bafd2d6`） | ACC-001—019+FAULT-POINTS 全 pass； normal 矩阵第 29 轮失败 | **产品缺陷1**： 进程内模型下 FakeNet.stop() 后 listener 套接字泄漏（port 80 由 MCP 进程持有； 下轮启动 10048）。修复： supervisor 停止/回滚后强制清扫 listener server/sock 套接字（提交含 `force-close listener sockets`） |
| r36 `mcp-c3e219062…`（源 `3e219062`） | ACC-001—019+FAULT-POINTS 全 pass； normal 矩阵第 8 轮失败 | **产品缺陷2**： supervisor.stop 内部基线核对用了原始五节文本 diff（netstat PID/瞬态行噪声→"environment differs"误报）。修复： 改用 P04 规范化 `audit_compare`（`full_audit_diff`） |
| r37 `mcp-cb32303e1…`（源 `b32303e1`） | ACC-016/002/003/P01-ENTRY/005/011/009-PRE pass； ACC-010 两次 `owner_reconnect_still_controls=false` | **产品缺陷3（已定位+修复， 提交 `d8d1ca8`）**： 单一根因——`supervisor.start` 的证据循环被 diverter **构造期**证据（WinDivert queue 参数 + DNS 探活）提前满足， 在 run() 线程仍在逐个绑定 listener 时（SSL listener 一项耗时数秒）就返回 healthy。后续 stop 与仍在进行的启动线程竞态： 对"已 append 未 start 完成"的 provider 做 0ms 空停（VM 日志中 HTTPListener80 停止 0ms 无 HTTP_STOP 行即其指纹）、关闭主 WinDivert 句柄；孤儿启动线程继续把 21/25/110/443/6667 绑上（套接字创建时间戳与 PID 6316 持有为其物证）、带已关闭句柄重入 `diverter.start()`（接收线程 NoneType 崩溃→PCAP_DUAL_WRITE_FAILED）、在 stop 的基线审计之后改写网卡 DNS（VM Ethernet0 DNS 污染 `192.168.204.149` 为物证）；`sys.exit(1)` 被孤儿化为静默 start_error。每次停止的 `PayloadReportError('before paired PCAP closes')` 同根因： 线程死于 listener 阶段→`diverter.start()` 未运行→dual_pcap=None 而 dump_packets=True。owner 停止在基线审计看到幽灵运行的环境差异→`failed`→ACC-010 失败。**r35"缺陷1"与 r37 泄漏同源**（清扫只是对竞态后果的兜底）。修复五处（详见提交 `d8d1ca8`）： start 线程 join 后才报告状态、stop 先等存活启动线程、capture-never-started 停止降级为 HTML_REPORT_SUPPRESSED、listener 启动失败改抛 RuntimeError、`begin_controlled_exit` 修复从未赋值的 context（每次 SvcStop 都 AttributeError 降级——ACC-011 时段日志为证）。Wine 门禁： test/mcp 63 pass、全仓 642 pass（1 失败为用户既有 gui 未提交改动的窗口几何断言， 与本修复无关） |

## r37 根因取证记录（2026-09-03， 供复核）

- VM 物证： PID 6316 持有 `0.0.0.0:21/25/80/110/443/6667` LISTENING； 套接字创建时间 80=10:33:57（act- 轮）、21/25/110/6667=10:34:05、443=10:34:51——全部晚于对应轮次的"成功"停止； `state.json` 停在 `needs_recovery: true`； Ethernet0 DNS=`192.168.204.149`（幽灵线程在停止后设置）。
- 日志指纹： 所有 default.ini 内容运行的停止清单固定只有 6 个 provider（ProxyTCP/UDP、RawTCP/UDP、DNS、HTTPListener80）且 HTTPListener80 停止 0ms 无 HTTP_STOP 步骤行（半启动空停）； 10:34:05 无任何配置加载行的"神秘启动"实为 act- 轮孤儿线程迟到抵达 `diverter.start()`（pcap 落盘 `C:\Windows\System32\` = 服务 CWD， 反证非 MCP start 工具调用——该路径经 supervisor 会钉到 exe 目录）； 10:34:51 SMTP:25 10048 为幽灵二代撞上幽灵一代泄漏端口。
- Wine 反证复现（`Logs/fakenetng-mcp/r38-repro/`）： 同一代码+同一配置直跑 Fakenet（不经 supervisor 提前返回）时 12 个 provider 全部启动、全部干净停止、8 个端口全部释放——证明 listener 自身无缺陷， 缺陷全在 supervisor 的提前返回竞态。

## 当前精确检查点（恢复入口）

1. r37—r41 各候选全部按 AUD-P05-002 作废； 当前候选 **r42 = `mcp-c03cc4d32-2694bba008a9`**（源 commit `03cc4d3201a69872a782673c786c5a05b3e763af`， ZIP SHA256 `2694bba008a9e875bd2eb65ea2627239415d8988d8aa99c828f33df7489eafaa`， 产物 `dist/mcp-r42/mcp-r1/`）， 构建 Verification PASS。
2. r42 在干净快照 VM 上执行完整流水线（`Logs/fakenetng-mcp/r42-pipeline.sh` 后台）： ACC 序列（Python 驱动 `r42-seq.py`, 顺序含 ACC-011 先于 ACC-009-PRE 的校准序）→ release normal → fault → final → summary。
3. r41 已证实： 19 ACC + FAULT-POINTS + **100 轮 normal 全部通过**（`Logs/fakenetng-mcp/mcp-c2b04465c-4025246820d4/`）； fault 首轮抓出缺陷5后换 r42。
4. 部署前 VM 重置按 AGENTS.md 门禁执行； 宿主机内存紧张时 `vmware -x` 可能静默夭折并消耗 checkpoint——恢复办法： 再次单次 `revertToSnapshot` 重新武装 checkpoint 后重试 `-x`（本轮已用过两次， 均恢复成功； 绝不允许 checkpoint 缺失时 `-x`（会冷启动））。

## 缺陷4/5（r38—r41 沿革）

| 候选 | 门禁结果 | 根因与修复 |
| --- | --- | --- |
| r38 `mcp-cd8d1ca83…`（源 `d8d1ca8`） | ACC-016/ENTRY/002/003/005 pass； ACC-009-PRE `run_reached_healthy=false`（启动线程 30s 预算超时， 大声失败+回滚 healthy=True） | **产品缺陷4a**： `http.server.HTTPServer.server_bind` 内 `socket.getfqdn(host)` 的反向 DNS 查询被已激活的 diverter 过滤器截走黑洞化， 每个 HTTP listener 绑定消耗解析器全部重试预算（>30s）。修复（提交 `eeab273`）： `ThreadedHTTPServer.server_bind` 改走 `TCPServer.server_bind` + 字面 host； 同时修 supervisor 失败路径 `KeyError('reason')` 与回滚前 join 存活线程。VM 二分取证： 6-listener 前缀（无 HTTP80）2.3s healthy； +HTTP80 卡 33s； 仅 HTTP80 无 divert 1.8s 完成 |
| r39 `mcp-ceeab2733…`（源 `eeab273`） | 同上 ACC-009-PRE 失败（getfqdn 已除但仍超预算） | **产品缺陷4b（py-spy 栈实锤）**： `fakenet-run` 线程永久阻塞于 `accept()←socket.socketpair()←ThreadedHTTPServer.__init__`——Windows 的 socketpair 用 127.0.0.1 环回 TCP 握手模拟， 握手包被 diverter 截走→`accept()` 永不返回。修复（提交 `069fdd0`）： 改用 bind+connect 的 UDP 对（创建零流量）； 停止时唤醒包被截亦安全（serve 循环按 `_stopping` 标志+select 超时退出）。Wine 复现 12-provider 全生命周期 8 端口全释放 |
| r40 `mcp-c069fdd04…`（源 `069fdd0`） | **19 ACC + FAULT-POINTS 全 pass**（首次 12-provider 全启动全停止 healthy=True）； normal 矩阵 72/100 轮后第 23-custom 轮 tool-error | **产品缺陷5a/5b**： ①`SSLWrapper.__del__` 在 GC 时 rmtree temp_certs + certutil 删信任库——陈旧 wrapper 的 GC 与新 wrapper 的 makedirs/写证书竞态→第 23 轮 443 启动 ENOENT； ②443 失败于 server 创建后 serve 线程前， 回滚 stop 对从未 serve 的服务器调 `BaseServer.shutdown()`→等待只有 serve_forever 才会置位的事件→回滚挂死 5.5 分钟直至 SCM 停止。修复（提交 `2b04465`）： `__del__` 改为记录日志的空操作（清理由生命周期负责）； stop() 以 `server_thread.is_alive()` 判定是否跳过 shutdown（兼容测试用 `__new__` 手工构造的实例）。首版用 `_serve_started` 标志被构建门禁的 `test_http_listener_stop.py`（11 失败）抓回， 改为线程状态判定后 11/11 通过 |
| r41 `mcp-c2b04465c…`（源 `2b04465`） | 19 ACC + FAULT-POINTS 全 pass； **100 轮 normal 全 pass**（builtin 50 + custom 50, 零异常轮）； fault 首轮（policy_pause）失败： 停止正确收敛 `failed/stop grace exceeded` 但环境审计报 `listen_ports` 漂移 | **产品缺陷5c**： 受界停止（FB-005 宽限超限）分支直接 teardown， 漏掉正常停止路径已有的 listener 套接字清扫——挂死运行的监听端口留在进程内。修复（提交 `03cc4d3`）： 该分支 teardown 前执行 `_force_close_listener_sockets`。另： fault 每轮服务重启（装载注入）本会清除端口， 但 runner 在重启前审计——产品侧立即清扫严格更优 |
| r42 `mcp-c03cc4d32…`（源 `03cc4d3`）**最终候选** | **全部门禁通过（2026-09-03 23:53）**： 19 项 ACC + FAULT-POINTS 全 pass（22:41）； 100 轮 normal 全 pass（23:14, builtin 50 + custom 50）； 五类×10 fault 全 pass（23:53, 50 轮）； final（ACC-017）pass； summary pass； **release manifest 入 `dist/mcp-c03cc4d32-2694bba008a9-release/release-manifest.json`**（22 项 ACC 条目全部 pass、同一候选聚合、missing/failures 全空） | — |

### 过程性校准（非产品缺陷）

- ACC 调用序： ACC-011 需先于 ACC-009-PRE（fresh 审计文件恰好 8 行， `>8` 计数校准依赖 ACC-011 的审计行）； r40/r41/r42 采用 005→011→009-PRE→010 序。
- bash 驱动器在 ACC-016 出现不可复现的 manifest 校验误报（同一命令手动通过）； 换 Python 子进程驱动（`r4x-seq.py`）后消失。
- 宿主内存紧张导致 `vmware -x` 秒退并消耗 checkpoint（见检查点第 4 条的恢复办法）； docker 构建容器偶发 wine 残留进程占住管道（`kill <start.exe pid>` 即恢复）。

## runner/工具侧已完成的修复（非产品缺陷）

- 发布 runner： FAULTS 导入、repo sys.path、capture 成功标记、完整 diff 载荷、FakeNet 端口归因收窄。
- P02 runner： 生命周期超时 300s、idle 卫生、healthy 门、唯一配置名、after-run 版本竞态重试。
- 主机侧 runner venv： `.venv-mcp-runners`（pip 安装 mcp/anyio Linux 轮子）； VM 直连通道 `Win10VmChannel`（ZCode 客户端会话在 VM 重置后失效时的替代路径， `Logs/fakenetng-mcp/r38-repro/vmps.py` 同源）。

## 下一步（按序）

1. ~~r42 流水线~~ **已完成（23:53）**： release manifest 入 `dist/`； P05 全部 IMP 落地。
2. MASTER_ACCEPTANCE： 总纲与需求逐条总体验收 → FINAL_REPORT（`最终汇报.md` + 有效参数， COMPLETED 前必须持久化）。

## 最终状态

- **状态：`CHILD_DONE`（2026-09-03 23:53）**。最终候选 `mcp-c03cc4d32-2694bba008a9`（commit `03cc4d3201a69872a782673c786c5a05b3e763af`）， 在干净快照 VM（DESKTOP-3FI41GR）上以脚本化流水线一次性完成全部发布门禁： ACC-001—019 + ACC-009-PRE + FAULT-POINTS + P01-ENTRY（22 项）、 100 轮正常矩阵、 五类×10 故障矩阵、 final（ACC-017）、 summary； 发布物 `dist/mcp-r42/mcp-r1/`（候选包）与 `dist/mcp-c03cc4d32-2694bba008a9-release/release-manifest.json`（发布清单）。
- 门禁期间共修复五个真实产品缺陷（提交 `d8d1ca8`/`eeab273`/`069fdd0`/`2b04465`/`03cc4d3`）， 每个均有 VM 实证与代码级根因（见上方沿革表与 r37 取证节）； 全部过程证据在 `Logs/fakenetng-mcp/mcp-c03cc4d32-2694bba008a9/`。
2. 之后进入总纲与需求逐条总体验收（MASTER_ACCEPTANCE）与最终汇报。

## CHK 修复批次与 r52→r58 最终候选沿革（2026-09-05/06 会话）

前置：用户裁决重开（CHK-003 方向 B 等）后， CHK-013—029 修复于 r50/r52 落地； r52 在 9/19 ACC 稳定通过后 ACC-004 串行六场景反复超时成为阻塞点。本节记录从阻塞到最终候选的完整缺陷链（每行均有 VM 实证与代码级根因）。

| 候选 | 门禁结果 | 根因与修复 |
| --- | --- | --- |
| r52 `mcp-cc9cdea21…`（源 `c9cdea2`） | 9 ACC pass； ACC-004 串行超时（6×服务重启累积+楔死端点继承） | **执行侧**： 拆分 ACC-004 为 S1..S6 独立调用（`fd29254`）， 每场景前后 `normalize_service` 归一化； `restart_service` 强杀兜底。复现取证发现两个产品缺陷（见下两行） |
| （r52 复现取证） | ACC-004-S3 确定性楔死： 端点死亡、SCM 仍 Running、三线程 WaitReason=Suspended | **产品缺陷A**： `rundll32 comsvcs MiniDump` 自转储的双层线程挂起与并发线程 churn 竞态， 恢复不完整。修复（`97f8346`）： 进程内 `dbghelp!MiniDumpWriteDump`（ctypes）， 转储 1.3s ok |
| （r52 复现取证） | py-spy 栈实锤 ABBA： stop 线程持 supervisor 锁在 `coordinator.snapshot()` 等元数据锁； 终末失败路径的 protective-stop 参数求值持元数据锁经 `health_detail` 等 supervisor 锁 | **产品缺陷B**： `snapshot()` 在元数据锁内调 `health_detail`， 与 stop 全程持 supervisor 锁的调用方向反转。修复（`038bf76`）： 健康采样移到元数据锁外（supervisor 锁为 RLock， stop 路径自身重入安全）。回归测试对旧实现失败、新实现通过 |
| r54 `mcp-c038bf76b…` | 27 ACC 全 pass（含 S1..S6, S3 收敛 22s）； normal 第 18 轮 `environment differs` | **产品缺陷C（审计噪声①）**： RPC/WMI 动态端口（≥49152）瞬态 LISTENING 被当 FakeNet 残留。修复（`628cdee`）： listen_ports 方向感知归因（新增仅 <49152 计残留； 消失仅 <1024 计杀伤） |
| r56 `mcp-c177fee91…` | 27 ACC 全 pass； normal 第 2 轮 `environment differs`（服务端） | **产品缺陷D（审计噪声②）**： 路由归一化保留自动调优 metric 列； FakeNet 每轮 DNS 设置/恢复触发适配器重估， metric 在基线与停止审计间漂移。修复（`cdb377b`）： 路由身份=目的/掩码/网关/接口。另有 **产品缺陷E**： `register_fakenet_outputs` 每 stop 复制全部历史输出（r54 时 8319 工件/233 输出， 二次方拖慢）——修复（`177fee91`）按运行起始 mtime 只登记本运行产物 |
| r58 `mcp-c94e7abdb…`（源 `94e7abdb`）**最终候选** | **全部门禁通过（2026-09-06 04:03）**： 27 项 ACC 全 pass； 100 轮 normal 全 pass（零审计失败）； 五类×10 fault 全 pass； final pass； summary pass； release manifest 入 `dist/mcp-c94e7abdb-57b2ee8a7dde-release/release-manifest.json`（27 条目全 pass, missing/failures/integrity 全空） | 诊断增强（`94e7abdb`）： stop/恢复审计失败时记录差异 JSON——本轮 §C/D 根因即来自该日志 |

runner 侧配套修复（非产品缺陷， 均有提交）： ACC-008/ACC-019 的 ff48f64 前旧布局路径（`3881326`/`04de520`）、ACC-014 事件 manifest 真实 schema 校验（`0fa7cdb`/`50ca2ca`——r48 因 `$m.items` 字段名错误空洞通过， CHK-020 字段修复后暴露）、P04 停止服务自愈（`ee036d5`）、探针/审计失败取证（`58f945d`）、SSE 洪泛（`177fee91` 布局探测有界化）。

基础设施注记： 验收通道 Win10VM MCP（28787）本轮两次 SSE 瞬断（ACC-016 部署期、normal 第 29/50 轮）， 重跑/断点续跑通过； 旧候选 r52-r57 证据目录按 AUD-P05-002 保留为修复链历史， 不作为发布证据。
