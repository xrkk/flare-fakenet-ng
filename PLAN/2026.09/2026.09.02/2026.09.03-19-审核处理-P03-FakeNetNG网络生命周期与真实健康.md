# 审核处理记录 — 实施子方案 P03（FakeNet-NG 网络生命周期与真实健康）首次第三方审核引入

> 会话角色：`作者/执行方`
>
> 处理性质：首次第三方审核结果引入；第 1 轮；首次审核后的作者自评闭环
>
> workflow_id：`fakenetng-mcp-20260902-6ada6479`
>
> 原审计记录：`2026.09.03-18-审核-P03-FakeNetNG网络生命周期与真实健康.md`，Git blob `0afcc0bf81aca1794492b3ff9d60ab363c8bda16`，SHA-256 `649b58e8303d0d6ceabd3399251ce360f7bb5b2932c6b8886855dcb85fcbb4d9`（主会话独立复算一致；含 10 条 AUD 与独立保真计数 18/17/1/0/0、结论 `存在不同意见`；审计方披露的创建后数秒笔误修正已记录于审计记录本身）
>
> 处理前子方案：`2026.09.03-16-实施子方案-P03-FakeNetNG网络生命周期与真实健康.md`，`v0`，blob `d3ec927494bcde86b705a6d75090851e5255674d`，状态 `未审核, 不得实施`
>
> 处理后子方案：同路径，`v1`，blob `5d39031a296208752da88a37a13addb1503c7513`，状态 `已审核, 可以实施`
>
> 规范性来源：需求 `2026.09.02-01`（blob `b91ded55…`）；总纲 `2026.09.02-02` v2（blob `e5c734ea…`）；`AGENTS.md`（`c84a7533…`）——均无漂移
>
> 本地基线 HEAD：`4de0705`
>
> 本记录创建后不可修改。

## 1. 引入门禁核对

- 审核者身份：`sub-plan-reviewer`（隔离上下文；未读作者自评）。
- 记录持久化核实：文件存在、SHA-256/blob 复算一致、结论非口头。
- 对象匹配：审计绑定 v0 blob `d3ec9274…` 与处理前对象精确一致；基线一致；关键路径指纹亲验无漂移。

## 2. AUD 逐项处理（10/10 全部完全同意并修改闭合）

| AUD | 严重度 | 实际修改（落点：子方案 v1） |
| --- | --- | --- |
| 001 主 filter 重建点覆盖 | 高 | §1 代码事实与 IMP-P03-01 重写：注入覆盖基础构造+`build_egress_control_filter` 重建两路径， 句柄打开前对最终 filter 断言包含排除子句， 缺失=拒绝启动（FB-002 失败关闭）；IPv6 宿主地址同规则 |
| 002 健康时序测试义务未落点 | 中 | IMP-P03-07 增“健康探测时序测试”（≤2 周期回退、无虚假 healthy）；ACC-006 增时序断言 |
| 003 故障注入手段未冻结 | 中 | IMP-P03-08 冻结注入手段清单（坏配置/端口冲突/日志文本追加/stop_blocker 钩子/进程操作）， 全部不入产品工具面 |
| 004 restart 真实复验未落点 | 中 | IMP-P03-08 与 ACC-009 补真实 `restart` 工具调用场景（绑定原 run_id+幂等）；保真未落点数清零 |
| 005 ACC-009 前置继承语义 | 中 | 冻结为“在 r6 候选上完整重跑 P02 全部前置子项， 不引用 r5 证据” |
| 006 锁/读顺序 TOCTOU | 低 | §3 冻结“锁先于读”：句柄→读内容→SHA→校验→注入→启动 |
| 007 IMP 拼写混用 | 低 | 统一 `IMP-P03-0x`；修 `Fakenet.stop` 拼写 |
| 008 基线进程清单未定义 | 低 | 定义为“加载 WinDivert 模块的进程（tasklist /m）+ fakenetng-mcp/fakenet 进程”， 核对=清单差异 |
| 009 Job 命名性 | 低 | 冻结未命名 Job（`CreateJobObjectW(None, None)`）+句柄 `bInheritHandle=False` |
| 010 事实精确性合并 | 低 | 修正 `check_filter` 条件执行表述；IPv6 失败关闭并入 001 修改；ACC-008 禁框架白名单依据=CON-006/NON-008 定义的 DB 引擎/重放/迁移框架（模块名 `sqlite3/_sqlite3/dbm` 等）；`wait_for_capture_failure:543` 复用记入事实 |

## 3. 引入后需求保真门禁（重做）

- 应承接数：`18`；完整承接数：`18`；未落点数：`0`；语义弱化数：`0`；不可验收数：`0`；异常 ID：无。
- 结论：`通过`（restart 真实复验经 AUD-004 修改后落点闭合）。

## 4. 再次第三方审核逐项判定

修改为：高危代码事实修正与双路径注入断言（仍属“唯一主 filter 构造边界”内的实现细节）、测试/注入/复验落点补强、顺序与命名冻结、拼写与引用修正——无跨项目契约、无不可逆/权限/安全架构变化、无总体架构或数据流改变（进程内+自 Job 判定已被审计验证成立）、影响面逐条可回溯、无竞争方向、无用户/项目约定要求复审。全部未命中。判定：`无需再次第三方审核`。

## 5. 最终结论

- AUD-P03-001—010：全部完全同意、全部修改并闭合；未解决/复开 AUD：无；熔断：未触发。
- 处理后子方案 v1（blob `5d39031a…`）状态 `已审核, 可以实施`，进入实施（workflow 长程授权覆盖）。
