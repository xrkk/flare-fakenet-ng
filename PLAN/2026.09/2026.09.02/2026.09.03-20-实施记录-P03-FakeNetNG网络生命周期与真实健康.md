# 实施记录 — P03 FakeNet-NG 网络生命周期与真实健康

> 会话角色：`作者/执行方`
>
> workflow_id：`fakenetng-mcp-20260902-6ada6479`（长程实施授权进行中）
>
> 实施子方案：`PLAN/2026.09/2026.09.02/2026.09.03-16-实施子方案-P03-FakeNetNG网络生命周期与真实健康.md`，`v1`，blob `5d39031a…`，状态 `已审核, 可以实施`
>
> 审核闭环：`2026.09.03-18-审核-P03`（10 AUD 含 1 高危）+ `2026.09.03-19-审核处理-P03`（全部闭合，作者自评闭环）
>
> 总纲 v2 blob `e5c734ea…`；需求正式 v1 blob `b91ded55…`；`AGENTS.md` `c84a7533…`
>
> 审核基线 HEAD：`4de0705`；分支：`process-redirect-v23`
>
> 任务边界：`fakenet/diverters/windows.py` 最小 hunk（双路径排除注入+最终断言）、`fakenet/mcp/{supervisor,health,snapshot,baseline,configlock,jobobject}.py`、`fakenet/mcp/{winservice,cli,server,tools}.py` 接线、`test/mcp/test_supervisor*.py`、`test/mcp/acceptance/run_p03_acc.py`、候选 r6、`Logs/fakenetng-mcp/**`。用户既有改动（含 windows.py 中用户未提交修改）——注意：windows.py 在启动快照中已是修改状态（用户既有）， 本方案在其上追加可机械归属 hunk。
>
> 状态：`未开始 → 进行中`（2026-09-03）

## IMP/ACC 总览

| 项 | 状态 |
| --- | --- |
| IMP-P03-01 filter 双路径排除注入 | 完成（De-Morgan IPv4 臂折入；检查 filter 语言无一元否定/pydivert 2.0.9 冻结陷阱均修复） |
| IMP-P03-02 supervisor+真实健康 | 完成（进程内 Fakenet+健康促升/撤销、运行窗口日志扫描、CWD 钉到包根、失败回滚、有界停止、restart 定居） |
| IMP-P03-03 state.json 快照 | 完成 |
| IMP-P03-04 环境基线 | 完成（稳定节恢复一致性比较） |
| IMP-P03-05 活动配置锁+重解析点 | 完成（内置+自定义均锁定；锁先于读） |
| IMP-P03-06 Job/SCM 恢复/受控退出 | 完成（未命名 kill-on-close Job 自分配、启动恢复审计、pywin32 SvcStop 入口） |
| IMP-P03-07 测试+构建 | 完成（宿主 82/82；全仓 Wine 门禁 638+；候选含 WinDivert 驱动/defaultFiles/ssl_utils） |
| IMP-P03-08 ACC runner+真机执行 | 完成 |
| ACC-001/004/006/007/008/009 | **全部 pass**（候选 `mcp-c9376869a-6ad29cc16091`，源 commit `9376869a…`，ZIP `6ad29cc1…`；同候选 ACC-016 回归 pass） |

## 轮次 1 结果（2026-09-03）

- 构建历程 r6→r23（18 次构建，全部可归因修复并提交）：diverter `_dict` 小写键、pydivert 2.0.9 冻结（无 check_filter）、filter 语言无 `not`/需 De-Morgan、process_redirect 重建路径括号、Wine 假符号链接探针、kernel32 句柄截断、user DNS 测试半套状态同步、包缺 defaultFiles/ssl_utils/WinDivert 驱动、CWD 资源解析、运行窗口日志扫描、多端口排除（28787 观察通道随控制链路一并排除， DEC-007 性质：同一受信宿主的观察通道）、有界停止+restart 定居。
- 真机里程碑：真实 FakeNet 接管期间 MCP 链路持续存活（ACC-004 连续探活全绿）；未处理异常撤销健康且链路保持；强杀 MCP→Job 收敛受管树→SCM 重启→恢复审计→不续跑不自动启动；运行期外部写/删被 OS 锁拒绝、停止后解锁；重解析点逃逸拒绝；restart 绑定原 run_id。
- 产品真机修复（全部有证据）：见提交 306ad04..9376869a。
- 证据目录：`Logs/fakenetng-mcp/mcp-c9376869a-6ad29cc16091/{ACC-001,ACC-004,ACC-006,ACC-007,ACC-008,ACC-009,ACC-016}`。

## 结论

**P03 子方案状态：已完整落地（CHILD_DONE）。**