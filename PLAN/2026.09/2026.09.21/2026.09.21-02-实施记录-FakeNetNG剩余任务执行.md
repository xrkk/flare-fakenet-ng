# 实施记录：FakeNetNG剩余任务执行与验收清单（R00 起）

依据：`PLAN/2026.09/2026.09.21/2026.09.21-01-FakeNetNG剩余任务执行与验收清单.md`（下称"清单"）。
执行者：GLM（ZCode 独立执行，用户 /goal 通知）。本记录按清单第12节格式逐 R 项追加，不覆盖清单起始事实。

## R00 接手责任与状态核对

| 项目 | 内容 |
| --- | --- |
| 状态 | 通过 |
| 输入 | 仓库 HEAD `77246a5`（清单自身提交，2026-09-21 22:14:29 +0800）；分支 master；工作树仅含已知未跟踪 `PLAN/2026.09/2026.09.13/2026.09.20-01-完成声明核验-FakeNet100场景.md`，保持原样。 |
| 动作 | 1) 只读核对 Git 状态与 HEAD。2) 只读核对协作目录 `.tmp/fakenet100-20260920-091921`。3) 只读核对 Herdr 面板 w7:p1（主会话 Codex session `01a0bc60`）与进程表。4) ping/`vmrun list`/vmware.log 只读核对 `.233` VM。5) 备份 progress.json 后未改写旧失败（见证据）。 |
| 证据 | SEQ65 交接闭合事实：S0065-派发索引.md+.ready 于 2026-09-21 13:44 发布；执行端 executor-log.md 最后条目为 13:35:00 等待 S0065（watcher agent_250a30ad），发布后无 TASK_READY 回调；`T037-R01-*.result.md`/`result-done`/`executor-timeout.md` 均不存在；执行端 watcher 进程已不存在；deadline 17:54:43/18:44:13 超期 3.5h+（核对于 22:17）。主会话 21:52 恢复、22:14:29 提交清单后空闲（面板确认"Worked for 5m 28s · done 10:14 PM"，无在途工作）。结论：SEQ65 已派未启，无有效结果可复用，其剩余范围由本执行者按清单 R01 步骤1完成。VM：`192.168.204.233` 不可达，`vmrun list`=0，vmware.log 显示 2026-09-21T11:31:31Z（19:31:31+0800）外部挂起（checkpoint `Win10(MalBox)-FakenetNG-db41ea36.vmss`，VMX 含 `checkpoint.vmState`）；主机会话均无法构成并发 VM 写入。progress.json 旧副本 `Logs/fakenetng-mcp/final100-20260920/progress.json.pre-handoff-20260921-2219.bak`（sha256 c6d81aab…，与原件一致）。 |
| 判定 | 单一业务写入者=本执行者（GLM）。源码身份=HEAD 77246a5（候选10 产品源 e1759692…，冻结于 mcp-ce1759692-3b16d9348be3）。VM 身份按部署记录为 DESKTOP-3FI41GR / 00-0C-29-C1-CA-49（当前挂起，恢复后需按清单第2节重验身份+时钟+责任状态）。正式最终100完成数 0/100（沿用清单，不因交接改变）。 |
| 责任 | 本执行者独占业务写入；主会话空闲且用户已暂停派发；无后台任务。VM 恢复待 R01 实机阶段，恢复动作前重新核验挂起点状态并保存事实。 |
| 提交 | 本记录随 R01 进展一并提交；R00 本身为只读核对，无代码改动。 |

## R02 sst-002 diverter_stop 完整区间

| 项目 | 内容 |
| --- | --- |
| 状态 | **通过（候选24 实机 sst-002 五类 Spike 内全 verdict 通过；无需语义变更）** |
| 输入 | `candidate12-spike-002-01`（候选 mcp-cb50b10e7-90122c80a962，run `48a64623-7b86-40f1-961d-113dbcea48d9`）；历史 `candidate06-spike-01`、`candidate07-spike-002-01`、T024–T028 记录。 |
| 动作 | 1) 候选12 严格复跑 sst-002：traffic_oracle 全过（nic 0），唯一失败=fault_oracle `single managed outbound connection must contain entire conservative action interval`。2) 区间取证：intervals_ns：begin_upper=5462844620798、trigger[lo=5463047974301, hi=5463084244898]、session_end_lower=5463059536901（早于上界 24.7ms）。3) 事件链核对：start 请求 15:44:20.777；注入（`inject_diverter_stop` 于 start 处理内，`_close_windivert_handle` 原生观测）began=15:44:23.0686；主连接 established 15:44:22.829（probe）+ relay 上游 50445→60.28.220.199:443；**会话内核终止 15:44:23.0595（早于注入 9.1ms）**；探针其后仍 request_sent 至 23.106，23.1859 收 RST（Write 失败）后 close、23.233 重连。4) run.log 核对：上游 ALLOW_INTERNAL_UPSTREAM 数据行止于 22.978（探针继续发送但上游流量停），relay worker 无 INFO 级关闭原因（仅 DEBUG）。 |
| 证据 | fault-action.json（native 双时钟+句柄前后观测）与全部区间引用可由 `fault-evidence-result.case.json` 字节回读。 |
| 判定 | 区间失败直接机制=peer TCB（relay 受理侧）内核终止不受 contradiction 规则约束（`terminal_bounds` 既定设计），连接真实死亡早于注入动作及其文件上界。原方案 L172"不改 existing trigger 语义"，修复不得触裁决语义。 |
| 追查（候选12/13 共7轮复跑+VM实测） | 根因=**gate 观察者发布时延**。原始观察者每轮 `Get-Content -Tail 200`+逐行 `ConvertFrom-Json`（~250ms/轮）；改增量解析后检测仅 6-13ms，但发布仍滞后 160-360ms，逐项排除（names 缓存、cmd dir 枚举、Select-String 预热）后归因为观察者 PowerShell 会话冷路径（不可稳定消除）。连接生命周期 200-540ms（真实服务器对合成 drip 的关闭时机）；包含需 `publish+~36ms ≤ peer_close`，即 publish 须在 established 后 ≤~50ms。观察者修复提交：`d7feae7`（增量解析+latch）、`1079263`（cmd dir 枚举）、`ec27081`（计数器修正）、`4de6dfb`（预热）。候选13（`682de038` 接收线程 None 守卫）已闭合第二失败面（AttributeError→capture_failure→3 异常块+句柄检查失败；run-04 验证包含曾以 246ms 裕度过）。 |
| 修复设计（待实施） | **gate 等待移入子进程**：`faultinject.wait_for_start_gate` 在 gate JSON 含 `probe` 键（listener_stop/diverter_stop）时本地轮询（10ms）：探针文件本 nonce 的 established + 自身 run.log 匹配 est.pid/sport/src 的 PROCESS_FLOW（与观察者同字段逻辑），满足即注入，不等 ready 文件；child_hang 保留 ready 路径。`_arm_fault` 在 gate 文件加 `probe` 路径（等值校验允许附加键）。`_run_gate` 降级为证据采集（脱离关键路径），ready 写入仅 child_hang。预期注入时延 established+~15ms，包含裕度 ≥150ms。 |
| 实施与验证 | 子进程内 gate 已实现并实机生效：候选14 `mcp-c96099ee5-06d0325b5630`（源 96099ee，deployment-14）sst-002 复跑——注入动作落在首个 established 后 ~13ms（trigger native ≈34.768，est 34.755），观察者发布时延（+156ms）已脱离关键路径。**但本轮连接生命周期为极端短值**：peer 终止 34.7532 早于探针 established 34.7552，观测区间倒置（begin_upper 34.7708 > end_lower 34.7532），任何 gate 时序均无法覆盖；且本轮出现两次 established（34.755/34.870，探针快速重连），疑似选中代次绑定到超短命连接或重定向前 TCB——需下一轮按 generation_manifest 分析该 run 的代次选择。 |
| 下一步 | 1) 分析 candidate14-spike-002-01 的 generation_manifest 与两次 established 的代次归属，判定超短连接是服务器关闭抽签还是代次选择病理；2) 若为抽签极值→复跑取分布（in-child gate 已把时延面闭合，正常寿命 200-540ms 的裕度 ≥150ms）；若为病理→修正观测代次选择；3) 通过后同候选跑 listener_stop 门与 R08 五类 Spike。 |
| 结构性结论（候选13–18 共6轮） | 包含失败恒为 18–32ms 差且成因逐层剥除后收敛：`assess.bounds()` 对**所有事件**统一加 ±15,625,000ns，包含需要 `会话终值 − 动作终值 ≥ 31.25ms`。逐层修复并实机验证：in-child gate（`96099ee`，注入时延 established+13-23ms）、gate 文件清理（`0d3be89`）、映射存活门（`d57843e`+`28dd214`）、接收线程 None 守卫（`682de03`，消除异常块级联）、watchdog 拆除宽限 250ms（`b4ba21f`，suspend 延至 +3.9s）、基础设施失败持有客户端（`b25154a`，worker/探针会话存活 5s）。终值来源随之演进：relay worker 关闭→watchdog 级联 peer 关闭→上游 RST 致 worker 退出→**最终：探针自身内核 TCB 在动作后 5-15ms 拆除**（注入后未过滤重传外泄引致中间盒/服务器即时 RST，与 R01 泄漏同现象，是该故障类别的固有物理）。 |
| 判定 | **R02 单例在冻结合同下结构性不可满足**：diverter_stop 动作本身（句柄关闭）在物理上必然于一个 RTO/中间盒 RTT 内终结任何活会话（观测 5-15ms < 要求 31.25ms）。产品侧六项修复已消除全部非物理成因；候选18 证据（`candidate18-spike-002-01`：action 04.5126，探针 TCB 终值 ≈04.5259−裕度，5s 应用层会话仅在内核层被外泄 RST 截断）为最终定论件。 |
| 实机验证（候选18/19） | 候选18（含 R03 核心）：case-3 native 记录已导出（`relay-native-events.jsonl` 三条：sni_mismatch/closed/relay_error，QPC+FILETIME 支持），但 deny 记录采样在关闭套接字之后 0.8ms，落在本案 ETW 窗口（2.7ms 宽）之外→绑定失败。`6c5acdc`（记录先于关闭）后候选19 `mcp-c6c5acdc8-de8f237ba3b0`（deployment-19，证据根 `candidate19-spike-004-01`）：**case-3 以 contract v2 native 绑定实机通过**（filetime 1790024074615643200），nic=0，case 0/2/3 全过。 |


| VFY005 专项审计 | 裁决链全部容差/替代/允许条件及来源：① ±15,625,000ns（resolution_ns−1）统一事件界——`sst_fault_evidence.py:281 bounds()`，来源=case 时钟声明（探针 stopwatch_frequency 墙/单调交叉验证强制漂移≤resolution，`clock_check` :292-310；统一方案 §4.1/T033 固化），对所有事件一致、无事件级放宽；② 观测构造 ±15,625,000ns——`scenario_suite.py:2079/2490` 同常量同来源（墙 vs native 比较）；③ policy_window ±15,624,999——`:2403` 同族；④ 裸 RST ≤60B 豁免——`:1996`，来源=sst-089 先例（代码内注释引证 discovery100-30），`2b1c0e9` 起双路径生效，界定=RST 标志+≤60B（含以太/IP/TCP 头）；⑤ not_after_local（diverter 停止界）/not_before_local（EGRESS 就绪界）——`:1947ff`，stop-window 交错设计，边界缺失（stop 楔死）时保持全捕获记账、不发明边界；⑥ 异常白名单 `{'listener_stop':{'L1'},'cleanup_error':{'C1'}}`——`sst_fault_evidence.py:587`，精确 kind 匹配，diverter_stop/policy_pause/child_hang=空集（零异常）；⑦ fault-action native 界 (end,end+99ns)——`time_bounds` 既有分支，当前仅作界值不作豁免（R02 选项A 的改动点，未动）；⑧ contract v2 native deny 包含——本轮 `3ef6091` 新增：native-to-native、身份+唯一性+ETW 窗口；无/弱 native 证据回落严格墙钟路径（存量拒绝测试全绿证明）。**已撤销项保持撤销**：500ms 邻近放行（b70fa2c 撤销，链内无任何 500ms 常量——grep 核实）、全局故障豁免（939f2cf 撤销，白名单按故障类精确匹配）、ESTABLISHED_BYPASS 仅为启动前流的显式 disposition 记录非豁免。跨协议借证/删绑定/改 reason 等反例由既有回归测试持续拒绝（全绿）。 |
| 最终验证（候选20） | `2b1c0e9` 修复 `is_bare_rst` 死代码（`parse_packets` 从不产出 `flags` 键，豁免从未生效）后，候选20 `mcp-c2b1c0e94-8bb5b3ebe380`（deployment-20，证据根 `candidate20-spike-004-01`）：**sst-004 STATE=pass，全部 verdict 通过，case 0–3 全过**，driver passed=true、IPC 恢复、final-closeout 七门全过。R01+R03 的 sst-004 链条闭合。 |
| 剩余 | sst-004 仅剩 case-1（198.51.100.77:1337 deny 例）失败：泄漏包为 54 字节裸 RST（8 组件全链）——主路径 `_pktmon_observations` 有 `is_bare_rst`(≤60B) 豁免（sst-089 先例），**case 路径的 nic 选择未套用同一过滤器**（同包类主路径豁免、case 路径计数的口径不一致）。下一步：核对 case 判定路径的 nic 选择代码，套用同一 `is_bare_rst` 过滤（口径统一，非新增豁免），构建候选20 复验。 |


| 最终解决（候选24） | `92b4c154`（gate 释放稳定期：存活条件成立后 60ms 再核验释放）+ `ac6341f9`（native deny 跨域 2ms 守卫）后，候选24 `mcp-cac6341f9-62ed01c4b41d`（deployment-24）**五类 Spike 全部通过**，含 sst-002（diverter_stop）与 sst-015（listener_stop）。机制：动作推迟到会话稳定段后，注入时探针 TCB 已无在途未确认数据——句柄关闭后无重传外泄、无中间盒 RST，会话以探针自身读超时（~5s）自然终结，动作区间（含双侧 ±15.625ms 裕度）完整落在会话内。**此前"结构性不可满足"结论的前提（动作必然落在会话边际裕度带内）被稳定期释放推翻；选项 A/B 决策请求撤销，裁决语义未做任何改动。** |
| 已撤销（语义变更） | ~~选项A：fault-action 记录（`fakenet.fault-action.v1`）已含 native `end_time_ns` 与 QPC 双观测，裁决器对该记录改用 native 界（`time_bounds` 已有该分支：`(end,end+99ns)`）替代统一 ±15.625ms——即 trigger 侧去掉裕度，包含条件变为 `终值−裕度 ≥ 动作native`（13.3ms ≥ 0ms ✓ 成立）。这是原方案 L172"不改 existing trigger 语义"所指边界内的最小语义调整，需要用户批准。选项B：diverter_stop 场景的 action 区间改以 native relay 记录（`relay-native-events.jsonl`，已随候选17+产出）重建——同样需要批准。不批准则 R02 按结构性不可满足记录，sst-002 保持 fail。 |
| 责任 | 本执行者；无后台任务。 |
| 提交 | 只读取证，未提交。 |

## R03 辅助SNI拒绝短窗口、VFY005

| 项目 | 内容 |
| --- | --- |
| 状态 | 通过（sst-004 全 verdict 实机通过，候选20/deployment-20；VFY005 审计完成见下） |
| 输入 | `candidate12-spike-004-01` case-4；`candidate10-residual-flow-01`（deny RST 送达缺陷=Defect A：deny/1337 监听 RST 以本地地址发出，客户端 TCB（remote=原目标）收到不匹配包由内核丢弃+ghost-RST 乒乓，TCB 存续至 RTO 中止；候选10 实证 RexmitCount 1→5）。 |
| 动作/证据 | 候选11/12 的 case-4 失败取证：`branch_log=None`、`sni_binding=None`，transport 层拒绝已生效（探针 AuthenticateAsClient 失败、nic_packet_count=0、TLS_SNI_DENY 行完整存在含 sport/original_ip/generation/sni_mismatch）；观测区间倒置：begin_upper−end_lower=21.4ms（连接存活 ~10ms < 2×15.625ms 裕度），包含判据结构性不可满足——候选10 同败，非 quiesce 引入。 |
| 判定 | 短窗口修复方向（符合"只补可证明的采样/产品身份"）：relay deny 路径记录 native 时钟样本（复用 faultinject.native_clock_observation 机制）到 run 目录原生事件文件；观测/绑定用 native-to-native 界重建 case 连接区间。deny RST 送达（Defect A）一并修复：deny 时经改写路径向客户端注入 RST（映射宽限内）。VFY005 专项审计未开始。 |
| 责任/提交 | 待实现。 |

## R01 sst-004 原目标物理包归因及产品修复

| 项目 | 内容 |
| --- | --- |
| 状态 | 通过（缺陷已消除并双候选实机验证；sst-004 场景整体仍剩 case-4，属 R03 范围） |
| 输入 | 候选10 `mcp-ce1759692-3b16d9348be3`（源 e1759692…），run `dd2c7b7e-d447-4cfb-ac4a-6bfd02cf7552`，证据根 `candidate10-spike-004-01`；冻结源码 `git show e175969` 只读核对 stop/relay/fault 路径。 |
| 动作 | 1) 完成 SEQ65/T037 剩余范围（A01–A04）：新证据根 `candidate10-residual-flow-01`，extract.py（双跑 byte-identical、`--check iso`/`--check tamper` 反例、6 输入 hash 封存）。2) A03 源码追查：`fakenet.py stop()` 有序路径 listeners→diverter 顺序正确；`managed.py child_main` 的 `fault.on_stop_error()` 在 `instance.stop()` 之前抛出使有序拆除整体跳过；relay worker 拆除依赖 `close_relay_mapping` 的改写宽限。3) 反例先行：`test/mcp/test_managed_stop_quiesce.py`（4 例：失败 stop 必 quiesce 一次、成功 stop 不 quiesce、quiesce 失败不掩盖原错误、无实例 stop 不 quiesce）+ `test/mcp/test_relay_quiesce.py`（4 例：linger(1,0) 全连接中止、幂等、quiesce 后 accept 中止新到并按宽限关映射且记 `RELAY_QUIESCE_RST`（不污染 TLS_SNI_DENY）、worker 因 quiesce 中止不产生 deny 日志）。4) 最小产品修复：`DomainEgressRelay.quiesce()` + `_abortive_close()`（SO_LINGER(1,0)）、accept 环 reject 模式、worker 异常路径 quiesce 保护；`Fakenet.quiesce_redirected_flows(reason)`；`managed.py` 失败 stop 路径触发（`managed_stop_failed`）。有序 stop 行为不变。5) 回归：新 8 例 + 受影响子集 70 例 + `test/mcp` 全量 1027 passed/8 skipped/12 subtests。 |
| 证据 | 归因链 F1–F7 见 `candidate10-residual-flow-01/residual-flow-report.json`（sha256 73580827…，双跑一致）与 `residual-flow-summary.md`。核心事实：泄漏包=探针内核 TCB 对 141B 未确认段的 RTO 重传（NIC Tx 13:38:54.9172493，Job 空确认 +108.0385ms）；relay 监听 TCB 中止（54.8110）晚于过滤器句柄释放（54.8092），abort RST 无法改写送达；case2 裸 RST=deny 流 TCB RexmitCount 1→5 后 RTO 中止（现有 oracle ≤60B 豁免，不计失败数）；+368µs 入站 RST 具中间盒（NAT）特征（RTT 实测 ~15ms）。不可证项如实记录：改写路径 ACK 是否到达客户端 TCB 在捕获中不可见。 |
| 判定 | 根因=有序拆除被故障跳过后，未确认数据的原 tuple 内核 TCB 在过滤器关闭后自发辐射绕过重定向。修复在过滤器仍开时主动终结被重定向流（复用既有改写宽限机制，未新增判据豁免、未放宽 NIC 断言）。实机严格复验待新候选部署后执行（sst-004 十项 verdict + 外层恢复门 + 正常 stop 回归）。 |
| 责任 | 本执行者（GLM）唯一业务写入者；无后台遗留；VM 仍处 19:31 外部挂起态，恢复后先重验身份/时钟/责任状态。 |
| 提交 | `8c8753cf…`（quiesce 修复）；`b50b10e7…`（延迟 250ms 修复区间包含）。 |
| 实机复验 | 候选11 `mcp-c8c8753cf-efc745d41d08`（deployment-11）：nic_original_packet_count **0**（候选10 为 1），run.log 实证 `RELAY_QUIESCE aborted_connections=2` + 探针每次重连 `RELAY_QUIESCE_RST`；fault_oracle 区间包含失败（session_end_lower 早 trigger_upper 32.5ms，即时 quiesce 提前杀会话所致）。候选12 `mcp-cb50b10e7-90122c80a962`（deployment-12，源 b50b10e）：fault_oracle 通过、traffic_oracle 主腿全过、curl 401 通过，唯一失败=case-4 SNI deny（R03 已知短窗口：观测 end_lower_ns < begin_upper_ns，区间结构性倒置，候选10 同败，与 quiesce 无关）。 |


## R04 P14恢复失败、有界操作员退出与P16回归

| 项目 | 内容 |
| --- | --- |
| 状态 | 通过（证据化闭合，无新增产品语义需求） |
| 三条路径证据 | ① 可恢复失败：本会话候选11–20 每轮故障 spike 的 fault_recovery verdict=true + recovery_cycle（loaded/started/stopped + 五节 before/after 原件）——10+ 次真实 Windows 实测（最新：candidate20-spike-004-01，七收尾门全过）。② 不可满足基线：`test_recovery_failed_when_environment_differs`（环境差异→失败结果保留）+ `04ef05d` 接管前拒绝 persistent-only 路由快照（`test_endpoint_attribution.py` 15 行反例，本轮 30 项 route/baseline/attribution 测试全绿）；真实机历史件 candidate06-udp-01（needs_recovery 贯穿失败恢复保留）。③ 操作员有界退出：`supervisor.stop()` 全程 deadline 界（stop_grace+360 总界、Job 终止 +30、退出证据 +120、审计稳定窗 150），每次调用一次有界尝试；失败结果清单（native capability cleanup/初始化清理/恢复快照损坏/基线身份错配/环境恢复审计失败/晚到操作/退出所有权未核实）各自 `_result('failed')` 且 **clear_recovery 仅在全成功路径执行**——不存在把未恢复写成正常的通路；`start()` 对 `corrupt or needs_recovery` 抛 `unresolved recovery responsibility`（覆盖拒绝，supervisor.py:139-140）。 |
| 判定 | P14 的"有界退出"在现行实现中已成立：失败=保留可解释责任+端点 failed 态可见，操作员可安全停止调用；无"放弃恢复/强制清理"新语义需求（清单允许"保留可解释失败"）。mDNS/端点差异归属由 `_attribute_section_difference`/`_difference_is_residue`（owner/代次归属，discovery100-99 先例）处理，测试绿。P16 反例（五列活动默认路由 vs 四列永久路由）持续拒绝。容量/时钟/Windows-MCP 内存无现行实证阻塞，未动。 |
| 责任/提交 | 只读核对+既有测试运行，无代码改动；证据引用见上行。 |



## R05 100场景业务覆盖优化与冻结

| 项目 | 内容 |
| --- | --- |
| 状态 | 冻结完成（行定义级；最终候选身份级重冻结在 R06/R09 执行） |
| 冻结证据（`r05-freeze-01/`） | seed 20260912/count 100；`scenario-manifest.json` sha256 `62b3ac46e8ce193765908eb0fd590efa106a888ca8ca8928143ba49abe5787be`；`planned-coverage-report.json` sha256 `96edcb548264534463cefd9c718e932783cfe7e0e5bcf833d30c6e50e2fd439c`；host commit `33e2cb0`（工具哈希清单 `host-tool-hashes.txt` 30 项）。 |
| 结构审计 | 行数 100（sst-001..100）；桶 B1=25/B2=20/B3=20/B4=20/default=15（满足 25/20/20/20/15 与原方案下限 B1≥25/B2≥15/B3≥15/B4≥15/default≤15）；故障五类各 3（child_hang/cleanup_error/diverter_stop/listener_stop/policy_pause）=15；良性 85；生命周期 start-stop=68、restart=32（restart 均为真实 restart 工具调用）；工具覆盖 15 种（全部 ≥1，其中 12 种覆盖 100 行）；coverage `problems=[]`、`passed=true`。 |
| 确定性 | 双次 `generate` byte-identical + 正式 `generate --regen-check` 通过。 |
| 判定 | 行为/结构/覆盖合同由生成器自审（problems 空）+ 上述机械审计共同满足；无新增杜撰需求。**注意**：本冻结绑定候选20 身份参数；R06 最终候选确定后须以最终身份重新 `generate --regen-check` 并重存哈希（行定义不变，仅身份字段换绑）。生成 100 行不计实机进度。 |



## R06 最终候选冻结、构建、受控部署

| 项目 | 内容 |
| --- | --- |
| 状态 | 完成 |
| 最终候选 | `mcp-c2b1c0e94-8bb5b3ebe380`，source `2b1c0e94a44f53e84affcdeb1aa41943d8025f45`，ZIP sha256 `8bb5b3ebe3805984e0a21402efcee3d7b551a55bafa51672cb432eec1e4c9c7a`（包 `dist/mcp-final100-20260921-flags-01/`，构建日志 `build-flags-01.log`，verification PASS）。HEAD（`687a9c6`）相对该 source 仅文档提交，产品树一致。 |
| 构建/部署门 | Windows 主 1682/0/0/8skip、HTTP 17/0/0、frozen-smoke/exit-role（构建日志）；受控部署 `deployment-20`（expected-current-source 链式核验，199 成员，配置字节不变）。 |
| 身份级冻结 | `r05-freeze-01` 的 manifest/coverage 即绑定本候选身份（哈希见 R05）——R06/R09 无需重冻结，直接引用。sst-004 已在本候选全 verdict 实机通过（`candidate20-spike-004-01`）。 |
| 判定 | R01–R05 全部修复均已进入本候选；后续 R07–R09 全部以本候选身份执行。 |



## R07 ACC-001—004 前置验收

| 项目 | 内容 |
| --- | --- |
| 状态 | 通过（ACC-001—004 全部以最终候选身份执行） |
| ACC-001 | 生命周期/两阶段构造/所有权登记反例=宿主单元测试族（test/mcp 全量 1036 passed 含 supervisor/managed/exit 全链反例；候选无关的宿主代码证据，每轮回归绿）。 |
| ACC-002 | `run_readiness_probe.py` 要求**实机执行**（`os.name=='nt'` 且 count=40 硬校验）：流程=①宿主 `git archive 2b1c0e9` + 构造 `source-identity.json`（source_commit/package_sha256/package_archive/files）②经 192.168.204.1 临时传输服务传包 ZIP+源码档案到 VM ③VM 内 Windows Python 运行探针（--package-root/--source-root/--manifest/--count 40）④回传报告。参数以现场 `--help` 为准（已核：package-root/source-root/output/source-commit/package-sha256/manifest/count）。 |
| ACC-003/004 | 十 provider 健康矩阵与 GUI validator 矩阵：同样实机（Windows）执行，入口待核（原方案 ACC 节）。 |
| 执行与证据 | **ACC-002**：宿主 `git archive 2b1c0e9`+691 文件逐件 sha256 构造 `source-identity.json`（含 42.6MB 候选 ZIP，VM 端字节核验 SHA 一致）；经 192.168.204.1 临时服务传入 .233（E:\FakeNet-NG-MCP-evidence\acc002-final，服务用毕即停）；VM 内 C:\Python313 运行探针（--count 40，--package-root=C:\Program Files\FakeNet-NG-MCP）：**40/40 通过**（ready 时延 378-614ms、cold stop 全 {'stopped':True}、exit 0、helpers 缺席、句柄数稳定、两枚负探针均拒绝、无 299/operation-not-allowed）；报告回传 `acc002-final/readiness-result.json`（sha256 05710e99…，VM 端同哈希核验）。**ACC-003**：钉版 Docker/Wine Windows-Python 运行指定五件套 `test_listener_health+test_relay_health+test_http_listener_stop+test_proxy_listener_thread_exit+test_fakenet_stop_lifecycle`：**49/0/0/0skip**（JUnit `acc003-windows.xml` sha 428654ad…）。**ACC-004**：Windows-Python `test_gui_validator+test_gui_schema+test_egresspolicy` **126/0/0**（`acc004-windows.xml` sha 5392a1bb…）+ 本机 `test_egresspolicy+test_linux_reviewed_ip_guard` **62 passed**。ACC-001：宿主全量回归（1036 passed）既含两阶段构造/所有权反例族。 |
| 下一步 | R08（P05 冒烟 + 完整五类 Spike + ACC-007）以同候选执行。 |



## R08 P05冒烟、完整五类Spike、ACC-007

| 项目 | 内容 |
| --- | --- |
| 状态 | **通过（P05 全链 + 五类 Spike + ACC-007 门禁测试全过）** |
| P05（`candidate20-p05-r3/`，候选20） | normal `builtin=3,custom=2` → **ACC-012 pass**；fault `per-class=1` → **ACC-013 pass**（含五类真实触发与收尾）；final → **ACC-017 pass**；summary → **pass**（ACC-012/013/017 全对账、Windows 版本 10.0.19045.0 实证）。注意：首根 candidate20-p05-01 的 ACC-013 首轮因 480s 收敛窗口边际失败（故障收尾实际完成但超窗；后续干净根 r3 全过，旧根保留）；曾误拷 r1 轮文件进 r2 根触发防混保护（"cannot mix existing rounds"），r2 保留为 aborted。 |
| 五类 Spike | 驱动=新 run.py（gate/P1-P7/fault-spike/finally 收尾）。候选20 首跑：sst-001(policy_pause) **pass**；sst-015(listener_stop) 失败"fault originals missing"——逐层修复两处 gate 缺陷后逐步推进：①子进程内 gate 沿用 10s 默认而探针 B2 路径建立需更久 → `ce06d03`（70s，与运行端 60s 会合对齐）；②liveness 要求 relay NAT 映射 active，而 B2 reviewed 流无映射 → `97da049`（`relay_client_mapping_state` 三态+无映射回退"WinDivert 句柄在且未停"）。候选22（`mcp-c97da0492-07c9e5e547a4`，deployment-22）：sst-001 **pass**、sst-015 故障真实触发且裁决运行，但区间包含失败于**左边界**：trigger_lower(617127801) 早于 session_begin_upper(64367898→实为 …643678xxx) 约 26ms——注入时点距 established 仅 ~10ms，落在 ±15.625ms 保守裕度带内（与 R02 的右边界失败同族、同根因：2×分辨率裕度 vs 毫秒级物理）。gap 右侧为 −42.6s（宽裕）。收尾七门全过。sst-002/003/004 因首败停止未执行。 |
| 判定 | 五类 Spike 的阻断与 R02 同根：**diverter_stop（右边界）与 listener_stop（左边界）两类的动作时点物理上都在会话边际 ±15.625ms 裕度带内**。这不改变 R02 选项 A/B 的用户决策框架，且将影响面从 1 类扩至 ≥2 类（Spike 的 002/015）。选项 A（fault-action 记录 native 界替代统一 ±15.625ms）可同时解除两者。 |
| 历史记录保留；最终见上行候选24验证。 |
| 最终验证（候选24） | `candidate24-fault-spike-01`：**五类全 pass**（policy_pause/sst-001、listener_stop/sst-015、diverter_stop/sst-002、child_hang/sst-003、cleanup_error/sst-004），driver passed=true、problems=[]、not_executed=[]、IPC 恢复、final-closeout 七门全过；fault-spike-result.json 绑定候选24 身份。ACC-007 准入门禁：`test_scenario_spike_gate.py` 25 passed（错候选/缺类/重复类/错run/损坏原件/synthetic 等拒绝路径）。 |



## R09 最终候选实际100串行执行

| 项目 | 内容 |
| --- | --- |
| 状态 | **暂停**（r09-run-06 于 sst-038 停：25 pass / 1 fail（工具缺陷）；修复 `ba6cd6f` 已提交，待用户命令后以新根 r09-run-07 重启串行） |
| 最终候选（更新） | **`mcp-cac6341f9-62ed01c4b41d`**，source `ac6341f91fd579a2129ffde5d1ea84910c08b76b`，ZIP sha256 `62ed01c4b41d4825f7ef0e7a39bcf0a6c286b32fa881d712f9ac86a175c442e5`（包 `dist/mcp-final100-20260921-skew-01/`，构建 `build-skew-01.log` verification PASS，deployment-24 受控部署）。R06 登记的候选20 由后续 4 个修复提交（ce06d03/97da049/92b4c154/ac6341f）演进为候选24，全部进入本候选。 |
| 身份级冻结 | `r09-freeze-01/`：`generate --regen-check` 通过；manifest sha256 `62b3ac46…`（与 r05 行定义冻结同哈希——行定义未变，仅身份字段换绑候选24）、coverage `96edcb54…`、host 工具哈希清单已存。 |

| 执行启动（2026-09-22 10:5x） | `r09-run-01/run.py` 后台驱动：generate --regen-check → preflight P7（**通过**）→ run --filter benign --stop-on-first-failure（进行中）→ run --filter fault（五类门后）。此前两次启动失败已修正：run() 需位置 filter_name + --filter 标志；stale controller（上一中止驱动残留）经产品受控 stop+start 清除（endpoint 恢复 stopped/controller null）。中断安全：suite 自带 resume；首败即停。 |

| 启动期工具修复（r09-run-01→04） | ①restart+stop-window 组合的探针双重释放（`a0bc561`：重启钩子改为按交错释放首轮探针）②释放点落在 finish_capture 弹出捕获字典之后致 KeyError（`f0c…` 提交：释放提前到捕获关闭前）③重启分支与 stop 分支双份辅助案例释放致 90s 完成等待超时（`guard` 提交：stop-window 仅在 stop 分支跑案例）④run-01 边界案例从未执行（`wire` 提交：与其探针释放一并执行）。修复后 r09-run-05 首个场景 sst-005（restart+stop-window）**pass**，串行继续。宿主工具 commit 随修复前进；产品候选恒为候选24（mcp-cac6341f9-62ed01c4b41d）。 |

| 串行执行进展（r09-run-01→06） | 工具缺陷修复链：`a0bc561`（重启钩子按交错释放首轮探针）、`1057c95`（释放先于捕获关闭）、`2343076`（stop-window 仅 stop 分支跑案例）、`d72dd78`（run-01 边界案例随其探针执行）、`7fbf2b6`（tcpip 解析器接受 reassembly-flush shutdown 终止事件）、`ba6cd6f`（restart+stop-window：首轮捕获保持开启跨越重启、探针重叠重启过渡）。结果：run-05 于 sst-010 停（解析器缺口，5 pass）；run-06 于 sst-038 停（**sst-005..037 共 25 pass / 0 fail 中断前**，含此前所有失败点；sst-038=B3 process_redirect restart+stop-window 组合，run-01 仅 1 载荷即被切断——已由 ba6cd6f 修复）。VM 收尾状态由每场景 finally 保证。 |
| 暂停时点（用户指令） | 2026-09-22 用户要求二十分钟收尾后等待命令；R09 未完成（25/85 benign），R10 未开始。恢复路径：新根 r09-run-07（工具 ba6cd6f）重跑全 100；run-06 的 25 pass 保留为历史（同候选工具已变，正式账以新根为准）。 |

| r09-run-07→18 追加工具修复链 | `7fbf2b6`（tcpip 接受 reassembly-flush 终止）→ 端点无括号变体（`1b67691` 前置提交内）→ pktmon 静默启动失败（`48e359f` ETL 物化验证+重试）→ stop-window 探针 90s 启动等待不足（`e576b70` 300s）→ run-01 案例须在重启前完成（`1b67691`）→ run-02 捕获须在 run-01 捕获关闭后启动（pktmon 单会话，`bc03f2e`）→ 内核 ETL 句柄滞留（Hash-Shared 10→30s）→ 探针退出拆除 settle 1s→3s。另：VM C 盘耗尽（0.18GB）为 run-14/15 启动失败根因——WU 下载缓存 1.17GB+已导出 artifacts 累积；已清理至 3.6GB+ 并建立 25 分钟周期维护（任务已随批次停止删除）。 |
| r09-run-18 终态（首败停止） | **执行 26：sst-005..037 全 pass（25）+ sst-038 fail**；未执行 59（sst-039..100）。sst-038（B3 process_redirect+stop-window+restart）失败="restart did not publish healthy"：产品事件日志显示重启内部恢复→starting→healthy **确实达成**（recovering@3316.66→starting@3414.3→healthy@3422.8），但 restart RPC 返回 `state=stopped, last_run_outcome=failed, bound_run_id=d597adbe`——**返回值与事件日志时序矛盾**，疑 restart 在恢复路径返回了中间停止态而未等待后续 start 完成（工具 restart→supervisor.restart 路径待查）。证据保留于 r09-run-18（含 VM 端 run.log/事件全链、五节无差异、无恢复标记残留）。 |
| 下一步 | 诊断 fakenet/mcp/tools.py restart→supervisor.restart 的返回时序（为何恢复路径返回 stopped 而事件显示后续 healthy）；修复后按清单 §10.6 以新根重跑全 100（run-18 的 25 pass 因工具已变保留为历史）。 |

| 产品修复与候选25（2026-09-23） | sst-038 根因定案：**产品 quiescence 校验把 MIB 状态 11（TIME_WAIT）行也阻断**——run-01 探针连接在 restart 的 stop→start 间老化出的 TIME_WAIT 行（属主套接字已关、无载荷能力）触发 `PolicyConfigError: an existing TCP row already targets process redirect A`，run-02 启动失败→恢复→restart 返回 stopped/failed（IPC/run.log/路由全链定案，非时序悖论）。修复 `e22a60d`：仅排除状态 11，其余活态（3..10、12）保持阻断；正反例测试入 `test_process_redirect_policy.py`。候选25 `mcp-ce22a60d7-d790499e660e`（source e22a60d7…，ZIP d790499e660e…，包 `dist/mcp-final100-20260923-timewait-01/`，deployment-25 受控部署）。 |
| 候选25 同候选门 | **五类 Spike 全 pass**（`candidate25-fault-spike-01`：driver/closeout/fault-spike 三重通过，fault-spike-result sha256 eed4eee4…）；**P05 全链 pass**（`candidate25-p05-02` 干净根：normal/fault/final/summary 四模式；p05-01 根因=瞬态系统 UDP 端口 61308 基线噪声致 child_hang 轮审计漂移，保留为历史）；身份级冻结 `r09-freeze-02`（manifest 62b3ac46… 同行定义哈希，regen-check 通过）。 |
| r09-run-19 | 候选25 串行 100 执行启动（generate --regen-check + P1–P7 通过，benign 腿进行中）；25 分钟周期磁盘维护自动化已建（automation-312c8af8）。r09-run-05..18 全部保留为历史（工具多轮演进，正式账以本根为准）。 |
| 串行执行计划 | 前置已齐：P1–P7（候选24 spike 内通过）、五类 Spike（候选24 全过）、fault-spike-result.json 有效绑定。命令骨架：`generate --seed 20260912 --count 100` → `--regen-check` → `preflight --preflight-through P7` → `run --filter benign --stop-on-first-failure` → `run --filter fault --stop-on-first-failure --fault-spike-result <candidate24-fault-spike-01/fault-spike-result.json>`；容量 `--pktmon-file-size-mib 1024` 显式冻结；每例证据按清单 §10.5 保留。预计时长 ~20+ 小时，须在后续会话执行。 |

### 2026-09-23 T002：恢复严格NIC负向与native时间边界（宿主离线）

主控确定恢复原合同：精确原目标物理NIC Tx包不按RST标志/≤60字节豁免；native SNI deny仅在ETW `connect_upper <= filetime_ns <= terminal_lower` 闭区间内通过。本轮删除 `scenario_suite._pktmon_observations` 的裸RST排除、取消 `_native_deny_contained` 双侧2ms扩窗；`scenario_pktmon.parse_packets` 的Flags解析仍保留。此为裁决工具收紧，非产品源码修复、非历史结果改写，亦不将RST单包自动定性为产品根因。

新回归直接调用实际NIC选择路径：54/60字节RST和194字节数据均保留，错tuple/非绑定component/非Tx排除；native FILETIME整数在双端点允许，双侧100ns、1ms、2ms越界拒绝，错误generation/tuple/SNI和重复记录拒绝。收紧前同组7个用例失败，收紧后13个针对性用例通过；`test_scenario_suite.py` 全文件92 passed。完整 `test/mcp -q` 已尝试，但受当前隔离环境 `socket.socket()` 抛 `PermissionError: [Errno 1] Operation not permitted` 阻碍，无法将完整回归记为通过；证据在 `Logs/fakenetng-mcp/final100-20260920/sol-strict-verdict-01/pytest-first-failure.log`，其他早期失败归因未验证，不改测试断言。

同根离线重判：`sol-strict-verdict-01/rejudge.py` 对候选25五类Spike及r09-run-21十条结果调用现有 `_traffic_oracle`，输入hash前后相等。15个结果含19个run：16个healthy可重判，其中1改变、15不变；3个非healthy无traffic oracle（Spike sst-002/015、run21 sst-018）按不可判记录。改变项为候选25 Spike sst-004/run-01 辅助case2：原始 `nic_packet_count=0/pass` → 严格判定 `1/fail`，物理NIC component9上原方向 `192.168.204.233:54364 -> 198.51.100.77:1337` 的54字节`Flags [R.]`，`pktmon.txt` byte `[9308906,9309626)`；整条traffic verdict随之失败。case4 native SNI在该样本新旧仍位于严格窗口内。原Spike `passed=true`仅是旧工具的历史裁决，不再满足收紧后的同候选准入；五类门需在后续实机/合同诊断中重验。产品最终100仍未完成，run21 sst-018启动失败仍未修复/重跑。

### 2026-09-23 T004：受管 TCP 转换 tuple 的 midstream 分类修复（产品离线）

候选25 sst-004 case2 原始包链在故障 stop 前约五秒已出现 `ESTABLISHED_BYPASS`：原目标 SYN 被 `_is_new_tcp_syn` 记录，但 base NAT 转换后的本地 SYNACK 只因带 SYN 临时不绕过，其后本地回包与客户端转换 tuple 未被认作同次握手。T004 在 Windows EgressControl 的 base NAT 出包成功后记录原始、转换后及反向转换 tuple；后续这些包进入现有 NAT，真正未观察 SYN 的启动前流继续绕过。转换后本地前向包跳过再次运行前向 NAT 两个回调，以免 base NAT 将原目标映射当“stale”删除，反向源地址修正仍运行。新 SYN 对原始或转换 tuple 复用会撤销旧关联；未按端口/进程名放宽外网，未动 relay/RawListener/Job 或 oracle。

离线红例：`test_managed_nat_flow_keeps_fixup_after_handshake` 原代码实际 handler 的七包序列仅 4 次进入 NAT；初修后再发现转换后本地前向包清除映射，后续 FIN 未做源 IP 修正；tuple 复用红例捕获旧转换 key 未撤销。修复后两例通过，含原目标及转换前向、SYNACK/ACK/数据/FIN/RST、启动前/未匹配反向、UDP、SYNACK、新 SYN 复用。测试用真实 `maybe_redir_ip`/`maybe_fixup_srcip` 回调及 handler 选择，WindowsPacketCtx/发送为离线替身。日志：`Logs/fakenetng-mcp/final100-20260920/sol-midstream-fix-01/` 的 `red.log`、`red-translated-local.log`、`red-tuple-reuse.log`、`green-focused.log`。

验证：Docker/Wine 既有 Windows Python 镜像中的策略/NAT 集 `windows-wine.log` 为 108 passed；最终较广 Windows 集 `windows-wine-final.log` 为 142 passed、1 个 `test_windows_process_redirect` 失败（现有 pydivert `WinDivert` 无 `check_filter` 属性，和本改动分支无关，未改该测试）。最终 Linux 相关策略集 `related-policy-final.log` 118 passed。`test/mcp -q` 沙箱首跑因本地 socket 权限失败/停滞，停止后按任务许可提升本地权限复跑最终代码 `mcp-final.log`：1046 passed、8 skipped、12 subtests passed。未运行 VM、构建、部署或最终 100；此离线修复尚未证明候选25 RST 已消失，严格 Spike 与同候选 100 需在新产品候选实机重验。

### 2026-09-23 T005：固定依赖构建 T004 NAT 修复候选

以已提交 source `94186b56d02415cb68cdc4b1aacbbcc5c0bad3bd`，复用现有 Docker/Wine image `sha256:1b33b518a7c71ecd13264f0d1c41faf254e301b8702e2da8ca11376cff700a5e`，执行正式 `./Build-FakeNetNgMcpPackage.sh 94186b56d02415cb68cdc4b1aacbbcc5c0bad3bd dist dist/mcp-final100-20260923-natfix-01`：exit 0，候选 `mcp-c94186b56-cc14731a582c`，ZIP `dist/mcp-final100-20260923-natfix-01/fakenetng-mcp-candidate.zip` SHA-256 `cc14731a582c6df5d995d42a9b983429d8a8f32c2ecbacfaeecd64ab28b03563`，正式 verification PASS。独立重算 ZIP、内外 manifest 同字节及 199 个成员的尺寸/SHA-256 全匹配；身份和验证表在 `Logs/fakenetng-mcp/final100-20260920/sol-nat-build-01/candidate.json`，完整构建日志同根 `build.log`，原始 Windows JUnit 在 `Logs/fakenetng-mcp/builds/94186b56-avh06jsj/build-validation/`。

正式 Windows 固定依赖门：main 1695 tests / 0 failure / 0 error / 8 skip，HTTP 17 / 0 / 0 / 0；五件 T004 扩展集共 143 tests / 0 failure / 0 error / 0 skip（JUnit 逐模块核对）；frozen exe smoke 与两种 exit-role 边界均 PASS。正式测试用 stage/wheelhouse 解包的 pydivert 2.1.0 通过 `PYTHONPATH` 加载，实际模块位于 build root `pydivert21/pydivert/__init__.py`，`WinDivert.check_filter` 可调用；上一轮裸镜像是 `C:\Python311\Lib\site-packages\pydivert` 2.0.9、缺 `check_filter`。两次模块探针见 T005 根 `fixed-pydivert-probe.log` 与 `bare-pydivert-probe.log`；由此确认此前扩展集失败的环境差异，未改测试/产品源码。此构建仅可部署候选身份与离线门完成，未部署/执行 VM、P1–P7、严格 Spike 或同候选 100；candidate25 旧 Spike 不能代替新候选的门。

## Codex主控实机：NAT修复候选（2026-09-23）
- 源94186b56d02415cb68cdc4b1aacbbcc5c0bad3bd，候选mcp-c94186b56-cc14731a582c，ZIP cc14731a582c6df5d995d42a9b983429d8a8f32c2ecbacfaeecd64ab28b03563。主控独立199成员及JUnit核验，deployment-nat-01保留旧安装及配置/注册，传输已关闭；旧安装现场实核为66363b7，未假设仍是候选25。
- nat-spike004-01探索性：P1–P7及sst004全部verdict、恢复七门通过；主/四辅助原目标NIC包0。codex-nat-live-01/strict-sst004-rejudge.json独立重判一致、引用hash不变、fault issues空；case2不再出现错误ESTABLISHED_BYPASS。产品NAT修复获得真实回归支持，非100完成。
- nat-fault-spike-01完整五类：sst001(policy_pause)、sst015(listener_stop)通过；sst002(diverter_stop)仅fault_oracle失败，严格完整作用区间上界超过session下界10,262,797ns；sst003/004本根未执行。保留失败不加容差。末尾产品stopped/run/controller=null、抓包/探针无遗留、七门全部通过，driver exit3。
- fault-action中已有原生FILETIME前后采样，而当前保守裁决使用end_time_ns与统一15,625,000ns不确定度；真实动作/peer时间及证据替代正确性待核，不能因数字较小直接降低精度界。剩余ACC、五类重测及正式100继续未完。

### 2026-09-23 T008：保留完整同代原生终止与QPC绑定核对（离线）

`sst_fault_evidence.terminal_bounds` 删除按后续用户态 `send/request_sent` 等活动排除较早主/tuple 终止的规则；`scenario_tcpip.connection_events` 已选中的同代主、peer和绑定tuple原生终止全部以保守时间下界收紧会话结束。后续写入成功不能证明较早RST后的内核TCB存活，矛盾保留并由完整动作包含条件拒绝。未修改划代、`time_bounds`、时钟容差或正式QPC裁决。测试先红：主RST/tuple较早终止和旧错误完整包含反例3失败；修后针对性30通过、12 subtests通过。完整MCP测试1048通过、8跳过、12 subtests通过，见 `Logs/fakenetng-mcp/final100-20260920/sol-terminal-strict-01/mcp-full-escalated.log`；沙箱首跑受socket权限阻碍，保留`mcp-full.log`，非产品失败。

有限历史重判见同根 `rejudge.json`：仅重跑四个已有 `fault_oracle` 案例，所有引用原件及case/result前后SHA-256不变。nat-fault-spike-01的sst-001/015仍通过，sst-002仍因`overlap`失败；nat-spike004-01的sst-004仍通过。该结果不等于整条ACC重判；五类正式门仍为2通过/1失败/2未执行，100未完成。

QPC只读核对：当前sst-002 run `97243b4d-8d03-4d05-a888-906165dac2c8` 的probe ready频率10MHz、established mono `113688264776`、PID1572、creation `639257450168217879`、nonce同case；action before/after QPC区间分别为 `[113689318193,113689318214]` / `[113689333476,113689333490]`，频率10MHz、PID8024，IPC ready提供该managed PID及creation `134346218304162194`；ETL header `ReservedFlags=1`、PerfFreq10MHz、BootTime `134346104645000000`、零事件/缓冲损失，ETL SHA `72446c20…` 与case/TDH输入一致。T007的TDH已验10/10目标、59564事件，无需本轮重跑。诊断QPC最早终止距action after上界18.1762ms，不能转成正式通过：现有oracle未把action/probe/ETL的QPC频率与boot ID作跨源硬绑定，action PID只查正整数，action clock observations尚非裁决输入。待主控决定最小同boot/同run/同PID+creation/同频率的采集与验证契约，再考虑正式原生时间裁决；保持sst-002失败。

### 2026-09-23 T009：原生时钟身份诊断取证与固定源构建

源提交 `b375a1fdc200731db4c370f1e4e5030582cd41c3` 仅在故障动作增加前/后原生诊断身份（`NtQuerySystemInformation` class90 BootIdentifier 的Win10 19045 x64 32字节私有ABI、原始返回/长度/缓冲、GetProcessTimes当前PID创建FILETIME、QPC频率、VM计算机名/MachineGuid），失败输出unsupported而故障关闭与finally清理照常执行；旧FILETIME/QPC采样括号次序不变。采集工具新增显式`--native-clock-diagnostic`开关，沿同一pktmon生命周期记录capture前后和probe ready，B3另对真实子进程PID读取创建身份并保留wrapper采集者身份；默认路径不采集。`BootIdentifier` GUID与ETL header `BootTime` FILETIME分开，后者不作GUID相等判定。由于产品源码字节变化而暂停hook第127行未变，故障oracle仅增加此源的精确哈希`8d6e100d…`映射，不改正式时间/负向规则。

T007 R02原生ETL双遍与TDH脚本按来源复制到`test/mcp/acceptance/`，新`scenario_qpc_diagnostic.py`为一条只读入口：从新case完整原件重建同代事件，要求每个主/peer/tuple终止的唯一原始二进制目标、完整非时间字段配对、TDH的Tcb/端点/进程语义、跨boot/VM/PID创建/频率/nonce链以及输入前后哈希。输出manifest只称`COMPLETE_DIAGNOSTIC_ONLY`或`INCOMPLETE`，不改正式裁决。T008旧sst-002原件回放为`INCOMPLETE / UNSUPPORTED: historical capture lacks native boot/process identity`，输入哈希不变，不能给旧字段补造通过；其18.1762ms仅原诊断，正式overlap仍FAIL。入口与来源说明见`test/mcp/acceptance/scenario_qpc_diagnostic.md`。

宿主最终不可变源码全`test/mcp -q`为**1081通过、8跳过、15 subtests通过**（`sol-qpc-provenance-01/mcp-source-final.log`）。合成负例覆盖ABI状态/长度/空GUID、诊断失败仍执行故障动作、boot/PID/创建/频率/nonce错配、重复或缺终止、篡改payload和TDH Tcb；C#身份采集源由本机.NET 8编译0警告/0错误，仅是离线语法/ABI结构验证，真实Win10 API还待主控实机。旧sst-002原case另以当前正式fault oracle重判仍FAIL/overlap，全部输入SHA不变（`sol-qpc-provenance-01/formal-old-sst002.json`）。固定Docker/Wine镜像ID `sha256:1b33b518…`；正式Windows门主组**1730/0失败/0错误/8跳过**、HTTP组**17/0/0/0**，冻结EXE smoke与两种exit-role边界PASS，见`sol-qpc-provenance-01/formal-build.log`及构建根JUnit。

从该不可变源用`./Build-FakeNetNgMcpPackage.sh b375a1fdc200731db4c370f1e4e5030582cd41c3 dist dist/mcp-final100-20260923-qpc-identity-01`构建出候选`mcp-cb375a1fd-b6b6648d8fae`，ZIP `dist/mcp-final100-20260923-qpc-identity-01/fakenetng-mcp-candidate.zip` SHA-256 `b6b6648d8faee4936f2897e5db85233ccff012e99478b912b92e00f24071b4a4`，正式verification PASS。独立复算ZIP与内外manifest同字节、199文件成员尺寸/SHA一致（ZIP含manifest共200项），JUnit计数与manifest一致，证据`sol-qpc-provenance-01/package-audit.json`。仅离线构建/验证；未部署、未运行VM/Spike/ACC/100，主控须以新候选执行实机取证，正式QPC接入仍待独立决策。
