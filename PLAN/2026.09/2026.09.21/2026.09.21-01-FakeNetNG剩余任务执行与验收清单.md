# FakeNet-NG 剩余任务执行与验收清单（GLM独立执行交接）

编制日期：2026-09-21。仓库：`/home/adminn/projects/flare-fakenet-ng`。

本文是用户要求的剩余工作交接，不是完成声明，也不发布新协作任务。用户已要求暂停新增派发，后续由用户通知 GLM 独立执行。本文交接后的执行者负责诊断、实现、构建、Spike、实机测试和自验；此前“Spike仅主会话执行”的协作分工不作为此次用户指定独立执行的障碍。涉及验收语义、需求或授权范围变更仍须明确提出，不得自行降低标准。

## 1. 接手顺序与当前状态

1. 先读本仓库 `AGENTS.md`，然后核对 Git 分支、HEAD、工作树，保留无关修改。本清单编制前业务 HEAD 为 `d40365398991b78a0bae930d894a69e21c2ba981`，分支 master；本清单自身提交会使 HEAD 前进。
2. 阅读原统一方案 `PLAN/2026.09/2026.09.13/2026.09.13-06-修复方案-FakeNet-NG统一问题修复.md`，按事项读取同目录 `2026.09.13-13-实施记录-FakeNet-NG统一问题修复.md`。原方案要求及后续用户明确批准的变更共同生效；不从头重启需求调查。
3. 阅读既有核验文档 `PLAN/2026.09/2026.09.13/2026.09.20-01-完成声明核验-FakeNet100场景.md`。这是已有未跟踪成果，保持原样，不删除、不随意加入其他提交。
4. **先解决单一执行者交接。** RUN_ID=`fakenet100-20260920-091921`，协作目录 `.tmp/fakenet100-20260920-091921`。暂停指令前 SEQ65/T037/R01 已发布，任务名“绑定Job结束后的原目标重传来源”，仅允许宿主离线取证、禁止VM。编制本文时尚未收到该轮回传，不能推断它未启动、已结束或已撤销。通过执行端实际交接取得其结果及 `WORK_STOPPED=true`、后台结束事实后，再开始重叠工作；有有效结果则直接复用，不重做同一提取。主会话遵守被动等待，不为编制本文查询 GLM 进度。
5. 主控最后实机任务 session82589 已 exit3，部署任务55478已 exit0，均结束；最后恢复七门全过。上述是历史检查点，不是未来启动许可：接手时重新只读核验 VM 责任状态，不复用旧 session 编号控制进程。
6. `Logs/fakenetng-mcp/final100-20260920/progress.json` 更新停留在较早候选05阶段，**不能作为最新候选或完成数的依据**。在取得单写入责任后，保存旧文件副本，再依据新证据更新当前摘要，不改写旧失败。

**正式最终100完成数：0/100。** 已有探索性单例、构建与局部前置成果可以继承；不能跨候选拼100个pass。旧591份结果只支持100个ID有尝试、99个ID曾pass且跨多个候选，不能用于最终完成声明。

### 当前已部署候选10

| 项目 | 冻结值 |
| --- | --- |
| candidate ID | `mcp-ce1759692-3b16d9348be3` |
| 产品源码 | `e175969221e1767a723b17a3c3d170857dbf99f0` |
| ZIP SHA256 | `3b16d9348be3d7ffb534d0ed61c7ae31a16e2ed55ebdc0659d240a4e759fdec3` |
| EXE SHA256 | `c1865a5f3bfb2bfc321c6de9ae72238c2943cfaa18733d0c724b0cbb65d69a82` |
| 包 | `dist/mcp-final100-20260921-jobclock-01/` |
| 构建 | `Logs/fakenetng-mcp/final100-20260920/build-jobclock-01/` |
| 部署 | 同根 `deployment-10/deployment.json` |
| 实机探索 | 同根 `candidate10-spike-004-01/` |

构建已独立核验199成员size/SHA；Windows主组1657 passed、8 skipped，HTTP17通过，五个Job控制流测试及原生精确时钟测试实际通过。候选10实机P1–P7全过，sst-004仅 `traffic_oracle` 失败，其余十项verdict通过；IPC环境恢复及final-closeout七门通过。它只是诊断候选，不是最终验收候选。

## 2. 环境、证据与执行纪律

- 唯一测试VM：VM MCP `http://192.168.204.233:28787/mcp`；产品MCP `http://192.168.204.233:28788`；计算机 `DESKTOP-3FI41GR`；MAC `00-0C-29-C1-CA-49`；安装目录 `C:\Program Files\FakeNet-NG-MCP`。每次改变VM前同时验证电脑名和MAC。
- 不操作 `.149` 的 MalTrace 环境，MAC `00-0C-29-4C-FD-C0`。默认Win10连接器可能指向它，使用明确绑定 `.233` 的协议客户端。
- 可复用 `Logs/fakenetng-mcp/final100-20260920/deploy-tools/channel.py` 和 `deploy_verified_candidate.py`。PowerShell响应同时核对 `Status Code`、结构化内容和产品状态；接口可连接不等于操作成功。
- Runtime证据、PCAP、重放、构建日志全部写 `Logs/`；`dist/`只放分发包、manifest、包验证。每轮用独立目录，碰撞就换新名称；失败目录和旧结果保持不可变。
- 本地修复、逐功能提交、构建、受控部署及测试重试已有授权；不push、不切分支、不覆盖他人修改。不因一次子任务失败永久停止总目标，完成收尾后修复必要条件继续。
- 一个业务写入者拥有VM及共享源码/Git索引；停写未知时仅处理不重叠的只读材料，不并发部署、重测或提交。
- `needs_recovery`、恢复失败和原始failure必须保留；用产品正常stop/同责任收尾和完整审计闭合，不能直接删标记、强杀或写成正常。
- Linux测试用 `.venv-mcp-runners/bin/python`；系统python3没有pytest。socket、Docker、Git写入、Herdr遇沙箱权限错误时按实际权限提升重试，不记成产品超时或失败。
- Docker/Wine复用 `flare-fakenet-ng/gui-vm-diagnostic-builder:py3119-pyi6220`，先inspect实际identity；Wine不能代替WinDivert、真实Windows句柄/路由/ETW验收。
- 每次VM运行前后核对：时钟、身份、C/E空间、产品stopped/run/controller为空、无本任务遗留SST/探针/pktmon、default配置哈希。当前runner最低C≥1GiB/E≥3GiB仅为已有门，必须保证本轮实际采集足够空间；不得删除未导出的guest证据腾空间。
- 用户已授权必要时恢复 `.233` 的 `Snapshot 2-固定IP+开机自启WindowsMCP`；历史核验VMX为 `/home/adminn/vmware/Win10(MalBox)-FakenetNG/Win10(MalBox)-FakenetNG.vmx`。需要重置时先重新核MAC/VMX/唯一快照对应，导出guest-only证据且校验哈希。**不可套用AGENTS中MAC为4C-FD-C0的另一台VM重置命令。** 重置不用于掩盖未闭合恢复责任，须保存故障和状态。
- 用户已批准临时停止Windows Time、校正到宿主时间、正常stop重做审计、finally恢复Windows Time原运行状态；只在证据显示确有时间异常时使用，保存前后事实，不据此放宽时钟容差。

## 3. 剩余工作总表

按依赖推进，复用适用证据；方框只有满足对应通过条件后才能勾选。

| ID | 剩余项 | 先决条件 | 完成依据 |
| --- | --- | --- | --- |
| R00 | 接手责任与状态核对 | 用户通知执行、SEQ65交接闭合 | 单写入者、源码/候选/VM身份清楚 |
| R01 | sst-004原目标物理包归因及产品修复 | R00 | 原始包/代次因果链、必要产品修复和新实机严格通过 |
| R02 | sst-002 diverter_stop完整作用区间失败 | R00 | 同代主/peer完整区间包含，原生关闭证据充分 |
| R03 | 辅助SNI拒绝短窗口、VFY005严格裁决审计 | R00 | 独立原生绑定成立、正反例/真实重判一致 |
| R04 | P14恢复失败、有界操作员退出与P16回归 | R00 | 责任闭合或明确失败停留，原生恢复证据 |
| R05 | 100场景真实业务覆盖优化与冻结 | R00，可与缺陷调查顺序交替 | 100行结构及业务行为覆盖均满足 |
| R06 | 候选冻结、构建、受控部署 | R01–R05影响产品/工具的修复已完成 | 源/包/工具/manifest/部署可机械绑定 |
| R07 | ACC-001—004及同候选前置验收 | R06 | 必需单元/Windows/40次原生就绪等证据齐全 |
| R08 | P05冒烟、完整五类Spike、ACC-007 | R07 | 同候选五类真实通过、准入独立重判通过 |
| R09 | 最终候选实际100串行执行 | R05–R08 | 同候选100/100，85正常+15故障 |
| R10 | 全量重判、完整台账与最终交付 | R09 | 原件完整、所有ACC对账、无悬空恢复责任 |

## 4. R01：sst-004停止后原目标物理包

### 已确认事实

- 候选09/10均实机失败；不要再调查“是否存在物理NIC命中”。候选10run=`dd2c7b7e-d447-4cfb-ac4a-6bfd02cf7552`。
- 候选10主连接：`192.168.204.233:50254 -> 119.188.220.215:443`；case2：`:50257 -> 198.51.100.77:1337`。目标IP/端口只属于本run，下一次从结果提取，不硬编码继承。
- Job空确认UTC整数ns=`1789969134809210800`；主连接comp9 Tx晚`108038500ns`，case2晚`5371549800ns`。定位见 `candidate10-spike-004-01/codex-packet-job-order.json` 及 `run-01/originals/ipc-parent.jsonl`。
- 这证明包晚于Job空确认，**不等于精确滤波器关闭时间，也不自动豁免负向包**。候选09主包141字节不能未经解析照抄为候选10payload。
- 当前生产源码已在 `ManagedProcess.terminate` 加故障开关下原生FILETIME/QPC计时，事件为begin/returned/empty-confirmed/error；这是诊断增强，不是防泄漏修复。

### 执行步骤

1. 接受并独立核验SEQ65有效提取，或在交接已停写后完成其剩余范围：从本run result读取tuple，解析完整物理包seq/ack/flags/payload及PktGroupId出现链；比较同代TCB此前发送、重传和关闭，禁止以长度相同证明内容相同。
2. 核对probe PID/creation、原tuple/relay映射、完整TCB代次及peer终止；TCB地址复用必须分代。IPC parent/child、服务EventLog来自guest，不能误标宿主；ISO七位小数用整数保留，显示精度不当时钟误差。
3. 对冻结源码追正常 `instance.stop` 与C1异常后 `incident -> Job terminate -> recovery` 两条路径，确认原tuple残留归谁处理。退出监视器负责诊断，恢复由supervisor/baseline链负责；`entry_read_attempts`不证明监视器实际执行。
4. 用可证伪的最小对照定位问题：一次只变一个停止/连接条件，保存变量、预期差异和实际包链；不把未知因果写成产品泄漏已定位，也不把停止后包直接忽略。
5. 在已有授权范围内实现最小产品修复，先建立捕获真实错误路径的反例；异常注入入口、完整区间、最早peer终止及NIC标准保持。必要仪器代码必须说明局限。
6. 逐点验证并提交；构建新候选、部署后以新目录复跑同一严格场景。修复影响其它生命周期路径时同时跑正常stop/restart及相关故障回归。

**通过条件：** 原目标物理NIC负向按原合同消除；请求/策略/转发证据完整，sst-004所有verdict及外层恢复门通过；因果与修复有前后原件支撑。只补计时、改日志或添加豁免不算通过。语义变更确有必要时先形成具体方案并取得用户批准，保持该项未完成。

## 5. R02：sst-002故障完整区间

起点：`candidate06-spike-01`、`candidate07-spike-002-01` 及 `T024/T025/T026/T027/T028` 已接收记录。历史失败出现约28–30ms区间缺口；新原生诊断证明实际API时间与墙钟显示粒度不同，不能用±500ms邻近、日志小数位或最终句柄状态替代完整action包含。

1. 只核对最新仍适用的原件，重建action begin/end、主连接和peer最早终止下界、原生时钟采样及不确定度。
2. 检查发生了真实diverter_stop、句柄关闭对象与run绑定，故障整体区间应落在同候选同run同代目标连接内；不能跨两次连接拼区间。
3. 区分实际动作/连接时序缺陷与观测精度不足；调整业务安排或产品缺陷必须有根因，不能延迟裁决边界或忽略已发生peer FIN来使结果变绿。
4. 新候选新目录重测；保留所有失败，核恢复、incident及完整退出证据。

**通过条件：** 严格validator独立重建全action包含并通过，在线/离线一致，原生句柄证据真实，相关反例仍拒绝。单例通过还须进入R08完整同候选Spike。

## 6. R03：SNI拒绝与VFY005判据来源

起点：`d51094c`修跨UDP/TCP借证；`1a1b172`修native建立上界与删除绑定逃逸；候选09/10辅助case4仍失败。候选09曾出现保守begin_upper晚于end_lower，不能视为有效连接窗口。

1. 对同run辅助TLS case核PID、creation、protocol、src/sport、dst/dport、TCB代次、generation、parsed SNI和 `reason_code=sni_mismatch`；早于实际ClientHello解析的失败不能借SNI拒绝日志通过。
2. 按完整原件重建native begin_upper/end_lower；核日志时间区间与唯一拒绝事件绑定，不能用probe最早行减误差代替建立上界。
3. 核同目标ALLOW歧义；缺src字段的真实ALLOW日志不能默默忽略。删除binding、同时删除binding和branch、改reason/tuple、跨run/协议借证均应被独立重判拒绝。
4. VFY005专项审计列出每个时钟容差、句柄证据替代和异常允许条件的方案/源码/原生来源；禁止以既往样本通过反推规则正确。已有撤销的500ms放行、全局故障豁免保持撤销。
5. 若证据不足：明确缺的是时钟能力、事件绑定还是真实连接行为；只补可证明的采样/产品身份，不扩大时钟精度假设或故障窗口。

**通过条件：** 本场景SNI拒绝满足原合同，所有反例拒绝；在线与离线重建一致；VFY005不存在无来源的容差/替代放行。没有可满足证据时保持fail，不能只改报告。

## 7. R04：P14/P16及恢复责任

P16永久默认路由误识别已由 `04ef05d` 修复，不重做根因调查；保留五列活动默认路由与四列永久路由混淆反例。P14仍须证据化判断及闭合，不能宣称全部问题解决。

1. 用受控基线重现可恢复失败、不可满足基线和操作员退出三类路径；操作前保存恢复资料及原始配置/路由/DNS/服务/监听端点/驱动状态，说明故障来源。
2. 检查不可满足基线能否在接管前拒绝；正常恢复失败后所有权、原failure和needs_recovery是否持续可见，start是否拒绝覆盖责任。
3. 明确现有stop/同责任重试是否有界，以及操作员如何安全结束操作而不把环境未恢复写成正常。若缺陷在现有方案内，修复并加真实责任链反例；若需要新增“放弃恢复/强制清理”等产品语义，列具体变更待用户决定，不自行实现。
4. 以实际Windows重做五节恢复审计及相关异常路径；mDNS端点差异必须有owner/代次/因果归属，不能泛化豁免。
5. 容量、时钟、Windows-MCP内存仅在再次出现实证阻塞时处理；已有授权只读诊断Windows-MCP，主机源码 `/home/adminn/projects/Windows-MCP`，不改该项目。

**通过条件：** 可恢复条件闭合且无遗留责任；不可满足条件明确拒绝或保留可解释失败；操作员退出有界且不伪造恢复成功；P16反例持续通过。仍需产品决策的项明确BLOCKED及最小决策，不记完成。

## 8. R05：优化并冻结100条真实业务覆盖

输入：`test/mcp/acceptance/scenario_suite.py`；`optimized-manifest.json`、`matrix-coverage.json`等同根旧材料仅作参考。当前宿主代码包含后续应用流程改进，旧manifest不能直接冒充最终冻结版本。

1. 为每个场景列：用户目标、输入协议/应用请求、预期响应内容/语义、策略分支、会话生命周期、配置桶、故障（如有）、实际证据和对应断言。明确哪些是真实业务差异，哪些只是时序变体。
2. 覆盖TCP/UDP echo、HTTP、DNS真实请求与响应；DNS核owner/问题与响应关联，HTTP核状态与内容，TCP核帧边界，不以调用成功/有字节代替业务成功。
3. 五桶都要实际start-stop与restart；restart标签必须对应真实restart调用，B1/B4策略与时序轴解耦；探针真实不同流程不能只换标签、延迟或API调用数。
4. 保持已定结构：100条、85正常+15故障、五类故障各3条、桶数25/20/20/20/15；每场景distinct工具≥10、15接口各≥5实际场景。另核原方案桶下限B1≥25/B2≥15/B3≥15/B4≥15/default≤15。
5. 不为追求“100种全不相同”杜撰新需求；对重复/缺口先给具体覆盖理由，在授权范围内优化。涉及改变矩阵合同或业务期望的方案单列待决定。
6. 生成后执行 `--regen-check`，保存manifest和coverage的size/SHA、生成参数、host工具commit/文件哈希；从此不得在同一次正式验收里改行定义。

**通过条件：** 行为与证据映射完整、结构数满足、业务覆盖缺口闭合、重复理由可审查、确定性再生成一致。生成100行不计实机进度。

## 9. R06–R08：最终候选及全部前置门

### 修复、构建与部署

1. 每个修复点运行相关正反例后本地提交。产品修复产生新source commit；宿主裁决工具身份另冻结，不能把宿主后续修复声称已进入旧产品包。
2. 离线基本命令：`PYTHONPATH=. .venv-mcp-runners/bin/python -B -m pytest test/mcp -q`；先跑受影响子集，必要完整回归，失败定位后重试，不随意加skip。
3. 正式包命令：`./Build-FakeNetNgMcpPackage.sh <不可变commit> dist dist/<新的唯一目录>`。执行前将占位参数替换为真实值并保存完整argv。核正式包装各Windows测试/HTTP/frozen-smoke/exit角色门、所有ZIP成员size/SHA、额外成员及外层ZIP SHA。
4. 使用受控部署器，显式设置 `--expected-current-source` 为现场确认的旧安装源，保留旧树、配置和注册记录；禁止绕过身份检查。host-only传输只绑定192.168.204.1，校验后关闭。

### ACC-001—004不得漏项

- ACC-001：按原方案生命周期/两阶段构造入口反例，验证所有权登记先于资源取得、失败不丢原责任、未结束对象不释放、总截止不重开；不能只mock被审清理分支。
- ACC-002：正式候选在真实Win10执行 `test/mcp/acceptance/run_readiness_probe.py` 的40次managed-child创建→ready→原生命令行读取→冷stop→Job退出，含句柄及辅助进程结束证据；无299/operation not allowed。无ready/目标早退反例另计，不充正常40次。参数用现场 `--help` 与原方案精确填入，冻结source依赖和包一致。
- ACC-003：十种实际provider的健康矩阵，未启动、真实socket线程运行、线程已死socket仍开、缺线程、socket关闭、stop后逐项；健康只允许真实运行组合，FTP走嵌套server.socket真实路径。
- ACC-004：Windows GUI validator/ConfigModel保存重载/核心解析同输入矩阵；非法IPv4、重复、通配冲突及32/33规则和16/17地址边界一致拒绝，合法输入规范化/rule_id/config_sha不变，runtime保护未放宽。
- 既有适用证据可引用，但最终候选要求的原生/同候选项必须重验；缺证据写未完成，不从代码存在推通过。

### P05与五类Spike

1. 新候选新Logs根执行P1–P7。使用 `run_p05_release.py`：normal `builtin=3,custom=2`，fault `per-class=1`，再final、summary；参数按当前CLI核实，最后两个不带rounds-limit。normal-custom旧失败条件必须覆盖。
2. 使用正式 `scenario_suite.py fault-spike` 入口一次收集同候选五类各1；前置根与正式100根分开。选例规则、五类故障及原断言依统一方案，不以候选10单004 driver冒充完整Spike。
3. `fault-spike-result.json` 必须具备同候选identity、冻结manifest hash、五类唯一、每例run/result/case/原件引用，真实独立重判及恢复全过。
4. ACC-007核对只有passed、错候选、缺类/重复类、错run、损坏/缺原件、synthetic等均在副作用前拒绝；run/resume一致；首非pass后无下一例，但finally恢复仍执行。

**通过条件：** ACC-001—004与P05必需项齐全，完整五类Spike真实通过，故障准入拒绝反例通过；仅单例或跨候选拼五类都失败。

## 10. R09：最终候选实际100执行

1. 固定最终产品identity、宿主工具commit/hash、manifest/hash、捕获容量、配置hash和部署记录。`--pktmon-file-size-mib 1024` 是当前验证过的容量配置之一，显式冻结并核磁盘，不当业务维度；改变容量须记录影响并按前置要求复验。
2. 共同参数包括candidate/source/ZIP/manifest/verification/deployment、`.233`两端点、新suite-root、seed20260912/count100。先生成、regen-check、P7；所有实际命令保存明确argv。
3. 串行运行 `run --filter benign --stop-on-first-failure` 与 `run --filter fault --stop-on-first-failure --fault-spike-result <同候选有效文件>`。故障仅在R08通过后运行。
4. 若使用小批次入口，优先审查当前 `Logs/fakenetng-mcp/final100-20260920/formal_batch_v2.py`：只接受显式1—5个ID，参数文件schema=`fakenetng.final100.suite-argv.v1`；`--suite-argv-json <文件> --batch-id <唯一ID> --scenario-id <ID>` 可重复ID参数。调用前核IPC尝试即负责恢复、异常/损坏JSON仍执行disable、每例pre/post gate、首失败停。不要沿用旧formal_batch.py的历史哈希宣称v2已冻结。
5. 每例保留请求/响应、run链、原生TCPIP/Kernel/ETL、物理NIC pktmon及元数据、策略/异常日志、产品PCAP（配置要求时）、故障receipt及完整恢复资料。导出失败不能先删除guest原件；关闭writer/封存后核sha。
6. 首非pass：停止该批后续调度，列未执行ID，保留原错误和收尾失败。完成责任闭合后定位并修复，再开新目录重试，不能把本次fail改成pass。
7. 若产品候选变化，正式完成账重新绑定新候选，旧pass只作历史回归。工具变更必须独立重判全部受影响结果；若旧原件不足以支撑新合同，重新实测，不补写证据。

**通过条件：** 冻结manifest的100个ID在同一最终候选下都有合法通过结果，85正常/15故障结构满足，所有必需证据完整，恢复和外层门全过。attempt数、历史ever-pass、manifest生成数均不代替完成数。

## 11. R10：重判、台账与最终交付

1. 对最终根运行 `verify --integrity`、至少一次实际ID的 `--replay-dry-run` 及 `summary`；另对100条逐例从原件完整独立重建traffic/fault及恢复结论，不能只读存储passed。
2. 候选隔离台账使用 `test/mcp/acceptance/scenario_result_ledger.py`；核正式ID全集为冻结manifest集合。`covers_id_universe`或missing_ids若仅以已观察ID为全集，不构成100完整性证明。
3. 对账通过、失败、跳过、未执行、未知、重复attempt与最终选择依据；保存每个有效result及所有引用的size/hash和候选归属。旧失败留在历史，不隐去。
4. sst-034历史全局故障豁免误通过及原目标物理包负证据必须按当前标准处理；旧sst-004原目标会话重叠/恢复差异须有新严格结果和归因，不用mDNS泛化豁免。
5. 按统一方案ACC-001—007逐项映射证据；P14/VFY005及所有本文R项列最终状态。必要项目未知或未验即未完成。
6. 最终确认VM stopped/run/controller为空、恢复标记无未闭合责任、无本任务抓包/探针/传输服务、IPC/故障环境恢复、配置与五节审计符合预期；保留原failed历史，不要求把历史失败字段篡改为ok。
7. 更新当前progress与实施记录，逐点提交归属改动；交付短总结：最终源/包/工具/manifest身份、实际100结果、ACC证据索引、修复清单、失败历史、剩余风险。任何未通过则交付未完成清单及准确恢复条件。

## 12. 执行记录格式与继承索引

每个R项按以下格式追加到本目录独立实施记录（不覆盖本清单中的起始事实）：

| 项目 | 必填内容 |
| --- | --- |
| 状态 | 未开始 / 执行中 / 部分完成 / 通过 / 阻塞 |
| 输入 | source、candidate、工具、manifest、VM身份及前置门 |
| 动作 | 实际命令、变量、修改范围、开始结束、exit |
| 证据 | 新Logs路径、原件hash、正反例和实机结果 |
| 判定 | 对照通过条件逐项结论，未知单列 |
| 责任 | 当前写入者、后台结束事实、恢复结果、下一步 |
| 提交 | 本地commit及范围；只读取证说明无需提交 |

按需读取，不重复全量历史调查：

- 当前实机：`Logs/fakenetng-mcp/final100-20260920/candidate10-spike-004-01/`；构建及部署见第1节。
- 前轮完整原件：同根 `candidate09-spike-004-01/`、`candidate09-traffic-analysis-01/`、`candidate09-exit-attribution-02/`、`candidate09-exit-fetch-01/`；其中过强结论以T035-R03-Codex验收限制为准。
- 仍在交接的离线任务：`.tmp/fakenet100-20260920-091921/T037-R01-绑定Job结束后的原目标重传来源.task.md`（SEQ65）。结果未收到前不推断其状态。
- 历史误判资料：`Logs/fakenetng-mcp/fakenet100-20260920-075724/` 下 `sst034-fixed-rejudge.json`、`sst004-failure-analysis.json`、`ledger-all`、`ledger-reconciliation.json`。
- 已解决点继承：P16路由解析、跨协议日志去重、产物有界并行校验、default就绪去背景流量依赖、严格peer/SNI绑定、cleanup入口顺序及IPC恢复；适用性核对后回归，不重新从零设计。

**最终唯一完成条件：全部必需修复与ACC闭合，最终冻结候选实际100/100及完整独立重判通过，恢复责任清零且有原件证明。否则继续在授权范围内修复，或准确列出必须由用户决定的最小阻碍。**
