# 当前会话核验 — 总纲-FakeNet-NG-MCP

**结论：核查不通过。** 用户本次明确指定当前主会话亲自核验维护者整改；本次未创建或调用核验子智能体。主会话此前参与实现，角色历史保留，因此本文是主会话直接复核，不冒称满足独立上下文门禁，不自行关闭历史 CHK、不登记为第3轮有效独立核验；独立闭环仍2/5。本次只增加核查记录和Logs证据，不整改、不改规范、不产生或关闭AUD、不消耗方案审核额度。

## 1. 对象与边界

- 平台：Codex；分支 `process-redirect-v23`；核查 HEAD `26debdd572b92c75dbef412c42526d9b9abd45da`。
- 整改基点 `ec5d0fdfc21e5549dd87d2b842d2a78381452537`；源码修复 `aef5ecc19e87958b1dae572ac1b836b94105c150`；后续 `6de0e3e`、`26debdd` 仅记录提交。源码变更是 `exit_installation.py`、`exit_retention.py`、`test_exit_installation.py`，规范未变。
- 当前需求 `PLAN/2026.09/2026.09.02/2026.09.02-01-需求提炼-FakeNet-NG-MCP.md` v3，blob `c38568168fdc792280aa82d57ec7fb589e267965`。
- 当前总纲同目录 `2026.09.02-02-总纲-FakeNet-NG-MCP.md` v7，blob `4670c91f46b224b5d2890a94a545d93c060c3dcf`。二者共同构成规范边界，重点总纲§18.3/5/7及P03/P04的句柄保留、真实结束、硬截止责任。
- P01/P03/P04/P05 v7/v6/v5/v5，blob依次 `8f05a89898bf208ea0d494d432743d8cbf0a6b2d`、`3b5747bafc2a3fa99f7bc84b3b168533b100990b`、`509cfd21cc06bb73e8ba5b2304d35d50cd499bee`、`ecf0757835af966cd2264b18443d728ce3cbefd4`。完整绝对定位可由 `Logs/fakenetng-mcp/main-review-26debdd/identities.json` 和仓库根目录恢复。
- 沿用初始全面记录 `2026.09.09-01-独立核验-总纲-FakeNet-NG-MCP.md` 和第2轮记录 `2026.09.10-06-独立核验-总纲-FakeNet-NG-MCP.md`，后者blob `ca2e05d9f063bef4a7a025ed467f006dc7fecf71`。本次直接范围是CHK-067/068、关联CHK-062、整改diff和受影响退出链，不重做未整改的全部ACC，不把历史未采纳方向加入范围。
- 作者回应为 `2026.09.09-02-整改回应与PLAN-CHANGE登记-FakeNet-NG-MCP.md` §70；§70.1明确纠正§69的 `end_helpers` 实机覆盖声明。
- 受保护：`PLAN/FaknetNG.md` 用户修改未触碰（blob `2c0d09e03162dfeb3a05517f9d3755af0d5e52ce`）；旧记录/失败包/证据均未覆写。未操作Win10VM、服务、登记、防火墙或网络配置；没有产品提交。

## 2. 亲自执行与证据边界

证据目录 `Logs/fakenetng-mcp/main-review-26debdd/`。执行 ` .venv-mcp-runners/bin/python -m pytest -q test/mcp/test_exit_*.py `：**82 passed, 1 skipped**（`exit-tests.txt`）。此前两文件定向组25项通过。实际Windows API由受控替身驱动，不能把Linux测试描述为真实Win10失败注入。

执行 ` .venv-mcp-runners/bin/python Logs/fakenetng-mcp/main-review-26debdd/probes.py `，结果 `probes.json`：从git提取ec5d0fd和HEAD模块，以相同控制输入运行真实函数，未改产品。

1. 同一枚举错误5：旧版返回部分表 `[(101,'unrelated.exe')]`；新版抛 `OSError [Errno 5]`，快照关闭。确认CHK-067的错误吞掉分支已修复。
2. 剩余10ms、Next占用200ms：新版在200ms之后抛异常，并未在10ms内停止原生工作。该单行表输入下旧版同样会抛异常；本次不声称它复现了旧版“空表成功”的全部场景。此反例用于区分“最终拒绝成功”和“工作实际有界”。
3. 生成器yield后，调用方耗尽期限：截止101，时钟推进至102后再次next，仍调用 `Process32NextW`（记录时间102），之后才报超限。已过期入口也仍调用 `CreateToolhelp32Snapshot` 后才guard。证实§70.3“每次原生调用之前guard”表述并未完整实现。
4. 相同残余检查失败（`exit helper still active: 4242`）：旧 `_finish` 抛错且不关闭保留目标；新版先在finally关闭目标再向外抛错。这是aef5ecc新引入回归，见CHK-069。

构建/证据亲自核对结果保存在 `artifacts.json`：

- 候选 `mcp-caef5ecc1-d61185c2a83f`，包 `dist/mcp-remediation-20260910-aef5ecc/fakenetng-mcp-candidate.zip`，SHA-256 `d61185c2a83f6225ee661f2209476a799736262ad5913fbc1f4756ddc849647a`；清单source为aef5ecc；CRC通过。
- 原生报告 `native-aef5ecc-verify.json` SHA `988d1b0324c778652623c096b564049dc2cebefec00ac6147007845152a8a1e1`；导出ZIP SHA `a58b5b80559f7b811bd736e9580c21c98d19c2390b056580102b18306c6e12ff`，28成员逐一匹配大小/SHA。成员包含旧run，未将旧run重复计作新候选验收。当前能力run `38a6d4e3-d512-45e9-8a4b-35a7e82ff4e5`，正常run `066f77b4-0e24-49e7-93b9-5c4d8c479510`。
- 构建日志SHA `acd5660c0d9c81e534cd406e389ed7a1ed95c89f513bf2d6632d1bd29ec85a0b`；原始JUnit解析主组949总计/8跳过/0失败/0错误，即941通过；HTTP17通过。这是本次读取核对的固定构建原件，不是本次重新执行Windows构建。
- 原生报告支持安装exit0、能力自检passed（0.593s）、一次正常退出、最终Stopped/PID0且helper/managed0；报告明确 `end_helpers NOT reached`，故不支持失败分支实机通过。没有独立当前VM读取，不将已有报告描述成此刻现场状态。
- Docker镜像只读查询因本执行沙箱不允许访问docker socket被拒；未因此断言Ubuntu无Windows构建环境。缺少本次Docker重跑不影响上述确定的本地反例。`git diff --check`仅两处记录EOF空行提示，不形成产品CHK。

## 3. CHK评价

### CHK-067｜部分修复，历史状态不关闭

需求记录034/035、REQ-012/CON-007，OUT-014/ACC-014/017；总纲§18.3/5/7；P03 IMP-P03-02/04、P04 IMP-P04-02/05、P05对应验收。仅18视作穷尽、其他错误抛出及快照finally释放已由实际函数测试和错误5对照证实。其最小闭合中的“不提前关闭责任句柄”不能由此推出；本次新增CHK-069正好违反该条件，且真实失败分支证据仍缺。剩余闭合：完成失败结果到清理/目标保留/助手结束的跨层验证，提供相同候选原生证据并交独立复核。

### CHK-068｜未闭合

需求记录034/035（并邻接记录015），REQ-011/012、OUT-012/014、ACC-014/017/019；总纲§18.3/5/7；负责P03/P04/P05。正向改进：取消全表物化，deadline/budget已传入扫描和最终复查；超期空表/表尾不会直接宣告成功。剩余反例：`exit_installation.py:204` 调用Next前无guard；`:226` 创建快照前无guard；同步原生调用不因生成器或返回后的guard而被中断。10ms/200ms及yield后截止已过仍调用Next均有执行证据。影响是清理链仍可能跨越既定60秒硬边界；异常最终抛出不足以证明原生工作/句柄已在截止内结束。

最小闭合：把整个清理链的原生调用、等待、最终复查纳入可执行的统一截止机制，避免过期后继续开始工作，验证慢/挂起、已过期、大表、最终复查及助手实际结束。采用何种实现由作者决定；这里不强行指定新依赖或架构。若现选型无法满足总纲§18.8则回流PLAN-CHANGE，不得只提高上限。现有“同一个4096”实为两次独立计数，记录应准确说明；本次不另造总表预算的新需求。

### CHK-069｜OPEN：残余检查失败时仍提前释放主管保留目标句柄（整改新增）

- 需求：记录034/035、REQ-012、CON-007、RACC-014；总纲OUT-014、ACC-014/017、§18.3明确要求助手未到或失败也须到明确失败/硬超时并证实助手结束后才关闭主管保留句柄；P03 IMP-P03-02/04与P04 IMP-P04-02/05，P05负责过早关闭/迟到/结束验收。
- 实际落点/提交：`fakenet/mcp/exit_retention.py:93`—104，aef5ecc新增 `try/finally`。产品源码diff可直接定位。
- 证据/反例：`probes.py` 对同一残余助手仍存活错误运行新旧 `_finish`。旧版事件仅残余失败；新版事件为残余失败→intent失效→target_closed，随后才抛错；尚未证实助手结束。`finally` 对枚举错误/超期同样执行，无区分依据。
- 影响：原本负责支撑迟到助手取得目标的保留句柄被提前释放；后续 `_watch` 的异常清理发生在释放之后，不能倒推释放当时条件已满足。当前成功路径证据无法覆盖这条失败路径。
- 最小闭合：按已审核的真实助手结束/目标保留顺序处理残余失败，完成错误传播、清理责任和最终句柄释放，不以无限持有替代有界结束。新增跨层回归须证明“残余仍在/未知→不能提前释放；结束确证→最终释放”，并在冻结候选上复验异常/迟到/挂起分支。若需要改变顺序或硬界，登记PLAN-CHANGE候选后回流；本次不改契约。

### CHK-062与其他历史项

§70.1已明确更正 `end_helpers` 旧实机声明，且本候选报告明确仅正常路径，记录口径修复可以认可；真实失败分支覆盖仍缺，CHK-062不关闭。CHK-053—066其他项均没有本次等价关闭证据：同候选全部ACC/退出负例、100+5×10发布计数、两阶段SCM失败竞态、额外四端口及双栈PCAP、GUI/客户端/防火墙、配置与环境恢复及最终映射仍以第2轮记录逐项最小闭合条件为准。作者§70.6也明确承认未完成。本次不重新编号这些旧缺口，不以一条新候选成功路径替代。

## 4. 需求保真复算与追溯

按当前需求40条累计有效结论，直接受影响切片仍为累计32/记录034、累计33/记录035。参与者、输入输出、触发、正常授权与异常边界、方向前提/重新裁决均由当前需求和总纲§18、负责Pxx可恢复；实际结束不变量在CHK-068/069处被削弱。

应承接数 / 完整数 / 未落点数 / 语义弱化数 / 不可验收数 / 失效或复核条件缺失数：**2 / 0 / 0 / 2 / 0 / 0**。异常ID：记录034、035；CHK-068、069（关联CHK-060/062）。完整计数是整条需求语义，不因某个局部错误修好就增加为完整。

双向切片映射：累计32/33→REQ-005/012、CON-007、RACC-014→OUT-014、ACC-004-S6/014/017及总纲§18.3/5/7→P03目标保留/P04有界结束/P05实机失败门→exit_installation/exit_retention→aef5ecc→上述pytest、归因探针、新候选和run证据；反向从新finally和Next/snapshot调用回溯到相同结束不变量。P01安装回归只支持入口，P02契约未变；未据此扩大直接计数。未执行的实机矩阵有规范落点，属于证据缺项而非“不可验收”定义。后四项存在非零，不能核查通过。

## 5. 结论与交付

**核查不通过。** 可以确认“枚举错误不再静默吞掉”和“旧end_helpers证据口径已更正”；不能确认“CHK-067/068已经完整闭合”。CHK-068仍有硬截止缺口，aef5ecc新增CHK-069过早关闭回归。本文不修改第2轮记录或闭环轮次数、不替代后续独立实例结论。作者应先处理明确实现问题和失败分支证据，再对新HEAD提交复核；本会话本次核验到此停止，不自动整改。
