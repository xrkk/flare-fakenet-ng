# FakeNet-NG MCP 第1轮独立核验整改回应与规范回流登记

会话角色：作者/执行方。创建日：2026-09-09。

本文件是作者回应及变更候选登记，不是核验结论或审核记录，不关闭任何CHK/AUD。初始核查：同目录`2026.09.09-01-独立核验-总纲-FakeNet-NG-MCP.md`，blob `860bc25a25851b77c1ac988d4015ee3fea7f3c9c`，HEAD `95695d2fb24dc998b506582392eef0f23ba4d05b`。本闭环最多5个有效核验轮次，初始全面核查固定为第1轮。

当前需求v1 blob `420e0288a027cc990c0d0b353aa3702ea64b67b9`、总纲v3 blob `b7da9968da72a435b0fa53b680c137b37d754daa`不变。以下候选未获裁决前不改变规范，也不允许用核查记录代替回流。模型参数固定为Codex `gpt-5.6-sol` / `high`；尚未建立独立核验实例，因为受影响规范资格尚未恢复。

## 1. 逐项回应、唯一负责层及当前动作

| CHK | 回应 | 唯一整改负责层 | 动作/证据与阻碍 |
| --- | --- | --- | --- |
| CHK-053 | 接受 | P05 | 当前包源为95695d2，旧原生ACC源为94e7abdb，不能继承。保持OPEN；规范回流及机制整改后同候选完整原生重跑 |
| CHK-054 | 契约冲突 | 总纲（下传P03/P04/P05） | 登记PLAN-CHANGE-001；暂停进程模型、故障映射、强制收敛及发布通过判据的受影响实施 |
| CHK-055 | 契约冲突 | 总纲（下传P04/P05） | 登记PLAN-CHANGE-002；暂停审计范围/快照基线及以其为前提的计数；不默认接受185或排除阈值 |
| CHK-056 | 接受 | P02 | 实际A/B/C并发反例可重复；按原规则修活动集合、运行态串行和接受/重试语义。暂不关闭；需与P03生命周期接线共同复核 |
| CHK-057 | 接受 | P04 | 当前SCM hook/stop_event与查询关闭顺序确实不等待成功审计；受PLAN-CHANGE-001生命周期交接影响，暂停接线整改 |
| CHK-058 | 接受 | P03 | 恢复结果只写日志、责任字段空及失败提前释放均属原合同实现缺陷；与PLAN-CHANGE-001/002共同解决，未整改/未关闭 |
| CHK-059 | 接受 | P04 | 在原有限宽限配置契约内接通ServiceConfig→AppContext→RealSupervisor；不改5..600范围或默认值。独立整改，不依赖进程模型选择 |
| CHK-060 | 接受 | P04 | 不提议降低证据清单或硬上限；采集失败判定/完整性按原合同整改，转储与受管进程取证待PLAN-CHANGE-001模型明确；不采用新规范豁免 |
| CHK-061 | 接受 | P05 | 原生Win10已证实-Compass错误被S尾标掩盖；在原DNS恢复审计要求内修采集命令及错误传播，单独验证，只读不改VM DNS |
| CHK-062 | 接受 | P05 | 不接受GUI/incident/升级的合成替代；保留旧证据，新候选按既有真实语义补齐，暂不跑不完整门禁 |
| CHK-063 | 契约冲突 | P01（总纲兼容权限回流） | 登记PLAN-CHANGE-003；不把代码注释或作者推断当作2026-09-08用户原始裁决；暂停兼容范围进一步变更及发布声明 |
| CHK-064 | 契约冲突 | P03（总纲ACC合同同步） | 登记PLAN-CHANGE-004；两条已采纳需求保持有效，只请求明确完整验收矩阵归属，不扩张产品需求 |
| CHK-065 | 接受 | P01 | 精确规则验证与启动前失败关闭按原规范整改；需P03入口接线，当前不更改真实防火墙或重置VM |
| CHK-066 | 接受 | P05 | 逐轮时间线/连续性/源包身份和汇总拒绝混证按原合同补齐；依赖前序同候选资格，不把摘要补写视作完成 |

未提出不同意项，无需以不完整反证要求核验方关闭。原核查的六项保真计数仍为40/17/0/21/2/1；作者未整改的条目不能因回应或登记而记完整。两项代码整改完成后也仅记READY_TO_REVERIFY，不自行改成核查通过。

## 2. 稳定PLAN-CHANGE登记

编号已扫描当前规范/闭环目录，没有既有数字实体；下列ID一经登记即固定。状态均为`PROPOSED / WAITING_USER_INPUT`。旧`2026.09.06-07-PLAN-CHANGE候选-第8轮核验规范回流.md`作为证据输入保留，不改名、不将其候选直接视为已生效决定。

### PLAN-CHANGE-001：进程隔离、强制收敛与故障验收合同回流

- 触发：CHK-054；相关CHK-057/058/060/062。记录001、006、015、016、024—026；REQ-001/003/006/011；CON-004；OUT-001/004/007/012/013；ACC-001/004/007/012/013/019。
- 证据：P03§2/3把FakeNet与主管置于同进程；超限停止只丢引用；S6杀MCP替代FakeNet退出；P05§3接受failed/grace-exceeded为通过。初始核查§6及`Logs/fakenetng-mcp/independent-20260909-95695d2/`反例。
- 受影响稳定依赖边：总纲§8 P02→P03（身份/生命周期）；P03→P04（真实生命周期、可注入故障、基线/恢复责任）；P04→P05（完整收敛能力及计数门槛）；P01—P04→P05 ACC-017同候选。
- 负责切片：总纲§3—5/§8—9；P03§2/3、IMP-P03-02/06/08及ACC-004/007；P04停止/取证/恢复/SCM；P05故障轮通过判据。Pxx数量和顺序不变。
- 推荐裁决：保持当前需求的“FakeNet故障域不断链、仅终止本次受管树、补偿恢复/审计后才stopped”的语义，允许将P03进程内模式回流为独立受管FakeNet子进程，主管持有Job；同步P04/P05契约，移除以未完成收敛的failed替代通过。内部通信只承载现有领域生命周期/健康，不新增通用框架或产品工具。
- 备选：提供能证明同进程模型在所有原故障域下等价的具体机制和可证伪证据；在得到证明及回流前不继续该模型的发布验收。本文件不推荐降低链路保证。
- 资格恢复：用户明确模型/契约调整→作者回流负责规范→按40项及受影响正式条目重新保真→自评及必要方案审核→源快照及新包→独立CHK复核。审核与CHK轮次分别记账。

### PLAN-CHANGE-002：完整恢复审计范围和快照适用性回流

- 触发：CHK-055；相关CHK-058/061/066。记录015—017；REQ-011；RACC-012/013；总纲OUT-012/013、FB-005/007。
- 证据：UDP/高TCP新增/管理监听消失/metric变化反例均空diff；既有记录将Snapshot185附加依赖列为误差来源，而DEC-009载有失效回退条件。当前用户给定AGENTS明确183流程。
- 受影响稳定依赖边：P03→P04共享基线schema及责任；P04→P05全审计；P01—P04→P05同候选/环境身份。
- 负责切片：总纲§5.4、§9 ACC-007/008/012/013/017/019；P04§3归一化与恢复；P05§3/5逐轮门槛/连续性/快照起点。
- 推荐裁决：不接受当前无归因的UDP/端口阈值/metric豁免，保留完整恢复要求；依当前AGENTS使用指定Snapshot183作为后续门禁起点，并由作者正式处理旧DEC-009适用状态。环境噪声只能凭当次可验证归因区分，不能按端口数值盲目忽略。此处是规范回流决策，不重复请求已授权的183重置操作权限；本阶段不实际重置。
- 备选：用户若要求保留185或调整审计归因面，须给出明确新边界、适用/失效条件并回流，不能由实现为了过关修改。
- 资格恢复：完成范围/基线决定与规范同步、保真、自评、必要审核；真实残留/噪声反例和完整计数重新验证。

### PLAN-CHANGE-003：显式legacy兼容与P01冻结同步

- 触发：CHK-063；记录008；REQ-002/015、RACC-002；总纲§4兼容裁决权限、OUT-002、FB-001。
- 证据：776c21a→f73d75b→95695d2已经增加默认false的allow_legacy_protocol及四旧版本路径，P01§3仍冻结仅现代版。本闭环无法仅从代码注释确认原始用户选择的完整前提。
- 受影响稳定依赖边：P01→P02 transport/Header/schema；P01—P04→P05包身份/目标客户端端到端验收。
- 负责切片：需求记录008的兼容范围/前提（若选择保留）、总纲§4及ACC-002、P01§3/IMP-P01-02/04/06/09；不改变默认现代协议、安全边界、控制者Header及单endpoint。
- 推荐裁决：确认保留现有默认关闭、部署显式启用的legacy HTTP兼容，明确四版本列表及真实客户端矩阵，正式回流规范。它不是静默fallback；不得宣称旧版请求满足现代版必需Header合同。
- 备选：按当前P01严格冻结移除legacy部署能力并重建包。必须由用户决定，作者不擅自撤销现有能力或编造历史授权。
- 资格恢复：上述决定→正式规范回流/保真/自评/必要审核→目标客户端互操作与新包原生验证；此前当前包不可宣称总纲完成。

### PLAN-CHANGE-004：记录041/042的完整验收归属补充

- 触发：CHK-064；累计39/40、记录041/042；总纲§4管理端口/环回行；P03 IMP-P03-01。
- 证据：当前规范已有产品语义，但缺完整可执行的默认/额外/非法端口矩阵及双栈环回record-only包级证据；不以旧blob过期作为缺口。
- 受影响稳定依赖边：P03→P04网络保护前置；P03/P04→P05同候选证据；总纲ACC-004→ACC-017汇总。
- 推荐裁决：保持既有产品需求，将管理端口的默认最小集、{28787,28790}、非法值失败关闭，以及IPv4/IPv6环回“不接管但PCAP可见”矩阵明确归P03/ACC-004，并由P05/ACC-017要求同候选证据。保留ACC编号，不新建产品功能/平台承诺；验收脚本可使用明确子场景标签。
- 负责切片：总纲§9 ACC-004证据位、P03 IMP-P03-08/ACC-004、P05最终候选索引/证据门。
- 资格恢复：用户确认验收合同同步→负责规范更新、保真、自评及必要审核→原生矩阵证据；字符串测试不替代PCAP。

## 3. 当前允许继续的独立整改

CHK-059只接通已冻结数值，CHK-061只恢复已经要求的DNS采集与失败传播。两者不改变规范、Pxx边或验收边界；先构造失败回归，再修复、重测并保存证据。其余条目保持OPEN，不用这两项通过宣称整体READY_TO_REVERIFY。

## 4. 停止与恢复

四组规范决定需要用户输入，闭环最终落入`WAITING_USER_INPUT`；第2轮未启动、核验子智能体未创建，已用轮次仍1/5。不得在资格未恢复时让子智能体修改契约或关闭问题。

用户作出裁决后，先对账HEAD、工作区diff、原核查blob、闭环状态及候选登记，再执行回流。每步记录意图、实际结果、验证和检查点；只在规范资格恢复、授权内整改及证据齐备时记`READY_TO_REVERIFY`，创建指定模型的独立Codex核验实例。作者始终不能自行关闭CHK。


## 5. 已验证的局部整改与停止检查点

- 中间源码提交：`259d4ef8e6279009a4a321f0ed6ba54796bc9c92`（父提交为初始核查HEAD）。仅修改`fakenet/mcp/tools.py`、`test/mcp/acceptance/run_p05_release.py`、`test/mcp/test_service_imports.py`和新增`test/mcp/test_release_dns_capture.py`。
- CHK-059：实际AppContext把部署的stop_grace_seconds传入RealSupervisor；5/60/600接线回归通过。作者判断`READY_TO_REVERIFY`，CHK仍OPEN。
- CHK-061：原生PowerShell改为错误即停止的DNS JSON采集，删除错误拼写和输出端替换；空/非JSON/缺接口字段不能作为正常基线，保留合法空ServerAddresses。实际Win10返回Ethernet0 DNS `192.168.204.1`及环回空地址集合。作者判断`READY_TO_REVERIFY`，CHK仍OPEN。
- 失败先验：15个定向测试初始11 failed/4 passed；整改后15 passed。扩大回归：Linux MCP 122 passed/1 skipped；固定Docker/Wine Windows-Python MCP 117 passed/6 skipped。原始证据位于`Logs/fakenetng-mcp/remediation-20260909/`。未将Wine结果计入原生ACC。
- `git diff --check`对本轮修改路径通过；仓库全量检查报告的`PLAN/FaknetNG.md`既有CRLF/尾空白未更改、不作CHK。当前受保护文件blob为`2c0d09e03162dfeb3a05517f9d3755af0d5e52ce`，是进入本次整改时的用户修改；不同于初始核查时的旧工作区blob，未擅自回滚。
- 整体停止状态：`WAITING_USER_INPUT`，等待PLAN-CHANGE-001—004的明确决定及后续回流资格恢复。已完成有效核验轮次1/5；未创建独立核验子智能体，未消耗第2轮，未关闭CHK/AUD，未改需求/总纲/Pxx。原核查记录blob仍为`860bc25a25851b77c1ac988d4015ee3fea7f3c9c`。
- 恢复顺序：对账本检查点与提交→取得四项明确决定→规范回流、保真、自评及必要审核→继续受影响整改与新候选原生证据→整体READY_TO_REVERIFY→首次创建Codex `gpt-5.6-sol`/`high`独立上下文核验实例并发送用户要求的完整复核指令。局部通过不恢复整体资格。

## 6. 2026-09-09 用户裁决（生效方向，实施资格另核）

用户原文：“4项都采纳. 第4项额外增加端口 29095/29094. 开始吧”。

PLAN-CHANGE-001—004的推荐方向全部采纳。PLAN-CHANGE-004验收标准额外端口集扩为 `{28787,28790,29095,29094}`；默认最小排除集不变，端点仍限定host-only管理/观察用途，非法值失败关闭，信任边界变化整体重裁。四项进入规范回流/保真/自评和必要审核阶段；旧核查记录和历史AUD不改，CHK仍OPEN，已完成核验轮次仍1/5。

## 7. 必要审核意见引入（作者，非CHK关闭）

04号必要审核记录blob `5f019ee35239ccd07865ec2d9499314b9c36a63c`；AUD-PC-001—004全部接受：原子JOB_LIST创建、IPC终止性时限和自动收敛、SCM失败有限回RUNNING且领域failed/升级不得替换、当前需求v2优先级。具体合同在总纲§16.1及P03/P04/P05回流细化；计划保真自评重新计算40/40/0/0/0/0，异常ID无，待独立复审，不继承03号自评被否定的结论。新代码提交 `28256ddd428c14aeac8a39bce818eb05243ddc15`仅含CHK-056/065原合同整改，Linux128pass/1skip、Wine123pass/6skip；全部CHK仍OPEN。

## 8. PLAN-CHANGE-005：标准SCM停止与失败在线诊断的可实施边界（待裁决）

- 状态：`PROPOSED / WAITING_USER_INPUT`。这不是重新询问已采纳的001—004；四项仍已采纳，四端口集合保持。
- 触发：必要方案复审AUD-PC-003；相关CHK-054/057/058/062。主会话独立核对官方规则：[Service State Transitions](https://learn.microsoft.com/en-us/windows/win32/services/service-status-transitions)规定STOP_PENDING后唯一合法状态是STOPPED，且非法迁移API不一定报错；[HandlerEx](https://learn.microsoft.com/en-us/windows/win32/api/winsvc/nc-winsvc-lphandler_function_ex)要求接受标准STOP后执行清理并停止。因此不能以调用成功证明STOP_PENDING→RUNNING合法，也不能一边接受标准STOP一边无限保持服务。
- 当前契约冲突：记录024/026、总纲§5.5/OUT-007/ACC-019和P04同时要求“标准SCM STOP触发完整收敛”“失败保持在线诊断且不得宣称正常停止”“有界结束”。独立主管/受管子进程能隔离FakeNet故障，但不会改变主管自身受SCM管理的停止状态机。作者此前写入的回RUNNING机制撤回为不可实施草案，不能用于恢复审核资格。
- 受影响稳定依赖边：需求记录024/026→总纲§5.5/OUT-007/ACC-019；P03→P04受控退出入口；P04→P05正常停止/升级及失败保持在线证据；P01安装/卸载/升级入口→P05最终候选。Pxx数量/顺序与单主管边界不变。
- 推荐A（两阶段停服）：运行期/恢复未完成时不宣告接受标准SCM STOP；本地管理脚本先通过受限、固定语义的预停入口请求停止（可选SCM用户自定义控制码，仅表示“排空并收敛”，不是任意命令入口，也不新增MCP产品工具），保持SCM RUNNING及HTTP诊断，拒新变更、等在途、停止FakeNet、必要Job终止、恢复及完整审计；失败则本次尝试有限返回失败，领域failed/draining、SCM RUNNING，不替换文件。成功后冻结变更并允许标准STOP，SCM合法进入STOP_PENDING→STOPPED，升级器才替换。原有MCP领域stop仍只停止FakeNet；完整停服由本地包装入口协调。
- A的用户可见取舍：FakeNet运行/failed/recovering期间，直接`sc stop`或服务管理器停止按钮可能返回“不接受该控制”，必须使用交付的受控停服/升级入口；服务干净且允许停止时标准STOP仍可用。验收ACC-019需改为“预停控制+标准STOP”的完整序列，并验证直接STOP拒绝、失败在线、升级未替换、成功收敛后合法SCM退出和重复预停。不能宣称旧“运行中直接sc stop触发全部收敛”已实现。
- A的适用/重裁：仅当前单主管、受信host-only、管理员本地部署管理；不新增独立诊断服务或任意控制通道。若必须支持运行中直接标准STOP且同时失败后同进程在线，或本地管理入口不被接受，则回流本条，不静默模拟成功。预停/升级有界预算及身份、重试、退出门槛在用户选择后由负责P01/P03/P04细化并再次必要审核；既有取证180秒和stop_grace上限不放宽。
- 备选B（标准STOP必结束）：接受标准STOP，失败时保存完整证据/恢复责任并以非零服务退出码完成STOPPED，升级器识别失败拒绝替换；失败后当前HTTP诊断会中断，需要带外读取或后续重新启动主管做恢复诊断。此选择明确改变记录024“失败保持在线”的保证，不能默认为用户接受；不引入第二诊断主管来掩盖变化。
- 不可接受替代：STOP_PENDING→RUNNING非法迁移；无限STOP_PENDING；失败报正常STOPPED；旧日志/模拟布尔替代原生状态机证据。
- 最小恢复条件：用户选择A/B或给出可实施的新边界→作者回流记录024/026、总纲/P01/P03/P04/P05→重算需求保真/自评→必要审核通过→继续受影响实现/同候选原生门禁→独立CHK复核。此前不继续进程/SCM/恢复发布实施，也不启动CHK第2轮。

## 9. 等待PLAN-CHANGE-005期间的独立整改检查点

- 当前源码HEAD `a34394313a3a2662898adc8725c71a9cfd41836d`，初始HEAD后已有3个局部源码提交：259d4ef(停止宽限/DNS)、28256dd(并发/防火墙)、a343943(incident采集错误分类)。
- 最新Linux MCP133passed/1skipped；固定Docker/Wine Windows-Python128passed/6skipped。第一版直接解压wheelhouse的Wine运行79passed/19skipped未作为门禁，后改用固定mcp2.1.1/pydivert2.1.0离线安装重测；所有原始日志保留在Logs。
- 这些只解决部分实现反例，不完成CHK-060完整证据/硬截止等剩余要求，也不替代任何原生ACC。CHK-053—066仍OPEN，已有全面核查计第1轮，未启动第2轮。
- 规范001—004方向已采纳并回流；新增005裁决决定停服入口与失败结束边界。保留单主管/四端口/完整审计/显式兼容，不使用非法SCM迁移继续实施。用户提问已发，未收到答案前不执行依赖动作。

## 10. 必要方案复审封存与停止检查点

05号必要方案定向复审已由独立实例封存，Git blob `cadc3fb0ff50e5db8b2df9b05aaa7bf383cc2859`。本批 AUD-PC-001/002/004 由复审方关闭，AUD-PC-003 保持 OPEN；方案保真为 `40/38/0/2/0/0`，异常记录024、026。主会话已逐一重算六份规范的 blob/SHA-256，与复审记录一致。作者自评不能覆盖该独立结论，受影响实施资格未恢复。

PLAN-CHANGE-005 继续 `WAITING_USER_INPUT`；四项已采纳裁决不重开。全部实施 CHK 保持 OPEN，有效核验仍为第1轮/最多5轮，未启动第2轮。当前源码 HEAD `a34394313a3a2662898adc8725c71a9cfd41836d`，初始不可变核查和受保护文件身份未变。

## 11. PLAN-CHANGE-005已采纳并回流

用户对推荐两阶段停服回复“采纳”。状态更新为ACCEPTED / REFLOW_IN_PROGRESS，§8待裁决状态仅代表历史时点。当前需求v3、总纲v5及P01v5/P03v4/P04v3/P05v3回流总纲§17；旧非法转换草案已明确撤销。稳定依赖边和切片沿用§8。待同实例必要复审，作者不关闭AUD-PC-003或CHK。

## 12. 必要方案审核通过，继续实施

07号记录由原必要方案审核实例封存，blob `be52161aaf7bc34877ebb0f4da690432fa8465f9`。计划保真40/40/0/0/0/0、无异常，AUD-PC-003及本批引入细化项由审核方关闭。主会话已逐一重算六份规范blob/SHA无漂移，恢复PLAN-CHANGE-001—005受影响实施资格。规范页眉“待必要审核”是提交审核时状态，资格以本次固定blob的不可变07记录为准，不改已审对象。继续IMPLEMENTING，全部CHK仍OPEN、有效核验1/5轮，不以方案通过宣称实现通过。

## 13. 两阶段停服与独立受管模型中间实现

已移除主管自加入Job、进程内FakeNet与裸stop fallback，增加Job创建原子归属和固定生命周期/健康IPC；预停128、SCM合法状态及CLI等待接线，卸载/安装不再taskkill旧服务；完整审计保留UDP/全部TCP/metric，快照结构/责任字段和在线recovering已接入。Linux145通过1跳过（另新增SCM两测试通过）、Windows-Python142通过6跳过；原生Job最小探针证明子成员/主管非成员与终止清空。这里只是中间源码，完整incident、补偿细节、故障场景与同候选ACC仍未完成。下一步构建独立候选验证真实服务行为；CHK全OPEN且1/5轮。


## 14. 新候选原生诊断（作者检查点，未关闭CHK）

- `f4c5f2c`：空闲状态直接SCM STOP拒绝，CLI预停128/标准STOP成功；首次FakeNet启动因certutil本地编码污染OS stdout协议管道失败，253次状态采样无断联。失败包 `Logs/fakenetng-mcp/remediation-20260909/native-f4c5f2c-runtime.zip` SHA-256 `c78b74930df0dc28f2def2e1a690fe09002f055007b63386d4be4e030ca39496`。
- `08e509a8206f26097c127130e88f33e7d2d588af`：独立OS句柄回归通过，原生初始化不再发生协议污染；真实FTPServer只有嵌套socket而无fileno，主管健康误判。退出全审计另发现本轮新增WinDivert1.3驱动服务和Dnscache PID2128的UDP57135/65214消失；当前failed/恢复责任保留，不放行。错误工作目录造成PCAP/HTML落在ProgramFiles，也纳入纯实现整改。完整现场含上述产物，`native-08e509a-runtime.zip` SHA-256 `f69e3fb73b4585fecab37c8d948f595b5919fb06cc11e8d824622962e812c0c5`。
- 当前修改：FTP内部socket及工作线程观测、受管子进程以本run产物目录为cwd；只对基线不存在、路径属于本安装包且无其他模块使用者的WinDivert驱动服务实施补偿，未知差异继续失败；故障注入采用P04冻结的显式本地文件，删除旧环境变量选类路径；原生采集使用实际OEM/明确UTF8编码。Linux166通过2跳过，Wine145通过6跳过（后续文件注入小改需新包构建门再验证）。
- 以上是中间源码及诊断证据，不是ACC-001—019总体验收，不继承历史150轮，不关闭CHK；有效独立核验仍1/5，下一轮2未开始。


## 15. 2026-09-10 用户明确续跑后的原生诊断与整改

用户明确VM他用结束并要求继续FakeNet-NG，恢复作者/执行方实施；此前暂停记录保持不可变。源865df0f正常启停两次诊断均healthy→stopped/ok，最新72个状态样本。原生立即CLI停止→SCM启动出现旧进程互斥句柄尚未释放，新进程拒绝；属于CHK-057已接受范围。修复为HTTP线程确认退出后释放本实例互斥，再交由SCM报告停止；失败退出仍由进程结束释放。

CHK-060继续接受：policy_pause故障已真实触发，最终stopped/last_run_outcome=failed、全量审计无差异、用户转储成功，但thread_stacks不可用，incident完整性失败。固定停止IPC会被卡顿占用；新增子进程停止期间的faulthandler定时真实线程栈文件，主管取栈IPC不可用时读取该证据，不以缓存摘要或补造内容放行。必须在新包重测。

CHK-066继续接受：P05回执原始目录和归档副本重复计数误判，已限定到artifacts/runs原始回执；配置锁探针实际以Write打开原文件、匹配Win32分享冲突32并验证前后SHA，释放后需成功打开；匹配故障配置的续跑保留服务实例，不再次停启。EvidenceWriter拒绝覆盖失败/原始证据，PowerShell缺少退出码不能算成功。其余P03/P04旧路径、完整汇总和150轮仍未完成，不声明READY_TO_REVERIFY。

证据均在Logs/fakenetng-mcp/remediation-20260910：native-865df0f-resume2-cycle.json、fault-mode-start-failure.json、fault-diagnostic-02/policy_pause.json。完整旧候选现场native-865df0f-resume2-runtime.zip，83文件逐一校验，SHA-256 b3b6d5f6445fbd8557ba80e22c4d260c16d0a6851e77474b0a88c6648bdbe0cf。保留全部失败调用日志；Wine初次缺SDK/固定pydivert及Linux受限socket失败均由正确环境重跑核实，未削减产品门槛。

当前代码验证：Linux MCP158通过2跳过，HTTP17通过；固定离线SDK+pydivert的Windows-Python154通过6跳过。新候选原生修复验证待执行。规范对象未改；CHK-053—066全部仍OPEN，有效核验1/5。此为作者实施检查点，不产生新核验结论或新的保真通过计数。


## 16. cddc780原生修复复测与下一组纯实现整改

cddc780f2badcd0ec7b522bbaa358d82b09820ff候选ZIP SHA e16bb06560c8d49ec0fd211243b7818f177a5d5de24b22f041eec42cd48f1458，构建及163文件包身份校验PASS。首次普通启停恢复审计失败；前七次完整样本均记录UDP端点变化，最后预算耗尽记录采集失败，不能据此通过。CLI预停也正确拒绝退出，保持failed在线；另暴露现有进程采集将CLI自身算入受管数量。失败现场16文件已共享只读导出校验，native-cddc780-runtime.zip SHA 3ff196b0e3aec6f0ff0d6ccb97348ed9044e8cd412bd7f18c873754e35586262。

按Snapshot183单次恢复及内存/身份门做无FakeNet对照，clean-vm-control下19次采样每次断言无FakeNet进程及任务ProgramData，18次相对首样有UDP变化，保留每个端点/PID/进程创建时间。该结果是环境观察，不构成UDP豁免，更不能替代每次运行的归因。

干净VM重新部署cddc780后，cddc780-scm-diagnostic下三个立即CLI停服→SCM启动全部通过，原互斥交接竞态修复已有原生局部证据。cddc780-policy-diagnostic下policy_pause的全部incident项完整，真实停止线程栈和用户转储均成功；该轮仍因UDP审计失败及6次HTTP查询超时而失败。另一次对同一failed run的标记诊断停止请求control-latency.json中HTTP127次/两条管理TCP均无失败，HTTP最大耗时0.009秒以内；不能用它覆盖原6次超时。

当前下一组实现修改：基线始终保留完整原始相关进程行，增加映像/命令行/创建时间和SCM注册命令；仅将映像与SCM路径一致且确为stop/uninstall/install入口者识别为控制客户端，不误作受管引擎。未知身份、外来同名映像、run/managed-child及WinDivert模块仍参与差异审计。P05复用同一采集语句。原生只读采集语句已验证。

另针对正常停止期间healthy与三项健康均false同时出现的实际时间线，停止恢复阶段改为已有recovering状态；短临界区撤销在途健康结果发布，阻止迟到探测重新报告healthy，失败态不被降级覆盖；每次新run清空前次缓存线程栈，防止跨run引用。这些是CHK-055/057/058/060既有契约内整改，不修改规范或AUD。

本组Linux161通过2跳过、Windows157通过6跳过。尚未在新候选验证；无新核验轮次，所有CHK仍OPEN。后续还需对原6次超时进行可复现分层定位、每次UDP变化归因或在原边界内修复、完成剩余P01/P03/P04/P05旧验收路径整改及同候选全部ACC/150轮，然后才提交独立复核。


## 17. 9ecda9f 原生崩溃与停止取证整改

用户明确虚拟机已空闲，继续作者/执行方整改。9ecda9f7f56c226d37abe09bdc7c0d82893f0be5 候选 ZIP SHA-256 1f992233f16028d762888a3d372277d47edfdd8714d74dbf766bf89d7f38b75a 的 policy_pause 诊断失败。Windows Event 1000 记录受管 PID2636 在 python311.dll 中发生 0xc0000005，偏移0x259ce0；导出的停止线程栈最后一次在 dpkt 活动帧遍历途中截断。该证据支持优先排查原生定时取栈的并发帧访问，但不单凭相似上游问题认定根因。必须以新候选原生复测验证。

本组改为 Python 线程定时通过 sys._current_frames 持有帧引用后格式化实际栈；停止结束时回收线程并补充最终栈。保留 run/PID/时间标识，保留原 incident 完整性与独立用户转储要求。无法取得完整证据仍失败，不以该更改豁免 GIL 卡死或任何必需项。CHK-060 接受且仍 OPEN。

同轮还记录 3 个 HTTP 超时与 28787/28788 两管理 TCP 的同时超时，以及完整 UDP 审计差异；二者尚未闭合，不能因取栈修改宣告整轮通过。原始30文件已导出逐项校验：Logs/fakenetng-mcp/remediation-20260910/native-9ecda9f-policy-runtime.zip，SHA-256 971a41fb7109217e6e0745d8871ba7e51321b0ac520ffdc07f63abab288bab02。

P01 部署预停改用已安装产品两阶段 stop 并等待旧服务进程退出，失败即停止部署；P03 通用启停辅助不再隐藏重试或强杀归一化，保留首次失败。场景专用旧路径仍需继续处理。Linux MCP164通过2跳过，Windows-Python160通过6跳过；包含阻塞期间栈采集、异常退出后采集线程回收、失败预停不部署和不隐式重试回归。此为作者实施记录，规范未变、所有 CHK 仍 OPEN、有效轮次1/5，新候选原生验收待执行。


### 17.1 新候选原生结果

2b4961586cf27a729dfa96d82e11e6818a9babb1 构建/包验证通过，ZIP SHA-256 6d356e2d7b335dbb8aac8c770b8fa394ce8eceda5e4d2bd8d3fe80b5ad5f1a0d。原生 policy_pause 运行29ec7dc7-7cf4-495f-8900-9783c4ff2244 的incident全部15项完整，真实线程栈25660字节、用户转储819732字节；237次HTTP状态查询及两个管理TCP均零错误。本轮没有重现上一候选崩溃/链路超时，不据单轮诊断关闭CHK或增加有效核验轮次。

该轮最终仍failed：listen_ports完整审计出现UDP61518消失、58381/63080新增及IPv6 DHCP546消失等差异，责任/锁保持，不能算ACC通过。后续按逐运行证据归因，不采用UDP普遍豁免。全部35文件已共享只读导出并逐项校验：Logs/fakenetng-mcp/remediation-20260910/native-2b49615-policy-runtime.zip，SHA-256 75743fafc4954af6450af8a4819e2e9301b621fc92c9434c80479bc325e4baa8。虚拟机保持本任务失败现场，继续整改。


## 18. 恢复作用范围与失败配置保护整改

本轮原始端点归属证据2b49615-udp-owners.json核对PID2128为Dnscache、PID1472为Dhcp，保留进程创建时间、映像、命令行和服务状态。当前证据不能将所有端点漂移归为环境并豁免。发现补偿仅采集IPv4 DNS却使用接口级Set，且对未变接口与已在目标状态服务重复调用；修复为Get-DnsClientServerAddress -AddressFamily IPv4定位唯一CIM对象，通过InputObject恢复差异，未变化则不修改，服务同样仅在状态不同恢复。参考Microsoft官方命令文档：https://learn.microsoft.com/en-us/powershell/module/dnsclient/set-dnsclientserveraddress 。不改变完整基线/审计要求或增加UDP豁免；是否改善实机差异待新候选验证。

同一实际生成的PowerShell恢复脚本在Win10隔离命令桩执行两遍，确认只修改变化的DNS/服务、第二遍无附加修改（compensation-script-harness.json）；前一HEAD同一检查返回EXPECTED_FAILURE/wrong DNS object（compensation-script-harness-before.json）。隔离桩未改真实网络，不能代替原生验收。首次旧脚本装配因测试globals绑定失败未执行，修正测试装配后完成反例，不计产品失败。

另对CHK-058继续接受：工具stop在runner返回failed时无条件清除ConfigStore活动配置保护。新增测试先复现失败，再改为仅真实stopped且无run_id时清除；恢复责任仍在时保留活动配置保护。此不替代原生文件句柄/控制权验证。Linux165通过2跳过，Windows-Python161通过6跳过。仅纯实现整改，无规范改动、无AUD操作，CHK全部仍OPEN，有效轮次1/5。


### 18.1 原生结果与继续定位

08d3d628df8fe98502354ca527995a6fcbaaa234候选构建及校验PASS，ZIP SHA-256 3b5123a251b4bbae88b49981818bc281a83479425e9b6dcab6803be5207dbe40。policy_pause run621b790d-cd7a-406e-9996-87f21fc802e5仍failed，incident完整；221个HTTP样本6失败，两管理TCP12失败，UDP全审计不一致。缩小补偿作用范围未解决原生问题，不宣称ACC通过。43文件已导出逐项验证，native-08d3d62-policy-runtime.zip SHA-256 cfcc131abeec22d6e200e3296ce05975ef2478352fda430de6c81e7bdc6720c6。

第一组双端口超时发生02:26:28—32，紧邻子进程02:26:23 DNS修改后，早于02:26:36 incident采集；因此不能把全部断联归因于incident后的Job终止。第二组在02:26:54—58，需继续对齐恢复调用。上述均属时序线索，不是DNS API根因已证。原始日志仅按任务证据保留，不在回应中复制与定位无关的请求内容。

顺查DEC-008落点发现非egress模式的入站record-only句柄仍为inbound and ip，不能记录IPv6环回；已有主过滤器环回排除不能替代记录要求。后续在现有P03契约内补齐并验证，不以未采纳建议扩大范围。CHK仍全OPEN，有效核验1/5。


## 19. 双栈环回观察与停服结果原子发布整改

WinDivert官方文档明确环回只在outbound出现（https://reqrypt.org/windivert-doc.html）。因此既有入站record-only句柄的实现需同时匹配主接管已排除的IPv4 127/8及IPv6 ::1出站包。沿用原SNIFF句柄、采集线程及观察来源标识，增加上述精确地址分支，元数据direction使用实际包方向；没有增加主接管范围、转发动作、句柄或对外配置。固定DLL不识别loopback关键字，实际DLL编译反例保留；改用已有支持的地址表达式后通过。符合DEC-008现有“环回不接管但保留记录”合同，未更改规范；完整原生PCAP标记包验证仍待新候选。

Windows回归抓到CHK-057范围内结果发布竞态：read_result的普通读取与os.replace竞争，超时worker写failed时WinError5，结果停留draining。修复读取允许FILE_SHARE_DELETE，已存在目标使用Windows ReplaceFileW；初次不存在目标仍原子移动。真实Win10独立临时文件证据native-shared-delete-probe.json表明MoveFileEx替换打开目标失败，native-replacefile-probe-02.json确认ReplaceFileW成功、旧句柄读old/目标路径读new。初版PowerShell将null字符串传为空字符串导致路径错误，已修正C#内传真实null后重验；未把工具错误作为产品结论。新增测试在真实读取句柄打开期间执行产品发布函数，确认读取完整旧文档、下一次读取完整新文档、缺失文件仍返回None。

固定Windows-Python MCP加入站采集回归178通过6跳过；Linux MCP165通过3跳过（平台专用新增用例），最终发布helper变更另跑service_stop定向回归。Linux直接加载Win32采集测试缺winreg，不作为产品失败，已按AGENTS切换固定Windows环境。新源码待提交/构建原生复测，所有CHK仍OPEN，有效1/5。

DNS通知独立对照：dns-notification-control-03.json以正确NULL参数调用DhcpNotifyConfigChange返回0，DNS值前后不变、71次HTTP及两管理TCP零失败。此只排除“未改DNS且无受管引擎时一次通知必然断联”，不能排除实际DNS改动、WinDivert交互或恢复调用。前两次装配/参数错误完整保留，不能拿零探测失败当正向证据。


## 20. d0a27bc实机与P05原始采样留存

d0a27bc7ae2bb33d640ef1aab0c914ae14c39889候选ZIP SHA-256 3d869e928be8d6db0479e4c8e0a63f197c66081f8976cc15440599bbc7fc3c00，构建/包验证通过。run d8e6f7a0-6678-4784-8f73-9572a6d225f6在policy_pause后真实stopped/last_run_outcome=failed，incident完整，两次完整产品恢复审计均无差异；188 HTTP及双管理TCP零失败。但外部runner采样新增UDP63411，仍按失败保留，不计有效核验或发布轮次。

DEC-008局部原生证据：127.0.0.2与::1真实UDP本地监听分别收到唯一标记并回ACK，导出的运行PCAP中逐包查到两地址请求和响应，记录包含PCAP路径/SHA、包序号/时间和IP版本。证据：d0a27bc-fault-diagnostic/loopback-listener-probe.json、d0a27bc-loopback-pcap-verification.json。归档及运行目录的重复PCAP不作重复验收次数。整份38文件现场native-d0a27bc-policy-runtime.zip逐项验证，SHA ffbd409a75dbd9afa8e275668e25682a845f1e0fadf8b95d59cbdae5acffc18b。

本轮宿主限制捕获目标VM管理TCP与目标ARP的128字节包头，240秒自动正常结束，8953条完整记录；d0a27bc-host-control.pcap SHA 724ed41e0c7b67179ce3dda8daf6d7dca907b61ca3d8cb3f07c0d20bb3fec286。没有复现断联，因此此抓包不能解释旧失败。宿主sudo需口令，使用已授权Docker原始包能力按vmnet8/目标过滤采集；停止请求前容器已按时限自动退出，未启动替代采集。相关验证JSON完整保留。

DNS改动无FakeNet子进程对照dns-change-without-fakenet.json：设置为本机地址并通知、finally恢复原NameServer值并通知均返回0，DNS前后相同、双TCP/HTTP零错误。仍不能证明接管过程中DNS/WinDivert交互安全，原失败仍待归因。

CHK-066继续接受：旧P05 capture_sections返回原始值后只持久化归一化diff，丢失前后采样原始PID及采样时间。现每次捕获在release目录排他创建独立JSON，保存完整五节原始值、起止时间、候选五项身份、采集完整性；失败保留已完成节及异常。每轮引用路径/长度/SHA，validate_round强制至少两个不同的完整采样、相同身份、原始哈希匹配、采样时间位于当前完整查询窗口。保持全部差异判据，未排除DNS/DHCP或任何UDP端口；不补写旧轮证据以使其通过。Linux165通过3跳过、Windows162通过6跳过。剩余入口/场景与所有ACC/150轮仍未完成，CHK全OPEN，有效1/5。


## 21. 真实故障入口与实际 incident 导出整改

延续 CHK 的接受回应，P03/P04/P05 共用 SCM 服务级测试模式与固定 fault-injection.json：匹配模式复用实例，切换前先受控 CLI 停服并确认旧进程退出；未消费故障文件禁止覆盖，每次使用唯一 nonce 并读回。不再使用机器级环境变量或追加伪造 traceback。P03 原异常文本入口改为真实 HTTP serve_forever 线程内抛 RuntimeError，主管受管子进程的 threading.excepthook 将真实未捕获异常堆栈写入本次 run.log；默认测试开关关闭时不安装故障钩子。该 listener_exception 属 P03 既定异常场景，不扩充 P05 固定五类稳定性计数。验收要求先 healthy、消费 nonce 匹配、实际线程堆栈、两周期内撤销健康及自动停止收敛；新钩子的原生打包运行仍待验证。

P05 故障回合现在实际导出 run 对应 incident ZIP，宿主核对整包 SHA、manifest 身份与 SHA、十四项基础文件逐项长度/SHA，绑定原始回合证据；P04 ACC-014 去除常量 localization_ok，要求实际五类故障包及包内引用可核对的开发定位记录。缺少 IPC/原生异常/取栈失败矩阵时显式阻断，未声称 ACC-014 完成。条件不触发时 userdump 的正式 skipped/no escalation condition 合法，不把它误判成基础证据缺项；必需 dump 场景仍要求实际转储文件。

d0a27bc 已安装包的 listener_stop 原生诊断 run 0db986c3-fae5-4d5e-bfc9-940bd3621a63：stopped、last_run_outcome=failed、全量外部审计无差异、160 HTTP 样本及双管理 TCP 零错误。单独实际导出的 ZIP SHA 8cd4c5c2b15b93a47e7ca48f3a94d84bf9992a926930fa2c56a11ca3e0a59efe，十四项逐项验证完整。首次宿主验证器把合法 skipped dump 误判为失败，原 incident-export-verification.json 保留，修正复验另存 incident-export-reverified.json。旧 listener_stop 回合创建时尚未集成导出绑定，不补写旧记录，也不计新发布门禁通过。

本组 Linux MCP 170 passed/3 skipped，固定 Docker/Wine Windows-Python MCP 167 passed/6 skipped；新增用例使用真实 socketserver 线程抛错，检查实际 Python 堆栈及消费回执，另含故障模式复用、受控停服失败禁改配置、唯一 nonce、篡改包成员哈希拒绝。初轮旧测试夹具遗漏 release/base 属性导致单项失败，修正夹具后重跑，失败日志保留。没有运行新的独立核验，有效轮次仍 1/5，CHK 全部 OPEN；六项实现保真沿用最近未闭合基线 40/17/0/21/2/1，未以本组测试替代全面映射重算。


## 22. 68b77d5 原生异常诊断与子进程退出入口

68b77d5c6bbdcc6682279347f761b0ffb0e63c02 固定源候选构建/164文件包校验通过，ZIP SHA 32104b0e227132963eb714bcee23e3d188424e9b8f35b5a44f0ddad8fb44b1e7。重置前 d0 场景71文件导出逐项匹配，SHA 062db4eb59b38e445e12baeadaa6ed4f2b3aaa57a7edd2d6392914d0ca37377d。指定快照唯一、一次revert、Snapshot99 checkpoint、一次vmware -x、计算机名/MAC/UUID门均通过；安装额外排除28787/28790/29095/29094。

新原生 run ca8d43ef-bbf8-48e4-aa3e-cbc8d3a5f979：healthy之后实际HTTP监听线程Thread-1在service_actions抛RuntimeError，nonce eaa9dec5-91cd-493a-bf1c-bf89c35a865e 与回执匹配，真实堆栈已导出；约2.571秒撤销健康。incident十四项基础内容与长度/SHA全部匹配，无转储触发条件时userdump合法skipped。保护性恢复审计未通过，420秒等待后原诊断明确失败，未重试停止、未禁用故障模式、未把failed清成stopped。

完整审计七次观测：先有变化的双栈UDP端口，之后新增消失，但基线UDP192.168.204.149:53572持续消失；最终期限内未完成全节采样。原PID7316为固定快照中的msedge.exe主进程，创建时间2026-08-31 08:55:14；DNS噪声归属及Edge端口归属只能作为调查证据，不能据此断言与FakeNet无关或豁免。33文件完整现场导出逐项SHA匹配，native-68b77d5-listener-exception-runtime.zip SHA a2f9446f43a79e00cd1ba0ef9c228b2df7f33bbd2d456e2cf59d372ee8a80980。自动审批曾误把导出脚本文件名替换当成故障执行而拒绝；读取原/生成脚本证明只有共享读取复制及压缩后，重新审批允许，导出完成，没有绕过拒绝执行故障。

本次诊断导入的是68b77d5版本P03 runner；等待阶段后续修改了工作树runner，Python最后格式化异常时读取新文件的行文本，diagnostic.json中源码行提示可能与运行code object不一致。实际触发、nonce、状态、原始堆栈及运行版本均另有固定证据，未改旧记录；该诊断不计正式ACC。后续正式运行须使用固定源码快照的runner且窗口内不修改其文件。

CHK既定P03范围继续整改：S6旧Get-Process按名批量强杀错误地杀主管，改为run UUID+PID+创建FILETIME固定，再核对SCM父PID、精确安装映像及managed-child命令行，打开Process句柄后仅终止该子进程并核对主管PID未变；全恢复窗口持续探活。S5改用真实受管stop阶段policy_pause并探测完整停止窗口；异常场景探活延伸到保护性清理结束。Linux MCP173通过3跳过；真实Win10 Parser.ParseInput只解析新终止脚本零语法错误，没有执行强杀；新增三组无效身份反例在任何VM调用前拒绝。上述入口完整原生验收仍待执行，CHK全部OPEN，核验1/5，未关闭AUD或扩充审核轮次。


## 23. 固定 IPC 故障矩阵与健康周期整改

按总纲§16.1补齐六个固定本地测试钩子：单次丢响应、永久丢健康响应、EOF、错run、重复和倒序响应。仍只走原health消息/私有管道，默认关闭、固定文件+nonce消费，不新增产品工具或任意命令。单次超时丢掉本次帧后接收下一递增seq，不宽容迟到旧帧；永久超时仅阻断health响应，原stacks/stop仍为固定领域操作。P05正式稳定性五类未扩充。父子侧测试日志记录实际原始请求/响应/故障动作、PID、单调时间；主管健康发布亦记录同一时钟，日志不作恢复源或重放框架。

健康循环原在每次请求结束后再wait2秒，1秒超时会使请求起点间隔约3秒；改为以请求起点调度2秒周期。确定性时钟反例检查请求起点2/4秒，首次超时3秒degraded，第二次5秒failed或4秒新响应healthy；真实匿名管道覆盖丢帧恢复、永久丢健康帧但保留stacks、EOF、错run/重复/倒序拒绝及测试开关关闭无效。

ACC-006接入六场景实际运行，每次先healthy再注入，持续HTTP探活至恢复结束，核对原始父子日志/nonce、同run恢复、终止性incident实际导出与条件转储；缺少证据或全恢复失败立即停止后续场景并保留现场。尚未补齐栈不可得/原生崩溃等其他矩阵，不宣称完整验收。新ipc_evidence还采用0.5秒检查即时发布，属于新脚本保守实现检查，不改总纲；一次拟调整该新检查的命令被自动审批以可能放宽门禁拒绝，未执行，现保留原检查。

Linux全量MCP182 passed/3 skipped、Windows-Python179 passed/6 skipped；随后增加缓存旧seq/错run伪恢复反例并定向复验。当前VM仍为68b77d5失败场景，没有发新操作。新IPC代码待提交构建及原生验证；CHK全部OPEN，有效核验1/5，最近实现六项计数40/17/0/21/2/1仍未满足通过门禁。


## 24. 1134521 原生单次 IPC 结果与审计失败 incident 遗漏

1134521b3156482c06122a8ce9eb44f8baed78b9固定候选构建/包验证PASS，ZIP SHA 65569887b4758360218fb7c8dc4614f610d05b08fb43a91263502fbf1684c536。原生runner从该git树独立冻结，不从随后变动的工作树导入。指定快照恢复及身份门通过后，run 58ced420-d87a-4313-a7d9-7d999ea8736b实际消费ipc_once_timeout：parent monotonic243.906记录seq2超时并立即degraded，244.906新有效响应恢复healthy，PID8860/创建FILETIME134334569694789286不变。双端原始管道记录及check_ipc_case通过；920次全窗口HTTP样本零错误。

随后普通stop全恢复审计失败：七次完整采样中无新增端点，但UDP0.0.0.0:59413与UDP[::]:59413从基线消失；最后一次采样在期限内未收齐各节。420秒等待结束，矩阵返回ipc_once_timeout=false，后五类未执行；没有重试清理或隐藏失败。自动审批曾因误判整个ProgramData范围拒绝导出；随后列明FakeNet专属目录18文件，再收窄到15个本run日志/PCAP/报告/基线/责任及本服务配置文件的明确列表，排除临时TLS材料，重新批准后导出。native-1134521-once-runtime.zip SHA e5b67d733bddebf1d881f1cf5cd350699df607a674f521d1b0e35a1520c55964，15文件逐项长度/SHA通过。未读取或传输其他应用目录。

原生目录证明普通stop的审计失败没有生成incident，对照supervisor.stop的differences分支确认为CHK-060范围的纯实现遗漏。现审计失败及补偿/恢复异常均调用原有有界incident采集器，并继续返回failed、保留恢复责任；清理结束前保存退出受管进程PID/创建时间、退出码及Job成员，供退出后的incident引用。没有改判恢复差异，既有IncidentCollector已用incident-02等后续目录保护前次采集，本组不更改其规范边界。新增反例检查两种失败路径均只采集一次且责任不清除；Linux MCP184 passed/3 skipped、Windows-Python181 passed/6 skipped。

额外只读环境观察：宿主ss -lun确认UDP53仅在127.0.0.53/127.0.0.54监听，基线DNS192.168.204.1未见该端口监听。这可提示系统DNS重试的调查方向，尚无当次端口变化因果证明，不据此豁免差异、修改DNS配置或增加服务。CHK仍全OPEN，有效核验1/5，实现六项保真基线40/17/0/21/2/1未替代重算；审计失败incident新改动待固定源码与原生验证。


## 25. 同run多次incident及完整汇总门

CHK-060/066继续接受：IncidentCollector已保留incident、incident-02等多个采集目录，但P05只实际导出首包，其余只检查manifest.complete。现按本run全部已观察采集目录逐个导出、核对真实内容；validate_round要求观察目录集合与不同导出集合完全一致，并重新读取每个ZIP的实际manifest、十四项基本文件及哈希，不信任缓存complete标记。P04定位记录在多包时显式引用incident_name，防止把不同采集时刻的同名文件混同；所有归档均进入结果evidence索引。IPC场景也导出全部实际incident，单次超时后普通stop明确失败时立即保留失败结果，不再等待其自行变成stopped，且导出该停止失败incident。

新增反例：首包存在而漏第二包、重复引用首包、整个ZIP哈希虽匹配但包内成员哈希错误，均拒绝。P05汇总补入总纲既有CONTROL-DEFAULT、四个CONTROL-EXTRA端口、CONTROL-INVALID、LOOPBACK-V4/V6子项；缺少任何实际结果仍不得汇总通过。只是门禁补齐，不表示对应原生场景已完成。停止清空健康标志时另保留本run最新实际final_filter（启动、健康更新和停止前采样），避免退出后新增incident丢失最终过滤器事实；新run重置此缓存。

Linux最终MCP186 passed/3 skipped。Windows-Python183 passed/6 skipped覆盖多包导出与反例；随后过滤器保留/IPC失败立即结束及全部包导出进一步修改已过Linux全回归和语法检查，最终Windows及原生验证交下一固定源码构建。

环境证据baseline-dns-hostonly-probe.json：从指定VM向基线192.168.204.1:53发送唯一fakenet.invalid测试查询，23ms返回SocketException10054，与宿主该地址无UDP53监听观察一致。尚未证实本次UDP端口变化的完整因果链，未改DNS、未引入DNS服务、未按系统进程豁免。已异步询问用户该基线DNS原应由何服务提供，属于环境信息澄清，不暂停其余授权内整改。无新的独立核验，CHK全OPEN，有效轮次1/5，未把新门禁/局部测试换算为总体完成或六项保真通过。


## 26. 50b8752永久IPC原生诊断及ACC015真实产物入口

50b8752839053143fd369ece073e1ce70a18a96b构建/包校验PASS，ZIP SHA ec7bfad279712adfafc846c5b6f047da3638977de5d4a1d18012ccf9a76e071d。经一次指定快照恢复/内存恢复及身份门，冻结runner仅执行永久IPC诊断，未声称完整矩阵。run 2c22f8e8-19d4-4dce-b109-fdbafb6d0e2d同PID8080在parent monotonic278.828的seq2超时立即degraded，280.843的seq3第二次超时立即failed，约2.015秒，满足四秒界限。843次完整窗口HTTP零错误；恢复审计仍不通过，诊断返回false。

两次incident已实际导出并逐项验证：incident ZIP107445字节、SHA96aae2e56c1d09e0248a41ae8760202ee6c8c5a4e240a63d483f79153d2fba8f，15项含真实dump；incident-02 ZIP61845字节、SHA3aa7afd8178082bd5c99a94c4dca5a5a48d797f8d72ac44a9db63ecc79368703，14基础项完整且dump无触发条件跳过。另从实际ZIP重读versions/windivert_filter确认退出后的进程事实及本轮最终filter仍在，详post-exit-facts-recheck.json。单次/永久超时局部时序通过不替代恢复审计、其他IPC/转储矩阵或完整ACC。

CHK-062的ACC015仍有合成band-test.txt，已替换：产品list_artifacts必须非空且仅含五项元数据；选择同一真实run的incident与已完成PCAP，导出全部incident并实际传输已列明PCAP。接收器仅绑定192.168.204.1随机端口，限定源VM/IP、单次路径、长度与SHA，输出Logs排他创建；finally关闭线程/监听并通过guest确认CLOSED。失败或无真实同run产物不能用临时文本补足。未知下载工具检查必须获得明确Unknown tool响应，不能把任意HTTP错误当工具不存在。

路径越界、父目录跳转、空长度、无效SHA、未完成文件五类反例均在任何接收器/VM调用前拒绝。Linux191 passed/3 skipped、Windows-Python188 passed/6 skipped；ACC015新入口仍待实机传输验证。所有CHK仍OPEN，有效核验1/5，未改变来源/规范或放宽恢复差异。


## 27. ACC015接收器关闭的实机反例与事实核对

74e657e冻结runner在50b8752已安装包的同run失败现场导出了两份真实incident，PCAP也已到宿主；但guest的1.5秒异步TCP探针返回TIMEOUT，新入口因此明确报错，原记录74e657e-band-diagnostic/diagnostic.json保留。未把传输文件已存在等同于完整场景通过。

P04要求临时服务停止，并未把收到TCP拒绝报文定义为唯一证据。修正验收核对为实际接收线程停止、已关闭socket的fileno=-1、宿主/proc/net/tcp中该端口无LISTEN记录，同时guest不能建立连接；TIMEOUT作为原始网络观察保留，只有前述宿主关闭事实同时满足才可确认本服务已停。若监听仍在，即使guest超时也失败。增加区分TIME_WAIT/其他端口与实际LISTEN的反例，传输与关闭原始事实另存排他创建的.transfer.json，失败也不丢已得到的事实。此不改变服务停止契约，不把无回应单独作为关闭证据。实际新核对仍待实机复验。


## 28. d7daa21带外传输事实与合法未知工具探针

d7daa21冻结runner在同一50b8752现场再次验证：真实PCAP91527字节、SHA960135f80f026d27c6db6496b9e5a181e029a3524708429f89b7e990c66be183双端一致；接收端192.168.204.1:45003线程已退出、socket fileno=-1、内核LISTEN记录为空，guest探针TIMEOUT原样保留。两次incident也已实际导出。但该次整体exit_code=1，因为“未知下载工具”探针缺少SDK要求的_meta，被-32602参数校验提前拒绝；没有把该400响应误报成工具不存在。

已复用既有P02 envelope()补齐协议版本/clientCapabilities及Accept头，单独实机请求返回HTTP200、isError=true、明确Unknown tool: download_artifact，证据acc015-valid-envelope-negative.json。原d7daa21失败记录不改；独立步骤事实不能拼写为同候选全ACC通过，最终冻结候选仍须重跑完整入口。Linux192项回归覆盖宿主关闭核对；本次仅修正已证实的请求信封，与其他SDK调用使用同一已验证结构。

## 29. 2026-09-10：ACC-015完整诊断及当前现场导出

固定runner源码63042dbcaef724095fbdbda97a64f5e218ae6c35，安装源码仍50b8752839053143fd369ece073e1ce70a18a96b。`Logs/fakenetng-mcp/remediation-20260910/63042db-band-diagnostic/diagnostic.json`实际exit_code=0；四项检查actual_same_run_exported、pcap_dual_sha_match、receiver_closed、no_download_tool均true。实际run为2c22f8e8-19d4-4dce-b109-fdbafb6d0e2d，两包incident及91527字节PCAP均已导出；PCAP SHA-256为960135f80f026d27c6db6496b9e5a181e029a3524708429f89b7e990c66be183。192.168.204.1:42645接收器线程停止、socket=-1、内核无LISTEN，guest关闭探针TIMEOUT原样保留。合法信封未知工具探针通过，不复用或覆盖前次失败记录。

新鲜只读inventory明确列出51个应用文件；排除3个临时TLS材料后，明确白名单复制48个文件，打包取回并逐文件校验大小与SHA。完整证据为`Logs/fakenetng-mcp/remediation-20260910/native-50b8752-permanent-runtime.zip`，SHA-256为8c68c9aaa4d230b25023777fa0aeaa60dd2a0c883287fc9fa217cc87382e63fb；核对记录为同目录`50b8752-permanent-export-verification.json`。导出未停止或重置VM，未改动产品状态。

以上只形成作者整改诊断及保存现场证据，formal_acc_pass=false。需求保真未重新完成全面计算，沿用未闭合基线40/17/0/21/2/1，不把局部通过提升为总体通过。CHK-053–066全部OPEN，有效核验仍1/5，未产生或关闭AUD。下一授权内切片为P03 ACC007的精确进程身份、受管树退出及测试副作用整改；当前按进程名批量终止和清理所有notepad不能作为安全有效的验收入口。

## 30. 2026-09-10：ACC-007精确故障范围与拒绝伪证据

P03验收原先按fakenetng-mcp名称批量终止，并清理所有notepad；这既不能证明仅主管崩溃导致Job收敛，也可能伤及无关实例。本次新增`test/mcp/acceptance/crash_current_service.ps1`：固定SCM运行实例、安装镜像、当前run UUID及受管子进程PID/创建FILETIME/父进程/命令行，持有主管及观测子树的真实句柄，只对主管句柄Kill。保留子树句柄有界观察退出；旁路canary由本次Start-Process返回句柄创建、验证存活并仅清理该句柄。身份门禁失败前不创建canary，不执行Kill。观测子树退出不替代全部原子创建窗口验收。

`run_p03_acc.py`增加healthy前置门禁，新SCM PID及Running核对、真实stopped结束门槛，并比较崩溃前后新增的恢复日志，禁止历史记录充当本次审计。删除以新create_config成功代表旧命令不续接的错误断言；旧命令行为和原子创建窗口矩阵当前明确False，整个ACC007不得通过。尚需落地这两项及完整原生正向运行，不能以本文代替。

验证：Linux现有MCP回归192通过/3跳过（`Logs/fakenetng-mcp/remediation-20260910/linux-acc007-exact-crash.txt`）；Windows真实Parser.ParseInput零错误（`acc007-exact-crash-parser.json`）。当前VM已退出的PID8080真实负向调用在Get-Process拒绝，前后SCM身份相同（`acc007-stale-child-rejected.json`），未触发终止；首次负向脚本因预期StepError未捕获而exit1，随后只增加catch保存实际拒绝，未改门禁。完整正向终止/恢复尚未运行；当前安装源仍50b8752，未计正式ACC。

此切片纯验收实现整改，不修改需求/总纲/Pxx契约，不新增或关闭AUD；CHK仍全部OPEN，有效独立核验仍1/5。需求保真仍未全面重算，沿用未闭合40/17/0/21/2/1，不以局部回归替代。
