# 独立核验 — 总纲-FakeNet-NG-MCP

会话角色：用户指定的独立 Codex 定向复核替换实例。模式：第三方整改后第 2 轮有效 CHK 核验。未派子智能体，未转换为作者/执行方，未修改被核验代码、规范、Pxx、审核/实施/总体验收、闭环 JSON、`PLAN/FaknetNG.md` 或 VM。

**结论：核查不通过。** `39c6d16` 确实消除了冻结包缺少 `psutil` 导致的安装阻断，固定包和现有真实 Win10 入口证据足以支持这一局部结论；但原生进程枚举替换新增两个可归因的失败关闭/硬期限缺陷（CHK-067、CHK-068），且 CHK-053—066 均没有达到关闭条件。作者记录还把只走 `assert_no_helpers` 的正常路径过度表述为 `end_helpers` 已经实机执行，沿用 CHK-062 处理。

本轮只产生 CHK 结论，不产生、复用或关闭 AUD，不消耗方案审核批次。PLAN-CHANGE-006 的必要方案定向复审已经通过；它是当前共同规范的一部分，不是本轮 CHK 通过证据。

## 1. 固定对象、轮次与保护现场

| 对象 | 本轮固定身份 |
| --- | --- |
| 需求 | `PLAN/2026.09/2026.09.02/2026.09.02-01-需求提炼-FakeNet-NG-MCP.md`；正式需求 v3；blob `c38568168fdc792280aa82d57ec7fb589e267965`；SHA-256 `1665c3ba08ce112ec054cb674342dd47d1c3470d91ba63005a21e9e9310aab31` |
| 总纲 | 同目录 `2026.09.02-02-总纲-FakeNet-NG-MCP.md`；v7，当前边界含 §16—18；blob `4670c91f46b224b5d2890a94a545d93c060c3dcf`；SHA-256 `62e842b8acaad342b32530f40f23d04376cc8e896677f2f1993a8d33a6c77045` |
| P01 / P03 / P04 / P05 | v7 / v6 / v5 / v5；blob 依次为 `8f05a89898bf208ea0d494d432743d8cbf0a6b2d`、`3b5747bafc2a3fa99f7bc84b3b168533b100990b`、`509cfd21cc06bb73e8ba5b2304d35d50cd499bee`、`ecf0757835af966cd2264b18443d728ce3cbefd4` |
| PLAN-CHANGE-006 必要复审 | `2026.09.10-05-必要方案定向复审-PLAN-CHANGE-006.md`；blob `560947a1707029b85902ab4e7ec15c1e2e7de6ad`；结论为方案通过，不是实现/ACC/CHK 通过 |
| 初始全面核验 | `2026.09.09-01-独立核验-总纲-FakeNet-NG-MCP.md`；初始 HEAD `95695d2fb24dc998b506582392eef0f23ba4d05b`；blob `860bc25a25851b77c1ac988d4015ee3fea7f3c9c`；CHK-053—066 OPEN |
| 整改基点 | `47c8173f0bd3432ec393a86f5f4c1808eb8ee8aa`，父提交 `84823cf3e27a9c2790ebca5dd93c6ca806a11e19` |
| 整改源码 | `39c6d16df6d8e3e4baf7b0ddf867d471dd3d9682`，父提交恰为 `47c8173…`；只修改 `exit_installation.py`、`supervisor.py`、`test_exit_installation.py` |
| 核查 HEAD | 分支 `process-redirect-v23`；`ec5d0fdfc21e5549dd87d2b842d2a78381452537`，父提交恰为 `39c6d16…`；只新增作者 §69 和闭环记录提交 |
| 作者回应 | `2026.09.09-02-整改回应与PLAN-CHANGE登记-FakeNet-NG-MCP.md` §69；HEAD blob `8e3133fa9a49a14d91bda6859ebece8676b6259e` |
| 候选包 | `dist/mcp-remediation-20260910-39c6d16/fakenetng-mcp-candidate.zip`；candidate `mcp-c39c6d16d-d48ea597ff08`；SHA-256 `d48ea597ff08722073aac7c3509fcdddf6d43ca28b7702a299c7775986e98beb`；`formal_acc_pass=false` |
| 本轮 | 第 2 轮有效独立 CHK；上一个替换实例未产出记录，不计轮次；本记录完成后有效轮次为 2/5 |

进入核验和落盘前，工作区已有修改只有闭环 JSON 与 `PLAN/FaknetNG.md`。两者均由主会话/用户持有，本实例未读取其未提交内容来改变规范、未改写、未暂存、未提交。VM 未读取、重置、部署、启动、停止或改防火墙；现有原生证据足以评定本次整改，不制造新的有副作用验收。

## 2. 范围、追溯与独立执行

本轮只核验 `47c8173..39c6d16` 的第三方源码整改、`39c6d16..ec5d0fd` 的记录一致性及它们对当前共同规范、既有 CHK 和受影响通过项的影响。更早实现只作为调用链和归因背景；不以旧版需求/总纲推翻已经批准的 PLAN-CHANGE-001—006，也不把未采纳建议或新建议变成缺口。

整改的稳定追溯为：累计结论 32/记录 034 与累计结论 33/记录 035 → REQ-012/OUT-014/ACC-014（并由 ACC-004-S6、ACC-017 汇总）→ 总纲 §18.3、§18.5—8 → P03 IMP-P03-02/04、P04 IMP-P04-02/05、P05 ACC-004/014/017 → `exit_installation.py`、`exit_retention.py`、`exit_native.py` → `39c6d16` 与当前候选。P01 的安装/卸载责任及 P03/P04 的句柄结束交接也被此次替换回归触及；P02 领域接口未变。

独立执行结果：

- `git diff --check 47c8173..39c6d16` 通过；提交拓扑、三文件范围和 blob 已复核。`39c6d16` 源中产品路径不再引用 `psutil`，`supervisor.py` 依赖元数据也与冻结依赖集合一致；没有新增依赖、第二服务或 fallback。
- `test_exit_installation.py`、`test_exit_retention.py`、`test_exit_entry.py`、`test_exit_monitor.py`、`test_exit_native.py`：`30 passed in 0.16s`。新增七项测试通过，但它们把 `live_processes` 替换成 Python 列表，未进入真实 Toolhelp 枚举的错误和截止边界。
- 尝试运行 Linux `test/mcp` 全量时，首个失败是 `socket.socket()` 返回 `PermissionError: [Errno 1] Operation not permitted`，确认是本轮沙箱禁止套接字；随后中断，不把该结果归为产品缺陷，也不以它覆盖固定构建证据。
- 构建 stage 的 JUnit 原件独立解析为 Windows 主组 `937 tests / 8 skipped / 0 failures / 0 errors`，即 929 通过、8 跳过；HTTP 组 `17 tests / 0 skipped / 0 failures / 0 errors`。候选 ZIP CRC 检查无错误。
- 受控 API 反例 1：令 `live_processes()` 占用 0.2 秒，而传入截止只剩 0.01 秒，实际 `_packaged_helpers` 返回 `[]`，输出 `elapsed_seconds=0.2, deadline_overrun=True`。证明当前 deadline/budget 检查位于原生枚举物化之后。
- 受控 API 反例 2：保持 `Process32FirstW` 成功，令 `Process32NextW` 以 Win32 错误 5 返回 FALSE；实际 `live_processes()` 未抛错并返回部分表 `[(101, 'first.exe')]`。证明实现没有区分 `ERROR_NO_MORE_FILES` 与枚举失败。

## 3. 包与原生证据核对

| 证据 | 独立核对 |
| --- | --- |
| `native-47c8173-install-diagnostic-02.json` | SHA-256 `dfabd965a56c88b6f0aabe74f8e645a178d1d25cddf3202b6b1c69d579953885`；exit 1；stderr 为 `No module named 'psutil'`，与 47c8173 源码和冻结依赖相符 |
| `native-39c6d16-install.json` | SHA-256 `592d30482cd6e8a881995a7d3c8af33a2b4e5935744e18790bc06039f3348c80`；绑定源/包身份；install exit 0，登记、ACL 和清单身份可恢复 |
| `native-39c6d16-entry.json` | SHA-256 `79bcc8d0baed027543630c6e4fb75cef63fc1b5cfa779cd7d44d1f6ea8c149aa`；真实 Win10 LocalSystem 下能力自检 self-exit 非零并生成目标 dump，随后一次受控正常零退出无 dump；最终 helper/managed 计数为 0 |
| `native-39c6d16-shutdown.json` | SHA-256 `046012c81174a6bb33b4fc9fd5c69f8ef5947a688090e463420a8eba0301cafe`；两阶段 CLI 停服 exit 0、SCM Stopped/PID 0、HTTP 不可达、进程计数为 0 |
| `native-39c6d16-exit-evidence.zip` | SHA-256 `82f4d84af2c102644cf161cbb74eb1f671317ae3832c9bfcfb8fddf599bef560`；15 个成员；成员清单与导出记录一致 |
| `build-39c6d16-console.txt` | SHA-256 `1b7d93b8fdb785aaab50079b434cac54e03a00054652901d1e12699e73abf82a`；固定源构建并记录同一 candidate/source/ZIP SHA |

这些证据真实支持：安装阻断闭合、正常成功路径的 `assert_no_helpers` 能在该次现场完成、受控自检和正常退出各一次、最终现场计数为零。它们没有注入助手挂起/超限或枚举 API 失败，因此不支持 `end_helpers` 错误分支已经实机通过，也不能替代 §18.7 完整矩阵、全部 ACC 或 150 轮发布门禁。

## 4. 受整改影响切片的六项需求保真复算

计数单位沿用当前需求文档 40 条累计有效结论。按 PLAN-CHANGE-006 已批准边界，直接受整改影响切片仍是累计结论 32/记录 034 和累计结论 33/记录 035；邻接的记录 001/003/006/007/015/017/023/024/026/036 只做回归核对，不扩大直接计数。

**应承接数 / 完整数 / 未落点数 / 语义弱化数 / 不可验收数 / 失效或复核条件缺失数 = `2 / 0 / 0 / 2 / 0 / 0`。**

| 结论 | 当前可恢复语义 | 分类 |
| --- | --- | --- |
| 累计 32 / 记录 034 | P03/P04/P05、实现和真实正路径证据可定位；但 CHK-067 允许部分进程表被当作完整表，CHK-068 使助手清理/目标句柄释放链的 60 秒截止不可执行地覆盖全部实际扫描，终止性失败的有界证据与清理保证被实现削弱 | 语义弱化 |
| 累计 33 / 记录 035 | 基础证据、目标 dump、清单及完整矩阵落点可定位；但同一两个缺陷可把未检查完的助手状态当作结束事实并使必需的有界结束证据超期，现有正路径证据不能反证失败分支 | 语义弱化 |

这里“不可验收”为 0，因为当前总纲/Pxx 已给出可执行的完整正反例落点；尚未执行完整矩阵由 CHK-053/060/062/066 约束，不把“临时未执行”误分类为规范不可验收。后四项存在非零值，需求保真门禁不得通过。

## 5. 沿用 CHK-053—066 的本轮评价

### CHK-053｜OPEN：当前 HEAD 原生验收对象仍不闭合

- 需求/总纲/Pxx：REQ-015、全部实机 RACC、OUT-017、ACC-017、总纲 §8/9/18.7；P01—P05。
- 证据/反例：候选身份已固定且入口局部证据真实，解除原安装阻断；但没有同候选全部 ACC、完整退出负例、150 轮、端口/双栈 PCAP、GUI/目标客户端、防火墙/发布矩阵。`formal_acc_pass=false` 与作者 §69.2 一致。
- 影响：当前 HEAD 不可继承旧候选或局部入口通过；本次新 CHK 还使失败分支前置不成立。
- 最小闭合：修复 CHK-067/068，固定新候选，完成当前共同规范要求的全部同候选原生门禁、映射与保真复算，再独立复核。

### CHK-054｜OPEN：批准后的受管/故障模型仍缺本轮关闭证据

- 需求/总纲/Pxx：记录 001/006/015/016/025，REQ-001/003/006/011，OUT-001/004/007/012/013；总纲 §16—18；P03/P04/P05。
- 证据/反例：PLAN-CHANGE-001/006 已替代初轮“待回流”边界，不能再以旧规范冲突阻断；`39c6d16` 只改助手枚举及元数据，没有给出完整故障模型/强制收敛矩阵。
- 影响：旧 PLAN-CHANGE 候选理由失效，但实现与验收关闭条件未由本次整改满足。
- 最小闭合：按现行模型完成受管异常、卡死、退出、Job/恢复及链路连续性的同候选原生证据，并映射到当前 P03/P04/P05。

### CHK-055｜OPEN：现行完整基线合同未由本次整改闭合

- 需求/总纲/Pxx：记录 015—017，REQ-011，OUT-012/013；总纲 §16 PLAN-CHANGE-002；P03/P04/P05。
- 证据/反例：规范已经禁止端口阈值/协议豁免并要求完整原始观测；三文件整改不修改基线代码，也没有提供 TCP/UDP、路由、DNS、服务、锁的同候选完整矩阵。
- 影响：不能沿用初轮旧快照裁决，也不能把本次 helper=0 提升为环境全审计通过。
- 最小闭合：以当前完整基线合同执行同候选正反例和逐项归因，缺采集/缺节/未知差异必须失败。

### CHK-056｜OPEN：配置并发与在途幂等未进入本次整改范围

- 需求/总纲/Pxx：记录 018/020/023/032/033，REQ-009/010，OUT-010/011；P02→P03。
- 证据/反例：`39c6d16` 不修改 coordination/tools/configstore；本轮退出链 30 项及入口证据不等价于 A/B/C 生命周期交错、版本推进和同 command 重试反例。
- 影响：没有新的可归因恶化，但也没有独立关闭证据。
- 最小闭合：对当前 HEAD 复跑原并发反例、运行/停止态配置矩阵和持久重试，并提供原子状态/版本证据。

### CHK-057｜OPEN：两阶段停服现行合同仅获一次成功正路径

- 需求/总纲/Pxx：记录 024/026、REQ-006/011、OUT-007、ACC-019；总纲 §17；P01/P03/P04/P05。
- 证据/反例：`native-39c6d16-shutdown.json` 支持一次成功预停→SCM Stopped；没有首次失败在线、重复成功、在途超限、直接 STOP 拒绝、陈旧结果、升级替换失败和竞态矩阵。
- 影响：PLAN-CHANGE-005 已消除旧触发定义争议，未消除实现/验收责任。
- 最小闭合：执行 §17 全部成功与失败路径，证明失败保留 HTTP/责任、成功后冻结和真实 STOPPED，并绑定同候选/实例/attempt。

### CHK-058｜OPEN：恢复在线状态与责任关闭不由本次正路径证明

- 需求/总纲/Pxx：记录 004/017/018/020/025/026/034/038，REQ-005/006/007/011，OUT-006/007/008；P03/P04。
- 证据/反例：本次一轮正常运行以 `last_run_outcome=ok` 结束，没有恢复失败、损坏 state、启动回滚、主管异常或保留责任输入；三文件整改没有修改恢复状态接入。
- 影响：无本次新增恶化证据；不能据 stopped 正路径关闭异常恢复问题。
- 最小闭合：当前候选的 recovering/failed/责任保留与恢复通过路径均以原生故障注入验证，状态、控制权、快照七字段和审计一致。

### CHK-059｜OPEN：停止宽限传递不由本次整改证明

- 需求/总纲/Pxx：记录 015、REQ-011、OUT-012；P04 IMP-P04-01/06。
- 证据/反例：`39c6d16` 不修改宽限配置或主管构造；一次正常停止没有注入 5..600 秒边界和超限。
- 影响：既有作者 READY_TO_REVERIFY 不能代替本轮独立原生时序。
- 最小闭合：配置值到实际主管/SCM硬期限端到端一致，并以至少两个边界及超限路径原生证明。

### CHK-060｜OPEN：退出证据局部正路径成立，但硬结束仍被新缺陷破坏

- 需求/总纲/Pxx：记录 004/034/035、REQ-012、OUT-014、ACC-014；总纲 §18.3/5/6；P03/P04/P05。
- 证据/反例：能力自检的 self-exit 非零目标 dump 和一次正常免 dump 均真实；但未覆盖外部终止、外部零码、旧/迟到/错身份、超时/配额/部分文件，且 CHK-067/068 直接破坏助手失败关闭与硬期限。
- 影响：原 `psutil` 安装错误已解决，不等于 incident/退出证据全合同闭合。
- 最小闭合：先修复新缺陷，再执行 §18.7 全部正反例，逐项证明缺项失败仍继续清理、助手/句柄/临时文件最终归零。

### CHK-061｜OPEN：DNS发布采集不在本次整改范围

- 需求/总纲/Pxx：记录 015/016、REQ-011、OUT-012/013；P05 ACC-012/013/017。
- 证据/反例：三文件整改、安装 JSON 和退出 ZIP 均不验证 DNS采集器对真实差异的失败关闭；完整 150 轮未执行。
- 影响：无新增恶化归因，不形成新 CHK；旧项仍缺关闭证据。
- 最小闭合：真实 DNS 前后数据可解析，注入差异使轮次失败，随后按同候选发布矩阵复跑。

### CHK-062｜OPEN：作者对 `end_helpers` 的实机声明超过原始事实

- 需求/总纲/Pxx：记录 002/024/028/030/035，REQ-001/006/012/013，ACC-001/014/015/019；P03/P04/P05。
- 证据/反例：`ExitRetention._finish` 成功路径只调用 `assert_no_helpers`；`end_helpers` 只在截止或异常分支调用。现有两条原生退出均 `complete=true`、`helper_ended=true`，没有触发截止/异常，因此 §69 “`assert_no_helpers`/`end_helpers` 在真实 Win10 下实际执行且通过”中的后半项不可由这些文件恢复。
- 影响：局部正路径仍有效；过度声明不能补足挂起/清扫/错误分支，且会掩盖 CHK-067/068。
- 最小闭合：更正后续证据口径；用可归因挂起/异常触发实际 `end_helpers`，保存调用、时序、枚举错误、终止与最终零残留原始事实。

### CHK-063｜OPEN：现行 legacy 合同未完成目标客户端矩阵

- 需求/总纲/Pxx：记录 008、REQ-002/015、OUT-002；总纲 §16 PLAN-CHANGE-003；P01/P05。
- 证据/反例：当前规范已批准默认现代、显式 legacy 的精确边界；本次整改不修改 transport，也没有目标客户端各版本互操作和失败重裁证据。
- 影响：旧“未纳入规范”理由已被规范回流取代，但实现验收仍不能关闭。
- 最小闭合：同候选完成默认/开启两模式、各声明版本 Header/工具调用、非法/不支持版本和真实目标客户端矩阵。

### CHK-064｜OPEN：041/042 的现行原生矩阵仍未执行

- 需求/总纲/Pxx：累计 39/40、记录 041/042、OUT-004、ACC-004；总纲 §16 PLAN-CHANGE-004；P03/P05。
- 证据/反例：安装证据只证明配置中保留 28787/28790/29094/29095 字符串；没有 CONTROL-DEFAULT/EXTRA/INVALID、LOOPBACK-V4/V6、真实监听端点与标记 PCAP。
- 影响：规范落点已经闭合，原生可观察事实仍为空，不能将配置读回替代包级流量证据。
- 最小闭合：按现行子场景在同候选真实 Win10 上执行，保存过滤器、端点请求响应、双栈 PCAP/SHA及负例。

### CHK-065｜OPEN：防火墙精确 ACL 与漂移失败关闭未由安装正路径闭合

- 需求/总纲/Pxx：记录 009/011、REQ-002/003、OUT-003/004；P01→P03。
- 证据/反例：install stdout `firewall rule ensured` 与后续启动成功只证明一个正现场；本次整改没有修改 `firewall.py`，也没有禁用/宽放行/错方向/错源/规则漂移和重启负样本。
- 影响：安装顺序修正仍可保留；不能据一个允许源正连接宣称精确 ACL 或每次接管前失败关闭。
- 最小闭合：结构化核验实际生效规则的 Enabled/Direction/Action/Protocol/LocalPort/RemoteIP 精确集合，并以漂移负例证明拒启。

### CHK-066｜OPEN：完整计数与同源同包汇总仍不存在

- 需求/总纲/Pxx：记录 016/017、REQ-011/015、OUT-012/013/017、ACC-017；P05。
- 证据/反例：当前只有单次能力自检、一次正常运行/停止和入口包；作者明确未执行 100 正常+五类各10故障，未给出逐轮完整探活、VM连续性、证据SHA和混源拒绝汇总。
- 影响：candidate/source/package 的局部身份一致不能替代发布计数门。
- 最小闭合：新候选修复后从指定干净快照执行全部计数；每轮机械绑定候选/源/包/实例/完整时间线和原证据 SHA，任何失败停止并按规范重跑。

## 6. 新增 CHK

### CHK-067｜OPEN：Toolhelp 枚举错误被当作正常结束，可漏报活跃助手

- 需求/总纲/Pxx：记录 034/035、REQ-012、OUT-014、ACC-014/017；总纲 §18.3、§18.5—7；P03 IMP-P03-02/04、P04 IMP-P04-02/05、P05 ACC-014/017。
- 证据：`exit_installation.live_processes` 在 `Process32NextW` 返回 FALSE 时无条件 `break`，没有读取并要求 `ERROR_NO_MORE_FILES`。受控 API 反例令第二次枚举返回错误 5，实际函数返回部分表而非抛错；新增测试全部 mock `live_processes`，没有覆盖此分支。
- 反例：若本包 helper 位于已返回部分之后，`assert_no_helpers` 可误报无 helper；`end_helpers` 可进入完成和目标句柄释放，而未检查的 helper 仍存活。这不是范围外旧问题，代码由 `39c6d16` 新增。
- 影响：失败关闭、助手最终归零、目标句柄释放及证据完整性事实不可信；影响记录 034/035 的语义保真和 CHK-053/060/062。
- 最小闭合：仅把 `ERROR_NO_MORE_FILES` 视作正常穷尽，其他 `Process32First/Next` 错误均失败；以 Win32 API 错误注入证明不发布清理成功、不提前关闭责任句柄，并保留既有同名外来程序不误杀保证。

### CHK-068｜OPEN：4096预算和deadline没有覆盖原生枚举与最终复查

- 需求/总纲/Pxx：记录 015/034/035，REQ-011/012，OUT-012/014，ACC-014/017/019；总纲 §18.3、§18.5—7；P03/P04/P05。
- 证据：`_packaged_helpers` 调用 `live_processes()` 完整生成列表后才在 Python `for` 内检查 `budget/deadline`；`end_helpers` 完成等待后又调用不接收预算或 deadline 的 `assert_no_helpers`，再次完整枚举。受控反例在只剩 0.01 秒时令枚举耗时 0.2 秒，函数越过截止后仍返回成功空列表。
- 反例：4096 只限制已物化列表的后续消费，不限制 Toolhelp 扫描次数、耗时或内存；即使第一次扫描和终止恰好在截止内，最终复查仍可在截止外运行。现有 deadline 测试把 `live_processes` mock 成单元素列表，只证明外层检查。
- 影响：助手清理、`ExitRetention._finish`、目标句柄释放及两阶段停服可超出 60 秒合同；外层等待返回后旧线程仍可能占用责任，违反“外层超时而助手继续存活不能通过”。该问题由 `39c6d16` 的全量物化结构引入。
- 最小闭合：采用逐项/可中断的原生枚举或等价有界机制，把统一剩余截止和观察上限传入首次扫描、终止等待及最终复查；以大表、慢枚举、已过期截止和挂起助手原生/可归因测试证明在截止内明确失败且全部后台工作、句柄和进程最终结束。无需 PLAN-CHANGE。

## 7. 争议候选、结论与最小闭合边界

- `psutil` 缺失是 47c8173 的确定根因，39c6d16 的“零新增依赖、原生绝对映像认领”方向符合当前规范；不是争议候选。
- CHK-067/068 是整改新引入、已有确定反例的实现问题，不需要降低规范或登记 PLAN-CHANGE。若作者无法在既定 60 秒/清理保证内实现，才触发总纲 §18.8 的重新裁决条件；在此之前不得提高上限或静默豁免。
- 作者 §69 对 `end_helpers` 的表述属于证据范围争议，已有 CHK-062 足以承接，不另增重复 CHK。
- 完整 ACC、150轮、端口/双栈、GUI、目标客户端、防火墙和发布材料缺失是既有 CHK 的未满足条件；本轮不把它们重复编号为新问题。
- Linux 全量测试的套接字权限失败是核验沙箱环境观察，不归因产品、不形成 CHK；固定 Windows 构建与无套接字定向测试仍可用。

**最终结论：核查不通过。** 本轮有效结果为第 2 轮，CHK-053—068 共 16 项均 OPEN。安装阻断局部结论可以保留，但任一局部通过不能聚合成当前 HEAD、ACC 或发布通过。

最小闭合顺序是：修复 CHK-067/068 并补充真实失败分支证据 → 固定新源码/候选身份 → 按当前需求 v3、总纲 v7 和 P01 v7/P03 v6/P04 v5/P05 v5 执行受影响正反例与全部同候选 ACC → 跑满 100+5×10 和发布汇总 → 重做双向映射与受影响/全面六项保真 → 发起下一轮独立 CHK。后续更正必须新建记录，不覆盖本文。

