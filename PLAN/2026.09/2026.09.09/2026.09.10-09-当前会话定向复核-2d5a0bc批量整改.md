# 当前会话定向复核：2d5a0bc批量整改

结论：**核查不通过**。实施方“有确定反例的缺陷全部修掉”不成立；存在原缺陷未闭合、修复未接入调用链、修改新引入的资源泄漏。本轮一次列出本批剩余问题，不修改产品。

## 1. 对象与边界

创建时间：2026-09-10T19:54:28.230666+08:00。平台Codex，当前主会话亲自执行；承接历史作者/执行方角色，不冒充独立上下文，不关闭CHK，不消耗独立轮次（仍2/5），不产生/关闭AUD。

分支process-redirect-v23；HEAD `9f2f31ff0fb151c1b8059c91cf127623ddd1028a`；产品源 `2d5a0bca84b55de35dbc036dd38414d6c0dc1cf9`；比较基点26debdd572b92c75dbef412c42526d9b9abd45da。核对整个差异及现行调用者，并读取作者§71、回归、包和原生报告。当前需求v3/总纲v7/五Pxx的路径与blob逐份核对与08号全面记录相同，没有规范漂移。精确路径/blob见 `Logs/fakenetng-mcp/review-9f2f31f/index.json`。

前次全面记录08号blob `dea934b9576ae18b7991dce97ca8866bd982c58b`；它的全部18 OUT/19 ACC/52正式ID及40有效决定映射继续作为本次范围基底。这里复核整改影响切片，不声称重新运行完整原生ACC。CHK-053—072共20项保持OPEN，以下缺口归回已有CHK，不另拆微小新ID。

用户保护文件PLAN/FaknetNG.md blob `2c0d09e03162dfeb3a05517f9d3755af0d5e52ce`未变。无规范、代码、闭环JSON、旧记录修改，无VM操作，无构建、部署、清场、提交。

## 2. 直接重跑与证据验证

当前Linux MCP全量：**401 passed、4 skipped、1 warning，6.19秒**。新增本地主会话驱动 `Logs/fakenetng-mcp/review-9f2f31f/probes.py` / `probes.json`：直接执行当前模块或AST抽出的当前源码段；所有注入均为本地可控替身/缩放预算，未冒充Win32现场证明。

已重新计算候选ZIP SHA `6a009071c5e434c0e4f5725a73610b1e566387ff0fe7de6bda096ddf42a32a33`，与原生报告source、candidate_id一致。报告SHA `622527d162627dda81b8fcb5d53c4ac64a4f8556156d69018ea3dfc9bb11de5c`；退出ZIP SHA `c91fd0c1f83af127c30246959ea9f2bb65ae0dd4a7dc8c33ae74b6e603b3d3eb`，41成员名称集合、尺寸和SHA逐一验证。原生原始报告明确formal_acc_pass=false，end_helpers NOT reached，认可作者这次限定口径。41份包括历史run，不是41个新场景。

构建原日志SHA `6709f4bc7e3686bcd859523c7142e37c082841bcd34af664b11e751fb1cee126`；Windows969/8、HTTP17是本次读取的构建侧记录，本会话没有重跑Windows构建。已有原生安装/自检/正常启停/两阶段停服正路径不能替代08号§6全部矩阵。

## 3. 本批尚未闭合的完整清单

| CHK、需求/总纲、负责Pxx | 当前证据与反例 | 影响及最小闭合条件 |
| --- | --- | --- |
| 054；REQ005、记录004/006、OUT006；P03 | supervisor.py:267—284虽然移动offset，但先把本次读取裁为末65536字节，再调用evaluate_health_evidence。实际源码段输入“Traceback+70000普通字节”，消费70035、只观察65536，healthy=true；原异常被永久跳过。新测试把异常放尾部且自行重写算法，没有检验异常位于被裁前部。 | 原漏报仍在。必须对全部新消费内容识别异常并保留故障事实；测试覆盖头/中/尾、跨块、超过4MiB、截断/旋转以及实际健康循环，不能只断言offset增加。 |
| 054；记录006/034、OUT006/014；P03→P04 | 在途60秒失败后新增第二次等待stop_grace+420，第二次仍不空闲则记录后退出；未有“该操作后来结束时”继续收敛的责任路径，也未证明所有配置/审计I/O在该窗口内必终止。 | 原问题延后而非全闭合。保留单一收敛责任，迟到动作结束后仍处理；不能并行raw stop，也不能以日志明确失败替代实际Job/恢复结束。 |
| 057；REQ006/011、OUT007/ACC019、总纲§17；P04/P01 | 已修慢publish_ready反例，但_write('succeeded')仍在最后检查之后、同锁内。注入成功文件落盘前150ms延迟、总预算30ms，约153ms后仍ready=true和succeeded。watchdog置expired后等锁，随后看complete直接返回。 | 慢fsync/replace仍可超时成功。最终结果写入、SCM接受STOP发布、超时撤回/禁止迟到成功必须整体闭合；不能只在另一个回调后加检查。 |
| 060；REQ012/CON007、记录035、OUT014；P04 | incident新检查只在_write_item之后，_record中的读/hash、_write_manifest以及supervisor预读仍无硬结束。通用dump虽补MDMP/PID，仍直接写最终userdump.dmp，写中按单文件512MiB轮询；没有活动incident剩余额度与原子发布。正常完成后的verify_dump/hash也没完整纳入剩余截止。 | 迟到item不再ok是有效修复，但不等于整个采集有界。普通incident/通用dump/SPE三个分支同时纳入硬截止、累计活动配额、原子发布和失败清理；不能只改SPE。 |
| 060；记录035、总纲§18.5；P04 | active_bytes跳过已发布文件的字节数，但count在跳过前递增，仍扫描全部历史；已归档文件超过10000即拒绝新采集，即使活动临时字节为零。 | 历史数量仍耗尽活动取证入口。活动索引/预算按已批准活动范围落实，保留历史而不让其数量变成未声明永久门禁；验证大量已完成历史与当前零活动。 |
| 062；REQ015/016、OUT017/018；P03 runner/P05 | run_p03_acc.py清理字符串中包含字面量 `$state=' + state_file[state_file.index('(') + 1:].rstrip(' )') + ';`，AST确认这不是Python插值，发送给PowerShell的就是该文本。 | 新清理分支无法按预期定位state。必须生成并检查实际发出的脚本，原生执行所有改造分支，而非只测Python能import。 |
| 062；记录017、OUT018；P03/P05 | 清理仍枚举baselines全部*.json后Remove-Item，前置压缩保留在guest TEMP只记路径/SHA，未导回Logs且无每项所有权过滤。GUI分支原有Get-Process FakeNet-NG按名kill仍在。SCM helper的映像判断是PathName通配包含exe名，不是初始固定包绝对路径；listener以执行PowerShell的$PID当自有子进程，未证明其不是共享执行宿主。 | “不再按名杀、不再无所有权删全部baseline”陈述过宽。先固定测试owned PID/创建时间/实际映像与独立listener所有权，导出到Logs并验证后仅清本次对象；所有GUI/ACC008入口一并覆盖，不能以压缩副本授权删除无关对象。 |
| 066；REQ011/015、OUT012/013/017；P05 | 新validate_rounds只有测试调用；AST扫描acceptance所有脚本，生产调用数为0；run_p05_release未改。函数自身以(run_id,class)去重，同run换类别还可重复使用。 | 跨轮校验没有进入发布门禁。实际mode_summary必须收集全部样本调用统一验证、拒绝跨类别重复run、检查必需类别/次数/连续性/失败后重计；用真实summary入口做复制样本反例。原始diff重算的局部修复认可。 |
| 068；CON007、记录035、OUT014；P04 | 新Observation前后检查仍无法给在途调用证明时长上限。另一个新资源错误：CreateToolhelp32Snapshot成功返回后先observation.check，再进入try/finally；注入慢创建返回有效句柄123后截止，抛错但CloseHandle调用列表为空。 | 除未解决硬结束，又新增确定句柄泄漏。成功取得句柄立即进入拥有资源的try/finally，再做会抛异常的检查；真实硬结束保持既有条件。该泄漏归因本批diff，不可称平台限制。 |
| 069；记录034/035、OUT014、总纲§18.3；P03/P04 | 取消提前close有效。但_watch错误路径end_helpers再次因耗尽失败后，仅设result和done，_target继续保留；没有证明后续回收者会接管该责任。 | 修“不能提前关”不能止于“永远不关”。最小闭合是所有成功/失败/迟到路径都有唯一owner，先实际助手结束再关闭目标，最终归零；没有通知/窗口耗尽也验证。静态责任链缺口，未称已实测VM泄漏。 |
| 070；REQ004/013、记录028、OUT005/015；P02/P04 | is_published仍仅检查不是.part/.partial。直接持有ongoing.log写入句柄，第一次metadata complete=true，继续写后再次complete=true且SHA改变。register_fakenet_outputs也直接copy2到最终名字，未原子发布。 | 修.partial特例不等于产品发布事实。需生产者完成/原子发布证据，覆盖run.log、pcap、collector日志及复制中间态；未完成不给最终SHA。 |

以上6组直接结果（日志、活动文件、慢成功文件、快照泄漏、脚本文本、生产调用扫描）均见probes.json，其余是当前差异及调用者静态证明，证据类型已区分。它们均沿已有CHK继续评价，不把更早被否决方向或新产品建议变成缺口。

## 4. 已有实质改善及没有新增要求的项

- CHK055：专用受管映像已加入捕获，路径/命令行保留；本地回归认可，实际全基线矩阵尚未完成。
- CHK056：旧run_id不再覆盖真实实例、failed原因合并、内置同名create/rename拒绝、审计McpError清暂存有真实代码和回归；继续核对实际重启/幂等及Windows锁，不重复说这些分支未修改。
- CHK058：failed/run为空不再直接短路，协调器钩子与SCM内部submit路径已联接；仍须真实恢复责任/所有停止入口测试。
- CHK060：单字节非MDMP已拒，打开的真实目标句柄创建时间复核已增加；保留上表未覆盖的完整预算/发布问题。
- CHK064：非法布尔/浮点/字符串/映射拒绝有落点；四额外端口及双栈PCAP仍需原生执行。
- CHK069：残余断言失败不再提前释放目标；上表要求后续责任结束，不撤回这项改善。
- CHK071：无MCP参数时早返回，当前GUI范围修复合理；CHK072：现代params数组得到结构化拒绝，原反例已有针对修复。
- CHK059/061/067沿用上轮“已有局部改善、等待独立关闭”判断；CHK053/063/065及所有原生矩阵、发布次数仍未齐。本次没有闭合任何CHK。

## 5. 三项所谓裁决的核对

1. **068硬结束**：现行总纲已有60/180秒及真实结束条件。“不超过一次在途调用”是次数限定，不是已证明的时间上限。若作者确证既有架构不能满足，应带替代机制、上界/损失、稳定依赖边回流，暂停受影响实施。不能把原定硬结束改称核验者新增的“绝对抢占要求”；也不能让用户先放宽条件来绕过本批快照泄漏等纯实现错误。依赖仍为记录034/035→OUT014/ACC014、P01→P03→P04→P05 ACC004/014/017。
2. **071 GUI/CLI**：用户这次没有提出扩大范围；按当前总纲保持独立GUI/CLI原行为即可继续，不需再次问用户。只有另行提出GUI也改变的目标才是PLAN-CHANGE候选。
3. **056配置/重启**：拒绝与内置同名、区分原请求绑定和当前实例事实是既有契约内整改，不需用户重新选公开寻址。新增bound_run_id应与现有响应schema/客户端验证一致；若真的打算更改公开操作语义才回流，当前不能把假设性变更当作继续工作的阻断。

本记录仅核对边界，没有批准新的PLAN-CHANGE或修改规范。DNS服务归属应优先用获授权的宿主/VM只读现场诊断查明，本次未运行该诊断，不要求用户凭记忆猜服务，不擅改DNS。

## 6. 受影响需求保真复算

沿用08号的完整40项及正式52项双向映射，本轮定向重算24个受产品/runner整改影响的有效决定：记录002, 记录003, 记录004, 记录005, 记录006, 记录008, 记录012, 记录014, 记录015, 记录016, 记录017, 记录018, 记录023, 记录024, 记录025, 记录026, 记录028, 记录031, 记录033, 记录034, 记录035, 记录036, 记录038, 记录041。计数 **应承接24 / 完整14 / 未落点0 / 语义弱化10 / 不可验收0 / 失效或复核条件缺失0**。

异常ID及原因：记录004、006（CHK054异常/收敛），015（057/060/068/069结束），016（066真实计数），017（062保全/所有权），024（057受控停服结束），028（070完成事实），034/035（060/068/069取证到清理），036（既有外部保证仍被这些结束缺口弱化）。其余14项原反例已见针对落点，不等于对应原生ACC通过。

参与者、输入输出、顺序、生命周期、异常/结束、不变量、原采纳理由/适用边界/取舍及失效/重新裁决仍以需求v3、总纲v7、现行五Pxx恢复。没有未落点不表示实际正确；上述反例是语义弱化。当前规范可定义观察判据，未执行原生矩阵不混算“不可定义验收”。后四项有非零，结论不能通过。此为受影响24项计数，不冒充新的全量40项计数。

## 7. 结论与交回

**核查不通过**。作者状态只能维持READY_TO_REVERIFY的局部落地含义，不能声称“确定反例全部修复”。优先一次修齐§3全部产品与runner缺口，补真实入口回归，随后按08号§6执行同一最终候选全部矩阵；原生机制未通过前不开始凑100+50计数。现有测试证明修改没有触发现有断言，并没有证明完整闭合条件满足。

本轮结束于记录与证据。历史CHK-053—072全部OPEN；不新增/关闭AUD；独立有效轮次仍2/5；无整改、无VM操作、无规范变更。
