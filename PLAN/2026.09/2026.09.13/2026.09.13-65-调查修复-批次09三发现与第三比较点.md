# 调查修复-批次09三发现与执行器第三比较点

会话角色: 作者/执行方

创建时间：2026-09-14（discovery100-09 返回后）

## 批次09结果概览

驱动在 T+60 分钟被运行环境的后台命令上限杀死（非驱动崩溃）：完成 20 条（product_failure 19 + evidence_error 1，0 pass），sst-042 中断，finally 未执行。现场已由主会话清理：IPC 证据环境恢复（environment_restored=true）、残留 sentinel 容器删除、VM 终态 clean stopped。

## 发现1（产品，已修复并部署）：诊断 IPC 帧上限击穿

- 12 条场景（sst-026..033 B2、sst-038..041 B3）集中败于 `list_artifacts rejected: DiagnosticError('diagnostic IPC failed: diagnostic result absent/oversize')`。
- 实测 VM：artifacts 累积 125 runs / 5170 文件 / 2.1GB，全量元数据（含逐文件 sha256）超过 `diagnostic_process.MAX_FRAME`（1MB）→ 管道 readline 拒帧。`list_artifacts` 当时当场复现失败。
- 修复（源提交 23e2e46）：MAX_FRAME 1MB→8MB（注释说明验收矩阵规模语义）。**实机验证：list_artifacts ok=true 列出 4930 条。**

## 发现2（产品，已修复并部署）：TEST-NET 不能作为进程重定向原始地址 + 检查器伴随模块缺失

- sst-042（B4 首条）渲染配置被 `PolicyConfigError: ExternalProcessRedirectOriginalIPv4 must be global unicast` 拒绝——Python `is_global` 对 RFC5737 文档段（198.51.100.0/24 等）返回 False，产品校验过度严格。
- 修复（23e2e46）：`_global_ipv4` 显式接受三个 RFC5737 段（自 _NON_GLOBAL_NETWORKS 排除集移出）。
- 修复后 B4 仍死于下一层包装缺口：`Test-ProcessRedirectRoutes.ps1` dot-source `test\process_redirect_vm\RouteTargetTools.ps1`/`RouteResultTools.ps1`——同样未打包。修复（源提交 bfff497）：spec datas 按相对路径补齐两个伴随模块（199 成员），构建源校验同步。
- **实机验证（mcp-cbfff4973）：sst-042 同款渲染配置 healthy→stopped 完整生命周期。**

## 发现3（执行器，已修复）：五段归因第三比较点漏接线 + 全差异误放行

- 批次09 sst-019 实证：restart 站点归因成功（vanished TCP 17858，residue 空）却仍抛错——`_restart_baseline`（记录57修复自身）内部还有**第三处**同文案严格比对未接归因。
- 顺带发现记录61引入的漏洞：三处站点只以 listen 归因结果做门槛，**非 listen 差异（DNS/路由等）也被放行**。
- 修复：新增 `_difference_is_residue` 统一门槛——非 listen_ports 差异恒为残留；仅 listen_ports 差异走属主归因；归因缺失/失败阻断。四处站点（restart站点、run后站点、finally、_restart_baseline）统一接入。离线 204 passed。

## 执行方式修正

批次09驱动被环境1小时后台命令上限杀死。批次10起驱动以 `setsid nohup` 脱离任务管理器运行（日志落盘），子智能体以有界阻塞循环等待报告文件出现，避免进程被杀与 finally 丢失。

## 后续

批次 discovery100-10（79条同范围，候选 mcp-cbfff4973-d7de49943fe3）。
