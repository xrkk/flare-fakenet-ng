# Spike07辅助连接建立边界修复

会话角色: 作者/执行方

方案v0.9资格与产品候选不变，属于IMP007/ACC005007合同内实现修正，不修改合同。Spike07首项policy_pause仅traffic失败，其余故障、恢复和清理判据通过；剩余四类未执行。原失败目录及spike07-failure-manifest.json保持不变。

根因：UR005将非curl连接的反向peer关闭阶段REINJECT_LOCAL也加入建立上界。case1连接约82.0587ms终止；84.808ms的无身份RST给出69.183001ms保守结束下界，但晚到peer日志将建立上界误推到96.577098ms。证据：loop/spike07-case1-timing.json；run.log原方向[5007,5195)、早peer[5291,5493)、晚peer[6590,6792)。只读找错子智能体确认该根因。

修正：非curl建立上界只使用探针PID和原元组的策略行，仍取全部匹配行最大值；peer完整身份/代次/终止证据保留。curl继续采用已审核窗口的主与peer策略。新增late-peer回归先失败后通过，late-main仍须拒绝，未延长探针或放宽无身份RST。

四文件回归185 passed, 6 subtests passed；spike07-traffic-replay-01.json原件重放所有辅助及curl通过，仅机制验证，不追认旧失败批次。新11文件指纹spike08-implementation-baseline.json，新argv-08.json；主会话运行独立Spike08。实际100仍0，待五类通过后由Terra/xhigh执行。
