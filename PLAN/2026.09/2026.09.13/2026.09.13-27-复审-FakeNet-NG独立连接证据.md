# FakeNet-NG 独立连接证据定向复审

会话角色: 第三方审核方

## 1. 身份、模式和结论

创建日期2026-09-13（Asia/Shanghai）。Codex，gpt-6-astra / medium，隔离任务包。CON-FIX-007同一审核批次第2轮、唯一一次定向复审。结论：**审计通过**。本结论仅覆盖本轮冻结切片，并显式继承25未受影响结论，不表示实施或真实验收通过。

对象为本地worktree同目录06统一方案v0.6，blob `19bbc7b6609830417183b733d8904fad2042ae7b`，当前状态`审核未通过, 不得实施`。当前HEAD `7ec8eff106a722122b93b1a1d0e50a7b8d1599c6`，tree `3914d93f77a718d2c2c55edaf8dc8e86eebbb817`，分支`process-redirect-v23`。方案页首3cdec为历史实施基线，不与当前审计身份混用。

基准25全面复审blob `1e8faad346e574358abc3518356ac38a6dda973f`，请求26处理记录blob `c4b7d396b92e92c24b96a9b90d39fe92b4ad9af2`，均全文读取。实际对照v0.5原件`Logs/fakenetng-mcp/unified-repair-20260913/7ec8eff-loop/plan-v0.5-before-audit-import.md`（blob `cd2339d308fc63ffc77badad4090cfff748a7f89`）重算unified diff，与同根`plan-audit-25.diff`逐字一致（blob `7aafba33a63b24520a18ce5a5017b6ab6f2ffc9b`）。25附录除已声明方案变化外98项路径指纹全部一致，含两个拟新增路径仍未创建；六份决策关键原件大小及SHA-256均匹配。本文引用25附录作为未变路径指纹清单，不复制或改写历史对象。

## 2. 本轮实际定向范围

开始语义核查前已输出以下范围，并按实际diff确认可可靠限定：

- 未解决AUD：AUD-002及AUD-003首次处理后的闭合检查；不复开AUD-001。
- 纳入方案IMP-007补充2a/2b/5，ACC-005/007新增正反例和版本状态；覆盖25 C07/C21/C22/C24及受影响C18/C20/C23。
- 稳定边：IMP-001→managed IPC→adapter/validator→ACC-005/007；TCPIP原文→纯解析→完整集合/最早边界。关键代码为managed.py、scenario_fault_evidence.py、sst_fault_evidence.py；scenario_tcpip.py与test_sst_tcpip_events.py是明确拟新增对象。
- 证据：26及上述实际diff、25全文与指纹、22决定及其proposal、23/24身份链、总纲§16.1、02完整UTF16 pktmon及probe/run.log、03原始IPC。所有证据本地读取，不连接VM。
- 待答：kind绑定是否消除seq常量根因并保留合法旧start；有限迁移表是否覆盖已绑定主/peer真实语法；peer FinWait1最早边界能否被删；完整集合/时钟/反例是否闭合。
- 全局不变量：对象/来源/双向追溯/无后序依赖/ACC闭合、未审核状态、无fallback、授权边界；独立ETW不冒充包、完整原件不删早终止、TCB复用拒绝、ready不是healthy；真实五类及100尚未通过。
- 排除及继承：25 C01—C06、C08—C17、C19原结论及上述部分覆盖项中的未受影响内容显式继承；不作第二次全面审计，不改旧情景其他AUD/CHK裁定，不赋予历史失败通过状态；产品实现、旧目录、VM、构建、提交均排除。
- 通过条件：两AUD根因合同闭合，纳入范围无新增中高风险问题，全局不变量与继承边界明确。同AUD同根因复开则熔断，本轮不先发现首项即停止。

## 3. 逐项覆盖和证据

| 覆盖 | 独立核查结果 |
| --- | --- |
| C07/C21，AUD-002 | managed.py记录请求kind并递增seq；adapter第138行及validator第256—261行现存seq1逻辑确为待修根因。03原始IPC前四帧同run `1d8e832d-6b28-442f-ac3e-9c4a7f0ac6e4`，seq1为ready、seq2才是start。v0.6要求完整本run唯一start请求、同seq唯一响应、先请求后响应、错run/缺失/重复/倒序/歧义拒绝，禁止常量1或2；start_response_ref与success_refs同源，响应时间保守计算。旧首请求start按kind关联，ready身份另账，不能代替start/健康。合同关闭原根因，代码仍待实施。 |
| C22，AUD-003 | 直接扫描02完整UTF16文本，逐项核对选中主TCB `0xFFFFD48FE08E8A20`和peer `0xFFFFD48FE0D68740`全部迁移，均落入2a有限表。四类建立上下文不代替含PID建立事件，其余全部终止候选。主TCB历史复用仍拒绝，不能裁剪出通过记录。全文件其他TCB有额外迁移对，属于有限支持外输入；若未来绑定到此类事件仍按未知拒绝，并未擅自泛化支持。 |
| C22最早peer | 02字节区间[3470590,3470954)为peer在09:49:55.515437100的Established→FinWait1；主CloseWait为[3489110,3489476)、09:49:55.516030200；peer FinWait2为[3489928,3490286)、09:49:55.516175000，disconnect随后09:49:55.516175900。2a与5共同要求最早FinWait1下界进入min，不能挑较晚disconnect或09:50:56的RST。偏移以原文件开头计、含BOM、区间末端排他，原行换行字节保留。 |
| C21身份依赖 | 02主connect completed为[1931882,1932272)、PID7948；peer accept为[1933976,1934378)、PID404、38927←16633。probe ready/established PID7948与原元组、run.log同run PROCESS_FLOW/REINJECT_LOCAL两条相符。只用这些证明语法/关联定位，不将缺clock的旧02判为合格。03 ready内身份PID3976/FILETIME134335655009289027不能误用父方日志pid5508，2b要求与本run managed身份一致。 |
| C24反例及ACC | 新合同明确真实ready→start四帧回放、合法首start、缺请求/错run/重复响应/倒序/ready冒充start拒绝；迁移从02按精确原始字节提取，唯一连接含已知迁移合成正例、未知状态/未列迁移、删早FinWait1/CloseWait/挑晚RST负例。与既有PID/创建时间/元组/复用/重复建立/删引用/丢事件/UTF16/时钟/哈希等负例合并，给出四个pytest文件的实际命令。合成仅算法验收，新五类真实原件重判与100义务不被替代。 |
| C18追溯与顺序 | CON-FIX-007→IMP-007补充→ACC-005/007及IMP-001 IPC直接边明确；版本记录和26解释改变范围，其他IMP依赖不变，无后序循环。 |
| C20类型兼容 | v1仍仅packet合同，v2明确packet/tcpip_etw互斥；2b也适用于v1，兼容不能接受ready冒充start；ETW不是应用请求成功或真实包。 |
| C23时钟/完整集合 | 2a扩充全集，5独立重扫逐字节相等，6保持原ETL/文本大小SHA、转换退出、0丢失、offset480、前后UTC/monotonic、FILETIME头覆盖和精度限制；删除早行引用或只选晚RST均失败。缺元数据旧02仍负例；不能后填追认。 |
| 全局不变量 | 22/proposal规范未变，禁止模糊/相邻猜测、旧packet/正常NIC判据和恢复义务保持；本次没有实施/VM/网络/提交，未修改受保护dirty；AUD-001资源责任链无本轮变化。 |

## 4. 意见处理与发现分类

AUD-002：本次范围内关闭，未以原seq常量/请求类型关联根因复开。AUD-003：本次范围内关闭，未以遗漏已绑定连接的已观测状态迁移根因复开。无新增中高风险实质意见，无本轮新增审核质量遗漏；25已记录的前轮直接依赖覆盖遗漏仍保留其历史裁定，不删除。

MEC-003（新增、机械字段措辞、非阻断）：补充2b写`direction=request`，而实际JSONL与managed.record_ipc使用`event=request`、`side=parent`。现有证据格式和26要求唯一确定其含义为请求方向，不存在第二种IPC格式选择；按原始`event`字段落实即可，不应新增不存在的direction字段或改写原件。该措辞不改变kind/run/seq绑定方向，不构成AUD-002同根因复开，按工作流机械问题不单独阻断或触发复审。

## 5. 结论和边界

**审计通过**。本轮冻结的AUD-002/003及受影响切片审查完毕，未触发审核循环熔断。未受影响内容明确继承25；没有判断作者引入后的再次审核必要性或模式。本结论不代替作者引入，不改写当前方案状态，不证明任何尚未实施的新测试、真实五类或实际100已经通过。

本轮只做本机读取、字节提取、hash/diff核对和静态反例推演；未执行产品或Windows测试，未用作者既有68项测试结果替代本轮合同判断。首次尝试本机python不可用，改用python3完成只读提取，无证据阻断。本记录按当日全部Markdown最大流水号加一、排他创建；新建后不再修改，自身blob外置交接。
