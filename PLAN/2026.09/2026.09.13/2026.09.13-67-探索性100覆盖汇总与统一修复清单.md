# 探索性100覆盖汇总与统一修复清单

会话角色: 作者/执行方

创建时间：2026-09-14（批次19完成，15条故障行全部执行后）

## 覆盖账（discovery100-01..19，含小批模式批次14-19）

- **distinct 尝试 88/100**：故障 15/15 全执行；良性 73/85；0 场景完整 pass。
- **从未尝试 12**：sst-089..100（良性 B1 后段，属21条B1暂缓集合的一部分）。
- 21条良性B1（005-013、089-100）与27条已定局P1族（018-024、026-033、057-060、063-065、067-070、073）待产品修复后复验。
- 执行方式演进：批次01-13整批派发→批次14起按用户指示5条/批；批次09起驱动 setsid 脱离运行；批次16起故障场景前置清扫未消费注入文件。

## 结果分类汇总（按最新判定）

1. **P1族（产品TLS/中继行为，27条已定局）**：traffic_oracle 干净判负（B2 sink-and-reviewed / B4 process-redirect），解析全链成功——产品中继/接管行为与判据不符，详见记录60（P1启动竞态既有流中途劫持、P2本地环回握手损坏）。
2. **fault_oracle 证据链缺口（影响全部15条故障行 + 若干良性）**：注入效应与恢复流程本身干净（多个场景gate观测成功、恢复审计通过），败于 adapter 构案：`unique native relay accept/reverse PROCESS_FLOW missing`（构案能力）、`unsupported TCP lifecycle: spurious RTO detection / retransmitting connect attempt`（解析句型缺口，同BH族）、`probe established event missing`、gate 观测时序（listener_stop 在 start 早期致败）。
3. **quiescence 自扰（产品发现，批次18 sst-036）**：门控启动期探针 SYN_SENT 行命中 `_validate_process_redirect_quiescence` → diverter init 失败 → 注入未消费。产品/合同层需决断。
4. **未消费注入清理（产品发现，sst-015/035/036/054）**：失败路径遗留 fault-injection/gate 文件，会毒化后续 start——执行器已前置清扫留痕，产品失败路径应自清理。
5. **系统socket成组消失（产品发现，批次13 sst-080）**：运行期间 NetBIOS/LLMNR/SSDP 监听成组消失+DHCPv6出现——网络栈扰动，待干净环境专项复现。
6. **执行器已修复（本阶段）**：ipc-parent环境预启（59）、sentinel容器回退（60）、五段属主归因+统一门槛（61/64/65）、IPC拆卸兜底（62）、ALLOW_REVIEWED_IP/trace停止容错（63）、BH句型/属主cmdlet（64）、传输上限/PCAP完整性（65-66）、pcap等待时机+直接回退（66+）、VM剪除门槛（66）、注入清扫前置（19批次）。
7. **churn伪影（已证伪）**：批次12的45行 capture/probe startup failed 在快照恢复后0复现。

## 统一修复清单（下一步）

**产品侧（按阻塞面排序）**：
1. P1/P2 TLS中继（解锁27条P1族+21条B1）
2. fault失败路径自清理未消费注入文件
3. quiescence与门控启动时序互斥的决断
4. 系统socket扰动专项调查
5. （背景）artifacts累积的保留策略

**证据侧**：
1. fault adapter 构案（relay PROCESS_FLOW、probe established）
2. TCP生命周期句型族（spurious RTO、retransmitting connect attempt）
3. policy flow missing 解析（default桶）

**流程**：批次14的5条default良性待pcap直接回退下复验；12条B1后段（089-100）待P1修复后执行。
