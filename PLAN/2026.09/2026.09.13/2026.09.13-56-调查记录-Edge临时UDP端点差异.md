# Edge临时UDP端点差异

会话角色: 作者/执行方

证据：Logs/fakenetng-mcp/unified-repair-20260913/7ec8eff-loop/port55246-investigation.json及port55246-current-owner.json，均为原件引用/只读查询，不改旧结果。

原始netstat明确UDP192.168.204.233:55246属PID1624。Kernel-Network原始ordinal24为同PID于14:50:15.2279467向239.255.255.250:1900发送175字节；TCPIP原件14:50:17.2122191的0658.2178（PID1624/TID8568）记录该端点closed。restart请求HTTP时间14:51:01，晚于关闭约44秒。当前同PID仍是14:38:11.907618创建的msedge.exe，创建时间早于本轮，无同PID重用支持；进程仍活，无55246端点，无服务关联。

原_section_difference重放只报listen_ports；仅在内存去掉此一条Edge端点后全部五段差异为空。反例重放不改正式结果。证据支持：这是外部应用自行关闭端点引发的全机快照差异，不能据此宣称FakeNet restart丢失恢复责任。没有采集Edge内部调用栈，不能断言其具体关闭动机；但关闭发生在重启前、且由Edge线程执行，不需要FakeNet复活该临时端点。

原sst005保持fail/不完整，不算通过；其余99尚未执行。环境清理已证实stopped无run/controller、无probe/fault/pktmon。解除该外部端点缺失对探索性找错的阻塞，通知Terra执行后续未执行情景；正式恢复比较器和产品代码不修改，不扩大为忽略任意UDP差异。
