# 诊断截止与资源结束补证及实施归因

会话角色: 作者/执行方

用户同意按16思路执行。统一方案06 v0.4/IMP-003及007、ACC-001/005/007保持；源码前基线25a0501af47e1c753c74ec776cfeb93a0afa5073，候选mcp-c25a0501a-3a4340750d43。仅处理FIND-LS-001/002，既有dirty和历史失败证据保护；下一正式验收失败仍停止。

## 原生最小补证

.233、Python3.13.7，使用已转移且与候选相同的diagnostic_process.py（SHA b839aed7b9e1c6d243c416d6d086e81f59313348f2923cf3be12ee0e2ad3c837）。只调用固定exit-read读取已失败run的entry.json，不创建FakeNet实例或网络接管。父方设frozen选择既有已安装diagnostic-task EXE；调用返回与原生进程创建/terminate/ended观察完整保存。

剩余预算0.001与0.1秒两例均在deadline抛DiagnosticError时pending=true/done=false/ended=false/cancel=true，同一attempt实际清理约20.45ms与10.27ms后ended=true；原生创建/terminate请求及观察均已登记。这证明调用截止不等于任务资源结束，立即_release_gate会把正常短时收尾误判成最终失败。1秒控制例约0.356秒返回IPC失败且ended=true；本探针父程序是Python而非安装主管，因此该例不证明完整诊断协议成功，不拿它冒充实际服务验收。也不能恢复历史run最后任务的精确身份。

证据根Logs/fakenetng-mcp/unified-repair-20260913/25a0501/listener-diagnostic-end-spike-01，probe.py/command.ps1/terminal.raw/result.json。没有VM重置、产品部署或压力测试。

## 修复与原合同边界

FIND-LS-001：evidence_integrity新增唯一incident_entry_omission_allowed，由P05/导出helper/独立ZIP核验共同调用。只保留原允许的userdump skipped（固定原因、size0、sha=null）及fault轮managed-exit failed/固定原因；不放宽其他条目，不改历史判定。实际manifest原函数回放见16；回归5通过。

FIND-LS-002：到达采集截止后固定一次已有FINALIZE_BUDGET=45最终化截止；取消并观察两个既有DiagnosticOwner的active任务真正ended，再进入原_finish/_release_gate。禁止消费迟到result或创建替代采集；同一个最终化预算同时约束诊断/intent/扫描调用，不因等待重新加45秒。未结束仍失败并保留同一owner；后续settle仍只清理责任，不复活失败结论。watch结束解除本轮最终化调用钳制以允许原合同的后续settle，不改变其原失败结果。

添加bounded before/after诊断观察，包含attempt/operation/deadline/native/done/cancelled/observed_monotonic/ended_monotonic，供正式run定位具体未结束任务。仅两个当前任务，没有泛化事件队列或新RPC。资源关闭安全门保留，既有60秒采集截止/5秒故障停止预算均不变。属于已有45秒最终收尾责任内实现纠正；没有改变来源、架构、恢复策略、权限或方案ACC通过边界，无需方案变更或再次第三方审核。

## 回归

先运行真实_watch/settle边界回归，第一项重现complete=false（与16控制流相同），永久pending负例通过；修复后组合test_managed_readiness、test_diagnostic_ownership、test_exit_retention、test_incident_manifest_contract、test_incident_export_integrity、test_release_evidence_integrity为51通过/1Windows平台跳过，XML在listener-stop-analysis/fix-local.xml。新增回归真实调用_watch/_finish，只有原生ended事件和发布边界替换；取消后的同任务结束方可通过，持续未结束不能释放。未将本地回归或短预算原生探针计为正式P05/100。

后续同一方案继续新源码快照、正式构建与同候选真实验收；首正式失败停。当前100仍0。
