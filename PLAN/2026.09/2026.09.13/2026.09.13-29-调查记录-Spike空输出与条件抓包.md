# Spike04工具失败与同源修复证据

会话角色: 作者/执行方

统一方案06 v0.6，已审核, 可以实施；27审计/28引入。产品候选保持mcp-c7ec8eff1-611c036301c8，代码产品部分无变化。用户持续修复授权有效，本次不更改规范、故障机制或异常白名单。

## 1. 实机失败与归因

证据根Logs/fakenetng-mcp/unified-repair-20260913/7ec8eff-loop/。Spike04前置P1–P7通过，首情景policy_pause因辅助cases/curl未在90秒完成失败；原件spike04-failure-manifest.json封存。probe只出现一次cases_released，四条case_close，一次curl_started但无curl_completed，随后InvokeMethodOnNull。尚未arm，不能将失败归因于policy_pause产品故障。最终服务stopped、run/controller为空，故障模式config_bytes_restored/environment_restored为真。旧结果不改判。

## 2. 空文件反例与修复

curl-diagnosis/inspect-empty-05.raw给出Windows PowerShell 5.1.19041.4046最小无网络反例：Get-Content空文件后is_null=true，([string]$err)仍null，.Trim抛InvokeMethodOnNull；字符串插值"$err"不是null，.Trim成功。先前仅强制转换的修复没有修复此执行面，不再沿用该错误结论。实际探针已改两个输出字段为字符串插值。

完整函数对照inspect-07.raw只将请求目标改成本诊断自建文件、使用同一WinPS5，并保留Process.Handle：单次开始/完成，原生exit_code=0、stderr为空，done落盘。inspect-06对应旧未保留句柄路径完成但exit_code=null，不作为通过。修复在退出前持有Start-Process返回对象原生句柄、从同对象读取创建时间；不以0常量或命令文字冒充退出码。该本地文件对照只证明工具执行链，不代替TLS/策略或五类Spike。

早期完整诊断03/04未及时结束、原生查询观察到较高CPU/内存；文件已通过FileSystem工具取回，针对启动回执PID4192/1120清理请求返回已不存在。随后原生Get-CimInstance复核4192/1120/1664均不存在，VM身份DESKTOP-3FI41GR；不能仅凭kill回执认定已结束。未停止VM/MCP/产品服务或其他进程。

## 3. PCAP纠正

此前只按runner无条件等待的实现建议强制DumpPackets Yes，是作者和找错方的来源核对遗漏。旧情景§4.1明确“DumpPackets=Yes配置额外自然产生dual-PCAP，不为取证改写产品配置”。Spike04渲染改变因此不具备验收资格。已撤销强制Yes，采集/总裁决/结果重判统一根据原模板的DumpPackets要求额外产品PCAP；B1/B2/B4保持No，default/B3保持Yes。正常独立pktmon层和NIC判据不变。default禁用重定向的四个占位字段在验收渲染副本省略，保留Enabled No，避免无效占位扫描阻止默认配置。

## 4. ETW实现及找错闭环

已实现完整IPC按kind配对、ETW有限语法/状态迁移、完整引用重扫、最早主/peer结束、原ETL/文本转换哈希与时钟。Spike04真实捕获元数据通过validate_capture，但整个情景仍失败。修复子智能体纯内存反例：精确六字段PROCESS_FLOW引用，异TCB同原元组RST不得忽略，生命周期冲突PID拒绝。原件03四帧回放选择seq2真实start，managed身份3976/134335655009289027。

故障主连接traffic gate接入独立v2原件重判，要求同run/nonce/PID/元组/connection_id且完整裁决通过；不填packet_record_count、不替换aux/curl连接或物理NIC包。在线和离线共用该绑定逻辑。

离线回归100 passed / 6 subtests passed（不是实际100情景）。新目录Spike05先P1–P7再五类；新工具指纹spike05-tool-fingerprints.json。实际100仍0，由后续新Terra/xhigh子智能体负责；任何失败本轮停派发后封存、继续根因修复。
