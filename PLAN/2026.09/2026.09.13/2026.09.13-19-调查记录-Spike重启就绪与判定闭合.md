# Spike重启就绪与判定闭合

会话角色: 作者/执行方

用户明确替换首失败停止边界为持续修复验证；Spike主会话执行修复，找错子智能体只读调查，实际100仍由Terra/xhigh。每次失败仍终止当前批次派发，保留原件后修复并使用新目录，不把失败改判。

来源统一方案IMP-007/ACC-005/007；原证据7ec8eff-restored/rerun-stop及five-class-spike-rerun。SCM Running先于uvicorn端点约3秒，执行器立即返回导致接口拒绝、随后的受控stop恰在恢复窗口失败。修复_fault_mode：60秒限内只读get_status，单调用受剩余预算约束，只接纳stopped且run/controller为空，允许临时传输错误/recovering，其他状态拒绝。超时不放行。新增3例先红后绿。真实恢复观测连接拒绝→recovering→stopped，证明原假设；配置字节及Environment精确恢复、grace60。

恢复属性判定原用@($null).Count==0，实际Count1，错误认定属性删除失败。改为显式属性存在性，存在时仍逐值比较。原始实机恢复已验证environment_restored=true/config_bytes_restored=true，证据7ec8eff-loop/fault-restored.json。

子智能体/root/runner_find_errors只读发现：Evidence记录相对attempt目录而capture_views消费按suite目录，原5文件全部存在且哈希吻合；主会话在序列化时重建suite相对file_record，保留校验器越界/哈希拒绝。正常窗口continuous_health原not fault对所有fault恒False，却参与all；改为正常判据仅适用于正常，故障仍强制fault_terminal/recovery/oracle。不是移除故障验收要求。真实_run_one失败路径回归证明路径均可解析且fault_oracle仍False、总结果仍fail，不把缺失证据当通过。

本地三组测试65pass/6subtests；证据7ec8eff-loop/runner-fix-identity.json。仅宿主机验收器改动，产品包仍mcp-c7ec8eff1-611c036301c8，不伪造已提交源码覆盖工作树修改。此为原IMP-007实施纠错，不扩需求/范围；后续使用新five-class-spike-01原件。原失败全部保留。
