# ACT-002 生命周期修复调查

会话角色: 作者/执行方

版本 v0；调查进行中，根因链未全部闭合，不具备写入修复方案或送审资格。
创建时间：2026-09-13T01:02:56.947342+00:00。

## 工作流与边界

入口 `/mnt/d/vpb/话术/方案工作流/12-修复方案/00-完整流程.md`；Codex，审核 gpt-6-astra / medium，隔离 fork_turns=none，实施主会话。模型和推理强度由当前子智能体工具可用模型元数据确认可用，尚未派遣正式审核，不产生第三方结论。
用户确认文档目录 `/home/adminn/projects/flare-fakenet-ng/PLAN/2026.09/2026.09.13`。复用现有同业务 Goal，其工具状态当前为 paused；未创建第二个 Goal，未宣称恢复自动100场景循环。

来源为架构审计 v4 选定 ACT-002：限定启动、退出取证和恢复责任的状态/资源/等待关系复核。ACT-001/003/005 不纳入，ACT-004 已反证不纳入。历史 ACK 锁缺陷已在 HEAD 修复，不重复立项。具体最新失败仅作为生命周期反例调查，WinError299 不预设根因。

基线分支 process-redirect-v23，HEAD 3cdec36784ed67cfd42149147e88cbb35cba3e50。受保护状态：PLAN/FaknetNG.md 已修改，两日期目录 2026.09.12/2026.09.13 未跟踪。实现范围无 staged/unstaged 修改。本次仅调查证据和流程记录，未修改业务代码、部署、发起网络流量或重新启动验收。

核心：supervisor/managed/exit_retention 的生命周期。直接依赖：coordination/snapshot/baseline、ManagedJob、exit_native/exit_monitor/diagnostic owner、配置锁与 endpoint observer，沿实际调用再精确固定。源规范与依赖全量读取尚未完成，不作完整根因或覆盖声明。
外发清查：本调查阶段本地读取和本地模拟，不调用付费外部 API、不创建凭据。隔离审核使用用户指定内置子智能体工具；尚未执行。后续实际 VM 最小调查和正式验收分别记录；原100场景失败停止约束未由目录确认撤销。

## 已核原始反例

失败 run 47e4fb8f-0c41-4202-b751-0b6cdf101812。既有原件目录：Logs/fakenetng-mcp/p05-c3cdec367-188ff94c4ce2-smoke-20260913-ackflight-01/failure-readonly-after-normal-custom-001-attempt2/run-47e4fb8f-0c41-4202-b751-0b6cdf101812。
creation.jsonl 记录到 after_api（PID6524/FILETIME134337313762636632），无 before_start。首 incident 的 exception 为 managed start failed / WinError299；第二份 incident 和 run.log 为 managed.py355 拒绝当前状态的操作。尚不以时序相关性证明299的原生API原因。

## 最小复现与判断

命令：`PYTHONPATH=. .venv-mcp-runners/bin/python -B Logs/fakenetng-mcp/act002-investigation-20260913/replay_prestart_stop.py`。
实际调用当前 managed.child_main，模拟 Job/系统API、管道和未创建的核心，输入同run按序 stacks→stop；没有改写分派器逻辑。输出 stop response.error=managed operation not allowed in current state、exit_code=1；预期的冷态清理断言失败。已执行两次，同一信号；复现脚本/输出/重复终态在同 Logs 根。

已证实：当前 child_main 的 stop 只接受 instance!=None，supervisor.start 在 ManagedProcess 创建后、发送 start 前的错误路径会调用通用 stop；supervisor.stop 发送 stacks→stop。因此核心未启动但受管进程已创建的清理阶段存在合同缺口。这是次生错误的复现，不是原始 WinError299 的 Windows 复现。
待核：299发生于具体哪次原生访问、为何当时不可读；是否还有其他已确认的状态/资源/等待不变量违反。不得只修次生报错就宣称整个原始故障已闭合。

## 接续检查点

INVESTIGATE：继续完整读取规范切片与直接依赖，重建状态/资源/等待表；对299建立原生失败定位或等价证据，再收敛最小必要方向。未创建修复方案、作者自评或第三方审计；WRITE及后续阶段未开始。若根因不可替代证据无法取得，只阻断相应条目并记录解除条件，不编写猜测性修复方案。

## 对象指纹

- `PLAN/2026.09/2026.09.13/2026.09.13-01-主动式架构健康审计-FakeNet-NG.md`: `a026e0ae20ff83e57259f9530449df99afd8c16c`
- `AGENTS.md`: `8c6e68314e6aa1852ec295dad30453d58577bc7a`
- `fakenet/mcp/managed.py`: `007c4f5ecf4e88f296f6ce8f6767c593933cff1a`
- `fakenet/mcp/supervisor.py`: `ad682e6d339800e022afea91bce02ce5ece3c776`
- `fakenet/mcp/exit_retention.py`: `1e92d1931a1b7695ab7b2cb199861022b5ee36e7`
- `/mnt/d/vpb/话术/方案工作流/12-修复方案/00-完整流程.md`: `3e8230253741c31a0e81f0b03efcc3d1cea6f9e9`
