# FakeNet-NG 100场景找错与修复——详细任务交接

会话角色: 作者/执行方

创建时间：2026-09-13T19:15:22.531182+08:00
仓库：`/home/adminn/projects/flare-fakenet-ng`
交接性质：执行中断点交接，不是验收完成报告，也不是第三方审计。

## 1. 接手后先理解这五件事

1. 用户真正要的是：**尽快实际运行100个场景，发现并修复FakeNet问题，使软件可用**。前任主会话把过多精力花在严格取证解析器、合同和审核循环上，实际100长时间没有开始，这是已明确承认的执行偏差，不能再重复。
2. 当前是**探索性找错执行**，不是把正式验收门强行判通过。仍保留完整原件及原始fail；产品问题、测试执行器错误、外部环境变化和证据不足必须分开。不能以找错名义取消停止/恢复安全要求。
3. 100场景不同条目目前只实际尝试了 **2个：sst-005、sst-006**。sst-006另复验一次，共3次场景尝试；没有完整通过的探索场景，剩余 **98个不同条目未执行（83良性+15故障）**。生成100条manifest、既有Spike和原件重放均不计实际100。
4. sst-006的**restart第二轮恢复基线错误已修复，191项离线测试通过，实机7项基线验证通过**。不要重新调查为未修，也不要因整场景仍fail而抹掉该局部修复的实证。
5. 当前最新失败是实机复验导出第一轮原件时缺少 `ipc-parent.jsonl`。**缺失根因尚未查明**，不能仅因驱动报告分类为product_failure就断言产品缺陷。先调查原件生成/路径/重启归属/导出行为，再最小修复。

## 2. 用户授权、分工及交互约束

### 2.1 持续有效的任务授权

- 全部已证实问题纳入同一统一修复方案，可以按根因分阶段修复，不拆成许多独立产品方案。
- 用户已解除早期“下一轮失败就停止”的边界，要求“子智能体找错＋主会话修复”持续推进；Spike由主会话执行和修复。
- 实际100必须由 **gpt-5.6-terra / xhigh 子智能体**执行，主会话不得自己替代实际矩阵执行。
- 每次新建子智能体必须显式 `fork_turns: "none"`，提供自包含任务包、代码/证据路径、候选身份和限制。
- 主会话负责查根因、修改代码、验证、必要的本地提交/构建/部署。已审核范围内重试不反复询问授权；不授权push、分支切换、破坏旧证据或无依据扩张产品能力。
- 用户已明确纠偏：不要继续让正式五类取证通过成为**探索性100开始**的前提。记录55固化了这个调整。正式ACC门禁本身没被伪造或删除。
- 原 `curl-suspended-start-proposal.md` 已**搁置**。不要继续等待这份提案批准，也不要擅自实施挂起curl/100ms间隔。

### 2.2 等待与沟通——务必遵守

用户多次纠正了前任的等待行为：派发后等待子智能体返回，等待超时 **60分钟，3600000ms**。最后明确要求不再60秒分段等待。

本环境对应调用：

```json
{"timeout_ms":3600000}
```

调用 `collaboration.wait_agent`。前任先承诺60分钟、实际却连续调用60000ms，引发严重不满；后来确实改为3600000ms，已成功收到子智能体通知。**不要再说一套做一套**。若更高优先级运行环境确实不允许，应直接说明真实限制，不能谎称已按3600000执行。

- 不循环用状态查询工具刷进度，不在每次等待超时后发无实质内容的状态。
- 用户主动问进度时，报告实际完成/尝试计数、当前问题及处理动作。
- 子智能体中间通知不等于完整返回；若仅通知“已启动”，继续等待最终结果或确需主会话处理的失败。
- 中文回复通常保留 `会话角色: 作者/执行方`。本会话已经实施/自评/引入审核，不能伪装成隔离第三方审核会话。

## 3. 代码、候选包与工作区身份

|对象|身份|
|---|---|
|分支|`process-redirect-v23`|
|HEAD|`7ec8eff106a722122b93b1a1d0e50a7b8d1599c6`|
|tree|`3914d93f77a718d2c2c55edaf8dc8e86eebbb817`|
|产品候选ID|`mcp-c7ec8eff1-611c036301c8`|
|ZIP SHA256|`611c036301c86659bcc3b81c512eba06135314bb5c7a6d5e0508a72d5f5d9e85`|
|候选目录|`dist/mcp-unified-repair-20260913-03`|
|部署记录|`Logs/fakenetng-mcp/unified-repair-20260913/7ec8eff-restored/deployment.json`|
|正式构建证据|`Logs/fakenetng-mcp/builds/7ec8eff1-lk9ic5x7`|

上表为实际读取的完整SHA。交接时实际git读取确认HEAD/tree；以下工具改动仍在worktree，**不能把产品包源码身份当作当前host验收工具身份**。

最新验收工具11文件冻结清单：
`Logs/fakenetng-mcp/unified-repair-20260913/7ec8eff-loop/restart-baseline-implementation.json`。

交接时再生成内容指纹清单 `handoff-current-files.json`，以及工作区状态 `handoff-git-status.txt`。接手后先核对，不覆盖别人新增修改。

当前相关改动包括：

- `test/mcp/acceptance/scenario_suite.py`
- `test/mcp/acceptance/scenario_probes.ps1`
- `test/mcp/acceptance/scenario_fault_evidence.py`
- `test/mcp/acceptance/sst_fault_evidence.py`
- 新文件 `test/mcp/acceptance/scenario_tcpip.py`
- 新文件 `test/mcp/acceptance/scenario_kernel_network.py`
- `test/mcp/test_scenario_suite.py`
- `test/mcp/test_scenario_spike_gate.py`
- `test/mcp/test_sst_fault_evidence.py`
- 新文件 `test/mcp/test_sst_tcpip_events.py`
- 方案06、追加实施记录13、流程状态JSON及后续不可变记录。

`PLAN/FaknetNG.md` 有无关既有修改，不要顺手修复/暂存。旧2026.09.12目录和架构审计01等也不能因为untracked就删除。不要全仓库git add、git clean或reset。当前这一轮没有提交新的产品修复，也没有构建/部署新产品包。

## 4. VM、通道和环境注意事项

### 4.1 本任务实际使用的VM

- VM MCP：`http://192.168.204.233:28787/mcp`
- 产品MCP：`http://192.168.204.233:28788`
- 计算机名：`DESKTOP-3FI41GR`
- 本轮持久化网卡MAC：`00-0C-29-C1-CA-49`
- Guest Python：`C:\Python313\python.exe`
- 产品：`C:\Program Files\FakeNet-NG-MCP`
- 状态/日志根：`C:\ProgramData\FakeNet-NG-MCP`
- run原件目录：`C:\ProgramData\FakeNet-NG-MCP\artifacts\runs\<run-id>`
- VM MCP自身运行PS7，但probe子进程是 **WinPS5.1.19041.4046**；不能用PS7结果代替WinPS5行为。
- VM时钟日期为9月11日，host为9月13日。因果比较使用同一VM原始时间域，不能直接拿host时间减VM时间。

使用 `scenario_suite.VmMcp` / `Suite._vm_json` 的显式端点，不用默认connector的`.149`。VM网络调用使用工具实际需要的网络授权方式；前轮用 `exec_command sandbox_permissions=require_escalated` 并说明具体VM用途。

### 4.2 不要贸然恢复快照

较早执行记录使用过另一个明确VMX：
`/home/adminn/vmware/Win10(MalBox)-FakenetNG/Win10(MalBox)-FakenetNG.vmx`，快照`Snapshot 2-固定IP+开机自启WindowsMCP`。

但用户最新AGENTS reset说明又指定`/home/adminn/vmware-machines/win10h2-MalBox-20241110/...vmx`及Snapshot183、另一MAC。**存在对象身份冲突，不要把两组拼接，不要仅按同计算机名判断是同VM。** 当前不需要reset。若以后必须reset，先保存guest-only证据并按最新明确授权重新核对对象；本交接不是新的重置授权。

最新`discovery100-03`结果记录：service stopped、run_id/controller均null、cleanup_errors为空，两次捕获有stopped时间。子智能体报告finally干净停止。交接本身没再次查询实时VM，因此这是**最近已记录状态**；恢复执行前做一次只读状态检查，不将旧状态伪称实时状态。

## 5. 方案与审核/实施记录

主方案：
`PLAN/2026.09/2026.09.13/2026.09.13-06-修复方案-FakeNet-NG统一问题修复.md`

- 版本v0.10。
- 文件登记状态：`已审核, 可以实施`。
- 当前blob：`207aef0aa98305304e39d424535f13b886879f87`。
- v0.10的局部新增关闭语法由作者自评闭环，**不是新一次第三方通过**。其余已有资格沿历史链继承。
- 主工作流：`/mnt/d/vpb/话术/方案工作流/12-修复方案/00-完整流程.md`。
- 可变实施记录：同目录13。
- 可变流程状态：`ACT002生命周期修复-完整流程状态.json`。其旧checkpoint曾落后于真实执行，以本交接+各批次原件为准。

重要历史链（均在同目录，不改旧记录）：

|记录|作用|
|---|---|
|22 / 25—28|独立TCPIP证据决定、全面/定向审计与引入|
|30—35|TCB按生命周期划代及辅助TCP/curl/UDP独立观察|
|36—41|精确无身份0x0 RST仅收紧终止时间，全面审核40与引入41|
|43—47|两种原生上下文及curl窗口；46定向审计、47引入v0.9|
|48—49|实现、Spike07非curl建立边界修复|
|50—53|精确STATUS_SUCCESS终止句型、自评v0.10、WinPS5精确时钟|
|54|curl窗口跨界，提出挂起启动提案；后已搁置|
|55|用户纠偏：立即探索性100，不再等待取证合同完善|
|56|sst005的Edge临时UDP端口归因|
|57|sst006 restart第二轮恢复基线修复|

40全面与46定向已耗尽该批次审核次数。不要为了微小局部修复再机械开同批次第三次审核。完整流程允许首次审核后、未触发证据化高风险条件的作者自评闭环；这不允许隐藏真正的范围变化或冒充审计。

## 6. 三批探索性执行——准确计数和结论

统一根简称L：
`Logs/fakenetng-mcp/unified-repair-20260913/7ec8eff-loop/`

固定manifest：seed20260912，100条，85良性+15故障；SHA256：
`825de7db56eb0b7d290a46419a6be73f7ef5e00df15ab888824eba06c22bb2f4`。

|批次|实际执行|结论|未执行|
|---|---|---|---|
|L/discovery100-01|sst005，B1良性restart|Edge端口自然生命周期导致全机快照差异；原结果fail保持|99条|
|L/discovery100-02|sst006，B1良性restart|执行器run02恢复基线取错；现已修复|98条|
|L/discovery100-03|复验sst006，不增加distinct|基线修复实机通过；随后原件导出缺ipc-parent，整例fail|仍98条|

累计：**2个不同条目，3次尝试，0个完整pass；不是“已跑100”，也不是“98个通过”。**

每批独立P1–P7均通过。前置通过不能替代场景通过。

### 6.1 sst005：Edge端口差异

原结果：`discovery100-01/results/scenario-sst-005.json`。

- UDP `192.168.204.233:55246` 原netstat所属PID1624。
- Kernel-Network原件：14:50:15.2279467，同PID向`239.255.255.250:1900`发175B。
- TCPIP原件：14:50:17.2122191，前缀`0658.2178`对应PID1624/TID8568，记录端点closed。
- restart请求HTTP时间14:51:01，端点关闭比请求早约44秒。
- 只读VM查询同PID为msedge.exe，创建14:38:11，早于本轮且仍活；没有55246端点。
- 仅在内存移除此一行后，五段恢复差异为空。旧结果未改。

支持的结论：不能将此差异归为restart未恢复；不需要FakeNet复活外部程序的临时UDP端点。不知道Edge内部具体关闭动机，不要声称所有影响路径都已排除，更不能推广为“所有UDP差异忽略”。

证据：`port55246-current-owner.json`、`port55246-investigation.json`、记录56。

### 6.2 sst006原失败：执行器基线错误

原结果：`discovery100-02/results/scenario-sst-006.json`。

旧代码在run01仍healthy时采`second_before`，然后restart后给run02当恢复基线。该快照当然含FakeNet DNS、WinDivert/managed child和53/80/443/1337监听。run02正常stop后这些消失，被误判差异。

已修改 `scenario_suite.py`：

- `_restart_baseline(previous_run)`要求同run recovery_audit文件存在，并严格比较run01原基线和恢复快照。
- run02基线使用**run01停止后、restart新start前的产品恢复审计快照**。
- 原运行中快照改存`run-02-pre-restart-context.json`，明确不是恢复基线。
- 新`run-02-five-sections-before.json`保留`source_run_id`和`source_recovery_audit`。
- 真正DNS/恢复差异仍拒绝，缺audit也拒绝，没有放宽产品恢复条件。

验证：191 tests+6 subtests；`sst006-restart-baseline-replay.json`原件重放旧三项差异变为空；discovery100-03的reverify报告中7项restart_baseline checks全部true。

### 6.3 最新失败：ipc-parent原件缺失（未查根因）

结果：`discovery100-03/results/scenario-sst-006.json`。
报告：`discovery100-03/reverify98-report.json`。
错误：`SuiteError('complete VM run originals missing: ipc-parent.jsonl')`。

- run01：`54cbf722-cfb3-479a-a54d-051babea8255`
- run02：`d84aef77-6b64-45d2-8441-f228e106ee18`
- 子智能体定位为finalization导出run01时报缺失；run02 exporter尚未执行。
- `_export_run_originals`在检查required集合后才建本地originals目录并转移，所以本地没有originals文件**不等于guest所有原件都不存在**。
- 当前report分类`product_failure`只是探索驱动的兜底分类（无法证明纯证据错误），**不是根因认定**。
- cleanup_errors为空、最终stopped，两个capture均停止。修复基线的实机结论有效，但整场景仍fail。

## 7. 当前实现中还需理解的既有工具修复

这里只列接手定位可能依赖的内容，不要求重新审核或全量重做。

- `scenario_tcpip.py`：全原件TCB代次重建，完整Closed→新生边界；仅选中代参与会话区间，后代仍保留引用；未知语句拒绝。
- 无身份0x0/Receive discarded RST只作独立负向结束约束，不能建立连接身份或替代具名终止。
- 原生精确`shutdown initiated (STATUS_SUCCESS). PID = <正PID>.`现已识别为终止，未知STATUS/缺PID/尾部拒绝。
- curl按唯一公网原tuple与既有进程窗口绑定，保留窗外策略上下文；上游日志不能借主连接。仍可能出现真实不确定边界拒绝，见Spike09；**探索性执行中不要再为此独占主线反复造合同**。
- WinPS5 `DateTime.UtcNow`过粗导致短连接关闭时间早于ETW建立，已改事件时间为`GetSystemTimePreciseAsFileTime`，同次采样生成UTC文本/ticks；UDP发送边界和B3原生客户端事件同样改用精确UTC。原15.625ms不确定度不缩减。
- `_application_observation`非curl建立上界只用探针原方向匹配策略行，不把peer关闭阶段日志当建立证据。
- `scenario_kernel_network.py`用原始Kernel-Network XML/UTC证据绑定UDP PID/元组/长度。不要使用已发现时区不可靠的tracerpt事件时间替代原始ToXml。
- `_continuation_gate`本身检查较粗，不能仅凭它盖过明确cleanup_errors；应结合实际finally/capture/原生进程证据。反过来也不能把已证明外部自然端点变化永久当产品恢复责任。

## 8. 新会话应按此顺序继续

### 第一步：核对身份和最后现场

读取本交接、记录57、discovery100-03的result/report/ledger/start及最新工具冻结。只读核对当前VM服务、run/controller和本任务capture/probe。不要先reset；缺失IPC原件可能仅存guest。

### 第二步：调查ipc-parent.jsonl缺失

直接入口：

- `test/mcp/acceptance/scenario_suite.py::_export_run_originals`（交接时约2238行）。
- `fakenet/mcp/artifacts.py::RUN_EVIDENCE_FILES`。
- 用`rg`搜索整个仓库的IPC parent日志生产者、run目录设置、重启后对象复用/日志关闭/保留清理；不要只搜索文件名字面量就认定没有生产者。

最小只读检查：

1. 列出上述两个确切run目录全部文件名、大小、创建/修改时间和哈希；限制到两个run，保存新Logs诊断文件。
2. 查询导出时实际目录拼接和返回列表，区分`IPC未生成`、`落到别处`、`重启时覆盖/关闭错误`、`清理删除`、`大小写/过滤/枚举问题`。这些是待检假设，尚无哪个成立。
3. 与早前正常候选同run IPC原件对照，核对真实部署与源码身份。不要为排除缺失把required里的IPC删除。
4. 如缺失仅为工具记录路径/时机错误，修最小执行器并做本地原件重放/针对回归；如为产品生命周期日志归属错误，主会话修产品、固定新源commit、重建并部署新候选，不能把变动源码冒充旧包。
5. 若某文件只是可选诊断、不涉及本次探索性产品判断，清晰分离“场景执行”和“正式证据齐备”，记录不完整而不中断剩余独立找错；必须先证据化判断，不能靠异常字符串白名单掩盖真实产品失败。

### 第三步：继续实际未执行矩阵

- 仍由Terra/xhigh执行，不让主会话跑100。
- 修复后用**新批次根**保存复验，旧005/006三个原结果不改。
- 是否再次复验006及何时补齐005，要依据修复所需最小验证决定；重复次数和distinct覆盖分开。
- 98个未执行ID的权威列表在`discovery100-03/reverify98-report.json.remaining_not_executed`，不要根据连续编号猜；故障ID在前面但执行顺序为良性后故障。
- 现有Logs驱动可作模板，不照抄其中“遇任何未知失败就永久停”的过度保守策略。先记录原始结果，分清是否存在真实未清理责任；能独立继续的条目应推进。
- 最终至少报告：distinct尝试覆盖、完整执行数量、原判据pass/fail、证据错误、确证产品缺陷、未执行清单、修复及复验关联。探索性100不得伪称正式ACC全部通过。

## 9. 可直接复用的命令和工具入口

### 离线回归

```bash
PYTHONPATH=. .venv-mcp-runners/bin/python -B -m pytest test/mcp/test_scenario_suite.py test/mcp/test_sst_fault_evidence.py test/mcp/test_scenario_spike_gate.py test/mcp/test_sst_tcpip_events.py -q
```

最后实际结果：`191 passed, 6 subtests passed in 1.89s`。不是整个仓库最新全测结果。

### 已有精确argv

- `L/argv-09.json`：生成/regen/P1–P7/fault-spike的候选实参模板；不要对旧root再次执行。
- `L/discovery100-03/argv-reverify98.json`：最新探索执行参数。
- `L/discovery100-03/discovery_reverify98.py`：最新探索驱动。
- `L/discovery100-03/reverify98-start.json`：argv、driver、manifest、工具和历史结果冻结关系。

VM读取常用Python构造（实际调用前先看当前工具接口）：

```python
import sys, json
from pathlib import Path
sys.path.insert(0, 'test/mcp/acceptance')
import scenario_suite as s
argv = json.loads(Path('Logs/fakenetng-mcp/unified-repair-20260913/7ec8eff-loop/argv-09.json').read_text())['preflight']
runner = s.Suite(s.parse_args(argv[3:]))
runner.require_clients()
value, raw = runner._vm_json("<限定run目录的只读PowerShell>", 60)
```

诊断输出新建在Logs中，不改原CLI、不要以这个示例运行旧preflight/场景。`Get-ChildItem -LiteralPath`用于确切run目录，禁止无范围递归导出用户数据。

### Windows构建

若确需产品重建，先看AGENTS和活动MCP包装入口，复用镜像：
`flare-fakenet-ng/gui-vm-diagnostic-builder:py3119-pyi6220`。

不要因Ubuntu无powershell宣称不能构建Windows。最新AGENTS明确正式GUI v35有Docker入口，但**MCP候选不是GUI包**，应先查看本任务已有MCP构建记录及脚本再选命令。Wine测试不代替真实WinDivert/路由/进程结束验收。

## 10. 历史通过项及不要重做的事

- 此候选历史Windows测试1193 pass/8 skip、冻结EXE检查的证据在构建目录；这来自前段执行记录，不能宣称交接时重新跑过。
- P05 normal5（builtin3+custom2）、fault5、final及新summary已闭合；`L/p05-summary-02/result.json`为新正式summary，旧工具错误summary仍保留。不要因为当前100失败再盲目重跑所有P05。
- 原五类Spike05—09有大量机制证据，但没有完整五类都通过，不计实际100，也不修改其failed。
- 不继续实现搁置的curl挂起启动，不再造一个只为通过取证的前置工程。
- 不把作者会话切成第三方审核角色、不重复索取用户已经给过的执行许可、不因无关worktree脏文件阻塞当前局部修复。

## 11. 子智能体与会话交接状态

最后实际执行者：`/root/terra_discovery100`，模型gpt-5.6-terra，xhigh，独立上下文。它已返回最终失败结果，未派发sst007。最近一次终端session66323属于第三批执行，子智能体已给最终结果；新会话不要假定session ID可跨会话复用。

其他旧代理名包含terra_repair100_v3、con008_recovery_find等，仅是历史对象，不要未经核对让多个代理并发操作同VM。新会话若不能复用旧agent，按模型/推理强度/forknone要求新建；给完整身份和范围，不能只说“继续上次”。

用户此刻要求的是生成详细交接文档。**交接创建期间未继续修改产品、未启动新测试、未重置VM。** 下一会话从第8节最新IPC原件缺失开始，不要从首次需求或原第三方角色门禁重头开始。

## 12. 对接手者的质量提醒

前任的主要失误是：用正式取证完善替代实际产品找错；每遇一个工具问题都让整条执行线停住；过早将未知错误分类为产品故障；口头承诺等待60分钟却用60秒。用户对这些行为明确不满。接手应以**实际场景进展和有证据的产品修复**为核心，谨慎报告因果和数量，修一个已经证明的执行器错误后就推进，不能再用文档或流程数量冒充完成度。
