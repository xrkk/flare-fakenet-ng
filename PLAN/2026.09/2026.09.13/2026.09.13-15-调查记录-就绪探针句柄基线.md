# 就绪探针句柄基线根因与处理

会话角色: 作者/执行方

对象：统一修复方案06 v0.4，blob e352087e003f989fa9a1934edc0da36334eefa0e，IMP-002/ACC-002；源码893378f7004190b83005c58ba0020c3fba263406，tree8976839f54b89f02851043b25740213d447b0271；已审核可实施。用户本轮明确同意继续处理此次失败；下一轮正式验收首失败仍停止。

原首错：readiness第1case完成真实ready/native command line/cold-stop/exit0/Job清空，但探针总句柄103→110断言失败。原证据及失败结果保留，不改判通过。

诊断仅在已授权.233 / C:\Python313（3.13.7）、原候选mcp-c893378f7-4845a650f14c执行，不改产品和部署目录。三次最小真实ManagedProcess循环：首次99→108，后两次108→108，drop引用及gc不变。逐调用计数定位首次socket导入和os.pipe；纯运行库对照无需创建ManagedProcess：socket导入99→105；首次os.pipe打开105→108、两端关闭→106；后两次打开106→108、关闭→106。原探针两个negative已触发CreateProcess的一次性初始化，因此原7增量恰为socket6+首次pipe1。不是依据稳定计数猜测无泄漏，而是无产品实例的独立控制完整重现同增量。

处理：仅验收探针新增prepare_probe_runtime，两个negative后、正式case前显式导入socket并创建/关闭一对管道，记录初始化前/后socket/后pipe的计数及关闭事实。没有预跑产品case，没有跳过首轮，没有计数容差/减常数，也没有删除每case严格相等与实际Job/目标/管道/helper结束断言。方案ACC-002本身要求自身资源无遗留，未要求把进程级运行库首次加载计入本次Job责任；属原合同内基线采集实现纠正，不改变来源、范围、修复机制、验收通过边界或fallback，因此不需方案变更或再次第三方审核。

修复对照使用相同三次真实循环，仅前置上述运行库初始化及原两个negative。结果初始化101→107→108，3个正式包诊断child每次108→108；ready/native读取/stop/exit/清理均成功。这3次是诊断，不能冒充正式40或100；后续需要新源码快照、正式构建及同候选完整验收。原探针失败保留为before。

证据：Logs/fakenetng-mcp/unified-repair-20260913/893378f/ 下 handle-diagnosis-01、handle-imports-01、handle-profile-01、handle-runtime-01、handle-after-control-01。每个目录保存实际执行脚本与VM原始终端响应；handle-diagnosis-manifest.json记录各文件大小/SHA-256。未改其他进程、服务、VM快照或旧证据。唯一产品工作区变更为test/mcp/acceptance/run_readiness_probe.py；受保护PLAN/FaknetNG.md和未跟踪来源保持原状。
