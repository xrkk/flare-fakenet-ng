# UR005实现与原件验证

会话角色: 作者/执行方

46定向通过→47引入v0.9最终68b248f06e03aa2e6f1048b1ea5a082123cd0b12。仅按IMP007/ACC005007修改验收工具；产品7ec8eff及mcp-c7ec8eff1-611c036301c8不变。当前11文件指纹：loop/ur005-implementation-baseline.json。

新增五项测试先在旧实现失败，再实现两完整transport context句型和policy_scope/partition；全部未知变体拒绝，不补正向/terminal。curl全run公网tuple先去重且DNS外立即拒绝，再全原件唯一connect/代，策略按原生进程窗口分inside/outside，边界相交或内冲突拒绝；主/peer完整policy_refs及policy_context_refs独立保存。原始ETW不裁剪，2e及完整代次最早终止保持。应用重判比较主/aux/curl全部观察及显示正向策略字段。

只读找错核对未发现核心放行缺口，指出curl process_flow展示仍最后窗外行，已改为真正窗内已裁定行。进一步核查原正向上游log_event按全run首事件选择，可借主probe的sport17019；现按curl窗口内首次登记的唯一上游socket（ip/port/sport/kind）匹配，重复包不算新socket，第二socket拒绝。SNI同窗口唯一。Spike06本curl已绑定自身sport17026及11个物理NIC包，不借主连接535包；原件loop/spike06-traffic-replay-02.json。此为落实已有“对应策略及NIC”的实现修正，无新增权限/合同。

回归183 passed,6subtests passed（ur005-test-result.json记录实际工具返回）；含新句型严格字段、窗口边界/冲突、第二公网tuple/集合外/第二代、真实application consumer保留完整context、各观察槽删除/重复/改序/污染tuple/context refs、不能借主upstream、重复包不制造新socket。完整Spike06四TCP+UDP+curl原件重放traffic true，但旧失败不追认、未计实机新五类/100。新Spike07使用独立目录，主会话执行，实际100仍0，Terra/xhigh待门禁。
