# FakeNet-NG 统一问题修复方案

会话角色: 作者/执行方

- 版本：v0.4
- 状态：已审核, 可以实施
- 类型：错误与局部架构债统一修复方案
- 对象：当前本地 worktree；基线 commit `3cdec36784ed67cfd42149147e88cbb35cba3e50`，tree `09bef30eca4a979aecf5d14a792ff0f303658163`，分支 `process-redirect-v23`。
- 单一方案承接用户“本地修复所有发现的问题，查明根因后可分阶段，但都包括在一个方案里”。阶段不拆成子方案，不继承旧情景方案的第三方通过资格，不关闭旧 AUD/CHK。

## 0. 规范来源与身份

路径均相对仓库根 `/home/adminn/projects/flare-fakenet-ng`。下列来源和63个相关路径的完整 Git blob 固定于 `Logs/fakenetng-mcp/act002-investigation-20260913/repair-baseline.json`，该文件 blob `4186a762d83934dd69dcd58e2beb02af933909cb`。自评补充直接依赖及07记录指纹见同证据根`repair-baseline-additional.json`，blob `4266a546c00bd6030925eb35320bd97131b31d80`。两份是本地内容指纹清单，不冒充提交对象。方案自身 blob 在自评/审计记录中外置记录。

| 来源 ID | 对象与实际采用语义 |
| --- | --- |
| REQ-FIX-001 | 同目录 `2026.09.13-03-范围确认-统一问题修复.md`，blob `ccc914f71d0c3fdb5fa1f62d5531443160a1591e`：全部已证实问题一个方案，本地分阶段修复；已反证项排除，已修项真实验证 |
| REQ-FIX-002 | 同目录01架构审计 v4，blob `a026e0ae20ff83e57259f9530449df99afd8c16c`：ACT-001/002/003/005；范围选择由03覆盖。ACT-004的生产错误传播反证有效 |
| REQ-FIX-003 | 同目录04调查 blob `0f0a1a10b84b01ad39a28e2f1d27ae8f1c386c52`：FIX-001/002/003根因、原始失败及实验限制 |
| REQ-FIX-004 | 同目录05调查 blob `1246cec3ebf6d9f50b1cadb858dca2db31f3697f`：FIX-004/005，真实watcher控制流反例，不冒充原生平台故障 |
| REQ-FIX-005 | 同目录07调查 blob `2fb7c2a272059ed2a44dba4fbfc895d4dd5d88c4`：FIX-006/007真实调用链回放及限定修复方向 |
| CON-FIX-001 | `AGENTS.md` 全文与 `docs/architecture.md`、`docs/testing.md`：Logs/dist分离、Windows builder复用、真实VM边界、保护并行修改；AGENTS构建说明的已证实漂移只由IMP-006修正 |
| CON-FIX-002 | `PLAN/2026.09/2026.09.02/2026.09.02-01-需求提炼-FakeNet-NG-MCP.md` 正式v4，blob `414d339324b402dc85424b1d45e676590b6f2366`：REQ-003/005/006/007/008/010/011/012/016、CON-004/006/007、NON-006/008/009、RACC-004/006/007/008/009/011/014/018；包含v2/v3/v4回流的同ID完整语义 |
| CON-FIX-003 | 同目录MCP总纲 v8，blob `63d3ec36958f46771daa8a41638829e0291c6df0`：§3不变量、§5生命周期、§16.1原子Job及请求序号、§17停服栅栏、§18创建/退出身份及60秒采集、§19单飞/平台边界、§19.2逐对象结束。只承接现行语义，不据总纲页首宣称本方案已审核 |
| CON-FIX-004 | `PLAN/2026.08/2026.08.05/2026.08.05-01-Windows指定IPv4全端口及指定端口放行方案.md` v5 §3.1/3.2/3.3与§26.1/26.2：大写TCP/UDP、规范global IPv4、缺失与空值区分、32规则/16地址、去重及通配冲突、保护地址排除。§26历史活动地址不被本方案扩展或新增真实探测授权 |
| CON-FIX-005 | `PLAN/2026.09/2026.09.12/2026.09.12-17-PLAN-CHANGE-008-验收矩阵重构.md` §1—5、同目录16用户授权、同目录01情景方案 v0.5 §4.1/4.2/9：当前 normal5+fault5 冒烟、100实际情景85良性+15故障、同候选、完整恢复及目标会话关联。v0.5原审核未通过状态保持，不由引用冒充已通过；使用用户随后直接授权的既有工具，结果仍按其严格合同核验 |
| CON-FIX-006 | 当前用户的100情景执行者约束：`gpt-5.6-terra` / `xhigh` 子智能体执行实际100，主会话实施并核验证据；修复完整流程指定首次审核 `gpt-6-astra` / `medium`，每次 `fork_turns=none` |

规范来源版本和整体blob变化仅用于追溯；引用稳定ID的语义变化才暂停受影响项。不可用的历史证据不得改写成已通过。没有新增产品能力、网络授权或任意执行接口。

## 1. 问题、根因与修复前证据

证据根 `Logs/fakenetng-mcp/act002-investigation-20260913/`；原清单 `manifest.json` SHA-256 `ff6ded562993692a472b87a3c514948ba025a759174ada8a902893fbb52d7fe2`，后续watcher两项为 `manifest-lifecycle-supplement.json`。两份不可变调查04/05解释输入、实际输出、局限和退出码。

| 稳定问题 | 现象及已坐实根因 | 原场景/反例 |
| --- | --- | --- |
| FIX-001 / ACT-002 | CreateProcess成功后没有用户态就绪因果屏障，立即读取远程PEB命令行；同类入口包括普通start与exit_capability | 原run `47e4fb8f-0c41-4202-b751-0b6cdf101812` 在after_api之后、before_start之前299。原生同句柄40对照中1次创建即读299、READY后40次全成功，PID/FILETIME不变；Python3.13实验不替代正式包 |
| FIX-002 / ACT-002 | 父方在start发出前失败也走stacks→stop，子方却只接受instance存在的stop | `replay_prestart_stop.py` 实际child_main两次同条件退出1，报operation not allowed；没有构造FakeNet |
| FIX-003 / ACT-003 | GUI复制静态语法并放宽正规化/忽略空项，核心另有严格语法/集合约束 | `gui_core_full_validate_probe.py` 实际完整validate对小写协议、尾逗号、*冲突、443/0443重复均无ERROR，render保留，核心拒绝；合法对照一致 |
| FIX-004 / ACT-002 | `_finish_local` 与 `_finish` 的释放前提分叉；无helper不能证明target已结束 | `replay_live_target_finalize.py` 走真实watcher硬截止分支，在给定仍活目标事实时close并报释放；退出1。原生事实为明确替身 |
| FIX-005 / ACT-002 | `_watch` DiagnosticError分支continue跳过期限判断 | `replay_watch_deadline.py` 固定1秒预算却在1.5—3.5秒继续读；退出1。模拟时钟不构成平台不可取消证明 |
| FIX-007 / ACT-002 | exit_capability局部Job在未结束时仍被finally关闭，清理异常覆盖主错误 | `replay_capability_cleanup.py` 实际verify_native在handoff失败和terminate未结束后仍close；退出1，原生观察为替身 |
| FIX-006 / 验收工具 | Suite.run(fault)只读取外部JSON的passed布尔值，不核对候选、五类及证据完整性 | `replay_unbound_spike.py` 实际run准入以仅`{passed:true}`调度故障执行器；执行器为记录调用的替身，无VM动作，退出1 |
| ACT-001 | MCP解释各listener私有socket/thread布局；资源变动要求外部同步，已有relay误报只是一项历史症状 | `managed.probe_instance` 的一般/DomainEgressRelay分支与10种provider的真实资源创建/销毁；已有relay功能修复保留 |
| ACT-005 | AGENTS仍说无正式Linux包装器/v34，实际脚本已固定formal v35 | Build-GuiVmPackage.sh→tools/build_gui_vm_package_wine.py→PowerShell合同；已有一致性测试，不合并两个OS入口 |

FIX-001证明的是当前读取顺序的不成立前提，并未证明所有299具有同一原因；就绪后仍有原生读取错误时保留具体API错误、失败关闭，不能重试掩盖。原历史包及Logs均保留。

ACT-004不改：Diverter/Fakenet真实失败先清理后抛异常，没有静默False的生产反例。历史ACK修复、网络前提修复、工具finally/原始异常保留修复已在基线；只按当前回归证明，不再重复实现。没有证据支持全项目推倒重写或安全漏洞利用结论。

## 2. 范围与资源边界

核心维护：`fakenet/mcp/{managed,supervisor,exit_retention,exit_capability,cli,jobobject}.py`；`fakenet/listeners/ListenerBase.py`与DNS/FTP/HTTP/IRC/POP/Proxy/Raw/SMTP/TFTP/DomainEgressRelay十种provider；`fakenet/diverters/egresspolicy.py`静态解析和runtime保护地址交界；`fakenet/gui/validator.py`；`AGENTS.md`构建章节；对应最小回归测试与受控验收入口；scenario_suite.py 的故障前置证据准入及一次失败停止分支。

直接依赖：exit_native/diagnostic_process/exit_intent/exit_guard的真实身份、实际结束、单飞与协议；coordination/snapshot/baseline/configlock/endpoint_observation的恢复责任；Fakenet.start/stop、faultinject、GUI configmodel/app、正式打包入口及既有scenario/P05 runner。默认只读，只有本方案已指定的调用适配/反例测试可修改。直接依赖切片变化必须更新映射。

决策关键路径：基线清单中的核心路径、exit_native/diagnostic_process/exit_intent/exit_guard/jobobject、上述根因脚本/结果、规范来源。上下文路径：GUI app/configmodel、coordination/snapshot/baseline/configlock/endpoint_observation与现有打包器；其原功能不改。验收工具可新增本方案的无接管就绪探针、五类Spike调用入口、同候选原始证据准入校验及显式首次失败停止参数；不能放宽既有通过断言。

排除：业务协议处理、主WinDivert过滤器/授权策略、GUI布局和启动权限、配置内容/活动公网规则、报告/PCAP功能、Linux支持、第二主管、通用插件/状态框架、未证实漏洞、旧AUD/CHK裁定、push与分支切换。`PLAN/FaknetNG.md`已有修改及两日期目录旧记录受保护，不暂存、不修改。

资源顺序：当前运行恢复标记仍先于接管；ManagedJob从CreateProcess返回即持原生进程句柄，不能在就绪等待中关闭；成功就绪后ExitRetention取得独立句柄并发布身份，再发送start。不存在无Job运行或以readiness替代healthy。stop意图、Job结束、退出证据、全恢复审计和锁释放保留原序。

## 3. 实施项与阶段

### IMP-001：创建与远程身份读取之间建立就绪屏障

来源 REQ-FIX-003/FIX-001、CON-FIX-002/003。前置：本方案审计引入；不依赖后续IMP。

普通路径：ManagedProcess.request允许内部`ready`固定操作，仍走现有run_id/递增seq/当次响应校验。child_main在既有Job核验、流重定向、模块导入和本机process_identity完成后，只在instance=None响应`{ready:true, identity:{pid,creation_time}}`；不创建Fakenet、不读配置、不触发故障、不打开listener/WinDivert，不表示产品healthy。父方显式`wait_ready`核对ready===True和完整PID/FILETIME后，才构造ExitRetention并读取实际镜像/命令行；子方自报不替代原生核验。父方在RealSupervisor.start中先保存ManagedProcess对象，再等待，因此失败仍可清理。就绪预算固定最多10秒（实现常量，有限启动子步骤；不改变已有start请求30秒），超时/EOF/错身份均立即失败，不补发ready，不接受旧响应。现有IPC错误终止语义不变。

同源自检路径：exit_capability没有生命周期IPC，采用一个仅本次创建、无名称的Windows Event；仅把该event句柄追加到现有CreateProcess HANDLE_LIST。内部exit-capability入口新增一个十进制句柄参数，校验专用映像、run UUID及Job后，在GetCommandLineW和process_identity成功后SetEvent并关闭子方句柄；不新增MCP工具。父方以event和原生目标进程句柄的WaitForMultipleObjects等待（最长10秒且不超过原started+60秒截止），目标先退出/失败/超时即拒绝；事件只是因果屏障，ExitRetention仍独立验证原生身份及实际命令行。event父方句柄在等待完成/失败后关闭，继承位在CreateProcess后清除，异常路径保留Job原清理。实现位于exit_capability与其CLI分派，必要原生绑定可放jobobject，不构建通用消息框架。

自检资源归属同时修复FIX-007：新增局部NativeCapabilityOwner（置于exit_capability模块）持有本次Job、ExitRetention、就绪event、截止、原始失败与清理失败。RealSupervisor在调用自检前保存该owner，再调用verify_native；验证成功且实际对象全部结束才缓存passed结果并清空owner。失败owner保持可达，原错误与cleanup_error分别保留。verify_native现有调用形式可保留，但失败异常必须携带owner；生产唯一调用者必须提前持有它，不能把异常回收当作清理。

RealSupervisor._ensure_exit_capability首先拒绝尚未结束的旧自检责任；RealSupervisor.stop（包括无运行/无marker分支及两阶段预停）先对同一owner在当前stop截止内发起取消/终止并观察结束，未结束返回failed且不得允许标准SCM STOP/升级替换。只有实际确认其目标、助手/诊断及Job成员结束才关闭对应句柄并清空owner；不重新跑自检、不新建采集，也不把先前失败改为成功。自检未曾网络接管，不虚造新的run恢复便签；当前内存owner与既有原生Job/SCM异常路径承担此辅助责任，无新持久状态源。取消/结束证据必须对外保留原始原因；主管自身崩溃仍按既有Job+SCM恢复，不承诺基础设施故障不断联。

构造失败责任交接（AUD-001，FIX-007同源边界）：ManagedProcess、ExitRetention和NativeCapabilityOwner统一采用明确的两阶段调用。`__init__`只保存参数、建立内存字段和空资源槽，不打开文件/管道/原生句柄、不创建Job/诊断任务或线程；可能失败的资源取得全部进入一次性`initialize()`（自检owner为`verify()`）。RealSupervisor按`self._fakenet = ManagedProcess(...); self._fakenet.initialize()`、`self._exit_retention = ExitRetention(...); self._exit_retention.initialize()`调用；自检先由主管保存NativeCapabilityOwner，该owner也先保存ExitRetention再调用initialize。禁止保留另一条带资源副作用的旧构造快捷路径。initialize不得重入或重试；成功、失败都由预先保存的同一对象负责已取得资源，不通过traceback或析构器传递责任。

| 部分初始化对象 | initialize前的空槽及取得规则 | 失败后的收尾与门禁 |
| --- | --- | --- |
| ManagedProcess | job/pid/identity/reader/send/receive为空，原始fd/继承handle/stream清单为空；每次取得立即登记；Job在spawn前归属self，spawn后即使identity或observe失败也不close活Job | 原始错误保留；身份未知不发送IPC、不按未知PID取dump，按已持有Job取消并观察实际成员结束；reader/管道/继承位同责任清理，全部结束才close；pid/identity缺失为未知，不冒充完整身份 |
| ExitRetention | target/helper/helper_job/record/intent/worker为空，两诊断owner先作为内存对象登记；TargetHandle返回后立即登记，再做identity/command_line/exit-init/worker.start | exit-init超时、worker.start失败、record/intent缺失都进入初始化失败结果complete=false、done置位；不启动watcher补救、不重跑exit-init、不新采集；诊断任务cancel只作用于已有active任务，等待其ended，原target已结束不能替代诊断Job结束 |
| NativeCapabilityOwner | Job/event/fds/retained先为空，主管保存后才执行资源取得；retained也先登记再initialize | 与普通路径同一逐对象实际结束规则；原错误与cleanup_error分别记录，所有槽清空且对象结束才可结束责任，失败不写passed缓存 |

每次资源取得失败之前已取得的资源也必须可达；清理每个槽独立记录异常，不能因第一个close失败跳过其余责任。`initialized`仅允许完整初始化后进入普通IPC或watcher协议，不作为资源结束证明。ManagedProcess未完整初始化时，主管stop走其专用同对象cleanup，不执行stacks/stop或访问缺失identity；ExitRetention初始化失败时，wait/settle只继续已有资源取消/结束观察，缺record/intent时不发布正常stop意图或伪造owner-result，内存报告仍保留failure_stage与原始错误。可以发布的小结果使用已知身份；缺身份不写假记录。

主管start前置、stop（含无marker）、两阶段预停/替换统一检查上述三个责任槽的真实结束状态；不能仅检查_fakenet Job空或retained目标退出。所有责任收尾失败均返回failed且保留对象，marker/锁/原恢复审计规则保持；后续stop只继续同一责任。全部对象后来结束允许恢复动作收敛，原启动失败与证据缺失仍为failed outcome。恢复便签仍只描述原受管运行，辅助初始化不建立第二持久状态源。此处是现有责任交接的局部补齐，不新增后台监控框架或诊断任务重试。

移除两个入口“无就绪条件即读远程命令行”的路径及自检异常finally无条件close活Job路径；不改TargetHandle.command_line为fallback/空命令行。普通IPC和自检各自已有通信条件不同，不能用固定睡眠统一。

### IMP-002：启动前停止的完整语义

来源 FIX-002、REQ-011/012、CON-007。依赖IMP-001的ready阶段；也覆盖尚未ready已失败的现有清理路径。

child_main接受instance=None时的stop，直接返回`{stopped:true}`并正常退出0；不构造FakeNet、不调用listener故障注入/cleanup_error。instance已构造（含parse/start部分失败）仍调用原instance.stop并传播原异常。invalid run/seq、未知操作、重复start/运行期ready不放宽。stacks仍可在实例缺失时读取。

父方仍做Job实际结束及全恢复审计；冷停止响应不直接清除恢复标记/锁。失败启动保留最初failure_reason、last_run_outcome=failed和incident，不能因为随后冷停止成功而改为ok。不把故障注入导致的正常负向启动门拒绝放宽成其他初始化错误通过。

### IMP-003：统一退出释放前提与不可跳过的截止

来源 FIX-004/005、CON-FIX-003 §18/19、REQ-012/CON-007。前置IMP-001/002；保留原SingleFlight与ACK修复。

把_finish/_finish_local最终释放所需的短时原生/内存判定集中在同一私有方法：已取得helper及其Job必须真实结束；两个DiagnosticOwner的pending必须为false；目标自身必须exited。没有helper只免除helper扫描，不免除target/诊断结束门。已经按实际结束释放的None句柄支持再次收尾；仍活对象不close、不置释放成功，不因done/future完成推导对象结束。对外保留complete=false、原失败阶段及cleanup_error、helper_ended/retained_target_handle_closed实际值；settle仍只继续同一责任，不启动新采集、不恢复晚结果为成功。发布失败不抹去内存真实释放事实及主错误。

_watch每轮先用当前单调时刻判共同采集deadline，再尝试entry读取；DiagnosticError重试仅在剩余窗口内，sleep上限min(0.5,remaining)，到期进入失败收尾而不是continue。deadline一旦成立不得被读取失败延长。到期不能启动新的数据采集；已有完整owner dump只能核验/引用既有结果，不把超过采集截止后新产生的dump变为成功。已有FINALIZE_BUDGET=45仅用于终止/确认和小结果收尾，不成为第二个采集窗口；仍未结束保留责任、拒绝新start/采集/替换。真实平台故障资格仍由原生证据判定，不由TimeoutError自动授予。

移除helper-less直接close和诊断错误无期限continue；不重写单飞协议、不更改60/180秒、512MiB等既有产品界限。等待writer不持ACK协议锁、验证前释放writer及同run结果身份校验继续保留。

### IMP-004：健康事实由listener提供

来源 ACT-001、REQ-005/RACC-006。前置IMP-003后集成；不更改listener启动/停止行为。

十种provider各提供无副作用`health_snapshot()`，返回`{handles:[int...], alive:bool}`。真实socket有效fileno与必需服务线程is_alive同时满足才alive；未启动、缺线程、死线程、关闭socket、观测异常均为不健康。九种server/server_thread类可共用ListenerBase中的显式参数工具，不反射搜索属性、不改变继承图；FTP与其余socketserver均使用server.socket的fileno；当前pyftpdlib 2.2.0的ThreadedFTPServer没有fileno方法。DomainEgressRelay方法在本类读取_listener/_accept_thread。

managed.probe_instance只调用此接口并装配既有provider/name/handles/alive输出，不再导入DomainEgressRelay作类型分支，不读provider的server/sock/socket/thread私有布局。缺方法或异常保守记该provider alive=false、可得原因，不跳过该provider；这是未知健康的失败关闭，不是旧布局fallback。Diverter与capture健康原判据、无provider失败、日志异常撤销健康及公开健康schema保留。

### IMP-005：共用核心静态IPv4规则解析

来源 FIX-003、ACT-003、CON-FIX-004。可在IMP-001—004后实施；不依赖VM。

在现有无socket/WinDivert依赖的egresspolicy模块公开`parse_reviewed_ipv4_rules(config, config_keys)`作为唯一静态解析入口，保持现有不可变rules/index/id/config_sha五元组和PolicyConfigError类型。旧内部_parse_reviewed_ipv4_rules调用它，再对parsed规则逐一执行本机IPv4/当前resolver/takeover保护地址校验；该runtime检查仍只在核心初始化，逐包索引及授权判定不改。

GUI _check_reviewed_rules把model.diverter键统一为核心所用小写配置映射，保留字段缺失/存在空值区别，调用同一静态入口；PolicyConfigError映射到原Diverter/ExternalAllowedIPv4Rules ERROR位置，消息带具体原因。删除GUI独立split/upper/strip/去重/数量/global语法副本及仅被它使用的helper。不能把小写自动改写为大写或过滤空项；只收紧此前错误漏报。GUI不进行DNS/路由/网络查询，不修改用户规则文本。Disabled模式原是否执行规则检查的条件不变。

### IMP-006：同步构建操作事实

来源 ACT-005、CON-FIX-001。依赖实际核对现有脚本，允许独立执行但仍在本方案内。

只修改AGENTS的Ubuntu构建章节：正式GUI入口已经是Build-GuiVmPackage.sh及其Wine builder，固定不可变提交并复现PowerShell formal v35合同；命令参数为source-commit/output-root/output-directory。保留诊断无参wrapper只构建v33-diagnostic-03、image-only不产ZIP、当前镜像检查、Wine不能代替真实VM等有效约束。历史v34可编译实证保留历史标签，不能当当前候选验收。无测试专为文字镜像实现；核对脚本和git diff即可。

### IMP-007：同候选回归和原场景验收证据

来源REQ-FIX-001、CON-FIX-002/005/006，依赖IMP-001—006。主会话实现/本地验证/构建并核对包；获资格后本地中间commit可用于不可变构建，不push。每次候选和Logs目录唯一，不覆盖失败原件。

最小测试覆盖ACC-001/003/004；Windows-Python用既有Docker/Wine镜像。正式MCP包用Build-FakeNetNgMcpPackage.sh，所有包内成员核验；新候选在已授权.233同一VM受控切换，旧安装保留，host-only传输SHA一致且用后停服。正式Win10验证就绪/冷停止和正常/故障收敛，先P1—P7、normal5/fault5/final/summary及五类目标会话证据Spike，再由指定terra xhigh子智能体执行实际85+15。旧包通过证据不可计入新候选，冒烟不等于实际100。

验收脚本错误也要保存首个实际错误与finally清理；若发现原合同内实现细节缺陷记录后修正，契约改变回流方案。用户此前失败即停边界保留：本次正式验收若失败，立即停止后续实际情景、导出本轮完整证据，报告根因和恢复点，不自行再开下一轮压力循环。未完成项不伪报通过。


IMP-007同时修复FIX-006的实际工具准入缺口（根因证据`replay_unbound_spike.py`/`unbound-spike-terminal.json`）：只存在passed布尔值不能承担CON-FIX-005要求的同候选五类原始证据。实现限定为：

1. 新命令`scenario_suite.py fault-spike`，公共身份/VM参数与generate/preflight相同，使用独立新`suite-root`（名称标spike，不能和实际100根混用）。读取已生成且通过覆盖校验的manifest，按scenario_id排序，每个FAULTS五类选第一个非default情景，共5个。require_clients/P1—P7通过后，复用真实Suite._run_one逐个运行及现有严判；不调用run(fault)的外部Spike前置门，以免循环依赖；每个case自身原始观测/关联/恢复/异常判据不变。首次非pass停止并封存余项未执行，不自动重试。该5例只证明前置，不计实际100。
2. 输出独立根`fault-spike-result.json`，schema=`sst.fault-spike.v1`，含identity（现有Identity.as_dict完整值）、manifest路径/大小/SHA、five_classes数组、cases数组（fault_class/scenario_id/run_ids/result文件路径/大小/SHA/evidence裁决结果路径/大小/SHA）、passed。passed仅在五类恰好各一次、每个真实case通过、原始字节引用经现有sst_fault_evidence独立重判且恢复成功时为true；缺类/重复/旧候选/合成数据/缺文件/哈希漂移均false。0=全通过，3=执行或断言失败，4=前置阻断，2=CLI用法错误；与现有EXIT_PASS/EXIT_FAIL/EXIT_BLOCKED/EXIT_USAGE一致。
3. 普通run(fault)在任何变更前验证上述完整对象、同候选identity、五类唯一、case与adjudicator输入和所有原件的大小/SHA及重新裁决；不直接信任passed。保留passed字段供人阅读，旧只有布尔的输入明确拒绝，不做兼容fallback。resume中涉及故障也使用同一准入，避免第二入口绕过。
4. 新可选`--stop-on-first-failure`仅控制本次run/resume调度；true时第一项非pass立即结束后续调度，原本次_run_one的必要收尾仍完成并保留原错误；不把未执行计pass，不改默认旧模式语义。本方案所有实际命令明确传true。新增测试用真实准入与记录调用的执行器替身证明拒绝发生在副作用前，并验证首fail后无第二次调度。

## 4. 依赖与全局不变量

唯一实施顺序：IMP-001 → IMP-002 → IMP-003 → IMP-004 → IMP-005 → IMP-006 → IMP-007。前三项同属生命周期一组；后两类根因独立但共同交付，不为加快推进绕过审核或改成多个方案。

始终保留：一个主管/一个受管树、原子Job归属、同run/seq的当次IPC、进程+初始化+主动探测同时满足才healthy、异常撤销、日志保留、单控制者和停服栅栏、原快照唯一恢复源、全部恢复审计后stopped、目标逐对象结束/单飞/额度/原截止。新readiness只证明用户态执行到确定点，无网络接管、无用户可见healthy。

## 5. ACC通用执行与证据合同

命令工作目录均为仓库根。`PYTHONPATH=. .venv-mcp-runners/bin/python -B -m pytest ...`为本机工具。Windows测试命令形状：`docker run --rm --volume "$PWD:/workspace" --workdir /workspace flare-fakenet-ng/gui-vm-diagnostic-builder:py3119-pyi6220 xvfb-run -a wine 'C:\Python311\python.exe' -m pytest <本项明确测试集合> -q`；Windows测试/编译前先docker image inspect，记录实际image ID。同一临时容器内先执行`xvfb-run -a wine 'C:\Python311\python.exe' -m pip install --no-index --find-links Z:\workspace\wheelhouse mcp==2.1.1 pydivert==2.1.0`（路径作为独立argv或正确引用），成功后再执行pytest；这是现有MCP builder的离线依赖准备，不能用裸镜像缺SDK的结果替代。不把测试需要的公网样例字符串发为真实网络流量。

每项证据写入新建 `Logs/fakenetng-mcp/unified-repair-YYYYMMDD/<candidate-or-source>/ACC-xxx/`，含原命令argv、cwd、源码/包SHA、开始/结束、returncode、stdout/stderr、逐case输入/预期/实际、结果与原始文件大小/SHA清单。既有runner保留其原schema，索引引用而不改写旧原件。pytest非零或必须case skip均不得通过；平台专属case在对应平台执行。无权限/环境不可得为该项blocked，不能把无关dirty当失败。

### ACC-001：生命周期反例及同源构造入口

对应IMP-001/002/003，来源FIX-001/002/004/005/007、AUD-001与CON-FIX-003。前置源码修改完成；输入04/05原脚本固定的同样请求、原生观察替身和模拟时钟。

实现回归 `test/mcp/test_managed_readiness.py`（新）、现有`test_managed_ipc_faults.py`、`test_exit_retention.py`、`test_supervisor_units.py`及`test_startup_network_readiness.py`；本机和Windows运行这五个文件。新test必须走真实child_main/RealSupervisor.start与真实watcher，不只测一层布尔helper。

断言：ready响应之前不得远程读命令行/发start；ready不是init/healthy；错误run/seq/PID/FILETIME、EOF、超时拒绝且不重试；自检event先退出/超时不读PEB、不改60秒总截止、句柄无泄漏；handoff失败且terminate未结束时保留owner/Job/target、同时保存原始与清理错误，阻止start及预停成功/替换，实际结束后同一owner清理且不重复采集。instance=None的stacks→stop退出0且无Fakenet构造；部分初始化仍调用stop；原始失败结果保留。无helper但target活/diagnostic pending不释放，不report完整；对象结束后同责任settle可完成；发布失败重入不丢主错误。持续DiagnosticError到截止后不再读/采集；单次错误随后窗口内恢复允许，晚成功拒绝。已有ACK让行、writer单飞和网络前提拒绝在baseline/marker之前的测试继续通过。

构造期反例必须执行真实ManagedProcess/ExitRetention.initialize及主管调用层：①exit-init已创建独立诊断Job但超时且未结束；②worker.start失败；③spawn成功后process_identity失败；④spawn后observe回调失败；⑤在record/intent尚缺时身份读取失败。只替换原生/进程结束事实、线程start失败及OS资源边界，不替换被审的责任保存和异常清理分支。逐次断言主管槽在第一项资源取得前已保存同一对象；target结束且诊断Job活仍不得放行；原始与清理错误均保留；无marker stop及预停不能成功；start不覆盖旧owner；改变替身为对象真实结束后同一owner可释放，不补启动、不采集、不把原失败改成功。另覆盖首资源取得失败和中间管道/句柄取得失败，证明已取得资源没有未登记空隙。AUD-001的审核反例是before控制流证据；适配两阶段调用后的after必须保持同一未结束谓词。

同条件前后：04/05记录为before；新回归包含同样输入/事实的after，并再次执行可保持调用合同的原始回放。原脚本如因接口增加而必须适配，保留原文件，另存差异并证明只适配调用输入、不替换失败谓词。所有断言通过才pass，模拟不代替ACC-002原生证据。

### ACC-002：真实Win10就绪及原始启动失败回归

对应IMP-001/002/003/007，来源FIX-001/002、REQ-011/012。前置Windows回归及正式新包核验、目标VM身份可读、无未结束责任。

新增一次性验收入口`test/mcp/acceptance/run_readiness_probe.py`，工作目录仓库根，输入参数`--package-root`（VM已部署目录）、`--source-root`（与包源码SHA一致的只读探针依赖）、`--output`（VM本次Logs目录）、`--count 40 --source-commit <当次SHA> --package-sha256 <当次ZIP_SHA> --manifest <当次manifest>`；这些值必须与源目录、部署成员SHA逐一核对，在真实Win10以可用Windows Python执行，仅启动随包managed-child，通过真实ManagedJob/ManagedProcess代码且显式选包内exe，禁止接管网络。输出`readiness-result.json` schema `fakenet.repair-readiness.v1`，字段source_commit/package_sha256/vm_identity/cases[]/passed；case含run_id/PID/FILETIME、creation/ready/remote_read的单调时刻、原始ready帧、真实命令行、cold-stop响应、exit_code、Job成员/句柄关闭事实。0=全部通过，1=断言失败，2=前提缺失；保存首错并停止。工具还需支持无ready/目标早退的受控测试对象，仅测试自身创建的对象；假对象不计40个正式包正常case。

40个随包child均须创建→ready→原生command_line成功→冷stop→本次Job实际退出，无299、无operation not allowed、无遗留；注册SPE导致的辅助进程也要原生枚举确认已结束（无匹配target的通知只允许原拒绝路径，不计退出根因采集），并保留探针进程自己的读写/Job/event/pipe句柄关闭事实；原生能力自检须在新安装服务实例的首次start真实通过，固定SPE身份、同一60秒总预算。原历史normal-custom-001以ACC-005 builtin3之后首个custom原条件再验，必须healthy并正常恢复；不能以无网络40个child代替它。

未在同候选实际运行、读取错误仍存在、未知句柄/Job残留或自检失败均不通过。若299在ready后仍发生，保留具体API/目标身份回流调查，不降级命令行核验。原Python3.13小进程实验只作before根因机制证据。

### ACC-003：全部provider真实资源健康矩阵

对应IMP-004，来源ACT-001、REQ-005/RACC-006。新增`test/test_listener_health.py`，并运行现有`test/mcp/test_relay_health.py`、`test/test_http_listener_stop.py`、`test/test_proxy_listener_thread_exit.py`、`test/test_fakenet_stop_lifecycle.py`。Windows-Python为正式provider依赖环境；可运行者同时本机测。

十种实际provider类逐一覆盖未启动、真实socket+服务线程运行、线程退出但socket仍开、缺线程、socket关闭、stop后；使用端口0/loopback、实际类方法与实际socket/线程，协议业务可最小配置，不访问外网。FTP同时证明server.socket.fileno的实际路径。预期只有真实运行组合alive，描述符来自真实资源，死线程/闭socket任一发生即不健康。managed只调用health_snapshot，对无接口/异常拒绝健康，不回退反射；既有Diverter条件和输出字段兼容。任何provider未覆盖不得用“都是server”代替。现有正常协议/停止回归失败为fail，Wine无权替代ACC-005真实listener_stop。

### ACC-004：GUI与核心同输入一致、runtime保护未变

对应IMP-005，来源FIX-003、CON-FIX-004。命令运行`test/test_gui_validator.py test/test_gui_schema.py test/test_egresspolicy.py test/test_linux_reviewed_ip_guard.py`。test_gui_validator/test_gui_schema及egresspolicy在准备完成的Windows-Python运行；本机运行egresspolicy与test_linux_reviewed_ip_guard，GUI正向Windows盘符fixture不以Linux路径运行，不放宽产品验证。实际完整validator、ConfigModel保存/重载和核心parse使用同一个矩阵。

固定四个反例全部GUI ERROR且核心拒绝；合法TCP/UDP/*/精确端口、合法缩进续行、整字段缺失通过；存在空值、分隔空项、片段误作option、协议/地址/端口非法、规范化重复、通配冲突、32/33规则与16/17地址边界同判定；稳定rule_id、规范化集合/索引/config_sha与修复前合法输入逐字一致。测试数据全离线。保存不改规则文本，GUI无网络调用；runtime本机/resolver/takeover地址冲突仍拒绝，Disabled既有行为不回归。重跑04完整GUI回放应由exit1变0。任一语法漏报、授权范围改变或GUI引入运行态I/O即fail。

### ACC-005：同候选冒烟、五类故障与实际100情景

对应IMP-003/004/007，来源CON-FIX-005/006、REQ-005/011/012。前置ACC-001—004、候选部署身份、无旧恢复责任；源commit/ZIP SHA/manifest/部署记录/requirements和master blob/配置哈希均从当次实物生成，不沿用c3cdec数字。

使用现有`scenario_suite.py generate --seed 20260912 --count 100`与`--regen-check`、`preflight --preflight-through P7`；共同实参明确为`--candidate-id <当次ID> --source-commit <当次SHA> --package-sha256 <当次ZIP_SHA> --package-manifest <实物路径> --package-verification <核验路径> --deployment-record <当次路径> --target-base-url http://192.168.204.233:28788 --win10vm-mcp http://192.168.204.233:28787/mcp --suite-root <新Logs根>`。尖括号是实施期机械实物参数，不是待裁决方向；执行前写确切argv到实施记录。

P05用`run_p05_release.py --mode normal --rounds-limit builtin=3,custom=2`，继而`--mode fault --rounds-limit per-class=1`、`--mode final`、`--mode summary`；其余必需参数按该工具现有CLI与同候选身份填入，final/summary不带rounds-limit。normal必须3+2，fault五类各1，所有逐轮原断言保留；原custom失败不得再次出现，diverter_stop必须有helper entry/ACK/结果/完整incident与真实结束证据，不能只看stopped。

五类目标会话证据Spike使用下述固定新增`scenario_suite.py fault-spike`契约及既有`scenario_fault_evidence.py`、v0.5 §4.1/4.2；全部通过后才启动故障情景，不依赖未固定的Logs临时runner。执行者为新建`gpt-5.6-terra`/`xhigh`子智能体，实际`run --filter benign --stop-on-first-failure`、`run --filter fault --stop-on-first-failure --fault-spike-result <当次结果>`，主会话独立`verify --integrity`、一次`--replay-dry-run <实际scenario_id>`、`summary`。

通过：85良性+15故障全pass；每情景distinct工具≥10、15接口各覆盖≥5实际情景；B1≥25/B2≥15/B3≥15/B4≥15/default≤15，故障不落default；正常健康窗口及零未知异常、目标策略流量原始包证据、精确异常白名单/真实作用区间被同run单目标会话包含、全部恢复/锁/树/持续链路/incident成立；实际文件逐项哈希和同候选身份一致。生成100或冒烟通过不计实际100。缺失/skip/unknown/失败均不通过；失败执行立即停止并导出，不自行开下一轮压力测试。

### ACC-006：文档、打包与归因

对应IMP-006/007，来源CON-FIX-001与REQ-FIX-001。核对AGENTS对实际v35入口/参数准确，诊断和实机界限保留；逐hunk只归属本方案，用户既有dirty保持。正式包命令`./Build-FakeNetNgMcpPackage.sh <已记录中间commit> dist dist/<唯一目录>`；实际image、commit/tree、ZIP SHA及全部manifest成员核验入Logs。构建器原必需测试照常执行，不另外扩大为全库安全审计。所有IMP/ACC证据齐全后仅提交可归属改动与本链文档/实施记录，核对SHA与diff；无push。构建成功不授予其他ACC通过。

### ACC-007：Spike证据准入与执行停止边界

对应IMP-007/FIX-006，来源CON-FIX-005/006。前置工具实现完成。命令`PYTHONPATH=. .venv-mcp-runners/bin/python -B -m pytest test/mcp/test_scenario_suite.py test/mcp/test_sst_fault_evidence.py test/mcp/test_scenario_spike_gate.py -q`（最后一个为本方案新增）。输入原unbound-spike回放、只有passed、错误identity、缺类、重复类、错run、损坏/缺少原件、synthetic、每类合格真实证据的离线副本，以及首case失败的调度替身。离线替身只验证拒绝/调度逻辑，不作为正向实机Spike。

预期非法前置均在_mutate/_run_one前被拒绝；同候选完整五类重新裁决通过才可准入。首fail时本次必要finally执行，下一情景调用计数0；run/resume同断言。原`replay_unbound_spike.py`从exit1变exit0。真实正向准入由ACC-005产生的同候选5例证明；只有单元测试通过不使本项完整通过。所有路径输出身份/哈希/失败原因明确，任一旧布尔fallback或未执行被计通过为fail。

## 6. 双向追溯

| 来源/问题 | 唯一主IMP | 必需ACC |
| --- | --- | --- |
| FIX-001/007 | IMP-001 | ACC-001/002/005 |
| FIX-002 | IMP-002 | ACC-001/002/005 |
| FIX-004/005 | IMP-003 | ACC-001/002/005 |
| ACT-001 | IMP-004 | ACC-003/005 |
| FIX-003 / ACT-003 | IMP-005 | ACC-004 |
| ACT-005 | IMP-006 | ACC-006 |
| FIX-006、全问题一方案、同候选、指定执行者 | IMP-007 | ACC-001—007 |

每个IMP正文反向引用来源；每个ACC明确IMP/来源。ACT-004唯一去向为反证排除；原ACK/网络和执行器修复唯一去向为ACC-001/005回归，未经证据不重复重写。没有后序Pxx依赖或新增Pxx，历史P01—P05职责不改。

## 7. 异常与fallback评定

- 不新增启动重试、固定sleep、空命令行、旧健康反射、GUI自动宽松正规化或新采集窗口fallback。
- 保留原有优雅停止超时→根因证据→仅本次Job终止→完整恢复审计：真实故障必需，有截止，恢复不合格维持failed；故障结果保留，不能掩盖主错误。
- 保留owner dump对无SPE通知的挂起路径取证，但仅在原允许的采集窗口和全局单飞内；到期没有合格dump即缺证失败，不临时重开预算。
- 保留同责任settle，仅在原对象后来真实结束时释放，不重跑故障或把超时结果改成功；有界且未结束不放行。
- 对无health_snapshot或观测异常返回不健康，是未知即失败的判据，无替代健康来源。
- 环境/来源关键切片不可得只阻断对应ACC。任何平台豁免需原生归因，不能把本地替身或超时当成证明。

## 8. 审核重点与版本

首次独立全面审核重点：就绪与原生身份核验是否仍各司其职；创建至ExitRetention的句柄覆盖是否连续；自检同源路径/事件生命周期是否完整；冷stop是否保留失败结果；watcher截止及释放反例是否真正闭合且不误删挂起取证；全部provider实际接口、GUI静态/runtime分界、原合法配置输出兼容；ACC是否可机械执行并保留真实VM/100情景义务；是否错误继承旧审核/CHK资格。

v0：依据03/04/05首次建立统一方案。正文仅目标合同，无实施进度或checkbox。首次送审前作者按全文自评并核对实际范围与fingerprint；审核通过并引入前不得改产品代码。

v0.1：作者自评补充同源exit-capability的事件资源、就绪探针SPE退出观察，并复现FIX-006，仅布尔Spike准入的缺口纳入IMP-007/ACC-007；固定五类Spike命令/独立证据根和首次失败停止参数。未送审、未实施。

v0.2：作者自评回流调查FIX-007后明确自检有限资源owner、主管保留、stop/预停共同结束门和原始错误保留；仍未送审/实施。

v0.3：引入09首次全面审核的AUD-001，补齐三个两阶段构造交接与部分初始化收尾、主管统一门和ACC反例；MEC-001补齐FIX-007索引，MEC-002明确离线依赖和平台fixture路由。局部高风险生命周期责任切片提交定向复审，其他审计覆盖继承。

v0.4：实施期真实FTP启动测试及Docker/Wine依赖对照证明ThreadedFTPServer无fileno；按PLAN-CHANGE-UR-001仅改为server.socket.fileno，与原正常资源语义一致，无健康判据变化。
