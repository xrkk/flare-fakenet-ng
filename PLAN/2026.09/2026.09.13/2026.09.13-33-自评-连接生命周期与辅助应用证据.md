# 连接生命周期与辅助应用证据作者自评

会话角色: 作者/执行方

送审准入: 通过。只表示作者自评；v0.7未审核，不得实施。

方案06 v0.7 blob `f44f002e4ddce41030a6152cf22060bbe1e8ff63`；本地HEAD `7ec8eff106a722122b93b1a1d0e50a7b8d1599c6` / tree `3914d93f77a718d2c2c55edaf8dc8e86eebbb817`。当前本地worktree是对象，非已提交新方案。冻结清单 `Logs/fakenetng-mcp/unified-repair-20260913/7ec8eff-loop/con008-review-baseline.json` blob `2702d21aa2942bca985b8faf76415d5bf352164f`；实际v0.6→v0.7 diff为同证据根plan-con008.diff。来源30 CON-FIX-008、调查31、变更32及原方案§0全部引用。完整规范来源切片语义按稳定ID继承，30只覆盖其明确A/B/C/D。

审核请求：需要再次第三方审核，全面模式。32已证明规范性来源/连接身份核心数据流变化，新批次首轮全面；历史25+27用尽的v0.6批次不复开。历史09/11/12及25/26/27/28全部不可变并保留。主会话无权自称第三方或授予资格。

范围：统一方案全部IMP001—007/ACC001—007质量及其规范来源；本轮变化核心为IMP007独立观察、scenario_suite/scenario_fault_evidence/scenario_tcpip/sst_fault_evidence/scenario_probes和计划新增scenario_kernel_network及对应测试。直接依赖原始run.log/IPC、probe身份、pktmon原件、Kernel-Network ETL及转换链。其余产品实现、生命周期方案结论核对继承；业务协议实现、策略权限、公网授权、旧AUD/CHK、PLAN/FaknetNG.md及其他项目排除。核心和直接依赖为决策关键；GUI/协调/打包外围只辅助理解，原先63路径及补充清单仍提供全部历史范围。

自评覆盖：问题/来源双向追溯、根因机制、范围/顺序、正常/异常/边界、全部IMP/ACC、资源恢复、身份/时钟/丢失、fallback和无后序依赖均检查。机械预检con008-mechanical.json全true。TCP旧代明确Closed与下一新生已实证；左截断首代不授资格；辅助更早结束不能选晚timeout；curl内部连接不靠PID唯一假设；UDP两组独立原生Send元组、PID、时序、大小吻合，字节序由实际socket对照验证。tracerpt错误时区被明确排除，UTC来源固定。旧PCAPNo配置保留。没有增加产品/故障/网络权限，旧失败不改通过。

作者发现并修正：原提案没有固定完整多行TCP事件/额外迁移语法；31补齐并在2c/2d写明。UDP不能只凭bound/closed，7c固定独立provider发送事件、原始ETL及双转换各自用途。curl没有直接socket元组，7b规定预先DNS集合与精确PROCESS_FLOW+ETW唯一交集；歧义fail，非猜选。实际100执行者、NIC正负、完整故障action包含、异常和恢复不变。

当前实现仍为v0.6及原边界工具修补，pre-con008-tests.txt只证明现存回归，不宣称新增v0.7实现或实际场景通过。Astra/medium隔离审核、引入资格之后才实施新机制，主会话五类Spike通过后新Terra/xhigh执行85+15。
