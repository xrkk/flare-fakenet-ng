# FakeNet-NG统一问题修复首次审核处理与定向复审请求

会话角色: 作者/执行方

## 1. 身份与引入

实际创建日2026-09-13，首次审核处理。基线分支process-redirect-v23，commit `3cdec36784ed67cfd42149147e88cbb35cba3e50`，tree `09bef30eca4a979aecf5d14a792ff0f303658163`。本地worktree方案，产品无修改。

- 基准审计：同目录09-审核-FakeNet-NG统一问题修复，blob `c77e8355799ae3be5b18c306105fca10d79ec75e`，首次全面、存在不同意见，完整30项覆盖。
- 处理前06方案v0.2 blob `630df6bd1ccf9df403d7cfb8d8d6ef213ec32f87`；完整旧内容固化于 `Logs/fakenetng-mcp/act002-investigation-20260913/plan-v0.2-frozen.md`，hash-object相同。
- 处理后06方案v0.3 blob `5dc3007bcbadbf8efb9829147b65a0500adf92ac`，状态审核未通过, 不得实施。
- 实际diff：同Logs根`plan-v0.2-v0.3.diff`；唯一语义增加为IMP-001构造交接和ACC-001反例，其余为状态/版本及MEC两项执行索引。源语义、Pxx、总架构、故障合同和授权不变。
- 原两份repair-baseline JSON的82个路径再次全部匹配；方案、审计、调查原件身份一致。保护PLAN/FaknetNG.md和原有两日期目录；无业务写入、无VM新动作。

## 2. 意见处理

AUD-001：同意。主管在构造返回后才赋值不能覆盖构造内部独立诊断任务，以及ManagedProcess spawn后identity/observe失败。实际代码exit_retention.py __init__ except、managed.py __init__ except与主管两个赋值可直接证实。DiagnosticCall后台线程自持有Job不等于主管可查询/取消/阻断责任；不把它误写成底层引用立即消失。

最小修改：三个对象构造改为纯内存，主管/自检owner先保存对象，再执行initialize/verify；逐资源登记，部分字段缺失可清理，原目标Job和独立诊断Job分别核对，init失败不重试、不新采集、不提前关闭活资源。start、无marker stop、预停/替换共用完整责任门。ACC-001指定真实初始化及主管调用链、五种失败边界、原/清理错误与实际结束后的同owner收尾。具体资源表、调用式、协议缺字段行为已写在IMP-001；不是仅重命名owner。

MEC-001：同意，ACC-001标题与来源补齐FIX-007。
MEC-002：同意，明确同容器offline SDK依赖准备；GUI Windows盘符fixture由Windows-Python执行，Linux guard本机执行，不放宽产品校验。修复前Windows准备完整后166项通过，仅作回归基线，不授予ACC通过。

## 3. 作者从头复评

| 项目 | 复评结果与依据 |
| --- | --- |
| 问题/根因与来源 | 04/05/07及09反例一致，FIX-007局部同源构造责任补齐；无新问题定义/规范变化 |
| 范围与依赖 | 仅已有managed/supervisor/exit_retention/exit_capability与既有诊断取消接口；jobobject原原子Job不改合同；82路径无漂移 |
| 双向追溯/条目 | 7唯一IMP与7唯一ACC，FIX-007主IMP-001，受影响ACC-001/002/005；新增AUD关联不复制来源 |
| 正常/异常/边界 | 成功调用顺序不变；ready不是healthy；未取得与部分取得资源区分；未知identity不伪造、不发IPC；同owner收尾 |
| 旧路径清理 | 明确删除资源副作用构造快捷路径、提前close活资源及仅检查外层Job的门；其他IMP原清理目标不变 |
| 机制与方向 | 两阶段唯一选择，无异常携带或工厂等未决替代；无新owner框架/持久源/重试 |
| 可编码与ACC | 明确initialize/verify调用式、资源表、缺字段分支和五反例，平台执行前提已补；实际VM/100仍必需 |
| 兼容/资源/一致性 | 同run/seq、Job原子归属、原deadline/额度、锁/恢复顺序、失败outcome保留；审计指向的责任丢失有明确目标 |
| fallback | §7全部保留评定；无新增fallback，settle仅结束观察不新采集，未知事实仍失败 |
| 机械预检 | 方案身份/状态、7IMP/7ACC、82指纹及旧blob复核通过；正文不填实施进度 |

作者送审准入：通过。此为修改后作者自评，不代替第三方意见闭合；当前不得实施。

## 4. 定向复审请求（全字段）

需要再次第三方审核：是。作者选定模式：定向。触发依据为AUD-001处理明确改变核心启动/停服责任在并发诊断任务未结束时的交接与门禁，命中完整流程“核心数据流/并发一致性/恢复切换”；不是因文件数或意见数。局部影响可限定，未改变规范、总体架构、根因定义或Pxx图，属于同一批次的定向变化。

基准记录：09首次全面审计（上述blob）、本10处理记录（自身blob由派遣消息外置）；旧方案v0.2完整Logs副本及当前v0.3，实际diff文件可逐字恢复。

纳入：AUD-001（首次处理，尚未第三方确认闭合）；方案IMP-001新增三对象两阶段构造/资源表/部分初始化收尾/统一门，IMP-003相交的真实结束规则，ACC-001新增反例，ACC-002/005受影响的实际同候选生命周期证明义务，§2/4/6/7全局不变量；MEC-001/002仅机械核对。路径managed.py、supervisor.py、exit_retention.py、exit_capability.py，直接依赖jobobject.py、diagnostic_process.py、exit_native.py、exit_intent.py、service_stop.py及现有test_managed_ipc_faults/test_exit_retention/test_supervisor_units，新增test_managed_readiness契约；身份均继承两份基线指纹。

受影响覆盖：COV-004/008/012/014/017/028及COV-029/030机械项。全局不变量：原子Job/唯一主管、先接管责任再副作用、逐对象实际结束、原失败结果、原60秒/180秒/额度、无新采集/新持久源、全部恢复再stopped、授权和状态门。

排除并继承：其他已覆盖COV和FIX-001就绪因果证据、FIX-002冷stop语义、FIX-003静态规则、FIX-005截止策略、ACT-001接口、ACT-005文档、FIX-006Spike裁决合同及旧修复回归；这些没有语义diff，不重开其质量判断。IMP-004/005/006/007、ACC-003/004/006/007除MEC平台执行细化均继承。旧情景链AUD/CHK仍排除，不借本次闭合。

待回答：①三个构造边界是否在首资源取得前已交付主管责任，无旧旁路；②record/intent/worker/identity缺失及独立诊断Job仍活时，收尾是否可机械执行而不提前释放/误放行；③原始/清理错误、取消与原截止是否保持，无新采集和重试；④ACC新增真实调用链反例是否足以判定上述合同且保留ACC-002/005实机义务。

证据：09真实构造微反例、三个源码调用边界与DiagnosticCall自持有/ended语义、v0.2-v0.3实际diff、82指纹及原21调查原件。通过条件：AUD-001四项最小闭合条件全部可定位、无定向变化引入的新中高风险、受影响既有结论与全局不变量成立。第三方不决定之后是否再次审核；同AUD同根因复开按流程立即熔断，不允许第二次文字修补。

最后检查点：AUDIT定向任务包准备完成；实施仍暂停于首次审核引入后的复审门。恢复入口为状态JSON和本记录§4。
