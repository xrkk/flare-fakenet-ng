# 独立TCPIP连接证据决定

会话角色: 作者/执行方

稳定ID DEC-SST-CONNECTION-02 / CON-FIX-007。

用户已明确同意按 connection-evidence-proposal.md 更新合同及审核资格。该提案位于Logs/fakenetng-mcp/unified-repair-20260913/7ec8eff-loop/，本记录只承接其七项严格边界，不接受把ETW冒充包或按相邻时间关联。

本决定优先于旧情景方案v0.5 §4.1的packet_refs唯一形式，仅在统一修复方案ACC-005/007目标会话重叠判定切片生效。旧方案及旧审计原件不改写、不赋予通过状态。上游DEC-SST-START-01六条边界保持，正常出站正负包、NIC链、精确异常、恢复义务保持。

新增TCPIP ETW独立连接证据类型：原始ETL+派生文本字节引用；严格绑定probe PID/创建时间、单次原元组/TCB及同run PROCESS_FLOW；中继端点由原始accept与PROCESS_FLOW显式映射。对所有已绑定端点扫描最早disconnect/abort/shutdown/RST/close及probe EOF/error/close，采用最保守区间，不接受选择晚关闭、不接受跨重试拼接。资料不完整、歧义、丢事件、时钟不明均失败。真实包类型保留，ETW不得填packet_refs。

用户同时已替换失败停止边界：每轮首失败停止该轮后续派发，封存原件后主会话继续修复验证。Spike主会话执行修复；子智能体找错；实际100由gpt-5.6-terra/xhigh新智能体执行，主会话修复，未通过前置不运行100。
