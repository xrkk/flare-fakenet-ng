# 精确关闭与探针时钟实施验证

会话角色: 作者/执行方

方案v0.10作者自评52闭环，最终blob 207aef0aa98305304e39d424535f13b886879f87。STATUS_SUCCESS只在完整正PID句型识别为终止；先红测1失败4通过，再完整190测试+6subtests通过。实机原UTF16 fixture已固化，Spike08原件重放证明后代正确划分且不污染目标代，但case2出现下一独立计时问题，旧失败不追认。

证据spike08-case2-timing.json：ETW连接建立后约6.484ms关闭，probe case_close却比ETW建立早3.9988ms。只读同VM WinPS5对照spike08-clock-resolution.json，200样本仅2个DateTime.UtcNow值，对照原生精确UTC有200个，旧值落后1.2013—11.7761ms。此为取时分辨率，不是实际因果倒置，也不调整解析器放行边界。

实现修正scenario_probes.ps1：事件时间改用GetSystemTimePreciseAsFileTime，转换到既有DateTime ticks纪元；JSON utc和utc_ticks来自单次采样，QPC照旧；辅助TCP/curl/UDP发送边界及B3原生客户端事件统一使用该调用，调度截止不变。API失败无fallback，15.625ms不确定度和最早终止/故障重叠/无身份约束不变，旧证据不重写。Microsoft原始API文档https://learn.microsoft.com/en-us/windows/win32/api/sysinfoapi/nf-sysinfoapi-getsystemtimepreciseasfiletime 明确其提供UTC高精度取时且Win8起支持；未将精度宣称等同零误差。

实际WinPS5验证spike08-probe-clock-verified.json：修改后的真实Write-JsonLine函数50事件、50互异递增ticks，ScenarioProbeClient编译成功。初次验证仅命令行过长WinError206，未执行；压缩脚本后执行成功，不改被测源码。完整四文件190 passed,6subtests passed；不替代真实流量Spike。

11文件冻结spike09-implementation-baseline.json；新argv-09.json，主会话实机五类，实际100仍0。产品候选/部署不变。旧Spike08及62文件SHA清单保留。此计时实现修正属于IMP007已有同一时钟/真实时间戳义务，无合同或权限修改，不新增审核。
