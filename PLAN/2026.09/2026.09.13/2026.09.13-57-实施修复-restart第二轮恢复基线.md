# restart第二轮恢复基线修复

会话角色: 作者/执行方

sst006证据discovery100-02/results/scenario-sst-006.json：第二轮before采自第一轮healthy，包含FakeNet DNS/driver/managed/listeners，after为正常停止状态。finally无cleanup_errors，已停止无run/controller。根因在scenario_suite.py restart分支以restart前active快照给run02 five_sections_before，必然误判正常恢复。不是产品恢复故障。

按IMP007现有恢复验证义务修正执行器：保留active原件为pre-restart-context；由第一轮同run产品recovery_audit的恢复快照建立第二轮基线，先严格核对第一轮原基线/恢复一致，缺证据或差异仍拒绝。新before记录来源run_id和完整audit引用，不借健康状态，也不忽略DNS/driver/监听差异。产品不修改，合同不修改。

新增回归验证恢复基线、真实DNS变化仍失败、第一轮恢复异常拒绝。191 passed,6subtests passed。原件重放sst006-restart-baseline-replay.json：旧比较有差异、正确中间停止快照与最终停止快照差异为空；只是机制重放，旧sst006原失败不追认。11文件冻结restart-baseline-implementation.json。

派Terra先独立新根复跑sst006验证修复（重复不增加不同情景计数），随后执行未尝试98项；保留此前两条原失败，分别计不同情景覆盖/完整执行/重试结果，探索性报告不冒充正式ACC。用户要求等待子智能体返回，60分钟等待上限；运行环境单次等待限制时分段等待，不用主动状态轮询。
