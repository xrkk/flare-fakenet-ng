# Spike探针启动与证据转存

会话角色: 作者/执行方

来源统一方案IMP-007/ACC-005/007；用户持续修复授权。子智能体/root/runner_find_errors只读找错，主会话修改。范围仅验收器与scenario_probes.ps1。

原five-class-spike-01的policy_pause在pktmon启动后失败，无产品run；故障配置恢复通过。当前现场证明pktmon仍活而probe缺失，停止此owned捕获并登记ETL274948B/SHA b40e897e239900619e0d10e4046acacb92084e5131aa44c824599d516dcd1235。原件保留guest；capture-diagnosis/stopped.raw持久化身份。不能把无异常cleanup列表当实际资源结束。

确定根因与修复：
1. -HoldSeconds.Substring(1)删掉dash，Start-Process拼接空值/JSON边界不可靠；改encoded PowerShell哈希表参数，不由命令行拆解JSON/空字符串。
2. Get-Endpoint使用只读自动变量$Host，改EndpointHost。PS5顶层JSON数组不枚举加上Depth2导致边界用例嵌套/字符串化，改显式foreach枚举及Depth12。
3. capture函数成功返回后才登记，启动中失败无人清理；函数内捕获错误并结束自己的子进程/pktmon，保存错误/清理信息。stdout/stderr分开保存，pktmon原生stdout捕获避免污染JSON，ready匹配PID/creation_ticks。
4. 最小capture-smoke02探针写ready但外层调用超时，未找到VM MCP服务端实现，继承管道/父等待仅为推断。换Win32_Process.Create，子进程自行日志重定向；capture-smoke03真实启动及时返回、随后可独立stop，证明替代入口可行。未release取消诊断，不作为正常流量或Spike通过。
5. _stop_capture_and_probe顶层数组与_vm_json对象契约冲突，改{files:[...]}。Linux Path不能提取Windows basename，guest边界改PureWindowsPath。suite-root指纹加入guest attempt目录，避免不同本地批次覆盖同一guest原件。
6. cleanup_error/stop-window原先在primary gate释放前同步等待辅助case，双方循环等待；将其辅助case等待移动到既定stop-window release之后，保留primary phase，不提前冒充after-healthy。

离线67pass/6subtests。新round02代码/探针指纹runner-02.json，最小原件capture-smoke-03.json，新预检five-class-spike-02。改动只实现既定参数/运行相位/证据和清理契约，不放宽流量/故障判据；原失败不改判。
