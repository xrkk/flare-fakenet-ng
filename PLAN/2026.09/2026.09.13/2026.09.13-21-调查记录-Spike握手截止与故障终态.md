# Spike握手截止与故障终态

会话角色: 作者/执行方

来源统一方案IMP-007/ACC-005/007，持续修复授权。原five-class-spike-02首例run8eeca849-4a00-40fc-a6bd-2f5c02cfcbff已healthy；probe.jsonl前三边界case_close，第四wrong SNI停case_tls_handshake_attempt，90秒cases等待超时。SSL同步握手未设ReadTimeout/WriteTimeout，修为各5000ms，原case_error/close语义保留，不把超时当产品允许。故障模式精确恢复成功；本例未走到arm，不能当故障覆盖。完整宿主机原件spike02-failure-manifest.json。

子智能体只读确认receipt元数据相对attempt根但adapter按suite根，修file_record(metadata,self.root)。原故障终态在禁用模式重启之后读取会丢last_run_outcome，现重启前单独封存，恢复后的final_status仍另记；回归验证原failed与重启后None不会互相覆盖。转存器拒绝空stdout（0字节）导致收尾误失败，改允许0且必须匹配空内容SHA，不允许负数或超限。

离线三组68pass/6subtests；产品代码/包不改，探针与执行器指纹runner-03.json，新目录five-class-spike-03。全方案仍未完成，实际100仍0；失败停止当前派发后继续已授权修复循环。
