# 启动门预检失败与证据判定联调

会话角色: 作者/执行方

依据记录 16 的用户授权持续执行测试、修复、重派；不变更方案审核状态，不宣称 AUD/CHK 闭合。

## 已完成的主机修复

- `6b8119f`：B1 域名探针按原始 socket RemoteEndPoint 字段 `actual_dst` 与数值报文关联；旧数值 `dst` 格式兼容。18 项 oracle 测试和 6 个子测试通过。
- `10317c6`：child_hang 触发上界必须是本 run 正确父子链的唯一挂起子进程 CreationDate，或含该子进程的原生快照观察时间；更早 healthy start 响应不能充作上界。新增反例先失败，修复后 19 项 oracle 测试和 6 个子测试通过。

上述均为离线判定工具修复，产品候选仍为 `mcp-c082ea3fb-d958768dc8fa`，不是新的真实场景通过结果。套件子智能体已接收接口变化，并补齐 B3 原生连接关闭证据，避免把任意进程退出当作连接关闭。

## r3 真实预检失败

`terra_fault_spike` 启动并轮询宿主 session 64974，首类 listener_stop 失败，其余四类尚未运行。原始目录：`Logs/fakenetng-mcp/scenario-suite-20260912/spike-evidence-contract/attempt-gated-c082ea3fb-r3-20260912/`。run 为 `f7218fc1-4e9c-4dce-b7bf-efa1ce0b78ad`，nonce 为 `23c78329-ef5a-430f-95ec-74f9d2f75091`。

已读取 listener_stop.json：arm 为 VM UTC 14:54:30.1956393；observer deadline 为 14:54:41.6236853，结束 14:54:41.6345424，ready=false、established=null、process_flow=null。子智能体报告产品 gate readiness deadline exceeded，需由正在导出的 run 原件核对准确时间。

代码显示 observer 10 秒从派发 start 后开始，而产品 gate 的 10 秒从 instance.start() 完成后开始。当前根因假设为观察窗口先于真实启动门结束，尚待原件确认；不得未经验证就扩大产品 gate 或修改产品生命周期。另 supervisor.start 的失败路径会同步收集 incident 并执行 stop；测试器 45 秒 join 不是整个产品恢复路径的截止时间，客户端超时也不能证明原操作结束。

## 后续执行边界

VM 仍由 terra_fault_spike 串行持有。先导出失败原件、只读核对当前 operation，确认原操作结束后按既有授权恢复；保留 r3，在新尝试修正测试观察与对账流程后重跑。terra_scenario100 并行完善完整 100 场景执行器，须采用相同原操作对账规则，待交接后真实执行。当前正式 100 场景尚未开始，目标保持执行中。
