# 一般功能方案三方审核结果

- 会话角色: 第三方审核方
- 创建日期: 2026-09-12（Asia/Shanghai）。
- 审核性质: 再次审核关联材料核对；模式: 07 §4 熔断解除 Spike 材料审查。用户本次转交执行方回复及证据，不构成已作出的 D6 语义变更决定。
- 轮次: 07 熔断后的材料核对，不重开同批次全面/定向方案质量轮次，不以此规避审核次数上限。
- 结论: **审核循环熔断**。接受启动期同 run 网络活动这一有限实证；07 的完整解除条件尚未满足。
- 方案: `2026.09.12-01-功能方案-MCP情景压力测试套件-出站策略负向验收.md`，v0.3，`未审核, 不得实施`，blob `17dcc9336390bade9cea314c4b4283bb1a299fe1`，本次未变。
- repository: `/home/adminn/projects/flare-fakenet-ng`，origin `git@github.com:xrkk/flare-fakenet-ng.git`。
- 仓库参考 commit/tree: `aee128b25c99fcc1df188fcbeb833cff3446c66a` / `a118257c497f04262e5dcff8fe38ebdbaebadcfe`。方案与本目录记录仍为本地未提交对象，参考 tree 不承载方案；不声称冻结了远端已提交方案。
- 规范来源: 04 全文 D0—D10/确认区；本次决定切片 D1 健康/日志、D2 外部出站主干、D6 流量中注入、D7 出站策略主体、D10 授权节拍。方案 REQ-SST-09→IMP-SST-05/07→ACC-SST-07。其余 S1—S15 沿 07 §2 和 05 §2 稳定定位继承，仅为上下文，不宣称本次重做全面审核。
- 当前任务包: 未变方案、04/05/06/07 稳定链、下列代码与 Spike manifest 所列 27 文件。新证据改变对启动期可达性的认识，不自行改写需求或方案架构。

## 1. 历史链、范围和证据指纹

| 对象 | Git blob / 指纹 | 本次用途 |
| --- | --- | --- |
| 04 用户决策记录 | `59259c337c3ae7e09d74b8e3cd6a416cd01dfdb3` | 规范语义，尤其 D1/D2/D6/D7 |
| 05 首次有效全面审计 | `671fa35b416deb913c75ebd05cc8101112ba152e` | AUD-005 首次提出；其他意见不自动关闭 |
| 06 作者审核处理 | `499e8ea596f019b0075e1b8ec13b3d626446a98c` | AUD-005 首次处理、v0.3 窗口修改 |
| 07 熔断审计 | `17ea9c3115b13dea9ade836c73d7995eff7e3822` | §3 反例、§4 Spike 问题与通过边界 |
| `fakenet/mcp/faultinject.py` | `1722880f49bb2833e68b4e16b8ff6e6c575cee68` | clear:56—77、inject_listener_stop:187 起：receipt 与实际关闭顺序 |
| `fakenet/mcp/managed.py` | `62d62fa0adb06ab004faa731b90ceee6d0bb602f` | start 注入后 probe；stop 收敛 |
| `fakenet/mcp/supervisor.py` | `61e3bc1769c53af726e2ab08b4442760005f9c3c` | start 健康门及失败处置 |
| `AGENTS.md` | `8c6e68314e6aa1852ec295dad30453d58577bc7a` | Logs 证据落点、真实 Windows 与静态证据边界 |
| Spike `manifest.json` | `0ab8a8fa652ac17781416105fe7d0ab0ffd364a6` | 全部 27 个宿主文件逐一字节数/SHA-256 重算通过 |

上述决策关键路径加方案 §2、IMP05/07、ACC07，以及证据目录下 hammer.py、run.log、ipc-parent/child、fault-triggered、published、creation、spike-window，均实际读取核对。manifest 位于 `Logs/fakenetng-mcp/spike-listenerstop-20260912/`，表中证据相对路径均相对此目录。manifest 自身由上述 blob 冻结，其各条 SHA-256 构成逐文件内容指纹，不仅以目录名定位。

三类范围：核心为 AUD-005 的 Spike 解除条件及方案生命周期/验收对应片段；直接依赖为三份触发/健康代码、上述原始证据和 04 决策；排除为产品/方案修改、其他四类故障实际验证、其他 AUD 关闭、套件全面验收、VM 操作与部署。上下文路径为 README/timeline 执行方解释、vmps.sh 与 service-log-tail、其余 S1—S15；不把执行方总结作为规范来源或独立通过证据。

## 2. 实际使用的历史运行态基线及核验边界

本次只读取既有 Windows 运行产物，未连接或操作 VM。证据标称日期 2026-09-12，VM `DESKTOP-3FI41GR / 192.168.204.233`，候选 `mcp-c6090f816-93d4b3cf2252`；该候选身份是材料登记，未由本次安装包/进程映像哈希独立重验。不能据此宣称当前 VM 或服务仍保持该状态。

- run_id `d460f5c8-6085-4e01-a50c-20d11debb237`；nonce `1e47bc1c-836a-44ef-a60c-04e833f3e8d0`；controller `7c76ff96-d95e-423f-9faa-e0815c975600`。
- creation/IPC 关联 supervisor PID 8160、child PID 1952；运行配置为 B1 egress_control 模板替换外部 DNS。published 登记实际 active-config SHA-256 `4a61c7ad584a81ae28431ce2eb396ae695e1cfe35feb896e9d3afad1f6ce2ff1`；该文件未作为本地原文导出，未独立复算它。
- 原始 ETL 留存 VM 的哈希仅作为登记引用，本次未读取 VM ETL；实际读取本地 pktmon 文本及窗口、run.log、IPC。27/27 文件完整性通过不等于全部运行结论通过。
- IPC 证明本次 start 健康输入 probe=False，之后 stop 返回 stopped=True；不据此替代完整恢复审计、后续服务 Running/无残留、U-4 持续可用等结论。现有 vmps.sh 说明实现了会话初始化路径，不能仅凭脚本将 IMP03 的全部运行前置标成通过。

## 3. Spike 逐项核对结果

| 待回答问题 / 通过条件 | 结果和原始证据 |
| --- | --- |
| 文件是否可独立复核 | 27/27 清单字节数及 SHA-256 相符；published 的对应 run.log、IPC、receipt 哈希与实际导出吻合 |
| 是否确有同一 run 的网络活动 | 支持。run.log 在 19:29:14.681—14.973 的 RawTCPListener 十六进制内容含 hammer 标记；不是仅凭启动前宿主发包，也不是借另一 run 的流量 |
| 流量是否已证明目标出站策略主干 | 未证明。hammer.py 从宿主 connect 到 .233 的 80/443/1337/53/38926/38927；pktmon 首包 14.4164338 为 .1→.233:53 的 Rx SYN；run.log 后续 REINJECT_LOCAL 指向 .1。真实入站 listener 活动及回复不能自动等价于 D2/D7 的 VM 外部出站回源/目标策略主干证据 |
| receipt 是否是精确实际 trigger 时间 | 否。fault-triggered.json 只含 fault/nonce；clear() 先写 receipt，再删 arm 文件（共享冲突可能重试），inject_listener_stop 随后才 shutdown/close。timeline 的 15.0448725 mtime 是执行方登记的 receipt 时间，不能直接标作精确 socket 关闭时间 |
| 是否支持启动期活动与故障时间关联 | 支持有限结论。run.log 15.044 EGRESS_CONTROL_READY 后，15.055 出现 serve_forever 非 socket 异常；IPC 同刻 start 返回 probe=False；15.121—15.298 仍见同 run 的网络处理。结合 nonce/代码可支持启动期发生 listener 故障且前后存在活动，不因缺精确 mtime 就否认该关联。此处是代码与原始日志的关联推断，不是纳秒级 trigger 实测 |
| 是否兼容 v0.3 规定的相位 | 不兼容。IMP05 串行 W-start→W-traffic，§2 W-start 以 healthy 发布结束；本 run start 健康门失败，没有证据表明按该合同进入 W-traffic。外部 hammer 预先并发的试验是新的编排可行性信息，未实现原串行相位合同 |
| 是否兼容当前日志验收 | 不兼容。run.log 15.055 的 `Unhandled exception in managed thread`/Traceback 是实际产物；IMP07/ACC07 只明确豁免 W-stop cleanup_error 的精确预期异常，其余异常 fail。不能审核方自行加入 listener_stop 白名单 |
| 是否因此完成 07 §4 完整通过条件 | 否。已证明较窄的启动期网络活动；目标策略流量与既定健康/日志窗口相容性仍不成立，不能仅凭五时间表宣布 Spike 整体通过 |

07 的反例明确限定“按当前串行顺序”。本次证据纠正任何将该反例外推为“启动期同 run 绝无网络活动”的理解；07 原文的串行相位反例并未被推翻。D6 单独的文字没有明确要求 healthy 中段，审核方不把 healthy 中段擅自新增为 D6；必须同时处理的是已生效 D1 与方案窗口，而非只二选一解释 D6。

## 4. AUD-005 处置与唯一上游决定

- 稳定意见: AUD-005；首次提出 05，首次处理 06，同根因复开 07；本次不编新 AUD、不进行第二次文字修补核准。
- 来源层级: 04 D1/D2/D6/D7；REQ-SST-09；IMP05/07、ACC07；触发代码及原始运行证据。
- 发现来源分类: 本次 Spike 新运行证据细化既有 AUD 的可达性与时间观测，不是本轮方案变更新增意见，不是前轮已通过项的覆盖遗漏。
- 根因/风险: 方案抽象错误，高风险。arm、消费 receipt、实际关闭、流量类别和相位不能互换。仅证明入站启动活动就计 15/15 会使目标验收名义通过；直接放宽日志会改变 D1。
- 反例: 本次 run 真有启动活动和故障，却 start probe=False、出现非允许的异常，且流量发起方向为宿主入站，按现 ACC07 仍不能通过。
- 受影响既有项: 07 已列 IMP05/07、ACC05/07、健康日志与故障覆盖；这些此前没有有效关闭，不使其他通过项失效。AUD-001 来源闭合继续继承；AUD-002/003/004/006/007/008/009/010 不在本次核对中关闭。
- 最小闭合条件及所需证据: 先形成用户确认的统一 D1/D2/D6 验收语义，再由作者使故障相位、目标策略流量、实际触发观测、可因果关联的预期异常及恢复义务与该决定一致；须保留原始证据和失败边界。本记录不预判引入后的审核必要性或模式。

**唯一上游决定：请用户明确是否批准将启动型故障的有效注入窗口纳入 starting，并允许与该故障因果关联的启动失败及精确预期异常，同时明确故障重叠区间须承载什么目标策略流量、是否准许宿主入站流量承担这一义务。** 这是对 D1/D2/D6 联合验收合同的决定；审核方没有权限代签。若保持现有合同，现机制与串行方案的不相容性就是需要交上游的限制。不得将作者“若如此解释则通过”的条件句当成用户已批准。

本次不要求重复同一个 hammer Spike，也不要求仅为证明同一入站重叠再修文字；现有证据应保留作为上游取舍依据。审核循环熔断继续有效。

## 5. 八项汇总、fallback 与继承边界

| 项目 | 本次结论 |
| --- | --- |
| 需求覆盖 | D6 的较窄时序可达性有新增实证；D1/D2/D6 联合合同尚未闭合 |
| 双向追溯 | REQ09→IMP05/07→ACC07 已定位；Spike 流量与 D2/D7 目标的语义落点不足 |
| 功能闭环 | 启动活动、故障、启动失败、停止有证据；无按方案进入 healthy 流量相位的闭环 |
| 可交接实施性 | v0.3 不变，执行者仍需上游裁定，不能直接据此编成合格的 15 个故障情景 |
| 决策闭合 | 未闭合；上游联合验收语义待确认 |
| 验收契约 | receipt 时间不等于精确触发；现健康与日志验收不容纳此次轨迹 |
| 条目 ID 完整性 | 沿用 AUD-005，不新编号；不重做全方案机械扫描，不宣称全部 ID 通过 |
| fallback | 逐项如下；本次材料不产生新的整体通过项 |

| fallback | 本次评定 |
| --- | --- |
| F1 会话中断对账 | vmps.sh 有初始化恢复路径，但重连成功的可重复运行证明及对账门未在本次完整验证；继承 07 未完成复验状态 |
| F2 preflight 失败退出 | 继续保留；入站 hammer 不替代 P7 外部正向中继真门，不能据此解除整体前置门 |
| F3 fail 归档与继续门 | IPC stopped=True 是局部停止证据，不替代五节恢复审计、锁/树/残留检查，未取得整体通过结论 |
| F4 证据不可关联则 FAIL | 本次 27 文件哈希通过；时间精度/流量语义仍须按各自证据边界裁决，不得用文件完整替代目标关联 |

未扩展到其他模块，无新实施/测试要求；本轮不运行产品测试，不进行环境恢复或端口操作。材料核验所用只读哈希/解析不等于重新执行 Spike。

## 6. 封存与交回

结论: **审核循环熔断**。本记录是熔断材料审查，不是新的完整全面审计或方案通过意见。只新建本文件，创建前重查当日全部 Markdown 序号并独占创建，创建后计算 blob；方案和既有记录不修改、不暂存、不提交。材料交回作者/执行方引入；审核方不决定引入后是否再次审核或其模式。
