# PLAN-CHANGE-008：验收矩阵重构

会话角色: 作者/执行方。创建日期: 2026-09-12。

执行授权已生效：04 D3/D4 已明确 normal-5 / fault-5 后执行100情景并以变更记录转移义务；最新用户指令“解除子智能体测试场景的限制, 派发子智能体测试后修复发现的错误, 然后再次派发, 至所有场景无错通过”授权实施与持续测试。16记录保存该指令。本记录不是第三方审核批准或CHK关闭，不修改任何历史规范原文。

## 1. 旧→新数字与覆盖落点

| 对象 | 旧口径 | 新口径 | 验收落点 |
|---|---|---|---|
| 正常发布矩阵 | normal 100：builtin50+custom50 | normal 5：builtin3+custom2，仅冒烟 | ACC-SST-02；每轮原逐字断言不减 |
| 故障发布矩阵 | fault 50：五类各10 | fault 5：五类各1，仅冒烟 | ACC-SST-02；每轮原逐字断言不减 |
| 广度与组合覆盖义务 | 原正常/故障固定矩阵承担 | 转入100情景：85良性+15故障（五类×3画像） | ACC-SST-04/05/06/07/08/09 |

固定历史来源 S2 blob `ec4f025a5926be82aa1728eff10dfef39d8a827a`（总体验收§4正常100、故障五类×10）；S3 blob `d8987f5be98269ee8465f3696f9a02bf39088106`（独立核验§94—97正常100份、故障50份）。历史policy_pause终态描述不再覆盖现行 S15 P05§3：五类故障均须最终真实stopped，且last_run_outcome=failed。

## 2. 非数字义务转移，不能用缩量删除

| 义务 | 冒烟保留 | 套件承接 |
|---|---|---|
| 五节恢复审计（DNS/路由/监听/WinDivert与进程/服务） | 每轮原validate_round/审计 | ACC-SST-05/07，每个实际run |
| 配置锁持有与释放、最终受管树清空 | 每轮保留 | ACC-SST-05/07/08 |
| 连续探活与正常流量窗健康、异常严格扫描 | 每轮保留 | ACC-SST-05/07，不接受缺样本 |
| 同候选、VM连续性及轮次身份 | 禁止混入其他源码/包/VM或旧全量轮 | ACC-SST-08/09，同一最终候选完整100，候选变化后重跑 |
| 故障实际作用、目标会话重叠、精确归因及incident | 原P05故障逐轮义务保留 | ACC-SST-07，五类各3；仅receipt或进程存活不够 |
| 真实出站策略边界及独立包证据 | 冒烟不冒充该覆盖 | ACC-SST-05/06/07 |
| 15接口各≥5情景、每情景distinct≥10、配置桶分布 | 不由固定矩阵替代 | ACC-SST-04/09，从实际interface_calls重算 |

## 3. 六数保真重算

六数的计数单位为当前规范正式条目、有效决定与整改影响切片，不是简单的测试轮数。依次记录 `应承接 / 完整 / 未落点 / 语义弱化 / 不可验收 / 失效或复核条件缺失`。同一稳定ID在其计数单位内只计一次；旧数字条目与本变更映射并读，列出历史数字为何不再作为当前轮数门槛。未完成实现、未跑场景或缺证必须进入相应未完整/异常清单，不得预填后四数为0。最终实际100情景、接口/桶覆盖、恢复和证据链全部完成才可据证据更新作者承接状态；独立核验方自行重算并裁定CHK。

## 4. 机器可核验合同

```json
{
  "schema": "sst.plan-change-008.v1",
  "old": {"normal-builtin": 50, "normal-custom": 50, "fault-per-class": 10, "fault-classes": 5},
  "new": {"normal-builtin": 3, "normal-custom": 2, "fault-per-class": 1, "fault-classes": 5},
  "suite": {"total": 100, "benign": 85, "fault": 15, "fault-per-class": 3, "tools": 15, "tools-per-scenario-min": 10, "scenarios-per-tool-min": 5},
  "obligations": {"five_sections": ["ACC-SST-05", "ACC-SST-07"], "lock_tree": ["ACC-SST-05", "ACC-SST-07"], "continuous_probe": ["ACC-SST-05", "ACC-SST-07"], "same_candidate_continuity": ["ACC-SST-08", "ACC-SST-09"], "traffic": ["ACC-SST-05", "ACC-SST-06", "ACC-SST-07"], "fault_incident": ["ACC-SST-07"], "actual_coverage": ["ACC-SST-04", "ACC-SST-09"]},
  "six_counts": ["应承接", "完整", "未落点", "语义弱化", "不可验收", "失效或复核条件缺失"],
  "sources": {"S2": "ec4f025a5926be82aa1728eff10dfef39d8a827a", "S3": "d8987f5be98269ee8465f3696f9a02bf39088106"}
}
```

机器输入matrix-manifest.json固定candidate/source/package_sha256/evidence_root/planned；首次缩量前创建且不可覆盖，summary/final按planned复算实际记录。无新参数且无matrix-manifest保持旧全量50/50/10。缺轮、重复run、错误身份、混入超计划轮数均失败。缩量summary仅汇总normal/fault/final冒烟，不宣称未执行的全项目ACC通过；100情景另由套件summary报告。

## 5. 审批栏与核验阅读指引

- 用户执行授权: 已登记（04 D3/D4与16最新持续目标）；没有将用户对持续执行的授权转录为其逐字确认本文件全文。
- 第三方审核结论: 本作者记录不生成、不代签。
- 独立核验/CHK状态: 不修改，不自关；待实际证据齐全后仅提交READY_TO_REVERIFY及材料。
- H8阅读顺序: 固定S2/S3原文 → S15现行逐轮判据 → 本记录数字映射与非数字义务 → 最终候选matrix-manifest/10轮冒烟原记录 → 100情景实际记录/覆盖/完整性报告 → 六数与异常清单。不能只读本记录把待测项当作完成。
