# r3 恢复与探针时钟校验

会话角色: 作者/执行方

继续按记录 16 授权执行；本文不构成第三方审核或最终验收。

## 真实失败定位与恢复

原始 IPC、creation 和 probe 已证明：r3 observer 于 UTC 14:54:41.6345424 结束，managed 创建于 14:54:53.195581，首连接建立于 14:54:54.782815。观察器在 managed 创建前约 11.56 秒已退出。派生报告及输入哈希为 `Logs/fakenetng-mcp/scenario-suite-20260912/iteration-20260912-01/r3-window-analysis.json`。修正的是测试观察期，产品故障 gate 保持 10 秒。

读取 r3 原目录 `r3-failure-recovery.json`：VM 23:04:36 (+08) 时 config_bytes_restored/environment_restored=true，needs_recovery=false，managed/probe/fault_controls 均空，pktmon 停止；SCM Status=4（Running），MCP lifecycle=stopped、run_id/controller=null。失败 ETL、解码文本和 incident 清单均登记路径、大小、哈希，旧失败目录继续保留，不计通过。

## 时钟校验修复

提交 `eee11da` 修复离线 oracle 仅扫描 `*-probe.jsonl` 的文件名依赖。完整套件使用 `probe.jsonl`，原规则会遗漏 UTC 与 Stopwatch 的一致性核对；现按 session.established_ref.path 定位原件，同时兼容旧文件命名。新增 1 秒漂移反例先暴露误通过；修复后该反例失败，正常单调时钟正例通过。

命令 `.venv-mcp-runners/bin/python -m pytest -q test/mcp/test_sst_fault_evidence.py`：20 passed，6 subtests passed。只验证主机工具，不替代 VM 测试。产品候选未改变。

## 当前执行

terra_fault_spike 继续在 r4 新目录执行修复后预检；terra_scenario100 完成全场景证据、真实时序调度、缺失/重复/损坏结果拒绝与原件重裁决。正式 100 场景尚未运行，目标继续执行，不能声明全部通过。
