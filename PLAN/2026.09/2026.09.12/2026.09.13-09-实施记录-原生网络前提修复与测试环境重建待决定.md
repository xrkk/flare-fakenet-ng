# 原生网络前提修复、完整备份与测试环境重建待决定

会话角色: 作者/执行方

承接记录08。本记录为作者执行证据及待批准操作包，不是第三方审核，不修改方案状态，不闭合AUD/CHK。正式100场景仍未开始；实际执行继续专属于gpt-5.6-terra(xhigh)子智能体terra_scenario100。

## 1. 已确认根因边界

c208失败窗口的System e1i65x64事件：2026-09-13 01:34:29.224(+08)链路断开，01:34:34.738恢复；Diverter在01:34:34.554报No active ethernet，位于断链区间。指定VM的vmware.log在UTC17:34:24.187及17:34:29.488也记录MACVNetLinkStateEventHandler up0/adapter0→up1/adapter1与定时恢复。证实真实VMware VNET/guest链路变化，不是GetAdaptersAddresses假阴性；没有证据确定具体宿主操作或P7 DNS调用是触发原因，不能把NetBT同名事件当因果。

原件根 Logs/fakenetng-mcp/mcp-c7cd934db-b657a031d1a0-interrupted-p05-export-20260913。network-events-window-r2.json保留原始Time/Id/Provider/Message/XML，vmware-log-exact-1734z.raw及窗口日志保留宿主证据。VMX只读确认NAT/e1000e/MAC00-0C-29-C1-CA-49；无显式vnet/startConnected/linkStatePropagation字段，不能推断其默认值。最近无修改两分钟窗口02:00:02–02:02:02(+08)未再见link事件，当前原生Ethernet0 IfType6/OperStatus1/IPv4 .233正常。未修改VM网络或其它VM。

## 2. 已完成预防修复及候选

提交51d8315f45afcace5faec5af8ea17f8ea56067e5把既有Windows Diverter前提（ETH UP、非零IPv4）移到baseline save之前，使用固定diagnostic worker/Job与30秒deadline，持久化pre-start-native-network.json。拒绝时释放本轮endpoint observation和配置锁，不写baseline/needs_recovery marker、不创建managed Job。DivertTraffic!=Yes保持跳过；不要求默认路由，不从route文本推断；五节恢复audit及已有marker语义不变。原生API返回码/接口字段新增观测；ctypes字段在生成器有效期内转换为纯Python数据。

Linux相关44通过2跳过；Wine聚焦7通过。构建session87604退出0，完整Windows主组1104总/8跳过/0失败，HTTP17/0失败，即1113通过8跳过。

新候选mcp-c51d8315f-25e54353e099：dist/mcp-network-readiness-20260913/fakenetng-mcp-candidate.zip，SHA256 25e54353e099591b7d3942151f7e9b637a79fc3e92c8a3762fdd5e6ca6d7a50d；195文件manifest/size/hash验证PASS，主会话独立复核ZIP一致。尚未部署；预检查无法保证检查后的链路永不变化，真实100仍须证明结果，不能用本修复改判旧失败。

## 3. 已完成不可覆盖备份

备份根 Logs/fakenetng-mcp/mcp-c7cd934db-b657a031d1a0-reinit-backup-20260913-PpvQy3。

fakenetng-mcp-reinitialization-backup.zip为322,540,286B，SHA256 011f212dd92aa97df2a75ab09b5b8bf7b360764dd75e8a1672f5c9c212e3f3a2。通过host-only单次PUT回传，guest/接收/落盘哈希一致，临时监听服务已停止。guest-archive-validation-r2.json对3351/3351 payload、2,410,102,292B逐字节哈希验证通过；3353 ZIP成员包含inventory/copy元数据，读取/复制/重解析错误均0。3339成员保留Windows反斜杠，恢复必须按embedded inventory原始相对路径映射；首轮规范化文件名校验失败原件保留，不重写。

范围为本服务ProgramData全树、当前ProgramFiles包和已知c72包备份、427目录ACL、4关键ACL及9条SCM/注册/防火墙原始命令。控制面验证backup-control-plane-validation.json SHA ec20aba8ed8c033b00a52964ca03983b9392f08985478d11a8ae38c62d162f05，passed=true。保留服务Environment类型/原值、IFEO/SPE注册原值及logs/exit-evidence/registration.json所有权链。未扩展到其它服务/VM数据。

主会话独立验证root-critical-backup-verification.json：c208 baseline SHA5ccbdf959c6209aba7246c56011d2e83d0fb1827e76905ca42aac57eeca55f73、state.json SHAb863ca2f69955265884563816dd99555ccd207170c51798f33ea80a9e8a61256、registration.json SHA6af3bd171852e86db870f7bf3cde76b523874eeb21b2a777346d3102e571fd1a均与embedded inventory一致。

## 4. 当前现场与正常门禁

reinit-live-state-r2-terminal.json SHA6351c4fd7cded3e3a80619ed0d989952c57b45c26f3f28400b18f9964e303c4d。唯一相关进程为SCM服务宿主PID5712、原生FILETIME134337076522216716，路径为当前FakeNet-NG-MCP/fakenetng-mcp.exe，LocalSystem，SCM Running。没有相关managed/exit-helper进程、FakeNet ETW observer、WinDivert驱动/module user，相关端口仅服务TCP、UDP空。IsProcessInJob(NULL)=false只说明该进程不是Job成员，不能枚举其持有的Job，保留该观测限制；c208原始退出证据已证明managed及helper退出完整。

state-live-backup-comparison.json SHA534a7fad56c22aaacdf6b7f7990a365a68b7903486518de91cb96b8362f90ca1，当前450B marker与备份哈希一致：c208/version13/needs_recovery=true，基线与产物原目录仍在。首个live脚本路径错误导致state_raw=null的原件保留，r2明确按paths.py路径读取并完成比较。

RealSupervisor.stop必须full_audit_diff无差异后才clear_recovery；recover回到同一路径。service_stop.py拒绝recovery responsibility remains及service exited with recovery responsibility。旧baseline真实记录断链状态，当前网络已恢复；正常stop/recover不能合法清除这份责任。修改旧baseline或假装恢复成功均不允许；现有正常重试授权没有提供强制放弃恢复责任的命令。

## 5. 待用户明确批准的最小测试环境重建

本节尚未执行。只针对已授权VM192.168.204.233 / DESKTOP-3FI41GR / MAC00-0C-29-C1-CA-49上的本测试服务，不重置VM，不改网卡，不触及其它VM/服务。

1. 执行前再次核对已备份状态哈希及唯一宿主PID/原生创建时间、无在途产品操作与相关managed/helper/observer/driver；若身份或状态改变则停止并补证。不能沿用过期PID直接结束进程。
2. 记录并临时抑制本服务自动恢复/自动启动，防止切换目录时SCM自行重启；按原始注册类型和值保留可恢复副本。此变化属于本次需批准的操作，备份阶段未执行。
3. 在身份精确匹配后强制结束当前测试服务宿主，证实其退出、28788释放且相关子进程/驱动/observer无残留。该步骤是正常controlled stop门禁之外的测试环境重建，不写成功恢复记录。
4. 将现有state和baselines目录整体移动到同卷唯一留存目录，保留内容/ACL/哈希和原路径映射，不删除或改写旧marker/baseline。configs、logs、artifacts（尤其exit-evidence/registration.json）原地保留，维持原IFEO/SPE所有权链。新建本服务空运行态目录并按既有安装契约设置ACL。
5. 保留当前程序目录为独立备份，部署上述已验证c51候选，复核195清单、ZIP/EXE与配置哈希；恢复SCM原设置后启动，确认新服务stopped、无run/controller且无遗留恢复责任。这里是新的测试环境，旧c208仍为failed，不能计为旧run恢复或通过。
6. 由同一指定gpt-5.6-terra(xhigh)子智能体在全新证据根执行P1–P7、P05正常5/故障5及final/summary，然后85+15正式100场景。新中高风险失败继续保留并修复重测，不混用旧候选结果。

需要批准的唯一决定是：是否允许执行上述明确保全旧失败现场的测试服务环境重建。若未批准，保持当前VM原状；可用正常受支持命令继续的恢复路径已不存在，不能擅自清除marker。备份、修复、构建和记录均已完成，可供决定；审核必要性/模式及AUD/CHK仍不由本会话裁定。
