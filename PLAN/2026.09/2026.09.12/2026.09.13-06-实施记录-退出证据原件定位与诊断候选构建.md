# 退出证据原件定位与诊断候选构建

会话角色: 作者/执行方

承接记录05及记录16授权。此为实施记录，不修改审核状态，不闭合AUD/CHK。

VM恢复补证：faultmode-recovery-20260913/post-restore-process-proof-v3.json SHA256 4076bf91dad02b56dbb85d046d7496765a5b57a63ed5d08834245dbccd4399bf，post-restore-process-validation-v4.json SHA256 63df16f4762204f90a83dc6e158b15c55847b1c8aedd41a5a51e8cfc60feb760。完整前缀 Logs/fakenetng-mcp/scenario-suite-20260912/mcp-c72bd090d-54b9dfaf3db7-。CIM证实唯一FakeNet进程为SCM对应服务宿主PID1772，stop_grace_seconds=60、needs_recovery=false。此前只检查PowerShell可执行文件Path的证据不足，现以命令行和进程身份补充；原件不覆盖。

terra_scenario100只读导出真实VM的logs/exit-evidence/377e5c44-46e4-4e99-b7e7-221ab211375f，独立目录同上述前缀exit-evidence-export-20260913。entry.json SHA e9bf73ee3f6a176855c38c14d41738dafc8f936cd2201b3a9ac197cd3c7aff49，result.json SHA 6fc5c27ec8353e7a733fea01af26be7c519d9d56523b854dc03caf0470e05f4f，owner-result.json SHA 8d19a8127a893b8fe19c422f869642c83b501bd87060ea47d68f887aa8e7ae85，dump-tool.json SHA 28b7bdf5ee5cfdc3d7deba160fa899e87043a2c031c356e2bea46c4273e7c7fe。目录及传输校验在export-manifest.json。owner-acquired/owner-dump/normal-claim/normal-ack/stop-intent均不存在。

原始result显示self_exit，但helper在60秒内未收到owner固定身份的ACK，complete=false，没有已生成dump；owner-result为裸WinError87。因而排除“已验证dump在后续清理中被覆盖”作为本轮主因。旧原件没有失败API，不能区分OpenProcess因处理过迟失败与Job admission失败。

提交7cd934d仅补充ExitRetention的entry读取次数/错误次数、entry观测时间、helper创建/期限、ACK尝试/完成时间及失败stage/native_api。ManagedJob.adopt_notification新增可选观察回调，原API、Job责任、DiagnosticOwner有界IO、退出完整性不变。没有采用尚无证据支持的supervisor直接文件IO替代方案。另修复incident_task在无存活managed dump目标时仍请求dump的问题：post-Job缓存栈不授权无目标dump，真实managed-exit incomplete仍失败，不豁免。红回归验证模拟AssignProcessToJobObject错误87被记录且仍complete=false，无ACK。

相关Linux测试125通过/1跳过；Wine新回归通过，既有test_diagnostic_ownership::test_windows_fixed_task_uses_job_and_returns_bounded_result在子进程导入win32file失败，待核查，不声称Windows门通过。镜像ID复核为sha256:1b33b518a7c71ecd13264f0d1c41faf254e301b8702e2da8ca11376cff700a5e。

构建命令 ./Build-FakeNetNgMcpPackage.sh 7cd934d dist dist/mcp-exit-admission-diagnostic-20260913，session34645，截止创建仍运行；输出目录独立，未覆盖c72失败包。只有构建与验证通过后，才由指定gpt-5.6-terra(xhigh)实机重新测试。当前实际100场景仍未开始。

并行的scenario清理证据修复尚未提交：现有CIM微秒创建时间与探针原生100ns时间不可直接相等比较，正改为相同原生时间来源，并准备短寿命无网络子进程验证活体拒绝/退出后通过。此修复不属于7cd934d冻结快照，不能把空列表查询当正式场景通过。
