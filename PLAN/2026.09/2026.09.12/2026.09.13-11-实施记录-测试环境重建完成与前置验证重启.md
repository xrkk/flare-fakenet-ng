# 测试服务环境重建完成与新候选前置验证

会话角色: 作者/执行方

- 创建日期：2026.09.13；本记录为实施证据，不是第三方审核或 ACC 总体验收。
- 授权：用户「批准」，承接当日记录 09 §5 与记录 10；方案状态不变。
- 源提交：`51d8315f45afcace5faec5af8ea17f8ea56067e5`。
- 候选：`mcp-c51d8315f-25e54353e099`；ZIP SHA-256 `25e54353e099591b7d3942151f7e9b637a79fc3e92c8a3762fdd5e6ca6d7a50d`。

## 1. 实际执行及失败保留

由指定 gpt-5.6-terra（xhigh）子智能体 terra_scenario100 独占 VM 执行。目标仍为 DESKTOP-3FI41GR / 192.168.204.233；没有执行 VM 快照复位、NIC 修改或其他机器操作。

第一次尝试因 PowerShell 向 sc.exe 传递空 actions 参数失败，在终止旧 host 前退出，SCM 原设置已恢复。真实 cmd /d /s /c 空参数往返试验验证后，第二次复用已校验的候选暂存目录，但遗留 Expand-Archive 对既存目录报错，在 SCM 修改前退出。两次失败原件均保留。第三次移除重复展开操作后，于 VM 时间 2026-09-12T23:11:19Z 前完成重建，退出码 0；传输监听已停止。

## 2. 已核验结果

根会话已将导出的九份成功阶段文件逐份对照 guest 清单核验长度和 SHA-256，并读取重建脚本的执行与失败恢复路径。

- 对旧 host PID 5712 重新核对原生创建时间后终止；退出后验证无产品子进程、28788 监听、WinDivert 驱动及 FakeNet 观察器。
- 旧 state（1 文件）和 baselines（47 文件）整体移动至 `C:\ProgramData\FakeNet-NG-MCP-preserved-runtime-c2081505-7794-442f-961e-a8560126d5e4-reinit-c51-1fbbebcfe7594be697c751a6efc0728a`。脚本比较移动前后内容树与根 ACL；分别为 `adc77aa6adb5b0e8036c9bafc3543ac9ea70b7981fbc9eaee37250d27320fc5b` 和 `2c706dec808f8ea1e3eeb8a3bee334197e94f59f85834e0f2da24ced5f41443e`。新运行目录继承原 ACL。旧 c208 失败事实不变。
- 原程序目录 212 文件整体保留于 `C:\Program Files\FakeNet-NG-MCP-backup-c7cd934db-20260913-reinit-c51-1fbbebcfe7594be697c751a6efc0728a`；新包 195 成员逐项大小/hash 核验通过，exe SHA-256 `0af98309d5838173c596a8211bd1134283ecb73cd6e5a526d0d06e8674961e6b`。
- SCM 已恢复 Auto、LocalSystem、原 ImagePath、单次 restart/30000ms、reset 0；DelayedAutoStart 和 FailureActionsOnNonCrashFailures 保持原始不存在，原始 FailureActions 二进制一致。
- 新服务 Running，PID 4904，原生创建 FILETIME `134337282736339898`。产品 MCP 返回 stopped/version 1、run_id/controller 均 null、无旧 recovery marker，未留下产品子进程、驱动和观察器。
- configs/logs/artifacts 与原位 exit-evidence 注册链未纳入移动操作；关键原件补充只读 hash 核对由子智能体随前置证据固化。

## 3. 证据身份及后续执行

- 成功原件目录：`Logs/fakenetng-mcp/mcp-c51d8315f-25e54353e099-reinitialization-attempt03-reuse-stage-fixed-20260913-Q8fNw4`。
- `approved-reinitialization-terminal.json` SHA-256 `e392de856024f27bb245f57aec474af40a0c8af70d262b9709c97c722544ce6d`。
- `rebuild-terminal-readonly.json` SHA-256 `98c3b26e731a7b284e3988decc8f25dba86f9580daa2315cb96c8e24bbec82dc`。
- 完整重建前备份仍见记录 09：ZIP SHA-256 `011f212dd92aa97df2a75ab09b5b8bf7b360764dd75e8a1672f5c9c212e3f3a2`。
- 新执行根：`Logs/fakenetng-mcp/scenario-suite-20260912/mcp-c51d8315f-25e54353e099-full-20260913-R6pKz9`；已生成 85 benign + 15 fault 清单并启动 P1–P7。

本记录冻结时实际 100 场景执行数仍为 0。后续顺序为 P1–P7、P05 正常 5 轮/故障 5 轮及 final/summary，然后由同一 terra 子智能体执行完整 100 场景；失败继续保留原始证据、修复与重新派发，不把前置验证当作实际 100 场景通过。
