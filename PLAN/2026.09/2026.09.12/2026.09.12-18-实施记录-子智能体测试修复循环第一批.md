# 子智能体测试修复循环：第一批

会话角色: 作者/执行方。日期: 2026-09-12。授权及完成目标见16记录；数字与非数字义务见17 PLAN-CHANGE-008。本记录是进行中的执行快照，不是审核结论或总体验收完成。上一目标轮分类为**进展**：新增实现、提交、构建、子智能体真实测试及失败证据均已发生。

## 1. 已完成的修复与构建

- 产品源commit `082ea3fb74c6b708db6c09e944aa7cc9b54f78c6`，tree `3646d3bb8b064fad9ff3ef9e4726c4f204c1d22a`。仅fault模式且显式gate文件存在时，start完成引擎启动后等匹配fault/nonce/run就绪（最长10秒），未满足即失败；无gate时原流程不变。同步只调度动作，不当作流量/成功证据。
- diverter_stop新增 `fault-action.json` 原始 `GetHandleInformation` 关闭前/后返回码、handle、错误码、run/pid/nonce与VM时间；不把receipt或probe=false当关闭成功。
- 最小gate回归先失败、修复后7 passed/1 skipped（Linux）；Docker/Wine Windows main1018 passed/8 skipped，HTTP17 passed。native开/关句柄测试在Windows Python构建门通过；仍需真VM WinDivert证据。
- 新候选 `mcp-c082ea3fb-d958768dc8fa`，ZIP `dist/mcp-fault-rendezvous-20260912/fakenetng-mcp-candidate.zip`，SHA-256 `d958768dc8fa7c64377646d40d7933ee08682d4b5121ec64feacd591a87e9e22`；冻结exe smoke和195成员哈希验证通过。构建日志 `Logs/fakenetng-mcp/scenario-suite-20260912/iteration-20260912-01/build.log`。

## 2. 宿主工具与文档

- 工具commit `90b11c3ac5bda4ffe87fd42b40d3500295c4cdf9`：P05显式rounds-limit与不可覆盖matrix-manifest，旧默认计数不变，拒绝其他候选/旧全量/超计划轮；reduced summary只声明normal/fault/final冒烟，不冒充全项目ACC。
- sst_fault_evidence新增原生句柄关闭适配、独立expected-candidate、原生CreationDate精度支持与精确policy源码映射；38项相关宿主测试通过。新候选仍需逐case提供原始输入，不复用旧候选通过轮。
- PC008断言10项通过，包含固定S2/S3原文数字读取；final传rounds-limit在任何VM调用前退出2。相关证据在iteration-20260912-01目录。
- 另修helpers.py：去除fault环境变量后若无其他变量则删除Environment属性，避免写空MultiString导致已实测的SCM87；7项helper测试通过，实际缩量fault收尾待子智能体验证。

## 3. 子智能体实际执行与当前状态

- `/root/scenario100`：模型 `gpt-5.6-terra`、reasoning `xhigh`。已创建suite/probes/tests首版并离线验证，正在补每情景fault case-builder、精确拒绝断言、完整args/response重放、多run独立捕获、配置字节恢复和场景轴差异。主会话已提供这些具体缺口，不能以初版计数通过宣称完整套件完成。尚未运行实际100场景。
- `/root/fault_spike`：同模型与reasoning。已实际部署c082候选至授权.233，195成员核验且保留旧8e4安装目录。首轮 `attempt-gated-c082ea3fb-20260912` 遇到测试脚本将PowerShell `$true/$false`错误生成为裸true/false，故未形成有效故障正样本；B1对照通过不能抵消此失败。
- 子智能体已保存未消费fault/nonce/哈希，单次受控stop恢复遗留run `17260985-eff8-4ee4-a567-09c8d8f722a9`；其原生复核为stopped、needs_recovery=false、fault/gate/ready/probe无、pktmon停止、configbytes/Environment恢复。此处引用子智能体原始恢复文件，不编造主会话实测。
- 第二轮独立目录 `Logs/fakenetng-mcp/scenario-suite-20260912/spike-evidence-contract/attempt-gated-c082ea3fb-r2-20260912` 正在解析验证脚本并重跑。运行进度以该目录、子智能体活跃句柄和VM原生记录为准，旧失败目录不覆盖。主会话不并发调用VM。

## 4. 环境事实与剩余完成门

宿主只读VMX确认目标MAC `00:0c:29:c1:ca:49` 的ethernet0已为nat，vmsd当前仅列Snapshot2。见 `iteration-20260912-01/host-vm-network.json`（文件哈希与语义行）。不按旧host-only假设重复添网卡，不重置含VM-only证据的目标；真实route/DNS/TLS及需要的快照固化仍须事实验证。

完整目标尚未证明。剩余包括：五类新候选真实Spike全部通过；套件实现与反例完整；P1—P7及normal5/fault5/final冒烟；指定子智能体实际85良性+15故障全部通过；同一最终候选、实际接口与桶分布、每run完整证据/恢复、重放/完整性/summary核验。任一失败须保留并修复、重新派发，不能把当前局部通过或代码存在当作最终完成。独立CHK/AUD不由作者关闭，受保护PLAN/FaknetNG.md未修改/暂存/提交。
