# 2026.09.12-06 审核处理: MCP 情景压力测试套件——出站策略负向验收

- 处理日期: 2026-09-12
- 会话角色: 作者/执行方
- 原审计记录: `PLAN/2026.09/2026.09.12/2026.09.12-05-复审-MCP情景压力测试套件-出站策略负向验收.md` (blob 由审核方创建后输出, 本记录引用其文件身份; 原记录不可修改)
- 审核性质与批次: 首次有效全面审核 (03 来源阻断未引入, 05 自述"第 2 次审核尝试、不表示已有首次审核祖先") → 本记录为**首次审核结果的引入处理**, 故选 `审核处理` 类名 (被引入记录文件名含"复审"系审核方命名, 以其自述性质为准)。
- 批次身份: 方案 v0.2 blob `196b077f56cc0aa3be8789b610860bfa366cdb25` + 决策记录 04 blob `59259c337c3ae7e09d74b8e3cd6a416cd01dfdb3` + 参考 commit `aee128b25c99fcc1df188fcbeb833cff3446c66a` / tree `a118257c497f04262e5dcff8fe38ebdbaebadcfe`; 已用有效全面轮次 1 / 定向 0。
- 被审核方案 (处理前): `2026.09.12-01-功能方案-MCP情景压力测试套件-出站策略负向验收.md` v0.2 / `196b077f…` / 未审核, 不得实施。
- 处理后方案: 同路径 **v0.3** / blob `17dcc9336390bade9cea314c4b4283bb1a299fe1` / 状态: **未审核, 不得实施** (命中再次审核条件, 见 §4)。
- 处理轮次: 1 (首次引入)。
- 漂移核对: 处理前复核方案与 04 blob 与 05 冻结一致; 05 所引代码/配置事实由作者独立重验 (见 §2 各条"验证"), 无漂移; 工作树仅 `PLAN/FaknetNG.md` 的会话前既有 M 标记 (用户自有载体, 未触碰)。
- 实施授权: 未授予 (本提示词与审核结果不创建实施授权; 用户 2026-09-12 明示执行听命令)。

## 1. 逐条处理结论 (AUD-002..010 全部同意; M-01..03 直接修正)

| AUD | 结论 | 理由与作者验证 | 修改位置 | 最小闭合条件满足 | 复审请求 |
|---|---|---|---|---|---|
| AUD-002 故障终态/覆盖转移未承接现行合同 (高) | 同意 | 作者复验: run_p05_release `_fault_round` 行 407-415 要求 `stopped`+`last_run_outcome=failed`, 否则记 failure; P05 §3 "仅最终真实 stopped…failed/grace-exceeded 不能算通过"。v0.1 引用 S3 §96 历史 `failed+"stop grace exceeded"` 确系历史判据误作现行 | 新增 S15 现行合同切片; IMP-SST-07②/ACC-SST-07 改为现行终态+五节审计+锁+受管树清空+连续探活+incident 保留; ACC-SST-01 增加"非数字义务转移清单"断言; §10 常量行"故障情景终态 (现行)" | 满足 (映射与两个对照裁决: 正确 stopped 通过 / 未恢复 failed 不通过, 载于 ACC-05/07 边界) | 需复审 |
| AUD-003 缩量与 summary 输入合同 (高) | 同意 | 作者复验: mode_summary 行 690-716 硬编码 50/50/10 并 validate_rounds 全量核验, 不读缩量摘要。v0.2 "仅提前停止计数"确实无法让 summary 判定有效缩量 | IMP-SST-02 重写: `matrix-manifest.json` 输入合同 (§4 契约: planned 计数/候选身份/证据根绑定; final/summary 检测即用、缺失即全量、不一致/混根退出 3); ACC-SST-02 预期与边界同步 | 满足 (默认全量合同不变 + 缺轮/混模式/旧目录反例入边界) | 需复审 |
| AUD-004 配置轴与 D7 不一致/配置未落成契约 (高) | 同意 | 作者复验: D7 四桶为 deepseek/私网接管/进程重定向/egress_control; v0.2 把 process_redirect 当 deepseek 桶且以"定制变体"顶替第四桶。配置事实: process_redirect 有 5 个 `__RUNTIME_*__` 标记+ExternalProcessRedirectEnabled; domain_reviewed 无 ExternalTakeoverIPv4 键 (egresspolicy 行 419-442 要求该键+DnsTTL 1-300, 缺键/孤儿键即 PolicyConfigError); windows.py:623-652 拒绝未替换 DNS 占位 | IMP-SST-04 重写为桶映射表 (B1 egress_control 原样=deepseek 放行; B2 domain_reviewed+补 takeover 键+reviewed 规则替换; B3 process_redirect 5 标记替换; B4 egress_control 字段变体; 不重叠计数; 标记零残留+最小有效键校验为进清单前置); ACC-SST-04 同步 | 满足 (每桶载体/替换来源/身份校验/专属正负探针齐; "文件名记桶但功能未启用"反例由桶字段+键校验排除) | 需复审 |
| AUD-005 全程 healthy/触发时点/零 Traceback 无可实现裁决 (高) | 同意 | 作者复验: managed.py:329-333 (listener_stop/diverter_stop/child_hang 于 start 处理注入) 与 338-341 (policy_pause/cleanup_error 于 stop 处理, cleanup_error 异常经 BaseException 捕获写真 Traceback); supervisor 行 452 区停止后三输入 False; 状态含 recovering | §2 新增五窗口模型 (W-config/start/traffic/stop/post) 与 C2 重写; IMP-SST-05 按类 arm/触发时序+receipt 断言+长连接跨越 arm 点; IMP-SST-07 分窗口裁决 (W-stop 仅放行精确匹配注入 Traceback); §10 增触发时序/窗口常量行 | 满足 (五类触发路径、receipt、流量重叠证明与四类时序反例载于 IMP-05/07 与 ACC-07 边界; 未删健康断言、未加故障类、未改产品) | 需复审 |
| AUD-006 探针/PCAP 不能机械判定策略正确 (高) | 同意 | 作者复验: egress_control/domain_reviewed DumpPackets=No (default/process_redirect=Yes); windows.py:1127 dual-PCAP 仅 DumpPackets on; S11 用独立 pktmon 且负向探针仅 OBSERVED | §4 取证契约重写 (每情景每 run pktmon 会话/流元组关联/正向 --noproxy+nonce 路径/负向发送证明+无外泄元组); IMP-SST-06 逐画像 oracle 表; ACC-SST-06/08 关联链断言; F4 扩为"缺失或不可关联"; run_chain 多 run 绑定 | 满足 (发送事实/观察视角/关联规则/缺失判据逐画像落成; 不为取证改配置) | 需复审 |
| AUD-007 覆盖下限可被重复调用满足 (中) | 同意 | 作者复验: v0.2 谓词为计划长度 ≥10, 审计离线反例成立 (同接口×10) | §4 `distinct(tool) ≥10`; 覆盖按接口对 distinct scenario_id 计数; ACC-SST-09 以**实际执行**核对计划; 契约性拒绝加错误身份+无副作用后置断言 (IMP-SST-07/ACC-SST-04/05/09) | 满足 (重复单接口/漏执行/错误拒绝类别反例入边界) | 需复审 |
| AUD-008 失败继续与断点重放缺恢复闭环 (高) | 同意 | 作者复验: v0.2 resume 无 running 分支、无继续前置门、无调用超时与对账; S7 有 controller_identity; restart 产生新 run | §4 状态日志/run_chain/command_id 构造; §2 A2/A3/B3 重写; IMP-SST-05 恢复责任协议 (120s 超时/未知结果对账/running 三分支/继续前置门/退出码 4); F1/F3 闭合条件落成 | 满足 (各中断点状态/证据预期与"恢复未完不得继续"反例入 IMP-05) | 需复审 |
| AUD-009 VM 回退路径与 preflight 证明范围 (中) | 同意 | 作者复验: AGENTS 已核验重置对象为另一 VMX+Snapshot 183; v0.2 "revert 新快照→host-only"语义错误; v0.2 P1-P6 无真实中继门却声称 U-2 固化 | IMP-SST-03 重写: 绝对 VMX+身份交叉校验+对象区分; S0/S1 双态图与快照语义纠偏 (revert Snapshot 3=NAT 形态); P1-P7 重排 (身份先行、副作用后置、P7 正向中继真门固化 U-2); ACC-SST-03 同步 | 满足 (门次序/失败边界/双态证据落成; 不宣称 VM 当前实态有错) | 需复审 |
| AUD-010 ACC 入口留必要参数给实施者 (中) | 同意 | 作者复验: runner 默认 .149 (行 779/781); IDENTITY_FIELDS 五元组既有绑定值 414d3393/63d3ec36; 候选三元组在 dist/S13 | ACC-SST-01 命名断言工具 `sst_pc008_assert.py`+退出码+输出 schema; ACC-SST-02 完整命令 (全参数机械取值, 显式 .233); §4 CLI 契约 (必填参数/无默认地址/输出/退出码); §5 开发完成与运行准入分离消除 preflight 循环 | 满足 (缺参/错误目录反例入各 ACC 边界) | 需复审 |
| M-01 ACC 无来源字段 | 同意 (机械) | §7 声明与实际不符 | 九个 ACC 均加"来源: REQ-SST-xx" | 满足 | 不单独触发 |
| M-02 S14 行列错位/F-1..F-5 与 F1..F4 近义 | 同意 (机械) | 表头五列 | S14 重写为标准五列 (v0.3 §0); fallback 节末加命名澄清 | 满足 | 不单独触发 |
| M-03 作者进度叙述混入方案 | 同意 (机械) | §5 "本方案仅完成写入阶段" | 已删除 (执行节拍条款保留) | 满足 | 不单独触发 |

## 2. 实际 diff 清单 (v0.2 → v0.3)

- §0: 新增 S15 行; S14 重写五列。§2: A2/A3/B3/C1/C2 重写; 新增窗口模型块。
- §4: 整节扩写 (distinct 谓词/matrix-manifest/状态日志与 run_chain/判决记录/实际覆盖/取证契约/CLI/退出码)。
- §5: 依赖顺序改"开发完成与运行准入分离"; 删进度叙述; 增 runner 默认地址警示与混根禁止。
- §8: IMP-02 (matrix-manifest)、IMP-03 (双态图+P1-P7+对象区分)、IMP-04 (桶映射表)、IMP-05 (窗口驱动+恢复责任协议)、IMP-06 (逐画像 oracle)、IMP-07 (分窗口裁决+现行终态) 重写; IMP-01 补非数字义务转移清单。
- §9: ACC-01..09 全部重写或增补 (来源字段+完整调用契约+现行判据)。
- §10: 新增/改写常量行 (现行故障终态/触发时序/窗口模型/调用等待上限/捕获层/桶映射/地址警示); F1-F4 重写; 命名澄清。
- 未改: S1-S13 来源行、§1、§3 (除 §5 移出的叙述)、§7 追溯矩阵、REQ 语义。

## 3. 影响分析

- 受影响的既有通过项 (05 通过项): 对象身份/来源可读取性 (AUD-001 闭合) 不受影响, 继承; 05 其余评审项均为"存在不同意见/未形成结论", 无可继承通过项。
- 未受影响继承项: §3 非目标与明确不引入项、GUI/IPv6 排除、权限归属高层边界 (04 D8/D10+S6)、S1-S13 切片。
- 全局不变量: 产品代码零改动; 15 接口/100/≥10/≥5/真实流量四项原始要求不变; 候选锚定 mcp-c6090f816; 受保护文件不触碰; 预算 60s/180s/512MiB 不变; 无外网降级/公共 DNS/GUI 驱动/新故障类均不引入。
- 前轮覆盖遗漏: 无 (05 为首次有效全面, AUD-002..010 均首次提出, 非复开; AUD-001 已闭合且未同根因复开)。

## 4. 再次审核判定 (逐项触发)

| 触发条件 | 命中 | 具体修改/影响/回滚隔离/证据 |
|---|---|---|
| 跨两项目或改多业务共享契约 | **命中** | IMP-SST-02 matrix-manifest 改变 run_p05_release final/summary 的授权输入合同; 该 summary 是整改闭环验收链 (作者+独立核验方) 共同消费的判据。影响: 缩量判定语义。隔离: 默认无 manifest=全量合同不变, 混根即退出 3。证据: §4/IMP-02/ACC-02 diff |
| 不可逆/高风险迁移权限安全隐私外部契约 | 未命中 | IMP-03 仅细化既有 D8 授权的操作契约, 不新增授权 |
| 改总体架构/核心数据流/一致性/恢复策略 | **命中** | 裁决模型由单一"全程 healthy"改为分窗口四要素 (核心数据流); 新增中断对账/继续前置门/running 恢复协议 (恢复策略)。影响: 全部情景裁决计算。回滚: 宿主侧方案文件级, 可整体回退 v0.2。证据: §2/§4/IMP-05/07 diff |
| 核心主路径回滚困难/影响面不可限定 | 未命中 | 全部宿主侧, 文件级可回退 |
| 仍有多个有证据可行方向 | 未命中 | 桶映射/触发时序/终态均由代码事实与 S15 唯一钉死 |
| 用户或生效约定明确要求 | 未命中 | 无 |

判定: **需要再次第三方审核**; 因命中项含核心数据流/恢复策略改变 (架构级), 下一审核模式选 **全面复审** (非"文件多/意见多"触发)。

## 5. 状态与入口

- 方案状态: `未审核, 不得实施` (v0.3, blob `17dcc933…`); 处理后复查: 9/9 ACC 含来源、稳定 ID 全命中、13 REQ、无占位/未声明 fallback、历史终态仅存于否定语境、预检通过。
- 引入后本记录构成该方案链第一轮已引入第三方审核 (05) 的处理记录。
- 下一阶段: 全面复审 (对象 v0.3; 任务包 = v0.3 全文 + 本记录 diff 清单 + 05 的 AUD-002..010 最小闭合条件)。
- 实施资格: 未取得 (审核未通过); 实施授权: 未授予。
