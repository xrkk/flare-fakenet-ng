# 执行调度更正与测试交接

会话角色: 作者/执行方。创建日期: 2026-09-12。持续目标仍为16记录的完整100情景子智能体测试—修复—复测直至无错通过，未完成、不缩小。

## 模型派发更正

主会话复核工具契约发现，前两任务创建参数同时使用 `fork_turns=all` 和显式模型覆盖；工具说明不保证这种组合应用覆盖。因此旧记录中terra/xhigh应理解为请求配置，**不作为实际执行模型已被确认的证据**。原任务 `/root/scenario100`、`/root/fault_spike` 已在安全边界结束；均报告无活跃终端/session、没有r3在途操作。没有重启或取消任何仍运行的VM请求。

重新派发时均明确使用 `fork_turns=none`、`model=gpt-5.6-terra`、`reasoning_effort=xhigh`：

- `/root/terra_scenario100`：接手4个现有suite/probe/fault-adapter/test文件，完成oracle与实际100测试。
- `/root/terra_fault_spike`：接手r3准备与五类真实故障证据复跑，当前独占.233；完成后明确交回suite任务。

实际100场景截至交接仍为未执行，没有将旧任务的计数/局部检查冒充指定模型的100情景执行。

## 交接的权威状态

- 新候选c082已部署，身份与源码/包见18记录及 `attempt-gated-c082ea3fb-20260912/deployment.json`。
- r2失败：PowerShell只读 `$PID` 被赋值；gate观察异常后并发start尚未收敛就发生后续mutation，导致operation_busy。未形成有效故障正样本。原目录保留。
- r2恢复原生证据：`Logs/fakenetng-mcp/scenario-suite-20260912/spike-evidence-contract/attempt-gated-c082ea3fb-r2-20260912/recovery-verified.json`；MCP stopped、SCM Running、configbytes/Environment恢复、needs_recovery=false、无fault/gate/ready/probe、pktmon停止。
- r3目录已有脚本修补，但尚待全生成PS系统变量赋值扫描、ParseInput与并发异常本地模拟后才能开始实际复跑。不能把join(45)返回本身当作线程已结束，须确认原请求收敛后再发下一mutation。
- 套件P1-P4原始检查已通过，路径 `Logs/fakenetng-mcp/scenario-suite-20260912/mcp-c082ea3fb-d958768dc8fa-p1p4-02/preflight-p1-p4.json`。实测Win10 build10.0.19045.0，route Ethernet0→192.168.204.2，api.deepseek.com A解析成功。该记录不替代P5-P7/TLS，也不计入100场景。
- 套件离线9测试通过，但良性run.log/元组策略/无外泄oracle、故障逐case导出裁决接入、restart第一run的完整PCAP/审计等尚待完善；新任务收到明确缺项清单，不能软通过。
- 主会话宿主工具又修复初次VM身份调用失败时probed_identity未赋值导致失败记录丢失（先复现再修复，commit `4d94df5`）；最新组合46测试通过。helpers空Environment修复commit `f40461a`。这些是独立宿主工具提交，不改变已部署产品候选。

本记录只保存执行事实与调度更正，不修改方案审核状态，不关闭AUD/CHK。未完成项继续由新指定模型任务执行，失败照常留存并修复后重派。
