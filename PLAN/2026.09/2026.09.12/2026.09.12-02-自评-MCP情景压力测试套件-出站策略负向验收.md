# 2026.09.12-02 自评: MCP 情景压力测试套件——出站策略负向验收

- 自评日期: 2026-09-12
- 会话角色: 作者/执行方
- 方案路径: `PLAN/2026.09/2026.09.12/2026.09.12-01-功能方案-MCP情景压力测试套件-出站策略负向验收.md`
- 方案版本 / blob: v0.1 / `7363b06df869a80dd7c3f4b729d08ea41f0294d2` (401 行; v0 初始 blob `30cbf6607901ee462116baa60ec68d49058a17c1`)
- 方案状态: 未审核, 不得实施
- 阶段判定: **首次审核未完成**
  - 证据: 同目录 `PLAN/2026.09/2026.09.12/` 仅含本方案文件; 仓库范围 grep (`情景压力测试套件|IMP-SST|ACC-SST`, PLAN/ 与 Logs/) 除方案自身外无命中, 无任何可匹配的第三方审计或审计处理记录。
- 历史首次审计和处理记录身份: 无 (本方案链为首链, 今日创建)。
- 实施授权: 未授予 (本作者会话未收到本方案链的实施提示词; 仓库无 `scenario_suite` 实施痕迹; 用户 2026-09-12 明示 "执行过程要听我命令。现在只做方案写入"。方案状态与自评结论不替代用户授权)。
- 本地审核基线 HEAD: `aee128b25c99fcc1df188fcbeb833cff3446c66a` (分支 `process-redirect-v23`)
- 来源漂移核对: S1/S2/S3/S7/S8/S9/S10(四档)/S11/S12/S13 全部 git blob 与方案 §0 记录一致, 零漂移。

## 五类范围

- 核心维护范围: `test/mcp/acceptance/` (`run_p05_release.py` 参数扩展; 新增 `scenario_suite.py`、`scenario_probes.ps1`); `PLAN/2026.09/2026.09.12/` (方案、PLAN-CHANGE-008 变更记录、自评); `Logs/fakenetng-mcp/` 下套件证据目录; .233 验收 VM 环境操作 (会话恢复、NAT 适配器、新快照)。
- 直接依赖: `fakenet/mcp/tools.py` (15 接口契约)、`fakenet/mcp/supervisor.py` (健康判定与采样周期)、`fakenet/mcp/faultinject.py` (5 故障类)、`fakenet/diverters/egresspolicy.py` 与 `fakenet/diverters/windows.py:522` (引擎接入)、`fakenet/configs/` 四配置档、`test/egress_control_vm/` (13 例语料)、`dist/mcp-remediation-20260912-threadexit/` (候选身份)。
- 明确排除范围: `fakenet/` 产品代码修改; GUI 部件层 (`fakenet/gui/`); 7 份固定 blob spec 原文; 受保护文件 `PLAN/FaknetNG.md`; 闭环 JSON 直接改写; CHK 裁决权; 维护方 .149 VM。
- 决策关键路径与内容指纹: 方案 v0.1 `7363b06d…`; S1 `40033637…`; S2 `ec4f025a…`; S3 `d8987f5b…`; S7 `fcf640b5…`; S8 `61e3bc17…`; S9 `5319c12b…`; S10 `078c81ca…`/`4b0d4c83…`/`21a35510…`/`dc8833d1…`; S11 `5f7e2f45…`; S12 `84c2b551…`; S13 `a4c626c5…` (均为读取时 git blob, 已于本日复核); S14 为对话决策 (Q1-Q9), 不可哈希, 以对话定位。
- 上下文路径: S4 交接文档、S5 PLAN-CHANGE-007 先例、S6 AGENTS.md、`Logs/fakenetng-mcp/main-remediation/` 既有证据、闭环机制本身。

## 机械预检 (修正后复跑)

- 稳定 ID: IMP-SST-01..07 / ACC-SST-01..09 各命中 ≥2 次 (定义+追溯+交叉), 无重复/复用; REQ-SST-01..13 唯一 13 条。
- 双向追溯: 追溯表全 ID 展开, 每个 IMP/ACC 的"来源"字段回指 REQ。
- 占位符: 无 (无 `<xxx>`/TODO/TBD/待定); 唯一 `%` 命中为 curl 命令字面量 `"%{http_code}"`。
- 未声明 fallback: 无 (F1-F4 与反向声明完整; 正文行为均对应声明项)。
- 方案正文不含自身 blob; 无 checkbox/完成百分比/实施进度。
- 结论: 机械预检通过。

## 业务评估 (逐项)

1. 需求覆盖与双向追溯 — 通过。REQ-SST-01..13 覆盖用户原始约束 (100/≥10/≥5/真实流量) 与 Q1-Q9 全部已确认决策及用户缩量指令; 无无落点需求、无无依据实施项 (每个 IMP/ACC 均回指 REQ)。证据: §7 追溯表 + 预检计数。
2. 现有机制不被破坏 — 通过。`run_p05_release.py` 不传新参数时行为与现状逐字节等价 (IMP-SST-02 目标条款); 产品代码零改动 (§3.1)。证据: S12 CLI 事实; §3.1/§5。
3. 可交接实施性 — 通过 (修正后)。方向性决定全部机械闭合: `--rounds-limit` 语法钉死 (FIND-SST-02)、故障子集配置档归属钉死 (FIND-SST-03)、退出码契约补全 (FIND-SST-04); 实施者按方案+S1-S14 可确定范围/顺序/验收。
4. 验收契约 — 通过。每个 ACC 含前置与输入、命令或可执行契约、精确预期、证据、通过/失败边界; 尚未创建的工具 (`scenario_suite.py`) 有工作目录/输入/输出 schema/退出码; ACC 只覆盖目标范围与直接依赖, 未把仓库级验证设为通过条件 (既有 pytest 仅作回归范围陈述)。
5. 正确性/兼容/资源/现实阻碍 — 通过, 有证据边界。运行时长 4-8h 标注为估算 (U-5); NAT/路由/DNS/会话四项未决事实已固化为 preflight 门 (U-1..U-4, ACC-SST-03), 未把推断写成硬约束; Win10VM MCP 会话故障 (F-4) 为已观测事实并有 F1 恢复路径与阻断条件。
6. fallback — 通过。F1-F4 各有机械触发条件与退出路径; 反向声明排除无依据降级/缩减/软过。无掩盖主流程错误的 fallback。

## 发现与修正 (ID, 风险, 对应章节, 证据, 问题和影响, 修改)

| ID | 风险 | 章节 | 证据 | 问题和影响 | 修改 |
|---|---|---|---|---|---|
| FIND-SST-01 | 低 | §0 S10 | 四配置档 git blob 实测值 (078c81ca…/4b0d4c83…/21a35510…/dc8833d1…) | v0 以 "…" 缩写 blob, 追溯与漂移核对无法机械执行 | 写全 40 位 blob; 不升版 (纯追溯补全) |
| FIND-SST-02 | 中 | §8 IMP-SST-02 | S12 (无该参数); v0 原文 "例如 `builtin=3,custom=2` 或 `per-class=1`" | 参数语法二义, ACC-SST-02 命令契约不唯一, 方向性未机械闭合 | 钉死: normal=`builtin=N,custom=M` (N,M≤50), fault=`per-class=K` (K≤10), final/summary 传参即用法错误退出码 2; 随 v0.1 升版 |
| FIND-SST-03 | 中 | §8 IMP-SST-04 / §9 ACC-SST-04 / §10 | S14 Q6=A (故障×流量交互) + Q7 反转 (出站策略为对象主体) | v0 未钉故障子集 15 情景的配置档归属, 允许其全部落入 default 档, 使故障×流量交互脱离被测功能主体 | 钉死: 故障子集全部取自 egress 家族配置档, default 桶内 fault_class 均为 null; ACC-SST-04 增加对应断言与常量表补注; 随 v0.1 升版 |
| FIND-SST-04 | 低 | §4 | 自评契约 "尚未创建的工具写…退出码和可观察结果"; v0 §4 无退出码 | 新工具可执行契约不完整 | 增加退出码契约 (0/2/3/4) 与 stdout 首行 JSON 路径输出; 随 v0.1 升版 |
| FIND-SST-05 | 低 | §8 IMP-SST-05 / §10 | S8 supervisor.py 行 15 `HEALTH_INTERVAL_SECONDS = 2.0` (已读证) | v0 "健康采样周期 2s" 未标事实来源 | IMP-SST-05 与常量表补注 S8 来源; 不升版 |
| FIND-SST-06 | 低 | §7 追溯表 | 预检计数: REQ-SST-10 原 IMP 列 "全部 IMP 的边界" 为非稳定 ID 引用 | 追溯表机械命中不完整 | 改为 "IMP-SST-05, IMP-SST-06 (范围边界)"; 不升版 |

- 遗留未修正项: 无。风险分布: 高 0 / 中 2 (FIND-SST-02/03, 已修正) / 低 4 (已修正)。
- 修正全部由规范性来源 (S8/S12/S14) 与已证实代码事实唯一确定, 无需用户取舍; 未改变规范性需求或上层边界。

## 审核路线判定

- 阶段判定为 `首次审核未完成` → 固定判定 **必须首次第三方审核**; 本轮不执行后续改版触发条件判断 (触发项逐项结论: 不适用)。
- 送审准入: **通过** (业务评估、来源追溯、方向性事实、IMP/ACC、直接依赖、验收、机械预检全部通过; 方向/验收阻断项为零)。
- 审核批次指纹: 方案 v0.1 `7363b06d…` + S1-S13 语义切片 (见上指纹) + S14 会话决策; 本批为该方案链首次审核批次。

## 紧邻入口状态

- 方案状态: `未审核, 不得实施` (v0.1, blob `7363b06d…`)。
- 下一阶段: 首次第三方审核 (由第三方审核方按本记录的批次指纹与五类范围执行)。
- 实施授权: 未授予; 审核通过后仍需用户命令方可实施。
