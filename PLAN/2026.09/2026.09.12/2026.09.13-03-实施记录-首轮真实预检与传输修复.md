# 首轮真实预检失败与传输修复入口

会话角色: 作者/执行方

依据记录16持续执行授权。实际100场景由 gpt-5.6-terra（xhigh）子智能体承担；本记录不是第三方审计或验收通过。

## r6关闭复验与交接

原件位于 Logs/fakenetng-mcp/scenario-suite-20260912/spike-evidence-contract/attempt-gated-c72bd090d-r6-20260913/。session60465退出0，diverter run d9c3bfb9-d7fa-44b9-becf-8aa161aa73ec 最终stopped/state3、last_run_outcome=failed。fault-action.json 记录同句柄1320由GetHandleInformation rc1/error0变为rc0/error6；完整run.log的Unhandled exception、Traceback及WinError2均为0。四条探针连接匹配的独立TCP报文数量分别14/28/28/28，root-diverter-connection-check.json记录原始文件指纹及连接时间。

r6-diverter-terminal-recovery.json已确认配置/环境恢复、控制文件与故障模式移除、受管及探针进程清空、PktMon停止、needs_recovery=false、MCP stopped/state1；VM已交terra_scenario100。上述事实证明关闭修复及环境可继续使用，不把缺少完整逐轮五节恢复/healthy证据的priority Spike冒充正式fault轮通过。

## 实际套件预检

新候选保持mcp-c72bd090d-54b9dfaf3db7；执行器首轮576c2fa。真实命令使用根 Logs/fakenetng-mcp/scenario-suite-20260912/mcp-c72bd090d-54b9dfaf3db7-full-20260913，generate成功。preflight.json显示P1身份、P2候选与服务、P3通道、P4路由DNS均通过；P4实测API IPv4 123.125.246.121，DNS192.168.204.2，Ethernet0/ifIndex11/默认下一跳192.168.204.2。

P5-probe-stage失败，原始错误为FileNotFoundError [WinError 206] 文件名或扩展名太长。原因定位为将完整探针脚本base64内联到远端PowerShell命令，超过Windows命令长度。此时未执行场景或P05，不得将该错误视为产品故障或VM无通道。

子智能体正改用有界分块或已授权host-only文件传输，并要求最终SHA校验；保留失败root，在独立attempt重跑。产品无需因测试工具变更重建。另已修复_run_one辅助case_release在部分primary interleave下未初始化的问题，主会话现有19项suite测试通过；待传输修复一起冻结下一执行器提交。

最终100场景仍未开始，目标保持全部真实场景无错通过。
