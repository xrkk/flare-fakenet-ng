# 新候选 P05 故障失败与退出 ACK 阻塞定位

会话角色: 作者/执行方

- 日期：2026.09.13；承接记录 11。方案状态不变，本记录不构成审计或总体验收。
- 候选 `mcp-c51d8315f-25e54353e099`，源提交 `51d8315f45afcace5faec5af8ea17f8ea56067e5`。
- 指定执行者：gpt-5.6-terra（xhigh）子智能体 terra_scenario100，唯一 VM owner。

## 1. 已执行结果

P1–P7 全部通过；根侧核验九份引用证据的长度与 SHA-256。P05 正常五轮（三 builtin、两 custom）通过；根侧以 evidence_integrity.validate_round、validate_sample_category 和 validate_rounds 核对原始记录，均零错误。

故障五轮执行器 session 98411 已返回 exit 1。policy_pause、listener_stop 已完成且上述证据完整性核验零错误；第三类 diverter_stop 失败，child_hang、cleanup_error 未执行。final/summary 和实际 100 场景均未开始。100 场景清单仅代表待执行覆盖，不能计作已通过。

失败 run `5cddf683-848d-403b-b1d0-f05a3514c4d3`，nonce `c9160d8e-879a-4463-a4ce-a79c27e5cf5d`。产品最终 stopped、last_run_outcome=failed、run/controller 清空，但 incident-02 managed-exit.json complete=false，错误为 `RuntimeError('helper ended without final result')`。完整性门正确判失败；不能将 stopped 等同于故障验收通过。

## 2. 原始证据及定位

证据根：`Logs/fakenetng-mcp/p05-c51d8315f-25e54353e099-smoke-20260913-R6pKz9`。

- `diverter-stop-exit-export.json` SHA-256 `d661f2538010b291a1245ed095b326f8062a666045743e8c623907075e9ebd3c`。
- `diverter-stop-exit-timing.json` SHA-256 `6682dc345cd1088b80c479d30c0751b12feba7d0f508fe4abaaa4e1ccaa56999`。
- `diverter-stop-service-log-shared-read.json` SHA-256 `f9b0e8bf8a130ed0c1fb5bfbe2bb9ab1ad3d70c5d9b15ed31e01c22129ef4e41`。

根侧核验 exit-export 中六份已保存原件长度/hash；service.log 通过 FileShare.ReadWrite|Delete 只读导出。其 metadata 的 atomic_snapshot=true 过强：前后长度相同只说明读窗口长度未变，不能证明原子快照；只按记录的读取时间窗使用，不改原件。

VM 单时钟域（UTC）时序：

- helper PID 7980 / FILETIME 134337294622434086，创建 23:31:02.2434086，60 秒截止 23:32:02.2434086。
- entry.json 创建 23:31:02.420。
- dump-tool.json 创建 23:31:02.855、末写 23:32:04.838；记录 WinError 31。
- service.log 23:32:04.879 报 owner dump collection failed；owner-acquired.json 创建 23:32:05.037，晚于 helper 截止约 2.8 秒。
- 23:32:05.434 watch 报 helper ended without final result；owner-result 创建 23:32:06.291。
- 退出目录只有 dump-tool/entry/owner-acquired/owner-result，无 result、normal-claim/ack、owner-dump 或 partial。

代码 `exit_retention.py::collect_owner_dump` 在整个转储过程持有 `_call_lock`；`_publish`/`_read_optional` 也经 `_call` 取得同锁，因此备用转储能够阻塞 helper ACK。这与本轮“转储结束后才出现 ACK、ACK 已晚于 deadline”的时序一致。WinError 31 原件来自 dumpworker 的宿主备用采集，不能直接当作 exit_monitor 内部报错。后续修复必须同时保持 PID/创建时间归属、60 秒窗口、单个目标转储路径和磁盘配额，不允许为了通过伪造结果或吞掉不完整证据。

## 3. 本地修复与剩余工作

另一个确定缺陷已用测试红→绿复现：watch 主失败后的 cleanup 再失败会丢失 failure_stage/owner_observation。当前未提交修复在 cleanup 前冻结原失败报告，cleanup_error 追加在其上，不改变 complete=false 或保留句柄责任。新增用例 `test_missing_helper_result_keeps_failure_observations_when_cleanup_fails`；相关 exit_retention/exit_monitor/exit_incident 共 22 项测试通过。

该字段保留修复不代表 ACK 阻塞已解决。本记录冻结时协议锁阻塞修复仍待完成，没有新候选构建或新的测试轮。旧失败原件保留，后续在既有授权内修复、构建、重新派发测试，不另索审批。实际 100 场景执行数仍为 0。
