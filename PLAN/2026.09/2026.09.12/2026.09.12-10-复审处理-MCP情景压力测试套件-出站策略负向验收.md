# 2026.09.12-10 复审处理: MCP 情景压力测试套件——出站策略负向验收

- 处理日期: 2026-09-12
- 会话角色: 作者/执行方
- 引入记录: `2026.09.12-09-复审-MCP情景压力测试套件-出站策略负向验收.md` (blob `a5738a19438fe4964e9589917f3745c4b96041a1`) 及其前轮 `2026.09.12-08-复审-…md` (blob `b3b8d5856e174a420ec9533b9efa7c84a0e9925f`)。两份均为复审链记录 → 本处理选 `复审处理` 类名。原记录不可修改。
- 处理轮次: 2 (08 材料核对 + 09 上游决定登记, 一并引入)。
- 修订注记: 2026-09-12 用户指令 "改为定向复审, 只审需要审的内容", §4 审核模式由全面改判定向并补齐 `定向复审请求范围`; 本记录初版 blob `be5d1a1ec4bce6f35605f191718399140e824d47` 自本修订起作废, 以修订后 blob 为准。
- 被审核方案 (处理前): `2026.09.12-01-功能方案-…md` v0.3 / `17dcc9336390bade9cea314c4b4283bb1a299fe1` / 未审核, 不得实施。
- 处理后方案: 同路径 **v0.4** / blob `b5696d78338c5efb38349aa0174839a5a2088cba` / 状态: **未审核, 不得实施** (命中再次审核条件, 见 §4)。
- 本地审核基线: HEAD `aee128b25c99fcc1df188fcbeb833cff3446c66a` / tree `a118257c497f04262e5dcff8fe38ebdbaebadcfe` (与 08/09 记录一致); 处理前复核方案 v0.3、04 (`59259c33…`)、05 (`671fa35b…`, 09 引用值)、06 (`499e8ea5…`)、07 (`17ea9c31…`, 09 引用值) 身份无漂移。
- 实施授权: 未授予 (审核未通过; 用户节拍 D10)。

## 1. 08/09 的处理结论

| 记录 | 结论 | 作者立场 | 理由与处置 |
|---|---|---|---|
| 08 (Spike 材料核对) | 接受"启动期同 run 网络活动"有限实证; 熔断维持 (D6 语义决定缺失); 指出 receipt mtime ≠ 精确 socket 关闭时刻、入站 hammer 不能替代目标出站流量 | 同意 | 与 Spike 记录的诚实边界一致 (README "nuances" 三条); 无需修改方案的部分 |
| 09 (上游决定登记 DEC-SST-START-01) | 用户已确认六条边界; AUD-005 的上游选择已明确; 不确认 AUD 关闭 (方案未引入该决定) | 同意并引入 | 本处理即"另建处理记录并依职责处理方案"的执行; 见 §2 diff |

- 审核循环熔断的解除路径: 07 §4 要求的最小 Spike 已完成 (08 核对接受有限实证) + 09 登记用户上游决定 → 作者按 DEC-SST-START-01 修订方案 (v0.4)。同一 AUD (005) 的根因链条 (arm/trigger/流量相位混淆) 在 v0.4 中以用户确认的联合语义 + 目标出站会话重叠义务闭合; 不再发生"同根因文字修补"。
- 09 明示不追溯修改 04/07/08; 本处理引用 DEC-SST-START-01 稳定 ID 与 09 记录身份 (09 §2 要求)。

## 2. 实际 diff (v0.3 → v0.4)

- §0: 新增 S16 行 (09 记录 blob + 08 关联 blob; DEC-SST-START-01 六条边界全文; 适用边界=仅故障裁决条款)。
- §2: 窗口模型后新增"故障情景窗口语义"块: 启动型三类 W-start 触发 + 预期启动门失败为合法负向终态 (恢复义务全保留); 重叠义务改为**同 run 目标出站策略会话区间 ∋ t_actual_trigger** (入站 hammer 降为辅助, S16 边界④⑤); 精确预期异常白名单; 停止型两类维持 W-stop 触发 + 会话跨越触发点; 良性 85 情景不变。
- IMP-SST-05: W-config 相位增加"目标出站会话发射器"职责 (VM 内经 Win10VM MCP 对桶目的地持续发起 nonce 绑定出站连接, 持有至 run 终态); 重叠断言以**触发时刻**为准 (修正 v0.3 的"arm 点跨越"表述——即 07 指出的根因)。
- IMP-SST-06: 新增"目标出站会话探针"探针类 (S16 边界④承载者): 长持有出站连接 + 关联规则 (会话↔Diverter PROCESS_FLOW/中继日志↔pktmon, 证明同 run 受管)。
- IMP-SST-07: healthy 规则增加启动型故障"无 W-traffic 采样段不判 fail, 义务由预期启动门失败+完整恢复承载"; log_clean 白名单改精确预期异常表 (基线 listener_stop→serve_forever, cleanup_error→精确消息; 其余三类实施时登记并经复审核对); ②终态融合 S16; 来源行加 S16。
- ACC-SST-07: 精确预期按 S16 六条边界逐条落点重写; 失败边界增加"无同 run 目标出站会话区间包含 t_actual_trigger (仅有入站 hammer 不够)"与"启动门失败不能归因指定注入"。
- §10: 新增"启动型故障联合验收语义"与"预期异常白名单基线"两行常量; "故障触发时序"行补 S16 边界①来源。
- 未改: S1-S15、§1、§3、§5、§7、IMP-01/02/03/04、ACC-01..06/08/09、REQ 语义。
- 修正过程零占位符/未声明 fallback; 9/9 ACC 来源字段保持; 稳定 ID 无新增无删除。

## 3. 影响分析

- 受影响既有结论: 05 AUD-005 → v0.4 语义闭合 (待复审确认); 05 其余 AUD (002-004/006-010) 的 v0.3 闭合经 06 处理后**仍未被任何成功审核轮次确认** (07/08/09 均未确认)。继承项: AUD-001 闭合 (08/09 均保持)、对象身份、S1-S16 来源链。
- 全局不变量: 产品代码零改动; 候选锚定 mcp-c6090f816; 15 接口/100/≥10(distinct)/≥5/真实流量不变; F1-F4 未放宽 (09 §3 表逐项保持); 预算不变; 受保护文件未触碰。
- Spike 证据 (Logs/fakenetng-mcp/spike-listenerstop-20260912/, 27 文件 manifest SHA 全录) 作为白名单基线与机制可行性的证据引用, 不作为"套件通过"证据 (08 已界定)。

## 4. 再次审核判定 (逐项)

| 触发条件 | 命中 | 证据 |
|---|---|---|
| 规范性来源含义改变 (DEC-SST-START-01 重定义 D1/D2/D6 联合语义) + 核心数据流 (故障裁决模型) 改变 | **命中** | §2 故障窗口语义块、IMP-05/06/07、ACC-07 的 v0.3→v0.4 diff; S16 引入 |
| 影响面无法可靠界定 | 未命中 | diff 完整枚举于 §2, 未触及 ACC-01..06/08/09 |
| 其余四项 (跨项目契约/不可逆权限/回滚困难/多可行方向/用户约定) | 未命中 | 宿主侧文件级可回退; 语义由用户决定唯一钉死 |

判定: **需要再次第三方审核**。模式选择: **定向复审** (2026-09-12 用户指令改判; 作者原判全面, 改判依据与后果见下)。作者原判全面的理由为: 05 所提其余 8 条 AUD (002-004/006-010) 的 v0.3 闭合至今未经任何成功审核轮次确认, 定向通过后该残留状态仍在。用户指令即接受该残留不在本轮补审——本轮**只审 DEC-SST-START-01 落地与 AUD-005 闭合**。

### 定向复审请求范围

- 基准审计/处理记录身份: 09 (blob `a5738a19438fe4964e9589917f3745c4b96041a1`, DEC-SST-START-01 载体) + 本记录 (修订后 blob, v0.3→v0.4 diff 与影响分析); AUD-005 历史链 05 (`671fa35b…`) / 06 (`499e8ea5…`) / 07 (`17ea9c31…`) / 08 (`b3b8d585…`)。
- 触发修改与实际 diff: 方案 v0.3 `17dcc933…` → v0.4 `b5696d78338c5efb38349aa0174839a5a2088cba`, 完整 diff 清单见本记录 §2 (§0 S16 新行、§2 故障情景窗口语义块、IMP-SST-05/06/07、ACC-SST-07、§10 两行常量; 其余章节字节不变)。
- 未解决/复开的 AUD: 仅 AUD-005 (07 同根因熔断, 09 上游决定已取得, v0.4 承接)。**明确排除**: 05 所提 AUD-002/003/004/006/007/008/009/010 的闭合确认不在本轮范围, 保持"经 06 作者处理、未经第三方确认"状态, 本轮通过与否的判定不得包含它们, 也不得因它们未审而判本轮失败。
- 纳入的章节及条目: 方案 §0 S16 行、§2 "故障情景窗口语义"块、§8 IMP-SST-05/06/07、§9 ACC-SST-07、§10 "启动型故障联合验收语义"+"预期异常白名单基线"两行; 代码切片: `fakenet/mcp/managed.py` (310-357 start/stop 触发分支, blob `62d62fa0…`)、`fakenet/mcp/faultinject.py` (clear/inject_*, blob `1722880f…`)、`fakenet/mcp/supervisor.py` (159-200 启动健康门, blob `61e3bc17…`); 证据: `Logs/fakenetng-mcp/spike-listenerstop-20260912/` (manifest 全 27 文件 SHA, 白名单基线与机制可行性引用)。
- 受影响的既有通过项: 无 (AUD-001 闭合与对象身份不受本 diff 影响, 无需重审)。
- 全局不变量 (复核不放宽): 产品代码零改动; 候选锚定 mcp-c6090f816; F1-F4 语义不变; 85 良性情景健康/日志要求不变; 恢复义务 (五节审计/锁/树清空/探活/incident) 不减免; 预算 60s/180s/512MiB 不变。
- 排除与继承项: 排除 = 其余 8 条 AUD 的确认 (见上) 与任何实机/VM 操作; 继承 = 08 接受的"启动期同 run 网络活动"有限实证、09 的 DEC-SST-START-01 文本与边界编号、Spike 证据 manifest。
- 待回答问题: ①v0.4 对 DEC-SST-START-01 六条边界的承接是否逐条准确无遗漏无扩张 (边界①-⑥ ↔ §2/IMP/ACC 落点); ②预期异常白名单基线 (listener_stop→serve_forever、cleanup_error→精确消息) 及"其余三类实施时登记并经复审核对"的安排是否可接受; ③"同 run 目标出站会话区间 ∋ t_actual_trigger"的判定契约 (发射器/探针/关联规则) 是否机械可执行; ④启动门失败"归因于指定注入"的判定是否机械闭合 (排除其他失败/超时/错误 healthy)。
- 所需证据: 上述方案切片 blob、记录链 blob、Spike manifest、三个代码切片; 无需新的运行态证据。
- 通过条件: 定向范围内 AUD-005 依据 DEC-SST-START-01 判定闭合, 且纳入切片无新中/高风险意见; 通过后方案状态与"其余 8 条 AUD 残留状态"的登记由该轮审核记录裁定, 作者按引入后复查流程处理。

## 5. 状态与入口

- 方案: v0.4 / `b5696d78…` / `未审核, 不得实施`; 处理后复查通过 (稳定 ID 全命中、13 REQ、S16/DEC 引用 18 处、无占位符、无未声明 fallback)。
- 实施资格: 未取得; 实施授权: 未授予。
- 下一阶段: **定向复审** (范围=本记录 §4 `定向复审请求范围`; 待用户转审核方)。
