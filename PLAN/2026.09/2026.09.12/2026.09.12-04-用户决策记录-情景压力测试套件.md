# 2026.09.12-04 用户决策记录: MCP 情景压力测试套件——出站策略负向验收

- 记录日期: 2026-09-12
- 转录者: 作者/执行方 (本会话)
- 性质: 作者对 2026-09-12 会话中用户决策与指令的逐条转录 (答复逐字引用)。
- 效力边界: §4 用户确认区登记确认事实之前, 本记录不得作为授权或需求依据; 关联方案
  v0.2 的来源切片 S14 在确认前仅为定位占位。
- 关联方案: `PLAN/2026.09/2026.09.12/2026.09.12-01-功能方案-MCP情景压力测试套件-出站策略负向验收.md` (v0.2)
- 用户侧原始要求载体: `PLAN/FaknetNG.md` 2026.09.12 段 (行 219-224, 读取时 blob `64d4fdc6892e4d85e8610ec1bc27a79fc39d1dc9`), 内容与本记录 D0 一致 (该文件为用户自有载体, 作者只读)。
- 关联事件: 首次全面第三方审核于来源门禁阻断, 审核记录 `PLAN/2026.09/2026.09.12/2026.09.12-03-审核-MCP情景压力测试套件-出站策略负向验收.md` (AUD-001); 本记录即针对该阻断的闭合动作。

## 1. 原始任务 (D0)

2026-09-12 用户消息 (逐字):

> 评估方案: 为 fakenetng-mcp 设计 "情景压力测试"
> - 设计 100 个 "情景"
> - 每个 "情景" 最少连续测试 10 个 mcp 接口
> - 每个接口最少要包含在 5 个 "情景" 中
> - 每个场景中都要系统产生 "真实" 的流量
> 需求不清晰可以用 grill-me 提问 (每次只问一个问题, 给出选项 ABC (最少 2 个), 推荐选项放 A, 且要说明理由)

## 2. 逐项决策 (D1-D10)

每项含: 问题、用户答复 (逐字)、生效解释。

| ID | 问题 | 用户答复 (逐字) | 生效决策 |
|---|---|---|---|
| D1 (Q1) | 套件定位与裁决标准 | "A" | 健康契约负向验收: 每情景裁决 = 15 接口语义正确 + 受管进程全程 healthy + run.log 零 unhandled/Traceback + 取证完整 |
| D2 (Q2) | "真实流量"的形态与拦截路径 | "A. 你刚测试用的 Win10VM MCP? 不是让你用 Win10VM-FakenetNG MCP 吗?" | 混合画像, 外部出站回源会话为每情景必含主干。附注: 用户同时质询 MCP 通道使用; 作者核实所调用的即 Win10VM-FakenetNG (http://192.168.204.233:28787/mcp), "Session not found" 为该服务自身会话层报错, 非通道用错 |
| D3 (Q3, 被指令取代) | 与主闭环 (20 CHK 整改闭环) 的关系和执行时点 | "大幅缩减主闭环的内容, 改为 fault-5/normal-5, 在之后执行情景套件." | 主闭环缩量为 normal-5 / fault-5 冒烟, 先于情景套件执行 (用户指令取代该问原选项) |
| D4 (Q4) | 缩量如何形式化以免 H8 保真对账失败 | "A." | 立 PLAN-CHANGE-008 (沿用 PLAN-CHANGE-007 流程: 变更记录→审核→闭环登记), 原正常/故障矩阵覆盖义务转移给情景套件, 六数保真按变更后基线复算 |
| D5 (Q5) | 100 个情景的生成与编排方式 | "A" | 参数化模板 × 固定种子受控交错 ({配置档}×{生命周期链}×{流量画像}×{交错模式}); "每情景 ≥10 接口、每接口 ≥5 情景"由构造保证; 每情景落盘种子与调用序列, 可复现可重放 |
| D6 (Q6) | 故障注入是否混入情景套件 | "CQ6 改为 A" | 小子集混入 15/100 = 5 个 CHK 故障类 × 3 种流量画像, 注入发生在流量进行中, 复合裁决; 其余 85 个纯良性单一裁决 (用户先答 C, 随即自行更正为 A, 以 A 为准) |
| D7 (Q7, 两轮演化) | 配置档轴的精确定义与分布 | 第一轮反问: "default 主干是不是不包括 "流量接管"? 例如放行 deepseek?" 第二轮转向: "default 没什么可测的, 在 fakenet-GUI 界面的第 2 个 Tab 上添加的功能才是要测试的功能, 你先评估一下" | default 全接管降为对照 ≤15; 测试主体转为 GUI 第 2 个 Tab「出站策略」承载的功能 (出站策略引擎+配置层); 共识轴分布: egress 家族 ≥70 (deepseek 放行档 ≥25、私网接管档 ≥15、进程重定向档 ≥15、egress_control 引导档 ≥15), 余量归定制变体——该分布随设计共识摘要被 D10 确认生效 |
| D8 (Q8) | 「公网IPv4放行」正向路径的网络前提 | "A" | 用户授权: 给 .233 验收 VM 一次性添加 NAT 适配器并重打快照, 正向中继真连 api.deepseek.com (TLS 真实链为证据标准); 管理通道保持 host-only; 明确否决无外网降级方案 (B) 与旁路方案 (C) |
| D9 (Q9) | GUI 部件层 (Tab 2 自动装备/拆除 UX) 是否纳入套件 | "B" | 完全无头, 0 个 GUI 情景; GUI 层依赖既有 Linux 单测与 v34 包自身验收, 声明为覆盖边界 |
| D10 (共识确认与节拍) | 设计共识摘要 (Q1-Q9 汇总) | "确认. 但执行过程要听我命令. 现在只做方案写入." | 共识摘要全部生效; 执行节拍受用户逐阶段命令控制; 当日仅执行方案写入阶段 (该消息同时附一般功能方案写入契约全文与"会话角色门禁"要求) |

## 3. 事件 (E1, 非决策)

- E1: 用户转达首次全面第三方审核结果: "审计被阻断, 未形成有效结论。首次全面审核在来源门禁停止: S14 将核心 Q1–Q9 决策及 NAT 授权定位到作者'本对话', 未找到可独立读取的持久化来源。已恢复四项初始数量及真实流量要求, 但不足以证明其余范围、授权及验收决定。" 本记录即针对该阻断的闭合动作。

## 4. 用户确认区

- 状态: **已确认 (2026-09-12)**。
- 确认事实: 2026-09-12 用户在本会话中对本记录全文逐条核对后回复 "确认" (逐字), 未提出任何更正。
- 登记人: 作者/执行方 (按本节登记规则代为登记; 确认原文与 D1-D10 同源于 2026-09-12 会话)。
- 效力: 自本登记起, 本记录 D1-D10 作为关联方案 S14 的需求与授权依据生效; AUD-001 的"可稳定定位、可独立读取载体"条件以本记录 (含本确认登记) 满足。
- 边界: 用户明示不采用在 PLAN/FaknetNG.md 追加确认的方式; 若审核方仍认定需要更强的用户侧持久化证据, 按其指出回问用户。
