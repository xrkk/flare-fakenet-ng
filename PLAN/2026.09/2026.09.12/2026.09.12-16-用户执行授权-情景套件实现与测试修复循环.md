# 情景套件实现与测试修复循环授权登记

- 会话角色: 作者/执行方
- 日期: 2026-09-12
- 最新用户原文: “解除子智能体测试场景的限制, 派发子智能体测试后修复发现的错误, 然后再次派发, 至所有场景无错通过”。这是持续目标，不以单轮试验或局部通过代替。
- 上轮用户约束继续适用: 实际100场景交由 `gpt-5.6-terra`、`xhigh` 子智能体执行；主会话准备、修复、构建、协调、核对证据。
- 本轮生效解释: 解除此前对子智能体的只读、不得实现及不得启动测试的阶段限制；允许为真实失败修复工具及必要产品代码、构建/本地提交/部署新候选、重派测试，直至完整场景无错通过。已明确授权的 .233 / DESKTOP-3FI41GR / MAC 00:0C:29:C1:CA:49 服务切换及host-only传输继续适用。
- 不放宽结果语义: 100情景=85良性+15故障、每情景至少10个distinct接口、15接口各覆盖至少5个distinct情景、真实流量与独立取证、五节恢复和原有精确异常规则均保留。故障类、出站边界、权限/无头范围不因可执行性困难而缩减。候选更换后完整通过证据须重新绑定同一最终候选，不混用历史成功轮。
- 与既有方案关系: 01 v0.5的历史 `审核未通过, 不得实施` 原样保留，不伪称第三方已批准。本记录直接登记新的用户执行指令，作为本轮实施/测试权限的上位依据；它不证明AUD、CHK或独立核验已关闭。既有03—15记录不修改。
- 调度: `/root/scenario100`（terra xhigh）实现套件并负责实际100场景；`/root/fault_spike`（terra xhigh）先完成新候选五故障证据回归。两者串行交接.233，避免并发改变run/捕获状态；这是资源互斥，不再是测试禁令。
- 当前循环输入: 15记录2/5故障证据case通过；diverter实际作用观测、child时序、policy独立包关联为明确失败输入。当前产品快照 `082ea3f` 增加仅fault模式、可选、10秒有界的启动就绪同步和真实GetHandleInformation关闭前后数据；处于构建/真实验证阶段，不预先宣称修复通过。
- 证据: 新尝试在 `Logs/fakenetng-mcp/scenario-suite-20260912/` 独立目录；旧失败证据不可覆盖。每轮记录失败、修复源码与候选身份、派发对象、原始测试结果及恢复状态。受保护 `PLAN/FaknetNG.md` 不修改、不暂存、不提交；不push，不操作无关VM。
