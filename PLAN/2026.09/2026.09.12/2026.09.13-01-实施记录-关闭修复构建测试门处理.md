# 关闭修复构建测试门处理

会话角色: 作者/执行方

延续记录 25，依据记录 16 授权进行修复重试，不修改方案或审核状态。

ab2727c 构建 session 87829 已退出 1，Windows 主测试 3 failed、1056 passed、8 skipped。原始报告保留在 Logs/fakenetng-mcp/builds/ab2727cd-q_2vzpa0/build-validation/windows-pytest-main.xml。三项失败分别是 test_active_probe_fault、test_p04_units 的旧替身缺少 _close_windivert_handle，以及 test_sst_fault_evidence 的源码指纹白名单未包含关闭修复后的文件。

提交 a5afee3 更新 P04 替身；提交 72bd090de464049fe4c5d15cedecbe4a6f99492e 同步 active-probe 替身并添加精确源码 SHA-256 567bcd10e579558de287ebc403bd90618048fe120a1f2d2178889462dff18f7d。policy_pause 的 sleep 仍在第 127 行；保持必须匹配源码指纹、行号及实时栈的原有条件，未改为接受任意源码。产品代码相对 ab2727c 未再改变。

相关四文件主机回归：45 passed、2 Windows 专用 skipped、6 subtests passed。socket 创建需要在沙箱外运行，该回归没有外部网络访问。前轮 Wine 的 49 项相关测试通过仍保留；最终构建测试门须单独通过。

新构建：./Build-FakeNetNgMcpPackage.sh 72bd090 dist dist/mcp-fault-closefix-20260913，session 46298，构建目录 Logs/fakenetng-mcp/builds/72bd090d-pukavtqx。截止记录创建仍在执行。

r5 本地重建：listener 有 L1 与四条连接各 14 个精确匹配的独立报文；child 有原生父子身份但对应独立报文缺失，不能计通过；diverter 明确失败。新候选须优先复验 diverter，不能以 receipt 替代原生动作证据。

VM 当前由 terra_scenario100 进行只读预检；terra_fault_spike 已明确保持 VM-idle，等待构建通过和串行交接。实际 100 场景尚未开始，仍由指定 gpt-5.6-terra（xhigh）子智能体执行。
