# 故障报文证据精确连接判定实施记录

会话角色: 作者/执行方

依据记录 16 的测试、修复、再次派发授权继续执行。本文为实施记录，不是第三方审核，不关闭 AUD 或 CHK，不修改方案状态。

## 修复与验证

离线判定器原先只验证源 IP、端口字符串出现在报文引用中，无法排除另一目标、端口前缀碰撞或多条报文拼接。这不满足同一目标出站连接的证据关联契约。

独立本地提交 `33609ee3550a42564f99bbff5ff8193d08d590fd` 将 `test/mcp/acceptance/sst_fault_evidence.py` 收紧为单条解码 TCP 报文的完整双端 tuple 精确匹配，允许正反两个方向。回归先运行原谓词，6 个反例均暴露误接受；修复后 26 项测试和 6 个子测试通过。封存的旧 listener_stop、cleanup_error 原始样本离线重放仍通过；旧证据未修改。证据与指纹见 `Logs/fakenetng-mcp/scenario-suite-20260912/iteration-20260912-01/packet-tuple-regression/regression.json`。

## 执行状态

产品候选仍为 `mcp-c082ea3fb-d958768dc8fa`；本提交只改变主机离线验收工具。实际 100 场景尚未执行，不把离线回归或旧样本算作其中场景。

`terra_fault_spike` 持有 VM 串行执行权，正在修正并解析 r3 测试脚本，尚未启动本轮真实故障运行；`terra_scenario100` 同时完成每 run 原件导出、故障 descriptor 实际判定、良性完整日志及报文契约、恢复闭环。两者均按记录 19 的独立 `gpt-5.6-terra` / `xhigh` 调度。VM 故障预检完成后交接给后者执行 P1–P7、缩减 smoke 与正式 100 场景；主会话不代跑实际 100 场景。

目标仍在执行中，当前不宣称全部通过。
