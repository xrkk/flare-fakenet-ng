# 报文采集判定与 r5 观察器修复记录

会话角色: 作者/执行方

依据记录 16 授权继续测试与修复。本文为实施记录，不形成第三方结论，不改变方案审核状态。

## 报文证据适配

提交 `1db93b3` 新增 `test/mcp/acceptance/scenario_pktmon.py` 及对应测试：保留原始字节范围，按完整方向性四元组和协议选择报文；用于外泄判定时还要求当次采集的 NIC component/direction 绑定，未知元数据或截断不能视为没有外泄。10 项本地测试通过，仅证明解析与拒绝规则，不替代真实采集完整性。

r4 只读原件 `Logs/fakenetng-mcp/scenario-suite-20260912/spike-evidence-contract/attempt-gated-c082ea3fb-r4-20260912/pktmon-nic-readonly.json` 给出 component 9、Intel(R) 82574L、MAC 00-0C-29-C1-CA-49 与活动 Ethernet0 的对应关系。此数字不是未来采集常量；套件必须逐次核对组件、适配器与采集计数。

## r5 故障事实与观察器错误

产品候选保持 `mcp-c082ea3fb-d958768dc8fa`，未作产品修改。r5 传输原件中的响应以 SSE `: ping` 心跳开头，旧通道解析误当 JSON，掩盖了真实 PowerShell 报错：产品消费 ready 文件后，观察器再 Get-Item 读取已删除文件。故障触发事实须由各 run 的 receipt、IPC、run.log、探针和 pktmon 原件独立重建，不能因观察器失败直接推断产品没有触发。

执行子智能体已在停止前保存 child_hang 原生进程快照：run `9d0db198-e7d5-4105-868a-10a96f5b398b`，managed PID 8336，子进程 PID 6272/ParentProcessId 8336，命令含 managed-fault-hang。一次 stop 返回 stopped/state_version 8、last_run_outcome failed。截止本记录，子智能体仍在导出与恢复；这些事实不等于完整 Spike 通过或环境恢复通过。

## 已准备的最小工具修复

证据目录：`Logs/fakenetng-mcp/scenario-suite-20260912/iteration-20260912-01/r6-runner-preparation/`。

- channel.py SHA-256 `86df49f11e71becbf4ea75e0888aba7f6a826343e40ac35fa0b235498bf58751`：解析 SSE 心跳和多行数据，绑定 RPC id，拒绝重复、缺失或畸形响应。
- run_spike.py SHA-256 `f0a0c65393c79c6f229ac816aac09d37ac78551b2faceced59487524883d623c`：同目录临时文件完整写入后 Move 原子发布 ready；移除发布后的 Get-Item，并保留既有观察预算及字段匹配修复。
- regression-result.json：11 项主机回归通过，含真实 r5 响应解码；明确 synthetic=true、vm_calls=0。真实响应 SHA-256 `e810f5cc3a416459501af36e6f6f6230b26c4172d93f4bd9191147e63942ec54`。

修复已交执行子智能体；必须先完成当前原件导出与恢复，尽量重建 r5 有效证据，再决定最小重跑。实际 100 场景尚未开始，仍须由 gpt-5.6-terra（xhigh）子智能体执行；本地回归和 Spike 不计入 100 场景结果。
