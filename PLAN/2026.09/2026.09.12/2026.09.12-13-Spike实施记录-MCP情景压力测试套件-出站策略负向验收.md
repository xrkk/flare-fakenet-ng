# MCP情景压力测试套件——证据裁决Spike实施记录

- 会话角色: 作者/执行方（用户已明确转换；本会话不得再执行第三方审核）。
- 日期: 2026-09-12。
- 授权: 用户“执行 spike (需要 VM 的话, 知道用哪个对吧?)”。仅执行本次Spike，不扩展产品修改/候选切换/套件实施。
- 方案v0.5 blob: `53b424bfbcdd329a2d572523ed91c5606e4c2633`；方案及11/12原记录未改。
- 输入审计: 11 blob `416246d7eb0339357e258e828d16efcd9b6c1438`；作者处理12 blob `679134ed378dd9cae0ec8f3692ff2fdb319ff409`。
- 仓库参考commit/tree: `aee128b25c99fcc1df188fcbeb833cff3446c66a` / `a118257c497f04262e5dcff8fe38ebdbaebadcfe`。
- 结果: **Spike前置门未通过（blocked）**。已经执行工具验证和真实VM尝试并收尾；不把运行完毕等同于五类通过。
- 证据目录: `Logs/fakenetng-mcp/scenario-suite-20260912/spike-evidence-contract/attempt-20260912-2040`；主报告README.md；机器summary.json；manifest包含130份本地文件，逐份大小/SHA复算通过。
- manifest blob: `8e4976abf091d1e275a1dfbfa9690b813041ea24`；SHA-256 `547ed37ae239d49ce28f80878fe41524b4237b357180193b1ebebe69f36b430b`。

## 执行与发现

固定.233/ DESKTOP-3FI41GR，MAC00:0C:29:C1:CA:49，候选mcp-c6090f816。listener_stop/diverter_stop/child_hang三轮均捕获原始证据；policy_pause在arm前启动失败，cleanup_error因同一前置失败未执行，均不计故障覆盖。

listener_stop：原始动作区间被同run目标VM出站会话完整覆盖，异常L1匹配、五节恢复一致；但同次启动还有DomainEgressRelay假阴性，指定注入唯一归因不成立。其余两轮亦未取得整体通过。独立无故障无探针B1对照仍失败，default恢复轮healthy及正常停止。

根因定位到固定候选managed.probe_instance未适配DomainEgressRelay的`_listener/_accept_thread`，实际读取`server/sock/socket/server_thread`而报告handles=[]/alive=false。候选commit与本地两文件blob一致，且IPC原始事实、无故障控制相互支持。未改产品绕过健康门，未换候选。

## 交付、验证和残留

新增宿主工具 `test/mcp/acceptance/sst_fault_evidence.py` blob `77e871da3d244a94988f70173e06ad48747227f2`；测试 `test/mcp/test_sst_fault_evidence.py` blob `b4ebffe15676781e70f8f3257f632dda43fdf13a`。12项离线检查通过、5项真实证据派生反例按预期拒绝。真实三份case命令退出码均3，具体check逐项留存；不支持的原生观测不假造成功。代码快照随manifest保存，未宣称这是完整套件实现。

原配置字节/环境恢复、故障开关关闭、宽限60s、无未消费故障、无受管/探针残留、pktmon停止、服务Running且MCP stopped，均有本轮原始核对。没有执行VM快照/NAT改动，没有遗留宿主传输服务。ETL留存VM并登记已取得的哈希；原配置备份只在VM本地。

AUD-005/011/012不由作者关闭；其余八条AUD状态不变。本结果说明零产品改动/同候选下存在真实前置障碍，应先处理固定候选的健康探测兼容性及候选边界，再决定后续工作；本记录不自行授权修复、不发起第三方审核请求。方案仍`审核未通过, 不得实施`。后续审核必须另开角色隔离会话。
