# 探针身份修复与 r4 脚本反例

会话角色: 作者/执行方

按记录 16 授权继续实现和测试；不修改方案审核状态，不计最终验收通过。

## 主机证据判定修复

提交 `8a9bc71` 按实际 established 事件的 pid/nonce 匹配此前 ready/process_ready 中唯一 creation_ticks。B3 原生 socket 子进程可以区别于 PowerShell 启动器；禁止借用启动器身份、后补 ready、混合不同创建时间。21 项 oracle 测试及 6 子测试通过；旧封存 listener_stop/cleanup_error 原件离线重放仍通过。

完整套件子智能体同步修正双向 FIN/RST 选择、B3 原始时钟和身份、脚本根目录、TLS 实际 cadence 发送，以及启动前有界重试。主会话定点复核再指出 `_run_gate` 使用只读 PowerShell `$PID` 赋值的问题，已交回修正并要求检查实际生成命令。

## r4 真实进程与本地反例

r4 宿主 session 67166 实际运行，B1 正常控制通过；listener_stop 因测试观察器失败，不能计入故障通过。子智能体确认原 start 已结束、MCP stopped、产品记录 gate TimeoutError，当前执行器已进入下一故障轮。

主会话不访问 VM 的 dry invocation 捕获 r4 实际生成 PowerShell 命令，证明：内部 observer 为 90 秒，外层工具 timeout 仍为 17 秒；raw rf 字符串生成双反斜杠的 sport 正则，不能匹配真实流量行的端口后空格。反例、实际生成值、源 SHA-256 见 `Logs/fakenetng-mcp/scenario-suite-20260912/iteration-20260912-01/r4-emitted-observer-counterexample.json`。该反例明确标为 synthetic、vm_calls=0，不替代 VM 证据。

须等待现有操作结束、导出原件并恢复，再在新 r5 目录修正外层时间预算与单层正则。新检查必须覆盖实际生成命令和传入工具的 timeout，不能只检查常量或 PowerShell ParseInput。

正式 100 场景尚未开始。产品候选保持不变，目标继续执行。
