# 新候选部署与五类故障 Spike 复跑

- 会话角色: 作者/执行方
- 创建日期: 2026-09-12，Asia/Shanghai
- 用户授权: “允许. 但是实际的100场景测试你需要创建子智能体(用模型 gpt-5.6-terra(xhigh)) 来做, 不是在主会话里”。承接记录14的具体目标、传包与受控替换服务确认，自动审批本轮放行。
- 目标: `192.168.204.233 / DESKTOP-3FI41GR / MAC 00:0C:29:C1:CA:49`，本轮未快照重置、未变更NAT或其他VM。
- 方案: 同目录01，v0.5，blob `53b424bfbcdd329a2d572523ed91c5606e4c2633`，仍 `审核未通过, 不得实施`，本轮未修改。
- 前序14记录 blob: `b3638669c6d9909b4f5453a96a4f3fe078d7400d`。
- 结论: 授权的部署与五类真实Spike完成采集、恢复和离线判定；证据case 2/5通过，整体Spike未通过。实际100场景未开始。本记录不构成第三方审核或AUD闭合。

## 1. 候选与部署

新候选 `mcp-c8e4d664b-a458787e6e92`，源commit `8e4d664beb236f33cc5c250925381c7e14f539bd`，tree `9017fac2506dc5a5f7b9e728c150886ea1be4440`。ZIP `dist/mcp-relay-health-20260912/fakenetng-mcp-candidate.zip`，SHA-256 `a458787e6e92f6126b213b19d81bbccac6a5d93a756eb583d26ee9464d99acba`。

21:43:20(+08) VM返回部署完成：195成员核验通过，配置哈希不变，新exe SHA-256 `8538d631f5fc6ab6f15d63d9d5718643fce7ca88bedae672e7eaa972d7b99fdd`。旧目录保留于 `C:\Program Files\FakeNet-NG-MCP-backup-c6090f816-20260912`。临时服务只绑定192.168.204.1，传输后已关闭。新候选用于用户另行授权的局部修复验证，不静默替换仍未审核方案的固定候选锚点。

## 2. 实测与离线结论

证据根 `Logs/fakenetng-mcp/scenario-suite-20260912/spike-evidence-contract/attempt-relay-8e4d664-20260912/`（下表路径均相对此目录）。

B1无故障对照run `7ea588bc-ab65-4c2a-9f37-3c57e127d8b7` healthy、3次健康采样通过、stop ok。DomainEgressRelay修复实机有效，见 `control-b1-before.json`。各故障均有匹配nonce原收据、独立run、原probe、IPC、完整run.log及pktmon文本。

| 故障 | run_id | 退出码 | 结果与证据 |
|---|---|---|---|
| listener_stop | `65185e84-a637-496e-989e-cf44df7b311a` | 0 | 9项通过；唯一死UDP listener、relay正常、L1精确异常、目标出站单会话包含保守触发区间、五节恢复。`listener_stop-case.json` / `listener_stop-result.json` |
| diverter_stop | `dead4b33-5a5a-4825-9738-ad67376fcfcd` | 3 | 缺主句柄实际关闭的独立观测，无法闭合该启动失败归因；目标会话未包含整个保守区间。`diverter_stop-result.json` |
| child_hang | `04c2a2ac-93ac-4908-9915-2f0056a9aa58` | 3 | healthy分支和原生子进程创建/父子身份通过；四个目标连接均建立晚于触发下界，重叠失败。`child_hang-processes-native.json` / `child_hang-result.json` |
| policy_pause | `8acf1a60-e47c-41e2-918a-d4b0f683e604` | 3 | healthy和暂停钩子live栈通过；四条目标连接在本轮NIC pktmon中均无对应包，不能证明独立包关联。`policy_pause-result.json` |
| cleanup_error | `35eeb3af-9beb-4b05-b804-a6785e332d2d` | 0 | 9项通过；C1完整异常与stop IPC对应、单会话包含异常作用区间、恢复通过。`cleanup_error-case.json` / `cleanup_error-result.json` |

通过的两个case是单轮可达性与本离线裁决范围结论，不代表15故障轮、持续探活、全部incident/dump质量、完整ACC或100情景通过。其余失败不通过任意缩窄时间界限、拼接连接、补造动作或把日志当作NIC包来修补。

`policy_pause` 的宿主180秒HTTP等待先超时；源码 `supervisor.stop` 默认预算为stop_grace+360秒，本轮365秒。原HTTP失败保留在 `run-launch.log`；`continue_spike.py` 未发第二次stop，仅对账原run至自行stopped，再导出证据与执行cleanup_error。后者等待420秒。该超时属于本轮宿主脚本等待契约问题，不把它删除，也不单独误判为产品恢复失败。见 `continue-launch.log` / `events-after-policy-reconcile.json`。

## 3. 解析工具与反例

未跟踪的离线工具 `test/mcp/acceptance/sst_fault_evidence.py` 增加独立 `--expected-candidate`，默认仍是旧候选，新case显式传新ID。policy_pause接受“原live栈文件/行127/函数”到精确源码SHA-256的映射（源码 `d22e2c7b174090e52d4cac96b2ade8ae0da57e4b9a244720d868e897fc249f60`）；缺源码映射或错误行不通过，未扩大异常白名单。

解析测试14通过，5个基于本轮原始证据的反例均按预期拒绝（synthetic标记保留），旧3case重放逐项布尔结论不变。见 `parser-tests.txt`、`counterexample-summary.json`、`old-case-replay/`。脚本和代码快照在证据目录，未再修改或提交产品代码、未push、未触碰用户 `PLAN/FaknetNG.md` 修改。

## 4. 恢复与证据封存

独立default恢复run `fc19dec9-5ac1-4e30-94e7-56a4a87cba11` 成功healthy，3次健康采样后stop ok。五个故障run的原五节与恢复审计比较均通过。21:57:30(+08)原生核对：配置字节与Environment恢复；fault_mode=false、fault文件不存在、stop_grace_seconds=60；无受管/探针进程；needs_recovery=false；pktmon停止；SCM Running、MCP stopped。

证据封存162文件、82,999,419字节，`manifest.json` 的Git blob为 `da61f850fcce575f4345f0c5ff92041f53a027b3`，所有列出文件带大小/SHA-256。旧13/14和旧Spike目录保持不变。原始ETL仍在VM本尝试目录，对应每类capture登记SHA-256；85个incident原文件路径/大小/哈希见 `incident-vm-inventory.json`。后续VM重置前必须导出需保留的VM-only ETL/dump；本轮没有重置。

## 5. 100场景的子智能体安排与当前前置项

已按用户要求创建 `/root/scenario100`，模型 `gpt-5.6-terra`，reasoning `xhigh`。仅做只读准备，未连接VM/网络、未执行实际100场景、未改文件。其确认以下缺项：

- `scenario_suite.py`、`scenario_probes.ps1`、`sst_pc008_assert.py` 尚不存在；现有P05 runner不能替代100情景及每情景10接口链、真实流量与pktmon契约。
- PLAN-CHANGE-008及方案实施门、新候选身份重锚尚未完成。
- 五故障Spike整体门未通过；P1—P7正式套件preflight未执行。

本轮用户的委派要求已落实，不将模型选择要求解释为解除方案实施门。未来实际100场景须由指定模型子智能体执行，主会话仅准备、协调和汇总。剩余证据前置为diverter主句柄作用观测、child_hang同run会话时序、policy_pause独立包关联；不能通过把本记录作者结论改名为独立审核来解除。
