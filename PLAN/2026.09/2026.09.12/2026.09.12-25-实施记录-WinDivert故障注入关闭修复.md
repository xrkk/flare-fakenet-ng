# WinDivert 故障注入关闭错误修复

会话角色: 作者/执行方

依据记录 16 授权继续测试修复。本文不修改方案审核状态，不作第三方闭合判断。

## 真实失败

r5 原件 `Logs/fakenetng-mcp/scenario-suite-20260912/spike-evidence-contract/attempt-gated-c082ea3fb-r5-20260912/vm-export/diverter_stop-run-run.log` 第 98–104 行：接收线程退出后，faultinject.py 的直接 handle.close 经 PyDivert 报 FileNotFoundError/WinError 2。run `53709694-7581-427d-a9ab-73aa47ba4421` 有 receipt，却没有 fault-action.json，故不能证明原生关闭动作通过。

r5-export-recovery.json 已记录导出与恢复通过。VM 串行使用交给 terra_scenario100 做只读格式/脚本预检；旧候选不用于最终通过。

## 最小修复与验证

提交 `ab2727c` 仅改故障注入及对应测试。inject_diverter_stop 改用既有 Diverter._close_windivert_handle，复用其 SetLastError(0) 行为；不吞关闭异常，保留原生 before/after 和时间区间证据。

Windows-Python 回归先在真实 Windows last-error 语义下保留错误码 2，调用实际故障注入入口，修复前得到相同 WinError 2（4 passed, 1 failed）。修复后执行 test/mcp/test_fault_start_gate.py 与 test/test_windows_egress_verdict.py，共 49 passed。句柄观测在新增单元回归中使用替身，因此该结果不证明实际 WinDivert 驱动关闭，仍须新候选 VM 复验。

测试环境：Docker 镜像 flare-fakenet-ng/gui-vm-diagnostic-builder:py3119-pyi6220，当前 image ID sha256:1b33b518a7c71ecd13264f0d1c41faf254e301b8702e2da8ca11376cff700a5e；UID 1000，Windows Python 3.11.9，Wine。运行命令形状：docker run --rm --network none --user 1000:1000 -v /home/adminn/projects/flare-fakenet-ng:/workspace <image> xvfb-run -a wine 'C:\Python311\python.exe' -m pytest <上述两文件> -q。

构建已启动：`./Build-FakeNetNgMcpPackage.sh ab2727c dist dist/mcp-fault-closefix-20260912`，宿主 session 87829。截止本记录尚未形成构建完成或 VM 通过结论。最终 100 场景仍须由 gpt-5.6-terra（xhigh）子智能体在最终候选下真实执行并全部通过。
