# 诊断候选部署及进程清理证据修复

会话角色: 作者/执行方

承接记录06，持续授权不变；不修改方案审核状态，不自作第三方结论。

构建session34645终态0。新候选mcp-c7cd934db-b657a031d1a0，源码7cd934db4a7dc825f26d70e074a34a1910e10bc3。dist/mcp-exit-admission-diagnostic-20260913/fakenetng-mcp-candidate.zip SHA256 b657a031d1a07f30c116f6971f8f2f897010be2f6e937844049e5d3f61cf1819，主会话独立核对一致。candidate-verification.json为PASS，195文件、manifest_match/size_hash_match均true。XML主组1095总/8跳过/0失败，HTTP17/0失败，即1104通过8跳过。裸镜像Wine缺win32file原因已明确：正式offline SDK步骤安装固定pywin32==312；在一次性容器同样安装后，相关2项回归通过，未改依赖/镜像。

terra_scenario100已部署新候选。证据 Logs/fakenetng-mcp/scenario-suite-20260912/mcp-c7cd934db-b657a031d1a0-deployment-20260913/deployment.json。195成员与ZIP/EXE核验通过，EXE SHA ca7f77416903ff9bc7d8ae789f8dd0b037bcdcfc884de51b9459d040244a5887，config_unchanged=true。旧c72包保留C:/Program Files/FakeNet-NG-MCP-backup-c72bd090d-20260913-exit-admission；host-only传输服务停止。

执行器提交33d5725修复清理证据：ready记录wrapper PID+原生StartTime UTC ticks，B3/curl同样从本run原始JSONL获取身份。原CIM微秒精度不能直接与100ns原生时间比较：实际query PID4432两值相差7ticks，证据同Logs前缀mcp-c72bd090d-54b9dfaf3db7-query-tick-precision-20260913.json，SHA 2c9b652bf828087c3ab2f6d786a82e7977411991b26704410115375b6a969004。

现在CIM用于命令行/进程名/父PID，Get-Process.StartTime用于原生精度，第二次CIM确认身份未变。本次query必须精确对应自身PID+原生创建时间；SCM对应正常宿主放行；精确活probe及相关未知残留拒绝；不同创建时间的无关PID复用只记录；范围内身份读取/快照竞态进入拒绝列表，无关系统进程读取失败不阻断。未改operation_unsettled禁止并发mutation的边界。

相关离线组53通过/6 subtests。实际无网络短寿命probe验证：活PID3600/原生ticks639248304141336272被判recorded_probe_pid_and_creation，query和SCM宿主不误判，finally按身份停止。v2证据SHA 040c079609869534fc1cec8d10520a3d88ba0e9844f57e85a52922f1cecbd25a，后续含相关身份race边界的v3也通过。首次临时harness因$PID变量碰撞留下PID4136，已按精确marker清理并留原始恢复证据，未覆盖失败。以上是执行器回归，不算100场景。

当前指定gpt-5.6-terra(xhigh)子智能体已生成新候选100 manifest，开始P1–P7预检。随后P05正常5/故障5、final/summary，再执行85+15正式场景。产品错误87仍待实机新stage/API/time验证；不以诊断字段或构建成功声称已修复。
