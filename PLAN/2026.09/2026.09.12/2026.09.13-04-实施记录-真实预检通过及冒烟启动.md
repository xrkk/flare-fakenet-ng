# 真实预检通过及冒烟启动

会话角色: 作者/执行方

依据记录16持续执行授权；不修改方案审核状态，不生成第三方结论。

执行器修复提交：e7d6af4（host-only单文件探针传输与辅助边界case释放）；d741b73（每个suite根独立命令ID命名空间、P6使用[string]读取IPAddress）。r2失败P6错误方法；r3失败P5-empty-cycle控制器命令ID冲突；各失败根均保留，没有覆盖。相关测试52 passed、6 subtests passed。

证据裁决器提交5f9e5ed修正过强的diverter启动归因条件。依据方案§4.1和windows._watch_diverter_thread既有行为，主句柄关闭后策略relay可能已停止；现在仅在同句柄原生关闭、同run精确receiver-exit日志、仅DomainEgressRelay/provider同名且handles=[-1]、其余listener均完好时允许该直接后果。缺日志或其它listener失败仍拒绝，不修改异常白名单。红绿回归保留该边界；本次不是AUD/CHK自行闭合。

r6四连接原始裁决见 spike-evidence-contract/attempt-gated-c72bd090d-r6-20260913/r6-diverter-connection-oracle-assessment.json。动作/归因/日志通过，但四连接保守overlap均失败，且缺完整逐轮恢复证据，故r6不是正式fault通过。其余四类未在c72的Spike运行，不混用旧候选证据。

真实预检根：Logs/fakenetng-mcp/scenario-suite-20260912/mcp-c72bd090d-54b9dfaf3db7-full-r4-20260913。preflight.json passed=true，P1–P7全部通过。P5真实传输两端哈希一致、服务已关闭，正常空载启停通过；P6 Test-NetConnection记录目标198.51.100.77、tcp=false，不作为DeepSeek可达证明；P7真实curl经relay请求api.deepseek.com，退出0、HTTP401，随后stopped且last_run_outcome=ok。P7 run bd2d8e25-eff9-47fd-a88e-b2bf19bafd8a。

产品候选保持mcp-c72bd090d-54b9dfaf3db7。terra_scenario100已按授权开始P05正常冒烟builtin3+custom2，随后故障5与final/summary，再执行85+15真实场景。截止本记录，正式100场景尚未开始，预检通过不能抵充场景数量。
