# 退出 ACK 互斥修复与新候选交付

会话角色: 作者/执行方

- 日期：2026.09.13；承接记录 12，仍在既有测试失败修复授权内。方案状态不变，不作审核或总体验收。
- 源提交：`3cdec36784ed67cfd42149147e88cbb35cba3e50`，仅 exit_retention.py 与对应测试。
- 候选：`mcp-c3cdec367-188ff94c4ce2`。

## 1. 修复与证据

记录 12 的实际反例为 SPE helper 已持有 SingleFlight、发布 entry，宿主备用 dump 随后占据协议锁直至 helper 的 60 秒截止以后，最终 ACK 迟到且 result 缺失。

现在备用转储在占用协议锁和启动 writer 前先取得现有 SingleFlight；普通路径遇到当前 run 的有效 entry 会让给 helper。等待不持协议锁，在绝对 budget 内完成；强制路径保留等待、超时及不完整结果语义。真正转储前再核 retained target 的 PID/创建时间和存活性，转储后释放 SingleFlight 才调用会自行取同一互斥的诊断验证子进程。既有 owner_dump/target.dmp 早退不会泄漏协议锁。截止路径等待期间 helper 已完成时，读取并验证其结果后收尾，不能单凭自报通过。

同时修复 cleanup 再失败丢失主失败阶段/owner_observation 的问题，保留原失败、追加 cleanup_error，不降低 complete 或句柄责任门槛。

新增/加强测试覆盖 ACK 让行、独占 writer、验证前释放互斥、既有证据不覆盖、强制 busy deadline 与二次清理错误仍保留主错误。相关 Linux 25 项通过；两条新反例在修复前分别触发字段缺失和 ACK 协议锁被占用，修复后通过。子智能体只读检查了实际 diff，没有修改代码。

## 2. 构建与独立包校验

Docker image `flare-fakenet-ng/gui-vm-diagnostic-builder:py3119-pyi6220` 本次实际 ID `sha256:1b33b518a7c71ecd13264f0d1c41faf254e301b8702e2da8ca11376cff700a5e`。

实际命令：`./Build-FakeNetNgMcpPackage.sh 3cdec36 dist dist/mcp-exit-ack-flight-20260913`。session 47141 已返回 exit 0。构建诊断位于 `Logs/fakenetng-mcp/builds/3cdec367-m59f6jdo/`。

Windows-Python main XML 1108 项、8 skipped、0 failures/errors，HTTP XML 17 项、0 failures/errors，合计 1117 passed / 8 skipped。程序构建及候选验证 PASS。

- ZIP：`dist/mcp-exit-ack-flight-20260913/fakenetng-mcp-candidate.zip`
- SHA-256：`188ff94c4ce2dd0204c7b6d7471d094e05a0bd6310df562fd7947923f306459b`
- 同目录 manifest/verification；根侧重新读取 ZIP 并逐项核验全部 195 成员的大小和 SHA-256，一致。

## 3. 真实环境交接

terra_scenario100（指定 gpt-5.6-terra xhigh）继续独占 VM。已交包及上述 SHA，要求正常受控服务停止、保留旧程序目录、部署核验；不再次重建 state/baselines，不改 VM/NIC，不并行启动 runner。

前轮补核：旧 c208 marker/baseline 和原位 registration/default 配置 SHA 均一致，证据为前轮 full 根 `preserved-c208-readonly.json`。前轮 P05 根 `post-fault-readonly.json` 确认 fault 环境标志移除、故障文件不存在、stop_grace=60、产品 stopped/run/controller null。共享 service.log 导出只按其读取时间窗使用，不声称原子快照。

新候选必须从新的证据根重跑 P1–P7、正常五轮、故障五轮、final/summary，随后实际 100 场景。本文冻结时新候选尚无真实 VM 通过结果，实际 100 场景执行数仍为 0；Windows 测试通过不替代退出 ACK 的真实故障验证。失败仍按原始证据定位、修复、构建与重新派发。
