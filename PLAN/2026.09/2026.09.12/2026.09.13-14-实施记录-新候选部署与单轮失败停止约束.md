# 新候选部署与单轮失败停止约束

会话角色: 作者/执行方

日期：2026.09.13。承接实施记录 13；不改变方案审核状态。

用户新增明确约束：「如果下一轮验收还不过, 停下来」。随后「resume」仅恢复当前候选验收，不撤销该约束。已向唯一 VM 执行者 terra_scenario100（gpt-5.6-terra xhigh）同步：当前候选任何验收门或场景失败后，保留失败证据、完成必要环境收尾即停止；不继续修复、重建、重跑或后续场景，等待用户新的明确指令。此前不限次修复授权在此处受新约束限制。

当前候选 `mcp-c3cdec367-188ff94c4ce2`、源码 `3cdec36784ed67cfd42149147e88cbb35cba3e50` 已部署。ZIP SHA-256 `188ff94c4ce2dd0204c7b6d7471d094e05a0bd6310df562fd7947923f306459b`，195 成员验证通过；旧 c51 程序目录 217 文件移动前后内容树与 ACL 一致。service.json 与 exit-evidence/registration.json 前后 SHA 一致。受控停止后新服务 MCP stopped/version 1、run/controller null；host-only 传输监听已停止。

证据：`Logs/fakenetng-mcp/scenario-suite-20260912/mcp-c3cdec367-188ff94c4ce2-deployment-20260913/deployment-terminal.json`，SHA-256 `37a5d71165ba2e871583961da2370628a8ac7b71e362e5585c8808a0918fa7bd`。根会话已读取核对候选、旧程序备份映射、配置/注册 hash 与新端点状态。

本记录创建时部署已终态 exit 0，无部署失败。后续由原 terra 执行 P1–P7、P05、实际 100 场景，每阶段仅在前阶段通过时继续。本记录不认定新候选验收通过；实际 100 场景尚未开始。
